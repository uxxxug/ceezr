/**
 * الغرض: `SS-04` — «مدينةٌ غيرُ مدعومة» (القسم 9.7). شاشةٌ **مكتوبةٌ وغيرُ
 *   موصولةٍ عن قصدٍ** بقرارِ مالكِ المنتجِ في `F1-07`.
 * الحالة: منفّذ عرضاً فقط — **غيرُ موصولٍ بأيِّ مصدرِ بيانات** — البند `F1-07`.
 * ينتمي إلى: apps/miniapp/src/system (حزمة `shell` — القسم 9.4)
 * يُتوقع أن يستخدمه لاحقاً: الشاشةُ التي تعرف مدينةَ المستخدمِ — ولا شاشةَ كذلك
 *   اليوم.
 *
 * ═══ حدٌّ معلَنٌ: لماذا لا يستوردها أحد ═══
 * القسم 10 يُدرِج `GET /v1/cities`، و**المسارُ غيرُ منفَّذٍ في البوابة**: لا مصدرَ
 * حقيقةٍ يقول «هذه المدينةُ مدعومةٌ أو لا». ولا يقول `GET /v1/me` مدينةً أيضاً
 * (`role` و`status` فقط بقرارِ `F1-05`). فلو وُصِلت هذه الشاشةُ اليومَ لوجب أن
 * يُخترَع للمدينةِ مصدرٌ في العميلِ — وذاك **حالةُ عملٍ في التطبيقِ المصغَّرِ**
 * يمنعها ADR 0035 منعاً صريحاً.
 *
 * ولا زرَّ «أبلغني عند الافتتاح» فيها للسببِ نفسِه: زرٌّ كهذا يُنشئ اشتراكاً —
 * كياناً جديداً يُكتَب في مكانٍ ما — ولا كيانَ كهذا في العقدِ ولا في القاعدة.
 * وزرٌّ لا يكتب شيئاً أسوأُ من لا زرٍّ: وعدٌ لا يُنفَّذ (`UX-8`: الصدقُ في الحالة).
 *
 * فما تُثبِته هذه الشاشةُ اليومَ: أنّ النصَّ والتصميمَ جاهزان، وأنّ **وصلَها
 * قرارٌ منتظرٌ لتنفيذِ `GET /v1/cities`** لا عملٌ منسيّ. وهي نقطةٌ مفتوحةٌ معلَنةٌ
 * في دليلِ `F1-07`، ويغطّيها اختبارُها وحدَه — والاختبارُ **لا يُعَدّ وصلاً**.
 */

import { SystemScreen } from "./SystemScreen.tsx";

export function UnsupportedCityScreen() {
  return <SystemScreen state={{ kind: "unsupported_city" }} />;
}
