node_modules/
dist/
.env
.env.local
*.log
.DS_Store
bun.lockb

# حالة تشغيل عامل الوكيل: ملفّاتٌ يكتبها التشغيل نفسه، لا مصدرٌ يُراجَع.
# الملفّ المؤقّت `keywords.json.tmp` كان متعقَّباً فعلياً — أي أنّ كلّ تشغيلٍ
# محلّيّ كان يُنتج تغييراً في Git لا معنى له، ويُغرق المراجعة بضجيجٍ يخفي التغيير الحقيقي.
runtime_agent/memory/*.tmp
