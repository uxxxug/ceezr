name: CI

# `feat/**` مُضافة عن قصد: سياسةُ العمل تُلزم بالتحقّق من نجاح CI **بعد كل دفعة**
# لا آخرَ العمل، وكان الزنادُ على `main` وحدها فكانت الدفعاتُ على فرع العمل تمرّ
# بلا فحصٍ بعيدٍ إطلاقاً — فيتراكم عملٌ يُظنّ محقَّقاً ثم يُكتشف إخفاقه كلّه دفعةً
# واحدة عند فتح طلب الدمج.
#
# `workflow_dispatch` مُضافٌ لسببٍ واحدٍ مُقيسٍ (D-09 · ADR 0142): سيرُ مزامنةِ
# `bun.lock` على فروعِ Dependabot يدفعُ القفلَ بـ`GITHUB_TOKEN`، ودفعةُ ذلكَ الرمزِ
# **لا تُشعِلُ `push` ولا `pull_request`** بسياسةِ المنصّةِ لمنعِ الحلقاتِ — فلولا
# مدخلٌ يُستدعى صراحةً لبقيَ رأسُ الفرعِ الجديدُ بلا حكمِ CI، وهو ما تمنعُه القاعدةُ
# `0-7`. **ولا يُخفِّفُ هذا المدخلُ شيئاً**: الوظائفُ والخطواتُ والحواجزُ هيَ عينُها
# في كلِّ زنادٍ.
on:
  push:
    branches: [main, "feat/**"]
  pull_request:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    # سقفُ زمنٍ لكلِّ وظيفةٍ: التعليقُ الصامتُ أسوأُ من الأحمرِ، إذ الأحمرُ يقولُ
    # أينَ. وقد علَّقَت وظيفةُ التكاملِ ساعةً في `F3-07` على وعدٍ لا يُحسَمُ حتّى
    # قِيسَ بالمهلةِ قسراً (`ADR 0122`). والسقفُ أضعافُ أطولِ تشغيلٍ مقيسٍ، فلا
    # يُسقِطُ صحيحاً بطيئاً ولا يُبقي معطوباً معلَّقاً إلى حدِّ المنصّةِ.
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Install
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: بناء تطبيق تِلِغرام المصغَّر
        run: bun run build:miniapp

      - name: Test
        run: set -o pipefail; bun run test 2>&1 | tee /tmp/ci-output.log

      - name: تفاصيل الإخفاق في تعليقٍ مقروء بلا سجلّات
        if: failure()
        run: |
          # الإخفاقُ الذي لا يُقرأ سببُه يُعالَج بالتخمين. وسجلّاتُ الوظيفة تُحمَّل
          # من تخزينٍ خارجيٍّ موقَّعٍ لا يبلغه كلُّ مراجع، فذيلُ المخرجات يُرفَع هنا
          # كملحوظةِ فحصٍ تظهر مع النتيجة نفسها.
          if [ ! -f /tmp/ci-output.log ]; then
            echo "::error::أخفقت الخطوة قبل أن تُنتج مخرجات — راجع الخطوة السابقة"
            exit 0
          fi
          # أسماءُ المخفقة أوّلاً مجموعةً في ملحوظةٍ واحدة: هي الفهرس. ثم تفصيلُ
          # الإخفاق في ملحوظاتٍ متتابعةٍ من ٢٥٠ سطراً، لأنّ الملحوظةَ الواحدة
          # محدودةُ الطول، وقطعُ التفصيل عند ستّين سطراً كان يُخفي أوّلَ الأسباب
          # ويُبقي آخِرَها — وأوّلُ الإخفاق هو ما يُشخَّص، وما بعده تَبَعٌ له.
          # الملحوظةُ الواحدة تُقتطع عند نحوِ ثلاثةِ آلافِ حرف، فرفعُ المخرجاتِ
          # كاملةً يضيع أكثرَه. ولذلك يُرفَع المفيدُ وحده: أسماءُ المخفقة فهرساً،
          # ثمّ ثلاثون سطراً قبل كلِّ إخفاقٍ — وفيها يطبع bun نصَّ الخطأ والمقارنةَ
          # والأثرَ — في ملحوظاتٍ صغيرةٍ لا تُقتطع.
          grep -n '^(fail)' /tmp/ci-output.log > /tmp/ci-fails.txt || true
          if [ -s /tmp/ci-fails.txt ]; then
            idx=$(cut -c1-160 /tmp/ci-fails.txt | sed 's/%/%25/g' | awk '{printf "%s%%0A", $0}')
            echo "::error title=فهرس الاختبارات المخفقة::${idx}"
          fi
          : > /tmp/ci-context.txt
          for lineno in $(cut -d: -f1 /tmp/ci-fails.txt | head -40); do
            start=$((lineno > 30 ? lineno - 30 : 1))
            sed -n "${start},${lineno}p" /tmp/ci-output.log >> /tmp/ci-context.txt
            echo "──────── نهاية سياق إخفاق ────────" >> /tmp/ci-context.txt
          done
          if [ ! -s /tmp/ci-context.txt ]; then cp /tmp/ci-output.log /tmp/ci-context.txt; fi
          rm -f /tmp/ci-part-*
          split -l 22 -d -a 2 <(cut -c1-300 /tmp/ci-context.txt | sed 's/%/%25/g') /tmp/ci-part-
          count=0
          for part in /tmp/ci-part-*; do
            count=$((count + 1))
            if [ "$count" -gt 24 ]; then
              echo "::error::سياقُ الإخفاق أطولُ من أربعٍ وعشرين ملحوظة — الباقي محجوب"
              break
            fi
            body=$(awk '{printf "%s%%0A", $0}' "$part")
            echo "::error title=سياق ${part##*-}::${body}"
          done

      - name: منع الكود الوهمي في الملفات المنفّذة
        run: |
          if grep -rn "In production this would" --include="*.ts" .; then
            echo "::error::تعليق كود وهمي ممنوع (القاعدة 0.1)"
            exit 1
          fi

      - name: منع الأرقام التجارية المرمَّزة داخل الكود
        run: bun run scripts/check-business-constants.ts

      - name: منع استيراد infrastructure داخل domain
        run: |
          if grep -rnE "^\s*(import|export)[^;]*from\s+['\"][^'\"]*infrastructure" \
               --include="*.ts" packages/domain; then
            echo "::error::domain لا يستورد من infrastructure إطلاقاً (ADR 0002)"
            exit 1
          fi

      - name: منع استيراد application داخل domain
        run: |
          if grep -rnE "^\s*(import|export)[^;]*from\s+['\"][^'\"]*application" \
               --include="*.ts" packages/domain; then
            echo "::error::domain لا يعتمد على application (ADR 0002)"
            exit 1
          fi

      - name: منع استيراد مزدوجات الاختبار في كود الإنتاج
        run: |
          if grep -rnE "^\s*(import|export)[^;]*from\s+['\"][^'\"]*tests/" \
               --include="*.ts" packages apps; then
            echo "::error::ممنوع استيراد أي شيء من tests/ داخل packages أو apps (القاعدة 0.1)"
            exit 1
          fi

      # خطّةُ هجرةٍ تسكنُ وثيقةً وحدَها تتقادمُ بلا سطرٍ خاطئٍ: تُبدَّلُ وجهةُ جدولٍ
      # في الجردِ فتبقى خطّتُه على الوجهةِ القديمةِ، فيقرأُ منفِّذٌ خطّةً صحيحةَ
      # الشكلِ تُخالِفُ القرارَ النافذَ. ويُخفِقُ الحاجزُ كذلكَ على أيِّ مُدخلٍ
      # يدّعي تنفيذاً وقسمُ «Migrated» في ROADMAP.md خالٍ. (ADR 0083)
      #
      # ويقعُ **قبلَ** حاجزِ القاعدةِ 0.4 عن قصدٍ: ذاكَ أحمرُ اليومَ بقرارِ مالكٍ
      # لم يُحسَم (`O-1`)، والوظيفةُ تنقطعُ عندَه، فخطوةٌ بعدَه لا تُشغَّلُ قطُّ
      # ولا يُقرأُ لها حكمٌ. وحاجزٌ لا يُقاسُ حاجزٌ يُظَنُّ أنَّه يحرسُ. ولا يُضعَّفُ
      # بذلكَ حاجزُ 0.4 ولا يُؤخَّرُ: هوَ الخطوةُ التاليةُ ويُخفِقُ كما كانَ.
      - name: مصفوفةُ الهجرةِ شاملةٌ ومتماسكةٌ ولا تدّعي تنفيذاً (W-2 / ADR 0083)
        run: bun run scripts/check-migration-matrix.ts

      # حدُّ الصادرِ: كلُّ مقصدٍ شبكيٍّ مُعلَنٌ، ولا مقصدَ في MARKET، ولا مرورَ
      # بينَ الأنظمةِ إلّا عبرَ بابِ CORE، ولا ادّعاءَ إزالةٍ بلا إزالةٍ. (ADR 0084)
      #
      # وموضعُه **قبلَ** حاجزِ القاعدةِ 0.4 لعينِ السببِ المذكورِ أعلاه.
      - name: حدُّ الصادرِ مُعلَنٌ بالكاملِ ولا مقصدَ في MARKET (W-6 / ADR 0084)
        run: bun run scripts/check-egress-boundary.ts

      # وموضعُ هذه الخطوةِ **قبلَ** حاجزِ `city_id` مقصودٌ للسببِ عينِه، وقد قِيسَ
      # مرّتَينِ: خطوةٌ بعدَ الحمراءِ تُقرَأُ `skipped` فلا حكمَ لها ألبتّةَ.
      - name: التشغيلُ الجافُّ لا يستطيعُ الكتابةَ ولا التسويةُ تدّعي أخضرَ (W-8 / ADR 0085)
        run: bun run scripts/check-migration-dry-run.ts

      # وهذه أيضاً **قبلَ** الحمراءِ، للسببِ عينِه المقيسِ ثلاثَ مرّاتٍ.
      - name: سجلُّ الحواجزِ مقروءٌ من مصدرٍ واحدٍ ولا إشهادَ يُلفَّقُ (W-8 · ADR 0087)
        run: bun run scripts/check-blocker-registry.ts

      # وهذه أيضاً **قبلَ** الحمراءِ، للسببِ المقيسِ عينِه.
      - name: سجلُّ التحوُّلِ لكلِّ خطوةٍ عكسٌ ومِسبارُ قراءةٍ، والتمرينُ مرفوضٌ بالإنشاءِ (W-9 · ADR 0088)
        run: bun run scripts/check-cutover-plan.ts

      # وهذه كذلكَ **قبلَ** الحمراءِ، وللسببِ عينِه: كلُّ ما بعدَ خطوةِ `city_id`
      # لا يُنفَّذُ ألبتّةَ ما بقيَت `O-1` مفتوحةً، فحاجزٌ يُوضَعُ بعدَها حاجزٌ
      # لا حكمَ له. والمقيسُ ههنا سندُ العقودِ المنقولةِ (`DEP-CORE-005`): لا
      # يقرأُ شبكةً ولا سرّاً، فلا عذرَ لتأخيرِه.
      - name: سندُ العقودِ المنقولةِ مُفكَّكٌ — لا ملفَّ بلا مصدرٍ ولا سندَ لمعدومٍ (DEP-CORE-005)
        run: bun run scripts/check-vendored-contract-pins.ts

      - name: سقوطُ مُقابِلِ الطزاجةِ مقيسٌ بخرقٍ مزروعٍ لا بنسخةٍ من CORE (DEP-CORE-005)
        run: bun test tests/unit/vendored-contract-pins.test.ts tests/unit/core-contract-freshness.test.ts

      - name: سقوطُ حاجزِ «السندُ يتبعُ البايتاتَ» مقيسٌ بمستودَعٍ مزروعٍ (DEP-CORE-005)
        run: bun test tests/unit/vendored-pin-follows-bytes.test.ts

      # وهذه كذلكَ **قبلَ** الحمراءِ، وللسببِ المقيسِ عينِه. والمقيسُ ههنا سجلُّ
      # وثائقِ الموافقةِ (`F2-01`): لا يقرأُ شبكةً ولا قاعدةً ولا سرّاً، بل نصَّ
      # الهجرةِ وقواميسَ اللغاتِ الثلاثِ، فلا عذرَ لتأخيرِه خلفَ `O-1`.
      - name: أصنافُ الموافقةِ مطابقةٌ لقيدِ الهجرةِ ولكلِّ مفتاحٍ ثلاثُ لغاتٍ (F2-01)
        run: bun run scripts/check-consent-documents.ts

      - name: سقوطُ حاجزِ وثائقِ الموافقةِ مقيسٌ بافتراقٍ مزروعٍ لا بالمستودَعِ كما هوَ (F2-01)
        run: bun test tests/unit/check-consent-documents.test.ts

      # ومثلُها لأنواعِ المكانِ المحفوظِ (`F2-02`): نصُّ الهجرةِ وشرطُ الفهرسِ
      # الفريدِ وقواميسُ اللغاتِ الثلاثِ، بلا شبكةٍ ولا قاعدةٍ ولا سرٍّ.
      - name: أنواعُ المكانِ مطابقةٌ لقيدِ الهجرةِ ولشرطِ الفهرسِ الفريدِ (F2-02)
        run: bun run scripts/check-place-kinds.ts

      - name: سقوطُ حاجزِ أنواعِ المكانِ مقيسٌ بافتراقٍ مزروعٍ لا بالمستودَعِ كما هوَ (F2-02)
        run: bun test tests/unit/check-place-kinds.test.ts

      # ومثلُها لعقدِ الوجهةِ (`F2-03`) — وهذا أوسعُها: يُطابِقُ جدولَ طيِّ الحروفِ
      # وصنفَ المحارفِ في **النطاقِ** بوسيطَي `translate` ونصِّ `regexp_replace`
      # في الهجرةِ حرفاً بحرفٍ، ورموزَ الرفضِ بما تُعيدُه الدالّةُ فعلاً، ووجودَ
      # الفهارسِ الثلاثةِ في طورِ `index` بـ`concurrently`.
      #
      # ولا يُغني عن `tests/integration/destinations.test.ts`: ذاكَ يُقارِنُ
      # **سلوكَينِ** على محرّكٍ حقيقيٍّ، وهذا يُقارِنُ نصَّينِ — وجدولانِ
      # متطابقانِ نصّاً قد يُنفَّذانِ مختلفَينِ. فالبوّابتانِ قائمتانِ معاً ولا
      # تُقرَأُ إحداهما إثباتاً للأخرى.
      - name: عقدُ الوجهةِ — تطبيعٌ واحدٌ في الزمنَينِ وأصنافٌ ورفوضٌ وفهارسُ مطابقةٌ (F2-03)
        run: bun run scripts/check-destination-contract.ts

      - name: سقوطُ حاجزِ عقدِ الوجهةِ مقيسٌ بافتراقٍ مزروعٍ لا بالمستودَعِ كما هوَ (F2-03)
        run: bun test tests/unit/check-destination-contract.test.ts

      # `F2-04`: حاجزُ عقدِ الاقتباسِ يُنفِذُ ما لا يُنفِذُه مُراجِعٌ بعينِه — أنَّ
      # **لا لفظَ أجرةٍ ولا وسيلةَ دفعٍ ولا حقلاً مُعَدّاً لهما** في شريحةِ
      # الاقتباسِ كلِّها (`ADR 0039` §٤ محجوبٌ على `DEC-11`، و`م13-7` يُجمِّدُ حتّى
      # الهياكلَ التمهيديّةَ)، وأنَّ رقمَ المسافةِ لا يُسافِرُ بلا وسمِه، وأنَّ كلَّ
      # رمزِ رفضٍ تُصدِرُه الهجرةُ مُترجَمٌ في الشاشةِ فلا يُعرَضُ رمزٌ يتيمٌ.
      # ولا يُغني عن `tests/integration/quote.test.ts`: ذاكَ يُقاسُ على محرّكٍ
      # حقيقيٍّ، وهذا نصٌّ ساكنٌ — والبوّابتانِ قائمتانِ معاً.
      - name: عقدُ الاقتباسِ — لا أجرةَ ولا دفعَ ولا رقمَ بلا وسمٍ ولا رمزَ يتيمٌ (F2-04)
        run: bun run scripts/check-quote-contract.ts

      - name: سقوطُ حاجزِ عقدِ الاقتباسِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-04)
        run: bun test tests/unit/check-quote-contract.test.ts

      # `F2-05`: طلبُ الرحلةِ. والحاجزُ يفرضُ عشرَ قواعدَ كانَت تُحرَسُ بالمراجعةِ
      # البشريّةِ وحدَها — أوّلُها أنَّ مفتاحَ التكرارِ **قيدُ مخطَّطٍ** لا فحصُ
      # تطبيقٍ: فحصٌ في الشِّفرةِ («اقرأْ ثمَّ اكتُبْ») يُنتِجُ رحلتَينِ عندَ
      # ضغطتَينِ متوازيتَينِ، والمهلةُ بينَ القراءةِ والكتابةِ هيَ البابُ. وأنَّ
      # مسارَ الشبكةِ لا يستوردُ الكاتبَ الأقدمَ بلا مفتاحٍ (`ARCH-006`)، وأنَّ
      # المؤقّتَ يُقاسُ من ميلادِ الرحلةِ لا من فتحِ الشاشةِ، وأنَّ صفرَ
      # المبلَّغينَ نصٌّ مُعلَنٌ لا رقمٌ في قالبٍ (`ADR 0023`).
      # ولا يُغني عن `tests/integration/ride-request.test.ts`: ذاكَ يُقاسُ على
      # PostgreSQL حقيقيٍّ بسباقٍ فعليٍّ، وهذا نصٌّ ساكنٌ — والبوّابتانِ معاً.
      - name: عقدُ طلبِ الرحلةِ — مفتاحُ تكرارٍ قيدَ مخطَّطٍ ولا حالةَ بحثٍ مُجمَّلةً (F2-05)
        run: bun run scripts/check-ride-request-contract.ts

      - name: سقوطُ حاجزِ عقدِ طلبِ الرحلةِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-05)
        run: bun test tests/unit/check-ride-request-contract.test.ts

      # `UX-021`: صنفٌ يُصدِرُه العرضُ بلا قاعدةٍ في ورقةِ النمطِ **لا يُخفِقُ في
      # بناءٍ ولا اختبارٍ ولا مُدقِّقٍ**: تُرسَمُ الشاشةُ بأنماطِ المتصفِّحِ
      # الافتراضيّةِ فيراها المستخدمُ عاريةً ويراها المُختبَرُ خضراءَ. وقد وقعَ
      # فعلاً في سطحَي `F2-02` و`F2-03`، وسببُ التكرارِ **غيابُ القياسِ** لا سهوُ
      # كاتبٍ. فالحاجزُ يقابلُ المُصدَرَ بالمُحدِّدِ في الاتجاهَينِ، ويمنعُ تعبيرَ
      # صنفٍ لا يُحَلُّ ساكناً كي لا يُخدَعَ القياسُ نفسُه (`ADR 0105`).
      - name: لا صنفَ يُصدِرُه العرضُ بلا قاعدةٍ ولا قاعدةَ بلا مُصدِرٍ غيرَ مُسجَّلةٍ (UX-021)
        run: bun run scripts/check-css-class-coverage.ts

      - name: سقوطُ حاجزِ تغطيةِ أصنافِ العرضِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (UX-021)
        run: bun test tests/unit/check-css-class-coverage.test.ts

      # `OPS-019`: أخطرُ من اختبارٍ أحمرَ اختبارٌ **أخضرُ بشرطٍ لم يطلُبْه**. كانت
      # ثمانيةُ ملفّاتِ تكاملٍ تختارُ المدينةَ بـ`where c.is_active`، والبذرةُ
      # تُنشئُ مدنَ الإطلاقِ معطَّلةً (`F2-05`) — فكانت تنجحُ لأنَّ ثمانيةً
      # وثلاثينَ ملفّاً آخرَ تُفعِّلُ مدينةً **ولا تردُّها**. فالأخضرُ مُستعارٌ
      # من ترتيبِ الاكتشافِ، ودمجُ بندٍ واحدٍ غيَّرَ الترتيبَ فأحمرَ CI فعلاً
      # (التشغيلُ `34909694080`). والعلاجُ الجذريُّ معينٌ واحدٌ يلتقطُ لقطةً
      # ويردُّها حرفاً، وحاجزٌ يمنعُ عودةَ النمطِ (`ADR 0116`).
      - name: لا اختبارَ تكاملٍ يستعيرُ شرطَه المسبقَ ولا يُفعِّلُ مدينةً بلا ردٍّ (OPS-019)
        run: bun run scripts/check-integration-city-precondition.ts

      - name: سقوطُ حاجزِ الشرطِ المسبقِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (OPS-019)
        run: bun test tests/unit/check-integration-city-precondition.test.ts

      # `UX-022` · `F2-06`: شاشةُ الرحلةِ النشطةِ أقربُ سطحٍ إلى وعدٍ لم يُبنَ:
      # موقعٌ بلا عُمرِه يرسمُ سائقاً واقفاً حيّاً (`BUG-001`)، وزرُّ طوارئَ أو
      # مشاركةٍ يعدُ مساراً موعِدُه `F2-09`/`F2-10`، وحقلُ مالٍ واحدٌ ينقُضُ
      # `ADR 0039` §٤. فالحاجزُ يقيسُ خمسَ قواعدَ: لا مالَ، ولا بطاقةَ سائقٍ بلا
      # إسنادٍ، ولا نقطةَ بلا عُمرِها في القاعدةِ والعقدِ والشاشةِ والقاموسِ،
      # ومفاتيحَ متطابقةً في ثلاثةِ قواميسَ، ولا زرَّ لمسارٍ لم يُبنَ.
      # ولا يُغني عن `tests/integration/active-ride.test.ts`: ذاكَ يقيسُ على
      # PostgreSQL حقيقيٍّ أنَّ المِلكيّةَ قيدُ استفسارٍ وأنَّ العُمرَ تقيسُه القاعدةُ
      # بساعتِها، وهذا نصٌّ ساكنٌ — والبوّابتانِ معاً.
      - name: عقدُ الرحلةِ النشطةِ — لا مالَ ولا نقطةَ بلا عُمرِها ولا زرَّ لمسارٍ لم يُبنَ (F2-06 · UX-022)
        run: bun run scripts/check-active-ride-contract.ts

      - name: سقوطُ حاجزِ عقدِ الرحلةِ النشطةِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-06 · UX-022)
        run: bun test tests/unit/check-active-ride-contract.test.ts

      # `F2-07` · `SR-07`/`SR-08`: شاشةُ ما بعدَ الرحلةِ هيَ موضعُ الكذبِ الأخيرِ:
      # ههنا تُعرَضُ أرقامٌ نهائيّةٌ يُبنى عليها حكمُ الراكبِ، فرقمُ `st_distance`
      # يُقرأُ «المسافةَ» وهوَ وترُ خطٍّ مستقيمٍ لا أثرُ مسارٍ (لا مطابقةَ خرائطَ
      # في المستودعِ)، و«الإكراميّةُ» تُزرَعُ ههنا في كلِّ تطبيقٍ فتنقُضُ
      # `ADR 0039` §٤، وزرُّ «مشكلةٍ في الرحلةِ» يعدُ تذكرةَ دعمٍ لا مسارَ لها.
      # فالحاجزُ يقيسُ ستَّ قواعدَ: لا مالَ، ولا مسافةَ تُدَّعى مقطوعةً، ومُعجَمَ
      # وسومٍ واحداً بينَ النطاقِ والهجرةِ حرفاً وترتيباً، ومفاتيحَ متطابقةً في
      # ثلاثةِ قواميسَ، ولا زرَّ لمسارٍ لم يُبنَ، ولا دالّةَ بلا نزعِ تنفيذٍ.
      # ولا يُغني عن `tests/integration/ride-summary.test.ts`: ذاكَ يقيسُ على
      # PostgreSQL حقيقيٍّ أنَّ المدّةَ بساعةِ القاعدةِ وأنَّ الغيابَ `null` لا صفرٌ
      # وأنَّ التقييمَ الثانيَ يُردُّ بقيدٍ فريدٍ — وهذا نصٌّ ساكنٌ، والبوّابتانِ معاً.
      - name: عقدُ الإنهاءِ والتقييمِ — لا مالَ ولا مسافةَ تُدَّعى مقطوعةً ولا زرَّ لمسارٍ لم يُبنَ (F2-07)
        run: bun run scripts/check-ride-summary-contract.ts

      - name: سقوطُ حاجزِ عقدِ الإنهاءِ والتقييمِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-07 · ح-7)
        run: bun test tests/unit/check-ride-summary-contract.test.ts

      # `F2-08` · `SR-09`/`SR-10`: السجلُّ شاشةٌ تُقرأُ ولا تُراجَعُ — يفتحُها الراكبُ
      # مرّةً ويُصدِّقُ ما فيها، فكذبُها لا يُكتَشَفُ. وثلاثُ كذباتٍ لا يُمسِكُها
      # حاجزٌ آخرُ: لحظةٌ تُصاغُ بساعةِ الجهازِ فيقعُ الحدثُ تحتَ شهرٍ لم يقع فيه،
      # وصفحةٌ بإزاحةٍ على جدولٍ يُكتَبُ فيه أثناءَ التصفُّحِ تُكرِّرُ صفّاً وتُسقِطُ
      # آخرَ، وخريطةٌ أو تذكرةُ دعمٍ تُرسَمُ زرّاً لا مسارَ له (`ADR 0007` · `F2-12`).
      # فالحاجزُ يقيسُ ثمانيَ قواعدَ، منها أنَّ الغيابَ **مُصرَّحٌ بنصٍّ** في الألسنةِ
      # الثلاثةِ لا مكتومٌ ولا موعودٌ. ولا يُغني عن `tests/integration/ride-history.test.ts`:
      # ذاكَ يقيسُ على PostgreSQL حقيقيٍّ أنَّ المفتاحَ لا يُكرِّرُ ولا يُسقِطُ وأنَّ
      # المنطقةَ تأتي من `platform_settings` — وهذا نصٌّ ساكنٌ، والبوّابتانِ معاً.
      - name: عقدُ السجلِّ والتفاصيلِ — لا ساعةَ جهازٍ ولا إزاحةَ ولا غيابَ مكتومٍ (F2-08)
        run: bun run scripts/check-ride-history-contract.ts

      - name: سقوطُ حاجزِ عقدِ السجلِّ والتفاصيلِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-08 · ح-7)
        run: bun test tests/unit/check-ride-history-contract.test.ts

      # `UX-023` · `F2-09` · `SR-13`: رابطُ المشاركةِ **يُقرأُ بلا حسابٍ**: مَن ملكَ
      # النصَّ ملكَ الجوابَ. فالخطأُ ههنا ليسَ عطلاً بل اسمُ راكبةٍ ورقمُها في يدِ
      # غريبٍ. والعلّةُ الأصليّةُ من هذا البابِ: `get_tracking_position` كانت تنشرُ
      # إحداثيّةً **بلا بوّابةِ طزاجةٍ** لحاملِ الرابطِ، وشاشةُ المالكِ تحجبُ ما
      # جاوزَ تسعينَ ثانيةً — فكانَ الغريبُ مُصدَّقاً أكثرَ من صاحبِ الرحلةِ.
      # فالحاجزُ يقيسُ ثمانيَ قواعدَ: لا هويّةَ في الحمولةِ العامّةِ، ولا موضعَ بلا
      # عُمرِه، وحدَّ عُمرٍ واحداً بينَ الهجرةِ والنطاقِ، ولا رمزَ في ردِّ قراءةٍ،
      # وحَكَماً واحداً في القاعدةِ يُجيبُ للمالكِ وللغريبِ، ومفاتيحَ متطابقةً،
      # وكلَّ رمزِ إفصاحٍ بنصِّه في ثلاثِ لغاتٍ، ولا دالّةَ بلا نزعِ تنفيذٍ.
      # ولا يُغني عن `tests/integration/ride-share.test.ts`: ذاكَ يقيسُ على
      # PostgreSQL حقيقيٍّ أنَّ الرابطَ ينتهي وأنَّ الإلغاءَ يُبطِلُ كلَّ الروابطِ
      # وأنَّ الحمولةَ العامّةَ بلا هويّةٍ — وهذا نصٌّ ساكنٌ، والبوّابتانِ معاً.
      - name: عقدُ مشاركةِ الرحلةِ — لا هويّةَ لغريبٍ ولا موضعَ بلا عُمرِه ولا حَكَمَينِ للطزاجةِ (F2-09 · UX-023)
        run: bun run scripts/check-ride-share-contract.ts

      - name: سقوطُ حاجزِ عقدِ المشاركةِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-09 · ح-7)
        run: bun test tests/unit/check-ride-share-contract.test.ts

      # `UX-024` · `F2-10` · `SR-14`: سطحُ الاستغاثةِ **يُخلِفُ صامتاً**. بطاقةُ
      # مشاركةٍ لا تعملُ يكتشفُها صاحبُها في دقيقةٍ ويشكو؛ وبطاقةُ استغاثةٍ لا
      # تعملُ لا يشكو منها أحدٌ — يحتاجُها إنسانٌ مرّةً واحدةً وحينَها لا يفتحُ
      # تذكرةً. فما لا يُقاسُ آليّاً ههنا لا يُقاسُ أبداً.
      # وأخطرُ صورِ الإخلافِ **وعدُ اتّصالٍ**: لا مزوِّدَ مكالماتٍ في المستودَعِ،
      # ومَن قرأَ «سنتّصلُ بكَ» انتظرَ مكالمةً لا تأتي بدلَ أن يطلبَ الطوارئَ
      # العامّةَ بنفسِه — فتأخيرُ دقيقتَينِ ههنا أذًى لا عطلٌ.
      # فالحاجزُ يقيسُ ستَّ قواعدَ: لا وعدَ اتّصالٍ في شِفرةٍ ولا في نصٍّ، وكلَّ
      # رمزِ إفصاحٍ بنصِّه في ثلاثِ لغاتٍ، ونفيَ الاتّصالِ منشوراً من القاعدةِ في
      # كلِّ حالٍ، ولا ساعةَ جهازٍ تحسبُ عُمرَ بلاغٍ، ولا مُعرِّفَ طلبٍ يُرسَلُ من
      # شاشةٍ (`ADR 0077`)، ولا دالّةَ بلا نزعِ تنفيذٍ عن الأدوارِ الثلاثةِ.
      - name: عقدُ سطحِ الاستغاثةِ — لا وعدَ اتّصالٍ ولا إفصاحَ بلا نصٍّ ولا ساعةَ جهازٍ (F2-10 · UX-024)
        run: bun run scripts/check-sos-surface-contract.ts

      - name: سقوطُ حاجزِ سطحِ الاستغاثةِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ (F2-10 · ح-7)
        run: bun test tests/unit/check-sos-surface-contract.test.ts

      # `SR-11` · `F2-12`: سطحُ الشكوى **يُخلِفُ بصمتٍ نافعٍ لنا**. شكوى لا
      # تُفتَحُ يُعيدُها صاحبُها، أمّا رمزُ عطبٍ بلا نصٍّ فيُعرَضُ مفتاحاً خاماً
      # في اللحظةِ التي يشكو فيها إنسانٌ — فيصيرُ العطبُ عطبَينِ. ومرجعٌ لا
      # يُعرَضُ يُنتِجُ تذاكرَ لا يتابعُها أحدٌ فتُقرأُ في القياسِ «شكاوى قليلةٌ»،
      # وهيَ أسوأُ كذبةٍ يمكنُ أن تُقالَ لفريقٍ يقرأُ لوحتَه.
      # ولأنَّ `check-i18n.ts` يقرأُ `packages/shared/i18n/*.json` وحدَها **ولا
      # يقرأُ قواميسَ التطبيقِ المُصغَّرِ**، فتطابقُ مفاتيحِ `rider.support.` في
      # الثلاثةِ يُفرَضُ ههنا أو لا يُفرَضُ أبداً.
      - name: عقدُ سطحِ الدعمِ — مرجعٌ منطوقٌ لا UUID ولا رمزَ بلا نصٍّ ولا بابَ إرفاقٍ صوريَّ (F2-12 · SR-11)
        run: bun run scripts/check-support-intake-contract.ts

      - name: سقوطُ حاجزِ سطحِ الدعمِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من السبعِ (F2-12 · ح-7)
        run: bun test tests/unit/check-support-intake-contract.test.ts

      # `SD-12`: سطحُ الحسابِ يُخلِفُ **بصمتٍ وفي أخطرِ لحظةٍ**. والدليلُ واقعةٌ
      # لا فرضٌ: كانَ `KNOWN_BASES` في سطحِ الراكبِ خمسةَ أسسٍ والنطاقُ تسعةً،
      # وقاعدةُ البياناتِ تُرسِلُ في إيصالِ **كلِّ** حذفٍ سطرَ `identityBar`
      # بأساسِ `BLOCK_AND_STANDING_SURVIVE_ERASURE` ونصُّه مكتوبٌ عندَنا — فكانَ
      # مَن سألَ «لماذا بقيَ حظري؟» يُجابُ «سببُ إبقاءٍ لا نعرفُ نصَّه بعدُ».
      # وكانَ `rider.account.section.identityBar` غائباً من القواميسِ الثلاثةِ،
      # فيُقرأُ على إيصالِ حذفٍ **مفتاحٌ خامٌ**. ولم يرَ ذلكَ مُصرِّفٌ ولا تغطيةٌ:
      # نقصُ عضوٍ في `Set<string>` لا يُنكِرُه نوعٌ. ولأنَّ `check-i18n.ts` يقرأُ
      # `packages/shared/i18n/*.json` وحدَها **ولا يقرأُ قواميسَ التطبيقِ
      # المُصغَّرِ**، فتطابقُ مفاتيحِ الحسابِ يُفرَضُ ههنا أو لا يُفرَضُ أبداً.
      - name: عقدُ سطحِ الحسابِ — لا رمزَ ولا قسمَ بلا نصٍّ ولا نسخةَ مجالٍ في سطحٍ (SD-12)
        run: bun run scripts/check-account-surface-contract.ts

      - name: سقوطُ حاجزِ سطحِ الحسابِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من الثمانِ (SD-12 · ح-7)
        run: bun test tests/unit/account-surface.test.ts

      # `F3-09` · `SD-08`: الفاتورةُ **وثيقةٌ يُحتجَّ بها على الدولةِ وعلى
      # السائقِ معاً**، وكلُّ صنفٍ من عطبِها صامتٌ أمامَ المُصرِّفِ والاختبارِ:
      # عمودُ `card_number` يُصرَّفُ فيُدخِلُنا نطاقَ `PCI DSS` بسطرٍ، ومسارُ
      # `GET` الذي يُصدِرُ يُعادُ نداءُه بزحفٍ **فيُصدِرُ أرقاماً تسلسليّةً لا رجعةَ
      # فيها**، وقسمةٌ على `10000` وحدَها تجعلُ الفاتورةَ تُخالِفُ ما في
      # `payment_transactions` وما رأى السائقُ في `F3-06`.
      - name: عقدُ الفاتورةِ الضريبيّةِ — لا بياناتِ بطاقةٍ ولا فاتورةَ تُعدَّلُ ولا إصدارَ في GET (F3-09)
        run: bun run scripts/check-tax-invoice-contract.ts

      - name: سقوطُ حاجزِ الفاتورةِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ من الثمانِ (F3-09 · ح-7)
        run: bun test tests/unit/check-tax-invoice-contract.test.ts

      # `F3-01` · `SD-01` · `SD-02`: وثائقُ السائقِ **منعُ عملٍ لا سطرُ بيانٍ**:
      # سائقٌ محجوبٌ لا يصلُه عرضٌ إطلاقاً (`F12-14`)، فرمزُ حجبٍ بلا نصٍّ
      # يجعلُه يقرأُ قائمةً فارغةً بلا سببٍ — ويزيدُ الدعمَ شكوى لا يُعرَفُ أصلُها.
      # وأخطرُ ما في هذا البندِ **رفعٌ إلى مضيفٍ ليسَ لنا**: ترويسةُ تفويضٍ
      # تُرسَلُ معَ الملفِّ تُسلِّمُ جلسةَ السائقِ أو مفتاحَ الخدمةِ لطرفٍ ثالثٍ،
      # وإذنٌ موقَّعٌ يُكتَبُ في سجلٍّ يبقى صالحاً لمن قرأَ السجلَّ.
      # فالحاجزُ يقيسُ سبعَ قواعدَ: مفاتيحَ متطابقةً في الثلاثةِ، وكلَّ نوعٍ
      # وحالةٍ ورمزِ حجبٍ بنصِّه، وكلَّ رمزٍ عامٍّ بنصِّه، ولا دالّةَ بلا نزعِ
      # تنفيذٍ، ولا تفويضَ ذاهباً إلى مضيفِ المخزنِ، ولا مسارَ ولا إذناً في
      # سجلٍّ، ولا نوعَ يفترقُ بينَ تعدادِ القاعدةِ وقائمةِ النطاقِ.
      - name: عقدُ وثائقِ السائقِ — لا حجبَ بلا نصٍّ ولا تفويضَ لمخزنٍ ولا إذنَ رفعٍ في سجلٍّ (F3-01 · SD-02)
        run: bun run scripts/check-driver-documents-contract.ts

      - name: سقوطُ حاجزِ وثائقِ السائقِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من السبعِ (F3-01 · ح-7)
        run: bun test tests/unit/check-driver-documents-contract.test.ts

      # `F3-02` · `SD-03` · `SD-04`: عرضٌ بمؤقّتٍ وقبولٌ ذرّيٌّ — وههنا أربعةُ
      # أعطابٍ **لا يُمسِكُها اختبارٌ أخضرُ**: مؤقّتٌ يُحسَبُ بساعةِ الهاتفِ يمرُّ
      # على كلِّ جهازٍ ساعتُه سليمةٌ ويُخفي عرضاً صالحاً عندَ سائقٍ ساعتُه
      # منحرفةٌ؛ وذرّيّةٌ ثانيةٌ في هذه الشريحةِ تجعلُ للإيكالِ كاتبَينِ فيُوكَلُ
      # طلبٌ لسائقَينِ في سبقٍ لا يُقاسُ إلّا نادراً؛ ورقمُ مسافةٍ بلا وسمٍ يُقرأُ
      # طولَ طريقٍ وهوَ خطٌّ مستقيمٌ (`ADR 0024`)؛ وهويّةُ راكبٍ في حمولةِ عرضٍ
      # تُكشَفُ **لكلِّ سائقي الجولةِ** لا لمن ظفرَ بالطلبِ. وتُقاسُ معها أنَّ
      # الطبقةَ الماليّةَ غائبةٌ بإعلانٍ: لا لفظَ مالٍ في الشريحةِ كلِّها
      # (`ADR 0039` §4 · `DEC-11`)، فحقلٌ فارغٌ اسمُه أجرةٌ يُقرأُ «مجّاناً».
      - name: عقدُ عروضِ السائقِ — لا ساعةَ جهازٍ في حكمٍ ولا ذرّيّةَ ثانيةً ولا هويّةَ راكبٍ في عرضٍ (F3-02 · SD-03 · SD-04)
        run: bun run scripts/check-driver-offers-contract.ts

      - name: سقوطُ حاجزِ عروضِ السائقِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من الثمانِ (F3-02 · ح-7)
        run: bun test tests/unit/check-driver-offers-contract.test.ts

      # ومَهمّةُ السائقِ النشطةُ شريحةٌ أخطرُ من العرضِ: العرضُ يُقبَلُ أو يُرفَضُ،
      # أمّا المَهمّةُ **فتُختَمُ أطوارُها** — ووصولٌ يُختَمُ بغيرِ فعلِ إنسانٍ
      # (باستنتاجِ قُربٍ) يُحاسِبُ الراكبَ على انتظارٍ لم يبدأْ، وذاكَ يمرُّ في كلِّ
      # اختبارٍ ببياناتٍ مُرتَّبةٍ (`ADR 0118`). ومعَه: كاتبٌ ثانٍ للانتقالِ يجعلُ
      # لبدءِ الرحلةِ كاتبَينِ فتنحرفُ الحالةُ عن سجلِّ التدقيقِ؛ وختمٌ لهُ كاتبانِ
      # يُكتَبُ مرّتَينِ فلا يُقرأُ منهُ أوّلُ وصولٍ؛ وشاشةٌ تُقارِنُ الحالةَ محلّيّاً
      # تُعيدُ بناءَ آلةِ الحالاتِ فتتقادَمُ عندَ أوّلِ طَورٍ يُزادُ؛ وهاتفُ الراكبِ
      # ليسَ من عقدِ هذا السطحِ ولو كانَ السائقُ صاحبَ الطلبِ.
      - name: عقدُ مَهمّةِ السائقِ — الطَورُ ختمُ إنسانٍ لا استنتاجُ قُربٍ، وكاتبٌ واحدٌ لكلِّ انتقالٍ (F3-03 · SD-05)
        run: bun run scripts/check-driver-job-contract.ts

      - name: سقوطُ حاجزِ مَهمّةِ السائقِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من الثمانِ (F3-03 · ح-7)
        run: bun test tests/unit/check-driver-job-contract.test.ts

      # والنبضةُ الدوريّةُ أخطرُ من قراءةٍ بالطلبِ: قراءةٌ خاطئةٌ تُصلَحُ بتحديثِ
      # الشاشةِ، أمّا نبضةٌ بمُدّةٍ **مُخترَعةٍ في العميلِ** فتستنزفُ بطاريّةَ
      # سائقٍ يعملُ ساعاتٍ، وتُشبِعُ حدَّ المعدَّلِ فتُرفَضُ نبضاتٌ حقيقيّةٌ،
      # ولا يظهرُ ذلكَ في أيِّ اختبارٍ لأنَّ كلَّ نداءٍ ناجحٌ. ومعَه: غيابُ
      # إعدادٍ يُقرأُ صفراً فيصيرُ السكونُ بثّاً متّصلاً؛ وسببٌ يُشتَقُّ في
      # الشاشةِ بمقارنةِ حالةٍ فتصيرُ آلةُ الحالاتِ نسختَينِ؛ ومُرسِلٌ ثانٍ
      # يُضاعِفُ النبضَ ويُقاسِمُ حدَّ المعدَّلِ نفسَه.
      - name: عقدُ بثِّ موقعِ السائقِ — المُدّةُ حقيقةُ خادمٍ والغيابُ عَدَمٌ لا صفرٌ (F3-04 · SD-05)
        run: bun run scripts/check-location-broadcast-contract.ts

      - name: سقوطُ حاجزِ بثِّ الموقعِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من الثمانِ (F3-04 · ح-7)
        run: bun test tests/unit/check-location-broadcast-contract.test.ts

      # شاشةُ أرقامٍ تُقرأُ على مُشتَغِلٍ أخطرُ من شاشةِ حالةٍ: حالةٌ خاطئةٌ تُرى
      # وتُشتكى، أمّا **نسبةٌ بلا مقامٍ** فتُقرأُ حُكماً نهائيّاً على رزقِ إنسانٍ
      # وهيَ عن عرضَينِ، ولا يسقُطُ بها اختبارٌ ألبتّةَ. ومعَها: مبلغٌ يُخترَعُ في
      # منصّةٍ لا تتوسّطُ في أجرةٍ فيُقرأُ وعداً؛ وصفرٌ يحلُّ محلَّ «لم يُقَسْ»
      # فيُحسَبُ حقيقةً؛ ونصٌّ يُوحي بأنَّ رفضاً يُنقِصُ ترتيباً والمعادلةُ لا
      # تقرأُه فيُخيفُ ويكسِبُ؛ وهويّةُ راكبٍ تسرُّبُ في أرشيفٍ لا يحتاجُها.
      - name: عقدُ حصيلةِ السائقِ — المقامُ يُنشَرُ دائماً والغيابُ مُعلَنٌ بأساسِه (F3-05 · SD-06 · SD-09)
        run: bun run scripts/check-driver-activity-contract.ts

      - name: سقوطُ حاجزِ الحصيلةِ مقيسٌ بافتراقٍ مزروعٍ لكلِّ قاعدةٍ من التسعِ (F3-05 · ح-7)
        run: bun test tests/unit/check-driver-activity-contract.test.ts

      # عطبٌ دُفِعَ ثمنُه **أربعَ مرّاتٍ**: في `$1::jsonb` يستنبِطُ PostgreSQL
      # نوعَ المُعامِلِ `jsonb` فيُرمِّزُ السائقُ القيمةَ **مرّتَينِ**، فيصلُ
      # الرقمُ نصّاً بلا خطأٍ ولا سجلٍّ. مرّةً أرسلَ بثّاً إلى الجمهورِ كلِّه
      # إذ قُرِئَت المرشِّحاتُ `null`، ومرّةً أسقطَ خمسةَ عشرَ اختباراً في
      # ملفّاتٍ لا علاقةَ لها بالبندِ (`F2-09`). والدرسُ كانَ مكتوباً في
      # ثلاثةِ تعليقاتٍ ولم يكن مُنفَذاً آلةً — فصارَ.
      - name: لا مُعامِلَ مربوطاً بـ`::jsonb` — القيمةُ تُرمَّزُ مرّةً واحدةً
        run: bun run scripts/check-jsonb-binding.ts

      - name: سقوطُ حاجزِ `jsonb` مقيسٌ بسالبةٍ مزروعةٍ — منها السطرُ الذي أسقطَ CI (ح-7)
        run: bun test tests/unit/check-jsonb-binding.test.ts

      # عطبٌ من الصنفِ نفسِه في الاتّجاهِ المعاكسِ: عمودُ `date` في عقدِ خرجِ
      # دالّةٍ يصلُ العميلَ كائنَ `Date` عندَ منتصفِ ليلِ UTC، فيقرأُه محوِّلٌ
      # عقدُه `string | null` **عَدَماً** بلا خطأٍ ولا سجلٍّ. وقد أخفى ذلكَ في
      # `F3-07` تواريخَ انتهاءِ رخصةِ السيرِ والتأمينِ والفحصِ عن السائقِ — وهيَ
      # الحقيقةُ التي تحجُبُه عن العملِ (`ADR 0115`) — ولم يُكشَف إلّا بسقوطِ
      # اختبارِ تكاملٍ في CI. فصارَ الدرسُ حاجزاً لا تعليقاً (`ADR 0121`).
      - name: لا عمودَ `date` في عقدِ خرجٍ — التاريخُ المجرَّدُ يعبرُ الحدَّ نصّاً (ADR 0121)
        run: bun run scripts/check-date-only-boundary.ts

      - name: سقوطُ حاجزِ الحدِّ مقيسٌ بعقدٍ مزروعٍ — منه العقدُ الذي أسقطَ CI (ح-7)
        run: bun test tests/unit/check-date-only-boundary.test.ts

      # وأخطرُ من الأحمرِ: **التعليقُ الصامتُ**. وسمُ استعلامِ `postgres.js`
      # مُرجَأٌ لا وعدٌ منطلقٌ — لا يُرسَلُ حتّى يُنادى `then` — ومُطابِقُ
      # `expect(…).rejects` لا يُناديه. فحالتانِ في اختبارِ تكاملِ `F3-07`
      # مرَّرَتا الوسمَ خامّاً، فعلَّقَتا وظيفةَ «تكامل على PostgreSQL حقيقي»
      # أكثرَ من ساعةٍ **بلا رسالةِ فشلٍ ولا سطرٍ في السجلِّ**؛ والأحمرُ يقولُ
      # أينَ، والتعليقُ لا يقولُ شيئاً. فصارَ الدرسُ حاجزاً (`ADR 0122`).
      - name: لا وسمَ استعلامٍ مُرجَأً داخلَ `expect(…)` — فلا وعدَ بلا حسمٍ (ADR 0122)
        run: bun run scripts/check-lazy-query-assertion.ts

      - name: سقوطُ حاجزِ الإرجاءِ مقيسٌ بسالبةٍ مزروعةٍ — منها الحالةُ التي علَّقَت CI (ح-7)
        run: bun test tests/unit/check-lazy-query-assertion.test.ts

      # وأمكرُ من الأحمرِ الثابتِ: **الأحمرُ المتقطّعُ**. الرقمُ في قناةِ
      # التتبُّعِ يُمنَحُ داخلَ معاملةٍ ويُنشَرُ بعدَها بلا قفلٍ، فمن نداءَينِ
      # متزامنَينِ قد ينشرُ صاحبُ ٣ قبلَ صاحبِ ٢ — وذلكَ صحيحٌ بنصِّ `ADR 0053`.
      # فتوكيدٌ يقولُ «آخرُ ما نُشِرَ = last_sequence» يقيسُ الجدولةَ لا العقدَ:
      # أسقطَ CI ثلاثَ مرّاتٍ من ملفٍّ واحدٍ (`OPS-016` · `OPS-017` · التشغيلُ
      # 35431980350)، وأُصلِحَ مرّتَينِ بيدٍ فعادَ في سطرٍ ثالثٍ. والأحمرُ
      # المتقطّعُ يُعلِّمُ إعادةَ التشغيلِ بدلَ القراءةِ، فصارَ حاجزاً (`ADR 0147`).
      - name: لا شاهدَ يقرأُ ترتيبَ التسليمِ في اختبارٍ متزامنٍ (ADR 0147)
        run: bun run scripts/check-sequence-witness-order.ts

      - name: سقوطُ حاجزِ شاهدِ الترتيبِ مقيسٌ بسالباتٍ مزروعةٍ — منها الأسطرُ التي أسقطَت CI (ح-7)
        run: bun test tests/unit/check-sequence-witness-order.test.ts

      # وأخفى من التعليقِ: **فحصٌ يمرُّ نهاراً ويسقطُ ليلاً**. نافذةُ التقاريرِ
      # تبدأُ عندَ منتصفِ ليلِ المدينةِ، فاختبارٌ ثبَّتَ `city_timezone` حرفاً
      # وزرعَ صفّاً «قبلَ ساعتَينِ» يقيسُ ساعةَ الحائطِ لا الدالّةَ: سقطَ فحصانِ
      # في `F3-07` عندَ ٢١:١٨Z (٠٠:١٨ بالرياضِ) بلا تغيُّرِ منتَجٍ — والتخفيفُ
      # ههنا كذبٌ، فصارَ الحدُّ يُثبَّتُ حساباً (`ADR 0123`).
      - name: لا منطقةَ زمنٍ مُثبَّتةً بحرفٍ في اختبارٍ يقرأُ نافذةً محليّةً (ADR 0123)
        run: bun run scripts/check-wall-clock-window-tests.ts

      - name: سقوطُ حاجزِ النافذةِ مقيسٌ بسالبةٍ مزروعةٍ — منها السطرُ الذي أسقطَ CI (ح-7)
        run: bun test tests/unit/check-wall-clock-window-tests.test.ts

      # وأخبثُ من ذاكَ: **حاجزٌ يقيسُ جارَ المُنفِّذِ لا شِفرتَنا**. توكيدُ نسبةِ
      # الزمنِ في اختبارِ الصمودِ قرأَ ٣٫٢ في التشغيلِ `35124024068` ثمَّ مرَّ
      # بإعادةِ التشغيلِ **بالبصمةِ عينِها** — إنذارٌ على ضجيجٍ، ومعَهُ صمتٌ محتمَلٌ
      # عن نموٍّ حقيقيٍّ يختفي تحتَه. فبُدِّلَت **وحدةُ القياسِ** لا العتبةُ
      # (`DEC-18` · `ADR 0130`): صفوفٌ ممسوحةٌ وكُتَلٌ ملموسةٌ من `pg_stat_database`،
      # والسقفُ ×٣ كما كانَ — ورفعُه مسارٌ محظورٌ يُنفِذُهُ هذا الحاجزُ لا المراجعةُ.
      - name: تدهورُ الصمودِ مقيسٌ بعملِ المحرِّكِ لا بزمنِ الساعةِ (DEC-18 · ADR 0130)
        run: bun run scripts/check-soak-work-measure.ts

      - name: سقوطُ حاجزِ قياسِ التدهورِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ من الستِّ (ح-7)
        run: bun test tests/unit/check-soak-work-measure.test.ts

      # `F9-06` · `OPS-005`: ميزانيةُ عملٍ مطلقةٌ للكتلةِ الدافئةِ الأولى —
      # سقفٌ مقيسٌ يكشفُ الانحدارَ الثابتَ الذي لا تُمسكُهُ نسبةُ `DEC-18`.
      # الانحدارُ الذي يضاعفُ العملَ في الكتلتَينِ معاً يمرُّ أصفاراً في النسبةِ.
      - name: ميزانيةُ العملِ المطلقةِ للانحدارِ الأدائيِّ (F9-06 · OPS-005)
        run: bun run scripts/check-work-budget.ts

      - name: سقوطُ حاجزِ ميزانيّةِ العملِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-work-budget.test.ts

      # `F1-09` · القسمُ 9.9 الصفُّ السابعُ: حدُّ بياناتِ جلسةِ راكبٍ في عشرِ دقائقَ
      # كانَ عهداً مكتوباً لا يفحصُه شيءٌ. وهذا الحاجزُ ساكنٌ يحرسُ **بقاءَ القياسِ**
      # ووحدةَ مصدرِه ومطابقتَه لصفِّ العقدِ — لا صحّةَ نتيجتِه: النتيجةُ تُقاسُ في
      # وظيفةِ التكاملِ على بوّابةٍ وقاعدةٍ وقناةٍ حقيقيّةٍ.
      - name: حدُّ بياناتِ جلسةِ الراكبِ محروسٌ في مصدرٍ واحدٍ (F1-09)
        run: bun run scripts/check-session-data-budget.ts

      - name: سقوطُ حاجزِ حدِّ بياناتِ الجلسةِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-session-data-budget.test.ts

      - name: حكمُ بياناتِ الجلسةِ مبرهَنُ السقوطِ قاعدةً قاعدةً (F1-09 · ح-7)
        run: bun test tests/unit/session-data-budget.test.ts

      # `SEC-15` (الشقُّ المملوكُ للمستودَعِ من `F8-08`): `Dependabot` يُخبِرُ ولا
      # يمنعُ — طلبٌ مفتوحٌ بلا مراجعٍ لا يُسقِطُ بناءً. وهذا الحاجزُ يُشغِّلُ
      # `bun audit` ويُسقِطُ البناءَ على نشرةٍ عندَ حدِّ الشدّةِ بلا إقرارٍ حيٍّ،
      # **ويسقطُ مغلقاً** متى تعذَّرَ القياسُ — فالعجزُ عن القياسِ عطبٌ لا أخضرُ.
      - name: لا تبعيّةَ بنشرةِ ثغرةٍ عندَ الحدِّ بلا إقرارٍ حيٍّ (SEC-15)
        run: bun run scripts/check-dependency-advisories.ts

      - name: سقوطُ حاجزِ نشراتِ الثغراتِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-dependency-advisories.test.ts

      - name: حكمُ نشراتِ الثغراتِ مبرهَنُ السقوطِ قاعدةً قاعدةً (SEC-15 · ح-7)
        run: bun test tests/unit/dependency-advisory-registry.test.ts

      # `ECO-002` (الشقُّ المملوكُ للمستودَعِ من القسمِ 17): «عددُ نداءاتِ مزوّدِ
      # التوجيهِ لكلِّ رحلةٍ» كانَ عهداً بلا رقمٍ، وحاجزُ `CAP-012` يقولُ في وثيقتِه
      # إنَّه «لا يُثبِتُ أنَّ التخزينَ يعملُ». وهذا الحاجزُ يحرسُ **بقاءَ العدِّ**:
      # ملفَّ قياسٍ يعدُّ طلباتِ `HTTP` على منفذٍ حقيقيٍّ من حاويةٍ إنتاجيّةٍ،
      # وسقفاً مُشتَقّاً من شكلِ النافذةِ لا مكتوباً رقماً يُرفَعُ عندَ الضيقِ.
      # والعددُ وحدَه محروسٌ: السعرُ في فاتورةِ مزوّدٍ (`REQ-09` · `[!]`).
      - name: عددُ نداءاتِ التوجيهِ لكلِّ رحلةٍ معدودٌ لا مُقدَّرٌ (ECO-002)
        run: bun run scripts/check-routing-call-budget.ts

      - name: سقوطُ حاجزِ عدِّ النداءاتِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-routing-call-budget.test.ts

      - name: حكمُ ميزانِ النداءاتِ مبرهَنُ السقوطِ قاعدةً قاعدةً (ECO-002 · ح-7)
        run: bun test tests/unit/routing-call-budget.test.ts

      # `ECO-003` (الشقُّ المملوكُ للمستودَعِ من القسمِ 17): «عددُ الرسائلِ الحرجةِ
      # لكلِّ رحلةٍ». والرسالةُ الحرجةُ ههنا **رسالةُ دفعٍ** — تُرسَلُ إلى طرفٍ لم
      # يُرسِل التحديثَ المُوجِبَ لها: تلكَ وحدَها تتوسَّعُ بعددِ السائقينَ وتصطدمُ
      # بحدِّ ≈30 رسالةً/ثانيةً (`TG-001`) فتستدعي بثّاً مدفوعاً. أمّا ردُّ الحوارِ
      # فواحدٌ بواحدٍ مع تحديثٍ واردٍ ولا يتوسَّعُ أبداً، وخلطُهما يُخفي النصفَ
      # النامي وراءَ النصفِ الثابتِ. والعددُ وحدَه محروسٌ: تمكينُ البثِّ المدفوعِ
      # وسعرُه قرارُ مالكٍ (`DEC-04` · `[!]`).
      - name: عددُ رسائلِ تيليجرام لكلِّ رحلةٍ معدودٌ لا مُقدَّرٌ (ECO-003)
        run: bun run scripts/check-telegram-message-budget.ts

      - name: سقوطُ حاجزِ عدِّ الرسائلِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-telegram-message-budget.test.ts

      - name: حكمُ ميزانِ الرسائلِ مبرهَنُ السقوطِ قاعدةً قاعدةً (ECO-003 · ح-7)
        run: bun test tests/unit/telegram-message-budget.test.ts

      # `ECO-004` (الشقُّ المملوكُ للمستودَعِ من القسمِ 17 — الزيادةُ الأولى):
      # «مواردُ رحلةٍ واحدةٍ مقيسةٌ لا مُقدَّرةٌ». والزيادةُ الأولى تقيسُ خمسةَ أسطحِ
      # مواردَ: القاعدةُ (`pg_stat_database` — وحدّةُ `DEC-18`)، والطابورُ
      # (`notification_outbox` + `order_offers`)، و`Redis` (عميلٌ مُحقونٌ يَعُدُّ)،
      # والنقلُ الشبكيُّ (بايتاتُ ردودِ `HTTP`)، والتخزينُ (صفوفٌ مُدخَلةٌ). ولا
      # تُقاسُ النسخُ (`WAL`/`LSN`) — غيرُ مستقرٍّ في CI. والسقوفُ مُشتَقَّةٌ من شكلِ
      # الرحلةِ المُعلَنِ لا مكتوبةٌ رقماً. والعددُ وحدَه محروسٌ: السعرُ في فاتورةِ
      # مزوّدِ سحابةٍ (`REQ-09` · `[!]`).
      - name: مواردُ الرحلةِ مقيسةٌ لا مُقدَّرةٌ — خمسةُ أسطحٍ (ECO-004 · الزيادةُ الأولى)
        run: bun run scripts/check-resource-usage-budget.ts

      - name: سقوطُ حاجزِ مواردِ الرحلةِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-resource-usage-budget.test.ts

      - name: حكمُ ميزانِ المواردِ مبرهَنُ السقوطِ قاعدةً قاعدةً (ECO-004 · ح-7)
        run: bun test tests/unit/resource-usage-budget.test.ts

      # `ECO-008` (الشقُّ المملوكُ للمستودَعِ): «حسّاسيّةُ شكلِ التكلفةِ عندَ ١٠x من
      # حجمِ الرحلاتِ مقيسةً لا مُفترَضةً». عشرُ رحلاتٍ متتابعةٍ بالشكلِ المُعلَنِ
      # نفسِهِ في حاويةٍ واحدةٍ — والسقوفُ الحجميّةُ مُشتقّةٌ من سقوفِ الرحلةِ
      # الواحدةِ (ECO-004) لا مكتوبةٌ رقماً. سؤالُ الشكلِ لا السعةِ: التزامنُ
      # عملُ مختبرِ النشرِ (DEC-17)، والسعرُ في فاتورةِ مزوّدِ سحابةٍ (REQ-09).
      - name: حسّاسيّةُ شكلِ التكلفةِ عندَ ١٠x محروسةٌ شكلاً واشتقاقاً (ECO-008 · الشقُّ المملوكُ للمستودَعِ)
        run: bun run scripts/check-cost-sensitivity.ts

      - name: سقوطُ حاجزِ حسّاسيّةِ التكلفةِ عندَ ١٠x مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-cost-sensitivity.test.ts

      - name: حكمُ حسّاسيّةِ الشكلِ عندَ ١٠x مبرهَنُ السقوطِ قاعدةً قاعدةً (ECO-008 · ح-7)
        run: bun test tests/unit/cost-sensitivity.test.ts

      # حاجزُ حداثةِ وثيقةِ حالةِ النظامِ: يمنعُ أن تبقى `docs/SYSTEM_STATE.md`
      # متقادمةً أكثرَ من أربعةَ عشرَ يومًا. الوثيقةُ هي المرجعُ الحاكمُ لحالةِ
      # المشروعِ الهندسية، وحداثتُها شرطُ صحّةٍ لكلِّ من يقرأُها.
      - name: وثيقةُ حالةِ النظامِ غيرُ متقادمةٍ (حوكمة)
        run: bun run scripts/check-system-state.ts

      - name: سقوطُ حاجزِ حداثةِ وثيقةِ الحالةِ مقيسٌ بسالبةٍ (ح-7)
        run: bun test tests/unit/check-system-state.test.ts

      # حاجزُ مقاماتِ التكلفةِ لكلِّ مستخدمٍ نشطٍ شهريّاً ولكلِّ رحلةٍ (ECO-001
      # · الزيادةُ الأولى): يمنعُ أن يُحذَفَ قياسُ المقاماتِ (المستخدمونَ النشطونَ
      # والرحلاتُ) أو يُفرَّغَ تأكيدُه. والمقاماتُ تُقاسُ على قاعدةٍ حقيقيّةٍ، والسعرُ
      # محقونٌ أو غائبٌ (`REQ-09` · `[!]`).
      - name: حاجزُ مقاماتِ التكلفةِ محروسٌ (ECO-001 · الزيادةُ الأولى)
        run: bun run scripts/check-eco-user-cost.ts

      - name: سقوطُ حاجزِ مقاماتِ التكلفةِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-eco-user-cost.test.ts

      # `ECO-006` (الشقُّ المملوكُ للمستودَعِ من القسمِ 17): «تذاكرُ الدعمِ
      # لكلِّ رحلةٍ معدودةٌ لا مُقدَّرةٌ». يحرسُ بقاءَ عدِّ تذاكرِ الدعمِ في
      # دورةِ حياةِ رحلةٍ واحدةٍ على السِلكِ — والسقفُ مُشتَقٌّ من شكلِ النافذةِ
      # لا رقماً مكتوباً. والسعرُ بيدِ المالكِ (`REQ-09` · `[!]`).
      - name: تذاكرُ الدعمِ لكلِّ رحلةٍ معدودةٌ لا مُقدَّرةٌ (ECO-006 · الشقُّ المملوكُ للمستودَعِ)
        run: bun run scripts/check-support-volume-budget.ts

      - name: سقوطُ حاجزِ تذاكرِ الدعمِ مقيسٌ بسالبةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-support-volume-budget.test.ts

      # `S-3` · `O-2`: يُقاسُ ههنا — في `verify` — أنَّ وظيفةَ Redis تملكُ خادمَها،
      # لأنَّ الوظيفةَ نفسَها لا تقيسُ تهيئتَها: لو انحرفَ منفذُها أو رمزُها لسقطَت
      # على رفضِ اتّصالٍ يُقرَأُ عيباً في كودِ الجلساتِ. وهذا نصٌّ لا شبكةٌ ولا سرٌّ.
      - name: وظيفةُ Redis الحقيقيِّ تملكُ خادمَها ولا تعودُ إلى نقطةٍ بلا خادمٍ (S-3 · O-2)
        run: bun run scripts/check-real-redis-runner.ts

      - name: سقوطُ حاجزِ خادمِ Redis مقيسٌ بانحرافٍ مزروعٍ لا بالمستودَعِ كما هوَ (S-3)
        run: bun test tests/unit/check-real-redis-runner.test.ts

      # `F7-08` · `CAP-011`: عطبُ لوحةِ الإدارةِ **لا يُخفِقُ — يُعرَضُ رقماً**. فلو
      # انقطعَ شوطُ التجميعِ أسبوعاً لَبقيَت الشاشةُ تعملُ وتعرضُ أرقامَ الأسبوعِ
      # الماضي بلا أنّةٍ؛ ولو كُتِبَت نافذةُ الكاتبِ ٢٤ ونافذةُ القارئِ ١٢ لَبقيَت
      # الشاشةُ **فارغةً أبداً** بلا سطرِ خطأٍ؛ ولو صارَ «لا صفَّ لهذه المدينةِ»
      # صفراً لَقُرِئَ «لا نشاطَ» فيُتَّخَذُ قرارٌ على عَدَمٍ. وثلاثتُها تمرُّ في
      # `tsc` وفي المُدقِّقِ وفي كلِّ اختبارٍ لا يعرفُ أنَّه يجبُ أن يسألَ.
      # فالحاجزُ يقيسُ سبعَ قواعدَ: لا رقمَ منشوراً بلا وَسمِ عمرِه، ولا ساعةَ في
      # النطاقِ، ونافذةً بمصدرٍ واحدٍ بينَ الكاتبِ والقارئِ، وجدولاً مُسجَّلاً في
      # القوائمِ المغلقةِ الأربعِ، وكادةً في `JOB_INTERVALS` وحدَها لا في
      # `platform_settings` (وعدٌ لا يُوفى)، ولا اصطناعَ زمنٍ في المحوّلِ، وسقوطاً
      # حيّاً مُعلَناً محروساً بإذنٍ. ولا يُغني عن
      # `tests/integration/admin-metric-snapshots.test.ts`: ذاكَ يقيسُ على
      # PostgreSQL حقيقيٍّ صفّاً لكلِّ مدينةٍ وإعادةَ تنفيذٍ وزمناً من ساعةِ
      # القاعدةِ — وهذا نصٌّ ساكنٌ، والبوّابتانِ معاً (`ADR 0128`).
      - name: عقدُ تجميعاتِ الإدارةِ — لا رقمَ بلا عمرِه ولا نافذتَينِ ولا كادةً في مكانَينِ (F7-08)
        run: bun run scripts/check-admin-metric-snapshot-contract.ts

      - name: سقوطُ حاجزِ تجميعاتِ الإدارةِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (F7-08 · ح-7)
        run: bun test tests/unit/check-admin-metric-snapshot-contract.test.ts

      # `F8-02`: مقياسٌ منشورٌ **عقدٌ** لا تعليقٌ. وأعطابُ المقاييسِ لا
      # يُمسِكُها اختبارٌ أخضرُ: الخدمةُ تعملُ والرقمُ يكذبُ. فالحاجزُ
      # يقيسُ: الفئاتَ التسعَ منشورةً بعائلاتِها، ولا وسمَ غيرَ محدودِ
      # التعدُّدِ (معرّفٌ في وسمٍ يُسقِطُ المُجمِّعَ لا الخدمةَ، فلا يُرى إلّا
      # في الإنتاجِ)، ولا نسبةً محسوبةً تُنشَرُ رقماً ثالثاً يناقضُ بسطَه
      # ومقامَه، وزمنَ الإسنادِ من الختمِ المكتوبِ لا من مؤقِّتٍ يُفقَدُ بإعادةِ
      # التشغيلِ، ونافذتَه منشورةً معَ أرقامِه، والمسارَ محروساً بسرٍّ.
      - name: عقدُ المقاييسِ الأساسيّةِ — لا وسمَ ينفجرُ ولا نسبةَ تُنشَرُ رقماً (F8-02)
        run: bun run scripts/check-core-metrics.ts

      - name: سقوطُ حاجزِ المقاييسِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (F8-02 · ح-7)
        run: bun test tests/unit/check-core-metrics.test.ts tests/unit/observability-edge-metrics.test.ts

      # `F8-08` (الضابطُ الأوّلُ): كلُّ مسارٍ يقرأُ معرِّفَ كائنٍ يحملُ هويّةَ
      # ناظرِه إلى طبقةِ التطبيقِ، أو يُسجَّلُ استثناؤه بصنفِه وسببِه ودليلِه.
      # وثغرةُ المِلكِيَّةِ لا تُسقِطُ اختباراً ولا تظهرُ في سجلٍّ: المسارُ يعملُ
      # ويُعيدُ ٢٠٠ وجسمُه صفٌّ لغيرِ سائلِه (`ADR 0132`).
      - name: التفويضُ على مستوى الكائنِ في كلِّ مسارٍ (F8-08 · الضابطُ الأوّلُ)
        run: bun run scripts/check-object-authorization.ts

      - name: سقوطُ حاجزِ تفويضِ الكائنِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (F8-08 · ح-7)
        run: bun test tests/unit/check-object-authorization.test.ts

      # `F8-08` (الرِجلُ الثانيةُ): نصُّ البندِ يشترطُ «١٦ ضابطاً» ولا قائمةَ لها
      # في المستودَعِ كلِّه. وبندٌ يُقاسُ إتمامُه بعددٍ بلا قائمةٍ لا يُغلَقُ
      # بصدقٍ ولا يُطعَنُ في دعوى إغلاقِه. فهذا سِجلٌّ مغلقٌ: العددُ مفروضٌ،
      # ودعوى البناءِ تشتري دليلاً وحاجزاً موجودَينِ، والرمزُ مُشتَقٌّ لا
      # مكتوبٌ بيدٍ (`ADR 0133`).
      - name: سِجلُّ ضوابطِ الأمنِ الستّةَ عشرَ ورمزُ البندِ مُشتَقٌّ منهُ (F8-08 · الرِجلُ الثانيةُ)
        run: bun run scripts/check-security-controls.ts

      - name: سقوطُ حاجزِ سِجلِّ الضوابطِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (F8-08 · ح-7)
        run: bun test tests/unit/check-security-controls.test.ts

      # `SEC-07` · ADR 0139: الاكتشافُ **معكوسٌ** — تُكتشَفُ المساراتُ المكشوفةُ من
      # القرصِ (93 مساراً) ثمَّ تُطابَقُ بسِجلِّ السياساتِ، فمسارٌ جديدٌ في صنفٍ
      # يُوجَبُ فيه حدٌّ ولا حدَّ له ولا إعفاءَ مكتوباً يُسقِطُ البناءَ. وحاجزٌ يفحصُ
      # المحدودَ وحدَه لا يرى المكشوفَ الذي نُسيَ — وذاكَ هوَ العيبُ بعينِه.
      - name: كلُّ مسارٍ مكشوفٍ مُكتَشَفٌ من القرصِ وعليهِ حدٌّ أو إعفاءٌ مكتوبٌ (SEC-07)
        run: bun run scripts/check-rate-limit-coverage.ts

      - name: سقوطُ حاجزِ حدِّ المعدَّلِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (SEC-07 · ح-7)
        run: bun test tests/unit/check-rate-limit-coverage.test.ts

      # ولا يُغني الحاجزُ الساكنُ عن هذا: ذاكَ يُثبِتُ أنَّ الحدَّ **مُعلَنٌ ومُوصَّلٌ
      # في النصِّ**، وهذا يُثبِتُ أنَّ `429` **يخرُجُ** برأسِ `Retry-After` على ردٍّ
      # فعليٍّ من موجِّهٍ حقيقيٍّ. ولو أُرجِعَ `null` من دالّةِ الردِّ لَبقيَ الأوّلُ
      # أخضرَ وسقطَ هذا.
      - name: حدُّ المعدَّلِ مقيسٌ على الردِّ الفعليِّ لا على السِجلِّ (SEC-07)
        run: bun test tests/unit/rate-limit-enforced.test.ts

      # ومساراتُ مركبةِ السائقِ كانَت تُخدَمُ على الجِذرِ لا على ما توصفُه الوثيقةُ
      # (`ح-8` · 2026-09-17): حاجزُ العقدِ يقرأُ النصَّ ولا يرى ما يُركِّبُه `app.route`.
      - name: مساراتُ مركبةِ السائقِ تُخدَمُ حيثُ توصفُ (F3-07)
        run: bun test tests/unit/driver-vehicle-mount-paths.test.ts

      # `SEC-10` · ADR 0140 (ومعَ `ADR 0006` القائمِ): الشرطُ الحاكمُ كانَ مكتوباً في
      # وثيقةٍ وحدَها — ووثيقةٌ لا تُسقِطُ بناءً. فأيُّ سياسةٍ لِـ`anon` أو
      # `authenticated` أو `public`، أو `force`، أو تعطيلٍ، أو مفتاحٍ عامٍّ في
      # المصدرِ، يُسقِطُ البناءَ ههنا. **وغيابُ ملفِّ القياسِ يُسقِطُهُ كذلكَ**: حاجزٌ
      # لا يحرسُ دليلَه يُنسَخُ بحذفِ ملفٍ.
      - name: لا بابَ لدورٍ عامٍّ في القاعدةِ بلا سياسةٍ مُختبَرةٍ (SEC-10)
        run: bun run scripts/check-row-security-condition.ts

      - name: سقوطُ حاجزِ شرطِ أمنِ الصفِّ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ صنفٍ (SEC-10 · ح-7)
        run: bun test tests/unit/check-row-security-condition.test.ts

      # `SEC-06` · ADR 0135: القياسُ **معكوسٌ** — تُكتشَفُ أسطحُ الصفحاتِ من القرصِ
      # ثمَّ تُطابَقُ بالسِجلِّ، فصفحةٌ جديدةٌ بلا وسيطِ ترويساتٍ تُسقِطُ البناءَ.
      # وحاجزٌ يفحصُ الموجودَ وحدَه لا يكشفُ الناقصَ أبداً.
      - name: كلُّ سطحِ صفحةٍ مُكتَشَفٌ من القرصِ وعليهِ وسيطُ الترويساتِ بلا شرطٍ (SEC-06)
        run: bun run scripts/check-security-headers.ts

      - name: سقوطُ حاجزِ الترويساتِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (SEC-06 · ح-7)
        run: bun test tests/unit/check-security-headers.test.ts

      # ولا يُغني الحاجزُ الساكنُ عن هذا: ذاكَ يُثبِتُ أنَّ الترويساتِ **مكتوبةٌ**،
      # وهذا يُثبِتُ أنَّها **تخرُجُ** على ردٍّ فعليٍّ — وبينَهما فرقُ جنسٍ لا درجةٍ.
      - name: ترويساتُ الصفحةِ العامّةِ مقيسةٌ على الردِّ الفعليِّ لا على النصِّ (SEC-06)
        run: bun test tests/unit/page-headers-measured.test.ts

      # `SEC-12` · ADR 0136: الاكتشافُ معكوسٌ — تُقرأُ تعاريفُ الدوالِّ كلُّها من
      # الهجراتِ ثمَّ تُطابَقُ بسِجلٍّ مغلقٍ، فدالَّةٌ مُتسلِّطةٌ كاتبةٌ جديدةٌ بلا
      # أثرٍ تُسقِطُ البناءَ. **والحُكمُ يُكتشَفُ مُنابَاً** أيضاً، فمَن أوكلَ
      # حُكمَ الدورِ إلى دالَّةٍ أخرى لا يُفلِتُ من الاكتشافِ.
      - name: كلُّ دالَّةِ فاعلٍ مُتسلِّطٍ مُكتشَفةٌ من الهجراتِ وتكتبُ أثراً باسمٍ مُعلَنٍ (SEC-12)
        run: bun run scripts/check-audit-actions.ts

      - name: سقوطُ حاجزِ الأثرِ التدقيقيِّ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (SEC-12 · ح-7)
        run: bun test tests/unit/check-audit-actions.test.ts

      # `F8-06` · ADR 0138: كُتّابُ حالةِ الدفعِ **مُكتشَفونَ من الهجراتِ** وحالاتُ
      # الدفعِ من **قيدِ المخطَّطِ** — فكاتبٌ جديدٌ بلا طورٍ مقيسٍ، أو حالٌ تُضافُ
      # إلى القيدِ بلا قياسٍ ولا إعلانِ دَينٍ، يُسقِطُ البناءَ ههنا.
      - name: كلُّ كاتبِ حالةِ دفعٍ مُكتشَفٌ وله طورٌ مقيسٌ في المصفوفةِ (F8-06)
        run: bun run scripts/check-payment-lifecycle-matrix.ts

      - name: سقوطُ حاجزِ مصفوفةِ الدفعِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (F8-06 · ح-7)
        run: bun test tests/unit/check-payment-lifecycle-matrix.test.ts

      # `S-4` · `O-7`: الوثيقةُ وسيلةٌ لا تسليمٌ. سقّاطةٌ تمنعُ نموَّ التوثيقِ:
      # الجديدُ محدودٌ بسقفٍ، والمُستثنى ينقصُ ولا ينمو، والمُخرَجُ الخامُّ في
      # `docs/evidence/archive/` وحدَه — بلا حذفِ دليلٍ (`ح-2`).
      - name: ميزانُ التوثيقِ سقّاطةٌ لا سقفٌ يُرفَعُ (S-4 · O-7)
        run: bun run scripts/check-docs-budget.ts

      - name: سقوطُ حاجزِ ميزانِ التوثيقِ مقيسٌ بخرقٍ مزروعٍ لا بالمستودَعِ كما هوَ (S-4)
        run: bun test tests/unit/check-docs-budget.test.ts

      # D-15: ملفّاتُ القوالبِ الفارغةُ (export {};) مُقيسةٌ — لا يُستوردُ منها
      # في الإنتاجِ. القالبُ الفارغُ مشروعٌ ما دامَ لا يُستعملُ.
      - name: لا استيرادَ من ملفّاتٍ فارغةٍ في الإنتاجِ (D-15)
        run: bun run scripts/check-scaffold-sprawl.ts

      - name: منع أي جدول بلا city_id في المخططات
        run: |
          bun run scripts/check-migrations.ts

      - name: سلامةُ الهجرات — الأطوارُ والفهارسُ والقيودُ والاسترجاعُ (F7-07)
        run: bun run scripts/check-migration-safety.ts

      # المُطبِّقُ المُعتمَدُ طريقاً واحداً: حلقةُ `psql` العاريةُ رجعَت إلى main
      # مرّةً بحلِّ تعارضٍ لا بكتابةِ كودٍ، فلذلكَ المنعُ آليٌّ لا وصيةٌ.
      - name: منعُ تطبيقِ هجراتٍ خارجَ المُطبِّقِ الآمنِ (F7-07 · ADR 0068)
        run: bun run scripts/check-migration-applier.ts

      - name: تغطية فهارس الاستعلامات الساخنة (F7-02)
        run: bun run scripts/check-hot-query-index-coverage.ts

      - name: تطابق قواميس اللغات
        run: bun run scripts/check-i18n.ts

      - name: عزل طبقة الذكاء الاصطناعي — اتجاه اعتماد واحد
        run: bun run scripts/check-agent-core-isolation.ts

      - name: حدّ النطاق — مطابقة المسار خارج الإصدار (ADR 0025)
        run: bun run scripts/check-map-matching-scope.ts

      - name: تفرّد أرقام القرارات المعمارية (ADR 0038)
        run: bun run scripts/check-adr-numbering.ts

      # جردٌ يسكنُ وثيقةً وحدَه يتقادمُ بلا سطرٍ خاطئٍ: هجرةٌ تُضيفُ جدولاً
      # فيبقى غيرَ مُصنَّفٍ، ويُقرأُ الجردُ بعدَها «كاملاً» فتُبنى عليه مصفوفةُ
      # الهجرةِ وعقدُ الهويّةِ ونموذجُ المهمّةِ. (ADR 0080)
      - name: جردُ حدودِ WASLA شاملٌ ومُطابِقٌ للمخطّط (W-1 / ADR 0080)
        run: bun run scripts/check-boundary-audit.ts

      - name: بناء الصورتين قابل لإعادة الإنتاج (قفل مجمَّد + استيرادات محلولة)
        run: bash scripts/verify-container-build.sh

      - name: كلّ متغيّر بيئة موثَّق مقروءٌ فعلاً في الكود
        run: bun run scripts/check-env-drift.ts

      - name: لا اختبارَ ينجح بترتيبِ الملفّات — تفعيلُ المدينة مكتملٌ في عبارته
        run: bun run scripts/check-test-city-activation.ts

      - name: لا حاويةَ اختبارٍ تُبنى لكلِّ اختبارٍ وتُغلق مرّةً واحدة
        run: bun run scripts/check-test-container-lifecycle.ts

      - name: عقدُ المخطّط يطابق ما يناديه الكود (فحصُ الجهوزية يقرؤه)
        run: bun run scripts/check-schema-contract.ts

      - name: كلُّ استدعاءِ تيليجرامٍ عبر طبقةِ التغليف وحدَها (F1-02 · ADR 0031)
        run: bun run scripts/check-telegram-wrapper-isolation.ts

      - name: لا سرَّ ولا بيانَ اعتمادٍ يُمرَّر إلى سجلٍّ (F1-03)
        run: bun run scripts/check-secret-logging.ts

      - name: رمزُ التجديدِ في التخزينِ الآمنِ وحدَه ورمزُ الوصولِ في الذاكرة (F1-04 · القسم 4.4)
        run: bun run scripts/check-session-storage-policy.ts

      - name: الدورُ من الخادمِ وحدَه — لا تخزينَ ولا افتراضَ ولا دورَ من الطلب (F1-05)
        run: bun run scripts/check-viewer-role-authority.ts

      - name: شاشاتُ الحالاتِ بلا استقصاءٍ دوريٍّ ومواضعُها مفردة (F1-07 · ADR 0035 §4)
        run: bun run scripts/check-system-screens-policy.ts

      - name: القياسُ لا يخرج من الجهازِ ومعرّفُ الطلبِ من الخادمِ وحدَه (F1-08 · ADR 0043)
        run: bun run scripts/check-telemetry-policy.ts

      # يقرأ `apps/miniapp/dist` فلا يصلُح قبلَ خطوةِ البناءِ أعلاه. ومفحوصُه ثلاثةُ
      # صفوفٍ من ثمانيةٍ: الحملُ الأوّلُ · الحزمُ المؤجَّلةُ · طلباتُ أوّلِ رسمٍ. والأزمنةُ
      # (FCP · LCP · زمنُ التفاعلِ) لا تُقاس ههنا ولا يُدَّعى قياسُها — ولذلك بندُ
      # `F1-09` لم يُختَم `[x]`. التفصيلُ في docs/evidence/architecture/F1-09-20260829.md
      - name: ميزانيةُ الأداءِ من مُخرَجِ البناءِ لا من وعدٍ (F1-09 · القسم 9.9 · ADR 0044)
        run: bun run scripts/check-performance-budget.ts

      # يقرأ `apps/miniapp/dist/index.html` أيضاً فلا يصلُح قبلَ البناءِ. ويقارن سياسةَ
      # المُخرَجِ بما تبنيه وحدةُ السياسةِ حرفاً حرفاً، فبصمةُ أنماطٍ متقادمةٌ أو سياسةٌ
      # مكتوبةٌ بيدٍ تُسقِط البناءَ. والوسمُ **أضعفُ من رأسِ الاستجابةِ** ولا يُدَّعى
      # غيرُ ذلك: `frame-ancestors` مُهمَلةٌ فيه ولا مضيفَ للتطبيقِ بعدُ (ADR 0045 §٥).
      - name: النطاقُ واحدٌ ولا أصلَ تنفيذيّاً من نطاقٍ ثالثٍ (F1-10 · TG-005 · ADR 0045)
        run: bun run scripts/check-single-origin-assets.ts

      # يقرأ مواضعَ التجاوزِ من الشيفرةِ ويطابقها بالسجلِّ، ثمّ يتحقّق من ملفِّ سيرِ
      # العملِ أنّ كلَّ تجاوزٍ على مسارٍ حرجٍ له خطوةٌ **تضبط شرطَ تفعيلِه وتُشغِّل مسارَ
      # ملفِّه**. والتجاوزُ غيرُ المشروطِ (`describe.skip(…)` نداءً · `only` · `todo`)
      # ممنوعٌ منعاً مطلقاً ولا يُفتَح له بابُ تسجيلٍ.
      - name: لا تجاوزَ اختبارٍ بلا تصنيفٍ ومالكٍ وشرطِ تفعيلٍ (OPS-009 · القسم 11-د · ADR 0046)
        run: bun run scripts/check-skip-classification.ts

      # يطابقُ قيدَ أنواعِ صندوقِ الصادرِ بالإعلانِ في `packages/shared/config`
      # وبصفوفِ بذرِ السياسةِ في هجرةِ F6-05، ويرفضُ نوعاً مُوجَّهاً إلى مجموعةٍ
      # مُصنَّفاً داخلَ التطبيقِ: لا صندوقَ واردٍ شخصيَّ له، فكتمُه لا يُنتِجُ رسالةً
      # ولا مدخلَ مركزٍ — إعدامٌ صامتٌ لبلاغِ استغاثةٍ لا تهدئةٌ.
      - name: لا نوعَ إشعارٍ بلا قناةٍ مُعلَنةٍ، ولا بلاغَ مجموعةٍ يُكتَم (F6-05 · TG-002)
        run: bun run scripts/check-notification-classification.ts

      # **تصحيحٌ مُضافٌ — `F6-07` (2026-09-09):** حاجزُ `F6-06` كانَ مكتوباً ولا
      # يُشغَّلُ في أيّةِ وظيفةٍ — لا في `bun run ci` ولا ههنا — **فلم يكن حاجزاً**
      # بل ملفًّ يُقرأُ. فُوصِلَ الاثنانِ: طوابيرُ الضغطِ العكسيِّ، ورتبُ
      # المرورِ متطابقةً بينَ الكودِ والقاعدةِ ومقروءةً في المُطالِبِ وفهرسِه.
      - name: حدودُ الطوابيرِ وأصنافُ التأجيلِ مُعلَنةٌ ومُنفَّذةٌ (F6-06 · ADR 0066)
        run: bun run scripts/check-queue-backpressure.ts

      # يطابقُ رتبةَ كلِّ نوعٍ في `packages/shared/config/traffic-priority.ts` بدالّةِ
      # `notification_kind_priority` في الهجراتِ طرفاً لطرفٍ، ويفرضُ أنَّ المجهولَ
      # حرجٌ لا منخفضٌ، وأنَّ `claim_notification_delivery` تقرأُ الرتبةَ قبلَ
      # الأقدميّةِ، وأنَّ للترتيبِ فهرساً — فرتبةٌ في الكودِ لا يقرأُها المُطالِبُ حِبرٌ.
      - name: رتبُ المرورِ متطابقةٌ ومقروءةٌ في المُطالِبِ (F6-07 · القسم 15 · ADR 0071)
        run: bun run scripts/check-traffic-priority.ts

      # آليّةُ بلاغِ المفقودِ (F12-07): تذكرةُ `lost_item` تُفتَحُ دائماً (عقدُ F3-08
      # المحفوظُ)، وصفُّ `lost_item_report` يُودَعُ في الصادرِ مشروطاً بسائقٍ مُسنَدٍ
      # ورحلةٍ منتهيةٍ — في معاملةِ التذكرةِ نفسِها. والحاجزُ يقرأُ نصَّ الهجرةِ فيُثبِتُ
      # القيدَ والبذرَ والرتبةَ والشرطَ ومفتاحَ منعِ التكرارِ والغناءَ الحيَّ. ولا يلمسُ
      # قاعدةً: الأثرُ في اختباراتِ التكاملِ أدناه.
      - name: بلاغُ المفقودِ مشروطٌ ومُغذًى حيًّا ومُفردٌ بتذكرةٍ (F12-07)
        run: bun run scripts/check-lost-item-mechanism.ts

      # F12-09: قدرةُ تزويدِ الهيئةِ بالبيانات — حزمةٌ مُتعلَّمةٌ لا بثٌّ حيٌّ.
      # ثلاثُ دوالَّ في كلِّ هجرةٍ تَحوي F12-09: إنشاءُ الطلبِ وتوليدُ الحزمةِ والموعدُ.
      # والكشفُ الفارغُ لا يُقرأُ نجاحاً (`ح-7`).
      - name: قدرةُ تزويدِ الهيئةِ بالبيانات — ثلاثُ دوالَّ في كلِّ هجرةٍ (F12-09)
        run: bun run scripts/check-authority-data-export.ts

      # F12-08: إبلاغُ حوادثِ اختراقِ البياناتِ الشخصيّةِ — المادةُ ٢٤ من اللائحةِ
      # التنفيذيّةِ لـPDPL. موعدُ ٧٢ ساعةً ثابتٌ، وخمسةُ عناصرَ لإبلاغِ الهيئةِ،
      # وأربعةٌ لإبلاغِ أصحابِ البياناتِ. والكشفُ الفارغُ لا يُقرأُ نجاحاً (`ح-7`).
      - name: إبلاغُ حوادثِ اختراقِ البياناتِ — موعدٌ ٧٢ ساعةً وعناصرُ إبلاغٍ (F12-08)
        run: bun run scripts/check-breach-notification.ts

      # F12-10: امتثال PDPL — أساسُ المعالجةِ وحقوقُ أصحابِ البياناتِ وتقييمُ
      # الأثرِ وضوابطُ النقلِ خارجَ المملكةِ. الموادُّ ٤ و١٠ و١٣ و١٨ و٢٢ و٢٩ و٣١.
      - name: امتثال PDPL — أساسُ معالجةٍ وحقوقٌ وDPIA ونقلٌ (F12-10)
        run: bun run scripts/check-pdpl-compliance.ts

      # F12-12: المرحلةُ الثانيةُ من الفوترةِ الإلكترونيّةِ لـZATCA — UUID، تجزئة،
      # PIH، ICV، UBL 2.1، حالةُ إبلاغ. والكشفُ الفارغُ لا يُقرأُ نجاحاً (`ح-7`).
      - name: امتثال ZATCA المرحلة الثانية — UUID وتجزئة وPIH وUBL (F12-12)
        run: bun run scripts/check-zatca-phase2.ts

      # F9-05: بصمةُ التشغيل — تُكتَبُ بعد كلِّ وظيفةٍ بـ`if: always()`
      # حتى الإخفاقُ يُنتِجُ بصمةً تقولُ «failure» لا صمتاً.
      - name: بصمةُ التشغيل — كتابة (F9-05)
        if: always()
        env:
          JOB_NAME: verify
          CI_VERDICT: ${{ job.status }}
        run: |
          mkdir -p /tmp/run-manifests
          bun run scripts/emit-run-manifest.ts /tmp/run-manifests/verify.json

      # F9-05: الحاجزُ الساكن — يقرأُ البصمةَ ويحكمُ على كمالِ حقولِها.
      - name: بصمةُ التشغيل — حاجز (verify) (F9-05)
        if: always()
        env:
          RUN_MANIFEST_DIR: /tmp/run-manifests
          JOB_NAME: verify
        run: bun run scripts/check-run-manifest.ts

      # F12-06: الشعارُ والباركودُ في المركبة — روابطُ قراءةٍ موقَّعةٌ.
      # وجودُ المسارِ والمُوقِّعِ والقارئِ والعَرضِ في كلِّ ملفٍّ يمسُّه.
      # والكشفُ الفارغُ لا يُقرأُ نجاحاً (`ح-7`).
      - name: الشعارُ والباركودُ في المركبة — روابطُ قراءةٍ موقَّعةٌ (F12-06)
        run: bun run scripts/check-vehicle-logo-barcode.ts

      # يقرأُ نصَّ سكربتِ Lua ونصَّ الدالّةِ الذرّيّةِ ونصَّ الكتابةِ المباشرةِ، فيُسقِطُ
      # البناءَ إن افترقَ مُسنَدُ حارسِ التسلسلِ بينَ المخزنَينِ أو انقلبَ اتّجاهُه، أو
      # ضاعَت التنقيةُ من الدفعةِ، أو بُذِرَ رقمٌ في الشيفرةِ بدلَ `platform_settings`.
      # **ولا يلمسُ قاعدةً ولا Redis**: الأثرُ يُثبَتُ في اختباراتِ التكاملِ أدناه.
      - name: حَكَمٌ واحدٌ على الأحدثِ في المخزنَينِ، ولا رقمَ يُخترَعُ (F4-02 · CAP-009 · ADR 0072)
        run: bun run scripts/check-hot-location-state.ts

      # يمنعُ سقفَ تجمُّعٍ لا تعرفُه ميزانيّةُ الاتّصالاتِ، ودوراً مُعلَناً بلا مُستدعٍ،
      # وتنافرَ `DECLARED_TOPOLOGY` مع `render.yaml`. والعلّةُ أنّ صيغةَ ADR 0056
      # §٣-ج كانت **نصّاً** فتقادمَت بلا أن يُخطِرَ بها شيءٌ: خدمةُ اللوحةِ فُصِلَت
      # بتجمُّعِها والصيغةُ لا تراه. ولا يُقارَنُ المجموعُ بسقفِ المزوِّدِ: ذاكَ مُدخَلٌ
      # من خارجِ المستودعِ، وحاجزٌ أخضرُ على رقمٍ مُخترَعٍ يُطمئنُ زوراً (ح-5).
      - name: لا سقفَ اتّصالاتٍ خارجَ الميزانيّةِ، ولا تنافرَ مع مانيفستِ النشرِ (F7-04 · CAP-004 · ADR 0073)
        run: bun run scripts/check-connection-budget.ts

      # يمنعُ انشقاقَ الناقلِ المشتركِ **صامتاً**: مفتاحُ المجرى كانَ نصّاً حرفيّاً
      # مكرَّراً في حاويتَي البوّابةِ واللوحةِ، وتعديلُه في واحدةٍ دونَ الأخرى لا
      # يُخفِقُ ترجمةً ولا اختباراً — تُقلعُ العمليّتانِ خضراءَ وتنشرُ كلٌّ في مجراها،
      # فلا يرى مشتركو اللوحةِ أحداثَ البوّابةِ أبداً. ويمنعُ كذلكَ ناقلاً محليّاً بلا
      # غلافٍ موزَّعٍ، وغلافاً بلا ماسحٍ مُشغَّلٍ، وبانياً ثانياً في العمليةِ نفسِها.
      # **ولا يلمسُ Redis**: عبورُ الحدثِ بينَ النسخِ يُثبَتُ في اختباراتِ Redis الحقيقيّ.
      - name: مجرى أحداثٍ واحدٌ مشتركٌ لا ناقلُ عمليةٍ منعزلٌ (F4-03 · SCL-004 · ADR 0058)
        run: bun run scripts/check-tracking-event-carrier.ts

      # يمنعُ عودةَ العطبِ الذي دخلَ في `F4-02` **بلا سطرٍ خاطئٍ**: لمّا صارَ
      # الاستمرارُ مجمَّعاً كتبَت الدالّةُ `last_location_at = now()` لحظةَ الإفراغِ،
      # فادّعى العمودُ حداثةً لا يملكُها بمقدارِ دورةِ إفراغٍ كاملةٍ — وتلكَ القيمةُ
      # تُطبَعُ على صفحةِ التتبّعِ العامّةِ `SS-06` «آخرَ تحديثٍ»، ويُقاسُ منها عمرُ
      # الموقعِ في حاجزِ `driver_location_max_age_seconds`. ولا ترجمةٌ تُخفِقُ ولا
      # اختبارٌ يسقطُ عندَ مثلِ هذا. فالحاجزُ يفرضُ حملَ لحظةِ القبولِ من موضعِ
      # الاستقبالِ إلى الكتابةِ. **ولا يلمسُ القاعدةَ**: أنَّ الطابعَ يُكتَبُ كما
      # حُمِلَ يُثبَتُ في اختباراتِ PostgreSQL الحقيقيّ (حالاتُ ٨–١٠).
      - name: زمنُ قبولٍ محمولٌ لا `now()` لحظةَ الإفراغِ (F4-05 · CAP-009 · ADR 0076)
        run: bun run scripts/check-location-acceptance-stamp.ts

      # يمنعُ عودةَ أخطرِ ما وُجدَ في المسارِ: `/sos` كانَ فرعاً في `switch`
      # كسائرِ الفروعِ، والمُوزِّعُ يقرأُ دليلَ المستخدمِ **قبلَ كلِّ أمرٍ** ويردُّ
      # «حدثَ عطلٌ» عندَ إخفاقِها — فبنيةٌ سليمةٌ لكلِّ أمرٍ آخرَ كانت بابَ
      # إسقاطٍ لأخطرِ أمرٍ. وإسقاطُ استغاثةٍ لا يُرى في لوحةٍ ولا يُشتكى
      # منه: من ضغطَ الزرَّ ولم يُجَب لا يفتحُ تذكرةً. فالحاجزُ يفرضُ أنَّ
      # موضِعَ الاستقبالِ انتظارٌ واحدٌ على `triggerSos` بـ`orderId: null`، وأنَّ الأمرَ
      # يُوزَّعُ قبلَ أوّلِ `await`. **ولا يلمسُ القاعدةَ**: أنَّ الطلبَ يُحَلُّ
      # صحيحاً تحتَ القفلِ يُثبَتُ في اختباراتِ PostgreSQL الحقيقيِّ.
      - name: عزلُ مسارِ استقبالِ الاستغاثةِ (F8-05 · ADR 0077)
        run: bun run scripts/check-sos-intake-isolation.ts

      # يمنعُ عودةَ ثلاثةِ عيوبٍ كانت مجتمعةً في المستودعِ: أربعةُ مُصدِراتٍ
      # للسجلِّ بثلاثةِ أشكالٍ متعارضةٍ، وأربعةٌ وسبعونَ رسالةً نصّاً عربيّاً حرّاً
      # لا يُجمَعُ عليها ولا تُصفَّى، وعنوانُ عميلٍ خامٌ ومُعرِّفُ تلغرام خامٌ في
      # سجلاتِ البوابةِ. والسجلُّ لا تُخفِقُ به ترجمةٌ ولا يسقطُ به اختبارٌ:
      # عيبُه لا يُرى إلّا يومَ تُطلَبُ منه إجابةٌ فلا يُجيبُ، أو يومَ يُقرأُ فيه
      # ما لا يجوزُ أن يُكتَبَ. فالحاجزُ يفرضُ: `console` محصورٌ في المُصدِرِ
      # الوحيدِ، ورمزُ الحدثِ حرفيٌّ لاتينيٌّ منقوطٌ، ولا اسمَ حقلٍ شخصيّاً في
      # نداءِ تسجيلٍ، والمُصدِرُ قائمٌ بمضمونِه لا بمُجرَّدِ وجودِه. **ولا يلمسُ
      # القاعدةَ**: تصنيفُ شدّةِ كلِّ حدثٍ وتصديرُه إلى جامعٍ خارجيٍّ بندٌ آخرُ
      # (`F8-02` · `F8-07`)، والحدُّ مُعلَنٌ في ADR 0078.
      - name: سجلاتٌ مُهيكلةٌ بلا بياناتٍ شخصيّةٍ (F8-03 · ADR 0078)
        run: bun run scripts/check-structured-logging.ts

      # سلسلةُ ارتباطٍ واحدةٌ من الحافةِ إلى القاعدةِ وعبرَ الطابورِ: سجلٌّ لا
      # يُربَطُ سطرُه بسطرٍ لا يُجيبُ سؤالَ ليلةِ العطبِ «ما جرى لهذا الطلبِ»؛
      # وإنّما يُقرأُ فيه ألفُ سطرٍ متجاورٍ لا يُعرَفُ أيُّها من أيٍّ. والحاجزُ
      # يفرضُ **بنيةَ** السلسلةِ: عمودٌ وقيدٌ ومُشغِّلٌ لكلِّ جدولٍ موصولٍ، ولا
      # هجرةَ تُدرِجُ المعرِّفَ صريحاً بقيمةٍ من عندِها، واسمُ المتغيّرِ الجلسيِّ
      # في موضعٍ واحدٍ لا يُنسَخُ، وترويسةُ العميلِ الوارِدةُ **لا تُقرأُ أبداً**
      # (`ADR 0043`)، والحافةُ والشوطُ يلفّانِ عملَهما بسياقٍ، والمُصدِرُ يُلحِقُ
      # المعرِّفَ، وكلُّ موضعٍ موصولٍ مُعلَنٍ ملفوفٌ فعلاً. **ولا يلمسُ ناقلَ
      # OTel ولا جامِعَه**: ليسا في المستودعِ (`DEC-17` · `F9-01`)، فالبندُ `[~]`.
      - name: سلسلةُ ارتباطِ الطلبِ (F8-01 · OPS-002 · ADR 0129)
        run: bun run scripts/check-request-correlation.ts

      # صمودُ الاعتماديّاتِ: العطبُ الذي أُغلِقَ ههنا لم يكن سطراً خاطئاً بل
      # سطراً صحيحاً ظاهراً — `new Api(token)` في سبعةِ مواضعَ، يورِّثُ كلُّ واحدٍ
      # منها مهلةَ grammY الافتراضيّةَ: خمسَمئةَ ثانيةٍ. ولا يُخفِقُ بذلكَ اختبارٌ
      # ولا تُنبِّهُ به ترجمةٌ؛ إنّما يُرى يومَ يَبطُؤ المزوّدُ فتُعمِّرُ المقابسُ
      # المُهمَلةُ ثمانيَ دقائقَ وتُعادُ المحاولةُ فوقَها. فالحاجزُ يفرضُ: مصنعٌ
      # واحدٌ لواجهةِ تلغرام، وميزانيّةٌ لكلِّ اعتماديّةٍ مُسمّاةٍ، وحاجزٌ في كلِّ
      # محوّلِ خروجٍ، ومهلةٌ **مقروءةٌ** لا مكتوبةٌ رقماً، و`signal` في كلِّ `fetch`.
      # **ولا يُدَّعى معايرةُ الأرقامِ**: هيَ قراراتٌ مُعلَنةٌ، وقياسُها في `F10`.
      - name: صمودُ الاعتماديّاتِ — مهلةٌ وقاطعٌ وحدُّ تزامنٍ (F8-04 · CAP-006 · ADR 0079)
        run: bun run scripts/check-dependency-resilience.ts

      # عقودُ CORE مُستنسَخةٌ في `docs/contracts/core/` لأنَّ لا نفاذَ متبادلَ بينَ
      # المستودعَينِ (`DEP-CORE-005`)، ونسخةٌ ساكنةٌ من عقدٍ حيٍّ أخطرُ من غيابِه:
      # تُقرأُ كأنّها العقدُ فيُبنى عليها مُغلَّفٌ لا يقبلُه المُستهلِكُ. فالحاجزُ
      # يُطابِقُ حرفَ العقدِ المُستنسَخِ بما تُصدِرُه الطبقةُ وبآلةِ الحالاتِ
      # وبالهجرتَينِ: حقلاً حقلاً، وقيمةَ تعدادٍ قيمةً، وحالةً حالةً. **ولا يدّعي
      # طزاجةَ النسخةِ**: تحقيقُ ذلك آليّاً محجوبٌ بـ`DEP-CORE-005`، والبصماتُ في
      # `docs/contracts/core/PROVENANCE.md` هيَ ما يُقارَنُ يدوياً حتّى يُرفَعَ الحجبُ.
      - name: مطابقةُ عقودِ CORE — مُغلَّفٌ وحمولاتٌ وآلةُ حالاتٍ (W-4 · W-5)
        run: bun run scripts/check-core-contract-parity.ts

      # سلامةُ العقودِ المنقولةِ: تُعادُ بصمةُ كلِّ ملفٍّ في `docs/contracts/core/`
      # وتُقابَلُ بالمكتوبةِ في `PROVENANCE.md`. لزِمَ هذا لأنَّ المُنسِّقَ أعادَ
      # تنسيقَ مخطَّطَينِ منقولَينِ فبطلَت بصمتاهما وبقيَت دعوى «منقولٌ حرفاً»
      # مكتوبةً — دليلٌ كاذبٌ لا تكشفُه عينٌ.
      - name: سلامةُ العقودِ المنقولةِ — بصمةٌ تُعادُ لا تُصدَّقُ
        run: bun run scripts/check-vendored-contract-integrity.ts

      # يقيس التغطيةَ مرّةً أخرى بعدَ خطوةِ Test — عن قصدٍ لا سهواً: خطوةُ Test
      # تُغذّي حاجزَ قراءةِ الإخفاقِ من `/tmp/ci-output.log`، فلا يُمَسُّ أمرُها.
      # والقياسُ ههنا **لا يقيس اختباراتَ التكامل** لأنّها تتجاوز نفسَها بلا قاعدةٍ
      # حقيقيّةٍ في هذه الوظيفة، والحدُّ مُعلَنٌ في `scripts/lib/coverage-registry.ts`.
      - name: بوابةُ تغطيةٍ دنيا على المسارات الحرجة (OPS-005 · القسم 11-د · ADR 0048)
        run: bun run check:coverage

      # **تصحيحٌ مُضافٌ — تكافؤُ الإنفاذِ (2026-09-17 · ADR 0143):** سبعةُ حواجزَ
      # كانَت في `package.json → ci` **ولا تُنادى في أيِّ سيرِ عملٍ** — لا ههنا ولا
      # في غيرِها. ووثيقتانِ تدَّعيانِ صراحةً أنَّ إحداها «حاجزُ CI قائمٌ». وهوَ
      # عينُ عطبِ `F6-07` أعلاه مكرَّراً سبعَ مرّاتٍ: **حاجزٌ لا يُسقِطُ بناءً بعيداً
      # ليسَ حاجزاً**، إنّما عادةٌ محلّيّةٌ تنكسرُ بأوّلِ من ينسى. فأُضيفَت خطواتٍ
      # ههنا، وأُضيفَ حاجزٌ يقيسُ التكافؤَ نفسَه كي لا يتكرَّرَ بلا مُكتشِفٍ.
      - name: مِلكيّةُ الحذفِ لكلِّ جدولٍ حكماً لا أمراً (F2-11 · ADR 0112)
        run: bun run scripts/check-erasure-policy.ts

      - name: سياسةُ الاستبقاءِ مُعلَنةٌ لكلِّ بياناتٍ تُعمَّرُ (F7-06 · CAP-010)
        run: bun run scripts/check-retention-policy.ts

      - name: سياسةُ العمولةِ مكتوبةٌ ومرئيةٌ (F12-17)
        run: bun run scripts/check-commission-policy.ts

      - name: سياسةُ تخزينِ المساراتِ — لا نداءَ لكلِّ نبضة (CAP-012)
        run: bun run scripts/check-route-cache-policy.ts

      - name: أمانُ الاستعادةِ — لا هجرةَ تُكسِرُ الرجوعَ (OPS-010)
        run: bun run scripts/check-rollback-safety.ts

      - name: ثابتُ المثيلِ الواحدِ مفروضٌ في النصِّ لا موصىً به (SCL-002 · ADR 0050)
        run: bun run scripts/check-instance-invariant.ts

      - name: منطقةُ التأجيلِ لا تُستورَدُ ولا تُطبَّقُ ولها README (O-7 · ADR 0095)
        run: bun run scripts/check-deferred-area.ts

      - name: عقدُ شاشةِ اشتراكِ السائقِ (F3-06)
        run: bun run scripts/check-driver-subscription-contract.ts

      - name: عقدُ شاشةِ مركبةِ السائقِ (F3-07)
        run: bun run scripts/check-driver-vehicle-contract.ts

      # والحاجزُ الذي يحرسُ الحواجزَ: يقابلُ المشغِّلَ بسيرِ العملِ بالقرصِ في
      # الاتجاهاتِ الثلاثةِ، ويفكُّ الأسماءَ المتداخلةَ، **ولا يقرأُ تعليقاً تنفيذاً**
      # (وذاكَ بعينِه ما أخفى الخلَلَ في أوّلِ قياسٍ). فلا يتقادمُ بإضافةِ حاجزٍ
      # جديدٍ: مصدرُ حقيقتِه المستودعُ نفسُه لا قائمةٌ تُصانُ يدويًّا.
      - name: كلُّ حاجزٍ يُنفَّذُ حيثُ يُدَّعى — محلّيًّا وفي CI (ADR 0143)
        run: bun run scripts/check-guard-enforcement.ts

      - name: سقوطُ حاجزِ تكافؤِ الإنفاذِ مقيسٌ بسالبةٍ مزروعةٍ لكلِّ قاعدةٍ (ح-7)
        run: bun test tests/unit/check-guard-enforcement.test.ts

      # وحاجزُ التكافؤِ أعلاه يقيسُ **العضويّةَ** لا **الصلاحيّةَ**: أنَّ الحاجزَ
      # مذكورٌ في الجانبَينِ، لا أنَّه يُنادى كما كُتِبَ. وذاكَ حدُّه المُعلَنُ الذي
      # أوقعَ عطباً حقيقيًّا: `check-no-skipped-tests.ts` أُدرِجَ في السلسلةِ بلا
      # وسيطِ سجلٍّ فكانَ يُخفِقُ في كلِّ نداءٍ محلّيٍّ، **ودُمِجَ خضراءَ** لأنَّ CI لا
      # تُشغِّلُ سلسلةَ `bun run ci`. فهذا الحاجزُ يسدُّ ذاكَ الحدَّ سدًّا ساكناً.
      - name: كلُّ أمرٍ في المشغِّلِ يُنادى كما كُتِبَ (D-24)
        run: bun run scripts/check-runner-command-validity.ts

      - name: سقوطُ حاجزِ صلاحيّةِ النداءِ مقيسٌ بسالبةٍ مزروعةٍ ومنها العطبُ التاريخيُّ (ح-7)
        run: bun test tests/unit/check-runner-command-validity.test.ts

      # F9-04: حتميّةُ بذرةِ القياس — الدوالُّ الصرفةُ تُنتجُ البصمةَ نفسَها.
      - name: حتميّةُ بذرةِ القياس — بصمةٌ ثابتةٌ وUUIDv5 صالح (F9-04)
        run: bun run scripts/check-bench-seed-determinism.ts

      - name: سقوطُ حاجزِ حتميّةِ البذرةِ مقيسٌ بسالبةٍ مزروعةٍ (ح-7)
        run: bun test tests/unit/bench-determinism.test.ts

      - name: لا نصّ ظاهر للمستخدم مكتوب داخل منطق الحوار
        run: |
          if grep -rnE '(reply\(sender, ")[^t]' packages/application/bots/ ; then
            echo "❌ نصّ مباشر داخل منطق الحوار: كل نصّ يمرّ عبر i18n"; exit 1;
          fi
          echo "✅ كل نصوص الحوار من القواميس"

  integration:
    name: تكامل على PostgreSQL حقيقي
    runs-on: ubuntu-latest
    # أطولُ تشغيلٍ ناجحٍ مقيسٍ لهذه الوظيفةِ ≈ ٣ دقائقَ (ADR 0122).
    timeout-minutes: 25
    # يُلزم الرقيبَ بالإخفاق لو ضاع رابطُ القاعدة من هذه الوظيفة: اختباراتُ
    # التكامل تُتخطّى بصمتٍ حين يغيب، فيُعلَن الفحصُ أخضرَ بلا تجريبِ SQL أصلاً.
    env:
      CI_REQUIRE_DB: "1"
    services:
      postgres:
        image: postgis/postgis:17-3.5
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: waslah
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s --health-timeout 5s --health-retries 20
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Install
        run: bun install --frozen-lockfile

      - name: أدوار Supabase التي تفترضها الهجرات
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d waslah -v ON_ERROR_STOP=1 <<'SQL'
          do $$ begin
            if not exists (select 1 from pg_roles where rolname = 'anon') then
              create role anon nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'authenticated') then
              create role authenticated nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'service_role') then
              create role service_role nologin noinherit bypassrls;
            end if;
          end $$;
          SQL

      # `BUG-017`: autovacuum was inflating `pg_stat_database` block-touch
      # counters during the ride-soak test. The soak test measures engine
      # work via `blks_read + blks_hit` from `pg_stat_database`, which
      # includes ALL block accesses — including autovacuum's. On a fresh
      # CI database with default autovacuum settings, autovacuum can trigger
      # during the warm-baseline window (rides 11–20), inflating the
      # `blocksTouched` delta by up to 161% (32481 vs 12430 on the same
      # commit, same code — run 35428393006 first attempt vs rerun).
      # The asymmetry (161% blocks vs 61% rows) is the signature of
      # autovacuum: it scans many blocks looking for dead tuples but
      # doesn't return tuples through the normal query path.
      # Disabling autovacuum here is a root-cause fix, not a budget raise:
      # the test measures application query work, not PostgreSQL
      # maintenance work. The budget (`scripts/lib/work-budget.ts`)
      # is not touched.
      - name: تعطيل autovacuum لقياسٍ حتميٍّ في اختبار الصمود (BUG-017)
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d waslah -v ON_ERROR_STOP=1 <<'SQL'
          alter system set autovacuum = off;
          select pg_reload_conf();
          SQL


      # مصدرُ الحقيقةِ الواحدُ لتطبيقِ الهجراتِ: `scripts/migrate.ts` (F7-07 · ADR 0068).
      # وحلقةُ `psql` العارية أُزيلَت لأنّها كانت تُطبِّق على CI بطريقٍ يخالف طريقَ
      # الإنتاج، فيصير الأخضرُ شاهداً على غيرِ ما يُشغَّل. والمُطبِّقُ يحكم على كلِّ
      # ملفٍّ قبل الاتّصال، ويضبط `lock_timeout`، ويملك المعاملةَ.
      #
      # تصحيحٌ بالإضافةِ (F5-06 · 2026-09-09): هذه الخطوةُ عادت إلى حلقةِ `psql`
      # في `a16bf97` — حلُّ تعارضٍ في دمجِ main إلى فرعِ F7-02 أخذَ نسخةَ الملفِّ
      # السابقةَ لـF7-07 — فبقيَ ADR 0068 يقولُ ما لا يفعلُه CI. أُعيدَت هنا،
      # وحاجزُ `scripts/check-migration-applier.ts` يمنعُ عودةَ العيبِ صامتاً.
      - name: تطبيق الهجرات بالمُطبِّقِ الآمنِ
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun run scripts/migrate.ts

      # `W-9` · ADR 0088: يُقاسُ **عجزُ** تمرينِ التحوُّلِ عن الكتابةِ على قاعدةٍ
      # حقيقيّةٍ — لا يُفترَضُ. والمُنفِّذُ يُخفِقُ لو نجحَت كتابةٌ داخلَ معاملتِه،
      # ولو زعمَ تمريناً تامّاً. ولا تحوُّلَ يجري: `B-5` مفتوحٌ والرفضُ بالإنشاءِ.
      - name: تمرينُ التحوُّلِ يرفضُ بالإنشاءِ ولا يكتبُ حرفاً (W-9 · ADR 0088)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun run scripts/rehearse-cutover.ts

      # `pg_dump` يرفض خادماً أحدثَ منه إصداراً رئيسياً، واختبارُ تحقّقِ الاستعادة
      # يستدعيه فعلاً على خدمةِ PostgreSQL 17 — أمّا صورةُ العامل فتحمل أدواتَ
      # إصدارٍ أقدم. النتيجةُ إخفاقٌ لا يظهر محلّياً حيث النسختان متوافقتان، فيبدو
      # كعيبٍ في منطقِ النسخ الاحتياطيّ وهو عيبُ بيئةٍ صِرْف. فتُثبَّت أدواتُ العميل
      # من مستودعِ PostgreSQL الرسميّ، ثم يُقارَن الإصدارانِ ويسقط البناءُ بالسببِ
      # مكتوباً إن اختلفا — لا سقوطاً صامتاً ولا تخطّياً للاختبار.
      - name: أدواتُ عميلِ PostgreSQL مطابقةٌ لإصدارِ الخادم
        env:
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          server_major="$(psql -h localhost -U postgres -d waslah -tAc 'show server_version' | cut -d. -f1 | tr -d '[:space:]')"
          echo "الخادم: إصدار رئيسيّ $server_major"
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -fsSL -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
            https://www.postgresql.org/media/keys/ACCC4CF8.asc
          codename="$(lsb_release -cs)"
          repo="https://apt.postgresql.org/pub/repos/apt ${codename}-pgdg main"
          signed="signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc"
          printf 'deb [%s] %s\n' "$signed" "$repo" | sudo tee /etc/apt/sources.list.d/pgdg.list > /dev/null
          sudo apt-get update -qq
          sudo apt-get install -y -qq "postgresql-client-${server_major}"
          echo "/usr/lib/postgresql/${server_major}/bin" >> "$GITHUB_PATH"
          export PATH="/usr/lib/postgresql/${server_major}/bin:$PATH"
          for tool in pg_dump pg_dumpall pg_restore psql; do
            version="$("$tool" --version)"
            echo "$tool: $version"
            tool_major="$(echo "$version" | grep -oE '[0-9]+' | head -1)"
            if [ "$tool_major" != "$server_major" ]; then
              echo "::error::$tool إصداره $tool_major والخادم $server_major — pg_dump يرفض خادماً أحدثَ منه، فتحقّقُ الاستعادة يُخفق لسببِ بيئةٍ لا لسببِ منطق."
              exit 1
            fi
          done

      - name: تمرين مسار العودة — الهجرات واحدةً واحدةً على قاعدة حقيقية
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun run scripts/rollback-schema-drill.ts

      # يُثبِتُ الحاجزُ الساكنُ أنَّ الإدراجَ **مكتوبٌ**، وهذا يُثبِتُ أنَّ الصفَّ
      # **يُكتَبُ** عندَ النجاحِ **ولا يُكتَبُ** عندَ الرفضِ — وبينَهما فرقُ جنسٍ
      # لا درجةٍ. وخطوةٌ باسمِها كي يُقرأَ الحكمُ مفرَداً لا مُبتلَعاً في مجموعةٍ.
      - name: أثرُ الأفعالِ المُتسلِّطةِ مقيسٌ بالأثرِ لا بالنصِّ (SEC-12)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/audit-trail-authority.test.ts

      # `F8-06`: أطوارُ دورةِ حياةِ الدفعِ (نجاحٌ · فشلٌ · معلّقٌ · مكرّرٌ · استرجاعٌ ·
      # تناقضٌ) **يُشغِّلُها المحرِّكُ** لا الحاجزُ الساكنُ. وخطوةٌ باسمِها كي يُقرأَ
      # حُكمُها مفرَداً لا مُبتلَعاً في حزمةٍ من خمسٍ وخمسينَ.
      - name: مصفوفةُ أطوارِ دورةِ حياةِ الدفعِ على محرِّكٍ حقيقيٍّ (F8-06)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/payment-lifecycle-matrix.test.ts

      # `F12-08`: إبلاغُ حوادثِ اختراقِ البياناتِ الشخصيّةِ — المادةُ ٢٤ من اللائحةِ
      # التنفيذيّةِ لـPDPL. خمسُ حالاتٍ على PostgreSQL حقيقيٍّ: تسجيلُ الحادثِ ·
      # موعدُ ٧٢ ساعةً · تقييمُ الخطرِ · كشفُ الانقضاءِ · تسجيلُ إبلاغَي الهيئةِ
      # وأصحابِ البياناتِ. وخطوةٌ باسمِها كي يُقرأَ حُكمُها مفرَداً.
      - name: إبلاغُ حوادثِ اختراقِ البياناتِ على PostgreSQL حقيقيٍّ (F12-08)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/breach-notification.test.ts

      # `F12-10`: امتثال PDPL — أساسُ المعالجةِ، حقوقُ أصحابِ البياناتِ، تقييمُ
      # الأثرِ، وضوابطُ النقلِ خارجَ المملكةِ. الموادُّ ٤ و١٠ و١٣ و١٨ و٢٢ و٢٩ و٣١.
      # ستُّ حالاتٍ على PostgreSQL حقيقيٍّ: نشاطٌ بلا أساسٍ · نشاطٌ عاليُ الخطرِ
      # بلا DPIA · DPIA معتمدٌ · نقلٌ مرفوضٌ افتراضياً · نقلٌ معتمدٌ غيرُ منتهٍ ·
      # طلبُ حقٍّ له مهلةٌ وحالاتُ انتقالٍ. وخطوةٌ باسمِها كي يُقرأَ حُكمُها مفرَداً.
      - name: امتثال PDPL على PostgreSQL حقيقيٍّ (F12-10)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/pdpl-compliance.test.ts

      # F12-12: اختباراتُ المرحلةِ الثانيةِ من امتثال ZATCA على PostgreSQL حقيقيٍّ.
      # بذرةُ PIH · دوالُ UUID والتجزئة والعدّاد · UBL XML · سلسلةُ الفواتير.
      - name: امتثال ZATCA المرحلة الثانية (F12-12)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/zatca-phase2.test.ts

      # `SEC-10` · ADR 0140: الحاجزُ الساكنُ يُثبِتُ أنَّ `RLS` **مكتوبٌ**، وهذا
      # يُثبِتُ ما **تفعلُه القاعدةُ** بدورٍ `nobypassrls` يُنشِئُه الاختبارُ: منعٌ
      # شاملٌ حيثُ لا سياسةَ · إذنٌ شاملٌ لِـ`service_role` · وتجاوزُ المالكِ الذي
      # تردُّه `force` وحدَها. وخطوةٌ باسمِها كي يُقرأَ الحكمُ مفرَداً؛ ولو صارَ
      # `RLS` زخرفةَ مخطَّطٍ لَبقيَ الساكنُ أخضرَ وسقطَ هذا.
      - name: أثرُ أمنِ الصفِّ مقيسٌ بدورٍ غيرِ مالكٍ لا موصوفٌ (SEC-10)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/row-security-effect.test.ts

      # `F1-09`: استهلاكُ جلسةِ راكبٍ يُقاسُ ردّاً ردّاً وإطاراً إطاراً على بوّابةٍ
      # وقاعدةٍ وقناةِ `socket.io` حقيقيّةٍ، ثمَّ يُحاكَمُ بحدِّ القسمِ 9.9. وخطوةٌ
      # باسمِها كي يُقرأَ حُكمُها — والأرقامُ المقيسةُ — مفرَداً لا مُبتلَعاً في حزمةٍ.
      - name: استهلاكُ بياناتِ جلسةِ راكبٍ في عشرِ دقائقَ مقيسٌ لا مُقدَّرٌ (F1-09)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/rider-session-data-budget.test.ts

      # `ECO-002`: نداءاتُ مزوّدِ التوجيهِ تُعَدُّ على **السِلكِ** — خادمٌ يُصغي على
      # منفذٍ حقيقيٍّ يعدُّ الطلباتَ، وبوّابةٌ من `buildContainer` بضبطِ `osrm`،
      # وقاعدةٌ حقيقيّةٌ. وخطوةٌ باسمِها كي يُقرأَ الرقمُ مفرَداً: صفرُ نداءاتٍ في
      # ستّينَ نبضةً، ونداءاتٌ منعَها التخزينُ، ومجموعٌ تحتَ سقفٍ مُشتَقٍّ.
      - name: عددُ نداءاتِ التوجيهِ في رحلةٍ واحدةٍ معدودٌ على السِلكِ (ECO-002)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/routing-call-budget.test.ts

      # `ECO-003`: رسائلُ تيليجرام تُعَدُّ على **السِلكِ** — مُرسِلٌ يُلتقَطُ خروجُه
      # نصّاً وصورةً ودبّوساً، وبوّابةٌ من `buildContainer` تمرُّ بالـwebhook وصندوقِ
      # الصادرِ. وخطوةٌ باسمِها كي يُقرأَ الرقمُ مفرَداً: خمسُ رسائلِ دفعٍ في رحلةٍ
      # كاملةٍ من سقفٍ مُشتَقٍّ، ونصيبُ البثِّ وحدَه ينمو بعددِ السائقينَ.
      - name: عددُ رسائلِ تيليجرام في رحلةٍ واحدةٍ معدودٌ على السِلكِ (ECO-003)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/telegram-message-budget.test.ts

      # `ECO-004` (الزيادةُ الأولى): مواردُ رحلةٍ واحدةٍ تُقاسُ على السِلكِ —
      # `pg_stat_database` للصفوفِ والكُتَلِ، و`notification_outbox` + `order_offers`
      # للطابورِ، وعميلُ `Redis` مُحقونٌ يَعُدُّ، وبايتاتُ ردودِ `HTTP` للنقلِ،
      # وعدُّ الصفوفِ المُدخَلةِ للتخزينِ. وخطوةٌ باسمِها كي يُقرأَ الرقمُ مفرَداً.
      - name: مواردُ رحلةٍ واحدةٍ مقيسةٌ على السِلكِ — خمسةُ أسطحٍ (ECO-004 · الزيادةُ الأولى)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/resource-usage-budget.test.ts

      # `ECO-008` (الشقُّ المملوكُ للمستودَعِ): عشرُ رحلاتٍ متتابعةٍ بالشكلِ المُعلَنِ
      # في حاويةٍ واحدةٍ — يقيسُ هل يبقى نصيبُ الرحلةِ من المواردِ ثابتاً معَ
      # توالي الرحلاتِ (نموٌّ خطّيٌّ) أم يتضخّمُ. السقوفُ الحجميّةُ مُشتقّةٌ من
      # سقوفِ الرحلةِ الواحدةِ (ECO-004) — لا سقوفاً جديدةً تُكتَبُ رقماً.
      - name: حسّاسيّةُ شكلِ التكلفةِ عندَ ١٠x من حجمِ الرحلاتِ مقيسةٌ على السِلكِ (ECO-008)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/cost-sensitivity.test.ts

      - name: مقاماتُ التكلفةِ لكلِّ مستخدمٍ نشطٍ ولكلِّ رحلةٍ محروسةٌ (ECO-001 · الزيادةُ الأولى)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/eco-user-cost.test.ts

      # `ECO-006` (الشقُّ المملوكُ للمستودَعِ): تذاكرُ الدعمِ في دورةِ حياةِ
      # رحلةٍ واحدةٍ تُعَدُّ على السِلكِ — لا تُقدَّرُ ولا تُخمَّنُ. والسقفُ مُشتَقٌّ
      # من شكلِ النافذةِ، والسعرُ بيدِ المالكِ (`REQ-09`).
      - name: تذاكرُ الدعمِ لكلِّ رحلةٍ مقيسةٌ على السِلكِ (ECO-006 · الشقُّ المملوكُ للمستودَعِ)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/support-volume-budget.test.ts

      - name: فعلُ تعذُّرِ الإكمالِ على PostgreSQL حقيقيّة (PD-020)
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun test tests/integration/driver-cannot-complete.test.ts

      - name: اختبارات التكامل على قاعدة حقيقية
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: set -o pipefail; bun run test:integration 2>&1 | tee /tmp/ci-output.log

      - name: تفاصيل الإخفاق في تعليقٍ مقروء بلا سجلّات
        if: failure()
        run: |
          # الإخفاقُ الذي لا يُقرأ سببُه يُعالَج بالتخمين. وسجلّاتُ الوظيفة تُحمَّل
          # من تخزينٍ خارجيٍّ موقَّعٍ لا يبلغه كلُّ مراجع، فذيلُ المخرجات يُرفَع هنا
          # كملحوظةِ فحصٍ تظهر مع النتيجة نفسها.
          if [ ! -f /tmp/ci-output.log ]; then
            echo "::error::أخفقت الخطوة قبل أن تُنتج مخرجات — راجع الخطوة السابقة"
            exit 0
          fi
          # أسماءُ المخفقة أوّلاً مجموعةً في ملحوظةٍ واحدة: هي الفهرس. ثم تفصيلُ
          # الإخفاق في ملحوظاتٍ متتابعةٍ من ٢٥٠ سطراً، لأنّ الملحوظةَ الواحدة
          # محدودةُ الطول، وقطعُ التفصيل عند ستّين سطراً كان يُخفي أوّلَ الأسباب
          # ويُبقي آخِرَها — وأوّلُ الإخفاق هو ما يُشخَّص، وما بعده تَبَعٌ له.
          # الملحوظةُ الواحدة تُقتطع عند نحوِ ثلاثةِ آلافِ حرف، فرفعُ المخرجاتِ
          # كاملةً يضيع أكثرَه. ولذلك يُرفَع المفيدُ وحده: أسماءُ المخفقة فهرساً،
          # ثمّ ثلاثون سطراً قبل كلِّ إخفاقٍ — وفيها يطبع bun نصَّ الخطأ والمقارنةَ
          # والأثرَ — في ملحوظاتٍ صغيرةٍ لا تُقتطع.
          grep -n '^(fail)' /tmp/ci-output.log > /tmp/ci-fails.txt || true
          if [ -s /tmp/ci-fails.txt ]; then
            idx=$(cut -c1-160 /tmp/ci-fails.txt | sed 's/%/%25/g' | awk '{printf "%s%%0A", $0}')
            echo "::error title=فهرس الاختبارات المخفقة::${idx}"
          fi
          : > /tmp/ci-context.txt
          for lineno in $(cut -d: -f1 /tmp/ci-fails.txt | head -40); do
            start=$((lineno > 30 ? lineno - 30 : 1))
            sed -n "${start},${lineno}p" /tmp/ci-output.log >> /tmp/ci-context.txt
            echo "──────── نهاية سياق إخفاق ────────" >> /tmp/ci-context.txt
          done
          if [ ! -s /tmp/ci-context.txt ]; then cp /tmp/ci-output.log /tmp/ci-context.txt; fi
          rm -f /tmp/ci-part-*
          split -l 22 -d -a 2 <(cut -c1-300 /tmp/ci-context.txt | sed 's/%/%25/g') /tmp/ci-part-
          count=0
          for part in /tmp/ci-part-*; do
            count=$((count + 1))
            if [ "$count" -gt 24 ]; then
              echo "::error::سياقُ الإخفاق أطولُ من أربعٍ وعشرين ملحوظة — الباقي محجوب"
              break
            fi
            body=$(awk '{printf "%s%%0A", $0}' "$part")
            echo "::error title=سياق ${part##*-}::${body}"
          done

      # الخطوةُ السابقةُ كانت تُعيد تشغيلَ الحزمةِ كاملةً ثمّ تبحث بـ`grep` عن جملةِ
      # تحذيرٍ يكتبها كلُّ ملفٍّ بيدِه — وأربعةٌ وعشرون ملفّاً من خمسةٍ وخمسين لا تطبعها،
      # فكانت تلك الملفّاتُ تُتجاوَز صامتةً والحاجزُ أخضرُ (OPS-009). والآن يُقرَأ
      # **ملخَّصُ المُشغِّلِ نفسِه** من سجلِّ الخطوةِ السابقةِ بلا إعادةِ تشغيلٍ.
      - name: منع تخطّي اختبارات التكامل صامتاً
        run: bun run scripts/check-no-skipped-tests.ts /tmp/ci-output.log

      # e2e منفصلة عن التكامل لأنها أطول وتراكمية: ثلاثون رحلة على حالة واحدة
      # لا تُنظَّف بينها. فصلها يجعل فشلها مقروءاً بذاته لا مبتلعاً في حزمة كبيرة.
      - name: اختبارات e2e — رحلات متتابعة بلا تدخّل
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: set -o pipefail; bun run test:e2e 2>&1 | tee /tmp/ci-output.log

      - name: تفاصيل الإخفاق في تعليقٍ مقروء بلا سجلّات
        if: failure()
        run: |
          # الإخفاقُ الذي لا يُقرأ سببُه يُعالَج بالتخمين. وسجلّاتُ الوظيفة تُحمَّل
          # من تخزينٍ خارجيٍّ موقَّعٍ لا يبلغه كلُّ مراجع، فذيلُ المخرجات يُرفَع هنا
          # كملحوظةِ فحصٍ تظهر مع النتيجة نفسها.
          if [ ! -f /tmp/ci-output.log ]; then
            echo "::error::أخفقت الخطوة قبل أن تُنتج مخرجات — راجع الخطوة السابقة"
            exit 0
          fi
          # أسماءُ المخفقة أوّلاً مجموعةً في ملحوظةٍ واحدة: هي الفهرس. ثم تفصيلُ
          # الإخفاق في ملحوظاتٍ متتابعةٍ من ٢٥٠ سطراً، لأنّ الملحوظةَ الواحدة
          # محدودةُ الطول، وقطعُ التفصيل عند ستّين سطراً كان يُخفي أوّلَ الأسباب
          # ويُبقي آخِرَها — وأوّلُ الإخفاق هو ما يُشخَّص، وما بعده تَبَعٌ له.
          # الملحوظةُ الواحدة تُقتطع عند نحوِ ثلاثةِ آلافِ حرف، فرفعُ المخرجاتِ
          # كاملةً يضيع أكثرَه. ولذلك يُرفَع المفيدُ وحده: أسماءُ المخفقة فهرساً،
          # ثمّ ثلاثون سطراً قبل كلِّ إخفاقٍ — وفيها يطبع bun نصَّ الخطأ والمقارنةَ
          # والأثرَ — في ملحوظاتٍ صغيرةٍ لا تُقتطع.
          grep -n '^(fail)' /tmp/ci-output.log > /tmp/ci-fails.txt || true
          if [ -s /tmp/ci-fails.txt ]; then
            idx=$(cut -c1-160 /tmp/ci-fails.txt | sed 's/%/%25/g' | awk '{printf "%s%%0A", $0}')
            echo "::error title=فهرس الاختبارات المخفقة::${idx}"
          fi
          : > /tmp/ci-context.txt
          for lineno in $(cut -d: -f1 /tmp/ci-fails.txt | head -40); do
            start=$((lineno > 30 ? lineno - 30 : 1))
            sed -n "${start},${lineno}p" /tmp/ci-output.log >> /tmp/ci-context.txt
            echo "──────── نهاية سياق إخفاق ────────" >> /tmp/ci-context.txt
          done
          if [ ! -s /tmp/ci-context.txt ]; then cp /tmp/ci-output.log /tmp/ci-context.txt; fi
          rm -f /tmp/ci-part-*
          split -l 22 -d -a 2 <(cut -c1-300 /tmp/ci-context.txt | sed 's/%/%25/g') /tmp/ci-part-
          count=0
          for part in /tmp/ci-part-*; do
            count=$((count + 1))
            if [ "$count" -gt 24 ]; then
              echo "::error::سياقُ الإخفاق أطولُ من أربعٍ وعشرين ملحوظة — الباقي محجوب"
              break
            fi
            body=$(awk '{printf "%s%%0A", $0}' "$part")
            echo "::error title=سياق ${part##*-}::${body}"
          done

      # وكذلك e2e: يُقرَأ سجلُّ خطوتِها وحدَه (وقد أعاد `tee` كتابتَه بعد التكامل).
      - name: منع تخطّي اختبارات e2e صامتاً
        run: bun run scripts/check-no-skipped-tests.ts /tmp/ci-output.log

      # F9-05: بصمةُ التشغيلِ للتكامل.
      - name: بصمةُ التشغيل — كتابة (integration) (F9-05)
        if: always()
        env:
          JOB_NAME: integration
          CI_VERDICT: ${{ job.status }}
        run: |
          mkdir -p /tmp/run-manifests
          bun run scripts/emit-run-manifest.ts /tmp/run-manifests/integration.json

      # F9-05: الحاجزُ الساكن.
      - name: بصمةُ التشغيل — حاجز (integration) (F9-05)
        if: always()
        env:
          RUN_MANIFEST_DIR: /tmp/run-manifests
          JOB_NAME: integration
        run: bun run scripts/check-run-manifest.ts

  # وظيفةٌ ثالثةٌ لا خطوةٌ في القائمتَين: هذه وحدَها تُخاطِب خدمةً خارجيّةً حقيقيّةً
  # بسرٍّ من مخزنِ أسرارِ المستودعِ (OPS-006 · ADR 0049). وفصلُها مقصودٌ لسببَين:
  # أنّ سقوطَها لسببِ شبكةٍ لا يجوز أن يُخفي نتيجةَ الحواجزِ في `verify`، وأنّ السرَّ
  # لا يُحمَّل في بيئةِ خطوةٍ لا تحتاجه.
  #
  # وحدُّها المُعلَنُ: طلبُ دمجٍ من نسخةٍ متشعّبةٍ لا يُمنَح أسرارَ المستودعِ في
  # GitHub Actions أصلاً، فتُتخطّى الوظيفةُ هناك — ولا يُتخطّى شيءٌ في الدفعِ إلى
  # `main` ولا في طلبِ دمجٍ من المستودعِ نفسِه، وهو مسارُ هذا المستودعِ الفعليّ.
  real-redis:
    name: تكامل على Redis حقيقي
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    # أطولُ تشغيلٍ ناجحٍ مقيسٍ لهذه الوظيفةِ ≈ ٤٠ ثانيةً (ADR 0122).
    timeout-minutes: 15
    services:
      postgres:
        image: postgis/postgis:17-3.5
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: waslah
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s --health-timeout 5s --health-retries 20
      # `S-3` · `O-2`: خادمُ Redis **حقيقيٌّ** مُستضافٌ في الشغلةِ نفسِها بدلَ سِرٍّ
      # خارجيٍّ لم يُضبَطْ قطُّ — وغيابُه كانَ يُسقِط هذه الوظيفةَ في كلِّ جريةٍ.
      # والاستقلالُ التجاريُّ (ADR 0094) يقتضي ألّا يتوقّفَ حكمُ CI على حسابٍ عندَ
      # مُزوِّدٍ لا يملكُه المشروعُ.
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s --health-timeout 5s --health-retries 20
      # قشرةُ REST تُنطِق Redis بروتوكولَ Upstash الذي يتكلّمُه كودُ الإنتاجِ
      # (`packages/infrastructure/redis/upstash.ts`) حرفاً بحرفٍ: `POST` لمصفوفةِ
      # أمرٍ و`Bearer` للرمزِ. فالمُخاطَبُ خادمٌ حقيقيٌّ والبروتوكولُ بروتوكولُ
      # الإنتاجِ — لا عميلٌ مزدوجٌ ولا محاكاةٌ في الذاكرةِ.
      srh:
        image: hiett/serverless-redis-http:latest
        env:
          SRH_MODE: env
          SRH_TOKEN: waslah-ci-real-redis
          SRH_CONNECTION_STRING: redis://redis:6379
        ports: ["8079:80"]
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Install
        run: bun install --frozen-lockfile

      # الجهوزيّةُ تُقاسُ بأمرٍ يمرُّ بالقشرةِ إلى الخادمِ لا بفحصِ منفذٍ مفتوحٍ:
      # المنفذُ يُفتَح قبلَ أن تُتِمَّ القشرةُ اتّصالَها بـRedis، فيسقطُ أوّلُ اختبارٍ
      # على سببٍ لا علاقةَ له بالمقيسِ، فيُقرَأ الأحمرُ الزمنيُّ عيباً في الكودِ.
      - name: جهوزية قشرة REST على Redis حقيقي
        run: |
          for i in $(seq 1 40); do
            body=$(curl -fsS -X POST http://localhost:8079 \
              -H 'authorization: Bearer waslah-ci-real-redis' \
              -H 'content-type: application/json' \
              -d '["PING"]' 2>/dev/null || true)
            case "$body" in *PONG*) echo "✅ Redis حقيقيٌّ مُجيبٌ عبرَ REST بعدَ ${i} محاولةً"; exit 0;; esac
            sleep 2
          done
          echo "✗ قشرةُ REST لم تُجِبْ PING من خادمِ Redis في ثمانينَ ثانيةً — ولا تُقرَأ الوظيفةُ خضراءَ بلا خادمٍ." >&2
          exit 1

      - name: أدوار Supabase التي تفترضها الهجرات
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d waslah -v ON_ERROR_STOP=1 <<'SQL'
          do $$ begin
            if not exists (select 1 from pg_roles where rolname = 'anon') then
              create role anon nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'authenticated') then
              create role authenticated nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'service_role') then
              create role service_role nologin noinherit bypassrls;
            end if;
          end $$;
          SQL

      # مصدرُ الحقيقةِ الواحدُ لتطبيقِ الهجراتِ: `scripts/migrate.ts` (F7-07 · ADR 0068).
      # وحلقةُ `psql` العارية أُزيلَت لأنّها كانت تُطبِّق على CI بطريقٍ يخالف طريقَ
      # الإنتاج، فيصير الأخضرُ شاهداً على غيرِ ما يُشغَّل. والمُطبِّقُ يحكم على كلِّ
      # ملفٍّ قبل الاتّصال، ويضبط `lock_timeout`، ويملك المعاملةَ.
      #
      # تصحيحٌ بالإضافةِ (F5-06 · 2026-09-09): هذه الخطوةُ عادت إلى حلقةِ `psql`
      # في `a16bf97` — حلُّ تعارضٍ في دمجِ main إلى فرعِ F7-02 أخذَ نسخةَ الملفِّ
      # السابقةَ لـF7-07 — فبقيَ ADR 0068 يقولُ ما لا يفعلُه CI. أُعيدَت هنا،
      # وحاجزُ `scripts/check-migration-applier.ts` يمنعُ عودةَ العيبِ صامتاً.
      - name: تطبيق الهجرات بالمُطبِّقِ الآمنِ
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
        run: bun run scripts/migrate.ts

      # `REQUIRE_REAL_REDIS=1` يجعل غيابَ النقطةِ **إخفاقاً** لا تخطّياً: وظيفةٌ وُجدت
      # لتُشغِّل على Redis حقيقيٍّ لا تُقرَأ خضراءَ وهي لم تُخاطِبه. والحاجزُ لم يُخفَّفْ
      # في `S-3` بحرفٍ؛ إنّما صارَ **شرطُه مُستوفى**: النقطةُ خادمٌ في الشغلةِ.
      #
      # والرمزُ ههنا ظاهرٌ عن قصدٍ لا سهواً: خادمٌ يُنشَأُ ويُهدَمُ مع الوظيفةِ، لا
      # يُخاطَبُ من خارجِ شبكتِها، ولا يحملُ بياناً. ودليلُ التشغيلِ ما زالَ يُكتَم
      # ويُراجَع بحاجزِه (`scripts/check-real-redis-proof.ts`) — والكتمُ حكمُ الحاجزِ
      # لا حُسنُ الظنِّ. وسرٌّ حقيقيٌّ لو دخلَ يوماً فموضعُ قراءتِه واحدٌ كما كانَ
      # (`tests/support/real-redis.ts`).
      - name: اختبارات الجلسات على Redis حقيقي
        env:
          UPSTASH_REDIS_REST_URL: http://localhost:8079
          UPSTASH_REDIS_REST_TOKEN: waslah-ci-real-redis
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/waslah
          REQUIRE_REAL_REDIS: "1"
          REAL_REDIS_PROOF_PATH: /tmp/real-redis-proof.json
        run: set -o pipefail; bun run test:real-redis 2>&1 | tee /tmp/real-redis-output.log

      - name: لا يُقرَأ زعمُ Redis حقيقيٍّ دليلاً (OPS-006 · ADR 0049)
        run: bun run scripts/check-real-redis-proof.ts /tmp/real-redis-proof.json

      # الدليلُ يُرفَع مُرفَقاً لا يُطبَع وحدَه: يُقرأ في ملفِّ الأدلّةِ بعددٍ من التشغيلِ
      # نفسِه. وهو خالٍ من النقطةِ والرمزِ بحكمِ الحاجزِ الذي سبقَه لا بحسنِ الظنّ.
      - name: رفع دليل التشغيل الحقيقي
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: real-redis-proof
          path: /tmp/real-redis-proof.json
          if-no-files-found: warn

      # F9-05: بصمةُ التشغيلِ للـRedis الحقيقي.
      - name: بصمةُ التشغيل — كتابة (real-redis) (F9-05)
        if: always()
        env:
          JOB_NAME: real-redis
          CI_VERDICT: ${{ job.status }}
          REDIS_URL: http://localhost:8079
        run: |
          mkdir -p /tmp/run-manifests
          bun run scripts/emit-run-manifest.ts /tmp/run-manifests/real-redis.json

      # F9-05: الحاجزُ الساكن.
      - name: بصمةُ التشغيل — حاجز (real-redis) (F9-05)
        if: always()
        env:
          RUN_MANIFEST_DIR: /tmp/run-manifests
          JOB_NAME: real-redis
        run: bun run scripts/check-run-manifest.ts

  # ── F5-06 / SCL-008: برهانُ البنيةِ متعدّدِ المثيلاتِ في CI ──────────────────
  #
  # لا بديلَ سحابيٌّ مدفوعٌ متاحٌ، فالثابتُ يُثبَتُ هنا: ٣ بوّاباتٍ خلفَ موزّعٍ يفحصُ
  # `/ready`، والحالةُ في DB+Redis لا في ذاكرةِ البوّابة، وقتلُ نسخةٍ أثناءَ رحلةٍ
  # جاريةٍ لا يُفقدُ الخطوة. والبوّابةُ تعملُ بـ`TELEGRAM_TRANSPORT=silent` (seamٌ
  # مُعتمَدٌ) و`NODE_ENV=test` — الوضعُ غيرُ الإنتاجيِّ يرخي إجراءاتِ الإقلاعِ فقط،
  # وintake موصولٌ دائمًا فسلوكُ التشغيلِ واحدٌ. لا يُقلبُ هذا البندُ إلى `[x]`:
  # البرهانُ آليٌّ لا إنتاجيٌّ، و«مُثبَت» تتطلّبُ بيئةً شبيهةً بالإنتاج (`ح-5`).
  chaos-multi-instance:
    name: فوضى متعدد المثيلات (F5-06)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Install
        run: bun install --frozen-lockfile

      # لا `--wait`: البوّاباتُ **يجبُ** أن ترفضَ الإقلاعَ اليومَ (ADR 0050 §٣-ب)،
      # فانتظارُ صحّتِها انتظارُ ما لا يجوزُ حدوثُه. والحكمُ في الخطوةِ التاليةِ.
      - name: رفعُ رصةِ Docker Compose
        run: docker compose -f docker-compose.f5-06.yml up -d --build

      # القياسُ المشروعُ اليومَ: أنَّ كسرَ شرطِ النسخةِ الواحدةِ صاخبٌ في رصةٍ
      # حقيقيّةٍ من ثلاثِ حاوياتٍ — لا صامتٌ ولا مُتدهوِرٌ (R-17 · ADR 0050 §٣-ب).
      - name: برهانُ رفضِ الإقلاعِ متعدّدِ المثيلاتِ (R-17 · ADR 0050 §٣-ب)
        run: bun run scripts/f5-06-invariant-refusal.ts

      - name: أدوار Supabase التي تفترضها الهجرات
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -p 5433 -U postgres -d waslah -v ON_ERROR_STOP=1 <<'SQL'
          do $$ begin
            if not exists (select 1 from pg_roles where rolname = 'anon') then
              create role anon nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'authenticated') then
              create role authenticated nologin noinherit;
            end if;
            if not exists (select 1 from pg_roles where rolname = 'service_role') then
              create role service_role nologin noinherit bypassrls;
            end if;
          end $$;
          SQL

      # المُطبِّقُ نفسُه على قاعدةِ compose — لا طريقَ ثانٍ للهجراتِ في هذا المستودعِ
      # (F7-07 · ADR 0068). والمنفذُ 5433 هو ما تنشرُه رصةُ compose على العُدّاءِ.
      - name: تطبيق الهجرات بالمُطبِّقِ الآمنِ على قاعدةِ compose
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5433/waslah
        run: bun run scripts/migrate.ts

      # المنسّقُ يقرأُ آليةَ التوزيعِ المُقرَّرةَ من الشيفرةِ: يقودُ الرحلةَ إن كانت
      # تعبرُ حدودَ العمليةِ، ويُعلِنُ التعذُّرَ البنيويَّ صريحاً إن لم تكن — بلا
      # تعطيلِ حاجزٍ وبلا إعلانِ نجاحٍ لم يقع (ADR 0050 §٣-د · §٨-أ · REQ-07).
      - name: منسّقُ اختبارِ الفوضى — قيادةُ رحلةٍ عبرَ المثيلاتِ وقتلُ نسخةٍ أثناءَها
        run: bun run scripts/f5-06-chaos.ts

      - name: سجلاتُ البوّاباتِ عند الإخفاق
        if: failure()
        run: |
          docker compose -f docker-compose.f5-06.yml logs --no-color gateway-1 gateway-2 gateway-3 worker-1 worker-2 upstash-shim haproxy 2>&1 | tail -200

      - name: إزالةُ الرصة
        if: always()
        run: docker compose -f docker-compose.f5-06.yml down -v

      # F9-05: بصمةُ التشغيلِ للفوضى متعددِ المثيلات.
      - name: بصمةُ التشغيل — كتابة (chaos-multi-instance) (F9-05)
        if: always()
        env:
          JOB_NAME: chaos-multi-instance
          CI_VERDICT: ${{ job.status }}
        run: |
          mkdir -p /tmp/run-manifests
          bun run scripts/emit-run-manifest.ts /tmp/run-manifests/chaos-multi-instance.json

      # F9-05: الحاجزُ الساكن.
      - name: بصمةُ التشغيل — حاجز (chaos-multi-instance) (F9-05)
        if: always()
        env:
          RUN_MANIFEST_DIR: /tmp/run-manifests
          JOB_NAME: chaos-multi-instance
        run: bun run scripts/check-run-manifest.ts
