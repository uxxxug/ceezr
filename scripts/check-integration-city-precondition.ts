#!/usr/bin/env bun
/**
 * # الحاجزُ: لا اختبارَ تكاملٍ يستعيرُ شرطَه المسبقَ — `OPS-019` · `OPS-020`
 *
 * **الغرض:** نقطةُ دخولٍ ونداءٌ للمُشغِّلِ الموحَّدِ. **لا قاعدةَ حكمٍ ولا سجلَّ
 * إعفاءاتٍ ولا قراءةَ قرصٍ في هذا الملفِّ**: القواعدُ في
 * `scripts/lib/city-precondition-audit.ts` (حَكَمٌ نقيٌّ تُزرَعُ فيه السوالبُ)،
 * والقراءةُ في `city-precondition-repository.ts`، والإعفاءاتُ في سجلٍّ واحدٍ
 * `city-precondition-exemptions.ts`، والطبعُ في `city-precondition-runner.ts`.
 *
 * **الحالة:** `OPS-019` مُنفَّذ · مُختبَر (`ADR 0116`)؛ و`OPS-020` وحَّدَ الحَكَمَ
 * والسجلَّ (`ADR 0148`).
 *
 * **ينتمي إلى:** الحجزانِ `OPS-019` و`OPS-020` في `ROADMAP.md`.
 *
 * **يُتوقع أن يستخدمه لاحقاً:** خطوةٌ مسمّاةٌ في وظيفةِ `verify` وفي سلسلةِ
 * `ci` في `package.json`.
 *
 * **ملاحظات مستقبلية:** إن صارَ في المستودَعِ معينُ شرطٍ مسبقٍ آخرُ (مركبةٌ،
 * سائقٌ، اشتراكٌ) فالأمتنُ أن يُضافَ قاعدةً في الحَكَمِ نفسِه بسجلِّ الإعفاءاتِ
 * نفسِه — وقاعدةُ ٧ تُسقِطُ البناءَ على سجلٍّ ثانٍ لو نُسيَ ذلكَ.
 */

import { runCityPreconditionGuard } from "./lib/city-precondition-runner.ts";

if (import.meta.main) process.exit(runCityPreconditionGuard("نقطةُ دخولِ التكاملِ"));
