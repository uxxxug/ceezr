/**
 * # الحاجزُ: تفعيلُ المدينةِ مكتملٌ في عبارتِه — `OPS-020` (نداءٌ لا منطقٌ)
 *
 * **الغرض:** كانَ هذا الملفُّ يحملُ منطقَ الحكمِ وسجلَّ إعفاءاتٍ **ثانياً**
 * (`INTENTIONAL_BARE_ACTIVATION`) لموضوعٍ يحكُمُ عليه حاجزٌ آخرُ بسجلٍّ آخرَ —
 * وذاكَ عينُ `OPS-020`. فصارَ **نداءً** للمُشغِّلِ الموحَّدِ، وانتقلَت قاعدتُه
 * كما هيَ قاعدةً سادسةً في الحَكَمِ النقيِّ (`ح-8`: نقلٌ بالإضافةِ لا حذفٌ).
 *
 * **الحالة:** `OPS-020` — منفَّذ (`ADR 0148`). القاعدةُ ٦ في
 * `scripts/lib/city-precondition-audit.ts`، وسوالبُها في
 * `tests/unit/check-integration-city-precondition.test.ts`.
 *
 * **ينتمي إلى:** الحجزُ `OPS-020` في `ROADMAP.md`.
 *
 * **يُتوقع أن يستخدمه لاحقاً:** خطوةٌ مسمّاةٌ في `.github/workflows/ci.yml`
 * وسلسلةُ `ci` — وتُبقى الخطوتانِ بأسمائِهما كي لا يُفقَدَ إنفاذٌ قائمٌ.
 *
 * **لماذا وُجِد هذا الفحص:** قيدُ `cities_active_requires_groups` يمنعُ تفعيلَ
 * مدينةٍ بلا قروباتِها الثلاثةِ، ولا تبذُرُ أيّةُ هجرةٍ تلكَ القروباتَ. فكانَ
 * خمسةُ ملفّاتِ اختبارٍ تُنفّذُ `update cities set is_active = true` مجرّدةً،
 * فتنجحُ فقط إن سبقَها ملفٌّ آخرُ ضبطَ القروباتِ — وترتيبُ الاكتشافِ يختلفُ بينَ
 * الجهازِ المحلّيِّ وآلةِ التكاملِ. فمرَّت محلّياً وسقطَت بعيداً، ثمَّ سرَّبَ
 * سقوطُها حاوياتٍ لم تُغلَق فأنفدَ اتّصالاتَ القاعدةِ وأسقطَ ملفّاتٍ لا علاقةَ
 * لها بالعيبِ أصلاً. والعيبُ من هذا النوعِ لا يُكتشَفُ بمراجعةِ الشيفرةِ لأنّه
 * لا يظهرُ إلّا بترتيبٍ معيَّنٍ.
 */

import { runCityPreconditionGuard } from "./lib/city-precondition-runner.ts";

if (import.meta.main) process.exit(runCityPreconditionGuard("نقطةُ دخولِ التفعيلِ"));
