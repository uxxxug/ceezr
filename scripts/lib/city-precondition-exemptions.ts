/**
 * # سجلُّ إعفاءاتِ عقدِ تفعيلِ المدينةِ في الاختباراتِ — **واحدٌ لا اثنانِ** (`OPS-020`)
 *
 * **الغرض:** أن يكونَ لعقدٍ واحدٍ سجلُّ استثناءاتٍ واحدٌ. فقبلَ `OPS-020` كانَ
 * حاجزانِ يحكُمانِ على موضوعٍ واحدٍ — تفعيلُ مدينةٍ في اختبارٍ — بسجلَّينِ
 * منفصلَينِ (`INTENTIONAL_BARE_ACTIVATION` في `check-test-city-activation.ts`
 * و`EXEMPTIONS` في `check-integration-city-precondition.ts`)، فمَن قرأَ أحدَهما
 * ظنَّ أنَّه قرأَ العقدَ.
 *
 * **الحالة:** `OPS-020` — مصدرُ الحقيقةِ الوحيدُ للإعفاءاتِ، تقرؤُه القاعدةُ ٥
 * (لا إعفاءَ ميّتٌ ولا مجهولُ سببٍ) والقاعدةُ ٧ (لا سجلَّ ثانياً في
 * `scripts/`).
 *
 * **ينتمي إلى:** `scripts/lib` · الحجزُ `OPS-020` في `ROADMAP.md` · `ADR 0148`.
 *
 * **يُستخدم من:** `scripts/check-integration-city-precondition.ts`
 * و`scripts/check-test-city-activation.ts` — كلاهما نداءٌ للحَكَمِ الموحَّدِ.
 *
 * **ملاحظات مستقبلية:** كلُّ مُدخلٍ يحملُ **القاعدةَ** التي يُعلِّقُها، لأنَّ
 * إعفاءً مُطلَقاً يُعلِّقُ قواعدَ لم يُقصَدْ تعليقُها. ومُدخلٌ لا يُستعمَلُ
 * يُسقِطُ البناءَ بالقاعدةِ ٥، فلا يُوضَعُ «احتياطاً».
 */

import type { PreconditionExemption } from "./city-precondition-audit.ts";

/**
 * الإعفاءاتُ المُعلَنةُ — اثنانِ، كلاهما للقاعدةِ ٦ (القروباتُ في نفسِ
 * العبارةِ)، ولا إعفاءَ واحدٌ للقاعدةِ ١.
 */
export const CITY_PRECONDITION_EXEMPTIONS: readonly PreconditionExemption[] = [
  {
    path: "tests/integration/pilot-city-activation.test.ts",
    rule: 6,
    reason:
      "يفحصُ قيدَ `cities_active_requires_groups` نفسَه: تفعيلٌ مجرَّدٌ **هوَ** موضوعُ الاختبارِ ويُتوقَّعُ أن يُرفَضَ. واشتراطُ القروباتِ فيهِ يُلغي ما يُثبِتُه.",
    owner: "منفّذ المستودع",
  },
  {
    path: "tests/unit/check-integration-city-precondition.test.ts",
    rule: 6,
    reason:
      "حالاتُه السالبةُ نصوصٌ **مُصنَّعةٌ** تُمرَّرُ إلى حَكَمٍ نقيٍّ في الذاكرةِ، لا عبارةٌ تُنفَّذُ على قاعدةٍ. فقراءتُها شِفرةً حقيقيّةً إنذارٌ كاذبٌ يُبطِلُ برهانَ `ح-7` على الحاجزِ.",
    owner: "منفّذ المستودع",
  },
];
