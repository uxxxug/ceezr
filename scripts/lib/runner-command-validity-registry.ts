/**
 * # سجلُّ إعفاءاتِ صلاحيّةِ نداءِ أوامرِ المشغِّلِ — `D-24`
 *
 * سجلٌّ مغلقٌ: أمرٌ يستهلكُ وسيطاً ويُنادى في السلسلةِ بلا وسيطٍ **إخفاقٌ**، إلّا
 * أن يُكتَبَ هنا سببٌ منطوقٌ لا يقلُّ عن أربعينَ حرفاً. والفراغُ هوَ الحالُ
 * الصحيّةُ: أمرٌ لا يُنادى كما كُتِبَ يُصلَحُ أو يُخرَجُ من السلسلةِ، لا يُعفى.
 */

import type { ArgumentExemption } from "./runner-command-validity.ts";

/**
 * فارغٌ بقصدٍ. والإعفاءُ الوحيدُ الذي كانَ سيُكتَبُ هنا
 * (`scripts/check-no-skipped-tests.ts`) **حُلَّ بالإخراجِ من السلسلةِ** إلى سجلِّ
 * إعفاءاتِ `ADR 0143` — لأنَّه يقرأُ سجلَّ مخرجاتِ خطوةِ الاختبارِ في CI،
 * ومحاكاتُه محلّيًّا بأنبوبٍ تُخفي رمزَ خروجِ `bun test` نفسَه.
 */
export const ARGUMENT_EXEMPTIONS: readonly ArgumentExemption[] = [];
