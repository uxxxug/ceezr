/**
 * # سجلُّ ميزانيةِ العملِ المطلقةِ — سقفٌ مقيسٌ للانحدارِ الأدائيِّ (`F9-06`)
 *
 * **الغرض:** أن يكونَ هناك سقفٌ مطلقٌ لعملِ الكتلةِ الدافئةِ الأولى في اختبارِ
 * الصمودِ، يكشفُ الانحدارَ الثابتَ الذي لا تُمسكُهُ نسبةُ `DEC-18`. فنسبةُ
 * التدهورِ (آخرُ عشرٍ ÷ أولُ عشرٍ) تقرأُ ×0.46 سواءٌ أكانَ العملُ سبعةَ آلافٍ
 * صفٍّ أم أربعةَ عشرَ ألفاً — فالانحدارُ الذي يضاعفُ العملَ في الطرفَينِ معاً
 * يمرُّ أصفاراً. وهذا السقفُ يمسكُه.
 *
 * **الحالة:** `F9-06` / `OPS-005` — ميزانيةُ عملٍ مقيسةٌ ومحروسةٌ بحاجزٍ ساكنٍ.
 *
 * **ينتمي إلى:** البند `F9-06` · القسم `11-د` (`OPS-005`) · `DEC-18` (وحدةُ القياسِ).
 *
 * **يُستخدَمُ من:** `tests/e2e/ride-soak.test.ts` (التوكيدُ) · `scripts/check-work-budget.ts`
 * (الحاجزُ الساكنُ).
 *
 * ## كيف قِيسَت هذه الأرقامُ
 *
 * | المصدرُ | التشغيلُ | الكتلةُ الدافئةُ الأولى (رحلاتٌ ١١..٢٠) |
 * |---|---|---|
 * | CI (`35135764279`@`8efd74b`) | جولةٌ واحدةٌ | 16057 صفّاً · 11552 كتلةً |
 * | محلّيّاً (ثلاثُ جولاتٍ متتاليةٍ) | متطابقٌ | ~17407 صفّاً · ~12652 كتلةً (مُحتَسَبةٌ من النسبةِ ×0.43) |
 *
 * والكتلةُ الأخيرةُ (رحلاتٌ ٢١..٣٠) محلّيّاً: 7485 صفّاً · 5314 كتلةً — **تكرَّرَت حرفاً**
 * في الجولاتِ الثلاثِ، فالقياسُ مُعادٌ لا مُصادَفٌ (دليلُ `DEC-18` §٣).
 *
 * والسقفُ مضروبٌ في ٢ هامشاً: 32000 صفّاً و23000 كتلةً — مساحةٌ كافيةٌ للتباينِ
 * الطبيعيِّ بينَ البيئاتِ، معَ قبضِ الانحدارِ الذي يضاعفُ العملَ.
 *
 * ## قاعدةُ التحديثِ
 *
 * - **تُرفَعُ** الأرقامُ ولا تُخفَضُ إلا ببيانِ سببٍ في commit مستقلٍ: الحاجزُ لا
 *   يُضعَّف لينجحَ بناءٌ.
 * - لا تُضبَطُ القيمُ على صفرٍ أو ما لا نهاية: سقفٌ بلا رقمٍ سقفٌ بلا حاجز.
 * - مصدرُ الحقيقةِ واحدٌ: هذا الملفُّ وحدَه. لا تُكرَّرُ القيمُ في الاختبارِ.
 */

/** سقفُ الصفوفِ الممسوحةِ للكتلةِ الدافئةِ الأولى. مقيسٌ × ٢ هامشاً. */
export const ROWS_SCANNED_BUDGET = 32000;

/** سقفُ الكُتَلِ الملموسةِ للكتلةِ الدافئةِ الأولى. مقيسٌ × ٢ هامشاً. */
export const BLOCKS_TOUCHED_BUDGET = 23000;

/** تاريخُ القياسِ الذي بُنِيَ عليهِ السقفُ. */
export const BUDGET_MEASURED_AT = "2026-09-16";

/** تشغيلُ CI الذي قِيسَ منهُ السقفُ. */
export const BUDGET_CI_RUN = "35135764279";

/** القيمُ المقيسةُ الأصليّةُ (قبلَ الهامشِ) — للتوثيقِ والمراجعةِ. */
export const MEASURED_VALUES = {
  rowsScanned: 16057,
  blocksTouched: 11552,
} as const;

/** هامشُ السقفِ: مقيسٌ × هذا الرقم. */
export const BUDGET_MARGIN = 2;

/** اسمُ ملفِّ اختبارِ الصمودِ المحكومِ بهذهِ الميزانيّةِ. */
export const SOAK_TEST_FILE = "tests/e2e/ride-soak.test.ts";

/** قائمةُ قواعدِ الحاجزِ الساكنِ — كلُّ قاعدةٍ لها سالبةٌ مبذورةٌ. */
export const RULE_NAMES = [
  "budget.imported",
  "budget.not-zero",
  "budget.not-infinite",
  "budget.single-source",
  "budget.assertion-present",
] as const;

export type RuleName = (typeof RULE_NAMES)[number];
