/**
 * # سجلُّ إعفاءاتِ تكافؤِ الإنفاذِ — مغلقٌ، وكلُّ مدخلٍ بسببٍ منطوقٍ
 *
 * **قاعدةُ الدخولِ:** لا يُدخَلُ هنا حاجزٌ لأنَّ إضافتَه مُتعِبةٌ؛ لا يُدخَلُ إلّا ما
 * **يستحيلُ** تشغيلُه في الجانبِ الآخرِ لسببٍ في البيئةِ لا في العزمِ: سرٌّ لا
 * يُتاحُ محلّيًّا · أثرُ وظيفةٍ أخرى · خدمةٌ حقيقيّةٌ · سياقُ دفعٍ من المنصّةِ.
 *
 * **والإعفاءُ ليسَ إسكاتاً:** المُعفى مذكورٌ باسمِه وسببِه، والحاجزُ يُسقِطُ البناءَ
 * إذا بَطَلَ الموجِبُ (إعفاءٌ بائتٌ) أو إذا كُتِبَ سببٌ لا يُعلِّلُ.
 */

import type { GuardExemption } from "./guard-enforcement.ts";

/**
 * حواجزُ في المشغِّلِ المحلّيِّ ولا تُنفَّذُ في سيرِ عملٍ.
 *
 * **فارغٌ عن قصدٍ.** السبعةُ التي كانَت هنا فعلاً (لا نظريًّا) أُضيفَت خطواتٍ في
 * `ci.yml` بدلاً من إعفائِها — فالعلاجُ إنفاذٌ لا استثناءٌ.
 */
export const LOCAL_ONLY_EXEMPTIONS: readonly GuardExemption[] = [];

/** حواجزُ ومُشغِّلاتٌ في سيرِ العملِ ولا تُنادى في `package.json → ci`. */
export const REMOTE_ONLY_EXEMPTIONS: readonly GuardExemption[] = [
  {
    script: "scripts/check-core-contract-freshness.ts",
    reason:
      "يقرأُ مستودعَ CORE الخاصَّ برمزٍ من مخزنِ أسرارِ المستودعِ (D-07 · SEC-15)، " +
      "ولا سرَّ في البيئةِ المحلّيّةِ — فتشغيلُه محلّيًّا يُخرِجُ أخضرَ لأنَّه لم يقرأْ شيئاً، وذاكَ أسوأُ من غيابِه.",
  },
  {
    script: "scripts/check-no-skipped-tests.ts",
    reason:
      "يقرأُ سجلَّ مخرجاتِ خطوةِ الاختبارِ نفسِها (`/tmp/ci-output.log`) الذي تُنتِجُه وظيفةُ CI، " +
      "ويُخفِقُ بلا مسارِ سجلٍّ (خروجٌ 1) — ومحاكاتُه محلّيًّا بأنبوبٍ تُخفي رمزَ خروجِ الاختباراتِ نفسِها، أي تُعطِّلُ بوّابةً أقوى.",
  },
  {
    script: "scripts/check-real-redis-proof.ts",
    reason:
      "يفحصُ الأثرَ (`real-redis-proof`) الذي تُنتِجُه وظيفةُ Redis الحقيقيِّ في سيرِ العملِ نفسِه (OPS-006 · ADR 0049)؛ " +
      "ولا أثرَ في المشغِّلِ المحلّيِّ، فلا مُدخَلَ له يُفحَصُ.",
  },
  {
    script: "scripts/check-roadmap.mjs",
    reason:
      "يقرأُ نطاقَ الالتزامِ الذي تُمرِّرُه المنصّةُ في سياقِ الدفعِ ليقابلَ تغييرَ التنفيذِ بتحديثِ الخارطةِ؛ " +
      "وبلا ذلكَ النطاقِ يتخطّى نفسَه — فتشغيلُه محلّيًّا يُنتِجُ أخضرَ متخطّياً يُقرأُ حكماً وليسَ حكماً. " +
      // **تصحيحٌ بالإضافةِ 2026-09-21 (`OPS-ROADMAP-GATE` · `ADR 0167`)**: السطرُ
      // أعلاه يبقى مكتوباً لأنَّه سجلُّ ما كانَ، **وقد كانَ يصفُ عطباً لا خصيصةً**:
      // «يتخطّى نفسَه» صحيحٌ في المشغِّلِ المحلّيِّ، **وكانَ صحيحاً في CI أيضاً**
      // في أوّلِ دفعةِ فرعٍ إذ `event.before` أصفارٌ. فصارَ الحاجزُ يرجعُ إلى أصلِ
      // الفرعِ من `main`، **ويسقُطُ بـ3 عندَ تعذُّرِ الحلِّ لا يمرُّ**. وبقاؤه
      // مُستثنىً من المشغِّلِ المحلّيِّ سببُه غيرُ ذلكَ: المدى شأنُ سياقِ دفعٍ.
      "وهوَ منذُ 2026-09-21 لا يتخطّى في CI ولا يخرُجُ صفراً عندَ تعذُّرِ حلِّ المدى.",
  },
  {
    script: "scripts/measure-db-rtt.ts",
    reason:
      "مِقياسٌ تشخيصيٌّ لا حاجزٌ: يفتحُ وصلةً إلى قاعدةٍ حقيقيّةٍ في سيرٍ مستقلٍّ ويُخرِجُ أرقاماً، " +
      "ولا يُسقِطُ بناءً — فإدخالُه المشغِّلَ يُطيلُه بلا حكمٍ.",
  },
  {
    script: "scripts/migrate.ts",
    reason:
      "المُطبِّقُ نفسُه (F7-07 · ADR 0068): يُشغَّلُ على قاعدةِ خدمةٍ حقيقيّةٍ في وظيفةِ التكاملِ. " +
      "ولا قاعدةَ في المشغِّلِ المحلّيِّ، وتطبيقُ هجراتٍ ليسَ فحصاً ساكناً.",
  },
  {
    script: "scripts/rehearse-cutover.ts",
    reason:
      "تمرينُ التحويلِ يحتاجُ قاعدةً حقيقيّةً وحالةً مبذورةً في وظيفةِ التكاملِ؛ " +
      "ولا يُحاكى بلا قاعدةٍ، ومحاكاتُه بلا قاعدةٍ تُنتِجُ ثقةً كاذبةً.",
  },
  {
    script: "scripts/rollback-schema-drill.ts",
    reason:
      "تمرينُ الاستعادةِ (OPS-010) يُطبِّقُ ثمَّ يستعيدُ على قاعدةٍ حقيقيّةٍ في وظيفةِ التكاملِ؛ " +
      "ولا معنى لتشغيلِه بلا قاعدةٍ يُقاسُ عليها الاستعادةُ.",
  },
  {
    script: "scripts/f5-06-chaos.ts",
    reason:
      "مُشغِّلُ سيناريوِ الفوضى (F5-06) يحتاجُ مثائلَ متعدِّدةً وRedis حقيقيًّا في وظيفةٍ مخصَّصةٍ؛ " +
      "ولا طوبولوجيا متعدِّدةً في المشغِّلِ المحلّيِّ.",
  },
  {
    script: "scripts/f5-06-invariant-refusal.ts",
    reason:
      "يقيسُ رفضَ الإقلاعِ حينَ تُخرَقُ الثوابتُ في طوبولوجيا متعدِّدةٍ معَ Redis حقيقيٍّ (F5-06)؛ " +
      "والرفضُ لا يُقاسُ بلا البيئةِ التي يُرفَضُ فيها.",
  },
  {
    script: "scripts/emit-run-manifest.ts",
    reason:
      "يُنتِجُ بصمةَ تشغيلٍ JSON بعدَ كلِّ وظيفةٍ في CI بـ`if: always()` (F9-05)؛ " +
      "وفي المشغِّلِ المحلّيِّ لا وظيفةَ ولا `GITHUB_SHA` ولا `GITHUB_RUN_URL`، " +
      "فالبصمةُ التي يُنتِجُها محلّيًّا تُخبرُ عن لا شيء.",
  },
];

/**
 * ملفّاتُ `scripts/check-*` لا تُنادى مباشرةً.
 *
 * **فارغٌ عن قصدٍ:** `check-coverage-gate.ts` يُنادى عبرَ الاسمِ `check:coverage`،
 * والمُحلِّلُ يفكُّ الأسماءَ المتداخلةَ فيراهُ — ولا يُعفى ما يُرى.
 */
export const UNINVOKED_EXEMPTIONS: readonly GuardExemption[] = [];
