# فحوص مسبار Socket.IO المحلية — 2026-08-27 (Asia/Riyadh)
# البيئة: Node v20.20.1 في صندوق التطوير. **ليست دليلاً على iOS أو Android أو حاوية تلغرام.**
# خلافاً لمسبارَي SSE وWebSocket، هذا المسبار **يحتاج مكتبةً**. تُثبَّت بلا تسجيلٍ في المستودع:
#   npm install --prefix probe/dec-06-socketio --no-save --no-package-lock socket.io@4.8.3 socket.io-client@4.8.3
# لا package.json داخل المسبار، ولا مساس بـbun.lock، ولا دخول في workspaces المستودع.

## 1) selftest.mjs
— فحص مسبار Socket.IO الذاتي —

[0] العزل
  ✅ لا استعمال لـpostgres في الشيفرة
  ✅ لا استعمال لـpg_notify في الشيفرة
  ✅ لا استعمال لـredis في الشيفرة
  ✅ لا استعمال لـupstash في الشيفرة
  ✅ لا استعمال لـsupabase في الشيفرة
  ✅ لا استعمال لـgrammy في الشيفرة
  ✅ لا محوِّل (adapter) خارج المحوِّل الافتراضي
  ✅ لا استيراد من apps/ أو packages/
  ✅ اعتماديةُ الخادمِ الوحيدةُ socket.io
  ✅ لا حزمةَ قاعدةِ بياناتٍ ولا Redis في المُثبَّت
  ✅ المُثبَّت يحوي socket.io وsocket.io-client
  ✅ الصفحة تأخذ العميل من الخادم لا من CDN
  ✅ الصفحة تسجّل الناقل المستخدَم
  ✅ الصفحة تسجّل السقوط إلى polling
  ✅ الصفحة تسجّل سبب الانفصال
  ✅ الصفحة تسجّل وقت بدء الاتصال ووقت نجاحه
  ✅ لا كتابة لقيمة initData الخام في الصفحة
  ✅ لا كتابة لـlocation.href في السجل
  ✅ لا كتابة لـlocation.hash في السجل
  ✅ لا كتابة لـlocation.search في السجل
  ✅ الصفحة فيها زر التصدير
  ✅ أزرارُ الحالات السبع كاملة
  ✅ استعادةُ حالةِ الاتصال مطفأة (غيرُ مذكورةٍ أصلاً)

[1] الحياة والصفحة وملفُّ العميل
  ✅ healthz = 200
  ✅ healthz يذكر bootId
  ✅ healthz يذكر النبضة المضبوطة
  ✅ healthz يذكر النواقل المسموحة
  ✅ healthz يذكر الترقية مسموحة
  ✅ الصفحة = 200
  ✅ الصفحة عربية RTL
  ✅ ملفُّ عميل Socket.IO مخدومٌ محلياً = 200
  ✅ ملفُّ العميل جافاسكربت
  ✅ مسارٌ مجهول = 404
  ✅ الخادمُ المانعُ للترقيةِ حيٌّ ويُعلِن ذلك

[2] الاتصال ووصفُ الجلسة
  ✅ الاتصال نجح
  ✅ للجلسة مُعرِّفٌ نصّيّ
  ✅ زمنُ الاتصالِ مقيسٌ وموجب
  ✅ وصفُ الجلسة يحمل bootId
  ✅ وصفُ الجلسة يحمل رقمَ الجلسة
  ✅ النبضةُ المُعلَنةُ تساوي المضبوطة
  ✅ الاستئنافُ يقودُه العميلُ صريحاً
  ✅ لا استعادةَ حالةِ اتصالٍ من المكتبة
  ✅ استعادةُ الحالةِ مُعلَنةٌ مطفأة
  ✅ وصفُ الجلسة يذكر نبضةَ المكتبةِ منفصلةً

[3] النبضةُ والتسلسلُ المتصاعد
  ✅ وصلت أربعُ نبضاتٍ على الأقل
  ✅ التسلسلُ يبدأ من 1
  ✅ التسلسلُ متصاعدٌ بواحدٍ بلا فجوة
  ✅ كلُّ النبضاتِ من نفسِ الجلسة
  ✅ كلُّ نبضةٍ تحمل الناقلَ المستخدَمَ فعلاً

[4] رسالةُ عميل→خادم بلا إقرار
  ✅ الخادمُ يُعلِن آخرَ إقرارٍ وصلَه
  ✅ عدّادُ الإقراراتِ يتقدّم

[5] إقرارٌ عميل→خادم (acknowledgement)
  ✅ الرِّدُّ وصلَ عبر دالّةِ الإقرار
  ✅ الرِّدُّ يُعيد نفسَ الرقمِ العابر
  ✅ الرِّدُّ يذكر ناقلَ الخادمِ وقتَه
  ✅ الخادمُ عدَّ طلبَ الإقرار

[6] إقرارٌ خادم→عميل ومهلتُه
  ✅ الخادمُ طلبَ إقراراً على الأقلِّ مرّةً
  ✅ إقرارُ العميلِ وصلَ الخادمَ
  ✅ لا مهلةَ إقرارٍ منتهيةٌ مع عميلٍ يردّ
  ✅ عميلٌ لا يردُّ ⇒ تُسجَّل مهلةُ إقرارٍ منتهية
  ✅ سجلُّ الخادمِ يذكر مهلةَ الإقرار

[7] الاستئنافُ من نقطةٍ يُمرّرُها العميل
  ✅ الخادمُ أقرَّ بنقطةِ الاستئناف
  ✅ النبضةُ الأولى بعد الاستئناف = 58
  ✅ بلا نقطةِ استئناف: يبدأ من الصفر
  ✅ النبضةُ الأولى في جلسةٍ جديدة = 1

[8] الناقلُ والترقيةُ والسقوطُ
  ✅ الافتراضُ يبدأ بـpolling
  ✅ الترقيةُ إلى WebSocket حصلت
  ✅ الناقلُ بعد الترقيةِ WebSocket
  ✅ سجلُّ الخادمِ يُدوّن الترقية
  ✅ بـWebSocket وحدَه: لا polling أصلاً
  ✅ بـWebSocket وحدَه: لا حدثَ ترقية
  ✅ بـpolling وحدَه: الجلسةُ تعمل
  ✅ بـpolling وحدَه: الناقلُ يبقى polling
  ✅ الخادمُ يُبلِّغ الناقلَ الحقيقيَّ في النبضة
  ✅ خادمٌ يمنع الترقية ⇒ البدايةُ polling
  ✅ خادمٌ يمنع الترقية ⇒ لا ترقيةَ حصلت
  ✅ خادمٌ يمنع الترقية ⇒ البقاءُ على polling
  ✅ وصفُ الجلسةِ يُعلِن أنّ الترقيةَ ممنوعة
  ✅ الجلسةُ تعمل على polling وحدَه

[9] الانفصالُ وسببُه كما تُسمّيه المكتبة
  ✅ قطعٌ من الخادم ⇒ السببُ io server disconnect
  ✅ سجلُّ الخادمِ يُدوّن القطعَ المتعمَّد
  ✅ سجلُّ الخادمِ يُدوّن سببَ الإغلاقِ نصّاً
  ✅ قطعٌ من العميل ⇒ السببُ io client disconnect
  ✅ الخادمُ يقرأ سببَ قطعِ العميل
  ✅ هدمٌ من جهةِ العميل ⇒ السببُ forced close
  ✅ إغلاقُ الناقلِ مُسجَّلٌ على مستوى المحرّك
  ✅ هدمُ الناقلِ من الخادم ⇒ السببُ transport close
  ✅ سجلُّ الخادمِ يُدوّن هدمَ الناقل
  ✅ الخادمُ يقرأ سببَ الإغلاقِ transport close

[10] خطأُ المصافحةِ يُبلَّغ بسببٍ مسمّى
  ✅ الرفضُ يُبلَّغ عبر connect_error
  ✅ نصُّ الخطأِ هو الذي أرسلَه الوسيط
  ✅ بياناتُ الخطأِ الإضافيةُ تصل
  ✅ العدّادُ يُسجّل الرفض

[11] إعادةُ الاتصالِ التلقائيةُ من المكتبة
  ✅ محاولةُ إعادةٍ مرقَّمةٌ سُجِّلت
  ✅ إعادةُ الاتصالِ نجحت
  ✅ عددُ محاولاتِ الإعادةِ يُقرأ من المكتبة
  ✅ الاستئنافُ بعد الإعادةِ من آخرِ تسلسلٍ معروف
  ✅ زمنُ الاتصالِ الجديدِ مقيس

[12] العدّ لا يتضاعف
  ✅ openSockets = 0 لا سالباً
  ✅ سطرُ إغلاقٍ واحدٌ لكلِّ جلسة
  ✅ لا رقمَ جلسةٍ يُغلَق مرّتَين
  ✅ كلُّ الجلساتِ بنفسِ bootId
  ✅ لا سطرَ إغلاقٍ بلا سببٍ مكتوب

— الحصيلة: 102 ناجح، 0 فاشل —
كل فحوص المسبار الذاتية نجحت على Node في بيئة التطوير.
وهذا لا يقول شيئاً عن iOS أو Android أو حاوية تلغرام.

## 2) جلسةٌ حقيقيةٌ بنبضةِ 5 ثوانٍ (لقطةُ عميلٍ على Node، لا متصفّح)
# الغرض: إظهارُ النقلِ الابتدائيِّ والترقيةِ وسببِ الانفصالِ وإعادةِ الاتصالِ بأرقامٍ خام.
# ملاحظة: هذا العميلُ المختبريُّ لا يُمرّر lastSeq، فالتسلسلُ يبدأ من 1 بعد الإعادة.
# صفحةُ المسبار تُمرّرها فعلاً، فترى استئنافاً من آخرِ رقمٍ معروف.

### العميل
+  0.0s manager open — النقل الابتدائي: polling
+  0.0s upgrade → websocket   (fallback إلى polling: لا)
+  0.0s connect — id=23O-wKz4YbuYdETuAAAB النقل الآن: websocket
+  0.0s connected — socketNo=1 bootId=xnzmuzlh heartbeatMs=5000 استعادة الحالة=false نواقل الخادم=polling+websocket
+  5.0s hb seq=1 transport=websocket lastAckSeq=0 acks=0
+ 10.0s hb seq=2 transport=websocket lastAckSeq=1 acks=1
+ 12.5s — أطلب من الخادم هدمَ الناقل (محاكاة فقد الشبكة) —
+ 12.5s engine close — transport close
+ 12.5s disconnect — السبب: transport close
+ 13.5s reconnect_attempt #1
+ 13.5s manager open — النقل الابتدائي: polling
+ 13.5s reconnect ناجح بعد 1 محاولة
+ 13.5s upgrade → websocket   (fallback إلى polling: لا)
+ 13.5s connect — id=v0S8YTNYAEccn1BnAAAD النقل الآن: websocket
+ 13.5s connected — socketNo=2 bootId=xnzmuzlh heartbeatMs=5000 استعادة الحالة=false نواقل الخادم=polling+websocket
+ 17.0s echo ack ← {"type":"echo-reply","nonce":null,"socketNo":2,"transport":"websocket","serverIso":"2026-08-27T11:55:52.309Z"}
+ 18.5s hb seq=1 transport=websocket lastAckSeq=0 acks=0
+ 19.0s — إغلاق من العميل —
+ 19.0s disconnect — السبب: io client disconnect
+ 19.0s engine close — forced close

### سجل الخادم
[probe] DEC-06 Socket.IO probe on http://0.0.0.0:8796 bootId=xnzmuzlh heartbeatMs=5000 pingIntervalMs=25000 pingTimeoutMs=20000 transports=polling+websocket allowUpgrades=1
[io] open socket=1 id=23O-wKz4YbuYdETuAAAB resumedFrom=0 transport=polling open=1 at=2026-08-27T11:55:35.323Z
[io] upgrade socket=1 from=polling to=websocket
[io] transport-killed socket=1 transport=websocket
[io] closed socket=1 id=23O-wKz4YbuYdETuAAAB why="forced close" description="" lastSeq=2 acks=2 serverAcks=0 ackTimeouts=0 transport=websocket open=0
[io] open socket=2 id=v0S8YTNYAEccn1BnAAAD resumedFrom=0 transport=polling open=1 at=2026-08-27T11:55:48.820Z
[io] upgrade socket=2 from=polling to=websocket
[io] closed socket=2 id=v0S8YTNYAEccn1BnAAAD why="client namespace disconnect" description="" lastSeq=1 acks=1 serverAcks=0 ackTimeouts=0 transport=websocket open=0
