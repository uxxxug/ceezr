/**
 * الغرض: تحويلُ **أصغرِ وحدةٍ نقديّةٍ** إلى نصٍّ يُقرأُ — دالّةٌ نقيّةٌ واحدةٌ
 *   لا مصدرَ حقيقةٍ ثانياً للقاسمِ (القاعدة 0.6).
 * الحالة: منفَّذٌ فعليّاً — البند `SD-12` (زيادةٌ على `F3-08`).
 * ينتمي إلى: packages/domain/financial
 * يُستخدم من: `apps/miniapp/src/surfaces/account/account-view.ts` وحدَه اليومَ.
 * يُتوقع أن يستخدمه لاحقاً: كلُّ سطحٍ يعرضُ مبلغاً — وأوّلُهم
 *   `apps/miniapp/src/surfaces/driver/subscription/SubscriptionScreen.tsx` الذي
 *   **لم يُحوَّلْ في هذا البندِ عن قصدٍ**: قاسمُه اليدويُّ يعرضُ `45` وهذا
 *   المِلفُّ يعرضُ `45.00`، فتحويلُه **تغييرُ معروضٍ** خارجَ نطاقِ الحجزِ
 *   المُعلَنِ — وهوَ دَينٌ مُسجَّلٌ في `ADR 0126`. والبديلُ عن هذا المِلفِّ قاسمٌ
 *   مكتوبٌ بيدٍ في كلِّ شاشةٍ، وهوَ ما كانَ قائماً قبلَه.
 *
 * ## لِمَ وُجِدَ هذا المِلفُّ ولمَ في **النطاقِ** لا في شاشةٍ
 *
 * كانَ القاسمُ مكتوباً بيدٍ في `SubscriptionScreen.tsx` وحدَها
 * (`entry.amountMinor / 100`). ثمَّ جاءَ `SD-12` بموضعٍ ثانياً يعرضُ مبلغاً —
 * رصيدَ المحفظةِ الذي يمنعُ المحوَ — فصارَ الاختيارُ بينَ قاسمَينِ يفترقانِ
 * يوماً وبينَ موضعٍ واحدٍ يُقرَأُ. **وليسَ هذا رقمَ أعمالٍ** (القاعدة 0.3):
 * القاعدةُ تمنعُ سعراً ومدّةً وحدّاً تجاريّاً في شِفرةٍ، وعددُ خاناتِ العملةِ
 * **بِنيةُ العملةِ نفسِها** لا سياسةَ منصّةٍ تُغيَّرُ في `platform_settings`.
 *
 * ## وما لا يفعلُه عن قصدٍ (`ح-5`)
 *
 *   ــ **لا يُلحِقُ رمزَ عملةٍ ولا اسمَها**: العملةُ نصٌّ من القاموسِ بلغةِ
 *      صاحبِها، وإلحاقُها ههنا يجعلُ النطاقَ يعرفُ لغةً.
 *   ــ **لا يُنسِّقُ بأرقامٍ محليّةٍ**: `toLocaleString` يحتاجُ لغةً، واللغةُ
 *      ليست من شأنِ النطاقِ. والشاشةُ تُنسِّقُ إن أرادَت.
 *   ــ **لا يُدوِّرُ ولا يُقرِّبُ**: الأصغرُ عددٌ صحيحٌ، والقسمةُ ههنا تُبقي
 *      الخانتَينِ كما هما فلا يُخفى قرشٌ عن صاحبِه.
 */

/**
 * خاناتُ العملةِ الفرعيّةُ — **بِنيةٌ لا سياسةٌ**: الريالُ مئةُ هللةٍ، وذاكَ
 * وصفُ العملةِ لا رقمٌ تجاريٌّ يُقرَّرُ في إعدادٍ.
 */
const MINOR_UNITS_PER_MAJOR = 100;

/** عددُ الخاناتِ العشريّةِ في النصِّ — مشتقٌّ من القاسمِ لا مكتوبٌ ثانيةً. */
const FRACTION_DIGITS = String(MINOR_UNITS_PER_MAJOR).length - 1;

/**
 * الأصغرُ إلى نصٍّ بخانتَينِ. **والسالبُ يبقى سالباً**: رصيدٌ سالبٌ في محفظةِ
 * سائقٍ واقعةٌ تُقالُ لا تُخفى بقيمةٍ مطلقةٍ.
 *
 * وأمّا غيرُ المنتهي (`NaN` · `Infinity`) فيُقرأُ **صفراً بالصيغةِ نفسِها**
 * لا بنصٍّ ثانٍ: مسارُ تنسيقٍ واحدٌ يعني أنَّ ما يراه الإنسانُ في موضعِ المبلغِ
 * شكلٌ واحدٌ دائماً — و«صفرٌ» بجانبِ «45.00» في الشاشةِ نفسِها يُقرأُ عطباً.
 */
export function minorUnitsToMajorText(minor: number): string {
  const value = Number.isFinite(minor) ? minor : 0;
  const sign = value < 0 ? "-" : "";
  const absolute = Math.abs(Math.trunc(value));
  const major = Math.trunc(absolute / MINOR_UNITS_PER_MAJOR);
  const remainder = absolute % MINOR_UNITS_PER_MAJOR;
  return `${sign}${major}.${String(remainder).padStart(FRACTION_DIGITS, "0")}`;
}
