/**
 * الغرض: كائنات القيمة الثابتة (Value Objects) لوحدة policy — سياسات قابلة للتخصيص (تسعير، إلغاء)
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: domain/policy
 * يُتوقع أن يستخدمه لاحقاً: packages/application/policy/*, packages/infrastructure/policy/*
 * ملاحظات مستقبلية: تُفعَّل جزئياً عبر platform_settings في الأمر الثاني.
 */
export {};
