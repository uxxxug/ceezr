/**
 * الغرض: كائنات القيمة الثابتة (Value Objects) لوحدة documents — إدارة وثائق السائقين (رخصة، استمارة)
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: domain/documents
 * يُتوقع أن يستخدمه لاحقاً: packages/application/documents/*, packages/infrastructure/documents/*
 * ملاحظات مستقبلية: هيكل فقط.
 */
export {};
