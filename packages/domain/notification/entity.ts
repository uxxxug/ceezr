/**
 * الغرض: الكيانات الجذرية (Aggregates/Entities) لوحدة notification — إشعارات النظام
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: domain/notification
 * يُتوقع أن يستخدمه لاحقاً: packages/application/notification/*, packages/infrastructure/notification/*
 * ملاحظات مستقبلية: يُفعَّل جزئياً في الأمر الثاني.
 */
export {};
