/**
 * الغرض: محوّلات (Adapters) التكاملات الخارجية لوحدة policy — تنفيذ منافذ الدومين (ports)
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: infrastructure/policy
 * يُتوقع أن يستخدمه لاحقاً: apps/* عبر حقن التبعيات فقط، ولا يستوردها packages/domain/policy إطلاقاً
 * ملاحظات مستقبلية: تُفعَّل جزئياً عبر platform_settings في الأمر الثاني.
 */
export {};
