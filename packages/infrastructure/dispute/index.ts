/**
 * الغرض: محوّلات (Adapters) التكاملات الخارجية لوحدة dispute — تنفيذ منافذ الدومين (ports)
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: infrastructure/dispute
 * يُتوقع أن يستخدمه لاحقاً: apps/* عبر حقن التبعيات فقط، ولا يستوردها packages/domain/dispute إطلاقاً
 * ملاحظات مستقبلية: يُفعَّل في الأمر الثاني (المرحلة 2.4).
 */
export {};
