/**
 * الغرض: محوّلات (Adapters) التكاملات الخارجية لوحدة safety — تنفيذ منافذ الدومين (ports)
 * الحالة: منفّذ فعلياً في 2026-08-13؛ كان هيكلاً.
 * ينتمي إلى: infrastructure/safety
 * يُتوقع أن يستخدمه لاحقاً: apps/* عبر حقن التبعيات فقط، ولا يستوردها packages/domain/safety إطلاقاً
 * ملاحظات مستقبلية: كل تنفيذ كتابة يمر عبر RPC الذري لا استعلامات موازية.
 */
export * from "./safety-adapters.ts";
