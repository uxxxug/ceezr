/**
 * الغرض: محوّلات (Adapters) التكاملات الخارجية لوحدة dispatch — تنفيذ منافذ الدومين (ports)
 * الحالة: هيكل فقط — لا تنفيذ. لا تُضِف منطقاً هنا قبل أمر تفعيل صريح.
 * ينتمي إلى: infrastructure/dispatch
 * يُتوقع أن يستخدمه لاحقاً: apps/* عبر حقن التبعيات فقط، ولا يستوردها packages/domain/dispatch إطلاقاً
 * ملاحظات مستقبلية: يُفعَّل جزئياً — مطابقة بسيطة أولاً (القسم 3.3).
 */
export {};
