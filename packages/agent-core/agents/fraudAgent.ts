/**
 * الغرض: كشف إشارات الاحتيال — حسابات متعدّدة لشخص واحد، رحلات صورية، تلاعب
 *   بالتقييم أو بالاشتراك.
 * الحالة: **عقد فارغ محجوز — لا تنفيذ.** القسم 3، البند ب.3.
 * ينتمي إلى: packages/agent-core/agents
 * يُتوقع أن يستخدمه لاحقاً: registry.ts حين يُفتح هذا البند بأمر صريح
 * ملاحظات مستقبلية: هذا **أخطر الوكلاء الأربعة**، ولذلك سيكون آخرها لا أوّلها.
 *   مخرجه يمسّ رزق إنسان: اتهامٌ خاطئ بالاحتيال يوقف حساب سائق يعتاش منه. حين
 *   يُبنى، يُبنى بمستوى `SUGGEST` وحده — لا إيقاف تلقائي بحال — وبمسار تظلّم
 *   معرَّف قبل أوّل قرار لا بعد أوّل شكوى.
 *
 * ⚠️ لا تُنفّذ هذا الملف لأنه موجود. وجوده حجزٌ لمكانه لا إذنٌ بملئه.
 */

import type { Agent } from "./types.ts";

export const FRAUD_AGENT_ID = "fraud-agent";

/** العقد المستقبلي. لا مصنع له عمداً. */
export type FraudAgent = Agent;
