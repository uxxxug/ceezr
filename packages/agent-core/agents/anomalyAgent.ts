/**
 * الغرض: رصد الشذوذ التشغيلي — انحراف مفاجئ في معدّل الطلبات أو الإلغاءات أو
 *   زمن الاستجابة عن نمطه المعتاد.
 * الحالة: **عقد فارغ محجوز — لا تنفيذ.** القسم 3، البند ب.3.
 * ينتمي إلى: packages/agent-core/agents
 * يُتوقع أن يستخدمه لاحقاً: registry.ts حين يُفتح هذا البند بأمر صريح
 * ملاحظات مستقبلية: يحتاج قبل بنائه — لا بعده — خطّ أساس تاريخياً (baseline)
 *   لكل مدينة على حدة. «شاذّ» في مدينة صغيرة يوم جمعة هو «عادي» في كبيرة يوم أحد.
 *
 * ⚠️ لا تُنفّذ هذا الملف لأنه موجود. وجوده حجزٌ لمكانه في المعمارية لا إذنٌ بملئه.
 * السبب مذكور في `docs/adr/0012-isolated-agent-core-layer.md`: وكيلُ رصدٍ بلا خطّ
 * أساس يُنتج إنذاراتٍ كاذبة، والإنذار الكاذب يُدرّب الفريق على تجاهل الإنذارات —
 * فيصير الضرر أكبر من غيابه.
 */

import type { Agent } from "./types.ts";

export const ANOMALY_AGENT_ID = "anomaly-agent";

/** العقد المستقبلي. لا مصنع (`create...`) له عمداً: مصنعٌ موجود يُستدعى. */
export type AnomalyAgent = Agent;
