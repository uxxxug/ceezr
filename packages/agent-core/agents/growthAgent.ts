/**
 * الغرض: رصد فرص النمو — مدينة يرتفع فيها الطلب بلا سائقين كافين، أو خدمة
 *   يُسأل عنها ولا تُقدَّم.
 * الحالة: **عقد فارغ محجوز — لا تنفيذ.** القسم 3، البند ب.3.
 * ينتمي إلى: packages/agent-core/agents
 * يُتوقع أن يستخدمه لاحقاً: registry.ts حين يُفتح هذا البند بأمر صريح
 * ملاحظات مستقبلية: أقلّ الوكلاء إلحاحاً وأكثرها اعتماداً على حجم بيانات: فرصة
 *   النمو نمطٌ عبر آلاف الأحداث، لا استنتاجٌ من عشرات. بناؤه قبل تراكم البيانات
 *   يُنتج توصياتٍ مبنيّةً على ضجيج تبدو رؤىً.
 *
 * ⚠️ لا تُنفّذ هذا الملف لأنه موجود. وجوده حجزٌ لمكانه لا إذنٌ بملئه.
 */

import type { Agent } from "./types.ts";

export const GROWTH_AGENT_ID = "growth-agent";

/** العقد المستقبلي. لا مصنع له عمداً. */
export type GrowthAgent = Agent;
