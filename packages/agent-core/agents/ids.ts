/**
 * الغرض: معرّفات الوكلاء في مكان واحد، كي لا تُكتب نصّاً حرفياً في السجلّات
 *   والصلاحيات والتوجيه فتنحرف إحداها عن الباقي.
 * الحالة: منفّذ فعلياً — القسم 3، البند ب.3.
 * ينتمي إلى: packages/agent-core/agents
 * يُتوقع أن يستخدمه لاحقاً: registry.ts، router.ts، permissions.ts
 * ملاحظات مستقبلية: تُضاف معرّفات الوكلاء المحجوزين هنا **يوم تُنفَّذ**، لا قبله.
 *   معرّفٌ لوكيلٍ غير موجود يُغري بكتابة فرعٍ يتعامل معه.
 *
 * ملفٌ مستقلّ لا داخل `registry.ts`: `permissions.ts` يحتاج المعرّف ولا يجوز أن
 * يستورد السجلّ (فيجرّ معه الوكيل والذاكرة والمعرفة في دائرة استيراد).
 */

/** الوكيل الحقيقي الوحيد في هذه المرحلة. */
export const SUPPORT_TICKET_AGENT_ID = "support-ticket-agent";
