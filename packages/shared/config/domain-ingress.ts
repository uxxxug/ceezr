/**
 * الغرض: إعلانُ صنفِ `domain-ingress receipt` — القائمةُ المغلقةُ لجداولِ الاستلامِ
 *    التي يجوز لها وحدَها ألّا تحمل `city_id` لحظةَ الإنشاء، وفقَ **الملحقِ الحاكمِ
 *    2026-09-04** في [`docs/MASTER_DIRECTIVE.md`](../../../docs/MASTER_DIRECTIVE.md)،
 *    ومعها حدودُ الحجزِ التشغيليّةُ التي تُمرَّر وسائطَ إلى دالّةِ القاعدةِ فلا تُكتَب رقماً فيها.
 * الحالة: منفّذ فعلياً — إعلانٌ يقرؤه حارسُ `scripts/check-migrations.ts` وكودُ التشغيل.
 * ينتمي إلى: shared/config
 * يُتوقع أن يستخدمه لاحقاً: `scripts/check-migrations.ts`،
 *    `packages/infrastructure/messaging/telegram-update-intake.ts`، `apps/gateway/src/index.ts`
 * ملاحظات مستقبلية: إضافةُ اسمٍ ثانٍ هنا **لا تكفي**: الملحقُ الحاكمُ نصَّ أنّ الصنفَ
 *    مغلقٌ، وأنّ أيَّ صنفٍ ثانٍ يتطلّب **ملحقاً حاكماً جديداً** — لا ADR، ولا تعليقاً في
 *    هجرةٍ، ولا استثناءً في حارس. فمن أضاف اسماً بلا ملحقٍ فقد خالفَ النصَّ لا الحارسَ.
 *
 * ## لماذا إعلانٌ في الشيفرةِ لا قائمةُ استثناءٍ في الحارس
 *
 * قائمةُ الاستثناءِ داخلَ الحارسِ تجعل الحارسَ خصماً لنفسِه: كلُّ من ضاق به سطرٌ
 * أضاف اسماً إلى القائمةِ فمرّ. والإعلانُ هنا يجعل الانتماءَ **مزدوجَ الشرطِ**:
 * اسمٌ في هذه القائمةِ، **و**تصريحٌ صريحٌ في الهجرةِ نفسِها بالصيغةِ المُثبَّتةِ أدناه.
 * فلا يمرّ جدولٌ بإحداهما وحدَها، ويبقى الاستثناءُ مغلقاً وقابلاً للتحقّقِ آليّاً.
 *
 * ## ما لا يُعفى منه هذا الصنف
 *
 * `RLS` لا تسقط، ولا يُشتَقّ `city_id` اصطناعيّاً، ولا يُخزَّن التحديثُ الخام —
 * والملحقُ نصَّ على الأوّلين، و[ADR 0054](../../../docs/adr/0054-telegram-webhook-durable-ingest-and-dedup.md) §٧/١ على الثالث.
 */

/**
 * جداولُ صنفِ `domain-ingress receipt`. **قائمةٌ مغلقةٌ** بمُدخلَينِ اليوم.
 *
 * `telegram_update_receipts`: سجلُّ استلامِ تحديثاتِ تيليجرام. لا مدينةَ له لحظةَ
 * الاستلام لأنّ القرارَ يسبق أيَّ تفسيرٍ للحمولةِ — فما ليس مقروءاً بعدُ لا يُنسَب
 * إلى مدينةٍ إلّا باشتقاقٍ اصطناعيٍّ، وهو المنهيُّ عنه في الملحقِ نفسِه.
 *
 * `telegram_update_jobs`: حاملُ حمولةِ التحديثِ الخامِّ المربوطُ بالإيصالِ 1:1 عبرَ
 * (bot, update_id). النصفُ الثانيُ في الصنفِ وفقَ الملحقِ الحاكمِ 2026-09-07
 * و[ADR 0057](../../../docs/adr/0057-telegram-ingress-bound-payload-carrier-and-worker.md):
 * لا مدينةَ له كالإيصالِ، لكنّه يخزّنُ الحمولةَ الخامَّ (JSONB) التي تُمكِّنُ الدرينرَ
 * الخلفيَّ من المعالجةِ بعدَ ACK دونَ إعادةِ استلامٍ. الحذفُ تقنيٌّ يتبعُ الإيصالَ.
 */
export const DOMAIN_INGRESS_RECEIPT_TABLES = [
  "telegram_update_receipts",
  "telegram_update_jobs",
] as const;

export type DomainIngressReceiptTable = (typeof DOMAIN_INGRESS_RECEIPT_TABLES)[number];

/**
 * الصيغةُ المُثبَّتةُ التي يجب أن تظهر في نصِّ الهجرةِ لإعلانِ انتماءِ جدولٍ إلى الصنف.
 * يُقرأ منها الاسمُ ويُطابَق بالقائمةِ أعلاه، فلا يُعفى جدولٌ بتصريحٍ بلا إعلانٍ ولا
 * بإعلانٍ بلا تصريح. تُبنى هنا في موضعٍ واحدٍ كي لا يختلف الحارسُ عن الهجرةِ حرفاً.
 */
export const DOMAIN_INGRESS_RECEIPT_DECLARATION_PREFIX = "-- domain-ingress-receipt:";

/** التصريحُ الحرفيُّ الواجبُ في هجرةِ جدولٍ من الصنف. */
export function domainIngressReceiptDeclaration(table: DomainIngressReceiptTable): string {
  return `${DOMAIN_INGRESS_RECEIPT_DECLARATION_PREFIX} ${table}`;
}

/** هل الاسمُ مُعلَنٌ في القائمةِ المغلقة؟ */
export function isDomainIngressReceiptTable(name: string): name is DomainIngressReceiptTable {
  return (DOMAIN_INGRESS_RECEIPT_TABLES as readonly string[]).includes(name);
}

/**
 * مهلةُ اعتبارِ الحجزِ مهجوراً، ثوانيَ.
 *
 * **حدٌّ تشغيليٌّ تقنيٌّ لا سياسةٌ تجاريّةٌ**، فلا مكانَ له في `platform_settings` —
 * والسابقةُ الفاصلةُ في `apps/gateway/src/index.ts` عندَ حدودِ المعدَّل بنصِّها:
 * «حدود تقنية لا تجارية: لا مكان لها في platform_settings». ويُمرَّر **وسيطاً** إلى
 * دالّةِ القاعدةِ فلا رقمَ محفورٌ فيها.
 *
 * لماذا ثلاثونَ ثانيةً: تيليجرام يُعيد إرسالَ التحديثِ إن لم يُجَب سريعاً، ومعالجةُ
 * تحديثٍ واحدٍ عندنا دونَ ثانيةٍ في الحالةِ السويّة. فالمهلةُ أوسعُ من أيِّ معالجةٍ
 * سليمةٍ بأضعافٍ، وأضيقُ من أن يبقى تحديثٌ محجوزاً بعمليةٍ ماتت دقائقَ بلا استرجاع.
 */
export const TELEGRAM_INTAKE_CLAIM_TIMEOUT_SECONDS = 30;

/**
 * مهلةُ اعتبارِ إيجارِ الوظيفةِ مهجوراً (للدرينرِ الخلفيِّ)، ثوانيَ. حدٌّ تقنيٌّ
 * كالسابقِ، يُمرَّر وسيطاً إلى `claim_telegram_update_job` لا رقمٌ محفورٌ فيها.
 * أوسعُ من أيِّ معالجةٍ سليمةٍ، وأضيقُ من أن يبقى محجوزاً بدريَنرٍ مات دقائقَ.
 */
export const TELEGRAM_JOB_LEASE_TIMEOUT_SECONDS = 30;

/**
 * أقصى محاولاتٍ لمعالجةِ الوظيفةِ قبلَ الموتِ النهائيِّ (`dead`). حدٌّ تقنيٌّ
 * لمنعِ إعادةِ المعالجةِ الأبديةِ لصفٍّ لن يُقبلَ (سمٌّ هادئٌ). يُمرَّر وسيطاً.
 */
export const TELEGRAM_JOB_MAX_ATTEMPTS = 5;

/**
 * فترةُ الاحتياطِ بينَ المحاولتَين (ثوانٍ). تُؤجِّلُ إعادةَ الالتقاطِ بعدَ الفشلِ كي
 * لا يلتقطَها الدرينرُ في الشوطِ نفسه فيستنفدَ المحاولاتِ كلَّها دفعةً واحدة بلا
 * احتياطٍ. حدٌّ تقنيٌّ بحتٌ يُمرَّر وسيطاً إلى `finish_telegram_update_job`.
 */
export const TELEGRAM_JOB_RETRY_DELAY_SECONDS = 5;

/**
 * ## حدودُ الضغطِ العكسيِّ لطابورِ الوارِدِ — `F6-06`
 *
 * أربعةُ حدودٍ تُكمِلُ `TELEGRAM_JOB_MAX_ATTEMPTS` أعلاه، فتصيرُ أبعادُ البندِ
 * الستّةُ (سعةٌ، عمرُ أقدمِ حدثٍ، إعادةُ محاولةٍ، طابورُ موتى، حدُّ منتِجٍ،
 * تزامنُ مستهلكٍ) كاملةً لهذا الطابورِ. وسكنَت ههنا لا في `platform_settings`
 * بالحُجّةِ المكتوبةِ فوقَ `TELEGRAM_INTAKE_CLAIM_TIMEOUT_SECONDS` حرفاً: حدودٌ
 * تشغيليّةٌ تقنيّةٌ لا سياساتٌ تجاريّةٌ. ولأنَّ الجدولَ **لا `city_id` له** بنصِّ
 * الملحقِ الحاكمِ، فبديلُ ذلك مِلءُ مدينةٍ مصطنَعةٍ (منهيٌّ عنه في القاعدةِ ٠.٤)
 * أو قراءةُ مدينةٍ عشوائيّةٍ (كذبٌ يُقرأُ رقماً صحيحاً) — وكلاهما أسوأُ من ثابتٍ
 * مُعلَنٍ يُراجَعُ في مُراجعةِ شيفرةٍ. وموضعُها مُسجَّلٌ في سجلِّ الطوابيرِ
 * `packages/shared/config/queue-backpressure.ts` ويُنفِذُه حارسٌ في CI.
 */

/**
 * سعةُ الطابورِ: أقصى ما يُقبَلُ أن يبقى غيرَ منتهٍ. عشرونَ ألفاً لأنَّ الوارِدَ
 * **لا يُملَكُ**: تيليجرام يُرسِلُ ما يُرسِلُ، والحدُّ ههنا ليسَ سقفَ إنتاجٍ
 * نتحكّمُ فيه بل عتبةَ **إنذارٍ وتهدئةٍ**. وعشرونَ ألفَ تحديثٍ في طابورٍ يعني
 * انقطاعاً في الدرينرِ لدقائقَ عندَ معدَّلِنا المُقاسِ، وهوَ ما يجبُ أن يُرى.
 */
export const TELEGRAM_JOB_DEPTH_LIMIT = 20000;

/**
 * أقصى عمرٍ مقبولٍ لأقدمِ وظيفةٍ **مستحقّةٍ**، ثوانيَ. مئةٌ وعشرونَ ثانيةً لأنَّ
 * التحديثَ يفقدُ معناه بسرعةٍ: رسالةُ راكبٍ تُجابُ بعدَ دقيقتَينِ رسالةٌ مهجورةٌ
 * في نظرِ صاحبِها. ولا يُقاسُ العمرُ من `created_at` بل من `next_attempt_at`
 * فلا تُحسَبُ فترةُ الاحتياطِ بينَ محاولتَينِ عُلوقاً.
 */
export const TELEGRAM_JOB_OLDEST_AGE_LIMIT_SECONDS = 120;

/**
 * أقصى موتى مقبولونَ في نافذةِ `QUEUE_DEAD_WINDOW_SECONDS`. خمسونَ في ساعةٍ:
 * موتُ وظيفةٍ وارِدةٍ **عطبٌ عندَنا لا عندَ المُرسِلِ** — فالوظيفةُ تموتُ بعدَ
 * استنفادِ محاولاتِها، أي أنَّ معالجَنا رفضَها خمسَ مرّاتٍ. فالحدُّ ضيّقٌ عن قصدٍ.
 */
export const TELEGRAM_JOB_DEAD_LIMIT = 50;

/**
 * حدُّ بابِ الإيداعِ: عندَ تجاوزِه يُجابُ الويبهوكُ بـ`429` فيُعيدُ تيليجرام
 * الإرسالَ لاحقاً. **ولا يُسقَطُ تحديثٌ**: الإعادةُ من تيليجرام أمانةٌ موثَّقةٌ
 * (`ADR 0054` §٣)، والرفضُ الصريحُ أصدقُ من قبولٍ لا يُعالَجُ. وهوَ أدنى من
 * السعةِ (لا مساويها) كي يبقى فوقَ الحدِّ متنفَّسٌ للمعالجةِ الجاريةِ فلا يُرفَضُ
 * وارِدٌ وطابورٌ ممتلئٌ في اللحظةِ نفسِها.
 */
/**
 * مهلةُ العودةِ التي تُعلَنُ في `Retry-After` حينَ يُردُّ الاستلامُ لاكتظاظِ
 * الطابورِ (F6-06). قصيرةٌ عمداً: الاكتظاظُ عابرٌ بطبعِه — الدرينرُ يستنزفُ
 * الطابورَ في ثوانٍ — وإطالتُها تُؤخِّرُ عملاً صار مقدوراً عليه.
 */
export const TELEGRAM_JOB_PRODUCER_RETRY_AFTER_SECONDS = 5;

export const TELEGRAM_JOB_PRODUCER_LIMIT = 15000;

/**
 * أقصى وظائفَ يعالجُها شوطُ درينرٍ واحدٌ. **حدٌّ يُصلِحُ عطباً قائماً**:
 * `drainTelegramUpdateJobsOnce` كانت تلتقطُ في `for (;;)` حتّى يفرغَ الطابورُ —
 * شوطٌ غيرُ محدودٍ يخالفُ القسمَ ١٤ («لا إعادةَ محاولةٍ بلا حدٍّ») روحاً ونصّاً:
 * دفعةٌ من ألفِ تحديثٍ كانت تحبسُ الشوطَ دقائقَ، فلا يُصرَّفُ إقلاعٌ نظيفٌ ولا
 * تُقرأُ نبضةٌ في `/ready`، وعطبٌ متكرّرٌ في المعالجةِ كان يُحوِّلُ الحلقةَ إلى
 * حرقٍ لوحدةِ المعالجةِ. ومئةٌ لأنَّ الشوطَ يجري كلَّ ثانيةٍ، فمئةٌ في الثانيةِ
 * طاقةٌ أعلى من أيِّ معدَّلٍ وارِدٍ متوقَّعٍ بأضعافٍ، والفائضُ يُلتقَطُ في الشوطِ
 * التالي لا يُفقَدُ.
 */
export const TELEGRAM_JOB_CONSUMER_CONCURRENCY = 100;
