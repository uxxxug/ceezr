/**
 * الغرض: حدودُ الضغطِ العكسيِّ الستّةُ لطابورِ `move_event_outbox` — صادرُ أحداثِ
 *    MOVE إلى CORE (`W-5`)، مُعلَنةً في **موضعٍ واحدٍ** تُمرَّرُ منه وسائطَ إلى دوالِّ
 *    القاعدةِ فلا رقمَ محفورٌ فيها.
 * الحالة: منفّذ فعلياً — 2026-09-11. الناقلُ الشبكيُّ محجوبٌ بـ`DEP-CORE-001`،
 *    والحدودُ مُنفَذةٌ على الحجزِ والتخلّي القائمَينِ في القاعدةِ.
 * ينتمي إلى: shared/config
 * يُستخدم من: `packages/shared/config/queue-backpressure.ts`،
 *    `packages/infrastructure/wasla/operational-job-repository.ts`،
 *    `scripts/check-queue-backpressure.ts`
 * ملاحظات مستقبلية: إذا حملَ الطابورُ `city_id` يوماً (يقتضي `DEP-CORE-006`) فمَوضِعُ
 *    هذه الحدودِ يصيرُ `platform_settings` كأخواتِها في `notification_outbox`،
 *    وتُنقَلُ بترحيلةٍ لا تُنسَخُ فيصيرَ للحدِّ موضعانِ.
 *
 * ## لماذا ثوابتُ شيفرةٍ لا مفاتيحُ إعداداتٍ
 *
 * القاعدةُ الحاسمةُ في `queue-backpressure.ts` أنَّ كلَّ بُعدٍ يسكنُ حيثُ يسكنُ حدُّ
 * إعادةِ المحاولةِ لطابورِه. وهذا الطابورُ **لا `city_id` له** (عقدُ
 * `core.fulfillment.created` لا يحملُ مدينةً — `DEP-CORE-006`)، فمِلءُ مدينةٍ
 * مصطنَعةٍ منهيٌّ عنه في القاعدةِ ٠.٤، وقراءةُ حدٍّ من مدينةٍ عشوائيّةٍ كذبٌ يُقرأُ
 * رقماً صحيحاً. فالسابقةُ المتَّبَعةُ حرفاً هيَ `telegram_update_jobs`: ثوابتُ
 * مُعلَنةٌ في `shared/config` تُمرَّرُ وسائطَ.
 *
 * ## لماذا هذه الأرقامُ
 *
 * وهيَ **قراراتٌ مُعلَنةٌ لا قياساتٌ** — معايرتُها تنتظرُ ناقلاً حقيقيّاً، ولا
 * يُدَّعى لها قياسٌ اليومَ.
 */

/**
 * سعةُ الطابورِ: أقصى ما يُقبَلُ أن يبقى غيرَ مُسلَّمٍ. ألفٌ لأنَّ الصادرَ
 * **مملوكٌ لنا** خلافاً للوارِدِ: كلُّ صفٍّ فيه أثرُ انتقالِ حالةٍ واحدٍ في مهمّةٍ
 * واحدةٍ، فألفُ حدثٍ غيرِ مُسلَّمٍ يعني انقطاعاً في CORE أو في العاملِ لا موجةَ
 * طلبٍ — وهوَ ما يجبُ أن يُرى إنذاراً لا أن يُبتلَعَ.
 */
export const MOVE_EVENT_OUTBOX_DEPTH_LIMIT = 1000;

/**
 * أقصى عمرٍ مقبولٍ لأقدمِ حدثٍ **مستحقٍّ**، ثوانيَ. ثلاثُمئةٍ لأنَّ المُتلقّيَ
 * نظامٌ لا إنسانٌ: خمسُ دقائقَ لا يشكو منها راكبٌ، لكنَّ CORE يبني عليها قراراً
 * تجاريّاً (فاتورةٌ، تسويةٌ، إشعارُ عميلٍ)، فتأخُّرٌ أطولُ يجعلُ حالةَ التنفيذِ
 * عندَ CORE كاذبةً. ولا يُقاسُ العمرُ من `created_at` بل من `next_attempt_at` فلا
 * تُحسَبُ فترةُ الاحتياطِ بينَ محاولتَينِ عُلوقاً.
 */
export const MOVE_EVENT_OUTBOX_OLDEST_AGE_LIMIT_SECONDS = 300;

/**
 * أقصى محاولاتٍ قبلَ الموتِ النهائيِّ (`dead_at`). ثمانيةٌ: أعلى من سقفِ الوارِدِ
 * (خمسٌ) عن قصدٍ — إسقاطُ حدثٍ صادرٍ يُفقِدُ CORE نتيجةَ تنفيذٍ لا يعرفُها من
 * موضعٍ آخرَ، فيبقى تنفيذٌ عندَه «مُرسَلاً» إلى الأبدِ. ومعَ التراجعِ الخطّيِّ
 * أدناه تمتدُّ المحاولاتُ الثمانِ إلى نحوِ ثمانيَ عشرةَ دقيقةً، وهيَ أوسعُ من كلِّ
 * انقطاعٍ عابرٍ وأضيقُ من إعادةِ محاولةٍ بلا حدٍّ (القسم ١٤).
 */
export const MOVE_EVENT_OUTBOX_MAX_ATTEMPTS = 8;

/**
 * أقصى موتى مقبولونَ في نافذةِ قياسِ الطوابيرِ. **عشرةٌ**: موتُ حدثٍ صادرٍ عطبٌ
 * عندَنا أو عندَ المُتلقّي، وفي الحالَينِ حالةُ تنفيذٍ صارت مختلفةً بينَ
 * المستودعَينِ. فالحدُّ ضيّقٌ عن قصدٍ كي يُرى قبلَ أن يصيرَ تسويةً خاطئةً.
 */
export const MOVE_EVENT_OUTBOX_DEAD_LIMIT = 10;

/**
 * حدُّ بابِ الإيداعِ. **مساوٍ للسعةِ لا أدنى منها**، وذلكَ خلافُ الوارِدِ عن قصدٍ:
 * الإيداعُ ههنا يجري **داخلَ معاملةِ تغييرِ الحالةِ**، فردُّه يعني رفضَ قبولِ
 * مهمّةٍ أو رفضَ إغلاقِها — أي تعطيلُ عملٍ ميدانيٍّ حقيقيٍّ لعُطلٍ في التسليمِ.
 * والصحيحُ أن يُقبَلَ تغييرُ الحالةِ ويُرى الاكتظاظُ إنذاراً، لا أن يُمنَعَ سائقٌ
 * من إتمامِ مهمّةٍ لأنَّ CORE لا يستقبلُ.
 */
export const MOVE_EVENT_OUTBOX_PRODUCER_LIMIT = 1000;

/**
 * أقصى أحداثٍ يُسلِّمُها شوطُ عاملٍ واحدٌ. **عشرةٌ**: الحجزُ صفٌّ واحدٌ في النداءِ
 * (`for update skip locked`)، والشوطُ يجري كلَّ ثانيةٍ، فعشرةٌ في الثانيةِ طاقةٌ
 * أعلى من كلِّ معدَّلِ انتقالاتِ حالةٍ متوقَّعٍ بأضعافٍ، والفائضُ يُلتقَطُ في
 * الشوطِ التالي لا يُفقَدُ. وحدٌّ صغيرٌ يُبقي الشوطَ قصيراً فيُصرَّفُ إقلاعٌ نظيفٌ.
 */
export const MOVE_EVENT_OUTBOX_CONSUMER_CONCURRENCY = 10;

/**
 * فترةُ التراجعِ بينَ محاولتَينِ (ثوانٍ)، تُضرَبُ في عددِ المحاولاتِ فيصيرُ
 * التراجعُ خطّيّاً لا ثابتاً — فلا يُلتقَطُ الصفُّ في الشوطِ نفسِه فتُستنفَدَ
 * محاولاتُه دفعةً واحدةً بلا احتياطٍ.
 */
export const MOVE_EVENT_OUTBOX_BACKOFF_SECONDS = 30;

/**
 * مهلةُ اعتبارِ حجزِ التسليمِ مهجوراً (ثوانٍ). أوسعُ من كلِّ تسليمٍ سويٍّ بأضعافٍ،
 * وأضيقُ من أن يبقى حدثٌ محجوزاً بعاملٍ ماتَ دقائقَ بلا استرجاعٍ.
 */
export const MOVE_EVENT_OUTBOX_CLAIM_STALE_SECONDS = 60;
