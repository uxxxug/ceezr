/**
 * الغرض: قراءةُ آخر موقعٍ معروفٍ لسائق طلبٍ بالرمز المؤقّت — بلا مصادقة، ولما
 *   يُعرض في متصفّحٍ مجهول: نقطةٌ ووقتٌ وحالةٌ، لا هويّةَ أحد.
 * الحالة: منفّذ فعلياً — أُضيف في 2026-08-14 (§4.2 من أمر الإطلاق التجاري).
 * ينتمي إلى: application/tracking
 * يُتوقع أن يستخدمه: apps/gateway/src/routes/public-tracking.ts.
 *
 * ## ثلاثةُ أجوبةٍ لا جوابان
 *
 * «غيرُ صالح» و«صالحٌ بموقع» ليستا كلَّ الحالات: بينهما رمزٌ صالحٌ لسائقٍ لم يُرسل
 * موقعاً بعد (قَبِل قبل ثوانٍ، أو أغلق تطبيقه). ودمجُ هذه في «غير صالح» كان
 * سيُظهر للمنتظِر «انتهى الرابط» وهو سليم — أسوأُ خطأٍ ممكن هنا لأنّه يدفعه إلى
 * الاتصال بالدعم بلا سبب. فلها حالتُها: `awaiting`.
 *
 * ## `active` من حالة الطلب لا من الانقضاء
 *
 * الرحلةُ تنتهي والرابطُ يبقى ربعَ ساعة (المهلة من `platform_settings`): في هذه
 * النافذة `active=false` والموقعُ آخرُ ما عُرِف. والصفحةُ تقولها صريحاً «انتهت
 * الرحلة — آخر موقع معروف»، فلا يُحدِّق أحدٌ في نقطةٍ ساكنة يظنّها عطلاً.
 *
 * ## ولماذا لا تُخفى الدقّةُ أو تُقرَّب النقطة
 *
 * سُئل السؤال: أليس نشرُ إحداثيةٍ دقيقةٍ لسائقٍ في صفحةٍ عامّة تعريضاً له؟ والجوابُ
 * أنّ الغرضَ نفسَه هو أن يُرى موقعُه لحظةً بلحظة — وتقريبُ النقطة إلى مئة متر
 * يُلغي الفائدة (المنتظِر يريد أن يعرف أنّه على الباب). والحمايةُ في مكانها
 * الصحيح: الرمزُ لا يُخمَّن، ويموت بانتهاء الرحلة، ويُلغيه صاحبُه في ضغطة.
 */

import { ok, type Result } from "../../shared/result/index.ts";
import type { PortFailureError } from "../ports/index.ts";
import type { TrackingReadState, TrackingTokenRpcPort } from "./tracking-token-ports.ts";

export interface GetLivePositionDeps {
  readonly tokens: TrackingTokenRpcPort;
}

/**
 * لا تُرجِع خطأً لرمزٍ غير صالح: «غيرُ صالح» جوابٌ صحيح لسؤالٍ مشروع، والبوابة
 * تُترجمه 404. وأمّا عطلُ القاعدة فخطأٌ حقيقيّ يُمرَّر كما هو ليصير 503 لا 404 —
 * الفرقُ يهمّ من يراقب: 404 حالةُ مستخدم، و503 عطلُ منصّة.
 */
export async function getLivePosition(
  token: string,
  deps: GetLivePositionDeps,
): Promise<Result<TrackingReadState, PortFailureError>> {
  const trimmed = token.trim();
  if (trimmed === "") {
    // رمزٌ فارغ لا يُسأل عنه القاعدة: نداءٌ بلا فائدةٍ يُستهلك به اتصال.
    return ok({ kind: "invalid" });
  }
  return await deps.tokens.read(trimmed);
}
