-- =============================================================================
-- migration-phase: validate
-- الغرض: `PD-021` — توثيقُ قيدِ `notification_outbox_kind_check` بعدَ توسيعِهِ
--   بالنوعَينِ `safety_resolution_closed` و`safety_resolution_blocked` في الهجرةِ
--   `20260921000001`. أُضيفَ القيدُ `not valid` لئلّا يَمسحَ الجدولَ كلَّه تحتَ
--   قفلٍ حاجزٍ، وههنا يُصادَقُ بقفلٍ أخفَّ (`SHARE UPDATE EXCLUSIVE`) لا يمنعُ
--   الكتابةَ. والقيدُ **مجموعةٌ فائقةٌ** تحوي القديمَ كلَّهُ، فلا صفَّ موجوداً
--   يُرفَضُ — والتوثيقُ شكلٌّ يُكملُ النمطَ لا حاجةٌ منطقيّةٌ.
-- الحالة: منفّذ فعلياً — البند `PD-021`.
-- ينتمي إلى: supabase/migrations
-- =============================================================================

alter table notification_outbox validate constraint notification_outbox_kind_check;
