-- migration-phase: validate
-- ------------------------------------------------------------------------------
-- SEC-21 — تصديقُ قيدِ أصلِ جلساتِ الإدارةِ
--
-- القيدُ `admin_sessions_origin_check` أُضيفَ `not valid` في طورِ `expand`
-- (الهجرةُ `20260923010000`) — تصديقُهُ هنا بقفلٍ أخفَّ لا يمنعُ القراءةَ.
-- والصفوفُ القائمةُ كلُّها `telegram_code` بالقيمةِ الافتراضيّةِ، فالتصديقُ
-- إقرارٌ بحقيقةٍ قائمةٍ لا تبديلٌ لها (`ح-8`).
--
-- الحاكم: `CAP-007` · `F7-07` · `ADR 0176`.
-- ------------------------------------------------------------------------------

alter table admin_sessions validate constraint admin_sessions_origin_check;
