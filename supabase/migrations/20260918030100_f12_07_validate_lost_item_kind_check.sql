-- migration-phase: validate
-- =============================================================================
-- `F12-07` — توثيقُ قيدِ `notification_outbox_kind_check` بعدَ توسيعِهِ بالنوعِ
--   `lost_item_report` في الهجرةِ `20260918030000`. أُضيفَ القيدُ `not valid` لئلّا
--   يَمسحَ الجدولَ كلَّه تحتَ قفلٍ حاجزٍ، وههنا يُصادَقُ بقفلٍ أخفَّ (`SHARE UPDATE
--   EXCLUSIVE`) لا يمنعُ الكتابةَ. والقيدُ **مجموعةٌ فائقةٌ** تحوي القديمَ كلَّهُ،
--   فلا صفَّ موجوداً يُرفَضُ — والتوثيقُ شكلٌّ يُكملُ النمطَ لا حاجةٌ منطقيّةٌ.
-- الحالة: منفّذ فعلياً — البند `F12-07`.
-- ينتمي إلى: supabase/migrations
-- =============================================================================

alter table notification_outbox validate constraint notification_outbox_kind_check;
