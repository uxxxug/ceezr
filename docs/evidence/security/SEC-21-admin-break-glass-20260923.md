# SEC-21 — البابُ الموازي للإدارةِ (Break-Glass): عاملٌ ثانٍ على الهويّةِ الداخليّةِ لا على تيليجرام

**التاريخ:** 2026-09-23
**البند:** `SEC-21` (`docs/ROADMAP-MASTER.md` §11-و · حجزُ النطاقِ في `docs/ROADMAP.md`)، الحالةُ `[ ]`
**الحاكم:** `ADR 0176` · `ح-7` · `ح-8`
**الفرع:** `feat/sec-21-admin-break-glass`

---

## ١. الفجوةُ المُسمّاةُ

مسؤولٌ فقدَ حسابَ تيليجرامَ (أو تعطَّلت قناةُ الدخولِ بسرمديّةٍ) لا يملكُ بابًا إلى اللوحةِ إطلاقًا: `open_admin_session` تُفتَحُ بجلسةِ تيليجرامَ فحسبُ. والبوّابةُ كاملةُ الاعتمادِ على قناةٍ واحدةٍ — من نقاطِ ضعفِ المفاتيحِ الوحيدةِ (`ADR 0176`).

## ٢. العلاجُ

عاملٌ ثانٍ **داخليٌّ** على الهويّةِ الإداريّةِ: زوجُ اسمِ دخولٍ وكلمةِ سرٍّ محفوظةٌ بـ`scrypt`، معَ رمزِ `TOTP` (RFC 6238) سرُّهُ مُشفَّرٌ بـ`AES-256-GCM` بمفتاحِ بيئةٍ `ADMIN_BREAK_GLASS_TOTP_KEY`. الدخولُ منَ البابِ يفتحُ جلسةَ إدارةٍ **موسومةَ الأصلِ** `admin_sessions.origin='break_glass'`، وتسجيلُ الاعتمادِ (وتدويرُهُ وتعطيلُهُ) **من جلسةِ تيليجرامَ فحسبُ** — فالبابُ لا يُنشئُ بابًا موازيًا لنفسِهِ.

## ٣. المكوّناتُ

### ٣-١. الهجرتانِ

- **`20260923010000_sec_21_admin_break_glass.sql` (طورُ `expand`):**
  - جدولُ `admin_break_glass_credentials` — لكلِّ مسؤولٍ صفٌّ واحدٌ (`user_id` فريدٌ) باسمِ دخولٍ فريدٍ (`^[a-z0-9_-]{3,}$`)، `password_hash` (scrypt بصيغةٍ مُرمَّزةٍ ذاتيةِ الشرحِ)، `totp_secret_encrypted` (AES-GCM بصيغةِ `v1:iv:tag:cipher`)، `last_totp_counter` (رفضُ الإعادةِ)، `failed_attempts` و`locked_until` (الإقفالُ)، `is_active` — RLS مفعّلةٌ وسلبُ التنفيذِ من `anon/authenticated`.
  - عمودُ `admin_sessions.origin` (افتراضيُّ `'telegram_code'`) — وسومُ الجلساتِ القائمةِ والجديدةِ بأصلِها.
  - خمسُ دوالَّ `security definer` معَ `revoke execute from public, anon, authenticated`:
    - `admin_break_glass_load_attempt` — قراءةٌ للفحصِ، و`INVALID_CREDENTIALS` موحَّدةٌ للاسمِ المجهولِ.
    - `admin_break_glass_finish_success` — ذرّيّةٌ: شرطُ `for update` يجمعُ (وليسَ يفترضُ) القفلَ والصلاحيّةَ وعدمَ الإعادةِ (`counter > last_counter`) وإعادةَ فحصِ الدورِ والانضمامِ في `users`، ثمَّ فتحُ الجلسةِ بـ`origin='break_glass'` وتدقيقُ `admin.break_glass_session_opened`.
    - `admin_break_glass_finish_failure` — عدُّ الفشلِ والإقفالُ بعدَ ٥ (`now() + make_interval`) وتدقيقُ `admin.break_glass_login_rejected`، محصورٌ بمسؤولينَ أحياءٍ (لا عدَّ لمُزالِ الدورِ — عدُّهُ موتٌ لا حياةٌ).
    - `admin_break_glass_enroll` — حرّاسُ: جلسةٌ حيّةٌ · مسؤولٌ · **`origin='telegram_code'`** · صحةُ الاسمِ · `LOGIN_NAME_TAKEN` لاسمِ مسؤولٍ آخرَ · `upsert` على `user_id` = تدويرٌ بعلمٍ `rotated`. تدقيقُ `admin.break_glass_enrolled`.
    - `admin_break_glass_disable` — الحرّاسُ نفْسُها (تيليجرامُ فحسبُ) وتعطيلٌ لا حذفٌ. تدقيقُ `admin.break_glass_disabled`.
  - كلُّ العباراتِ مُتراجِعةٌ (`if exists/not exists`).
- **`20260923010100_sec_21_admin_sessions_origin_validate.sql`** — تصديقُ قيدِ التحقُّقِ على `origin` بعدَ تعبئةِ الأثرِ.

### ٣-٢. التشفيرُ في TypeScript وحده (`packages/infrastructure/identity/break-glass-crypto.ts`)

- `hashBreakGlassPassword` / `verifyBreakGlassPassword` — scrypt (N=16384 · r=8 · p=1) بصيغةٍ `scrypt:N:r:p:saltB64:hashB64` ومقارنةٍ ثابتةِ التوقيتِ.
- `totpCode` / `totpCounterAt` / `verifyTotp` — HMAC-SHA1 واقتطاعٌ ديناميكيٌّ بستِّ خاناتٍ، نافذةُ ±1، ورفضُ الإعادةِ (المرشَّحُ ≤ `lastCounter` يُتجاوَزُ).
- `deriveAesKey` / `encryptTotpSecret` / `decryptTotpSecret` — مفتاحُ البيئةِ (≥32 بايتًا) يُشتقُّ بـ`sha256` إلى 32 بايتًا؛ AES-256-GCM؛ الفشلُ `null` لا نصٌّ فاسدٌ.
- `generateTotpSecret` (20 بايتًا base32) · `base32Encode`/`base32Decode` · `breakGlassOtpauthUri`.
- المفتاحُ في `packages/shared/config/index.ts`: `ADMIN_BREAK_GLASS_TOTP_KEY` — اختياريٌّ؛ عندَ غيابِهِ يُرفَضُ الدخولُ منَ البابِ جماعيًّا برَدٍّ موحَّدٍ (لا 500)؛ عندَ حضورِهِ يُفحَصُ حرفيّةً وطولًا.

### ٣-٣. المنفذُ والبوّابةُ

- `apps/gateway/src/admin/break-glass.ts` — `createAdminBreakGlassPort`: `login()` (قراءةٌ → مضاهِئُ توقيتٍ بقيمةٍ دُمّيةٍ للاسمِ المجهولِ → `scrypt` → فكُّ السرِّ → `verifyTotp` → إنهاءُ نجاحٍ/فشلٍ ذرّيٌّ في القاعدةِ) و`enroll()` (يولِّدُ السرَّ ويُعيدُ رابطَ `otpauth` مرةً واحدةً في نصِّ الصفحةِ لا في بارامترِ تحويلٍ) و`disable()`. ثوابِتُ الإقفالِ: ٥ محاولاتٍ · 900 ثانيةٍ.
- المساراتُ: `GET /admin/login?break=1` (نموذجُ الباب) · `POST /admin/login/break-glass` (قبلَ الحارسِ — طلبُهُ لا يحملُ جلسةً) · `GET /admin/break-glass` (حالةُ الاعتماد) · `POST /admin/break-glass` (تسجيل/تدوير) · `POST /admin/break-glass/disable` (تعطيل). نصُّ الرفضِ موحَّدٌ: «اعتمادٌ غيرُ صحيحٍ أو مقفلٌ».
- واجهةُ `apps/admin-dashboard`: خطوةُ دخولٍ بديلةٌ في `login.ts` بوصلٍ «تعطَّلَ تلغرام؟» وصفحةُ `pages/break-glass.ts`.
- `admin_sessions.origin` يُبلَّغُ في الاستعلاماتِ الإداريّةِ (`adminBreakGlassCredential`).

### ٣-٤. التصنيفُ في السجلّاتِ الأربعةِ (قبلَ الدفعِ — `ح-7`)

- الاستبقاءُ (`retention-policy.ts`): `admin_break_glass_credentials` → `lifecycleBound` (المعجمُ 64).
- المحوُ (`erasure-policy.ts`): `D.erase` · موضوعُهُ `S.admin` · مرتبطٌ بـ`user_id` · قيدُ التنزيلِ على `F12-10` (تجهيلٌ لا إفلاتٌ).
- جردُ الحدودِ (`wasla-boundary-registry.ts`): وجهةُ `KEEP` لِمالكِ الهويّةِ (CORE — `ADR 0031`: ينتقلُ معَ `users` في `W-3`)؛ أُعيدَ توليدُ `docs/wasla/boundary-audit.md`.
- مصفوفةُ الهجرةِ (`wasla-migration-matrix.ts`): `noMigration` في الموجةِ ٠؛ أُعيدَ توليدُ `docs/migration/matrix.md`.
- سجلُّ أفعالِ التدقيقِ: ٤ دوالَّ جديدةٍ ⇒ `REQUIRED_AUDITED_FUNCTION_COUNT` 20→24 بتعليقٍ تاريخيٍّ (`ح-8`).
- سياسةُ تقليلِ المعدَّلِ: ٥ مساراتٍ ⇒ `ROUTE_POLICY_COUNT` 101→105 و`EXEMPT_ROUTE_COUNT` 3→4 (`POST /admin/login/break-glass` معفيًا — الإقفالُ في القاعدةِ لا في الذاكرةِ) بتعليقٍ تاريخيٍّ.
- البيئةُ: `ADMIN_BREAK_GLASS_TOTP_KEY` في `.env.example` و`render.yaml` (`sync: false`) و`tests/support/config.ts`.

## ٤. القياسُ

### ٤-١. وحدةً (`tests/unit/break-glass-crypto.test.ts` — 19 اختبارًا · 138 توقُّعًا)

- **متجهاتُ RFC 4226 §D**: سرُّ `12345678901234567890` — العدّاداتُ 0→755224 · 1→287082 · 2→359152 · 3→969429 · 5→254676 · 9→520489 (تصحيحٌ أثناءَ الكتابةِ: العدّادُ 9 في المعيارِ 520489 لا ما حُفِظَ خطأً منَ الجدولِ — التطبيقُ كانَ صحيحًا والاختبارُ هو الذي صُحِّحَ).
- scrypt: دوّارُ الهضمِ والملحُ الجديدُ في كلِّ مرةٍ والصيغةُ الذاتيةُ الشرحِ ورفضُ الهضمِ الممسوسِ.
- AES-GCM: الدوّارُ، ومسُّ بايتٍ واحدٍ يُسقِطُ الفكَّ، ومفتاحٌ آخرُ لا يفكُّ، والاشتقاقُ 32 بايتًا، ورفضُ الصيغةِ غيرِ المفهومةِ.
- TOTP: رفضُ الإعادةِ بعدَ الاستهلاكِ، نافذةُ ±1 تُعيدُ العدّادَ المُستهلَكَ، رفعُ الشكلِ غيرِ السداسيِّ، سرٌّ مُولَّدٌ 20 بايتًا.
- otpauth: شكلُ الرابطِ كاملًا؛ base32: دوّارٌ على 40 طولًا، ورفضُ `1/I`، وتسامحُ الحالةِ والحشوِ.

### ٤-٢. تكاملًا على PostgreSQL 18 حقيقيّةٍ (`tests/integration/admin-break-glass.test.ts` — 12 اختبارًا)

1. التسجيلُ من جلسةِ تيليجرامَ يُنشئ الاعتمادَ ويدقِّقُ الأثرَ (بلا أسرارٍ في الحمولةِ).
2. التدويرُ يستبدلُ السرَّينِ ويُصفِّرُ العدّادَ — والرمزُ الجديدُ يفتحُ.
3. الدخولُ يفتحُ جلسةً `origin='break_glass'` تعملُ أمامَ الحارسِ — وأثرٌ واحدٌ مسجَّلٌ.
4. رمزُ TOTP لا يُستعمَلُ مرّتَينِ — والرفضُ لا يُعدُّ فشلًا مُقفلًا.
5. خمسُ محاولاتٍ فاشلةٍ تُقفِلُ (`locked_until` في المستقبلِ) وخمسةُ آثارِ رفضٍ.
6. الاسمُ المجهولُ: الردُّ الموحَّدُ نفْسُهُ، وصفرُ آثارٍ منتحَلةٍ في `audit_log`، وصفرُ صفوفَ اعتمادٍ (الفجوةُ المُعلَنةُ `ح-5`: محاولاتُ الأسماءِ المجهولةِ لا تدخلُ سجلَّ التدقيقِ — حصنُها الوحيدُ سياسةُ تقليلِ المعدَّلِ على المسارِ العامِّ — ولا سجلَّ مُهيكَلًا في البوّابةِ تُصبُّ فيه).
7. غيرُ المسؤولِ: الحارسُ يردُّهُ و`open_admin_session` نفسُها ترفضُهُ — لا ثغرةَ بينَ الطبقتَينِ.
8. **جلسةُ البابِ لا تُنشئُ بابًا** — بوّابةُ `origin='telegram_code'` في القاعدةِ.
9. مُزالُ الدورِ لا يفتحُ ولا يُعَدُّ فشلَهُ (لا أثرَ).
10. اسمٌ محجوزٌ لمسؤولٍ آخرَ لا يُسرَقُ.
11. التعطيلُ: `is_active=false` والدخولُ يُرفَضُ والصفُّ لا يُحذَفُ وأثرٌ واحدٌ.
12. صفحةُ الدخولِ تعرضُ البابَ، والرسمُ **بلا مفتاحِ بيئةٍ** يرفضُ موحَّدًا لا 500.

النتيجةُ: **12/12 نجاحًا** على `TEST_DATABASE_URL` حقيقيّةٍ.

### ٤-٣. المسحُ الكاملُ للتكاملِ محليًّا (دفعاتٍ على قاعدةٍ واحدةٍ)

جُمِعَ ملفّاتِ التكاملِ الـ136 ودُفِعَتْ دفعاتٍ متتابعةً على القاعدةِ المحليّةِ (لا يُتَحَمَّلُ التشغيلُ الكاملُ في نداءٍ واحدٍ محليًّا). النتيجةُ: **كلُّ الدفعاتِ خضراءُ إلا ثلاثَ فشائلٍ كلُّها بيئيّةٌ لا تعودُ إلى هذا التغييرِ**: (١) `hot-query-index-plans` يتوقَّعُ انحدارَ الخُطَّةِ إلى `Seq Scan` عندَ إسقاطِ فهرسِ `audit_log_action_entity_idx` — والقاعدةُ المحليّةُ **PostgreSQL 18** يختارُ فيها المخطِّطُ فهرسَ `(entity_type, entity_id)` بمسحٍ فهرسيٍّ على `entity_id` وحدَه (سلوكُ الإصدارِ 18)، بينما **CI يُديرُ PostgreSQL 17** حيثُ تنحدرُ الخُطَّةُ كما يتوقَّعُ الاختبارُ — والملفُّ وسجلُّ الفهارسِ **غيرُ ممسوسَينِ في هذا الفرعِ** (قِيسَ الفرقُ ببناءِ الجدولِ نفسِهِ في معاملةٍ مرتدَّةٍ). (٢) و(٣) مهلةُ `5000ms` في `safe-migration-runner` وحالةٌ في `ride-request` — كلتاهما تمرُّ عندَ إعادةِ التشغيلِ المعزولِ ولا علاقةَ لهما بمسارِ البابِ. ودفعتُ الملفِّ الجديدِ (12 حالةً) داخلَ دفعتِهِ خضراءُ في المسحِ.

### ٤-٣. الحواجزُ (كلُّها بعدَ التصنيفِ)

`migrations` (61 جدولًا · 199 هجرةً) · `migration-safety` · `audit-actions` (24) · `retention` (64) · `erasure` (64) · `boundary` (64: KEEP=30 · REFACTOR=12 · MOVE_TO_CORE=22) · `matrix` (64 · موجةٌ 0=30) · `env-drift` · `rate-limit-coverage` (105 · معفيًا 4) · `system-state` · `docs-budget` · `security-controls` · `object-authorization` · `row-security-condition` · `city-precondition` · `skip-classification` — نجاح. typecheck وbiome نظيفان.

## ٥. ما لا يُدَّعى (`ح-5`)

- لا نشرَ حيًّا ولا مسؤولًا حقيقيًّا استعملَ البابَ — المُتحقَّقُ منه السلوكُ على قاعدةٍ حقيقيّةٍ في CI.
- محاولاتُ الأسماءِ المجهولةِ **لا تُدوَّنُ في `audit_log`** (لا فاعلَ حقيقيًّا يُنسبُ إليه الأثرُ بلا انتحالٍ) — تصنيفُها في سياسةِ تقليلِ المعدَّلِ وسجلِّ البوّابةِ المُهيكَلِ، وهذه فجوةٌ مُعلَنةٌ لا فجوةٌ صامتةٌ.
- `WebAuthn` أقوى منَ الكلمةِ والسرِّ — خيارٌ مستقبليٌّ لا يُدَّعى أنَّهُ بديلٌ مُنجَزٌ.
- حكمُ CI بعدَ الدفعِ يُقرأُ بالوظيفةِ — الأخضرُ المحليُّ ليسَ بديلًا.

## ٦. ما يتبقّى

لا `[x]` قبلَ ثلاثِ جولاتٍ CI خضراءَ على `main` (`ح-4`). عمودُ الحالةِ في §25 يبقى `[ ]` (قيدَ التنفيذِ) حتّى اكتمالِها.
