=== دليل البوابة C: البناء والأنواع والتحليل الساكن والاختبارات ===
البيئة: bun 1.3.14 / PostgreSQL 18.4 / PostGIS 3.6 / الفرع feat/final-launch-hardening

----- قبل الإصلاح (خطّ الأساس على 5ac3445) -----
$ bun run lint  -> exit 1 : 8 errors + 1 warning + 9 infos
  المواضع: tests/unit/tracking.test.ts:45:48 (noExplicitAny), :204:12 (noNonNullAssertion),
           tests/unit/tracking-route.test.ts:39:48 (noExplicitAny),
           tests/unit/payment.test.ts:87:14, tests/unit/payment-webhook-http.test.ts:56:12,
           tests/unit/city-change.test.ts:84:12 و:91:12,
           tests/integration/full-live-path.test.ts:76:12 (noNonNullAssertion), :126:41 (noConfusingVoidType)
$ bun run typecheck -> exit 0
$ bun test (بلا قاعدة) -> 1168 pass / 304 skip / 2 fail  — الفشلان في tests/integration/dispatch-redispatch.test.ts:
  ECONNREFUSED 127.0.0.1:5432 وcontainer.close is not a function — السبب الجذري: هذا الملف وحده
  من 29 ملف تكامل لا يحرس غياب TEST_DATABASE_URL، وخطّافاته علوية فتعمل حتى مع التخطّي.
$ bun run ci -> RED

----- الإصلاح (سبب لا أثر) -----
1) noExplicitAny: استُبدل (as any) بنوع مشتقّ من العقد نفسه:
   type StoredLocation = Awaited<ReturnType<LocationStore["getCurrent"]>>  — لا كتم نوعٍ بل ربطه بالمنفذ.
2) noNonNullAssertion: أُزيلت التوكيدات بحرسٍ حقيقي (const current = ...; if (current === undefined) ...)
   أو بسلسلة اختيارية في التوكيدات (expect(x?.key)) — الاختبار يفشل إذا غاب العنصر، لا يُخفى.
3) noConfusingVoidType: ok(undefined as void) -> ok(undefined) — التوقيع Result<void, E> يستنتجه.
4) useTemplate: دمج التسلسل النصّي في قالبٍ واحد في packages/agent-core/agents/supportTicketAgent.ts.
5) الفشلان: أُضيف إلى dispatch-redispatch.test.ts نفس حرس بقية اختبارات التكامل
   (describeIf + describe.skip) وحرسٌ صريح في beforeAll/afterAll لأنّ الخطّافات العلوية تعمل مع describe.skip.
   لم يُحذف اختبار ولا توكيد ولا يُسكَت استثناء: مع قاعدةٍ حقيقيةٍ تعمل الاختبارات كلها فعلاً (انظر أدناه).
6) package.json: "ci" صار lint && typecheck && test لمطابقة MASTER_DIRECTIVE §0.7 وترتيب خطوات .github/workflows/ci.yml
   (كان يُسقط lint فيمرّ محلياً ويسقط بعيداً).

----- بعد الإصلاح: مخرجات حرفية -----
$ bun run lint

Checked 635 files in 831ms. No fixes applied.
Found 8 infos.
exit=0

$ bun run typecheck
$ tsc --noEmit
exit=0

ملاحظة على 8 infos المتبقية: كلها useLiteralKeys في packages/agent-core على سجلّات ذات فهرس
نصّي (source["AGENT_CORE_..."]، meta["id"]، attributes["order_id"]). تحويلها لنقطة يخالف
قراءة الفهرس الآمنة تحت noUncheckedIndexedAccess في tsconfig، وbiome يصنّفها info فلا تُفشل lint (exit=0).
تُترك مقصودةً وموثّقةً بدل مخالفة الأنواع.
