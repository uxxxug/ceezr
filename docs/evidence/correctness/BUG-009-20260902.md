# دليلُ `BUG-009` — ترتيبُ أحداثِ التتبُّعِ رقمٌ من الخادمِ لكلِّ جلسةٍ

**البند:** `BUG-009` — `docs/ROADMAP-MASTER.md` §11-أ (السطر 625)
**التاريخ:** 2026-09-02
**السند:** [`ADR 0053`](../../adr/0053-tracking-order-is-a-server-sequence-per-session.md) — وينبني على [`ADR 0015`](../../adr/0015-live-tracking-is-one-authoritative-path.md) و[`ADR 0042`](../../adr/) و[`ADR 0050`](../../adr/) و[`ADR 0052`](../../adr/0052-tracking-service-is-deleted-not-merged.md)
**الدرجة:** **مُنفَّذ · مُختبَر · مُتحقَّق منه** (سلّم `ROADMAP` §1.4)
**ما لا يُدَّعى:** لا **مَقيس** ولا **مُثبَت عند N** — لا حِملَ قِيسَ ولا تزامنَ حقيقيَّ سعةٍ ولا زمنَ استجابةٍ (`ح-5`). ولا يُقرأ هذا الدليلُ إثباتاً لصلاحيةِ realtime للإنتاجِ ولا لإغلاقِ `BUG-001` ولا `R-17`.

---

## ١) المشكلةُ الأصليّةُ بنصِّ البندِ

نصُّ البندِ بحرفِه (لم يُمَسّ — `ح-1`):

> **`BUG-009`** — لا رقابة تسلسل زمني على مسار الموقع في الطبقة العليا · `tracking` ·
> **الأثر:** خريطة تتراجع للخلف · **الإصلاح:** ختم زمني تسلسلي (monotonic sequence) من
> الخادم + رفض الأقدم صراحةً · **الاختبار:** 5 نبضات مختلَطة الترتيب.

والعيبُ المقيسُ قبلَ الإصلاحِ لم يكن غيابَ حكمٍ على القِدَمِ — الحكمُ كان قائماً في
الدومين (`recordFix` يردُّ `FIX_BEFORE_SESSION_START`) وفي سياسةِ الـGPS
(`rejectOlderThanSeconds: 300`) — بل كان **أنَّ الحكمَ لا يصل إلى المستهلِكِ**:

| الموضع | ما كان يحدث |
|---|---|
| `TrackingEvent` | لا يحمل رقمَ ترتيبٍ ولا معرِّفَ جلسةٍ — فالمستهلِكُ لا يملك ما يوازن به حدثَين |
| `live-tracking.onFix` | ينشر `location_updated` بعدَ الكتابةِ، والكتابةُ قد تكون رُفِضت |
| المستهلِكُ (`customer-live-relay`) | يطبّق كلَّ حدثٍ يصله بترتيبِ وصولِه — لا بترتيبِ حدوثِه |
| اللقطةُ الرسميّةُ | لا تحمل مرساةً تُحاذي بها الواجهةُ `lastAppliedSeq` |

وسببُ التراجعِ **بنيويٌّ لا احتماليٌّ**: الطابعُ الذي يصل مع النبضةِ هو
`message.date * 1000` (`apps/gateway/src/bots/shared/telegram-mapper.ts:90`) — **ساعةُ
الجهازِ بدقّةِ الثانيةِ**، تمرُّ عبرَ شبكةٍ وطابورِ ويبهوكٍ. فترتيبُ الوصولِ ليس
ترتيبَ الحدوثِ، ولا الطابعُ صالحاً حكماً على الترتيبِ (وقد صُنِّف
`TIMESTAMP_NOT_MONOTONIC` تحذيراً لا رفضاً عن قصدٍ). فخريطةٌ تُطبِّق بترتيبِ الوصولِ
**تتراجع بلا خطأٍ في أيِّ مكوِّنٍ**.

---

## ٢) كيف طُبِّق `ADR 0053` — العقدُ الأربعةَ عشرَ سطراً وموضعُ كلِّ سطرٍ

| # | ما ألزمه `ADR 0053` §٣-أ | الموضعُ المُنفِّذُ |
|---|---|---|
| ١ | رقمُ الترتيبِ من الخادمِ | `packages/infrastructure/tracking/session-repository.ts` — من `update … returning` |
| ٢ | لكلِّ جلسةِ تتبُّعٍ | العمودُ صفٌّ في `tracking_sessions`، لا عدّادَ عالميّاً |
| ٣ | محفوظٌ في `tracking_sessions` | `last_sequence integer not null default 1` |
| ٤ | يُثبَّت ذرّيّاً مع تقدُّمِ الجلسةِ | جملةٌ واحدةٌ: `set last_fix_at = $at, last_sequence = t.last_sequence + 1 … returning` |
| ٥ | `session_started`/`session_ended`/`location_updated` بعدّادٍ واحدٍ | `live-tracking.ts` — الثلاثةُ تقرأ `record.sequence` من المستودعِ نفسِه |
| ٦ | القديمُ/المختلطُ يُرفَض صراحةً | `SessionAdvanceOutcome = accepted \| stale \| no_session` — والرفضُ قيمةٌ مُسمّاةٌ لا `null` |
| ٧ | لا نشرَ إن رُفِضت الكتابةُ | `onFix` يعود قبلَ النشرِ عندَ غيرِ `accepted` |
| ٨ | كلُّ حدثٍ يحمل `sessionId + sequence` | `TrackingEvent` في `packages/tracking/types.ts` — الحقلانِ **إلزاميّانِ** لا اختياريّانِ |
| ٩ | اللقطةُ تحمل آخرَ رقمٍ يمثِّل الحالةَ | `apps/gateway/src/admin/queries.ts` → `sessionSequence` |
| ١٠ | المستهلِكُ يكتفي بـ`lastAppliedSeq` في الذاكرةِ | `customer-live-relay.ts` — رقمٌ واحدٌ لكلِّ بثٍّ، لا مجموعةَ معرِّفاتٍ |
| ١١ | المكرَّرُ غيرُ ضارٍّ | `sequence <= lastAppliedSeq` ⇒ إسقاطٌ صامتٌ مع عدّادِ قياسٍ |
| ١٢ | الفجوةُ ⇒ لقطةٌ رسميّةٌ عبرَ HTTP | لا إعادةَ بثٍّ؛ الفجوةُ تُطبَّق والمحاذاةُ من اللقطةِ |
| ١٣ | لا مخزنَ أحداثٍ ولا Redis ولا ترتيبَ عالميٍّ | لم يُضَف شيءٌ من ذلك — ويحرسه اختبارٌ بنيويٌّ |
| ١٤ | لا عدّادَ في الذاكرةِ | يحرسه اختبارٌ بنيويٌّ يقرأ نصَّ `live-tracking.ts` |

**ولم يُنشَأ `eventId`**: `ADR 0053` لا يطلبه، ولا أثبتَ المُصرِّفُ حاجةً قائمةً إليه —
و`(sessionId, sequence)` كافٍ للتمييزِ لأنَّ الرقمَ لا يُعادُ داخلَ الجلسةِ.

---

## ٣) المخطَّطُ الجديدُ

الهجرةُ الواحدةُ: `supabase/migrations/20260902000000_tracking_session_sequence.sql`

```sql
alter table tracking_sessions
  add column if not exists last_sequence integer not null default 1;

alter table tracking_sessions
  add constraint tracking_sessions_sequence_positive check (last_sequence >= 1);
```

| القرار | السببُ |
|---|---|
| عمودٌ في صفِّ الجلسةِ لا `create sequence` | كائنُ `sequence` في PostgreSQL **عالميٌّ وغيرُ معامليٍّ**: يتقدَّم وإن انسحبَت المعاملةُ، فيُنتِج فجواتٍ ولا يُقيَّد بجلسةٍ. والعقدُ يطلب عدّاداً **لكلِّ جلسةٍ يتقدَّم ذرّيّاً مع الحالةِ** — وذاك بعينُه صفٌّ واحدٌ في جملةٍ واحدةٍ |
| `integer` لا `bigint` | `postgres.js` يُرجِع `int8` **نصّاً** فتصير `"10" < "9"` صحيحةً ويكسر كلَّ موازنةٍ صامتاً. و`integer` يكفي: ٢١٤٧م نبضةٍ للجلسةِ الواحدةِ فوقَ أيِّ سقفٍ واقعيٍّ (`maxSessionSeconds = 43200`) |
| `not null default 1` | الصفوفُ القائمةُ تُملأ بـ١، فلا صفَّ بلا مرساةٍ ولا فرقَ في المعالجةِ بين قديمٍ وجديدٍ. و`nullable` كان سيُدخِل فرعاً ثانياً في كلِّ موازنةٍ بلا مقابلٍ |
| البدءُ من ١ | `ADR 0053` §٣-أ سطرُ ٣: `session_started` يحمل ١، فأوّلُ إصلاحةٍ مقبولةٍ تحمل ٢. و٠ كان سيلتبس بـ«لا شيءَ بعدُ» في مستهلِكٍ يبدأ بصفرٍ |
| قيدُ `>= 1` | حرزٌ على التراجعِ: أيُّ كتابةٍ تُنزِله دونَ ١ تُسقَط في المحرِّكِ لا في الشيفرةِ |

**والرجوعُ مكتوبٌ في رأسِ الهجرةِ نصًّا** ومُجرَّبٌ:

```sql
alter table tracking_sessions drop constraint if exists tracking_sessions_sequence_positive;
alter table tracking_sessions drop column if exists last_sequence;
```

---

## ٤) موضعُ توليدِ الرقمِ — واحدٌ لا غيرُه

`packages/infrastructure/tracking/session-repository.ts` · دالّةُ `advance`:

الرقمُ يُولَد **داخلَ جملةِ التحديثِ نفسِها** التي تُثبِّت `last_fix_at`، ويعود منها
بـ`returning`. فليس في المسارِ كلِّه موضعٌ ثانٍ يُولِّد رقماً، ولا `read-then-write`،
ولا `Math.max` في JS، ولا عدّادٌ في الذاكرةِ، ولا `Date.now()`.

**والذرّيّةُ ليست دعوى تصميمٍ بل خاصّةُ الجملةِ**: القراءةُ (`t.last_sequence`) والزيادةُ
والكتابةُ والحكمُ على القِدَمِ (`where last_fix_at < $at`) كلُّها في جملةٍ واحدةٍ، فقُفلُ
الصفِّ في PostgreSQL يُسلسِل كاتبَين متزامنَين حتماً — ولا يستطيع اثنانِ أخذَ الرقمِ
نفسِه ولا أن يُثبَّت `last_fix_at` بلا رقمٍ يقابله.

**و`recordedAtMs` بقيَ metadata زمنيّاً فقط** ولم يُستعمل رقمَ ترتيبٍ في أيِّ موضعٍ.

---

## ٥) السلوكُ عندَ القديمِ والمكرَّرِ والفجوةِ وإعادةِ الوصلِ

| الحالةُ | الكاتبُ (`advance`) | الناشرُ (`onFix`) | المستهلِكُ |
|---|---|---|---|
| إصلاحةٌ أحدثُ | `accepted` + رقمٌ جديدٌ | ينشر `location_updated` | `== lastAppliedSeq + 1` ⇒ يُطبَّق |
| إصلاحةٌ أقدمُ | `stale` — **لا الحالةُ تتغيَّر ولا الرقمُ يُستهلَك** | **لا ينشر** | لا يبلغه شيءٌ |
| طابعٌ مساوٍ | `accepted` — لأنَّ الشرطَ `<` صارمٌ | ينشر | يُطبَّق |
| لا جلسةَ مفتوحةً | `no_session` | لا ينشر | — |
| حدثٌ مكرَّرٌ | — | — | `<= lastAppliedSeq` ⇒ يُسقَط، **والعرضُ لا يتراجع** |
| فجوةٌ | — | — | يُطبَّق الأحدثُ ويُرفَع `lastAppliedSeq`؛ **ولا إعادةَ بثٍّ** — والمحاذاةُ التامّةُ من اللقطةِ الرسميّةِ |
| إعادةُ وصلٍ | — | — | يقرأ اللقطةَ عبرَ HTTP فيأخذ منها الحالةَ **ومرساةَ الرقمِ** معاً |
| إعادةُ تشغيلِ الخادمِ | يُكمِل من الصفِّ في القاعدةِ | — | — |

**والفجوةُ لا تُسقِط التحديثَ**: إسقاطُ الأحدثِ انتظاراً لما ضاعَ يجعل العيبَ أسوأ.
والمُطبَّقُ أحدثُ ما وصلَ، واللقطةُ هي التي تُصحِّح — وهذا نصُّ `ADR 0053` §٣-ب.

---

## ٦) الملفّاتُ

| الوحدةُ | الملفُّ | ما جرى |
|---|---|---|
| ١ | `supabase/migrations/20260902000000_tracking_session_sequence.sql` | **جديدٌ** — العمودُ والقيدُ والرجوعُ |
| ١ | `packages/tracking/session-store.ts` | `TrackingSessionRecord` و`SessionAdvanceOutcome` — والرقمُ موجودٌ **في `accepted` وحدَها** |
| ١ | `packages/infrastructure/tracking/session-repository.ts` | `advance` جملةٌ واحدةٌ بـ`returning` |
| ٢ | `packages/tracking/types.ts` | `TrackingEvent` صار يحمل `sessionId` و`sequence` **إلزاميَّين** |
| ٢ | `apps/gateway/src/tracking/live-tracking.ts` | النشرُ تابعٌ لقرارِ الكاتبِ لا سابقٌ له |
| ٣ | `apps/gateway/src/admin/queries.ts` | اللقطةُ تحمل `sessionId` و`sessionSequence` |
| ٣ | `apps/gateway/src/routes/admin-live.ts` | الدفقُ يحمل الحقلَين |
| ٣ | `apps/gateway/src/tracking/customer-live-relay.ts` | بوّابةُ `lastAppliedSeq` |
| ٣ | `apps/gateway/src/routes/public-tracking.ts` | **لا تغييرَ سلوكيَّ** — وترويسةٌ تشرح السببَ (§٨) |
| ٤ | `tests/integration/tracking-sequence.test.ts` · `tests/unit/tracking-sequence.test.ts` | الدليلُ |
| ٤ | `scripts/lib/skip-registry.ts` · `tests/unit/skip-audit.test.ts` | تصنيفُ التجاوزِ المشروطِ ورفعُ العددِ المُثبَّتِ |

**ولم يُنشَأ endpoint جديدٌ، ولا مُلِف بملفٍّ لا يلزم `BUG-009`.**

---

## ٧) الاختباراتُ — التسعةَ عشرَ حالةً وأينَ تعمل

### أ) على PostgreSQL حقيقيٍّ — `tests/integration/tracking-sequence.test.ts` (٨)

| # | الحالةُ | ما تُثبِته |
|---|---|---|
| ١ | **خمسُ نبضاتٍ مختلطةِ الترتيبِ** | الدليلُ الحاسمُ (§٨) |
| ٢ | الاستئنافُ بعدَ إعادةِ التشغيلِ | `createSql` ومستودعٌ جديدانِ لا يُشاركانِ الأوّلَ ذاكرةً ⇒ الرقمُ من الصفِّ |
| ٣ | الأقدمُ يُرفَض | `stale` · الرقمُ لا يُستهلَك · `last_fix_at` لا يتغيَّر |
| ٤ | الطابعُ المساوي | يُقبَل ويأخذ رقمَه — وهو واقعةٌ لا فرضٌ لأنَّ `message.date` بدقّةِ الثانيةِ |
| ٥ | لا عدّادَ عالميٌّ | جلستا سائقَينِ ترقّمانِ استقلالاً (٤ و٢) |
| ٦ | عدّادٌ واحدٌ للدورةِ | `session_started` = ١ · `session_ended` = آخرُ مقبولةٍ + ١ |
| ٧ | اللقطةُ مرساةٌ | `sessionSequence` = `last_sequence` في الصفِّ · ولا تابعَ إعادةِ بثٍّ في الناقلِ |
| ٨ | ساعةٌ منحرفةٌ إلى الأمامِ | القناةُ لا تتراجع والأرقامُ تبقى متزايدةً صارماً (§٩ من العقدِ) |

### ب) بلا قاعدةٍ — `tests/unit/tracking-sequence.test.ts` (١١)

قرارُ النشرِ (`stale` ⇒ لا نشرَ · `no_session` ⇒ لا نشرَ · المردودُ لا يستهلك رقماً)
· المستهلِكُ (مكرَّرٌ غيرُ ضارٍّ · أقدمُ لا يُرجِع الدبّوسَ · فجوةٌ تُطبَّق بلا إعادةِ
بثٍّ · جلسةٌ أخرى لا تُقاس بعدّادِ هذه) · وثلاثةُ **امتناعاتٍ بنيويّةٍ** تُقرأ من نصِّ
المصدرِ: لا عدّادَ مُصعَّدٌ في التطبيقِ · لا رقمَ مقترناً بـ`recordedAtMs` أو
`Date.now()` أو `message.date` · لا `create sequence` في الهجرةِ ولا `Math.max` في
المستودعِ.

**والامتناعاتُ تُقرأ من النصِّ عن قصدٍ**: عدّادٌ في الذاكرةِ **ينجح** في كلِّ اختبارِ
سلوكٍ داخلَ عمليةٍ واحدةٍ ولا يُخفق إلَّا في الإنتاجِ بنسختَين أو بإعادةِ تشغيلٍ —
فاختبارُ السلوكِ لا يحرسه، ونصُّ المصدرِ يحرسه.

### ج) خريطةُ الحالاتِ الخمسَ عشرةَ التي طلبها التوجيهُ

| # | مطلوبٌ | المُثبِتُ |
|---|---|---|
| ١ | خمسُ نبضاتٍ مختلطةٌ | تكامل ١ |
| ٢ | الأقدمُ يُرفَض صراحةً | تكامل ٣ · وحدة (`stale`) |
| ٣ | لا نشرَ عندَ الرفضِ | وحدة (قرارُ النشرِ) · تكامل ١ (٣ أحداثٍ لا ٥) |
| ٤ | العرضُ لا يتراجع | وحدة (المستهلِكُ) · تكامل ١ (`last_fix_at` = الأحدثُ) |
| ٥ | الأرقامُ متزايدةٌ صارماً | تكامل ١ |
| ٦ | تساوي الطابعِ لا يرفض | تكامل ٤ |
| ٧ | المكرَّرُ غيرُ ضارٍّ | وحدة (المستهلِكُ) |
| ٨ | الفجوةُ ⇒ لقطةٌ لا إعادةُ بثٍّ | وحدة (الفجوةُ) · تكامل ٧ |
| ٩ | الرقمُ صحيحٌ بعدَ إعادةِ التشغيلِ | تكامل ٢ |
| ١٠ | عدّادٌ واحدٌ للدورةِ | تكامل ٦ |
| ١١ | لا عدّادَ عالميٌّ | تكامل ٥ · وحدة (بنيويٌّ) |
| ١٢ | لا عدّادَ في الذاكرةِ | وحدة (بنيويٌّ) · تكامل ٢ |
| ١٣ | الرقمُ ليس من `recordedAtMs` | وحدة (بنيويٌّ) |
| ١٤ | الرفضُ لا يغيِّر الرقمَ | تكامل ٣ |
| ١٥ | التحقُّقُ على PostgreSQL حقيقيٍّ | الحزمةُ (أ) كلُّها |

**والحالاتُ التسعُ التي شرطَها `ADR 0053` §٨ مُستوفاةٌ كلُّها**: ١←تكامل ١ · ٢←تكامل ٣ ·
٣←تكامل ١ ووحدة · ٤←تكامل ١ · ٥←وحدة · ٦←وحدة وتكامل ٧ · ٧←تكامل ٢ · ٨←تكامل ٤ ووحدة
بنيويّةٌ · ٩←تكامل ٨. والحالتانِ ١ و٧ على PostgreSQL حقيقيٍّ كما شُرِطَ نصًّا.

**وحدٌّ مُعلَنٌ في الحالةِ ٩ لا يُخفى**: إصلاحةٌ بساعةٍ منحرفةٍ إلى الأمامِ **دونَ**
`maxFutureSkewSeconds` تُقبَل وتُقدِّم `last_fix_at`، فنبضةٌ صادقةٌ بعدَها تُرَدُّ
`stale`. وذاك «رفضُ الأقدمِ صراحةً» بحسبِ الطابعِ المتاحِ — والخادمُ لا يملك ساعةً
أصدقَ لهذه النبضةِ. والمحروسُ المُقاسُ أنَّ القناةَ لا تتراجع، لا أنَّ الانحرافَ
يُصحَّح.

---

## ٨) الدليلُ الحاسمُ — الخمسُ نبضاتٍ

**ليس اختبارَ دالّةٍ**. النبضاتُ تدخل من `container.handler.handle("driver", …)` وهو
**نفسُ ما يُنادى من موجِّهِ الويبهوكِ**، فتمرُّ بالمُخطِّطِ والدومينِ والمستودعِ
والناقلِ كما تمرُّ في الإنتاجِ.

- طوابعُ الحدوثِ: أ < ب < ج < د < هـ (٥٠ · ٤٠ · ٣٠ · ٢٠ · ١٠ ثانيةً مضت)
- ترتيبُ **الوصولِ**: أ · ج · ب · هـ · د

المقيسُ:

| المقيسُ | النتيجةُ |
|---|---|
| `last_fix_at` في الصفِّ | **هـ** — لا **د** آخرَ الواصلاتِ |
| أحداثُ `location_updated` | **٣** بالضبطِ (أ · ج · هـ) لا ٥ |
| أرقامُها | متزايدةٌ **صارماً**، وكلُّها من `sessionId` واحدٍ |
| آخرُ رقمٍ منشورٍ | **يساوي** `last_sequence` المقروءَ من الصفِّ |
| `session_started.sequence` | **١** |

**ولا رقمَ مصطنعٌ في أيِّ توقُّعٍ**: كلُّ رقمٍ يُوازَن إمّا مقروءٌ من القاعدةِ أو
محمولٌ في حدثٍ أصدرَه المستودعُ. فلو كان المولِّدُ مزيَّفاً لسقطَ الاختبارُ.

---

## ٩) القياسُ المحلِّيُّ على PostgreSQL حقيقيٍّ

قاعدةُ PostgreSQL **18.6** في بيئةِ التنفيذِ (CI يستعمل `postgis/postgis:17-3.5`):

| ما جرى | النتيجةُ |
|---|---|
| هجرةٌ من الصفرِ: ٥٨ ملفّاً بالترتيبِ | **نجحت كلُّها** |
| هجرةٌ على قاعدةٍ قائمةٍ فيها صفُّ جلسةٍ سابقٌ | العمودُ أُضيفَ و**الصفُّ القديمُ صار `last_sequence = 1`** وبقيَ مفتوحاً |
| سطرُ الرجوعِ كما هو مكتوبٌ | نفَّذَ وأزالَ القيدَ والعمودَ |
| `tests/integration/tracking-sequence.test.ts` | **٨ نجاحاً · ٠ فشلاً** |
| `bun run test:integration` كاملاً | **٤٥٤ نجاحاً · ٠ فشلاً · ١١ تخطّياً** (التخطّي دَينُ `BENCH_DATABASE_URL` القائمُ) |
| `tests/unit/tracking-sequence.test.ts` | **١١ نجاحاً · ٠ فشلاً** |
| `bun test tests/unit/` | **١٨١٨ نجاحاً · ٠ فشلاً** |
| `bun scripts/rollback-schema-drill.ts` | ✅ ٥٨ هجرةً على قاعدةٍ حقيقيّةٍ · كلُّ تضييقٍ مُقاسٍ له مدخلٌ |
| `bun run check:coverage` | ✅ «التتبّعُ وموقعُ السائق: **48.04%** (515/1072)» فوقَ أرضيّةِ 47% |
| `bunx tsc --noEmit` · `biome` · حواجزُ العقدِ والقياسِ والأسرارِ | نظيفةٌ |

**وأرضيّةُ التغطيةِ لم تُخفَّض**: كانت الوحدتانِ ١ و٢ تُسقِطانِ بوّابةَ التغطيةِ
(44.91% دونَ 47%) لأنَّ الشيفرةَ سبقت دليلَها، **وأُصلِح السببُ بالدليلِ لا بتخفيفِ
الأرضيّةِ** — وهو نصُّ `scripts/lib/coverage-registry.ts`: «يُصلَح السببُ ولا تُخفَّض
الأرضيّةُ».

---

## ١٠) الأمنُ

لا يُسجَّل حدثٌ خامٌّ ولا رمزُ جلسةٍ ولا `initData`. المضافُ إلى القياسِ رقمانِ
وحدَهما: `sessionId` (UUID داخليٌّ) و`sequence` (عدَدٌ صحيحٌ) — ولا واحدَ منهما يُشتَقُّ
منه سرٌّ ولا هويّةُ تيليجرام. وحاجزُ `check-secret-logging.ts` يمرّ.

---

## ١١) ما لا يُغلِقه هذا الدليلُ — مُعلَناً لا مُخفىً

| البندُ | حالُه |
|---|---|
| `BUG-001` | **`[ ]` مفتوحٌ ولم يُمَسّ**. `packages/infrastructure/identity/directories.ts:162-175` ما زال يكتب `drivers.last_location` بلا CAS. وقد فصلَ `ADR 0053` §٦ الطبقتَين نصًّا، فلا تعارضَ يوجب التوقُّفَ (§١٤ من التوجيهِ) |
| مسارُ الراكبِ العامُّ | `GET /api/track/:token/position` يقرأ `drivers.last_location` عبرَ `get_tracking_position`، **لا `tracking_sessions`**. فلم يُضَف إليه رقمٌ: اقترانُ رقمٍ صاعدٍ بحقلٍ غيرِ محروسٍ يُوهِم ترتيباً لا يملكه. وهو استطلاعٌ كلَّ ٥ ثوانٍ بلا اشتراكٍ، فلا `lastAppliedSeq` يُحاذى فيه أصلاً |
| `R-17` | مفتوحٌ — نسخةٌ واحدةٌ. والرقمُ **لكلِّ جلسةٍ في القاعدةِ** فلا يمنع نسختَين، لكنَّ منعَهما ليس ههنا |
| realtime في الإنتاجِ | لم يبدأ. `DEC-06` معتمدٌ و`Socket.IO` هو الناقلُ المعتمدُ، ولا Adapter ولا توزيعَ Redis ولا `F4` |
| عيبٌ قائمٌ مُوثَّقٌ | `currentSession` يُنادي `sessions.close(driverId, "EXPIRED", nowMs)` **بلا نشرِ `session_ended`** — فيُستهلَك رقمٌ بلا حدثٍ يقابله. لا يكسر عدمَ التراجعِ (الأرقامُ تبقى متزايدةً)، ويظهر فجوةً واحدةً عندَ المستهلِكِ فتُصحَّح باللقطةِ. **وليس من نطاقِ `BUG-009` فلم يُصلَح ههنا** |
| `packages/application/tracking/tracking-session.ts` | **لا وجودَ له في المستودعِ**. آلةُ حالةِ الجلسةِ هي `packages/domain/tracking/session.ts`، ولم يُنشَأ ملفٌّ باسمِ التوجيهِ لأنَّه لا يقابله شيءٌ |
