# دليلُ `BUG-001` — كتابةٌ شرطيّةٌ على `drivers.last_location` في القاعدةِ لا في `JS`

**البند:** `BUG-001` — `docs/ROADMAP-MASTER.md` §11-أ (السطر 617)
**التاريخ:** 2026-09-02
**السند:** [`ADR 0015`](../../adr/0015-live-tracking-is-one-authoritative-path.md) — والمُسنَدُ مأخوذٌ حرفاً من [`ADR 0053`](../../adr/0053-tracking-order-is-a-server-sequence-per-session.md) §٦
**الدرجة:** **مُنفَّذ · مُختبَر · مُتحقَّق منه** (سلّم `ROADMAP` §1.4)
**ما لا يُدَّعى:** لا **مَقيس** ولا **مُثبَت عند N** — لا سعةَ قِيست ولا أداءَ ولا تحقُّقَ جهازٍ ولا صحّةَ نسختَين ولا جاهزيّةَ realtime (`ح-5`). ولا يُقرأ هذا الدليلُ إغلاقاً لـ`R-17` ولا لـ`F4` ولا استئنافاً لأيِّ بندٍ آخرَ.

---

## ١) المشكلةُ بنصِّ البندِ

نصُّ البندِ بحرفِه (لم يُمَسّ — `ح-1`):

> **`BUG-001`** — تحديث `drivers.last_location` بلا حراسة ولا مقارنة زمنية ·
> `packages/infrastructure/identity/directories.ts:162-175` · **الأثر:** إصلاحة قديمة
> تصل متأخرة تُرجِع الموضع للخلف · **الإصلاح:** تحديث شرطي CAS في SQL:
> `WHERE ... AND (last_location_at IS NULL OR last_location_at < $ts)` مع عمود تسلسل
> تصاعدي. **الرقابة في القاعدة لا في JS** · **الاختبار:** اختبار سباق: تسليم مقلوب
> لثلاث نبضات.

والعيبُ المقيسُ قبلَ الإصلاحِ: الجملةُ كانت `update drivers set last_location = … where id = $1` — بلا شرطٍ. فمن وصلَ أخيراً كتبَ، وإن كانت إصلاحتُه أقدمَ. ورسائلُ الموقعِ تصلُ عبرَ ويبهوكٍ وطابورٍ فترتيبُ الوصولِ ليس ترتيبَ الحدوثِ، ونبضتانِ متزاحمتانِ تُرجِعانِ الموضعَ القانونيَّ إلى الوراءِ: تقيس المطابقةُ مسافةً من نقطةٍ غادرَها السائقُ، ويرى الراكبُ السيارةَ ترتدُّ على الخريطةِ (`get_tracking_position` يقرأ هذا الحقلَ نفسَه).

---

## ٢) كلُّ كاتبٍ وكلُّ قارئٍ لهذا الحقلِ — قبلَ التنفيذِ وبعدَه

### الكُتّابُ

| الموضعُ | النوعُ |
|---|---|
| `packages/infrastructure/identity/directories.ts` → `DriverDirectory.updateLocation` | **الكاتبُ الإنتاجيُّ الوحيدُ** — وهو الموضعُ الذي نصَّ عليه البندُ |
| `tests/integration/live-sequence-validation.test.ts:211` · `tests/integration/admin-dashboard.test.ts:895` · `tests/integration/canonical-driver-location.test.ts:231,242` · `tests/integration/driver-location-freshness.test.ts:248,375` | تهيئةُ اختباراتٍ (`set last_location`) — لا مسارَ إنتاجٍ |

والفحصُ أُعيدَ **بعدَ** التنفيذِ (§١١ من التوجيهِ): لا كاتبَ إنتاجيَّ ثانياً. والنداءُ الوحيدُ لـ`updateLocation` في `packages/application/bots/driver-dialog.ts` (مسارُ رسالةِ الموقعِ)، ومزدوجُه في `tests/support/bot-doubles.ts`.

### القُرّاءُ

| الموضعُ | ما يقرؤه |
|---|---|
| `packages/infrastructure/identity/directories.ts` — `DRIVER_SELECT` → `toLastFix` | `last_location` + `last_location_recorded_at` ⇒ الإصلاحةُ السابقةُ لمُقيِّمِ التتابعِ |
| `packages/infrastructure/dispatch/dispatch-adapters.ts` | الموضعُ + `last_location_at` ⇒ المطابقةُ والمسافةُ |
| `packages/infrastructure/transport/order-adapters.ts` · `packages/infrastructure/tracking/tracking-queries.ts` | الموضعُ + الطابعُ |
| `apps/gateway/src/admin/queries.ts` | لوحةُ التشغيلِ والخريطةُ الحيّةُ والحرارةُ |
| `apps/gateway/src/routes/public-tracking.ts` عبرَ `get_tracking_position` | مسارُ الراكبِ العامُّ — استطلاعٌ كلَّ ٥ ثوانٍ |
| `bench/scenarios/catalog/ride-dispatch.ts` | سيناريو قياسٍ |

فالتراجعُ في هذا الحقلِ الواحدِ يُرى في المطابقةِ وفي لوحةِ التشغيلِ وفي خريطةِ الراكبِ معاً — ولذلك كان الحارسُ في الكاتبِ لا في كلِّ قارئٍ.

---

## ٣) الإصلاحُ — الشرطُ داخلَ جملةِ الكتابةِ

الجملةُ صارت واحدةً بثلاثةِ تعابيرَ جدوليّةٍ: `current` تُثبِت وجودَ الصفِّ، و`written` هي التحديثُ نفسُه مشروطاً في `where`، والاختيارُ الأخيرُ يُحوِّل «هل كُتِب صفٌّ؟» إلى قيمةٍ منطقيّةٍ واحدةٍ يعودُ بها المنفَذُ:

```sql
with current as (
  select id from drivers where id = $driverId
),
written as (
  update drivers as d
     set last_location = st_setsrid(st_makepoint($lng, $lat), 4326)::geography,
         last_location_at = now(),
         last_location_recorded_at = $at,
         last_location_accuracy_m = $accuracy,
         last_location_quality = $verdict,
         updated_at = now()
   where d.id = $driverId
     and (d.last_location_recorded_at is null or d.last_location_recorded_at <= $at)
  returning d.id
)
select true as accepted from written
union all
select false as accepted from current where not exists (select 1 from written)
```

**الذرّيّةُ**: لا قراءةَ ثمّ مقارنةَ ثمّ كتابةَ. الشرطُ في `where` الخاصِّ بجملةِ `update`، فإن انتظرت الجملةُ معاملةً متزامنةً على الصفِّ نفسِه أعادَ `PostgreSQL` في `READ COMMITTED` تقويمَ الشرطِ على **النسخةِ المُحدَّثةِ** بعدَ التثبيتِ — فالخاسرُ يرى ما كتبَه الفائزُ ويُحكَم عليه به. وهذه هي حجّةُ الذرّيّةِ نفسُها المكتوبةُ في `session-repository.advance` (حارسُ `BUG-009`)، وصياغةُ الجملةِ واحدةٌ عن قصدٍ لا توارُدٍ.

**ولا `Math.max` ولا `greatest`**: القيمةُ تُكتَب كما وصلت أو لا تُكتَب البتّةَ — و`greatest` كانت ستبتلعُ الإصلاحةَ الأقدمَ بأن تكتبَ موضعَها مع طابعِ الأحدثِ (وهو الخطأُ نفسُه الذي وُثِّق تركُه في حارسِ `BUG-009`).

**ولا `Date.now()` ولا `message.date` مباشرةً حَكَماً**: المقارنةُ بين طابعَي إصلاحتَين لا بين طابعٍ وساعةِ خادمٍ. و`message.date` يصلُ إلى الحارسِ بعدَ أن يمرَّ بمُقيِّمِ المرحلةِ ٣ في `gps-fix.ts` الذي يرفضُ غيرَ المحدودِ — فهو `fix.recordedAtMs` لا الرقمَ الخامَّ.

### لماذا `last_location_recorded_at` لا `last_location_at`

نصُّ البندِ يقترحُ `last_location_at`، وهو **يُحدَّث بـ`now()` مع كلِّ كتابةٍ** فلا يرفضُ شيئاً أبداً؛ والحكمُ به حكمٌ بساعةِ الخادمِ لا بترتيبِ الإصلاحاتِ. والعمودُ الذي يحملُ طابعَ **الجهازِ** هو `last_location_recorded_at` (أُضيفَ في `supabase/migrations/20260812020000_phase_5_fix_recorded_at.sql`)، وهو ما يقرؤه المجالُ إصلاحةً سابقةً وما تحكمُ به الجلسةُ. فالحرفُ اتُّبِع في **موضعِ الرقابةِ ونوعِها** (`where` في القاعدةِ)، والعمودُ اختيرَ بما يجعلُ الرقابةَ عاملةً لا صوريّةً.

### لماذا `<=` لا `<`

`ADR 0053` §٦ يُلزِم أن يكونَ مُسنَدُ هذا الحارسِ مُسنَدَ حارسِ الجلسةِ **حرفاً**: قِدَمٌ صارمٌ والتساوي مقبولٌ (§٤-ج) — وأيُّ اختلافٍ بينَ المُسنَدَين يوجبُ ADR ناسخاً. والمُسنَدانِ متطابقانِ الآنَ فلا ناسخَ. وطابعُ تلغرام بدقّةِ الثانيةِ، فإصلاحتانِ في ثانيةٍ واحدةٍ حالةٌ عاديّةٌ ورفضُهما كان سيُجمّد الخريطةَ عندَ الحركةِ السريعةِ.

### ما لم يُنفَّذ من نصِّ البندِ — مُعلَناً

نصُّ البندِ يذكرُ «مع عمود تسلسل تصاعدي». **لم يُضَف عمودُ تسلسلٍ على `drivers`**، وهذا تركٌ مقصودٌ لا سهوٌ:

- `ADR 0053` §٦ يُلزِم أن يكونَ مُسنَدُ هذا الحارسِ مُسنَدَ حارسِ الجلسةِ حرفاً — وهو مُسنَدٌ **زمنيٌّ**. فإضافةُ رقمٍ تصاعديٍّ حَكَماً ثانياً على «الأحدثِ» هو عينُ ما نهى عنه القرارُ: «ولا يجوزُ أن يصيرَ للنظامِ حَكَمانِ مختلفانِ على الأحدثِ».
- ورقمُ الترتيبِ في `ADR 0053` §٣-أ/٢-٣ **لكلِّ جلسةِ تتبُّعٍ** لا ترتيبَ كلّيّاً، ومحلُّه `tracking_sessions.last_sequence` — وهو قائمٌ ومُختبَرٌ. وعدّادٌ ثانٍ على `drivers` لا قارئَ له في المستودعِ كلِّه.

فإن أُريدَ العمودُ لاحقاً فهو قرارٌ معماريٌّ يوجبُ ADR ناسخاً، لا تفصيلَ تنفيذٍ يُضاف صامتاً.

### ولا هجرةَ جديدةً

العمودُ المحروسُ قائمٌ، والشرطُ على المفتاحِ الأساسيِّ. فعددُ الهجراتِ لم يتغيَّر (٦٠) و`check-schema-contract` (٧٧ دالّةً و٢٣ جدولاً) لم يتأثّر.

---

## ٤) الحكمُ صار جواباً لا صمتاً

كان `updateLocation` يعودُ بـ`Result<void, …>`: «نجحَ» تعني «لم يقع عطلٌ»، لا «كُتِبت إصلاحتُك». فصار المنفَذُ يعودُ بـ`LocationWriteOutcome`:

| القيمةُ | معناها |
|---|---|
| `accepted` | كُتِبت — هي الأحدثُ أو مساويةٌ |
| `stale` | لم تُكتَب — في القاعدةِ أحدثُ منها. **وليست عطلاً**: الحالةُ سليمةٌ ولم تتراجع |
| `no_driver` | لا صفَّ بهذا المعرِّفِ — عطلٌ حقيقيٌّ لا يُخلَط بالأقدمِ |

والمفرداتُ هي مفرداتُ `SessionAdvanceOutcome` نفسُها عن قصدٍ: اختلافُ الأسماءِ على المعنى الواحدِ أوّلُ خطوةٍ إلى حَكَمَين.

**و`StoredLocationQuality.recordedAtMs` صار لازماً** بعد أن كان اختياريّاً، و`quality` صارت مُعامَلاً لازماً. والسببُ ليس التجميلَ: كتابةٌ بلا طابعٍ كانت ستمرُّ بلا حكمٍ **وتمحو** الطابعَ المخزَّنَ (`= null`)، فتُسقِط أساسَ الحكمِ على ما يليها — بابٌ خلفيٌّ للتراجعِ الذي وُجِد الحارسُ ليمنعَه. واللزومُ في النوعِ يمنعُه عندَ الترجمةِ لا عندَ التشغيلِ. (وثيقةُ `docs/production-readiness/PHASE-4-CANONICAL-LOCATION.md:42` تصفُ `quality` اختياريّةً — وصفٌ تاريخيٌّ لحالةِ المرحلةِ ٤، لم يُعدَّل ههنا.)

**وامتناعُ النشرِ عندَ امتناعِ الكتابةِ**: `driver-dialog` عندَ `stale` لا يُنادي `tracking.onFix` ولا `redispatch` — نشرُها كان سيُخرِج موضعاً أقدمَ من المصدرِ القانونيِّ إلى الخريطةِ، أي التراجعَ نفسَه من بابٍ آخرَ. وهي قاعدةُ `ADR 0053` §٣-أ/٧ («امتناعُ الصفِّ امتناعُ النشرِ») مطبَّقةً على الصفِّ القانونيِّ، لا حَكَمٌ ثانٍ. ولا رسالةَ عطلٍ للسائقِ: لم يقع عطلٌ.

---

## ٥) الاختباراتُ

### أ) على PostgreSQL حقيقيٍّ — `tests/integration/driver-location-cas.test.ts` (٨)

| # | الحالةُ | ما تُثبِته |
|---|---|---|
| ١ | الأحدثُ يُقبَل والكاتبُ يعلم | `accepted` مرّتَين · الصفُّ يحمل الأحدثَ |
| ٢ | الأقدمُ الواصلُ بعدَ الأحدثِ | `stale` · والصفُّ **مطابقٌ حرفاً** لما كان قبلَ المحاولةِ |
| ٣ | الرفضُ لا يمسُّ عموداً | الدقّةُ والحكمُ والموضعُ كما كانت — لا صفَّ خليطاً من إصلاحتَين |
| ٤ | تساوي الطابعِ | يُقبَل — لا يُرفَض لمجرَّدِ التساوي (`ADR 0053` §٤-ج) |
| ٥ | معرِّفٌ لا وجودَ له | `no_driver` لا `stale` |
| ٦ | **سباقُ ثلاثِ نبضاتٍ مقلوبةٍ** | ثلاثةُ اتّصالاتٍ مستقلّةٍ · `Promise.all` · الباقي في القاعدةِ هو الأحدثُ · كلُّ نداءٍ عادَ بحكمٍ |
| ٧ | **إعادةُ تقويمِ الشرطِ بعدَ تثبيتِ معاملةٍ متزامنةٍ** | الدليلُ الحاسمُ (§٦) |
| ٨ | ساعةُ الجهازِ | طوابعُ في ٢٠٢٤ وطوابعُ في المستقبلِ ⇒ الحكمُ واحدٌ |

### ب) بلا قاعدةٍ — `tests/unit/driver-location-write.test.ts` (٨)

تحرسُ **شكلَ الجملةِ وترجمةَ جوابِها** لا الحكمَ: جملةٌ واحدةٌ لا اثنتانِ (فلا قراءةَ سابقةَ عليها) · الشرطُ مكتوبٌ في `where` · الحكمُ على `last_location_recorded_at` لا `last_location_at` · لا `greatest` ولا `max` · الطابعُ معاملٌ مُمَعلَمٌ لا موصولٌ في النصِّ · والأحكامُ الثلاثةُ، وعطلُ الاتّصالِ يبقى عطلاً لا `stale`. والمزدوجُ **لا يحكمُ** على «الأحدثِ» ولو محاكاةً — ذاك حَكَمٌ ثانٍ.

### ج) بلا قاعدةٍ — `tests/unit/driver-dialog.test.ts`

حالةٌ واحدةٌ مضافةٌ: إصلاحةٌ رفضَتها الكتابةُ الشرطيّةُ **لا تُبَثُّ ولا تُعيد الإسنادَ**، والردُّ على السائقِ ردُّ الحفظِ العاديُّ لا رسالةَ عطلٍ. والمزدوجُ **لا يُحاكي** الحارسَ: مقارنةُ الطوابعِ في الذاكرةِ كانت ستُنشئ حَكَماً ثانياً في الاختباراتِ نفسِها — فالحكمُ قيمةٌ تُضبَط من الاختبارِ، والحارسُ الحقيقيُّ يُختبَر على قاعدةٍ حقيقيّةٍ وحدَها.

### د) خريطةُ الحالاتِ التسعِ التي شرطَها التوجيهُ

| # | مطلوبٌ | المُثبِتُ |
|---|---|---|
| ١ | الأحدثُ يُقبَل | تكامل ١ |
| ٢ | الأقدمُ الواصلُ بعدَه يُرفَض | تكامل ٢ |
| ٣ | القاعدةُ لا تتراجع | تكامل ٢ و٣ (مقارنةُ الصفِّ كلِّه قبلَ وبعدَ) |
| ٤ | التساوي لا يُرفَض لمجرَّدِ التساوي | تكامل ٤ |
| ٥ | نتيجةٌ صريحةٌ للكاتبِ | تكامل ١ و٢ و٥ · وحدة ٥–٨ · ووحدة (سلوكُ `stale`) |
| ٦ | سباقٌ حقيقيٌّ بين كاتبَين متزامنَين | تكامل ٦ (ثلاثةُ اتّصالاتٍ) و٧ (قفلٌ حقيقيٌّ) |
| ٧ | آخرُ قيمةٍ مخزَّنةٍ هي المقبولةُ قانوناً | تكامل ٦ و٧ |
| ٨ | لا قراءةَ ثمّ كتابةَ | تكامل ٧ — وهو الفاصلُ (§٦) · ووحدة ١ (جملةٌ واحدةٌ لا اثنتانِ) |
| ٩ | استقلالٌ عن ساعةِ الجهازِ | تكامل ٨ |

---

## ٦) الدليلُ الحاسمُ — القفلُ الحقيقيُّ

الحالتانِ ٦ و٧ تُثبِتانِ شيئَين مختلفَين، والثانيةُ هي التي لا تمرُّ على تنفيذٍ زائفٍ:

اختبارُ `Promise.all` وحدَه **لا يفصلُ**: تنفيذٌ يقرأُ ثمّ يقارنُ في `JS` قد ينجحُ فيه صدفةً إذا تسلسلت الأوامرُ. فالحالةُ ٧ تفتحُ معاملةً على اتّصالٍ مستقلٍّ تكتبُ الأحدثَ **ولا تُثبِّتها**، ثمّ تُطلِق الكاتبَ الأقدمَ على اتّصالٍ آخرَ:

| المقيسُ | النتيجةُ |
|---|---|
| هل أجابَ الأقدمُ قبلَ التثبيتِ؟ | **لا** — بقي محبوساً على قفلِ الصفِّ ٣٠٠ مللي ثانيةً |
| حكمُه بعدَ التثبيتِ | **`stale`** |
| ما استقرَّ في الصفِّ | الأحدثُ: الموضعُ والطابعُ والدقّةُ كلُّها من المعاملةِ المُثبَّتةِ |

وتنفيذٌ يقرأُ ثمّ يقارنُ في `JS` كان سيُجيب **فوراً** (لا قفلَ على القراءةِ) **وبالقبولِ** (اللقطةُ القديمةُ لم تكن تحوي الأحدثَ) ثمّ يكتبُ فيُرجِع الحالةَ. فالاختبارُ يسقطُ عندَه في موضعَين، ويمرُّ فقط لأنَّ الرقابةَ في القاعدةِ.

---

## ٧) القياسُ المحلِّيُّ

قاعدةُ PostgreSQL **18.6** مع PostGIS 3.6 في بيئةِ التنفيذِ (CI يستعمل `postgis/postgis:17-3.5`):

| ما جرى | النتيجةُ |
|---|---|
| هجرةٌ من الصفرِ: ٦٠ ملفّاً بالترتيبِ | **نجحت كلُّها** |
| `tests/integration/driver-location-cas.test.ts` | **٨ نجاحاً · ٠ فشلاً** |
| `tests/integration/canonical-driver-location.test.ts` · `live-sequence-validation` · `driver-location-freshness` | **١٨ نجاحاً · ٠ فشلاً** (لم تُعدَّل واحدةٌ منها) |
| `tests/unit/driver-dialog.test.ts` | **٨٧ نجاحاً · ٠ فشلاً** |
| `tests/unit/driver-location-write.test.ts` | **٨ نجاحاً · ٠ فشلاً** |
| `bun run test` كاملاً بقاعدةٍ | **٢٥٢٤ نجاحاً · ٠ فشلاً · ٢٣ تخطّياً** (تخطّي Redis والقياسِ: دَينٌ بيئيٌّ قائمٌ يُشغِّله CI) |
| `bun run test` بلا `TEST_DATABASE_URL` — كما في وظيفةِ `verify` | **٢٠٩١ نجاحاً · ٠ فشلاً** |
| `bun run check:coverage` بقاعدةٍ | ✅ «التتبّعُ وموقعُ السائق: **88.94%** (764/859)» |
| `bun run check:coverage` بلا قاعدةٍ — وهو مقياسُ `verify` | ✅ «الهويةُ والجلسةُ والصلاحيات: **76.66%** (657/857)» فوقَ أرضيّةِ 72% — وكانت قبلَ العملِ 72.24% |
| `bun run typecheck` · `bun run lint` | نظيفانِ |
| `bun run ci` (٢٤ حاجزاً) | كلُّها مرَّت عدا `verify-container-build.sh` — يشترطُ bun 1.3.14 وفي بيئةِ التنفيذِ 1.4.0. **حدٌّ بيئيٌّ قائمٌ قبلَ هذا العملِ**، ويُشغِّله CI على الإصدارِ المثبَّتِ |

### ثلاثُ حواجزَ سقطَت وما فُعِلَ بها

**١) حاجزُ تسجيلِ التجاوزِ (`OPS-009`) محلّياً** — ملفُّ تكاملٍ جديدٌ يتجاوز بلا قاعدةٍ ولم يُصنَّف. والمضافُ إلى `scripts/lib/skip-registry.ts` مدخلٌ **تسجيليٌّ** يوجبُه الحاجزُ لكلِّ ملفٍّ مشروطٍ بـ`TEST_DATABASE_URL`: سببٌ وشرطُ تفعيلٍ ومالكٌ ومسارٌ حرجٌ — تصنيفٌ للتجاوزِ لا إعفاءٌ منه، والملفُ يُشغَّل كاملاً في وظيفةِ التكاملِ.

**٢) حرزُ أرقامِ السجلِّ في CI** (تشغيلا ٢٢٤ و٢٢٥) — `tests/unit/skip-audit.test.ts` يُثبِّت العددَ عمداً ليُقرَأَ السجلُّ عندَ كلِّ ملفٍّ جديدٍ. فرُفِعَ إلى ٥٨ ملفّاً و٥٤٤ حالةً — **والأعدادُ تُرفَع ولا تُخفَّض** كما ينصُّ الملفُ نفسُه.

**٣) بوّابةُ التغطيةِ (`OPS-005`) في CI** (تشغيل ٢٢٦) — مسارُ «الهويةِ والجلسةِ والصلاحياتِ» كان عندَ **72.24%** فوقَ أرضيّةِ 72% بفرقٍ لا يحتمل سطرين. و`directories.ts` مُغطّى **6/214** في وظيفةِ `verify` لأنَّ تغطيتَه كلَّها من اختباراتِ تكاملٍ تتجاوز هناك — وذاك حدٌّ **مُعلَنٌ في `coverage-registry.ts` نفسِه** — فكلُّ سطرٍ يُضاف إلى ذلك الملفِّ يُنقِص النسبةَ، والمقياسُ يعُدُّ أسطرَ التوثيقِ أيضاً أسطراً غيرَ مغطّاةٍ.

وسجلُّ التغطيةِ ينصُّ على أنَّ النقصانَ الحقيقيَّ **يُصلَح** ولا تُخفَّض له أرضيّةٌ. **فلم تُخفَّض أرضيّةٌ ولم يُحذَف توثيقٌ ولم يُضَف استثناءٌ**: أُضيفَت تغطيةٌ حقيقيّةٌ للمحوّلِ ببديلِ `sql` مُسجِّلٍ — وهو نمطٌ قائمٌ في المستودعِ (`tests/unit/viewer-account-reader.test.ts`) — فصارَ المسارُ **76.66%**، أعلى ممّا كان قبلَ العملِ.

---

## ٨) الأمنُ

لا سرَّ ولا رمزَ جلسةٍ في أيِّ سطرٍ مضافٍ. والمضافُ إلى المنفَذِ قيمةٌ من ثلاثِ كلماتٍ (`accepted` · `stale` · `no_driver`) لا تُشتَقُّ منها هويّةٌ. وحاجزُ `check-secret-logging.ts` يمرّ.

---

## ٩) ما لا يُغلِقه هذا الدليلُ — مُعلَناً لا مُخفىً

| البندُ | حالُه |
|---|---|
| `BUG-009` | **مغلقٌ سابقاً بـ`ADR 0053`** ولم يُمَسّ ههنا. ولم يُنشَأ حَكَمٌ ثانٍ: المُسنَدانِ متطابقانِ حرفاً (§٣) |
| `R-17` | **مفتوحٌ** — نسخةٌ واحدةٌ في `render.yaml`. وهذا الحارسُ **في القاعدةِ** فهو صحيحٌ مع عدّةِ نُسَخٍ من حيثُ عدمُ التراجعِ، لكنَّ `R-17` عن الناقلِ والبثِّ لا عن هذا الحقلِ — ولا يُقرَأ إغلاقُ `BUG-001` تقدُّماً فيه |
| `F4` والنقلُ اللحظيُّ في الإنتاجِ | لم يبدأ. لا Adapter ولا توزيعَ Redis |
| مسارُ الراكبِ العامُّ | يقرأ الحقلَ نفسَه عبرَ `get_tracking_position`، فينتفعُ بعدمِ التراجعِ. ولم يُضَف إليه رقمُ ترتيبٍ — وهو استطلاعٌ لا اشتراكٌ |
| عمودُ التسلسلِ على `drivers` | **لم يُنفَّذ عمداً** (§٣) — وإن أُريدَ فبـADR ناسخٍ |
| سعةٌ · أداءٌ · تحقُّقُ جهازٍ · صحّةُ نسختَين · جاهزيّةُ realtime | **لا يُدَّعى منها شيءٌ** |
