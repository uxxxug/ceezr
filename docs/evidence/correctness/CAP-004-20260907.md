# CAP-004 — اشتقاق `prepare` من رابط الاتصال + قرار pooler + ميزانية الاتصالات

**معرّف البند:** `CAP-004` (ROADMAP §11-د · مخاوف شاملة)
**التصنيف:** `reliability` · `architecture`
**التاريخ:** 2026-09-07
**المنفّذ:** وكيل Perplexity Computer
**الـcommit التنفيذي:** `a1b2e3d` (التزامُ الكودِ الذي أغلقَ `CAP-004`)؛ والتزامُ تسجيلِ نتيجةِ CI: `9389d4c` (رأسُ `main` الحالي).
**بصمة الإعدادات:** `bun` 1.4.2 محليّاً · `tsc --noEmit` نظيف · `biome check` نظيف.
**الطوبولوجيا:** مهامُ CI الثلاث — `verify` (lint/typecheck/build:miniapp + 21 حاجزاً + `verify-container-build.sh` + `test` + `check:coverage`) · `تكامل على PostgreSQL حقيقي` · `تكامل على Redis حقيقي`.

## نصُّ البند

> `CAP-004` | ميزانية الاتصالات ≈ `5N + 10M`، و`prepare: true` يتعارض مع Supabase transaction pooler (`packages/infrastructure/db/client.ts:8,20-21`) | نموذج ميزانية اتصالات معلن + قرار pooler موثَّق بـADR (transaction vs session) + تعطيل الجُمل المُحضَّرة عند pooling المعاملات | `[ ]`

## المشكلةُ بحرفِها

`createSql` في `packages/infrastructure/db/client.ts` كان يُمرِّرُ `prepare: options.prepare ?? true` — أيّ الافتراضيَّ `true` إن لم يُمرَّر صراحةً. والبوابةُ `apps/gateway/src/container.ts:324` تستدعي `createSql({ connectionString })` **بلا `prepare`**، فترثُ الافتراضيَّ `true`. والعاملُ كان ضبطَ `prepare: false` صراحةً فحُمِيَ. **لكنّ البوابةَ لم تضبطْه**، فلو أُطلقَتْ في الإنتاجِ على رابطِ Supabase transaction pooler (المنفذُ 6543) لفشلَتْ: transaction pooling لا يدعمُ الجُملَ المُحضَّرة، فيُلقي `postgres` أخطاءً أو يفشلُ في إعدادِ الجملة.

والعيبُ جذرُهُ أنّ ضبطَ `prepare` كان مسؤوليّةَ كلِّ مستدعٍ (البوابةُ نسِيَتْه)، فمتى ظهرَ مستدعٍ جديدٌ دونَه عادَ العيبُ.

## المُنفَّذُ

ملفٌّ إنتاجيٌّ واحدٌ + ADR + اختبارُ وحدة:

### ١. `packages/infrastructure/db/client.ts` (تعديل)

دالتانِ جديدتانِ مركزيّتانِ:

- **`detectDbPoolerMode(connectionString)`** → `"transaction" | "session" | "direct" | "unknown"`:
  - `transaction`: المضيفُ يحوي `pooler.supabase` والمنفذُ `6543`، **أو** `?pgbouncer=true`.
  - `session`: المضيفُ يحوي `pooler.supabase` والمنفذُ `5432`.
  - `direct`: اتصالٌ مباشرٌ (`db.…supabase.co` أو مضيفٌ محليٌّ).
  - `unknown`: رابطٌ غيرُ قابلٍ للتحليلِ بصيغةِ URL (key=value أو فارغ).
- **`resolvePrepare(options)`** → `boolean`:
  - **قاعدةُ السلامةِ:** إن كان `transaction` ⇒ `false` حتماً — حتى لو طُلِبَ `prepare: true` صراحةً، لأنّ transaction pooling لا يدعمُ الجُملَ المُحضَّرة فيعطّلُ الإعدادَ آمناً قبلَ أن يفشلَ في وقتِ التشغيلِ.
  - لغيرِ `transaction` يُحترَمُ التمريرُ الصريحُ أو يُرجَعُ الافتراضيُّ `true`.

وصارَ `createSql` يستخدمُ `resolvePrepare(options)` مركزيّاً بدلَ `options.prepare ?? true`. **فحُمِيَ كلُّ مستدعٍ تلقائيّاً** — البوابةُ التي كانت تنسى `prepare: false` صارَت تأخذُه من السياسة، والعاملُ يُحقَّقُ له آليّاً. ولا يعودُ العيبُ في مستدعٍ جديد.

### ٢. ADR 0056 (جديد)

[`docs/adr/0056-supabase-transaction-pooler-and-prepared-statements.md`](../adr/0056-supabase-transaction-pooler-and-prepared-statements.md) يوثِّقُ:
- **القرار:** transaction pooler هو مسارُ الإنتاجِ المعتمد، فتُعطَّلُ الجُملُ المُحضَّرةُ آليّاً عندَه.
- **السياسةُ المركزيّة:** اشتقاقُ `prepare` من رابطِ الاتصالِ، فلا يُتركُ لكلِّ مستدعٍ.
- **نموذجُ الميزانية:** `5N + 10M` (مُقاسٌ من الثوابتِ الفعليّة: بوابة `max:5`، عامل `max:5 + max:5`، `MAX_JOB_CONCURRENCY=4`).

### ٣. `tests/unit/db-pooler-policy.test.ts` (جديد)

**18 توكيداً** تختبرُ **سياسةَ `detectDbPoolerMode`/`resolvePrepare`** لا كائنَ `postgres` نفسَه. وتُثبتُ قاعدةَ السلامةِ مباشرةً: transaction pooler ⇒ `prepare: false` حتماً حتى مع `prepare: true` صريح. **واستخدامُ `createSql` لِـ`resolvePrepare` مركزيّاً يُتحقَّقُ منه بالكودِ والـtypecheck وCI** (لا باختبارِ mock مستقل).

## مطابقةُ المتطلَّبِ بالنصّ

| نصُّ CAP-004 | المُنفَّذُ | مطابق؟ |
|---|---|---|
| «نموذج ميزانية اتصالات معلن» | ADR 0056 §٣-ج: `5N + 10M` مُقاسٌ من الثوابتِ الفعليّة | ✓ |
| «قرار pooler موثَّق بـADR (transaction vs session)» | ADR 0056 §٢-٣: transaction vs session vs direct موثَّق | ✓ |
| «تعطيل الجُمل المُحضَّرة عند pooling المعاملات» | `resolvePrepare`: transaction ⇒ `false` حتماً (حتى مع `prepare: true` صريح) | ✓ |
| «`packages/infrastructure/db/client.ts:8,20-21`» | التعديلُ في الملفِّ نفسِه (السطرُ 20-21 صار `resolvePrepare`) | ✓ |

## الاختباراتُ (كلُّها خضراء)

### `tests/unit/db-pooler-policy.test.ts` (18)

- **detectDbPoolerMode** (7): transaction pooler 6543 ⇒ `transaction` · `?pgbouncer=true` ⇒ `transaction` · session pooler 5432 ⇒ `session` · اتصالٌ مباشرٌ ⇒ `direct` · اتصالٌ محليٌّ ⇒ `direct` · رابطٌ فارغٌ ⇒ `unknown` · صيغةُ key=value ⇒ `unknown`.
- **resolvePrepare** (9): transaction ⇒ `false` · `?pgbouncer=true` ⇒ `false` · **`prepare: true` صريحٌ مع transaction ⇒ `false`** (قاعدةُ السلامة) · session بلا تمريرٍ ⇒ `true` · session مع `prepare: false` ⇒ `false` (يُحترَم) · مباشرٌ ⇒ `true` · محليٌّ ⇒ `true` · غيرُ قابلٍ للتحليلِ ⇒ `true` (السلوكُ السابقُ) · فارغٌ ⇒ `true`.
- **resolvePrepare — نمطُ البوابة** (2): البوابةُ بلا `prepare` يُشتقّ من الرابط (transaction ⇒ `false`) · أنواعُ `DbPoolerMode` الأربعةُ ممكنةٌ كلُّها.

## قرارٌ تصميميٌّ مُعلَن

نُقلتْ سياسةُ `prepare` من كلِّ مستدعٍ إلى مركزِ الإنشاءِ (`createSql` → `resolvePrepare`). لا يُعتمدُ على تعديلِ البوابةِ وحدَها — لأنّ العيبَ قد يعودُ في مستدعٍ جديد. والسياسةُ المركزيّةُ تحمي كلَّ مستدعٍ تلقائيّاً. **ولم يُفرَضْ مسارُ اتصالٍ واحدٌ في الشيفرةِ:** السياسةُ تُكيِّفُ `prepare` مع الرابطِ، فلا تنكسرُ بيئةُ التطويرِ بالاتصالِ المباشر، ولا يفشلُ الإنتاجُ على transaction pooler.

## حدودُ هذا الإغلاقِ (مُعلَنة)

- **هذا إغلاقٌ لِـ`CAP-004` وحدَه** لا فتحٌ لبوابةِ المرحلةِ F5. بوابةُ F5 تبقى مغلقةً.
- **لا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه منطقُ الاشتقاقِ في اختبارِ وحدة، لا throughputُ الإنتاجِ ولا latencyُ القاعدةِ ولا ميزانيّةُ اتصالٍ فُعِلَتْ على إنتاج.
- **لا هجرةَ قاعدةٍ ولا schema** — التغييرُ كلُّه في طبقةِ عميلِ القاعدة.
- **لا `numInstances` يُرفَعُ ولا عاملٌ يُنقَلُ** — نموذجُ الميزانيّةِ مُعلَنٌ من الثوابتِ القائمة لا مُغيَّر.
- **لا `BUG-002` ولا `F5-01`/`F5-02`/`F5-04` ولا أيُّ بندٍ آخر مُسَّ.**
- **لا `force-push` ولا تاريخٌ أُعيدَ كتابتُه.**

## قيدُ البيئةِ المحلّيّةِ (مُعلَن)

- `verify-container-build.sh` يسقطُ محلّياً لتباينِ إصدارِ `bun` (1.3.14 في `docker/Dockerfile.*` مقابل 1.4.2 محلّياً) — قيدٌ سابقٌ لا علاقةَ لتغييراتِ هذا الإغلاقِ به. **والحكمُ النهائيُّ من GitHub Actions بعدَ الدفع.**
- اختباراتُ التكاملِ (622) متجاوَزةٌ محلّياً لعدمِ توفّرِ PostgreSQL/Redis حقيقيَّين — يُقاسُ سلوكُها في CI.

## CI

محليّاً: `lint`=0 (27 معلومةً غيرَ حاجزة) · `typecheck`=0 · `build:miniapp`=0 · **٢١ حارساً كلُّهم خروجُ `0`** · `test`=**2169 ناجحاً · 622 متجاوَز · 0 فاشل** (٢٧٩١ اختباراً في ٢١١ ملفّاً) · `check:coverage`=0 فوقَ أرضيّاتِه التسع. الفشلُ الوحيدُ `verify-container-build.sh` (قيدُ بيئةٍ سابقٌ). **وGitHub Actions (الحكمُ النهائيُّ): التشغيلُ `34072012637` على `a1b2e3d` اكتملَ بنتيجةِ `success` (2m).**

## الحُكمُ النهائي

`CAP-004` أُغلقَ بـ`[x]`: نُفِّذَ نموذجُ ميزانيّةِ الاتصالاتِ (`5N + 10M` مُقاسٌ من الثوابتِ) + قرارُ pooler موثَّقٌ بـADR 0056 (transaction vs session vs direct) + تعطيلُ الجُملِ المُحضَّرةِ عندَ transaction pooling عبر سياسةٍ مركزيّةٍ (`detectDbPoolerMode`/`resolvePrepare`) تحمي كلَّ مستدعٍ تلقائيّاً — فلا يعودُ العيبُ في البوابةِ أو أيِّ مستدعٍ جديد. والدليلُ ثمانيةَ عشرَ توكيداً تُثبتُ قاعدةَ السلامةِ: transaction pooler ⇒ `prepare: false` حتماً حتى لو طُلِبَ `true` صراحةً.
