# F1-09 · D-29 — قاموسا `en`/`ur` خارجَ الحِملِ الأوّلِ (2026-09-25)

**البند**: `F1-09` (الصفّانِ 4–5 من القسمِ 9.9) · **الدَّينُ**: `D-29` · **القرارُ**: `ADR 0186` · **PR**: #262

## الاكتشافُ

بعدَ `D-27` (CI `36066248642`): LCP/السطحُ وسيطُهما 3,064 ms والبايتاتُ حتّى السطحِ 208,120.
خريطةُ مصدرِ `shell` (94 KB مضغوطةً · 384 KB خاماً) حملَت القواميسَ الثلاثةَ كاملةً:
`ar` ≈ 29.0 KB · `en` ≈ 24.2 KB · `ur` ≈ 28.5 KB مضغوطةً — ≈ 52.7 KB للغتَينِ لا تُعرَضانِ.

## الإصلاحُ

نواةٌ بسجلٍّ (`core.ts`، `ar` ثابتٌ) · `load.ts` بـ`import()` · `index.ts` متزامنٌ كاملٌ للخادمِ والحواجزِ ·
`RoleRouter` يُحمِّلُ قاموسَ لغةِ الحسابِ موازياً لحزمةِ السطحِ · حاجزُ بناءٍ
`apps/miniapp/vite/assert-initial-dictionaries.ts` (أُثبِتَ فشلُه: إعادةُ استيرادِ `index.ts` في
`RoleRouter.tsx` ⇒ `RolldownError` بثلاثِ مخالفاتٍ في `shell`: `en.json` · `ur.json` · `index.ts`).

## البناءُ المحلّيُّ (`check-performance-budget`)

| | قبلُ | بعدُ |
|---|---|---|
| `shell` مضغوطةً | ≈ 94 KB | 41.0 KB |
| الحِملُ الأوّلُ | — | 106.3 KB / 180 |
| طلباتُ أوّلِ رسمٍ | 6 | 6 |
| حزمٌ مؤجَّلةٌ جديدةٌ | — | `en` 23.7 KB · `ur` 27.8 KB (سندٌ في `DECLARED_EXTRA_BUNDLES`) |

## حكمُ CI (run `36068849297` · job `107864823230` · Slow 4G + CPU ×4 · سطحُ الراكبِ)

| التشغيلُ | FCP | LCP | بلوغُ السطحِ |
|---|---|---|---|
| #1 | 668 | 2,752 | 2,752 |
| #2 | 660 | 2,756 | 2,756 |
| #3 | 660 | 2,732 | 2,732 |
| **الوسيطُ** | **660** | **2,752** (كانَ 3,064) | **2,752** (كانَ 3,064) |

- البايتاتُ حتّى السطحِ: **155,891** (كانَت 208,120) · الطلباتُ 10.
- حارسُ 3G: وسيطُ السطحِ 9,264 ms ≤ 18,000.
- الجدولُ الزمنيُّ (بلا تقييدٍ لا يعنينا؛ هذا Slow 4G #1 من السجلِّ): `shell` 614→1687 · `vendor-react` 614→1770 ·
  `/v1/session/telegram` 643→1237 · `/v1/me` 1242→1812 · **`rider-home` 1885→2670** (40,184 B).

## ما لا يُدَّعى (`ح-5`)

الصفّانِ 4 و5 **غيرُ مستوفيَينِ** (2,752 > 2,500 · > 2,000) · `F1-09` `[~]` · `SLOW_4G_GATE_MODE` تقريرٌ ·
لا حدَّ خُفِّضَ · مستخدمُ `en`/`ur` لم يُقَس زمناً. **السببُ التالي الظاهرُ**: `rider-home` لا يبدأُ قبلَ 1,885 ms
(بعدَ تقييمِ `shell` و`/v1/me`) ويستغرقُ 785 ms — يُسجَّلُ ويُحجَزُ بنداً مستقلّاً.
