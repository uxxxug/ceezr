# F11-02 — فقدانُ عمليّةِ عاملٍ حقيقيّةٍ

**التاريخ**: 2026-09-25 · **الفرعُ**: `feat/f11-02-worker-loss-reclaim` من `main`@`7d959d4` · **القرارُ**: `ADR 0195`

## المقيسُ قبلَ التنفيذِ

- `tests/integration/notification-outbox.test.ts` «حجزٌ متروكٌ … يُستعادُ ويُسلَّم»: `claimed_at` يُعدَّلُ ساعةً إلى الوراءِ داخلَ عمليّةٍ واحدةٍ.
- لا اختبارَ يقتلُ عمليّةَ عاملٍ؛ وعدُ تحريرِ القفلِ الاستشاريِّ تعليقٌ في `advisory-lock.ts`.

## المقيسُ (محلّيّاً · PostgreSQL 16)

| الطورُ | تحريرُ القفلِ بعدَ خروجِ العمليّةِ | التسلُّمُ والتسليمُ بعدَ القتلِ | الرسائلُ | المحاولاتُ | إعلانُ الميّتِ | الحكمُ |
|---|---|---|---|---|---|---|
| `/gate` (قبلَ الإرسالِ) | 1ms | 1172ms | 1 (الثاني) | 2 | مرفوضٌ | `ok` |
| `/send` (بعدَ الوصولِ وقبلَ الإقرارِ) | 0ms | 1174ms | 2 (الميّتُ · الثاني) | 2 | مرفوضٌ | `ok` |

مهلةُ الاسترجاعِ في الاختبارِ 2s (تُعادُ الأصليّةُ بعدَه). والعاملُ الثاني حيٌّ قبلَ القتلِ ولم يبلُغِ الناشرَ ما دامَ القفلُ على جلسةِ الأوّلِ.
حكمُ CI يُضافُ في حجزِ `ROADMAP.md`.

## ما لا يُدَّعى

لا حِملَ ولا بيئةَ إنتاجٍ (`ح-5`) · مهمّةُ الصادرِ وحدَها · `D-37` مُسجَّلٌ لا مُصلَحٌ.
