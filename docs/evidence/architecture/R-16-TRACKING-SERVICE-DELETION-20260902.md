# دليلُ `R-16` — حذفُ `TrackingService`: مسارُ تقييمٍ واحدٌ لا مساران

**البند:** `R-16` — `docs/production-readiness/PHASE-6-REALTIME.md` §١٠ (السطر 197)
**التاريخ:** 2026-09-02
**السند:** [`ADR 0052`](../../adr/0052-tracking-service-is-deleted-not-merged.md) · وينبني على [`ADR 0015`](../../adr/) و[`ADR 0042`](../../adr/)
**الـcommit المُقاسُ عليه قبلَ الحذفِ:** `3bb8781e008a17305439d7d13114e5d9fcc902cc`
**commit الحذفِ:** `66a5ed0f29bf6d036034bfdbbe8acc16f7dba53b`
**تشغيلُ CI:** `33572493851` — `success` في الوظائفِ الثلاثِ
**الدرجة:** **مُنفَّذ · مُختبَر · مُتحقَّق منه** (سلّم `ROADMAP` §1.4)
**ما لا يُدَّعى:** لا **مَقيس** ولا **مُثبَت** — لا حِملَ قِيسَ ولا سعةَ ولا زمنَ استجابةٍ (`ح-5`).

---

## ١) ما الذي كان `R-16` يقوله، وما الذي أغلقه هذا الحذفُ

نصُّ البندِ بحرفِه (لم يُمَسّ — `ح-1`):

> **`TrackingService` القديم مازال غير موصول** (رموزٌ في الذاكرة، ومساره غير مركَّب).
> لم يُحذف لأن حذفه يُسقط ~٤٠ اختباراً، ولم يُركَّب لأن تركيبه بمخزنٍ في الذاكرة
> يُنشئ مصدر حقيقةٍ ثانياً للتتبّع.

فالبندُ يصف **مأزقاً بين طرفَين**، وكلا طرفَيه كان قائماً: التركيبُ يُنشئ مصدرَ حقيقةٍ
ثانياً، والحذفُ يُسقِط اختباراتٍ. و`ADR 0052` حسم أنّ **الطرفَ الثاني ثمنٌ لا مانعٌ**:
الاختباراتُ الساقطةُ تختبر شيفرةً لا تُشحَن، فسقوطُها ليس فقدَ تغطيةٍ بل حذفُ ادّعاءِ
تغطيةٍ لم يكن يقابله شيءٌ في الإنتاج.

**والمُغلَقُ بهذا الحذفِ هو البندُ بعينِه**: لم يبقَ في المستودعِ رمزٌ غيرُ موصولٍ
اسمُه `TrackingService`، ولا مسارٌ غيرُ مركَّبٍ يخصُّه، ولا احتمالُ تركيبٍ يُنشئ مصدرَ
حقيقةٍ ثانياً. **ولا يُقرأ إغلاقُه إثباتاً لشيءٍ آخرَ**: لا لصلاحيةِ realtime للإنتاج،
ولا لتغطيةٍ جديدةٍ، ولا لقدرةٍ لم تكن موجودةً.

---

## ٢) البرهانُ أنّ `TrackingService` كان غيرَ موصولٍ

قِيسَ على `3bb8781e00` قبلَ أيِّ تعديلٍ:

| الشاهد | النتيجة |
|---|---|
| ذِكرُ `TrackingService` في `apps/gateway/src/container.ts` | **صفر** |
| مستهلِكوه في شيفرةِ الإنتاجِ | **واحدٌ فقط**: `apps/gateway/src/routes/tracking.ts` |
| تركيبُ `routes/tracking.ts` في `apps/gateway/src/index.ts` | **غيرُ مركَّبٍ** — لا `app.route` ولا استيرادَ |
| مستهلِكوه في الاختباراتِ | `tests/unit/tracking.test.ts` و`tests/unit/tracking-route.test.ts` وحدَهما |

أي أنّ السلسلةَ كانت مقطوعةً في موضعَين معاً: الخدمةُ لا تُبنى في الحاويةِ، والمسارُ
الذي كان سيبنيها **لم يكن مركَّباً في التطبيق**. فما كان أحدٌ يستطيع بلوغَها في
الإنتاجِ بأيِّ طلبٍ.

وحالتُها الداخليةُ كانت `sessions: Map` و`previousPositions: Map` — **ذاكرةً داخلَ
العمليةِ**. وهذا بعينِه ما علّق عليه `ADR 0015` صحّتَها:

> «`previousPositions` في `TrackingService` ما يزال ذاكرةً داخل العملية، ولن يصحّ
> إلا حين تصير الإصلاحة السابقة تُقرأ من المصدر القانوني نفسه»

والمسارُ الحيُّ **استوفى هذا الشرطَ فعلاً** (§٣)، فلم يبقَ للخدمةِ الموازيةِ سببُ
بقاءٍ: هي إمّا مصدرُ حقيقةٍ ثانٍ إن رُكِّبت، أو شيفرةٌ ميتةٌ إن تُرِكت.

---

## ٣) المسارُ الحيُّ الحاليُّ هو المسارُ الوحيدُ

مسارُ التقييمِ الوحيدُ اليومَ، وكلُّ حلقةٍ فيه موصولةٌ:

1. **المُدخَل** — `handleLocation` في `packages/application/bots/driver-dialog.ts:2050`.
2. **الإصلاحةُ السابقةُ** — تُقرأ من `driver.lastFix` أي من العمودِ `drivers.last_location`
   في القاعدة، **لا من ذاكرةِ عمليةٍ**.
3. **التقييمُ** — `assessGpsFix` في `packages/domain/geo/gps-fix.ts` — دالّةٌ نقيّةٌ واحدةٌ.
4. **الكتابةُ** — `drivers.updateLocation` هو **الكاتبُ الوحيدُ** لموقعِ السائقِ
   (`ADR 0015`).
5. **النشرُ** — `packages/application/tracking/live-tracking.ts`، موصولٌ في
   `apps/gateway/src/container.ts:641`.

**فالمصدرُ الوحيدُ للإصلاحةِ السابقةِ هو القاعدةُ**، **والناشرُ واحدٌ**، **ولا مسارَ
تقييمٍ ثانٍ**. وبعدَ الحذفِ لم يبقَ في المستودعِ من يستدعي `assessGpsFix` أو يكتب
موقعَ سائقٍ خارجَ هذه السلسلةِ.

---

## ٤) ما الذي حُذِف

| الملف | لماذا |
|---|---|
| `packages/tracking/tracking-service.ts` | الخدمةُ نفسُها: مسارُ تقييمٍ ثانٍ وذاكرةُ إصلاحةٍ سابقةٍ داخلَ العمليةِ، غيرُ موصولةٍ |
| `packages/tracking/tracking-auth.ts` | `TrackingTokenStore` — **واجهةٌ بلا تنفيذٍ ولا مُصدِرٍ ولا مستهلِكٍ**. وتتبّعُ العميلِ نُفِّذ بنظامٍ آخرَ تماماً (`trip_tracking_tokens` والمرحلة ١١)، فهذا رمزُ نظامٍ لم يُبنَ |
| `apps/gateway/src/routes/tracking.ts` | المسارُ الوحيدُ الذي كان يستهلك الخدمةَ، **وهو نفسُه غيرُ مركَّبٍ في `index.ts`** |
| `tests/unit/tracking-route.test.ts` | 16 اختباراً لمسارٍ محذوفٍ غيرِ مركَّبٍ |
| `tests/unit/tracking.test.ts` | 12 اختباراً: 10 منها لـ`tracking: service`، و2 لـ`haversine` (§٦) |

**وحُذِفت رموزٌ صارت ميتةً بالحذفِ نفسِه** — لا استباقاً بل بعدَ أن أثبت المُترجِمُ
ورسمُ الاستيرادِ موتَها:

- `resolveTrackingConfig` في `packages/tracking/config.ts` — كانت تبني `TrackingConfig`
  الذي لم يعد له نوعٌ ولا مستهلِكٌ.
- `GpsUpdate` و`ValidationResult` في `packages/tracking/types.ts` — لا مستهلِكَ لهما بعدَ الحذفِ.
- إعادةُ تصديرِ رموزِ الخدمةِ من `packages/tracking/index.ts`.

**وهذا انحرافٌ مُعلَنٌ عن حرفِ `ADR 0052` §٨** الذي قال «`types.ts` يبقى بلا مساسٍ»:
المساسُ وقع في رمزَين ميتَين لا في `TrackingEvent`، وسندُه أمرُ الوحدةِ الصريحُ بألّا
تبقى `dead exports`. **و`TrackingEvent` نفسُه لم يُمَسّ حرفاً**، ولا `TrackingEventType`
ولا حقولُه — **ولم يُقلَّم نوعُ الحدثِ رغمَ أنّ ستّةً من أنواعِه الأحدَ عشرَ بلا ناشرٍ
اليومَ**، لأنّ ذلك تغييرُ عقدٍ لا حذفُ شيفرةٍ ميتةٍ، وهو خارجُ نطاقِ هذه الوحدةِ.

---

## ٥) ما الذي بقيَ ولماذا

| الرمز | لماذا بقيَ |
|---|---|
| `packages/tracking/session-store.ts` | **ليس من `TrackingService` في شيءٍ.** `TrackingSessionStore` وتنفيذُه على PostgreSQL في `packages/infrastructure/tracking/session-repository.ts`، وهو مستعمَلٌ في المسارِ الحيِّ |
| `resolveGpsPolicy` | **حيٌّ**: `container.ts:142` يستورده و`container.ts:677` يستدعيه |
| `packages/domain/geo/gps-fix.ts` | مُقيِّمُ الإصلاحةِ الوحيدُ في المسارِ الحيِّ — لم يُمَسّ |
| `packages/domain/tracking/session.ts` | وقائعُ الجلسةِ وسياستُها — لم تُمَسّ |
| `TrackingEvent` و`TrackingEventType` | عقدُ الحدثِ — لم يُمَسّ حرفاً |
| `TrackingEventPublisher` | **نُقِل لا حُذِف**: كان مُعرَّفاً في الملفِّ المحذوفِ، ومستهلِكاه `live-tracking.ts` و`event-bus.ts` — أي المسارُ الحيُّ وناقلُه. فنُقِل إلى `packages/tracking/types.ts` بجوارِ `TrackingEvent` بلا تغييرِ توقيعٍ |
| ثوابتُ البيئةِ الثلاثةُ | `TRACKING_GPS_INTERVAL_SECONDS` · `TRACKING_GPS_IDLE_INTERVAL_SECONDS` · `TRACKING_MIN_DISTANCE_METERS` — تبقى مقروءةً عندَ الإقلاعِ (فحصُ انحرافِ البيئةِ أخضرُ) **ولا مستهلِكَ إنتاجيَّ لها بعدَ الحذفِ** |

**والثوابتُ الثلاثةُ دَينٌ مُعلَنٌ لا مُبتلَعٌ:** حذفُها تغييرُ عقدِ نشرٍ يمسُّ
`render.yaml` و`docs/render-deployment-vars.md`، وهو بندٌ مستقلٌّ لم تأمر به هذه
الوحدةُ ولم يُنفَّذ فيها. وقد كُتِب سببُ بقائها تعليقاً في `packages/tracking/config.ts`
حتى لا يُقرأ سهواً.

**ولم يُنشَأ بديلٌ لـ`TrackingService`** — لا صنفٌ ولا وحدةٌ ولا واجهةٌ. المسارُ الحيُّ
كان قائماً قبلَ هذه الوحدةِ ولم يُعَد كتابةُ سطرٍ منه.

---

## ٦) الاختباراتُ: ما سقط، وما حُفِظ، ولماذا لم تضِع قيمةٌ

الجردُ الحقيقيُّ **36 اختباراً في ثلاثةِ ملفّاتٍ، لا «~٤٠»** كما قدّر نصُّ البندِ.
المحذوفُ منها **26 اختباراً** تختبر الشيفرةَ المحذوفةَ نفسَها: 10 في `tracking: service`
و16 في `tracking-route`. **فسقوطُها ليس فقدَ تغطيةٍ**: لم يكن أيٌّ منها يحرس سلوكاً
يصل إليه مستخدمٌ.

**والاختباران الوحيدان اللذان كانا يحملان قيمةً في الملفِّ المحذوفِ هما اختبارا
`haversine`، وهما مكرَّران حرفاً في `tests/unit/geo.test.ts:53-73`**: نقطتانِ
متطابقتانِ ⇒ صفرٌ · جدّة ← مكّة بين 65 و73 كم · جدّة ← الرياض · التماثلُ. فلم يُنقَل
شيءٌ لأنّه موجودٌ أصلاً، **ولم تُحذف قيمةٌ بلا بديلٍ**.

والتغطيةُ القيميّةُ الباقيةُ للتتبّعِ:

| الملف | الاختبارات |
|---|---|
| `tests/unit/gps-fix.test.ts` | 22 |
| `tests/unit/tracking-session.test.ts` | 15 |
| `tests/unit/tracking-realtime.test.ts` | 33 |
| `tests/unit/tracking-config-resolution.test.ts` | سياسةُ GPS و5 اختباراتِ الانتقالِ الآنيِّ |
| `tests/unit/geo.test.ts` | `haversine` |

و`tests/unit/tracking-config-resolution.test.ts` عُدِّل ولم يُحذف: أُسقِطت منه
استيراداتُ `resolveTrackingConfig` و`DEFAULT_TRACKING_CONFIG` المحذوفَين، **وبقيَ فيه
كلُّ ما يختبر `resolveGpsPolicy` الحيَّ**، وكُتِب موضعُ الاختبارِ الساقطِ تعليقاً
يشرح سببَه بدلَ أن يُمحى بلا أثرٍ.

**فرقُ العدّ يُطابِق الحسابَ بالضبط:** 2100 → **2071** ناجحاً، أي **‎−29** =
26 اختبارَ خدمةٍ ومسارٍ + 2 `haversine` مكرَّرَين + 1 توكيدُ ضبطٍ محذوفٌ. **و0 فاشلاً**
و189 ملفّاً بدل 191.

---

## ٧) البوابات — قِيسَت برمزِ الخروجِ لا بقراءةِ نصٍّ

محليّاً على `bun 1.4.0`:

| البوابة | النتيجة |
|---|---|
| `bun run lint` | **خروج 0** |
| `bun run typecheck` | **خروج 0** |
| `bun test` | **2071 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6564 توكيداً · 189 ملفّاً** |
| `bun test` مُعاداً مرّتَين | **2071 / 0 فاشلاً** في كلتَيهما — لا لاحتميّةَ |
| `bun run check:coverage` | **خروج 0** — المساراتُ التسعةُ فوقَ أرضيّتِها، ومسارُ «التتبّعُ وموقعُ السائق» **47.24% · 15/16 ملفّاً مقيساً** |
| حواجزُ `bun run ci` العشرةُ الأولى | **كلُّها خضراءُ**، ومنها `check-env-drift` و`check-adr-numbering` و`check-secret-logging` و`check-session-storage-policy` و`check-instance-invariant` و`check-migrations` |

**وحدُّ الدليلِ المحلّيِّ مُعلَنٌ:** `bash scripts/verify-container-build.sh` سقط محليّاً
لأنّ `bun` في بيئةِ التنفيذِ **1.4.0** والصورةُ مثبَّتةٌ على **1.3.14** — **وهو قيدُ
بيئةٍ سابقٌ لهذه الوحدةِ لا أثرٌ لها**، وقد مرّ في CI حيثُ الإصدارُ مطابقٌ.

**والحكمُ النهائيُّ من CI لا من المحلّيِّ:** التشغيلُ `33572493851` على
`66a5ed0f29` انتهى **`success` في الوظائفِ الثلاثِ**: `verify` · «تكامل على
PostgreSQL حقيقي» · «تكامل على Redis حقيقي». وقُرِئ `conclusion` لكلِّ وظيفةٍ من
الواجهةِ البرمجيةِ لا من نصِّ السجلِّ.

---

## ٨) ما الذي **لم** يُنفَّذ في هذه الوحدةِ

هذا القسمُ يُقرأ حرفيّاً ولا يُلوَّن:

- **لا `version` ولا `sequence` ولا `eventId`.** `BUG-009` **مفتوحٌ بحرفِه**، ولم
  يقترب منه هذا الحذفُ: لا ترقيمَ رتيبٌ ولا رفضٌ صريحٌ للأقدمِ ولا اختبارُ خمسِ
  نبضاتٍ مختلطةٍ.
- **لا إعادةَ تشغيلٍ (replay) ولا تاريخَ مواقعَ.** `R-9` على حالِه.
- **لا Socket.IO إنتاجيٌّ** ولا مُحوِّلٌ ولا Pub/Sub ولا Redis Streams.
- **لا توزيعَ بين النسخِ (multi-instance distribution).** `R-17` **مفتوحٌ بحرفِه**
  ولم يُقلَب، والناقلُ داخلَ العمليةِ آليةُ التوزيعِ المقرَّرةُ الوحيدةُ
  (`ADR 0050` · `ADR 0051`).
- **لا realtime إنتاجيٌّ صار جاهزاً ولا مُثبَتاً.** `R-19` على حالِه.
- **لا سعةَ قِيسَت ولا زمنَ استجابةٍ ولا حِملَ** — فلا **مَقيس** ولا **مُثبَت** (`ح-5`).
- **لا تقليمَ لأنواعِ الحدثِ** رغمَ أنّ ثلاثةً منها وحدَها تُنشَر في الإنتاجِ
  (`session_started` · `session_ended` · `location_updated`) وأنّ `teleport_detected`
  و`speeding_detected` فقدا ناشرَهما الوحيدَ بهذا الحذفِ. **والإشارةُ لم تضِع**:
  هي في `location_updated.metadata.quality` و`.findings` من المسارِ الحيِّ،
  **ولم يكن لهما مستهلِكٌ أصلاً** (`event-bus.ts` لا يفرّق بالنوعِ،
  و`customer-live-relay.ts:129,134` يقرأ `session_ended` و`location_updated` وحدَهما).
- **لا مساسَ بـ`ADR 0015` ولا `ADR 0042` ولا `ADR 0052`** — والقرارُ المنشورُ لا يُحرَّر.
- **لا حذفَ لمتغيّراتِ البيئةِ الثلاثةِ** (§٥) — دَينٌ مُعلَنٌ ببندٍ مستقلٍّ.

---

## ٩) الحُكم

`R-16` **مغلقٌ بالحذفِ لا بالتركيبِ**، وسندُه `ADR 0052`، ودليلُه هذا الملفُّ،
وشاهدُه `commit 66a5ed0f29` وتشغيلُ CI `33572493851` بنتيجةِ `success` في الوظائفِ
الثلاثِ. **والهدفُ المعماريُّ متحقّقٌ**: مسارُ تقييمٍ واحدٌ · مصدرٌ واحدٌ للإصلاحةِ
السابقةِ هو القاعدةُ · ناشرٌ واحدٌ · لا مصدرَ حقيقةٍ ثانٍ للتتبّعِ · لا رمزَ تتبّعٍ
قديمٍ غيرَ مستعمَلٍ · لا مسارَ تتبّعٍ غيرَ مركَّبٍ · لا `dead exports`.
