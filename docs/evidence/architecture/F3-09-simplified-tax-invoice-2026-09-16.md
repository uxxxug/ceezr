# دليلُ التنفيذِ — `F3-09` · `SD-08`: الفاتورةُ الضريبيّةُ المبسَّطةُ لاشتراكِ السائقِ

**الغرض:** أن يُقرأَ في موضعٍ واحدٍ **ما بُنيَ فعلاً** في هذه الدفعةِ، و**ما قِيسَ
به**، و**ما لم يُبنَ ولا يُدَّعى**. ولا يُقرأُ هذا الملفُّ إثباتاً إنتاجيّاً: حكمُ
CI هوَ الحاكمُ (`ح-6`)، وموضعُه القسمُ ٧ ههنا.

**الحالة:** الدفعةُ الأولى (الخادمُ والقاعدةُ) مبنيّةٌ ومقيسةٌ محلّيّاً · بندُ
الخارطةِ يبقى `[ ]` حتّى ثلاثِ جولاتِ CI خضراءَ تُقرأُ بالوظيفةِ (`ح-4`).

**ينتمي إلى:** `F3-09` · `SD-08` · `ADR 0127`.

**يُستخدم من:** سجلُّ التنفيذِ في `docs/ROADMAP-MASTER.md` §٢٥ · `docs/SYSTEM_STATE.md`.

**يحرسُه:** `scripts/check-tax-invoice-contract.ts` (ثمانِ قواعدَ) ·
`scripts/check-rollback-safety.ts` · `scripts/check-skip-classification.ts` ·
`scripts/check-retention-policy.ts` · `scripts/check-schema-contract.ts`.

**الحاكم:** `docs/MASTER_DIRECTIVE.md` (٠٫٣ · ٠٫٤ · ٠٫٥ · ٠٫٦ · ٠٫٧) · قواعدُ `ح`.

## ١) ما بُنيَ — الطبقاتُ ومواضعُها

| الطبقةُ | الملفُّ | ما فيه |
| --- | --- | --- |
| القاعدةُ | `supabase/migrations/20260916050000_f3_09_subscription_tax_invoice.sql` | سبعةُ أعمدةٍ ضريبيّةٍ على `subscription_invoices`، وقيدُ «الكلُّ أو لا شيءَ»، وزنادَا ثباتٍ، ودالّةُ حمولةٍ، ودالّتا `TLV`، وكاتبٌ وحيدٌ مُعادُ تعريفُه، ومُغلِّفُ مِلكيّةٍ، وقارئٌ، وحالُ دفعةٍ |
| المجالُ | `packages/domain/driver/subscription-invoice.ts` | شكلُ الفاتورةِ ورموزُ رفضِها — بلا نداءِ قاعدةٍ |
| التطبيقُ | `packages/application/driver/subscription-invoice.ts` · `subscription-invoice-ports.ts` | ترجمةُ **تسعِ** رفوضِ مخزنٍ إلى **اثنَي عشرَ** رمزاً عامّاً |
| البنيةُ | `packages/infrastructure/driver/subscription-invoice-store.ts` | `PostgresSubscriptionTaxInvoiceStore`: تحقُّقُ حقولٍ، وتصديقُ `excl + vat = incl`، واستثناءٌ ⇒ `STORE_ERROR` |
| الحدُّ | `apps/gateway/src/routes/driver-subscription-invoice.ts` | `POST` يُصدِرُ · `GET` يقرأُ · `GET` للحالِ — ولا كتابةَ في `GET` |
| الحاجزُ | `scripts/lib/tax-invoice-contract.ts` · `scripts/check-tax-invoice-contract.ts` | ثمانُ قواعدَ مقيسةٍ، ولكلِّ قاعدةٍ **حالةٌ سلبيّةٌ مبذورةٌ** (`ح-7`) |

## ٢) القرارُ الجوهريُّ — سجلُّ فواتيرَ **واحدٌ** لا اثنانِ

كُتِبَت الهجرةُ أوّلاً بجدولٍ جديدٍ `tax_invoices` ومُتوالٍ عامٍّ
(`tax_invoice_number_seq`) وأرقامٍ بصيغةِ `INV-00000001`. ثمَّ أسقطَ
`tests/unit/retention-policy.test.ts` البناءَ: **جدولٌ جديدٌ بلا تصنيفِ استبقاءٍ**.
ولم يُصنَّف الجدولُ لِيَخضَرَّ الحاجزُ، بل قُرِئَ الإسقاطُ على حقيقتِه — فوُجِدَ في
المستودعِ سجلُّ فواتيرِ اشتراكٍ **قائمٌ** منذُ
`20260813010000_subscription_financial_wallets.sql`: جدولُ `subscription_invoices`
بترقيمٍ لكلِّ مدينةٍ وسنةٍ، وكاتبٌ وحيدٌ `issue_subscription_invoice(uuid)` بقُفلٍ
استشاريٍّ، ومَنفذٌ يُناديه، وتصنيفُ استبقاءٍ ماليٍّ، وارتباطُ محوٍ في `SD-12`.

فحُذِفَ الجدولُ الثاني ومُتوالُه وزنادَاهُ ودالّةُ حمولتِه **قبلَ دفعِ أيِّ
التزامٍ**، وامتدَّ السجلُّ القائمُ، وصارَ الكاتبُ القائمُ هوَ الكاتبَ الذي يُفوتِرُ
ضريبيّاً، و`issue_subscription_tax_invoice(telegram_id, txn)` مُغلِّفَ مِلكيّةٍ
**يُفوِّضُ إليهِ** ولا يُدرِجُ صفّاً بيدِه. والتفصيلُ وأسبابُه في `ADR 0127` §٦.

## ٣) الحسابُ — ضريبةٌ مُستخرَجةٌ لا مضافةٌ

سعرُ الاشتراكِ في `platform_settings` **شاملٌ** للضريبةِ، فالمُستخرَجُ:

\[ \text{vat} = \operatorname{round}\!\left(\frac{\text{amount} \times \text{rate}_{bps}}{10000 + \text{rate}_{bps}}\right) \]

بدقّةِ `numeric` في المحرِّكِ لا بعائمٍ في الشِفرةِ، و`excl = amount - vat`، وقيدُ
الجدولِ يرفضُ صفّاً لا يجمعُ. ولا عمودَ ثالثاً للإجماليِّ شاملَ الضريبةِ: هوَ
`amount_minor` القائمُ، وعمودٌ ثانٍ بنفسِ المعنى مصدرٌ مُكرَّرٌ ينحرِفُ.

## ٤) الإغلاقُ عندَ غيابِ هُويّةِ البائعِ

`tax_seller_name` أو `tax_seller_vat_number` غائبٌ ⇒ الكاتبُ يرفعُ
`TAX_IDENTITY_NOT_CONFIGURED` **قبلَ** كتابةِ صفٍّ ⇒ الحدُّ يردُّ `503`. والهجرةُ
تبذُرُ `vat_rate_bps = 1500` لكلِّ مدينةٍ **ولا تبذُرُ الهُويّةَ**: رقمٌ ضريبيٌّ
مُختَرَعٌ في هجرةٍ يصيرُ رقماً حقيقيّاً على إقرارٍ حقيقيٍّ، والقاعدةُ السابعةُ من
الحاجزِ تمنعُه نصّاً. وأثرُ ذلكَ على النشرِ **مُعلَنٌ لا مضمرٌ**: مدخلٌ في
`scripts/lib/rollback-registry.ts` بـ`breaksPreviousRelease: true` وإجراءٌ مقروءٌ في
`docs/rollback.md` («عودةُ نشرٍ بعدَ إغلاقِ إصدارِ الفاتورةِ على هُويّةٍ ضريبيّةٍ»)،
لأنَّ صورةَ الشِفرةِ السابقةَ تُنادي هذا الكاتبَ في مسارِ تثبيتِ الدفعِ فينكسرُ
حيثُ لم تُضبَط الهُويّةُ.

## ٥) رمزُ الاستجابةِ — عرّافٌ لا يعرفُ كيفَ رُكِّبَ

`zatca_simplified_invoice_qr` تُركِّبُ خمسةَ حقولِ `TLV` (١ اسمُ البائعِ · ٢ رقمُه
الضريبيُّ · ٣ الوقتُ بـ`UTC` · ٤ الإجماليُّ شاملاً · ٥ الضريبةُ) بأطوالٍ **بالبايتاتِ**
لا بالمحارفِ. ويقرؤها `tests/support/zatca-tlv.ts`: **مُفكِّكٌ فقط بلا مُركِّبٍ** عن
قصدٍ — مُركِّبٌ في القياسِ يُصدِّقُ ما تصوَّرناهُ لا ما فعلَه `plpgsql`. وله عشرُ
حالاتٍ في `tests/unit/zatca-tlv-oracle.test.ts` تقيسُ المُفكِّكَ نفسَه على أطوالٍ
مكذوبةٍ ووسمٍ صفرٍ ونصٍّ ليسَ `base64`.

## ٦) القياسُ — ما قِيسَ وبأيِّ مُشغِّلٍ

| القياسُ | العددُ | الحكمُ |
| --- | --- | --- |
| `bun test tests/unit` (٢٦٠ ملفّاً) | 4385 | **0 fail** |
| منها حاجزُ عقدِ الفاتورةِ | ٢٦ حالةً | نجحَ — ولكلِّ قاعدةٍ سالبةٌ مبذورةٌ |
| منها مسارُ الحدِّ (`driver-subscription-invoice-routes`) | ٣٣ | نجحَ |
| منها عرّافُ `TLV` | ١٠ | نجحَ |
| `tests/integration/subscription-tax-invoice.test.ts` | ٣٤ | **مُتجاوَزٌ محلّيّاً** — لا PostgreSQL في صندوقِ التنفيذِ، والحكمُ لوظيفةِ «تكامل على PostgreSQL حقيقي» |
| `bunx tsc --noEmit` (الجذرُ) · `-p apps/miniapp` | — | أخضرانِ (نداءانِ منفصلانِ: النداءُ الجامعُ يتجاوزُ المهلةَ) |
| `biome check` · `build:miniapp` | — | أخضرانِ |
| الحواجزُ (٢٤ حاجزاً شُغِّلَت) | — | خُضرٌ، ومنها الاستبقاءُ والعودةُ وعقدُ المخطَّطِ وميزانُ التوثيقِ (384 وثيقةً · 0.422 والسقفُ 0.75) |

**ولا يُقرأُ الأخضرُ المحلّيُّ بديلاً عن حكمِ CI** (`ح-6`): الهجرةُ **لم تُطبَّقْ على
PostgreSQL حقيقيّةٍ بعدُ** ألبتّةَ، فكلُّ دعوى عن قيدٍ أو زنادٍ أو قُفلٍ ههنا
**دعوى نصٍّ لا دعوى محرِّكٍ** حتّى تُقرأَ الوظيفةُ.

## ٧) حكمُ CI — يُكتَبُ بعدَ القراءةِ لا قبلَها

| الجولةُ | المعرِّفُ | حكمُ كلِّ وظيفةٍ | التاريخُ |
| --- | --- | --- | --- |
| الأولى | `35074775194` | `verify`: **failure** · `تكامل على Redis حقيقي`: success · `فوضى متعدد المثيلات (F5-06)`: success · `تكامل على PostgreSQL حقيقي`: أُلغيَ بسقوطِ الجولةِ | 2026-09-16 |
| الثانيةُ | `35076192872` | `verify`: **success** · `تكامل على Redis حقيقي`: success · `فوضى متعدد المثيلات (F5-06)`: success · **`تكامل على PostgreSQL حقيقي`: failure** (٨ حالاتٍ من 1059) | 2026-09-16 |
| الثالثةُ | `35077331367` | **الأربعُ خُضرٌ**: `verify`: success · `تكامل على PostgreSQL حقيقي`: **success** · `تكامل على Redis حقيقي`: success · `فوضى متعدد المثيلات (F5-06)`: success | 2026-09-16 |

### ٧٫٣) أوّلُ حكمٍ أخضرَ كاملٍ — وما يعنيهِ وما لا يعنيهِ

الجولةُ `35077331367` أوّلُ جولةٍ تخضرُّ فيها **الوظائفُ الأربعُ** على هذا الفرعِ،
وفيها طُبِّقَت الهجرةُ على PostgreSQL 17 ونجحَت الحالاتُ الخمسُ والثلاثونَ كلُّها.

**وما لا يعنيهِ**: البندُ `F3-09` يبقى `[ ]`. القاعدةُ `ح-4` تطلبُ **ثلاثَ جولاتٍ
خضراءَ متتاليةً تُقرأُ بالوظيفةِ**، وهذه الأولى. وجولةٌ خضراءُ واحدةٌ تُجيزُ الدمجَ
(القاعدةُ ٠٫٧) ولا تُجيزُ دعوى الإنتاجِ. ولا مرحلةَ ثانيةً من الفوترةِ
الإلكترونيّةِ، ولا سطحَ فاتورةٍ في التطبيقِ المُصغَّرِ — تلكَ الدفعةُ الثانيةُ.

### ٧٫٢) أوّلُ تطبيقٍ للهجرةِ على محرِّكٍ حقيقيٍّ — وأربعةُ عيوبٍ لم يكن لِنصٍّ أن يراها

الجولةُ الثانيةُ **أوّلُ مرّةٍ** تُطبَّقُ فيها الهجرةُ على PostgreSQL 17 فعليّةٍ.
والنتيجةُ الصادقةُ: **عشرونَ حالةً من حالاتِ الإصدارِ والحسابِ والثباتِ والتفرُّدِ
نجحَت على المحرِّكِ** (١ … ٢٠)، وسقطَت ثمانٍ لأربعةِ أسبابٍ **كلُّها في شِفرَتِنا**
لا في البيئةِ:

1. **`base64` ملفوفٌ كلَّ ٧٦ محرفاً** (٥ حالاتٍ: ٢١–٢٥). `encode(…, 'base64')` في
   PostgreSQL يُدرِجُ `\n` كلَّ ٧٦ محرفاً — سلوكٌ موروثٌ من `MIME`. ورمزُ
   الاستجابةِ **حِمْلٌ واحدٌ لا رسالةُ بريدٍ**، فالعرّافُ الصارمُ ردَّهُ
   `NOT_BASE64` — وأصابَ. **ولم يُخفَّف العرّافُ ليمرَّ الحِمْلُ**: التخفيفُ كانَ
   سيُنتِجُ رمزاً لا يقرؤُه ماسحٌ حقيقيٌّ ويمرُّ الاختبارُ. بل صُحِّحَ **الكاتبُ**:
   `translate(encode(…), e'\n\r', '')`، وزيدَت حالةٌ «٢١أ» تقيسُ أنَّ المُخزَّنَ
   سطرٌ واحدٌ بلا فراغٍ، وحالةُ وحدةٍ في العرّافِ تُثبِتُ أنَّ الملفوفَ **يُرَدُّ**
   ولا يُصلَحُ صامتاً (`ح-7`).
2. **سطحُ صلاحيّاتٍ مفتوحٌ** (حالتانِ: «سطحُ الصلاحيّاتِ» الطبقةُ ٢، والفحصُ
   الهجوميُّ). `create function` يمنحُ `execute` لـ`public` تلقائيّاً، و`public`
   تشملُ `anon` و`authenticated` في Supabase: فأربعُ دوالَّ جديدةٍ
   (`zatca_tlv_field` · `zatca_simplified_invoice_qr` ·
   `subscription_tax_invoice_payload` · `subscription_invoices_are_immutable`)
   كانت **مكشوفةً للعالمِ** ولو لم تُذكَر في أيِّ مسارٍ. أُقفِلَت صراحةً بـ`revoke`.
   وقُفلُ دالّةِ الزنادِ لا يُعطِّلُ الزنادَ: الزنادُ يُنفَّذُ بحقِّ مالكِ الجدولِ.
3. **تلويثُ إعداداتٍ عبرَ الملفّاتِ** (حالةٌ واحدةٌ: تكافؤُ المدنِ). كانَ
   `tests/integration/financial-wallet.test.ts` يبذُرُ `tax_seller_name` و
   `tax_seller_vat_number` لمدينةِ تجربتِه **ولا ينزِعُهما**، فصارَت المدنُ
   الأربعُ الأخرى «ناقصةً» فسقطَ ملفٌّ آخرُ. والعلاجُ نزعٌ في `afterAll` لكلِّ
   مدينةٍ بُذِرَت — **لا استثناءُ المفتاحِ من حاجزِ التكافؤِ**: الحاجزُ صادقٌ
   والتلويثُ هوَ الخطأُ.
4. **العددُ المُثبَّتُ للتجاوزاتِ** رُفِعَ 34 ⇒ 35 و1179 ⇒ 1180 بالزيادةِ لا
   بالاستبدالِ، والأرقامُ السابقةُ مذكورةٌ نصّاً في `tests/unit/skip-audit.test.ts`.

**وهذا بعينِه ما تعنيه القاعدةُ `ح-6`**: الأخضرُ المحلّيُّ في الجولةِ الأولى كانَ
صادقاً في نطاقِه — 4385 اختبارَ وحدةٍ و٧٣ حاجزاً — **ولم يكن قادراً** على رؤيةِ لفِّ
`base64` ولا سطحِ `grant` ولا تلويثِ صفٍّ بينَ ملفَّينِ، إذ لا محرِّكَ في هذه
البيئةِ ألبتّةَ. فالمحرِّكُ هوَ الحاكمُ، والدعوى قبلَه نصٌّ.

### ٧٫١) ما أسقطَ الجولةَ الأولى — عيبانِ حقيقيّانِ لا ضجيجُ بيئةٍ (`ح-6` · `ح-8`)

**السطرُ أعلاه يبقى نصّاً كما قُرِئَ**؛ وهذا تصحيحٌ بالإضافةِ لا بالمحوِ.

1. **`check-lazy-query-assertion`** ردَّ ثلاثةَ خروقٍ في
   `tests/integration/subscription-tax-invoice.test.ts` (الأسطرُ ٥٧٦ و٥٧٧ و٥٨١):
   وسمُ قالبِ `sql` مُرَّرَ خامّاً إلى `expect(…)` — و`ADR 0122` يقولُ إنّ ذلكَ
   **يُعلِّقُ الملفَّ صامتاً** لا يُسقِطُه. وكانَ في الملفِّ خمسةُ مواضعَ أخرى
   بالصورةِ نفسِها معَ إسنادٍ وسيطٍ (`const attempt = sql\`…\``) **لا يراها الحاجزُ
   نصّاً وتحملُ الخطرَ عينَه**. فالعلاجُ لم يقتصِر على ما رآهُ الحاجزُ: أُدخِلَ
   `rejectionOf(run)` — `try`/`catch` يقيسُ نصَّ ردِّ القاعدةِ — وحُوِّلَت
   **الثمانيةُ جميعاً**، ثمَّ شُدَّت ثلاثُ دعاوى كانت `length > 0` إلى مطابقةِ اسمِ
   القيدِ (`subscription_invoices_tax_all_or_none` · مفتاحِ التوريدِ الفريدِ).
2. **`check-telegram-wrapper-isolation`** ردَّ خرقَينِ في
   `SubscriptionScreen.tsx`: استيرادٌ عميقٌ من `tg/app.ts` و`tg/outcome.ts` بدلَ
   مَنفذِ `tg/index.ts` الواحدِ. فوُحِّدَ الاستيرادُ في سطرٍ واحدٍ من المَنفذِ.
   ولم يكن الحاجزُ ليُقرأَ في الجولةِ الأولى ألبتّةَ: موضعُه في `ci.yml` سطرُ ٥٥٨،
   بعدَ سطرِ ٤٧٥ الذي أسقطَ الوظيفةَ — **فسقوطٌ واحدٌ يحجبُ ما بعدَه**.

**وسببُ الغفلةِ محليّاً مُسمّىً**: انتُقيَت الحواجزُ يدويّاً (أربعةٌ وعشرونَ من
خمسةٍ وسبعينَ)، والانتقاءُ اليدويُّ ليسَ قياساً. فصارَ الإجراءُ: تُستخرَجُ **كلُّ**
أسماءِ `scripts/check-*.ts` من `package.json` وتُشغَّلُ حلقةً قبلَ كلِّ دفعٍ.
والحكمُ بعدَ التصحيحِ: **ثلاثةٌ وسبعونَ حاجزاً خُضرٌ** (واثنانِ لا يُقاسانِ
محليّاً: `core-contract-freshness` يطلبُ مستودَعَ `CORE` الخاصَّ — `O-6` — و
`coverage-gate` يطلبُ مخرَجَ تغطيةٍ)، و`bun test tests/unit`: **4385 ناجحاً · 0 فاشلاً**.

## ٨) ما لم يُبنَ ولا يُدَّعى — (`ح-5`)

- **لا امتثالَ للمرحلةِ الثانيةِ**: لا رَبطَ بـ«فاتورة»، ولا إبلاغَ خلالَ أربعٍ
  وعشرينَ ساعةً، ولا `CSID`، ولا ختمَ تشفيرٍ، ولا `UBL 2.1`.
- **لا سطحَ فاتورةٍ في التطبيقِ المُصغَّرِ** في هذه الدفعةِ — الدفعةُ الثانيةُ.
- **لا إشعارَ دائنٍ ولا إلغاءَ فاتورةٍ ولا تصحيحَ صفٍّ صدرَ**.
- **لا فاتورةَ لمعاملةٍ غيرِ ناجحةٍ**، ولا فاتورةَ لصفٍّ سبقَ الهجرةَ: يُردُّ
  `INVOICE_WITHOUT_TAX_FIELDS` (`409`) ولا تُلفَّقُ حقولٌ.
- **لا فهرسَ زائداً** على قيودِ التفرُّدِ القائمةِ.
- **لا ترقيةَ بندٍ من `[~]` إلى `[x]`** في هذه الدفعةِ.

## ٩) المصادرُ

- [قرارُ تنفيذِ الفوترةِ الإلكترونيّةِ — هيئةُ الزكاةِ والضريبةِ والجمارك](https://zatca.gov.sa/en/E-Invoicing/Introduction/LawsAndRegulations/Documents/E-Invoicing%20Implementation%20Resolution_EN.pdf)
- [معيارُ تنفيذِ الفاتورةِ الإلكترونيّةِ `XML`](https://zatca.gov.sa/ar/E-Invoicing/SystemsDevelopers/Documents/20230519_ZATCA_Electronic_Invoice_XML_Implementation_Standard_%20vF.pdf)
- [الدليلُ المبسَّطُ للفوترةِ الإلكترونيّةِ (عربيٌّ)](https://zatca.gov.sa/ar/RulesRegulations/Taxes/Documents/E-Invoicing%20Simplified%20Guideline.pdf)
- [الدليلُ المبسَّطُ (إنجليزيٌّ)](https://zatca.gov.sa/en/E-Invoicing/Introduction/Guidelines/Documents/Simplified%20guide_En.pdf)
- [نسبُ ضريبةِ القيمةِ المضافةِ في السعوديّةِ](https://www.cleartax.com/sa/vat-rates-saudi-arabia)

## ١٠) الدفعةُ الثانيةُ — سطحُ الفاتورةِ في التطبيقِ المُصغَّرِ (2026-09-16)

الدفعةُ الأولى مدموجةٌ في `main` بـ`6ba8177`. وهذه الدفعةُ تُوصِلُ ما بُنيَ إلى
السائقِ نفسِه: يقرأُ حالَ دفعتِه، فإن أمكنَ الإصدارُ أصدرَ، ثمَّ يقرأُ وثيقتَه
كاملةً بحِمْلِ رمزِ استجابتِها نصّاً يُنتقى بلمسةٍ.

### ١٠٫١) العطبُ الذي وقعَ فعلاً — وما فُعِلَ به

كُتِبَ اللوحُ أوّلاً يُظهِرُ زرَّ الإصدارِ إذا `status === "paid"`. و`paid` **حالٌ
لا وجودَ له** في `payment_transactions_status_check` (`active` · `pending` ·
`past_due` · `failed` · `canceled` · `expired` · `refunded`). **فالنتيجةُ زرٌّ لا
يظهرُ أبداً**، ولا اختبارُ وحدةٍ يكشفُه: المزدوجُ يبذُرُ ما تكتبُه اليدُ التي
أخطأَت. ولم يكن العطبُ حرفاً بل **مصدرَ حقيقةٍ مُكرَّراً**: شرطُ «أهذه الدفعةُ
مُسدَّدةٌ» في القاعدةِ ومُعاداً في العميلِ (القاعدةُ ٠٫٦).

فالعلاجُ في الجذرِ (`ADR 0127` §٨):

| ما فُعِلَ | أينَ | ولِمَ |
|---|---|---|
| محدِّدٌ واحدٌ `subscription_payment_is_settled(text)` `immutable` مقفلٌ عن `public`/`anon`/`authenticated` | `supabase/migrations/20260916120000_f3_09_settled_predicate_and_issuable_flag.sql` | يُنادِيه الكاتبُ **وقارئُ الحالِ**، فلا رايةٌ تُظهِرُ زرّاً يرفضُه الكاتبُ |
| `invoice_issuable` في جوابِ `driver_subscription_payment_status` | الهجرةُ عينُها بـ`create or replace` وجسمٍ منسوخٍ حرفاً بحرفٍ | الجوابُ يحملُ **الحكمَ** لا مادّتَه؛ واسمُ الحالِ للعرضِ لا للحكمِ |
| رفضُ حمولةٍ بلا رايةٍ بوليانيّةٍ (`MALFORMED_RESULT` ⇒ `503`) | `packages/infrastructure/driver/subscription-invoice-store.ts` | نشرُ شيفرةٍ قبلَ هجرةٍ يُقرأُ **عطباً صريحاً** لا لوحاً بلا زرٍّ |
| القاعدةُ ٩ في الحاجزِ: اسمُ حالٍ مقروناً بحقلِ `status` في مِلفِّ سطحٍ ⇒ سقوطُ البناءِ | `scripts/lib/tax-invoice-contract.ts` | يمنعُ **عودةَ** العطبِ على كلِّ سطرٍ يُكتَبُ لاحقاً، و`paid` مُدرَجٌ فيها عن قصدٍ |

### ١٠٫٢) ما بُنيَ

- **عقدُ الجوابِ** `invoice-contract.ts`: أنواعُ الجوابِ الثلاثِ حرفاً بحرفٍ كما
  يُصدِرُها المسلكُ — بلا حقلٍ مُخترَعٍ وبلا `any`.
- **مُنادي المسلكِ** `invoice-api.ts`: `readDriverPaymentStatus` ·
  `issueDriverTaxInvoice` · `readDriverTaxInvoice`، وباني مسارٍ واحدٌ. **ولا
  إعادةَ محاولةٍ تلقائيّةً للكاتبِ** ولا استقصاءَ دوريَّ: الإصدارُ فعلٌ يُنشئُ
  وثيقةً نظاميّةً، وإعادتُه بلا طلبِ السائقِ اختراعُ نيّةٍ.
- **نموذجُ العرضِ** `invoice-view.ts`: مفتاحُ نصٍّ لكلِّ رمزٍ عامٍّ (١٢) و`UNKNOWN`
  لِما لا يُعرَفُ، وتصنيفُ ما يُعادُ فيه بإصدارٍ وما يُعادُ فيه **بقراءةِ حالٍ**،
  و`vatRatePercentText` **تحويلُ وحدةٍ لا حساباً**، ونقلُ الوثيقةِ بلا جمعٍ.
- **اللوحُ** `PaymentInvoicePanel.tsx`: اتّحادٌ موسومٌ بسبعةِ أحوالٍ — فلا
  «يُحمَّلُ» و«سقطَ» معاً — وزرٌّ واحدٌ ظاهرٌ في كلِّ حالٍ، ووثيقةٌ في `dl`.
- **موضِعُه** في `SubscriptionScreen.tsx`: بعدَ تجديدٍ ناجحٍ، وتحتَ كلِّ دفعةٍ في
  السجلِّ بلوحٍ **واحدٍ مفتوحٍ** لا أكثرَ.
- **النصوصُ**: ١١٧٦ مفتاحاً في كلٍّ من `ar` و`en` و`ur` (كانت ١١٣٥)، والقاعدةُ ١١
  في الحاجزِ تُسقِطُ البناءَ إن نقصَ رمزٌ في لغةٍ.
- **ورقةُ النمطِ**: كتلةُ `dinv` مُعلَنةً في `DECLARED_BLOCKS` وستٌّ وعشرونَ قاعدةً
  لكلِّ صنفٍ يُصدِرُه اللوحُ — وحاجزُ `check-css-class-coverage` كانَ قد **ردَّ**
  اللوحَ بلا نمطٍ، وهوَ عطبٌ لا يراهُ إلّا المستخدمُ (`UX-021`).

### ١٠٫٣) القياسُ — أرقامٌ مقروءةٌ لا موعودةٌ

| ما قِيسَ | الحكمُ |
|---|---|
| `bun test tests/unit` | **4415 ناجحاً · 0 فاشلاً** (كانَ 4404 قبلَ هذه الدفعةِ) |
| حواجزُ `package.json` **بالحلقةِ** | **73 أخضرَ**؛ واثنانِ لا يُقاسانِ محليّاً: `core-contract-freshness` (`O-6`) و`coverage-gate` |
| `tsc` (الجذرُ · التطبيقُ المُصغَّرُ) | نداءانِ منفصلانِ — نظيفانِ |
| `biome` · `build:miniapp` | 29 تحذيراً · 0 خطأٍ · بناءٌ ناجحٌ |
| حالاتُ التكاملِ الجديدةُ | **٦** (٣١–٣٦) — ٤١ في المِلفِّ، مُصنَّفةً في `skip-registry` (١١٨٦ مجموعاً) |

والقياساتُ الجديدةُ في الوحدةِ: سالبةٌ مبذورةٌ لكلٍّ من القواعدِ ٩–١٢ **وسالبةُ
إنذارٍ كاذبٍ** للقاعدةِ ٩ (اسمُ حالِ لوحٍ لا يُقارِنُ حقلاً لا يُلتقَطُ)، ونموذجُ
العرضِ حقلاً حقلاً، و**قارئُ الحمولةِ حينَ تصلُه قاعدةٌ قبلَ الهجرةِ** — وهيَ حالٌ
لا تُزرَعُ في وظيفةِ التكاملِ لأنَّ هجراتَها كلَّها مطبَّقةٌ.

### ١٠٫٤) ما لم يُبنَ في هذه الدفعةِ ولا يُدَّعى — (`ح-5`)

- **لا رسمَ مقيساً للوحِ**: في المستودعِ نسختانِ من `react` (`19.2.6` في الجذرِ و
  `19.2.8` في التطبيقِ المُصغَّرِ)، ورسمٌ يخلطُهما يسقطُ **بعطبِ مُوزِّعٍ** لا بعطبِ
  سطحٍ. فما قِيسَ نموذجُ العرضِ ورايتُه وحاجزُه الساكنُ. وتوحيدُ النسختَينِ
  وبناءُ مِرسَمٍ **عملٌ لم يُنفَّذ**.
- **لا صورةَ لرمزِ الاستجابةِ**: الحِمْلُ يُعرَضُ نصّاً `base64` يُنتقى ويُنقَلُ؛
  ورسمُ مُربَّعِ الرمزِ يحتاجُ مكتبةً لم تُضَفْ.
- **لا حفظَ للوثيقةِ ملفّاً** ولا مشاركةَ ولا طباعةَ.
- **لا امتثالَ للمرحلةِ الثانيةِ** (ما زالَ كما في القسمِ ٨).
- **لا ترقيةَ بندٍ إلى `[x]`**: حكمُ CI بعدَ الدفعِ يُقرأُ بالوظيفةِ (`ح-4`).

### ١٠٫٥) تصحيحٌ بحكمِ CI للدفعةِ الثانيةِ — الجولةُ `35081765124` (`ح-8`)

قُرِئَ الحكمُ **بالوظيفةِ**: `verify` و«تكامل على Redis حقيقي» و«فوضى متعدد
المثيلات» **خُضرٌ**، و«تكامل على PostgreSQL حقيقي» **أحمرُ باثنتَينِ من ستٍّ**
(٣٣ و٣٤)، **وأربعٌ من الستِّ نجحَت على المحرِّكِ**: انقلابُ الرايةِ بالإصدارِ (٣١)،
و`active`/`refunded` كلاهما مُسدَّدٌ (٣٢)، وقُفلُ المحدِّدِ عن `anon` و`authenticated`
و`public` (٣٥)، و**أنَّ `paid` يرفضُه `payment_transactions_status_check` أصلاً**
والمحدِّدُ يُكذِّبُه (٣٦) — وهيَ الفرضيّةُ التي وُجِدَ الحاجزُ لأجلِها.

**والعطبُ في القياسِ لا في المنطقِ**: قِيسَ رفضُ الكاتبِ بـ`rejectionOf` وبـ`try/catch`
افتراضاً أنَّه **يرفعُ استثناءً**، وعقدُه — كما تقرؤُه الحالةُ ٤ المارّةُ منذُ الدفعةِ
الأولى — **فشلٌ مغلقٌ مُسمّىً يُرَدُّ في الجوابِ**: `{ ok: false, error: "TRANSACTION_NOT_PAID" }`.
فكانَ القياسانِ **لا يقيسانِ شيئاً**: أحدُهما يطلبُ استثناءً لا يأتي، والآخرُ يعدُّ كلَّ
جوابٍ قَبولاً.

**ولم يُخفَّف القياسُ بل قوِيَ**: يُطابَقُ الجوابُ **كاملاً** (أقوى من مُطابقةِ نصِّ
استثناءٍ بنمطٍ)، ويُقرأُ `ok` من الجوابِ في قياسِ التطابقِ، **وزيدَ `past_due`** إلى
القائمتَينِ فصارَت السبعةُ أحوالِ القيدِ كلُّها مقيسةً في الحالةِ ٣٤. ولم تُمَسَّ
هجرةٌ ولا دالّةٌ ولا حاجزٌ: **حكمُ المحرِّكِ صدَّقَ المنطقَ وردَّ فرضيّةً في اختبارٍ**.
والعددُ يبقى ٤١ متجاوَزاً محليّاً (١١٨٦ مجموعاً) لأنَّ الحالتَينِ لم تُزادا بل صُحِّحَتا.
