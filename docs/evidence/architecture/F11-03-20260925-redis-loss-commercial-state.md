# F11-03 (الشقُّ المملوكُ للمستودَعِ) — فقدانُ Redis كلِّه وسطَ الدورةِ التجاريّةِ

**التاريخ**: 2026-09-25 · **الفرعُ**: `feat/f11-03-redis-loss-commercial-state` من `main`@`5583475` · **القرارُ**: `ADR 0193`

## المقيسُ قبلَ التغييرِ

- `tests/integration/redis-sessions.test.ts`: انقطاعٌ على **مزدوجٍ** أثناءَ حوارِ **التسجيلِ** (لا صفَّ ناقصاً).
- `tests/real-redis/location-hot-state-outage-real.test.ts` (`F4-08`): **المخزنُ الساخنُ وحدَه** على خادمٍ حقيقيٍّ.
- لا قطعَ لـRedis كلِّه وسطَ إنشاءٍ ← قبولٍ متسابقٍ ← بدءٍ ← إقفالٍ، ولا حَكَمَ على ثوابتِ القاعدةِ بعدَه.

## القياسُ المحلّيُّ (PostgreSQL 18 + PostGIS · Redis 7 على الحلقةِ المحلّيّةِ عبرَ محوِّلِ REST نفسِه في العمليّةِ)

| الطورُ | أوامرُ مقطوعةٌ | الأثرُ في القاعدةِ |
|---|---|---|
| قطعٌ قبلَ الإنشاءِ (نقطتا الرحلةِ) | 4 | 0 طلبٍ — لا يتيمَ |
| عودةٌ وإنشاءٌ | 0 | طلبٌ واحدٌ · عرضانِ `pending` |
| قطعٌ: قبولانِ معاً + إعادتاهما + بدءٌ + إقفالٌ ×2 | 8 | `completed` · `accepted` واحدٌ للمُسنَدِ · الآخرُ `cancelled` |
| عودةٌ وتسليمٌ متقادمٌ ×4 | 0 | لا تغييرَ |

الحَكَمُ: `{"ok":true,"violations":[]}` في ثلاثِ جرياتٍ متتاليةٍ؛ كلُّ ويبهوكٍ (15) 200؛ مارٌّ 67 أمراً؛ إخطارُ القبولِ 1 والإقفالِ 1؛
الجداولُ الماليّةُ السبعةُ بلا فرقٍ؛ لا مفتاحَ باقٍ بعدَ التفكيكِ. السجلُّ: `session.redis_failed` · `language.hydrate_session_unreadable`.

## اكتشافٌ — `D-36`

ردُّ الراكبِ على نقطتَيه أثناءَ القطعِ: «🤔 لم أفهم هذه الرسالة. أرسل /help …» مرّتين — `loadState` تقرأُ الإخفاقَ غياباً.
حُجِزَ `D-36` قبلَ أيِّ تعديلٍ عليه.

## ما لا يُدَّعى

لا حِملَ ولا بيئةَ شبيهةً بالإنتاجِ (`ح-5`)؛ `F11-03` يبقى `[ ]`. حكمُ CI يُضافُ في حجزِ `ROADMAP.md`.
