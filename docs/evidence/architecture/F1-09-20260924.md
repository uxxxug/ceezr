# دليلُ `D-25` و`F1-09` (الصفّانِ 3 و4) — 2026-09-24

القرارُ: `docs/adr/0183-the-mini-app-is-opened-by-a-real-browser-in-ci.md`. والقرارُ المُعلَّقُ: `DEC-19`.

## ١. خطُّ الأساسِ قبلَ أيِّ تعديلٍ

- `main`@`571c891`، وCI أخضرُ على الوظائفِ الأربعِ (`35938249171`)، ولا طلبَ دمجٍ مفتوحاً.
- `vite build` لـ`apps/miniapp` نجحَ؛ والحملُ الأوّلُ بعدَ الضغطِ: `shell` 94.61 KB · `vendor-react`
  57.18 KB · `identity` 1.26 KB · المدخلُ 0.71 KB (≈ 154 KB).
- **فتحُ المُخرَجِ بمتصفّحٍ بلا رأسٍ** (Chromium headless shell 1217 · CDP): `#root` فارغٌ،
  و`Log.entryAdded` ثلاثَ مرّاتٍ «404» على `/vendor-react-*.js` و`/identity-*.js` و`/shell-*.js`.
  والمستندُ يحملُ `from"./shell-DmHxhh6f.js"` في `<script type="module">` مُدمَجٍ، والملفُّ في
  `/assets/`. و`dist/assets/index-Bw_Wn9nk.js` باقٍ رغمَ أنَّ الإضافةَ «تحذفُه».

## ٢. الإصلاحُ

`apps/miniapp/vite/inline-entry-script.ts`: `absolutizeEntrySpecifiers` + الحذفُ بالمفتاحِ الكاملِ.
وبعدَه: `from"/assets/shell-DmHxhh6f.js"` · لا `index-*.js` في `dist/assets` · لا تعليقَ خريطةٍ
في المستندِ · والتطبيقُ يرسمُ «افتح وَصْلة من تيليجرام».

## ٣. برهانُ السقوطِ على العطبِ الحقيقيِّ (`ح-7`)

أُعيدَت الإضافةُ إلى نسخةِ `main` وبُنيَ ثمَّ شُغِّلَ `scripts/measure-first-paint.ts`:

```
بلا تقييدٍ: FCP=— ms · عُقَدُ #root=0
✗ [NO_PAINT] بلا تقييدٍ: لم يُرسَمْ شيءٌ ألبتّةَ
✗ [EMPTY_ROOT] بلا تقييدٍ: `#root` فارغٌ — شاشةٌ بيضاءُ
✗ [MODULE_NOT_SCRIPT] … text/html http://127.0.0.1:…/shell-DmHxhh6f.js
✗ [MODULE_NOT_SCRIPT] … text/html http://127.0.0.1:…/vendor-react-CXezKH2q.js
```

خروجٌ `1`. ثمَّ أُعيدَ الإصلاحُ.

## ٤. القياسُ بعدَ الإصلاحِ (محلّيّاً)

| الملفُّ | FCP (ثلاثةُ تشغيلاتٍ) | الوسيطُ | الحدُّ |
|---|---|---|---|
| بلا تقييدٍ | 104 ms | — | — |
| Chromium «Slow 3G» (2000 ms · 50000 B/s · CPU ×4) | 7320 · 7304 · 7332 ms | **7320 ms** | 1800 |
| WebPageTest «3G Slow» (400 ms · 50000 B/s · CPU ×4) — للمقارنةِ | 4064 · 4072 · 4048 ms | 4064 ms | 1800 |

و`LCP` = `FCP` في كلِّ تشغيلٍ (الشاشةُ عنصرٌ نصّيٌّ واحدٌ). وسكربتُ `telegram.org` محجوبٌ؛ ومعَه
(استكشافٌ قبلَ الحجبِ) كانَ `FCP` ≈ 9980 ms على «Slow 3G» و≈ 5160 ms على «3G Slow».

## ٥. الاختباراتُ

- `tests/unit/first-paint-budget.test.ts` — 21 حالةً: كلُّ قاعدةٍ من العشرِ بسالبٍ مزروعٍ.
- `tests/unit/inline-entry-script.test.ts` — 8 حالاتٍ: الإطلاقُ بالأشكالِ الثلاثةِ و`base` وحذفُ
  الخريطةِ والرميُ على مُعيِّنٍ مجهولٍ وصاعدٍ.

## ٦. حكمُ CI

يُضافُ في `docs/SYSTEM_STATE.md` بعدَ الدفعِ مقروءاً بالوظيفةِ (`ح-8`).

## ٧. ما لا يُدَّعى

الصفُّ 5 غيرُ مقيسٍ · الأرقامُ حدٌّ أدنى (تيليجرام محجوبٌ) · لا جوّالَ · لا `brotli` · الصفُّ 8 خارجُ
النطاقِ · `F1-09` يبقى `[~]` · `DEC-19` `[!]`.
