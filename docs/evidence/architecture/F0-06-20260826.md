# دليل البند F0-06 — إنشاء بنية مجلدات الأدلة

| الحقل | القيمة |
|---|---|
| معرّف البند | `F0-06` |
| المرحلة | F0 — تثبيت الهدف والقرار |
| التاريخ | 2026-08-26 |
| المنفّذ | وكيل Perplexity Computer، بأمر مدير المشروع |
| درجة الدليل | **مُنفَّذ** (بنية ملفات لا سلوك نظام — لا تحتاج قياساً) |

## 1. الـcommit

`c49aa4c0353e446a8236f4b5eda7e02d7d7d5b9b` هو الـcommit الذي أُدخلت فيه الخارطة الحاكمة.
هذا الدليل رُفع في الـcommit `63af23dcc79a8a5bdce4a757d3a27376962ba03f` على نفس الفرع `main`.

## 2. بصمة الإعدادات

لا تنطبق — لا كود ولا زمن تشغيل ولا متغيّرات بيئة في هذا البند.
الطريقة: GitHub Contents/Git Trees API على مستودع `noor-seez/ceezr`، فرع `main`.

## 3. الـtopology

لا تنطبق.

## 4. نسخة الحِمل

لا تنطبق.

## 5. النتيجة

أُنشئت تسعة مجلدات تحت `docs/evidence/`:

```text
architecture/  correctness/  distributed/  security/   capacity/
chaos/         recovery/     release/      compliance/
```

وأُضيف:

- `docs/evidence/README.md` — يحدّد نمط التسمية، وما يوضع في كل تصنيف، والمحتوى الإلزامي الثماني لكل ملف دليل.
- `.gitkeep.md` في كل مجلد لضمان بقائه في Git قبل رفع أول دليل حقيقي فيه.

## 6. الأخطاء

لا أخطاء.
ملاحظة تشغيلية: الدفع عبر مكتبة `requests` فشل بخطأ تحقّق شهادة الوسيط؛ نُفِّذ عبر `curl` بنجاح. لا أثر لذلك على المحتوى — جرى تنزيل الملف بعد الدفع وتطابق بايت ببايت مع النسخة المحلية.

## 7. الحدود المعروفة

1. البنية جاهزة، **وهي فارغة من الأدلة الفعلية** — إنشاء المجلد لا يعني وجود دليل واحد على أي بند آخر.
2. 53 ملف دليل سابق يبقى مسطّحاً في جذر `docs/evidence/` ولم يُصنَّف ولم يُنقَل، منعاً لكسر أي إشارة إليه في تقارير أو PRs سابقة.
3. لا فحص آلي بعد يمنع وضع `[x]` في الخارطة بلا ملف دليل مقابل. أُوصي ببند لاحق: فحص CI يقارن الرموز `[x]` في `ROADMAP-MASTER.md` بوجود ملف مطابق هنا وسطر في القسم 25.

## 8. الحُكم النهائي

**نجاح.** البند `F0-06` مُستوفى كما هو منصوص عليه: `إنشاء بنية docs/evidence/{architecture,security,correctness,distributed,capacity,chaos,recovery,release,compliance}/`.
لا يُستنتج من هذا إنجاز أي بند آخر في المرحلة F0.
