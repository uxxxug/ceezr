# `F2-09` · `SR-13` — رابطُ مشاركةٍ مؤقّتٌ: دليلُ التنفيذِ (2026-09-14)

* **الغرض**: تسجيلُ ما نُفِّذَ في البندِ `F2-09` بالملفِّ والسطرِ والقياسِ، وحدُّ
  ما يُدَّعى، والحاجزُ الذي يمنعُ الإغلاقَ.
* **الحالة**: مُنفَّذٌ · مُختبَرٌ محلّيّاً وحدَه · **حكمُ CI غيرُ مقروءٍ
  (`B-CI-001`)** · البندُ **مفتوحٌ** `[ ]`.
* **ينتمي إلى**: `docs/ROADMAP-MASTER.md` §9.5 (`SR-13`) · `ADR 0109`.
* **يُستخدم من**: مراجعُ طلبِ الدمجِ · منفِّذُ `F2-10` (يقرأُ سطحَ الرحلةِ
  النشطةِ) · مَن يقرأُ حالةَ بوّابةِ `F2`.
* **يُتوقَّع أن يستخدمه لاحقاً**: مَن يرفعُ البندَ إلى `[~]` أو `[x]` بعدَ قراءةِ
  حكمِ CI (`ح-4`).

---

## لماذا هذا الملفُّ

لأنَّ البندَ **لا يُغلَقُ** اليومَ، ومع ذلكَ نُفِّذَ كاملاً. فبينَ «مكتوبٌ» و
«مُغلَقٌ» مسافةٌ يجبُ أن تكونَ **مقروءةً بالملفِّ والرقمِ** لا متروكةً لذاكرةِ
منفِّذٍ. ومَن يقرأُ الفرعَ بعدَ شهرٍ يحتاجُ أن يعرفَ: ما العطبُ؟ وأينَ أُصلِحَ؟
وما الذي قِيسَ فعلاً؟ وما الذي **لم** يُقَسْ ولماذا؟

## ١. العطبُ الأصليُّ — بالملفِّ والسطرِ

| الطرفُ | ما كانَ | الأثرُ |
|---|---|---|
| حاملُ الرابطِ | `get_tracking_position(text)` (هجرةُ `20260814150000`) تُعيدُ `lat`/`lng` **بلا حدِّ عُمرٍ** | نقطةٌ عمرُها ساعتانِ تُقرأُ «الآنَ» |
| صاحبةُ الرحلةِ | `driverPositionVerdict` + `DRIVER_POSITION_MAX_AGE_SECONDS = 90` في `packages/domain/transport/active-ride.ts` (`ADR 0106`) | النقطةُ تُحجَبُ فوقَ ٩٠ ثانيةً وسببُ الحجبِ مُصنَّفٌ |

**فالغريبُ المجهولُ كانَ مُصدَّقاً أكثرَ من صاحبةِ الرحلةِ.** والرابطُ الذي
يُشارَكُ للطمأنينةِ كانَ يُنتِجُ طمأنينةً كاذبةً — وهيَ أسوأُ من غيابِه.

## ٢. الإصلاحُ الجذريُّ

الهجرةُ `supabase/migrations/20260914060000_f2_09_ride_share_link_view.sql`
(313 سطراً):

| ما | أينَ | ماذا |
|---|---|---|
| بذرُ الإعدادِ | أعلى الهجرةِ | `driver_position_max_age_seconds` = `'90'::jsonb` لكلِّ مدينةٍ — **مطابقاً للثابتِ** فلا يتغيَّرُ سلوكُ `F2-06` بحرفٍ |
| الحَكَمُ الواحدُ | `tracking_link_view(uuid)` — السطرُ 81 | أربعةُ أحكامٍ مفصولةٍ · عُمرٌ يُنشَرُ دائماً متى وُجِدَ ختمٌ · **مفاتيحُ `lat`/`lng` محذوفةٌ** متى لم يكن الحكمُ `LOCATED` · مصدرُ الحدِّ مُعلَنٌ (`SETTING` / `FALLBACK_DEFAULT`) والتراجعُ `v_max_age := 90` مكتوبٌ صريحاً |
| حالُ المشاركةِ | `rider_ride_share_state(bigint, uuid)` — السطرُ 172 | المِلكيّةُ **قيدُ استعلامٍ** · الروابطُ الساريةُ ببقيّةٍ مطروحةٍ بساعةِ القاعدةِ · `preview` = حمولةُ الحَكَمِ **كما هيَ** |
| الغلافُ | `get_tracking_position(text)` — السطرُ 264 | أُعيدَت كتابتُها **غلافَ إذنٍ رقيقاً** فوقَ الحَكَمِ. التوقيعُ لم يُمَسَّ، والدالّةُ لم تُحذَفْ |
| سطحُ الصلاحيّاتِ | الأسطرُ 310–312 | `revoke execute … from public, anon, authenticated` للدوالِّ الثلاثِ |

فالصفحةُ العامّةُ (`apps/gateway/src/public/tracking-page.ts`) ومعاينةُ المالكةِ
تقرآنِ **جواباً واحداً حرفاً**.

## ٣. الطبقاتُ

| الطبقةُ | الملفُّ | المحتوى |
|---|---|---|
| النطاقُ | `packages/domain/transport/ride-share.ts` | `SharedPositionVerdict` · `PositionMaxAgeSource` · `SharePreview` اتّحاداً مُميَّزاً · `ShareLink` · `isLiveLink` · `liveLinks` · `isSharingNow` · `longestRemainingSeconds` · `shareAvailabilityOf` · `SHARE_DISCLOSED` (3) · `SHARE_WITHHELD` (7) |
| المنافذُ | `packages/application/transport/ride-share-ports.ts` | ثلاثةُ منافذَ **مفصولةٍ**: قراءةٌ · بدءٌ · إيقافٌ |
| المحوّلُ | `packages/infrastructure/transport/ride-share-store.ts` | `createRideShareReader(sql)` → `select rider_ride_share_state($1, $2) as result` — قراءةٌ ذرّيّةٌ واحدةٌ |
| الاستخدامُ | `packages/application/transport/ride-share.ts` | `readRideShare` · `startRideShare` · `stopRideShare` · `SHARING_NOT_CONFIGURED` رمزاً صريحاً |
| المسارُ | `apps/gateway/src/routes/rides.ts` | `GET` · `POST` · `DELETE` على `/v1/rides/:id/share` |
| السطحُ | `apps/miniapp/src/surfaces/rider/share/` | العقدُ · العميلُ · المنظورُ · `RideShareCard.tsx` (312 سطراً) مركَّباً في `ActiveRideScreen.tsx` |
| الترجمةُ | `packages/shared/i18n/miniapp/{ar,en,ur}.json` | 44 مفتاحاً جديداً لكلٍّ: **391 → 435** |

### ٣.١ قراراتٌ ثلاثةٌ تستحقُّ التسجيلَ

1. **الاتّحادُ مُحرَّسٌ وقتَ التشغيلِ**: حكمُ `LOCATED` بلا `lat`/`lng`/`ageSeconds`
   رقميّةٍ **يُحجَبُ** ولا يُرقَّعُ. فلو انزلقَت الهجرةُ يوماً، تُخفي الواجهةُ
   ولا تُلفِّقُ.
2. **لا مؤقِّتَ في الجهازِ**: البقيّةُ تُعرَضُ كما قالَتها القاعدةُ.
3. **الرمزُ مرّةً واحدةً**: عندَ الإصدارِ وحدَه، ولا يعودُ في أيِّ قراءةٍ.

## ٤. الحاجزُ الساكنُ — وسقوطُه المزروعُ

`scripts/lib/ride-share-contract.ts` + `scripts/check-ride-share-contract.ts`
(92 سطراً): **ثمانيةُ قواعدَ** مسجَّلةٌ في `package.json` وفي `ci.yml` بخطوتَينِ
(الحاجزُ، واختبارُ سقوطِه).

**وقاعدةُ «الحَكَمُ واحدٌ» (٥) سقطَت سقوطاً كاذباً أوّلَ تشغيلٍ** — وهذا أهمُّ ما
في هذا القسمِ: كانت تبحثُ عن نداءِ `tracking_link_view` داخلَ جسمِ
`get_tracking_position`، وكانَ الجسمُ يُقطَعُ عندَ الدالّةِ التاليةِ فيدخلُ فيه
ذيلُ الهجرةِ حيثُ `revoke execute on function tracking_link_view(uuid)` —
فيُحسَبُ **النصُّ في أمرِ الصلاحيّةِ نداءً**. فكانَ الحاجزُ يمرُّ ولو أُفرِغَ
الغلافُ من نداءِ الحَكَمِ. صُحِّحَ بقطعِ الجسمِ عندَ `$$ language`، ومكتوبٌ في
الملفِّ تعليقاً.

**والدرسُ مسجَّلٌ لا مُبتَلَعٌ**: الحاجزُ كانَ **أخضرَ على المستودَعِ الحقيقيِّ**
وهوَ معطوبٌ. ما كشفَه أخضرُه، بل **الحالةُ السالبةُ المزروعةُ** (`ح-7`).

## ٥. ما قِيسَ محلّيّاً

| الملفُّ | الحالاتُ | النتيجةُ |
|---|---|---|
| `tests/unit/ride-share-domain.test.ts` | 13 | نجحَت |
| `tests/unit/ride-share-store.test.ts` | 19 | نجحَت |
| `tests/unit/ride-share-view.test.ts` | 18 | نجحَت |
| `tests/unit/ride-share-usecase.test.ts` | 16 | نجحَت |
| `tests/unit/check-ride-share-contract.test.ts` | 29 | نجحَت (سالبةٌ مزروعةٌ لكلِّ قاعدةٍ) |
| `tests/unit/check-active-ride-contract.test.ts` | 27 | نجحَت بعدَ نقلِ المفردةِ |
| `tests/integration/ride-share.test.ts` | **21** | **لم تُشغَّلْ** — لا `postgis` في صندوقِ التنفيذِ |

`bun run typecheck` = 0. الحالاتُ المسجَّلةُ تجاوزاً صارَت **95 ملفّاً · 960
حالةً** في `scripts/lib/skip-registry.ts`، **مقيسةً بالحاجزِ نفسِه** لا محسوبةً
جمعاً باليدِ، والأرقامُ السابقةُ باقيةٌ مكتوبةً بلا محوٍ (`ح-8`).

## ٦. ما تقيسُه الحالاتُ الإحدى والعشرونَ — وهيَ التي تُغلِقُ البندَ لا غيرُها

| الحزمةُ | الحالاتُ | المُدَّعى الذي تقيسُه |
|---|---|---|
| المِلكيّةُ قيدُ استعلامٍ | 4 | رحلةُ راكبٍ آخرَ ومعرِّفٌ معدومٌ **جوابُهما الحرفُ نفسُه** وبلا معاينةٍ ولا روابطَ |
| الإتاحةُ | 1 | تتبعُ حالةَ الرحلةِ لا شرطاً مُوازياً — وحكمُ المعاينةِ وحكمُ `issue_tracking_token` واحدٌ |
| **حَكَمٌ واحدٌ** | 7 | المالكةُ والغريبُ يريانِ **الإحداثيّةَ نفسَها** طازجةً، و**غيابَ المفتاحِ نفسَه** متقادمةً · العُمرُ بساعةِ القاعدةِ · رفعُ الإعدادِ يكشفُ للاثنَينِ في اللحظةِ نفسِها بلا نشرِ كودٍ · غيابُه يُنشَرُ `FALLBACK_DEFAULT` باسمِه · «لم يُبلِّغْ» و«بلا ختمٍ» و«تقادمَ» ثلاثةُ أحكامٍ مفصولةٍ |
| الروابطُ | 6 | السارياتُ وحدَها · **لا رمزَ في أيِّ قراءةٍ** (يُقاسُ بغيابِ نصِّه من الحمولةِ كلِّها) · البقيّةُ بساعةِ القاعدةِ · الإيقافُ يُبطِلُ **كلَّ** روابطِ الطلبِ فتموتُ الصفحةُ فوراً · مُلغىً ومعدومٌ جوابُهما واحدٌ |
| الحمولةُ العامّةُ | 2 | **ثلاثةُ مفاتيحَ لا رابعَ** — لا اسمَ ولا هاتفَ ولا لوحةَ ولا وجهةَ ولا معرِّفَ طلبٍ |
| سطحُ الصلاحيّاتِ | 1 | `has_function_privilege` لا نصُّ الهجرةِ |

## ٧. الحاجزُ `B-CI-001` — ولماذا لا يُغلَقُ البندُ

الشغلتانِ `34787375529` و`34787375506` سقطتا في ثانيتَينِ بـ`steps: []`،
والتعليقُ: «The job was not started because recent account payments have failed
or your spending limit needs to be increased». وإعادتانِ للتشغيلِ
(`run_attempt: 3`) أعطَتا النتيجةَ نفسَها.

**فهذا سببٌ ماليٌّ في حسابٍ لا سببٌ في شِفرةٍ**، ولا يُصلِحُه المنفِّذُ: يُصلَحُ
من «Billing & plans» في إعداداتِ الحسابِ `uxxxug`. والنتيجةُ أنَّ:

* حالاتِ التكاملِ الإحدى والعشرينَ **لم تُشغَّلْ قطُّ**؛
* والحاجزَ الجديدَ وخطوتَيه في `ci.yml` **لم يُقرأْ لهما حكمٌ**؛
* و`ح-4` (ثلاثُ جولاتٍ خضراءُ) **لا يمكنُ أن تبدأَ**.

ولذلكَ يبقى `F2-09` في §9.5 على `[ ]` **بلا قلبٍ**، ولا يُدمَجُ فرعُه
(`ح-6`: الأخضرُ المحلّيُّ ليسَ حكماً).

## ٨. حدُّ ما يُدَّعى (`ح-5`)

لا `مَقيس` ولا `مُثبَت`. المُدَّعى: **شِفرةٌ مكتوبةٌ وهجرةٌ وحاجزٌ واختباراتُ
وحدةٍ خضراءُ محلّيّاً، وواحدٌ وعشرونَ اختبارَ تكاملٍ مكتوبٌ لم يُشغَّلْ**. ولا
نشرَ حيَّ (`ADR 0099`)، ولا راكبةَ حقيقيّةً شاركَت رابطاً، ولا قياسَ زمنِ استجابةٍ
على حِملٍ، وبوّابةُ `F2` غيرُ مُدَّعاةٍ.

## وما لا يفعلُه هذا الملفُّ عن قصدٍ

* **لا يُغلِقُ البندَ ولا يرفعُ حالتَه.** حالةُ `F2-09` تُقرأُ من §9.5 وحدَها.
* **لا يُكرِّرُ نصَّ `SR-13`** ولا نصَّ الحجزِ: يُقرآنِ من `docs/ROADMAP-MASTER.md`
  و`ROADMAP.md` فلا مصدرَ حقيقةٍ ثانٍ يفترقُ صامتاً (القاعدةُ 0.6).
* **لا يُكرِّرُ قرارَ `ADR 0109`** حُجَجاً: يُلخِّصُ أثرَه بالملفِّ والسطرِ.
* **لا يَحكي مخرجاتٍ خامّاً**: لا سجلَّ CI ولا مخرجاتِ اختبارٍ (موضعُها
  `docs/evidence/archive/`).
* **لا يزعمُ أنَّ `B-CI-001` عطبٌ في المشروعِ**: هوَ حاجزُ حسابٍ، وتسجيلُه ههنا
  بيانُ سببٍ لا اعتذارٌ.

## إضافةُ البند `F2-09` (2026-09-14 · أوّلُ حكمٍ حقيقيٍّ لـCI · `ح-8`)

رُفِعَ `B-CI-001` بجعلِ المستودَعِ عامّاً، فقُرِئَ أوّلُ حكمٍ. وهذا نصُّه ثمَّ
ما أصلحَه:

| الجريةُ | `verify` | Redis | فوضى `F5-06` | PostgreSQL | Roadmap |
|---|---|---|---|---|---|
| `34790672521` (`a4180df`) | ✅ | ✅ | ✅ | **❌** | ✅ |
| `34792768951` (`549d9a5`) | ✅ | ✅ | ✅ | **❌** | ✅ |

**والفشلُ في مِرصادِ البندِ لا في النِّظامِ المقيسِ** — وهذا لا يُخفِّفُه:

**١) رمزٌ أقصرُ من الحدِّ (13 حالةً).** `issue_tracking_token` والقيدُ
`trip_tracking_tokens_token_long_enough` يشترطانِ 64 محرفاً، و`freshToken()`
كانَ يُنتِجُ 36، فيَرُدُّ `TOKEN_TOO_SHORT` **قبلَ** أيِّ حكمٍ موضوعيٍّ. وأخطرُ
ما فيه أنَّ الحالةَ (٥) — «المنتهيةُ تمنعُ الإصدارَ» — **كانت لتمرَّ كاذبةً** لو
توقَّعَت رفضاً بلا تسميةِ رمزِه: رفضٌ لسببٍ آخرَ يُقرَأُ نجاحاً. فأُصلِحَ الطولُ
إلى حدٍّ مُسمّىً (`TOKEN_MIN_LENGTH`)، **وأُضيفَ حاجزٌ في المِرصادِ**: أيُّ
`TOKEN_TOO_SHORT` يُرفَعُ **عطبَ أداةٍ** لا فشلَ توقُّعٍ. والإنتاجُ سليمٌ أصلاً
(`randomBytes(32).toString("hex")` = 64).

**٢) إعادةُ إعدادٍ خرقَت نوعَه فسمَّمَت ملفّاتِ غيرِها (15 حالةً).** الحالةُ
(١٢) تحذفُ `driver_position_max_age_seconds` لتُثبِتَ `FALLBACK_DEFAULT` ثمَّ
تُعيدُه؛ والإعادةُ بـ`${x}::jsonb` تصلُ القاعدةَ `"90"` — نصّاً — فتخرقُ
`platform_settings_value_type_coherent`، فتسقطُ الإعادةُ **وتبقى المدينةُ ناقصةَ
مفتاحٍ**، فكلُّ ملفٍّ يُفعِّلُ مدينةً بعدَها يسقطُ بـ`CITY_SETTINGS_INCOMPLETE`:
`scheduled-jobs` (6) و`realtime-transport` (6) و`city-settings-inheritance` (1)
وأخواتُها. **فعطبُ بندٍ أسقطَ أدلّةَ بنودٍ أخرى** — وهذا أسوأُ من سقوطِه.
فأُصلِحَ الجذرُ: الصفُّ يُحفَظُ بتمثيلِ القاعدةِ (`value::text`) بنوعِه ووصفِه
ومؤقّتيّتِه، ويُعادُ بـ`::text::jsonb`، **ثمَّ يُتحقَّقُ من الإعادةِ بتوكيدٍ** —
فإن فشلَت سقطَ صاحبُها وحدَه.

**وما زادَ على الإصلاحِ**: العطبُ الثاني هوَ **رابعُ** ظهورٍ لنمطٍ واحدٍ في هذا
المستودَعِ، وكانَ الدرسُ مكتوباً في ثلاثةِ تعليقاتٍ **بلا إنفاذٍ**. فصارَ
مُنفَذاً: `ADR 0110` و`scripts/check-jsonb-binding.ts` (١٢٣٨ ملفّاً · خطوتانِ في
`ci.yml` · تسعُ سالباتٍ مزروعةٍ منها **السطرُ الذي أسقطَ CI**). وهذا اختيارٌ
مقصودٌ بينَ مسارَينِ مشروعَينِ: إصلاحُ السطرِ وحدَه كانَ يُخضِرُّ CI أسرعَ،
والإنفاذُ الآليُّ يمنعُ الخامسةَ.

**وحدُّ ما يُدَّعى**: لا اختبارَ حُذِفَ ولا توقُّعٌ خُفِّفَ ولا تخطٍّ صُنِّفَ.
والحكمُ الأخضرُ **لم يُقرَأْ بعدُ** لوظيفةِ PostgreSQL، فـ`F2-09` يبقى `[ ]`
و`ح-4` لم تبدأْ.
