# دليل `F1-09` · `DEC-19` — تسجيلُ قرارِ المالكِ وأوّلُ تشغيلٍ على CI (2026-09-24)

**الحالة**: `F1-09` يبقى `[~]` · `DEC-19` محسومٌ اتجاهاً لا استيفاءً · الاكتشافُ في آليّةِ المقياسِ الثالثِ حسمَه المالكُ (§ «قرارُ المالكِ في الآليّةِ»).
**الفرعُ**: `feat/dec19-slow4g-rider-surface` · **CI**: <https://github.com/uxxxug/ceezr/actions/runs/35975348387>
**السابقُ**: `F1-09-20260924-dec19-comparison.md` (باقٍ كما هوَ · `ح-8`) · **القرارُ**: `docs/adr/0185-*.md`

## ما نُفِّذَ

- `scripts/lib/rider-surface-budget.ts`: Slow4GConditions حرفاً (562.5 ms · 180,000 B/s · 84,375 B/s · CPU ×4)،
  الحدودُ 1,800/2,500/2,000 ms، الوضعُ `report-only`، و`READY_TO_BLOCK` متى استُوفيَت والوضعُ تقريرٌ.
- `scripts/lib/interactive-budget.ts`: المقياسُ «زمنُ بلوغِ سطحِ الراكبِ المرسومِ» لا «TTI»، وحارسُ «3G» بسقفِ 18,000 ms.
- `scripts/measure-tti.ts`: الملفّانِ في تشغيلٍ واحدٍ، وإصلاحُ LCP (مُدخَلاتُه اسمُها فارغٌ فكانَ الترشيحُ
  `e.name === "largest-contentful-paint"` يُبقيه `null` دائماً).
- `scripts/lib/first-paint-budget.ts`: العباراتُ الثلاثُ مصحَّحةٌ في موضعِها، وشاشةُ «افتح من تيليجرام» حارسٌ لا قبولٌ.

## نتائجُ CI (`35975348387` · سطحُ الراكبِ)

| الملفُّ | FCP | LCP | علامةُ `waslah-surface-rendered` | الفرقُ عن FCP |
|---|---|---|---|---|
| بلا تقييدٍ | 404 | — | 401 | −3 |
| 3G #1 · #2 · #3 | 14,216 · 14,228 · 14,208 | 16,280 · 16,296 · 16,260 | 14,214 · 14,228 · 14,212 | −2 · 0 · +4 |
| Slow 4G #1 · #2 · #3 | 4,140 · 4,132 · 4,132 | 4,728 · 4,728 · 4,732 | 4,144 · 4,128 · 4,132 | +4 · −4 · 0 |

الوسيطُ على Slow 4G: FCP 4,132 · LCP 4,728 · العلامةُ 4,132 — **غيرُ مستوفاةٍ** (الحدودُ 1,800 · 2,500 · 2,000).
وحارسُ «3G»: 14,214 ≤ 18,000. وشاشةُ النظامِ على «3G»: FCP 7,220 ≤ 9,200 (وظيفةُ أوّلِ رسمٍ خضراءُ).
والطلباتُ حتى العلامةِ: 11، والبايتاتُ المنقولةُ 214,658 (منها `/favicon.ico` 9,961 بايتاً يُعيدُ `index.html`).

## الاكتشافُ (`ح-8`)

الوظيفةُ **حمراءُ** بقاعدةِ `RENDERED_BEFORE_PAINT` في 4 من 7 تشغيلاتٍ. وقد وُثِّقَ في الفرعِ أنَّ العلامةَ «حدٌّ
أعلى لعرضِ أوّلِ إطارٍ للسطحِ»؛ والأرقامُ تنقضُه: العلامةُ في استدعاءِ `requestAnimationFrame` الثاني تقعُ
بينَ −4 و+4 ms من FCP. وFCP في Chromium توقيتُ **عرضِ** الإطارِ، واستدعاءُ `rAF` توقيتُ **الخيطِ الرئيسيِّ**
عندَ بدءِ الإطارِ — فالثاني قد يسبقُ عرضَ الإطارِ الأوّلِ. ويدلُّ تطابقُهما أيضاً على أنَّ أوّلَ رسمٍ ذي محتوىً
في هذا المسارِ **هوَ** سطحُ الراكبِ نفسُه (لا شيءَ يُرسَمُ قبلَه).

فالقاعدةُ عمِلَت كما صُمِّمَت ولا تُرخى؛ والخطأُ في الآليّةِ والادّعاءِ، وقد صُحِّحَ الادّعاءُ في
`RoleRouter.tsx` و`interactive-budget.ts` و`ADR 0185`. **وآليّةُ القياسِ معلّقةٌ على قرارِ المالكِ** ولم يُعدَّل
غيرُ ذلك. والخياراتُ المعروضةُ:

1. **Element Timing** (`elementtiming` على نصٍّ في أوّلِ شاشاتِ الراكبِ): توقيتُ عرضٍ بساعةِ FCP نفسِها؛
   يربطُ القياسَ بشاشاتٍ بعينِها ويحتاجُ حارساً يُلزِمُ كلَّ شاشةٍ أولى بالسمةِ.
2. **العلامةُ معَ تعريفٍ صادقٍ** («بدءُ الإطارِ الثاني بعدَ إيداعِ السطحِ» · خيطٌ رئيسيٌّ لا عرضٌ)
   وحذفُ قاعدةِ `RENDERED_BEFORE_PAINT` — تخفيفٌ لقاعدةٍ صحيحةٍ، فلا يُنصَحُ به.
3. **أكبرُ القيمتَينِ** (العلامةُ وFCP): حدٌّ أدنى لعرضِ السطحِ لا قياسٌ مباشرٌ.

## قرارُ المالكِ في الآليّةِ وتنفيذُه (2026-09-24 · يُضافُ ولا يمحو ما فوقَه)

**القرارُ**: Element Timing؛ لا علامةَ `rAF`، ولا حذفَ لـ`RENDERED_BEFORE_PAINT`، ولا `max(mark, FCP)`، ولا تغييرَ للحدودِ.

**التنفيذُ** (الدفعةُ `3d8a50f`):

- `apps/miniapp/src/surfaces/rider/welcome/surface-timing.ts`: المعرّفُ `waslah-rider-surface` مصدراً واحداً.
- `WelcomeScreen.tsx`: السمةُ على `<p>` السطرِ الأوّلِ (`welcome.line1`) في **الفرعِ الجاهزِ** وحدَه. وشاشةُ الترحيبِ
  أوّلُ شاشةٍ لسطحِ الراكبِ لأنَّ `proceeded` يبدأُ `false` في `RiderRoot.tsx`. وحالةُ التحميلِ (العنوانُ والهيكلُ)
  بلا سمةٍ، فلا يُلتقَطُ الرسمُ المبكرُ.
- `measure-tti.ts`: `PerformanceObserver({type:"element", buffered:true})` · يُرشَّحُ بـ`identifier` · تُقرأُ أوّلُ
  `renderTime > 0` · ويُعَدُّ المُدخَلاتُ ويُفحَصُ `element.closest('[aria-busy="true"]')`.
- الحَكَمُ: `NO_SURFACE_RENDERED` (بلا شرطٍ · ولو وصلَ FCP) · `RENDERED_WITHOUT_RIDER` · `DUPLICATE_TIMING_ENTRY` ·
  `TIMING_IN_LOADING` · و`RENDERED_BEFORE_PAINT` باقيةٌ.
- حاجزٌ ساكنٌ `scripts/check-rider-surface-timing.ts` (سلسلةُ `ci` وخطوةٌ في `verify`) بسبعِ قواعدَ:
  `NO_TIMING_ELEMENT` · `DUPLICATE_TIMING_ELEMENT` · `OUTSIDE_FIRST_SCREEN` · `RAW_ELEMENTTIMING` · `NOT_IN_READY_BRANCH` ·
  `NO_READY_ANCHOR` · `FIRST_SCREEN_MOVED`.
- أُزيلَت علامةُ `waslah-surface-rendered` واختبارُها (لم يُدمَجا قطُّ). وعلامتا `waslah-interactive` و`waslah-surface:*` باقيتانِ.

**الاختباراتُ المقصودةُ** (`ح-7`): وجودُ العنصرِ ⇒ لا مشكلةَ · غيابُه ⇒ `NO_SURFACE_RENDERED`/`NO_TIMING_ELEMENT` ·
FCP بلا عنصرِ الراكبِ ⇒ `NO_SURFACE_RENDERED` وحدَها · نقلُه إلى حالةِ التحميلِ ⇒ `NOT_IN_READY_BRANCH` · ورسمُ حالةِ
التحميلِ بـReact بلا `elementtiming`. **وإثباتُ نقطةِ القياسِ في المتصفّحِ**: `TIMING_IN_LOADING` لم يسقطْ، و`renderTime`
(4,688 ms في التشغيلِ الأوّلِ) بعدَ اكتمالِ `GET /v1/consents` (4,636 ms بساعةِ سجلِّ الطلباتِ التقريبيّةِ)، وFCP (4,088 ms)
قبلَه — فأوّلُ رسمٍ حالةُ التحميلِ، والعنصرُ المقيسُ الحالةُ الجاهزةُ.

**CI** <https://github.com/uxxxug/ceezr/actions/runs/35977101351> — **الوظائفُ الستُّ ناجحةٌ**:

| الملفُّ (سطحُ الراكبِ · وسيطُ 3) | FCP | LCP | زمنُ بلوغِ السطحِ المرسومِ | الحدودُ |
|---|---|---|---|---|
| Slow 4G (المعيارُ · تقريرٌ) | 4,108 | 4,692 | **4,692** | 1,800 · 2,500 · 2,000 — **غيرُ مستوفاةٍ** |
| 3G (حارسُ انحدارٍ) | 14,184 | 16,216 | 16,216 | السقفُ 18,000 — ضمنَه |

وعنصرُ القياسِ هوَ عنصرُ LCP أيضاً (القيمتانِ متطابقتانِ). **ولا تُقارَنُ** 4,692 بـ4,134 المسجَّلةِ في القرارِ: تلكَ لحظةُ بلوغِ
حالةِ السطحِ قبلَ `GET /v1/consents`، وهذهِ لحظةُ عرضِه جاهزاً بعدَه. ونتائجُ القرارِ باقيةٌ كما هيَ. `F1-09` يبقى `[~]`.
