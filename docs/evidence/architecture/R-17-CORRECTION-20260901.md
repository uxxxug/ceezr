# `R-17` — دليلُ تصحيحِ الإنفاذ: الطوبولوجيا تُعلَن ولا تُستنتَج من مخزنِ الجلسات

| الحقل | القيمة |
| --- | --- |
| البند | `R-17` — صحّةُ الناقلِ مشروطةٌ بـ`numInstances: 1` (`docs/production-readiness/PHASE-6-REALTIME.md` السطر 198) |
| التاريخ | 2026-09-01 |
| المنفّذ | وكيل Perplexity Computer — بأمرِ مديرِ المشروع |
| الوثيقةُ الحاكمة | [ADR 0051](../../adr/0051-process-topology-is-declared-not-inferred-from-session-store.md) — ناسخٌ **لشطرٍ واحدٍ** من [ADR 0050](../../adr/0050-single-instance-is-a-correctness-invariant-not-a-comment.md)، وسائرُه قائمٌ. و`ADR 0011` و`ADR 0042` **لم يُمَسّا** |
| الشطرُ المنفَّذ | **تصحيحُ الإنفاذِ وحدَه**: مصدرُ الحكمِ على الطوبولوجيا صار إعلاناً صريحاً |
| الشطرُ غيرُ المنفَّذ | **القدرةُ**: توزيعُ الأحداثِ بين النسخِ (ADR 0050 §١ العيبُ الثاني) — لم يُبدأ ولم يُقتَرَب منه |
| درجةُ البند بعدَ هذه الوحدة | **`مفتوح` كما هو — لا `[x]`.** المُغلَقُ عيبُ الإنفاذِ الخاطئِ لا البندُ |
| `main` قبلَ هذه الوحدة | `d398ca8e40` — `feat(R-17)` شطرُ الإنفاذِ (2026-08-31) |
| نوعُ هذا الملف | **دليلُ تصحيحٍ**: يُبيِّن العيبَ المنطقيَّ في الإنفاذِ السابقِ، وما صار مفروضاً بدلاً منه، وبرهانَ السقوطِ برمزِ الخروج |

> **قاعدةُ هذا الملف:** لا يُنقَل ههنا رقمُ سعةٍ ولا زمنُ استجابةٍ ولا نتيجةُ قياسٍ ميدانيٍّ — **ولا شيءَ من ذلك قِيسَ في هذه الوحدةِ ولا يُستخرَج منها**. والمنقولُ رموزُ خروجٍ وأعدادُ فحوصٍ ونصوصُ رسائلَ فحسبُ. **ولا يُقرأ من خُضرةِ أيِّ حاجزٍ ههنا أنّ التوسّعَ الأفقيَّ صار ممكناً.**

---

## ١) العيبُ الذي وُجِد — استنتاجٌ مقلوبٌ

الإنفاذُ المدمَجُ في `d398ca8e40` حكَم على الطوبولوجيا من `SESSION_STORE`:

```
if (distribution === "in-process" && sessionStore === "redis") ⇒ ارفض الإقلاع
```

وهذا يقرأ الاقترانَ في `ADR 0011` بالعكسِ. العلاقةُ الصحيحةُ **اتّجاهٌ واحدٌ**:

| المنطوق | الحكم |
| --- | --- |
| `multi-process ⇒ redis` | **صحيحٌ** — تعدُّدُ العملياتِ يوجب مخزنَ جلساتٍ مشتركاً |
| `redis ⇒ multi-process` | **باطلٌ** — عكسُ اللازمِ، وليس لازماً |

`SESSION_STORE=redis` يقول **أين تُخزَّن الجلسة**، ولا يقول **كم عمليةً تعمل**. فنسخةٌ واحدةٌ تكتب جلساتِها في Redis ضبطٌ مشروعٌ تماماً — بل هو المفيدُ عملياً: بقاءُ الجلساتِ عبرَ إعادةِ النشرِ وإعادةِ التشغيل.

**أثرُ العيبِ المقيس:** الحالةُ `single-process + redis` كانت تُرفَض في الإقلاعِ برمزِ خروجٍ `1` — أي أنّ الإنفاذَ كان **يمنع ضبطاً صحيحاً**، لا يمنع خرقاً.

### ١-أ) شاهدٌ ثانٍ داخلَ المستودعِ نفسِه

`docs/render-deployment-vars.md` (الأسطر 86–88) يصف مسارَ التوسّعِ المُوصى به هكذا: اضبط `SESSION_STORE=redis` → انشر → تحقّق → **ثمّ** ارفع `numInstances`. فالخطوةُ الأولى من مسارِ المستودعِ نفسِه كانت **مستحيلةً** تحتَ الإنفاذِ السابقِ: النشرُ يُرفَض عندَ `redis` قبلَ رفعِ النسخِ. عيبٌ مقروءٌ من وثيقتَين متعارضتَين لا من رأيٍ.

## ٢) ما صار مفروضاً بدلاً منه — محورانِ مستقلّان

| المحور | المتغيّر | القيم | وظيفتُه الوحيدة |
| --- | --- | --- | --- |
| ١ | `SESSION_STORE` | `memory` \| `redis` | **أين تُخزَّن حالةُ الجلسةِ** — ولا شيءَ غيرَ ذلك |
| ٢ | `PROCESS_TOPOLOGY` | `single-process` \| `multi-process` | **كم عمليةً تعمل** — إعلانٌ صريحٌ لا استنتاج |

ولم يُضَف `INSTANCE_COUNT` ولا أيُّ متغيّرٍ تخمينيٍّ: Render لا يوفّر عددَ النسخِ للعمليةِ بقيمةٍ موثوقةٍ تلقائياً، فمتغيّرٌ كهذا يكون رقماً يكتبه إنسانٌ ويكذّبه الواقعُ صامتاً.

### جدولُ الحكمِ الكامل (ADR 0051 §٢-ج)

| الطوبولوجيا | مخزنُ الجلسات | التوزيع | الحكم | السبب |
| --- | --- | --- | --- | --- |
| `single-process` | `memory` | داخلَ العملية | **يُسمَح** | كلُّ شيءٍ في عمليةٍ واحدةٍ |
| `single-process` | `redis` | داخلَ العملية | **يُسمَح** | **وهذه هي الحالةُ الإنتاجيةُ الراهنةُ** — جلساتٌ باقيةٌ عبرَ النشرِ، ولا علاقةَ لها بعددِ العمليات |
| `multi-process` | `redis` | داخلَ العملية | **يُرفَض** | `MULTI_PROCESS_WITHOUT_DISTRIBUTION` — الناقلُ داخلَ العمليةِ لا يعبر حدَّها |
| `multi-process` | `memory` | داخلَ العملية | **يُرفَض** | خرقانِ: التوزيعُ **و**مخزنُ الجلساتِ (ADR 0011) |

## ٣) الملفّاتُ ومواضعُ التغيير

| # | الملفُّ | ما صنعَه |
| --- | --- | --- |
| ١ | `docs/adr/0051-…-not-inferred-from-session-store.md` (جديد) | القرارُ الناسخُ — ينسخ **شطرَ الاستنتاجِ وحدَه** من ADR 0050 ويُبقي سبعةَ بنودٍ منه بحرفِها |
| ٢ | `packages/shared/config/index.ts` (معدَّل) | `PROCESS_TOPOLOGY_NAMES` و`ProcessTopologyName`، وحقلُ `processTopology` في `AppConfig`، وقراءةُ المتغيّرِ بالطريقةِ القياسيةِ للمشروعِ: افتراضُه `single-process`، **وقيمةٌ غيرُ صالحةٍ ⇒ `InvalidEnvVarError` حتميٌّ لا تجاهلٌ ولا ردٌّ إلى الافتراض** |
| ٣ | `packages/shared/config/single-instance.ts` (أُعيد بناؤه) | الحكمُ صار على `topology + distribution`؛ `SINGLE_INSTANCE_VIOLATION_REASONS` قائمةٌ مغلقةٌ بسببَين؛ `distributionCrossesProcessBoundary()`؛ ورمزُ الخطأِ `SINGLE_INSTANCE_INVARIANT` **لم يتغيّر** |
| ٤ | `apps/gateway/src/index.ts` (معدَّل) | يمرّر `topology: config.processTopology` إلى الحكمِ — سطرٌ واحدٌ. **ودلالاتُ Redis للجلساتِ لم تُمَسّ** |
| ٥ | `scripts/check-instance-invariant.ts` (وُسِّع لا كُرِّر) | الحاجزُ القائمُ نفسُه صار يقرأ `PROCESS_TOPOLOGY` كذلك، بأربعةِ رموزِ خرقٍ جديدةٍ |
| ٦ | `render.yaml` (معدَّل) | `PROCESS_TOPOLOGY: single-process` مُعلَنٌ صراحةً في الخدمتَين. **و`numInstances` بقي `1` ولم يُرفَع** |
| ٧ | `.env.example` · `docs/render-deployment-vars.md` | المتغيّرُ الجديدُ موثَّقٌ، ومسارُ التوسّعِ صار متّسقاً مع الإنفاذِ |
| ٨ | `tests/unit/single-instance-invariant.test.ts` | 15 فحصاً · 56 توكيداً |
| ٩ | `tests/unit/check-instance-invariant.test.ts` | 24 فحصاً · 51 توكيداً |

### التكافؤُ المفروضُ في ملفِّ النشر

`numInstances == 1 ⟺ PROCESS_TOPOLOGY == single-process` — في الاتّجاهَين معاً، فلا يُرفَع عددُ النسخِ وحدَه ولا تُقلَب الطوبولوجيا وحدَها.

## ٤) برهانُ السقوطِ — رمزُ الخروجِ لا نصُّ المُخرَج

### ٤-أ) عندَ الإقلاعِ (تشغيلٌ فعليٌّ للبوّابةِ بـ`Bun.spawn`)

| الحالةُ المزروعة | المُنتظَر | المُقاس |
| --- | --- | --- |
| `PROCESS_TOPOLOGY=multi-process` · `SESSION_STORE=redis` | رفضٌ | **رمزٌ غيرُ صفريٍّ** + `SINGLE_INSTANCE_INVARIANT` |
| `PROCESS_TOPOLOGY=multi-process` · `SESSION_STORE=memory` | رفضٌ | **رمزٌ غيرُ صفريٍّ** |
| `PROCESS_TOPOLOGY=many` (قيمةٌ باطلة) | رفضٌ | **رمزٌ غيرُ صفريٍّ**، والرسالةُ تذكر `PROCESS_TOPOLOGY` و`single-process` |
| `PROCESS_TOPOLOGY=single-process` · `SESSION_STORE=redis` | **قبولٌ** | **جاوَزَ الحارسَ ولم يُقتَل** — وهذا هو التصحيحُ عينُه |
| `PROCESS_TOPOLOGY` غائبٌ · `SESSION_STORE=redis` | **قبولٌ** (الافتراضُ `single-process`) | جاوَزَ الحارسَ |

وفحوصُ القبولِ لا تُقرَأ من غيابِ رسالةٍ وحدَه: كلُّ واحدٍ منها يُثبِت أنّ العمليةَ **كانت ما تزال حيّةً** بعدَ موضعِ الحارسِ، فلا يمرُّ سقوطٌ أبكرُ مرورَ نجاحٍ.

### ٤-ب) في المستودعِ (حاجزُ `render.yaml`)

| الخرقُ المزروع | رمزُ الخروج | رمزُ الخرق |
| --- | --- | --- |
| `PROCESS_TOPOLOGY` غائبٌ من الإعلان | `1` | `MISSING_PROCESS_TOPOLOGY` |
| `PROCESS_TOPOLOGY: many` | `1` | `INVALID_PROCESS_TOPOLOGY` |
| `multi-process` مع `numInstances: 1` | `1` | `TOPOLOGY_INSTANCES_MISMATCH` + `MULTI_PROCESS_WITHOUT_DISTRIBUTION` |
| `single-process` مع `numInstances: 2` | `1` | `TOPOLOGY_INSTANCES_MISMATCH` + `INSTANCES_ABOVE_ONE` |
| `multi-process` + `numInstances: 2` (وأيّاً كان مخزنُ الجلسات) | `1` | `MULTI_PROCESS_WITHOUT_DISTRIBUTION` |
| الخروقُ الخمسةُ مزروعةً في نسخةٍ من `render.yaml` الحقيقيِّ | `1` | — |
| **المستودعُ الحقيقيُّ كما هو** | **`0`** | — |

والزرعُ كلُّه في نصٍّ داخلَ الاختبارِ أو في نسخةٍ خارجَ المستودعِ — **ولم يُعدَّل `render.yaml` بخرقٍ قطُّ، ولم يُرفَع عددُ النسخِ فيه**.

### ٤-ج) لا تسرُّبَ سرٍّ

الحكمُ مُدخَلُه ثلاثةُ حقولٍ مقيَّدةٍ بالأنواعِ لا يحمل واحدٌ منها سرّاً. وفُحِص ذلك بتشغيلِ الإقلاعِ الفاشلِ بأسرارٍ صناعيّةٍ والبحثِ عن قيمةِ كلٍّ منها في `stdout` و`stderr` معاً — **صفرُ مطابقات**.

## ٥) الفحوصُ ونتائجُها (محلياً · Bun 1.4.0)

| الفحص | النتيجة |
| --- | --- |
| `bun run lint` | خروج `0` |
| `bun run typecheck` | خروج `0` |
| `bun run scripts/check-adr-numbering.ts` | خروج `0` — **51 قراراً بأرقامٍ فريدة** |
| `bun run scripts/check-env-drift.ts` | خروج `0` — كلُّ متغيّرٍ مُعلَنٍ مقروءٌ فعلاً في الشيفرة |
| `bun run scripts/check-secret-logging.ts` | خروج `0` |
| `bun run scripts/check-session-storage-policy.ts` | خروج `0` |
| `bun run scripts/check-instance-invariant.ts` | خروج `0` |
| `bun test` (كامل) | **2095 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6615 توكيداً في 191 ملفّاً** |
| `bun run check:coverage` | خروج `0` — المساراتُ التسعةُ الحرجةُ كلُّها فوقَ أرضيّتِها |

### تشغيلُ CI

تشغيلُ CI **`33568001488`** على `ea107e55e7` — **`success` في الوظائفِ الثلاثِ** معاً: `verify` · «تكامل على PostgreSQL حقيقي» · «تكامل على Redis حقيقي».

> **حدُّ الدليلِ ههنا مُعلَنٌ:** وظيفةُ `verify` في CI **ليس فيها خطوةٌ مستقلّةٌ** تنادي `scripts/check-instance-invariant.ts` — الحاجزُ في سلسلةِ `bun run ci` المحليّةِ. لكنّه **يعمل في CI فعلاً وبطريقٍ غيرِ مباشرٍ**: خطوةُ `Test` تُشغِّل `tests/unit/check-instance-invariant.test.ts` وفيه تشغيلٌ حقيقيٌّ للحاجزِ على `render.yaml` الحقيقيِّ **يُوجِب رمزَ خروجٍ صفريّاً**، فخرقٌ في ملفِّ النشرِ يُسقِط CI. **وإضافةُ خطوةٍ صريحةٍ لوظيفةِ `verify` عملٌ خارجَ نطاقِ هذه الوحدةِ، ولم يُنفَّذ، ويُسجَّل نقطةً مفتوحةً لا يُقرَأ خلافُها.**

والفارقُ عن قياسِ `R-17-ENFORCEMENT-20260831` (2079 · 6569) هو **16 فحصاً و46 توكيداً** أُضيفت في هذه الوحدةِ وحدَها.

## ٦) دَينٌ مُعلَنٌ لا مسكوتٌ عنه

| الدَين | المالك | الحال |
| --- | --- | --- |
| اختيارُ مُحدِّدِ المعدَّلِ ما يزال مقروناً بـ`SESSION_STORE` لا بالطوبولوجيا (`apps/gateway/src/index.ts`) | **`SCL-003` · `F5`** | **لم يُمَسّ في هذه الوحدةِ بأمرٍ صريحٍ.** والاقترانُ سليمُ الأثرِ عملياً اليومَ لأنّ الطوبولوجيا `single-process` |
| `SCL-002` و`F5-03` يطلبان إفشالَ الإقلاعِ عندَ `memory` في الإنتاج | `F5` | لا تعارضَ اليومَ. وقد **زال** التنافرُ الذي سجّله دليلُ 2026-08-31: `redis` على نسخةٍ واحدةٍ صارت مسموحةً، فلا يصطدم البندانِ بهذا الإنفاذ |
| توزيعُ الأحداثِ بين النسخِ | `R-17` · `F2` | **غيرُ منفَّذٍ** — ولم يُختَر له محوِّلٌ ولا آليةٌ |

## ٧) ما أُغلِق وما بقي مفتوحاً

**أُغلِق:** عيبُ الإنفاذِ المقلوبِ — لم يعد النظامُ يستنتج الطوبولوجيا من مخزنِ الجلساتِ، ولم يعد يمنع `single-process + redis`.

**بقي مفتوحاً — ولا يُقرَأ من هذا الملفِّ خلافُه:**

1. **`R-17` مفتوحٌ**، ونصُّ سطرِه لم يُعدَّل ولم يُقلَب `[x]`.
2. **التوزيعُ متعدّدُ النسخِ لم يُنفَّذ** — لا Redis Adapter ولا Pub/Sub ولا Streams ولا `LISTEN/NOTIFY`.
3. **لا سعةَ قِيسَت ولا أداءَ** في هذه الوحدةِ (`ح-5`).
4. **realtime الإنتاجيُّ ليس جاهزاً** ولم يُبدأ، و`F2` لم تُفتَح.
5. **`ADR 0011` و`ADR 0042` و`DEC-06` و`DEC-11` لم تُمَسّ**، والأجرةُ والتسعيرُ والدفعُ لم تُقتَرَب.
6. **`numInstances` ما يزال `1`** — ورفعُه ممنوعٌ بالحاجزِ حتّى يُنفَّذ التوزيعُ.
