# دليلُ `ECO-002` — نداءاتُ مزوّدِ التوجيهِ لكلِّ رحلةٍ **معدودةٌ على السِلكِ** (2026-09-20)

- **البندُ:** `ECO-002` (§17 — اقتصادُ التشغيلِ) · **الشقُّ المملوكُ للمستودَعِ
  وحدَه**: عددُ النداءاتِ.
- **ما لا يشملُه هذا الدليلُ:** **السعرُ**. حاصلُ ضربِ العددِ في سعرِ فاتورةٍ
  محجوزٌ بـ`REQ-09` (`[!]` · القاعدةُ 0-9) — فـ`ECO-002` **يبقى `[ ]` بعدَ هذا
  الدمجِ**، وذاكَ مقصودٌ ومُعلَنٌ لا سهوٌ.
- **القرارُ:** `ADR 0151`.
- **الفرعُ:** `feat/eco-002-routing-call-budget` من `main`@`5abdd8a`.
- **حجزُ النطاقِ:** `3769de6`.
- **المالكُ:** منفّذ المستودع.

---

## ١) بصمةُ القياسِ

| الحقلُ | القيمةُ |
|---|---|
| القاعدةُ | `PostgreSQL 18.6` + `PostGIS 3.6` — قاعدةٌ حقيقيّةٌ (`TEST_DATABASE_URL`) |
| `bun` | 1.4.2 (744846f84) |
| البوّابةُ | مبنيّةٌ من `buildContainer(testConfig({ routingProvider: "osrm", … }))` — لا مزوّدَ مبنيّاً باليدِ |
| مزوّدُ التوجيهِ | خادمُ `OSRM` مُزيَّفٌ يُصغي على منفذٍ حقيقيٍّ (`Bun.serve({ port: 0 })`) **يعدُّ طلباتِ `HTTP`** |
| عتباتُ التخزينِ | `DEFAULT_ROUTE_CACHE_THRESHOLDS` **مستوردةٌ** من `packages/application/tracking/route-cache.ts` — `minChangeMeters: 50` · `ttlSeconds: 60` |
| شكلُ النافذةِ | `RIDE_ROUTING_PROFILE`: نافذةُ 10 دقائقَ · 60 نبضةً · 8 قراءاتٍ نشطةٍ متحرّكةٍ · 4 قراءاتٍ مكرَّرةٍ · تسعيرةٌ واحدةٌ |
| السقفُ | **مُشتَقٌّ** `activeReadCount + quoteCallCount` = **9** (`maxAllowedBudget` = 9) |

## ٢) الرقمُ المقيسُ — منسوخٌ حرفاً من التشغيلِ

```
$ TEST_DATABASE_URL=postgres://…/waslah bun test tests/integration/routing-call-budget.test.ts

── ECO-002 ──
9 نداءَ توجيهٍ في رحلةٍ واحدةٍ من سقفِ 9 — 0 منها في 60 نبضةً · 4 نداءً منعَهُ
التخزينُ في 4 قراءةٍ مكرَّرةٍ. ولا يُدَّعى أنَّ هذا تكلفةٌ: العددُ مقيسٌ والسعرُ
في فاتورةِ مزوّدٍ (REQ-09).

(pass) ECO-002 — نداءاتُ التوجيهِ لكلِّ رحلةٍ، معدودةً على السِلكِ > رحلةٌ
       واحدةٌ: نبضاتٌ لا تُنادي، ومكرَّرٌ يُخزَّنُ، والمجموعُ تحتَ سقفٍ مُشتَقٍّ
1 pass · 0 fail · 4 expect() calls
```

### توزيعُ النداءاتِ على مراحلِ الرحلةِ

| المرحلةُ | العملُ المُنفَّذُ على البوّابةِ | نداءاتُ المزوّدِ |
|---|---|---|
| ١) تسعيرةٌ | `POST /v1/quote/ride` × 1 | **1** |
| ٢) إنشاءُ رحلةٍ وبذرُ إسنادٍ | كتابةٌ في القاعدةِ | **0** |
| ٣) نبضاتُ موقعِ السائقِ | `POST /v1/driver/location` × **60** | **0** |
| ٤) قراءاتٌ نشطةٌ بموضعٍ **متحرّكٍ** | `GET /v1/rides/:id` × 8 (0.002° لكلِّ قراءةٍ ≈ 220 م > 50 م) | **8** |
| ٥) قراءاتٌ مكرَّرةٌ بموضعٍ **ثابتٍ** | `GET /v1/rides/:id` × 4 داخلَ مدّةِ الصلاحيّةِ | **0** (4 نداءً منعَها التخزينُ) |
| **المجموعُ** | | **9 / 9** |

**الدعوى الثلاثيّةُ المُثبَتةُ بهذا الجدولِ:**

١. **نبضةُ الموقعِ لا تُنادي مزوّداً**: ستّونَ نبضةً نصيبُها **صفرٌ بالضبطِ** لا
   «قليلٌ». ومن أدخلَ غداً مساراً لكلِّ نبضةٍ صارَ المجموعُ 69 فتجاوزَ السقفَ
   في أوّلِ جولةٍ.
٢. **التخزينُ يمنعُ نداءً فعلاً**: أربعُ قراءاتٍ مكرَّرةٍ أنتجَت **صفرَ طلباتٍ على
   السِلكِ** — وهذا بعينِه ما قالَ حاجزُ `CAP-012` في وثيقتِه إنَّه **لا
   يُثبِتُه**.
٣. **السقفُ مُشتَقٌّ لا مكتوبٌ**: الفاتورةُ تنمو بقراءاتِ الراكبِ لا بترددِ نبضةِ
   السائقِ — وترددُ النبضةِ **غائبٌ عن حسابِ السقفِ قصداً**.

## ٣) ما بُنيَ

| المِلفُّ | الدورُ |
|---|---|
| `scripts/lib/routing-call-budget.ts` | حَكَمٌ نقيٌّ: شكلُ النافذةِ · السقفُ **دالّةً** · سقفُ السقفِ · تسعُ قواعدَ · وصفُ الخرقِ |
| `tests/integration/routing-call-budget.test.ts` | القياسُ: خادمٌ على منفذٍ حقيقيٍّ يعدُّ الطلباتِ · بوّابةٌ من الحاويةِ · قاعدةٌ حقيقيّةٌ · خمسُ مراحلَ |
| `tests/unit/routing-call-budget.test.ts` | 15 حالةً · 21 تأكيداً — سالبٌ مبذورٌ لكلِّ قاعدةِ حكمٍ (`ح-7`) |
| `scripts/check-routing-call-budget.ts` | حاجزٌ ساكنٌ بعشرِ قواعدَ يحرسُ بقاءَ القياسِ ووصلَه |
| `tests/unit/check-routing-call-budget.test.ts` | 13 حالةً · 19 تأكيداً — سالبٌ مبذورٌ لكلِّ قاعدةِ حاجزٍ (`ح-7`) |
| `scripts/lib/skip-registry.ts` | مسارٌ حرجٌ جديدٌ في القائمةِ المغلقةِ («الاستدامةُ الاقتصاديّةُ») + مدخلُ تجاوزٍ للملفِّ |
| `scripts/lib/coverage-registry.ts` | أرضيّةُ تغطيةٍ **مقيسةٌ** للمسارِ الجديدِ (لكلِّ مسارٍ مدخلٌ واحدٌ) |
| `package.json` · `.github/workflows/ci.yml` | وصلُ الحاجزِ في سلسلةِ `ci` وأربعُ خطواتٍ مُسمّاةٍ في `CI` |

### قواعدُ الحَكَمِ التسعُ وقواعدُ الحاجزِ العشرُ

| حَكَمٌ | ما يُسقِطُ |
|---|---|
| `facts.measured` | حقائقُ غيرُ مقيسةٍ تُقرأُ حُكماً |
| `counts.sane` | عددٌ سالبٌ أو نافذةٌ صفرٌ |
| `calls.consistent` | مجموعُ المراحلِ لا يطابقُ المُعلَنَ |
| `heartbeat.no-route` | نداءٌ واحدٌ في مرحلةِ النبضاتِ |
| `cache.suppresses-repeats` | نداءٌ في قراءةٍ مكرَّرةٍ |
| `ride.within-budget` | مجموعٌ فوقَ السقفِ |
| `budget.derived` | سقفٌ لا يطابقُ الاشتقاقَ |
| `thresholds.single-source` | عتبةٌ منسوخةٌ لا مستوردةٌ |
| `window.declared` | نافذةٌ غيرُ مُعلَنةٍ |

| حاجزٌ | ما يُسقِطُ |
|---|---|
| `guard.measurement-present` | غيابُ مِلفِّ القياسِ |
| `guard.judge-imported` | قياسٌ لا يستوردُ الحَكَمَ |
| `guard.wire-counted` | عدٌّ بجاسوسٍ لا بخادمٍ على منفذٍ |
| `guard.container-wired` | بوّابةٌ لا تُبنى من `buildContainer` |
| `guard.phases-measured` | مرحلةٌ من الخمسِ غيرُ مقيسةٍ |
| `guard.assertion-intact` | تأكيدٌ مُفرَّغٌ |
| `guard.thresholds-imported` | عتباتٌ منسوخةٌ في القياسِ |
| `guard.cache-policy-guard` | سقوطُ حاجزِ `CAP-012` سنداً |
| `guard.self-enforced` | الحاجزُ نفسُه خارجَ سلسلةِ `ci` |
| `guard.budget-sane` | سقفٌ غيرُ عقلانيٍّ |

## ٤) الفحوصُ المحليّةُ (قبلَ الدفعِ)

| الفحصُ | النتيجةُ |
|---|---|
| `bun run scripts/check-routing-call-budget.ts` | ✓ سقفٌ مُشتَقٌّ 9 · 60 نبضةً نصيبُها صفرٌ · 9 قواعدَ حكمٍ · 10 قواعدَ حاجزٍ |
| `bun run scripts/check-route-cache-policy.ts` | ✓ (السندُ باقٍ) |
| `bun run scripts/check-skip-classification.ts` | ✓ |
| `bun run scripts/check-adr-numbering.ts` | ✓ 151 قراراً بأرقامٍ فريدةٍ |
| `bun run scripts/check-docs-budget.ts` | ✓ 455 وثيقةً · نسبةٌ 0.418 من سقفِ 0.75 |
| `bun run scripts/check-guard-enforcement.ts` | ✓ |
| `bun run scripts/check-runner-command-validity.ts` | ✓ |
| `node scripts/check-roadmap.mjs` | ✓ |
| `bun run lint` | ✓ 30 تحذيراً · 3 إفاداتٍ — **كلُّها سابقةٌ لهذا العملِ** |
| `bun run typecheck` | ✓ |
| `bun run check:coverage` | ✓ 11 مساراً حرجاً فوقَ أرضيّتِه · «الاستدامةُ الاقتصاديّةُ» 87.42% (264/302 · 2/2 مقيساً) |
| `bun test` (بلا `TEST_DATABASE_URL` — كوظيفةِ `verify`) | ✓ **5435 pass · 1402 skip · 0 fail** · 17508 تأكيداً |
| `bun test tests/integration/routing-call-budget.test.ts` (بقاعدةٍ حقيقيّةٍ) | ✓ 1 pass · 0 fail |

## ٥) حكمُ CI

> **لم يَرِدْ بعدُ.** يُكتَبُ ههنا حكمُ الجولةِ على الفرعِ **وظيفةً وظيفةً**
> بمعرِّفِها، ثمَّ حكمُ الجولةِ على `main` بعدَ الدمجِ. والقاعدةُ 0-7: لا دمجَ
> بلا حكمٍ، **وخُضرةٌ محليّةٌ ليسَت حكماً**.

## ٦) حدودٌ مُعلَنةٌ — ما لا يُدَّعى (`ح-5`)

- **لا رقمَ ماليّاً**: لا سعرَ في الحَكَمِ ولا في القياسِ ولا في الحاجزِ.
  و`ECO-002` باقٍ `[ ]`.
- **أعدادُ النافذةِ مُعلَنةٌ لا مقيسةٌ من نشرٍ حيٍّ** (`ADR 0099`): ستّونَ نبضةً
  وثمانُ قراءاتٍ شكلٌ محافظٌ. والمقيسُ **نصيبُ كلِّ صنفٍ**، وهوَ ثابتٌ على
  تغيُّرِ الشكلِ.
- **لا يُقاسُ صوابُ `OSRM`**: الخادمُ يردُّ حمولةً ثابتةً صحيحةَ الشكلِ؛ المقيسُ
  كم طلباً وصلَه.
- **لا `nearest` ولا `table`**: غيرُ مخزَّنتَينِ في الغلافِ ولا يستدعيهما سطحُ
  الراكبِ اليومَ.
- **لا يُدَّعى أنَّ رحلةً حقيقيّةً لن تتجاوزَ التسعةَ**: يُدَّعى أنَّ **هذا
  السلوكَ** مقيسٌ ومحروسٌ.
- **أرضيّةُ التغطيةِ الجديدةُ جذرانِ في `scripts/` لا شريحةٌ في `packages/`**:
  مِلفّا التخزينِ داخلَ جذرٍ قائمٍ لمسارِ «التتبّعِ»، وإدراجُهما مرّتَينِ مصدرُ
  حقيقةٍ مُكرَّرٌ. ونقصُ 37 سطراً في الحاجزِ مسالكُ `main()` — تُقاسُ بتشغيلِه
  في `CI` لا بتغطيةِ وحدةٍ.

## ٧) مراجعُ الشِفرةِ

- الحَكَمُ: [`scripts/lib/routing-call-budget.ts`](../../../scripts/lib/routing-call-budget.ts)
- القياسُ: [`tests/integration/routing-call-budget.test.ts`](../../../tests/integration/routing-call-budget.test.ts)
- الحاجزُ: [`scripts/check-routing-call-budget.ts`](../../../scripts/check-routing-call-budget.ts)
- سندُ التخزينِ: [`packages/application/tracking/route-cache.ts`](../../../packages/application/tracking/route-cache.ts) · [`packages/application/tracking/cached-routing-provider.ts`](../../../packages/application/tracking/cached-routing-provider.ts)
- القرارُ: [`docs/adr/0151-routing-calls-per-ride-are-counted-not-estimated.md`](../../adr/0151-routing-calls-per-ride-are-counted-not-estimated.md)
