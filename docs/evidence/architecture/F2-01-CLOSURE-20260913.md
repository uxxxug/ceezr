# دليلُ إغلاقِ `F2-01` — معيارُ القبولِ مقيسٌ على قاعدةٍ حقيقيّةٍ، وعطبُ CI مُصلَحٌ في جذرِه (2026-09-13)

- **البندُ:** `F2-01` — «SR-01 الترحيب والموافقات بختم زمني مسجَّل»
  (`docs/ROADMAP-MASTER.md` §9.5)
- **الفرعُ:** `feat/f2-01-closure` من `main`@`9b7b54f`
- **القرارُ المعماريُّ المُنشأُ ههنا:** `docs/adr/0100-a-test-port-is-asked-of-the-system-not-computed.md`
- **الدليلُ السابقُ لا يُمحى ويُقرأُ معَه:** `docs/evidence/architecture/F2-01-CONSENT-20260912.md`

## ١) لماذا عقدةُ إغلاقٍ منفصلةٌ لا بناءٌ جديدٌ

شِفرةُ `F2-01` مبنيّةٌ ومدموجةٌ من قبلُ: الهجرةُ
`supabase/migrations/20260912210000_f2_01_user_consents.sql`، و`GET`/`POST /v1/consents`
في `apps/gateway/src/routes/consents.ts`، والقرارُ النقيُّ في `packages/domain/consent/`،
وحالةُ الاستعمالِ في `packages/application/consent/`، وشاشةُ الترحيبِ في
`apps/miniapp/src/surfaces/rider/welcome/`، والحاجزُ `scripts/check-consent-documents.ts`،
و31 مفتاحاً في `ar`/`en`/`ur`. **فلم يُبنَ ههنا شيءٌ من البندِ** — وإنّما رُفِعَ
سقفُ الادّعاءِ المسجَّلُ في `ROADMAP.md` بعدَ أن زالَ سببُه، وقِيسَ ما كانَ
يُتخطَّى، وأُصلِحَ عطبُ CI الذي ظهرَ على `main` قبلَ القلبِ.

## ٢) معيارُ القبولِ بنداً بنداً — وأينَ يُقرأُ في الشِفرةِ

نصُّ `SR-01` في §9.5: «شرح في ثلاثة أسطر، اختيار اللغة، الموافقة على الشروط
وسياسة الخصوصية (بختم زمني مسجَّل)، طلب إذن الموقع» والفعلُ «ابدأ».

| الشرطُ | موضعُه | الحالُ |
|---|---|---|
| شرحٌ في ثلاثةِ أسطرٍ | `welcome.line.1..3` في `packages/shared/i18n/miniapp/{ar,en,ur}.json` | مُنفَّذٌ |
| اختيارُ اللغةِ | `languagePicker` في `WelcomeScreen.tsx` بثلاثةِ أزرارٍ و`aria-pressed` و`dir` مشتقٍّ | مُنفَّذٌ |
| موافقةٌ بختمٍ زمنيٍّ مسجَّلٍ | `user_consents` + `record_user_consent` (الختمُ من ساعةِ الخادمِ لا من العميلِ) | **مُتحقَّقٌ منه** — §٣ |
| «ابدأ» | `welcome.start` ثمَّ `onProceed` في `RiderRoot.tsx:32` | مُنفَّذٌ |
| طلبُ إذنِ الموقعِ | **مُستثنىً بقرارٍ منشورٍ** `ADR 0093`: §9.12 «كلُّ إذنٍ يُطلَبُ في لحظةِ الحاجةِ» يَغلِبُ جردَ الشاشةِ في §9.5 | مُعلَنٌ لا مُسكَتٌ |

ونصُّ البندِ لم يُمَسَّ حرفاً (`ح-1`)، والاستثناءُ في ADR لا في نصِّ الخارطةِ.

## ٣) القياسُ الذي لم يكن ممكناً يومَ البناءِ

يومَ 2026-09-12 كانَ ملفُّ التكاملِ `tests/integration/user-consents.test.ts`
**متخطّىً** بعقدِ المستودَعِ (`describeIf(TEST_DATABASE_URL)`) لأنَّ لا قاعدةَ
حقيقيّةً كانت مُهيَّأةً. وبعدَ `S-5` صارت القاعدةُ مُهاجَرةً إلى آخرِ هجرةٍ
(68 جدولاً)، فأُجريَ الملفُّ عليها:

```
TEST_DATABASE_URL=<مُجمَّعُ Supabase · ap-southeast-2> bun test tests/integration/user-consents.test.ts
```

| الحالةُ | الحُكمُ |
|---|---|
| ١) `city_id` موجودٌ، `not null`، ومرتبطٌ بـ`cities` بمفتاحٍ أجنبيٍّ | ناجحةٌ · 635.19ms |
| ٢) `RLS` مفعَّلٌ ولسياستِه صفٌّ مُعلَنٌ | ناجحةٌ · 422.65ms |
| ٣) قيدُ `kind` يقبلُ المُعلَنَ ويردُّ ما ليسَ مُعلَناً | ناجحةٌ · 1112.10ms |
| ٤) الدالّةُ تستنبطُ المدينةَ من صفِّ المستخدمِ ولا تُمرَّرُ إليها | ناجحةٌ · 1037.06ms |
| ٥) إعادةُ الإرسالِ: `already_recorded` بلا صفٍّ ثانٍ وبلا تحريكِ الختمِ | ناجحةٌ · 829.16ms |
| ٦) موافقةٌ على إصدارٍ جديدٍ تُضافُ ولا تمحو القديمَ | ناجحةٌ · 632.33ms |
| ٧) مستخدمٌ لا صفَّ له: `USER_NOT_FOUND` ولا صفَّ يُنشَأُ | ناجحةٌ · 621.50ms |
| ٨) `list_user_consents` تعيدُ صفوفَ صاحبِها وحدَه | ناجحةٌ · 1243.94ms |
| ٩) لا `anon` ولا `authenticated` ينفِّذُ دالّتَي الموافقةِ | ناجحةٌ · 212.10ms |

**تسعٌ من تسعٍ · صفرُ إخفاقٍ · 28 توكيداً · 9.97s.** وهذا يرفعُ «الموافقةُ بختمٍ
زمنيٍّ مسجَّلٍ» من **مُختبَرٍ** إلى **مُتحقَّقٍ منه** بحدِّ §1.4: جرى على قاعدةٍ
حقيقيّةٍ مُدارةٍ لا محاكاةٍ. **ولا `مَقيس` ولا `مُثبَت`** — لا حِملَ ولا مولِّدَ
خارجَ المضيفِ، ولا جهازَ حقيقيٌّ، ولا رحلةً كاملةً.

ووحدةُ البندِ تُقرأُ معَه: `bun test` على
`tests/unit/consent-decision.test.ts` · `consent-route.test.ts` ·
`check-consent-documents.test.ts` · `apps/miniapp/src/surfaces/rider/welcome/`
⇒ **54 ناجحةً · 0 فاشلةً · 293 توكيداً · 92ms**.

## ٤) عطبُ CI على `main` — مُصلَحٌ في جذرِه لا مُرفَعٌ بمهلةٍ

الشغلةُ `34734754215` على `9b7b54f`: `verify` أخفقَت في الخطوةِ 8، وحالةٌ واحدةٌ
ساقطةٌ في `tests/unit/gateway-graceful-shutdown.test.ts` بمهلةِ 40074.09ms.
والسجلُّ يُسمّي السببَ: `EADDRINUSE` على المنفذِ **33096** في العمليّةِ
الوليدةِ، فماتَت وبقيَ المُختبَرُ ينتظرُ صحّةً لا تأتي. والتفصيلُ والقرارُ في
`ADR 0100`. والمُنفَّذُ ثلاثةٌ: منفذٌ يُسنِدُه النظامُ (`port: 0`) · إعادةُ إقلاعٍ
حتّى أربعٍ **عندَ `EADDRINUSE` وحدَه** · وكَفُّ الانتظارِ حالَ موتِ الوليدِ.

**ولا مهلةَ رُفِعَت، ولا توكيدَ خُفِّف، ولا حالةَ صُنِّفَت تخطّياً، ولا شِفرةَ
إنتاجٍ مُسَّت.** والقياسُ بعدَ الإصلاحِ محليّاً: **3 ناجحةٍ · 0 فاشلةٍ · 15.11s**.

## ٥) الحدودُ المُعلَنةُ — ما لا يُدَّعى ههنا

- **بوّابةُ `F2` لا تُدَّعى**: نصُّها «رحلةٌ كاملةٌ من البدايةِ إلى النهايةِ بلا
  لمسِ محادثةِ البوتِ، على جهازٍ حقيقيٍّ، مسجَّلةٌ بالفيديو» — وبندٌ واحدٌ من
  اثنَي عشرَ لا يقترِبُ منها، ولا جهازَ حقيقيٌّ يُبلَغُ من ههنا.
- **لا نشرَ حيَّ**: حسابُ Render بلا خدمةٍ واحدةٍ (مقيسٌ في `ADR 0099`)، فالشاشةُ
  لم يفتحها مستخدمٌ حقيقيٌّ بعدُ — والمقيسُ أنَّ مسارَها وسجلَّها يعملانِ على
  قاعدةٍ حقيقيّةٍ.
- **حِدّةُ `F5-07` الزمنيّةُ لم تُدَّعَ مُغلَقةً** — الشغلةُ المقيسةُ ههنا سببُها
  `EADDRINUSE` بحرفِ السجلِّ، لا ازدحامُ الحزمةِ.
- **48 نصّاً صريحاً في `apps/miniapp` وحاجزُ §9.11 الغائبُ** يبقيانِ عقدةً
  منفصلةً كما سُجِّلا في `ROADMAP.md`.

## ٦) حكمُ CI الفعليُّ

يُقرأُ لكلِّ وظيفةٍ ولكلِّ خطوةٍ من الواجهةِ البرمجيّةِ لا من نصِّ سجلٍّ.

**السابقُ المقيسُ على `main` قبلَ هذا الفرعِ** (لا يُدَّعى، يُقرأُ):

| الشغلةُ | الشجرةُ | الحكمُ |
|---|---|---|
| `34732986162` | `92a73c7` (`S-3`) | خضراءُ · الوظائفُ الأربعُ |
| `34734205137` | `90b00b9` (`S-5`) | خضراءُ · الوظائفُ الأربعُ |
| `34734666105` | `40a3290` (`ADR 0099`) | خضراءُ · الوظائفُ الأربعُ |
| `34734754215` | `9b7b54f` (وثائقُ فحسبُ) | **حمراءُ** · `verify` الخطوةُ 8 — `EADDRINUSE`، وهوَ المُصلَحُ في §٤ |

فثلاثُ دوراتٍ خضراءَ متتاليةٍ سابقةٌ (`ح-4` بسابقتِه في `SCL-004` و`F6-07`)،
والحمراءُ الرابعةُ سببُها مُسمّىً ومُصلَحٌ في جذرِه لا مُتخطّىً.

**وحكمُ هذا الفرعِ** — والقلبُ `[ ]` ⇐ `[x]` **لا يُنفَّذُ إلّا بعدَه**:

| الشغلةُ | الشجرةُ | الحكمُ |
|---|---|---|
| `34735979680` | `ee5bef9` | **خضراءُ** · `verify` ✅ · `تكامل على PostgreSQL حقيقي` ✅ · `تكامل على Redis حقيقي` ✅ · `فوضى متعدد المثيلات (F5-06)` ✅ · ولا خطوةَ فاشلةَ واحدةَ |
| `34735978258` | `ee5bef9` | **خضراءُ** · الوظائفُ الأربعُ نفسُها بـ`success` |
| `34735978242` | `ee5bef9` | `Roadmap freshness` ✅ |

| `34736416230` | `be67f25` | **خضراءُ** · الوظائفُ الأربعُ (قلبُ الرمزِ وصفُّ السجلِّ) |
| `34736418060` | `be67f25` | **حمراءُ** · `تكامل على PostgreSQL حقيقي` الخطوةُ 11 — `Failed to start server. Is port 46602 in use?` في `admin-service-separation`: **العطبُ نفسُه في ملفٍّ ثانٍ**، والشجرةُ نفسُها خضراءُ في الشغلةِ الأخرى ⇒ مسابقةٌ لا منطقٌ. أُصلِحَ في جذرِه بنقلِ `reserveFreePort` إلى `tests/support/free-port.ts` وإعادةِ إقلاعٍ عندَ `EADDRINUSE` وحدَه (إضافةُ `ADR 0100`) · القياسُ بعدَه **3/3 · 6.85s** على القاعدةِ الحقيقيّةِ |

فسُؤالُ المنفذِ منَ النِّظامِ أزالَ العطلَ الموروثَ على عاملِ CI نفسِه لا محليّاً فحسبُ،
وعليه قُلِبَ الرمزُ في `docs/ROADMAP-MASTER.md` §9.5 إلى `[x]` معَ صفٍّ واحدٍ في §25
وهذهِ الوثيقةِ — الخطواتُ الثلاثُ الإلزاميّةُ (`ح-4`).
