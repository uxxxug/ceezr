# دليلٌ — `W-6` الزيادةُ الثانيةُ: بوّابةُ الصادرِ وقتَ التشغيلِ

- **التاريخُ**: 2026-09-12
- **البندُ**: `W-6` — لا اقترانَ تجاريَّ مباشرَ مع MARKET، وكلُّ مرورٍ عبرَ CORE
- **القرارُ**: `ADR 0086` (يُكمِّلُ `ADR 0084`)
- **الحالةُ المُدَّعاةُ**: مُنفَّذٌ · مُختبَرٌ · مَقيسٌ — **وليسَ مُثبَتاً** (`ح-5`)

## ما كانَ قبلَ هذه الزيادةِ، بنصِّ الزيادةِ الأولى

الزيادةُ الأولى (`ADR 0084`, `docs/evidence/architecture/W-6-20260912.md`) أعلنَت
حدَّ نفسِها: **«لا منعَ للصادرِ وقتَ التشغيلِ»**. أي أنَّ الإنفاذَ كانَ في البناءِ
وحدَه، وأنَّ نداءً وقتَ التشغيلِ إلى مضيفٍ غيرِ مُعلَنٍ — بمفتاحِ بيئةٍ مضبوطٍ خطأً،
أو بمضيفٍ مُركَّبٍ، أو بناقلٍ محقونٍ — كانَ يمضي ولا يُخفِقُ شيءٌ.

وهذه الزيادةُ تُغلِقُ ذلكَ الحدَّ بالذاتِ، ولا تُوسِّعُ البندَ ولا تُعدِّلُ نصَّه.

## ما نُفِّذَ، مقيساً

| الأثرُ | القياسُ |
|---|---|
| السجلُّ مصدرٌ واحدٌ للطبقتَينِ | نُقِلَ `scripts/lib/wasla-egress-registry.ts` إلى `packages/shared/wasla/egress-registry.ts` بـ`git mv` (لا نسخةٌ ثانيةٌ)، ويقرؤُه الحاجزُ الساكنُ والبوّابةُ معاً |
| الإلزامُ مكتوبٌ لكلِّ مقصدٍ | حقلُ `runtimeGate` في **١٥** مُدخلاً: **١١** `gated` · **٤** `not-applicable` بسببٍ مكتوبٍ لكلٍّ |
| البوّابةُ | `packages/shared/wasla/egress-gate.ts` — `decideEgress` صافيةٌ (السجلُّ والبيئةُ مُمَرَّرانِ) · `createGuardedFetch` · `assertEgressAllowed` · `assertEgressEnvironment` |
| الوصلُ الفعليُّ | **٩** ملفَّ نداءٍ لـ**١١** مقصداً، كلٌّ بمعرّفِه |
| حاجزُ الإقلاعِ | `assertEgressEnvironment(process.env)` في `apps/gateway/src/index.ts` بعدَ تحميلِ الضبطِ وقبلَ `buildContainer` — **والترتيبُ مقيسٌ في اختبارٍ** لا موصوفٌ |
| الحاجزُ الساكنُ كبِرَ ولم يُضعَف | الفحوصُ ١١ و١٢ و١٣ أُضيفَت، ولا فحصَ من ١..١٠ خُفِّفَ ولا حُذِفَ |
| الوثيقةُ مُولَّدةٌ | `docs/wasla/egress-boundary.md` أُعيدَ توليدُها بـ`--write` وزادَها عمودُ «بوّابةُ التشغيلِ» — لم تُحرَّرْ بيدٍ (قاعدةُ 0.6) |

### المواضعُ التِسعةُ الموصولةُ

| الملفُّ | المقصدُ |
|---|---|
| `packages/infrastructure/wasla/core-event-shipper.ts` | `core-events-ingress` |
| `packages/infrastructure/notification/telegram-client.ts` | `telegram-bot-api` (داخلَ ناقلِ grammY نفسِه) |
| `packages/infrastructure/financial/moyasar-provider.ts` | `moyasar-payments` |
| `packages/infrastructure/financial/tap-provider.ts` | `tap-payments` |
| `packages/infrastructure/backup/google-drive-adapter.ts` | `google-drive-backup` (خمسةُ نداءاتٍ) |
| `packages/infrastructure/observability/metrics-exporter.ts` | `metrics-collector` |
| `packages/infrastructure/i18n-translation/translation-providers.ts` | `deepl-translation` · `google-translation` · `mymemory-translation` |
| `packages/maps/providers/osrm/osrm-provider.ts` | `osrm-routing` |
| `packages/infrastructure/redis/upstash.ts` | `upstash-redis-rest` |

وصُحِّحَ في السجلِّ موضعانِ كانا يُشيرانِ إلى ملفٍّ ليسَ فيه الناقلُ
(`telegram-api-sender.ts` و`apps/gateway/src/redis/upstash.ts` وهوَ إعادةُ تصديرٍ)
— تصحيحٌ بالإضافةِ في هذا الدليلِ لا محوٌ لدليلٍ سابقٍ (`ح-8`).

## فرقٌ عن الحجزِ، مُسجَّلٌ لا مسكوتٌ عنه

الحجزُ في `ROADMAP.md` قالَ `packages/infrastructure/egress/egress-gate.ts`. ثمَّ
قِيسَ أنَّ من مُنادِيها `packages/maps` وهيَ لا تستوردُ من `infrastructure` (حدُّ
طبقاتٍ محروسٌ)، فكانَ الخيارُ: خرقُ حدٍّ، أو مقصدٌ يبقى خارجَ البوّابةِ، أو نقلُ
البوّابةِ إلى `shared` — أدنى طبقةٍ يستوردُ منها الجميعُ وموضعُ السجلِّ نفسِه.
واختيرَ الثالثُ.

## القياسُ المحلّيُّ — ليسَ حكماً (`ح-8`)

| الأداةُ | الحاصلُ |
|---|---|
| `bun test` | **3035** نجاحاً · **829** تجاوزاً · **0** إخفاقاً · 10666 `expect()` · 3864 حالةً في 288 ملفّاً |
| `biome check .` | 1114 ملفّاً · لا إصلاحَ |
| `tsc --noEmit` (والتطبيقُ المصغَّرُ) | نظيفٌ |
| `check:egress-boundary` | ١٥ مقصداً · صفرُ مقصدٍ في MARKET · الوثيقةُ مطابقةٌ |
| `check-skip-classification` | 86 ملفّاً · 770 حالةً · دَينٌ واحدٌ مُعلَنٌ كما هوَ |
| `check:migration-matrix` · `check:migration-dry-run` | 47 جدولاً · 6 مِسباراً (لم تُلمَسْ) |
| `check-adr-numbering` | 85 قراراً فريداً قبلَ إضافةِ `0086` |

وزيادةُ الاختباراتِ عن قياسِ `main` (**3008**) هيَ **٢٧** حالةً جديدةً: ١٧ للبوّابةِ
و١٠ للفحوصِ ١١–١٣، وأكثرُها يزرعُ الخرقَ ويقيسُ **الرفضَ** وأنَّ الناقلَ لم يُنادَ.

## حكمُ CI

**قُرِئَ الحكمُ الفعليُّ بالوظيفةِ وبالخطوةِ من الواجهةِ البرمجيّةِ، ولم يُفترَضْ.**
الفرعُ `feat/w6-runtime-egress-gate` عندَ `9669dbf`، الطلبُ
[#8](https://github.com/uxxxug/ceezr/pull/8)، الجولتانِ `34673628192` (دفعٌ) و
`34673661270` (طلبُ سحبٍ)، و«طَرافةُ الخارطةِ» `34673628196` = `success`.

| الوظيفةُ | الحكمُ (الجولتانِ سواءً) |
|---|---|
| `تكامل على PostgreSQL حقيقي` | **`success`** |
| `فوضى متعدد المثيلات (F5-06)` | **`success`** |
| `verify` | **`failure`** — عندَ الخطوةِ ١٨ وحدَها |
| `تكامل على Redis حقيقي` | **`failure`** — الخطوةُ ٨ «اختبارات الجلسات على Redis حقيقي» (`O-2`) |

وفي `verify` خطوةً خطوةً: **٥ Lint `success`** · **٦ Typecheck `success`** ·
**٨ Test `success`** — وفيها تعملُ الحالاتُ الـ٢٧ الجديدةُ — · **١٥ حاجزُ `W-2`
`success`** · **١٦ «حدُّ الصادرِ مُعلَنٌ بالكاملِ ولا مقصدَ في MARKET» `success`**
وهيَ الخطوةُ التي تُشغِّلُ الفحوصَ ١١ و١٢ و١٣ الجديدةَ · **١٧ حاجزُ `W-8`
`success`** · **١٨ «منع أي جدول بلا `city_id`» `failure`** · **١٩–٥٤ `skipped`**.

**فالمقيسُ في هذه الزيادةِ مرَّ عندَ CI لا محلّيّاً وحدَه**، والأحمرانِ هما
حاجزا المالكِ بلا تغيُّرٍ: `O-1` (`DEP-CORE-006` · القاعدةُ 0.4، سببُه الجذريُّ في
CORE) و`O-2` (سرّا Upstash). وكلاهما أحمرُ على `main` عندَ `227cb4d` بالوظيفةِ
والخطوةِ عينِهما، **فهذه الزيادةُ لم تُضِفْ أحمرَ واحداً ولم تُخفِّفْ حاجزاً**.
**ولا يُدَّعى أنَّ الجولةَ خضراءُ، ولا `[x]` للبندِ (`ح-4`).**


## ما يبقى مفتوحاً بعدَ هذه الزيادةِ

1. **نصفُ القناةِ من `W-6`** ما زالَ محجوباً بـ`DEP-CORE-004` — لم يُلمَسْ.
2. **لا إنفاذَ على مستوى الشبكةِ**: شيفرةٌ تستوردُ ناقلاً من مكتبةٍ لا تمرُّ
   بالبوّابةِ، وإغلاقُه يقتضي وكيلاً أو جدارَ نارٍ محجوباً بـ`B-1`/`B-5`.
3. **لا يُقاسُ أنَّ المضيفَ المضبوطَ هوَ CORE حقّاً** — `DEP-CORE-005`.
4. **`O-1`/`DEP-CORE-006`** و**`O-2`** كما هما: لم تُلمَسْ، ولم يُغيَّرْ اختبارُ
   Redis، ولم يُضَفْ سرٌّ إلى المستودعِ.
