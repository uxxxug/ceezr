# F9-02 — Redis حقيقي في CI

**التاريخ**: 2026-09-18
**البند**: F9-02 في `docs/ROADMAP-MASTER.md` §9
**الحالة**: `[x]` — مُنفَّذ ومُثبَت بثلاثِ جولاتٍ خضراءَ متتاليةٍ على `main`

## الأساس

`OPS-006` بُني وأُثبِتَ في `2026-08-30`: وظيفةُ CI ثالثةٌ «تكامل على Redis حقيقي»
تُشغِّل `tests/real-redis/redis-sessions-real.test.ts` بـ`REQUIRE_REAL_REDIS=1` على خادمِ
Redis حقيقيٍّ في الوظيفة. والحاجزُ `scripts/check-real-redis-proof.ts` يقرأُ الدليلَ
ويحكمُ على عشرةِ فحوصٍ مغلقةٍ وحدٍّ أدنى للأوامر (30) وصفرِ مفاتيحَ متروكةٍ.

وكانَ `F9-02` قد تُرِكَ `[ ]` متعمَّداً في `OPS-006` لأنَّهُ «بندُ مرحلةٍ أخرى ولا
يُوسَم من ههنا». والآن توفَّرت ثلاثُ جولاتٍ خضراءَ متتاليةٍ على `main` تُثبِتُ أنَّ
الوظيفةَ تعملُ فعلاً لا تُتخطَّى.

## القياس

ثلاثُ جولاتٍ خضراءَ متتاليةٍ على `main` تُقرأُ بالوظيفةِ لا بالجولةِ (`ح-4`):

| الجولة | التشغيل | الالتزام | الوظيفة | الحكم |
|---|---|---|---|---|
| ١ | `35343609397` | `feat(F12-12)` | تكامل على Redis حقيقي | success |
| ٢ | `35347495021` | `feat(F9-05)` | تكامل على Redis حقيقي | success |
| ٣ | `35367197373` | `feat(F9-04)` | تكامل على Redis حقيقي | success |

وفي الجولاتِ الثلاث:
- `verify` — success
- تكامل على PostgreSQL حقيقي — success
- تكامل على Redis حقيقي — success
- فوضى متعدد المثيلات (F5-06) — success

## ما لا يُدَّعى (`ح-5`)

- **لا يُدَّعى أنَّ انقطاعَ Redis جُرِّب على خادمٍ حقيقيٍّ** — ذاكَ مُثبَتٌ على المزدوجِ
- **لا يُدَّعى أنَّ النقطةَ نقطةُ اختبارٍ** — الدليلُ يُثبِتُ أنَّ خادماً أجاب بسلوكِ Redis
- **لا يُدَّعى أنَّ الكتمَ يمنعُ كلَّ صياغةٍ ممكنةً** — يمنعُ الأنماطَ المعروفةَ فقط
