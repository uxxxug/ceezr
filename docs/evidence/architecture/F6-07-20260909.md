# شاهدُ F6-07 — أولويّاتُ المرورِ في الطابورِ والمُرسِلِ — 2026-09-09

- **الالتزام**: `3ca611d` (الشيفرةُ والهجرتانِ والاختباراتُ والحاجزانِ وADR-0071) على الفرعِ `f6-07` من `a1a99e5` (`main`).
- **Bun**: `1.4.2` محلّيّاً · CI يُثبِّتُ `1.3.14`.
- **الطوبولوجيا**: نسخةٌ واحدةٌ محلّيّاً · **لا محرِّكَ PostgreSQL في بيئةِ التنفيذِ** (لا `docker` ولا `postgres` ولا `psql`) · لا Redis. **حكمُ اختباراتِ التكاملِ من CI وحدَها** — والأخضرُ المحلّيُّ ليسَ بديلاً عن حكمِ CI.
- **نسخةُ الحِمْلِ**: لا شيءَ. **لا رقمَ سَعَةٍ ولا رقمَ زمنٍ يُدَّعى في هذا الشاهدِ** (ADR-0034).
- **حالةُ الدليلِ**: `مُنفَّذ · مُختبَر` — **لا** `مَقيس` ولا `مُثبَت` ولا `VERIFIED` (القسمُ 1.4 و`ح-5`). والبندُ يبقى `[ ]` في الخارطةِ حتّى يستوفيَ `ح-4`.

---

## ١) الحجزُ والملكيّةُ والتعارضُ — ما فُحِصَ قبلَ لمسِ ملفٍ

**لا عُرفَ حجزٍ في المستودعِ**؛ الحجزُ الفعليُّ هوَ الفرعُ والـPR (كما في شاهدِ `F6-06`).

| الفحص | النتيجة |
| --- | --- |
| فروعٌ بعيدةٌ تذكرُ `F6-07` | لا شيءَ |
| طلباتُ دمجٍ تذكرُ `F6-07` | لا شيءَ (`#33`–`#53` مفحوصةً) |
| هجرةٌ أو دالّةٌ سابقةٌ لأولويّةِ النوعِ | لا شيءَ — لا `notification_kind_priority` في المستودعِ ولا في القاعدةِ الاختباريّةِ |
| تبعيّاتُ البندِ (`F6-01`…`F6-06`) | كلُّها `[x]` في الخارطةِ ومدموجةٌ في `main` عندَ `a1a99e5` |
| قيدٌ سياديٌّ يمسُّ البندَ | لا شيءَ — لا قرارَ مالكٍ ولا خدمةَ خارجيّةً يلزمُها التنفيذُ. و`DEC-14` (نسخةٌ واحدةٌ) **لا يمسُّ هذا البندَ**: الترتيبُ حكمُ محرِّكٍ لا حكمُ نسخٍ |
| تعارضٌ مع `PR #53` المفتوحِ (`F5-06`/`DEC-14`) | يمسُّ الخارطةَ و`SYSTEM_STATE` وADR-0070. **فرقمُ ADR ههنا `0071` لا `0070`** لأنَّ `0070` محجوزٌ في ذلكَ الـPR وغيرُ مدموجٍ؛ وسطرُ سجلِّ التنفيذِ أُضيفَ في ذيلِ §25 فلا يُزاحِمُ سطرَ ذاكَ الـPR |

## ٢) ما نُفِّذَ — الشطرانِ اللذانِ يطلبُهما نصُّ البندِ

### شطرُ الطابورِ (`claim_notification_delivery`)

| العنصر | النوع | الموضع |
| --- | --- | --- |
| `TRAFFIC_PRIORITY_CLASSES` · `TRAFFIC_PRIORITY_RANK` | أربعةُ أصنافٍ برتبٍ `1..4` | `packages/shared/config/traffic-priority.ts` |
| `NOTIFICATION_KIND_PRIORITY` | سجلٌّ **مغلقٌ** للأنواعِ الأحدَ عشرَ | المرجعُ نفسُه |
| `priorityClassOfKind` | **المجهولُ ⇒ `critical`** لا `low` | المرجعُ نفسُه |
| `notification_kind_priority(text)` | دالّةٌ `immutable` — `case` على أحدَ عشرَ نوعاً و`else 1` | `20260909150000` |
| `notification_kind_is_deferrable(text)` | `priority >= 3` — لا قائمةً ثانيةً تُصانُ | `20260909150000` |
| `claim_notification_delivery()` | `create or replace` **بالتوقيعِ نفسِه**، والفرقُ سطرُ الترتيبِ وحدَه | `20260909150000` |
| `notification_outbox_priority_due_idx` | فهرسٌ جزئيٌّ متزامنٌ على `(notification_kind_priority(kind), created_at)` | `20260909151000` (طورُ `index`، ملفٌّ منفصلٌ) |

### شطرُ المُرسِلِ (دلوُ المعدّلِ الصادرُ · `TG-001`)

| العنصر | ما تغيّرَ |
| --- | --- |
| `outbound-rate-bucket.ts` | `acquire(scope, key, priority?)` — الافتراضُ `critical`؛ و`limitWithReserve` يضربُ الحدَّ في `lowPriorityShare` **لنطاقِ `global` وحدَه** ولا يبلغُ صفراً أبداً |
| `rate-aware-telegram-sender.ts` | `awaitSlot` يُحوِّلُ أولويّةَ الإرسالِ إلى صنفِ دلوٍ (`bucketClassOfSendPriority`) ويُمرِّرُه إلى النداءَينِ |
| `traffic-priority-sender.ts` | `withTrafficPriority(inner, kind)` يسمُ الرتبةَ من السجلِّ **بلا إلغاءِ أولويّةٍ صريحةٍ** |
| `apps/workers/src/container.ts` | وسمُ العرضِ · دورِ التفاوضِ (ثلاثةُ أنواعٍ) · الدائرةِ الأوسعِ · تعذُّرِ السائقِ · الإلغاءِ · فضِّ النزاعِ — كلٌّ بنوعِه لا بنوعٍ واحدٍ يُعمَّمُ |

### تصحيحٌ مُضافٌ لا محوٌ (`ح-8`)

- **حاجزُ `F6-06` (`scripts/check-queue-backpressure.ts`) كانَ مكتوباً ولا يُشغَّلُ في أيّةِ وظيفةٍ** — لا في سلسلةِ `bun run ci` ولا في `ci.yml`. فوُصِلَ ههنا مع حاجزِ `F6-07`. وحاجزٌ لا يُشغَّلُ ليسَ حاجزاً بل ملفٌّ يُقرأُ؛ والإغفالُ مُعلَنٌ في تعليقِ الخطوةِ نفسِها لا مسكوتٌ عنه.
- **قائمةُ `DEFERRABLE_UNDER_BACKPRESSURE_KINDS` صارت مُشتقّةً** من السجلِّ فاتّسعَت من نوعٍ واحدٍ (`broadcast_recipient`) إلى أربعةٍ (بإضافةِ `no_driver_found` · `dispute_resolution` · `subscription_notice`). والاتّساعُ مُعلَنٌ بتعليقٍ مُضافٍ في موضعِه.
- **وأثرُه على اختبارِ `F6-06`**: `tests/integration/queue-backpressure.test.ts` كانَ يستعملُ `no_driver_found` كـ«حِمْلٍ حرجٍ»، وقد صارَ قابلاً للتأجيلِ — فمقدّمةُ الحالةِ لم تبقَ صحيحةً. فبُدِّلَ النوعُ إلى `negotiation_turn_opened` (حرجٌ) **بتعليقِ تصحيحٍ مُضافٍ**، ولم تُحذَفْ حالةٌ ولا خُفِّفَ حكمٌ.

## ٣) القياسُ — ما جرى محلّيّاً بالأرقامِ

| الفحص | النتيجة |
| --- | --- |
| `bun run lint` | نظيفٌ — 1021 ملفّاً |
| `bun run typecheck` | نظيفٌ (الجذرُ + `apps/miniapp`) |
| `bun test tests/unit` | **2218 ناجحاً · 0 فاشلاً** · 8154 تحقُّقاً · 147 ملفّاً |
| `tests/unit/traffic-priority.test.ts` | **12 ناجحاً** |
| `tests/unit/check-traffic-priority-guard.test.ts` | **11 ناجحاً** (لكلِّ قاعدةٍ من الستِّ حالةٌ سالبةٌ، ومعها المستودعُ الحقيقيُّ يمرُّ) |
| `check-traffic-priority` · `check-queue-backpressure` | خروجُ `0` |
| `check-migration-safety` | خروجُ `0` — 92 هجرةً |
| `check-migrations` · `check-schema-contract` · `check-rollback-safety` · `check-hot-query-index-coverage` · `check-notification-classification` · `check-skip-classification` · `check-adr-numbering` · `check-test-container-lifecycle` | خروجُ `0` |
| `check-skip-classification` بعدَ التسجيلِ | **77 ملفّاً · 672 حالةً** — رفعاً لا تخفيضاً |

**والقياسُ الذي يُحتَجُّ به في شطرِ المُرسِلِ ليسَ محاكياً نكتبُه**: الحالةُ تبني دلوَ معدّلٍ **حقيقيّاً في الذاكرةِ** بحدٍّ معلومٍ وحصّةٍ `0.5`، ثمَّ تُثبِتُ أنَّ الطالبَ المنخفضَ يُمنَعُ عندَ حصّتِه **بينَما الحرجُ ما زالَ يُجازُ بعدَه** — وهوَ عينُ السلوكِ المطلوبِ، لا وصفُه.

## ٤) ما لم يُقَسْ ولا يُدَّعى — حدودٌ مُعلَنةٌ

1. **اختباراتُ التكاملِ لم تُشغَّلْ محلّيّاً.** لا PostgreSQL في بيئةِ التنفيذِ. **والقاعدةُ الاختباريّةُ المتاحةُ متأخّرةٌ عن `main`** — قُرِئَت بالمُجمِّعِ فلا `notification_kind_priority` فيها ولا `queue_backpressure_events` ولا فهارسُ `F7-02`، أي إنَّ هجراتَ `F6-05` وما بعدَها غيرُ مُطبَّقةٍ عليها. **ولم تُهاجَرْ ههنا عن قصدٍ**: طرفُ سلسلتِها المُطبَّقُ غيرُ معلومٍ، والهجراتُ الموروثةُ غيرُ مُسترجَعةٍ (دَينٌ مُعلَنٌ في `scripts/lib/migration-baseline.ts`)، فتطبيقُ ذيلٍ مجهولِ المبدأِ على قاعدةٍ مشتركةٍ **يُفسِدُها ولا يُنتِجُ حكماً أصدقَ** من وظيفةِ CI التي تُطبِّقُ السلسلةَ كلَّها على قاعدةٍ فارغةٍ بالمُطبِّقِ الآمنِ (`F7-07`). فحكمُ الحالاتِ الستِّ من CI.
2. **خطّةُ تنفيذِ المُخطِّطِ لم تُقَسْ** على حجمٍ إنتاجيٍّ: المُثبَتُ **وجودُ** الفهرسِ و**صحّةُ** الترتيبِ، لا أنَّ المُخطِّطَ اختارَه (ADR-0071 §٨ · دَينٌ من صنفِ ADR-0069).
3. **أثرُ الحصّةِ على تسليمٍ حقيقيٍّ عبرَ Bot API** لم يُقَسْ: المُرسِلُ في الاختبارِ مزدوجٌ.
4. **الجوعُ**: الترتيبُ رتبيٌّ صرفٌ بلا حصّةٍ زمنيّةٍ في المُطالِبِ، وما يمنعُ الجوعَ اليومَ ثلاثةُ أمورٍ بنيويّةٍ مُعلَنةٍ في ADR-0071 §٥ لا حصّةٌ مُنفَّذةٌ. **وهذا حدٌّ لا حلٌّ**، ويُعادُ النظرُ فيه يومَ يدخلُ نوعٌ منخفضٌ **دُفعيٌّ** مدى هذا المُطالِبِ.
5. **الطابورُ الوارِدُ** (`telegram_update_jobs`) بلا رتبةٍ — رتبةُ الوارِدِ غيرُ معروفةٍ وقتَ الالتقاطِ (ADR-0071 §٧).

## ٥) ما لم يُمَسّ

**لا حاجزٌ أُسكِتَ · لا اختبارٌ عُطِّلَ أو حُذِفَ · لا مهلةٌ زيدَتْ · لا أرضيّةُ تغطيةٍ خُفِّضَت · لا `force-push` ولا تاريخٌ أُعيدَ كتابتُه · لا نصُّ بندٍ غُيِّرَ (`ح-1`) · لا بندٌ رُقِّمَ أو حُذِفَ (`ح-2`) · لا ADR منشورٌ عُدِّلَ (`ح-6`) · لا رمزُ حالةٍ قُلِبَ.** و`DEC-14` و`R-17` و`F5-06` و`numInstances: 1` تُقرأُ كما هيَ.

## ٦) حكمُ CI

أرقامُ التشغيلِ وخُلاصاتُها مقروءةٌ من الواجهةِ البرمجيةِ (`actions/runs/<id>/jobs`)
ومن سجلّاتِها المُنزَّلةِ، لا من ظنٍّ. **والشوطُ الأخمرُ لا يُمحى بالأخضرِ الذي بعدَه**
(`ح-8`): سلسلةُ الأحكامِ كما جرَت:

| الشوطُ | الالتزامُ | تشغيلُ الدفعِ | تشغيلُ الـPR | `verify` | تكاملُ PostgreSQL | تكاملُ Redis |
|---|---|---|---|---|---|---|
| ١ | `e04ee0c` | `34393076336` | `34393156434` | **فشلَ** | **فشلَ** — 595 ناجحةً · 11 متجاوزةً · **4 فاشلةً** | نجحَ |
| ٢ | `4826cf2` | `34395013243` | `34395019908` | نجحَ | **فشلَ** — 599 ناجحةً · **2 فاشلةً** من 612 في 79 ملفّاً | نجحَ |
| ٣ | `3d0e8bc` | `34396239581` | `34396245300` | نجحَ | **نجحَ** — 602 ناجحةً · 11 متجاوزةً · **0 فاشلةً** من 613 في 79 ملفّاً · 3601 دعوى | نجحَ — 13 حالةً |

### ما قالَه كلُّ حكمٍ ولماذا لم يكُن الأخضرُ المحلّيُّ كافياً

**الشوطُ ١ — أربعُ إخفاقاتٍ، سببانِ جذريّانِ لا عَرَضانِ:**

1. **انقلابُ التتابعِ السببيِّ لطلبٍ واحدٍ.** فشلَت
   `tests/integration/unsubscribed-negotiation.test.ts › «تم الاتفاق» يُسنِد الطلب…`
   لأنَّ الترتيبَ بالرتبةِ وحدَها قدَّمَ `wider_circle_opened` (رتبةُ ٢) على
   `negotiation_agreed` (رتبةُ ٣) **للطلبِ نفسِه**، فوصلَ صاحبَ الطلبِ خبرُ
   «دائرةٍ أوسعَ» بعدَ خبرِ الاتفاقِ. وهذا كشفٌ فعليٌّ لعيبٍ في التصميمِ الأوّلِ
   لا خللٌ في الاختبارِ: **الأولويّةُ بينَ الطلباتِ مطلوبةٌ، وداخلَ الطلبِ الواحدِ
   ممنوعةٌ.** والعلاجُ شرطُ **رأسِ الطلبِ** (`head-of-line`) في المُطالِبِ.
2. **بذرٌ يخرقُ قيودَ الشكلِ في القاعدةِ.** فشلَ ملفُّ الحالاتِ الجديدُ
   `traffic-priority-claim.test.ts` بـ`23514`: صفُّ `broadcast_recipient` خرقَ
   `notification_broadcast_shape`، ثمَّ — في الشوطِ الثاني — صفُّ `offer` خرقَ
   `notification_offer_shape` (يلزمُه ثلاثيُّ العرضِ). فالبذرُ صُحِّحَ إلى أنواعٍ
   من نطاقِ المُطالِبِ نفسِه بلا شكلٍ خاصٍّ، **والدعوى لم تُخفَّف**: الرتبُ الثلاثُ
   ما زالَت تُقاسُ مُرتَّبةً فعلاً.

**الشوطُ ٢ — الشرطُ صحيحٌ ولا يُطبَّقُ على شيءٍ:** حالتا السببيّةِ الجديدتانِ نجحَتا،
لكنَّ اختبارَ القروبِ **بقيَ فاشلاً بالانقلابِ نفسِه**. والسببُ الجذريُّ أنَّ شرطَ رأسِ
الطلبِ يقرأُ عمودَ `notification_outbox.order_id`، و`enqueue_notification` كانَت
تتركُه فارغاً وتضعُ معرّفَ الطلبِ في الحمولةِ وحدَها (`order_id` نصّاً، أو `claim_id`
يُحيلُ إليه عبرَ `unsubscribed_claims`). فكانَ الشرطُ يقيسُ `null = null` فلا يحجُبُ
أحدٌ أحداً. **والعلاجُ إكمالُ العمودِ عندَ الإيداعِ** لأنواعِ دورةِ الرحلةِ وحدَها —
لا قراءةُ حمولةٍ نصّيّةٍ في المُطالِبِ — فيبقى للمُطالِبِ مصدرُ حقيقةٍ واحدٌ مفهرَسٌ،
وتبقى الحمولةُ كما هيَ حرفاً فلا يُمَسُّ قارئوها في العامِلِ. ويُقاسُ هذا العلاجُ
بحالةٍ تاسعةٍ صريحةٍ تُودِعُ عبرَ `enqueue_rider_order_notification` وتقرأُ العمودَ
والحمولةَ معاً.

**الشوطُ ٣ — أخضرُ في الوظائفِ الثلاثِ.** وهذا **حكمُ CI الأوّلُ الأخضرُ** لهذا البندِ،
لا ثالثُ الأخضرِ المُتتابِعِ الذي تطلبُه `ح-4`؛ فحالةُ البندِ تبقى `[ ]` حتّى تتمَّ
ثلاثةُ أشواطٍ خُضرٍ مُتتابِعةٍ على `main` بعدَ الدمجِ المستقلِّ. **ولم يُدمَج أيُّ
Pull Request من الوكيلِ** (`PR #54` مفتوحٌ للمراجعِ المستقلِّ).
