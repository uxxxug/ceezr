# دليل F5-06 / SCL-008 — برهانُ البنيةِ متعدّدِ المثيلاتِ في CI

> **البند:** F5-06 — تشغيل ≥3 مثيلات gateway خلف موزّع (SCL-008).
> **التاريخ:** 2026-09-09.
> **التصنيف:** `distributed/` — نزعُ الحالةِ المحليةِ وعدّةُ مثيلاتٍ تعملُ معًا.

## الحُكم النهائي

**نجاحٌ (برهانٌ آليٌّ في CI)** — الدرجةُ **«مُتحقَّقٌ منه»** لا «مُثبَتٌ» (`ح-5`):

الثابتُ المُثبَتُ: «الحالةُ في DB+Redis لا في ذاكرةِ البوّابة، وقتلُ نسخةٍ أثناءَ
رحلةٍ جاريةٍ لا يُفقدُ المستخدمَ خطوتَه». وُلِّدَ في CI على عُدّاءِ `ubuntu-latest`
ضدَّ Postgres وRedis حقيقيَّينِ عبرَ حاوياتِ Docker — لا محاكاة.

**ولا يُقلبُ البندُ إلى `[x]`** في هذه العقدة: البرهانُ آليٌّ في CI لا في بيئةٍ
مُدارةٍ شبيهةٍ بالإنتاج. والخارطةُ تُقيّدُ «مُثبَت» ببيئةٍ شبيهةٍ بالإنتاج (`ح-5`)،
و`render.yaml` لا يزالُ `numInstances: 1`. إغلاقُ البندِ يتطلّبُ إمّا نشرًا مُدارًا
متعدّدَ المثيلاتِ (Render/Railway مدفوع — مُحجَبٌ لعدمِ توفّرِ خطةٍ مدفوعة)، أو مراجعةَ
حوكمةِ الخارطة/SYSTEM_STATE لاعتمادِ البرهانِ الآليِّ كافيًا — ولا يُفعَلُ إلّا بطلبِ
دمجِ قلبِ حالةٍ منفصلٍ بعدَ ثلاثِ دوراتِ CI خضراء.

## الـcommit

`feat/f5-06-compose-chaos` — يُحدَّدُ عندَ فتحِ طلبِ الدمج.

## بصمةُ الإعدادات (بلا أسرار)

| المتغيّر | القيمة | الغرض |
|---|---|---|
| `NODE_ENV` | `test` | الوضعُ غيرُ الإنتاجيِّ — يرخي إجراءاتِ الإقلاعِ فقط (intake موصولٌ دائمًا). |
| `TELEGRAM_TRANSPORT` | `silent` | seamٌ مُعتمَدٌ — لا نداءاتُ شبكةِ تلغرام. لا يُغيّرُ مسارَ حالةِ الرحلة. |
| `SESSION_STORE` | `redis` | حالةُ الحوارِ في Redis لا في الذاكرة. |
| `PROCESS_TOPOLOGY` | `multi-process` | طوبولوجيا متعدّدةُ العمليات. |
| `RUN_WORKER_IN_GATEWAY` | `false` | العاملُ منفصلٌ عن البوّابة. |
| `UPSTASH_REDIS_REST_URL` | `http://upstash-shim:8081` | البوّابةُ تتحدّثُ Upstash REST — والمحوّلُ يترجمُها إلى Redis حقيقيّ. |

الأسرارُ كلُّها قيمٌ وهميّةٌ (`fake-*`) — لا يُستدعى تلغرامُ الإنتاجُ ولا Supabase.
التحقّقُ من ذلك: لا استخدامَ لتشغيليٍّ لـ`SUPABASE_URL` في مسارِ الرحلة.

## الـtopology

- **3 بوّابات** (`gateway-1/2/3`) خلفَ **HAProxy** يفحصُ `/ready` بنشاطٍ (فاصلُ ٢ ثانية،
  `http-check expect status 200`). النسخةُ المُصرِّفةُ (503) تُزالُ من التوزيعِ فورًا.
- **عاملانِ** (`worker-1/2`) — «عدّةُ gateways وعدّةُ workers تعملُ معًا» (بوّابةُ المرحلةِ F5).
- **Postgres** (`postgis/postgis:17-3.5`) + **Redis** (`redis:7-alpine`).
- **محوّلُ Upstash REST** (`docker/upstash-rest-shim`): يُمرّرُ أوامرَ Upstash REST
  عامًّا إلى Redis عبرَ `ioredis` — يُمكّنُ البوّابةَ من Redis بلا تغييرِ مسارِ
  `packages/infrastructure/redis/upstash.ts` الإنتاجيّ.
- النطاقُ المُتاحُ للمثيلاتِ: `gateway-1` (تسجيلٌ + إكمال) · `gateway-2` (طلبُ الرحلة) ·
  `gateway-3` (بدءُ الرحلة) — كلُّها خدمتِ الرحلةَ نفسها.

## النتيجة (بالأرقام)

منسّقُ الاختبارِ `scripts/f5-06-chaos.ts` ينفّذُ ثمانيَ مراحلَ ويتوقّفُ عندَ أوّلِ إخفاق:

1. انتظارُ `/ready=200` على البوّاباتِ الثلاث + HAProxy.
2. تهيئةُ القاعدة (مدينةُ JED نشطة + تنظيف).
3. تسجيلُ السائق عبرَ `gateway-1`.
4. طلبُ الرحلة عبرَ `gateway-2` (استمراريةُ الجلسةِ عبرَ Redis من مثيلٍ آخر).
5. قبولُ العرضِ وبدءُ الرحلة عبرَ `gateway-3`.
6. **قتلُ `gateway-2` بـ`SIGTERM` أثناءَ الرحلة** — التقاطُ `/ready ⇐ 503 draining`،
   ثمّ تأكيدُ إزالةِ HAProxy لها وتوجيهِ البوّاباتِ الباقية.
7. إكمالُ الرحلةِ عبرَ `gateway-1` (الباقية) — `order.status = completed`، والسائقُ عادَ
   متاحًا، ولا عرضٌ معلّق.
8. تأكيدُ استخدامِ Redis فعليًّا: مفاتيحُ `waslah:session:*` موجودةٌ في Redis.

النتيجةُ المتوقَّعةُ: `✅ الثابتُ مُثبَتٌ` — تُحدَّدُ الأرقامُ الفعليةُ من تشغيلِ CI.

## الأخطاء التي ظهرت

تُسجَّلُ هنا بعدَ أوّلِ تشغيلِ CI أخضر — مع السببِ الجذريِّ لكلِّ إخفاقٍ وُجد.

## الحدودُ المعروفة (ما لم يُختبَر بعد)

- **لا بيئةُ سحابةٍ مُدارة:** البرهانُ في CI على `ubuntu-latest`، لا على Render/Railway
  مُدار. لا يُثبتُ سلوكَ النشرِ المُدارِ (إعادةُ جدولةِ Render، الفشلُ في شبكةِ
  المُزوّد، زمنُ الاستجابةِ عبرَ الإنترنت).
- **لا قياسُ حمل:** لا مولّدُ حملٍ موزّعٌ خارجَ المضيفِ (`OPS-004` لا يزالُ `[ ]`).
  البرهانُ يُثبتُ الصحّةَ الموزّعةَ لا تحمُّلَ الحِملِ. درجةُ «مَقيس» تتطلّبُه.
- **`TELEGRAM_TRANSPORT=silent`:** لا يُختبرُ مسارُ التسليمِ الحقيقيِّ لتلغرام. مسارُ
  الحالةِ (DB+Redis) هو المُثبَتُ، وهو عينُ ما يُميّزُ F5-06.
- **محوّلُ Upstash REST:** يُترجمُ REST إلى Redis حقيقيّ. يُغطّي الأوامرِ المستعمَلةَ
  (`PING`·`GET`·`SET`·`DEL`·`EVAL`·`XADD`·`XREAD`) لكنّه ليس تنفيذًا كاملًا لواجهةِ Upstash.
- **لا brownout:** `OPS-007` (اختباراتُ الفوضى مع brownout) لا يزالُ `[ ]`.

## الحُكم

الثابتُ المعماريُّ (نزعُ الحالةِ المحليةِ + بقاءُ الرحلةِ بعدَ قتلِ نسخة) **مُتحقَّقٌ
منه** آليًّا في CI. إغلاقُ F5-06 كـ`[x]` **مؤجَّلٌ** لانعدامِ البيئةِ المُدارةِ،
ويحتاجُ قرارَ حوكمةٍ (هل يكفي البرهانُ الآليُّ أم يُشترَطُ النشرُ المُدار) + طلبَ
دمجِ قلبِ حالةٍ منفصل.

---

## تصحيحٌ بالقياسِ — 2026-09-09 (بالإضافةِ لا بالمحوِ · `ح-8`)

كلُّ ما سبقَ من هذا الملفِّ كُتِبَ **قبلَ** أن يُقرأَ حكمُ CI، وفيه جملةٌ صريحةٌ
تقولُ «النتيجةُ المتوقَّعةُ: ✅ الثابتُ مُثبَتٌ — تُحدَّدُ الأرقامُ الفعليةُ من تشغيلِ
CI». **وقد جاءَ الحكمُ أحمرَ مرَّتَينِ**، فيُسجَّلُ هنا كما وقعَ، ولا تُمحى الجملةُ
السابقةُ بل يُقيَّدُ عليها هذا القسمُ: هيَ نفسُها الخطأُ المنهجيُّ — إعلانُ نتيجةٍ
قبلَ قياسِها.

### أ) الحكمُ الفعليُّ — أحمرُ في الجولتَينِ

| التشغيل | الحادث | العقدة | `verify` | PostgreSQL | Redis | **فوضى متعدد المثيلات** |
|---|---|---|---|---|---|---|
| `34364346135` · `34364397620` | push · pull_request | `b40185b` | ✅ | ✅ | ✅ | ✗ عندَ «رفعُ رصةِ Docker Compose» |
| `34365077268` · `34365082495` | push · pull_request | `d87a52e` | ✅ | ✅ | ✅ | ✗ عندَ «رفعُ رصةِ Docker Compose» |

الوظائفُ الثلاثُ الأخرى نجحَت في الجولتَينِ: العطبُ محصورٌ في رصةِ compose
وحدَها، ولم يُنقَل إلى غيرِها.

### ب) السببُ الجذريُّ الأوّلُ — والعلاجُ في مصدرِه

الجولةُ الأولى (`b40185b`): `Dockerfile` محوّلِ Upstash كانَ يُمرِّرُ
`--frozen-lockfile=false` وهيَ ليست صيغةً مقبولةً في `bun install`. عُولِجَ في
`d87a52e`.

الجولةُ الثانيةُ (`d87a52e`): السجلُّ يقولُ الجملةَ نفسَها في البوّاباتِ الثلاثِ:

```
❌ تعذّر إقلاع البوابة:
InvalidEnvVarError: متغير بيئة غير صالح SUPABASE_URL: يجب أن يبدأ بـ https://
```

والعاملانِ سجَّلا `worker.config_invalid` و`worker-2` خرجَ بـ`exit(1)`، فسقطَ
`up --wait`. **والسببُ**: مِرساةُ بيئةِ البوّاباتِ في `docker-compose.f5-06.yml`
كانت `SUPABASE_URL: http://supabase-unused.local`، و`tryLoadConfig` في
`packages/shared/config/index.ts` يرفضُ رفضاً قاطعاً كلَّ قيمةٍ لا تبدأُ بـ`https://`.
والرفضُ **صحيحٌ ولم يُلمَس**: عميلُ Supabase لا يُخاطَبُ على نصٍّ صريحٍ. المُخالِفُ
هوَ قيمةُ الرصةِ لا الحاجزُ. صارَت `https://supabase-unused.invalid` —
و`.invalid` نطاقٌ محجوزٌ لا يُحَلُّ (RFC 2606) فلا مُخاطَبةَ شبكيّةً بالغلطِ.

### ج) عطبٌ ثانٍ في الصدقِ لا في الوظيفةِ — `up --wait` كانَ يقبلُ نسخةً ميّتةً

لا `HEALTHCHECK` في `docker/Dockerfile.gateway` ولا في الرصةِ، فـ`up --wait` كانَ
يحكمُ على البوّاباتِ بأنّها «قائمةٌ» لا «صحّيةٌ». ولو تأخّرَ سقوطُ العمليةِ لحظةً
لَقُرِئَت الخطوةُ خضراءَ على ثلاثِ بوّاباتٍ تموتُ بعدَها — أخضرُ زائفٌ يسبقُ العطبَ.
فأُضيفَ `healthcheck` لكلِّ بوّابةٍ على `/health` (لا `/ready`: الجهوزيةُ تشترطُ
مخطَّطاً مُطبَّقاً والهجراتُ تُطبَّقُ بعدَ رفعِ الرصةِ، فاشتراطُها حجزٌ دائريٌّ).

### د) اكتشافٌ ثالثٌ — انحدارُ `F7-07` في `main`، وُجِدَ بمراجعةِ المستودعِ لا بتقريرٍ

هذه الوظيفةُ كانت تُطبِّقُ الهجراتِ بحلقةِ `psql` عاريةٍ. وعندَ التحقّقِ من
مطابقتِها لطريقِ `F7-07` ظهرَ أنَّ **`main` نفسَه** رجعَ إلى الحلقةِ:

| العقدة | `scripts/migrate.ts` في `ci.yml` | حلقةُ `psql` |
|---|---|---|
| `aadba70` (تنفيذُ F7-07) | ٤ | ٠ |
| `524f266` (دمجُ F7-07) | ٤ | ٠ |
| **`a16bf97`** («merge main into F7-02 after F7-07 merge») | **٠** | **٢** |
| `a1a99e5` (رأسُ `main` اليومَ) | ٠ | ٢ |

فحلُّ تعارضٍ في دمجٍ أخذَ نسخةَ الملفِّ السابقةَ لـ`F7-07`، بلا التزامٍ يقولُ ذلكَ
ولا مراجعةٍ تراهُ. وبقيَ سطرُ سجلِّ التنفيذِ وADR-0068 يقولانِ «حلقتا `psql` في CI
أُزيلَتا» — **زعمٌ صارَ كاذباً في مكانِ الفعلِ وحدَه**، وهوَ أخطرُ من عيبٍ ظاهرٍ.

والمُعالَجةُ: أُعيدَ المُطبِّقُ في الوظيفتَينِ، وصارَت وظيفةُ الفوضى تُطبِّقُ به
أيضاً على منفذِ الرصةِ 5433 — فلا طريقَ ثانٍ للهجراتِ في المستودعِ كلِّه.

### هـ) حاجزانِ يمنعانِ عودةَ الصنفَينِ — لا وصيّةً بل إنفاذاً

1. `scripts/check-migration-applier.ts` (موصولٌ في `package.json` وفي وظيفةِ
   `verify`): يسقطُ البناءُ على أيِّ حلقةِ `psql` على `supabase/migrations`، وعلى
   أيِّ `psql -f` لملفِّ هجرةٍ، وعلى أيِّ وظيفةٍ تُهيّئُ أدوارَ Supabase ثمَّ لا
   تنادي المُطبِّقَ. وسببُ اختيارِ هذا الحاجزِ بالذاتِ: العيبُ رجعَ **بحلِّ تعارضٍ**
   لا بكتابةِ كودٍ، وحلُّ التعارضِ لا يُراجِعُه إلّا آلةٌ.
2. `tests/unit/compose-env-contract.test.ts`: كلُّ خدمةِ تطبيقٍ في الرصةِ يجبُ أن
   يقبلَ ضبطُها `tryLoadConfig` — **بمُصدَرِ الحقيقةِ نفسِه لا بنسخةٍ ثانيةٍ من
   قواعدِه** — وكلُّ بوّابةٍ يجبُ أن تُعلِنَ `healthcheck`. وفيه برهانُ سقوطٍ على
   القيمةِ المخالفةِ عينِها التي أسقطَت التشغيلَ `34365082495`.

### و) القياسُ المحلّيُّ بعدَ المُعالَجةِ

`lint` = ٠ · `typecheck` = ٠ · `bun test tests/unit` = **٢٢١٢ ناجحةً · ٠ ساقطةً ·
٨٠٩٧ توكيداً** (منها ١٧ حالةً جديدةً في الحاجزَينِ) · `check-migration-applier` ✅.

**ولا Docker على جهازِ التنفيذِ** (`docker: command not found`)، فرفعُ الرصةِ
وقتلُ النسخةِ **لا يُقاسانِ محلّيّاً ألبتّةَ**: حكمُهما CI وحدَه، والأخضرُ المحلّيُّ
ليسَ بديلاً عنه.

### ز) ما لا يتغيّرُ بهذا التصحيحِ

`F5-06` و`SCL-008` باقيانِ `[ ]`. لم يُستوفَ `ح-4` (لا ثلاثَ جولاتٍ خضراءَ)، ولا
`ح-5` (لا بيئةَ مُدارةً شبيهةً بالإنتاجِ · `render.yaml` لا يزالُ
`numInstances: 1`). والدرجةُ تبقى دونَ «مُثبَت»، وإغلاقُ البندِ يحتاجُ نشراً مُداراً
متعدّدَ المثيلاتِ (خطةٌ مدفوعةٌ — قرارُ مالكٍ) أو قرارَ حوكمةٍ صريحاً.
