# سجلُّ الديونِ التقنيّةِ — تدقيقٌ خارجيٌّ

**التاريخ:** 2026-09-17
**المصدرُ:** تقريرا تدقيقٍ خارجيَّانِ مرفقانِ من المالكِ، مطابقانِ مع `main`@`832e4a3`
**المنهجيّةُ:** كلُّ ادّعاءٍ قُوبِلَ بالقراءةِ المباشرةِ على `main` الحاليِّ — لا يُعتمَدُ رقمٌ أو وصفٌ من التقريرِ إلا بعدَ التحقُّقِ.

**قاعدةُ الإغلاقِ (ح 0-9):** لا يُغلقُ دينٌ يتعلَّقُ بالمالكِ (D-02 · D-03 · D-05 · D-07 · D-08 · D-22) إلّا بعدَ تصريحٍ صريحٍ من المالكِ بالإغلاقِ أو التجاوزِ. والديونُ غيرُ المرتبطةِ بالمالكِ (D-01 · D-06 · D-09 · D-10 · D-11 · D-15 · D-16 · D-19 · D-20 · D-21) تُغلَقُ بالأدلّةِ الآليّةِ وحكمِ CI.

---

## مصفوفةُ التحقُّقِ

| # | الادّعاء | الحالة | الدليل | البند/القرار المرتبط | الأولوية | عملٌ repo-local؟ |
|---|---|---|---|---|---|---|
| D-01 | تعدُّدُ مساراتِ إنشاءِ الطلب (`rider-dialog.ts` يستعملُ `OrderWriter.create` المباشرَ مقابلَ `RideRequestCommand` في `request-ride.ts`) | **مُغلَقٌ** | `fix/d-01-unify-order-authority` → PR #84: `createOrderAndMatch` و`requestDelivery` يُنشِئانِ عبرَ `RideRequestCommand.create()` · `OrderCancellationPort` منفصلٌ · `ACTIVE_RIDE_EXISTS` يُعالَجُ صراحةً · حاجزُ `check-ride-request-contract.ts` القاعدةُ ٤ توسَّعَتْ · اختباراتٌ سلبيّةٌ (ح-7) · استعادةُ فحصِ `ORIGIN_OUTSIDE_SERVICE_AREA` المُسقَطِ في هجرةِ D-01 · `seedCapableDriver` في 6 ملفّاتِ تكاملٍ · مناطقُ خدمةٍ للمدنِ الخمسِ · `update_id` في اختباراتِ e2e · CI أخضرٌ بالكامل (verify · PostgreSQL · Redis · F5-06 · roadmap) | — | P0 | نعم — تمَّ التنفيذُ |
| D-02 | قراراتُ المالكِ المحجوزةُ (DEC-11/14/16/17 + ADR 0099) تمنعُ التقدُّمَ في 5 مناطق | **مُغلَقٌ جزئيًّا** | DEC-11 مُغلَقٌ بقرارِ المالكِ (2026-09-17): أجرةٌ تقديريّةٌ بين الراكبِ والسائقِ · اشتراكٌ 150/150/300 · بوابةُ دفعٍ مؤجَّلةٌ 10 أيامٍ قبلَ نهايةِ الشهرِ الأول · `DEC-11-20260917-owner-fare-policy.md` · هجرةُ `20260917140000` · DEC-14/16/17 + ADR 0099 ما زالت مفتوحةً | DEC-11/14/16/17 | P0 | **DEC-11 مُغلَقٌ · الباقي مفتوح** |
| D-03 | `render.yaml` يُعلنُ `numInstances: 1` لكلِّ خدمةٍ، والاختبارُ يُثبتُ الرفضَ لا التشغيلَ المتعدِّدَ | **مُغلَقٌ** | DEC-14 مُغلَقٌ بقرارِ المالكِ (2026-09-09): إبقاءُ `numInstances: 1` وتأجيلُ `F5-06` صراحةً · `DEC-14-single-instance-deferral-20260909.md` · ADR 0050 باقٍ نافذاً | F5-06 · DEC-14 | P0 | **مُغلَقٌ بقرارِ المالكِ** |
| D-04 | لا يوجدُ إثباتُ أداءٍ أو سعةٍ على بيئةٍ مماثلةٍ للإنتاج | **مؤكَّد** | F9-01…F9-06 كلُّها `[ ]` · F10-01…F10-06 كلُّها `[ ]` | F9 · F10 | P0 | لا — بنيةٌ تحتيّة |
| D-05 | المنظومةُ المراقبيّةُ غيرُ مكتملةٍ (لا OTel collector ولا لوحاتٌ ولا تنبيهاتٌ ولا SLO/RTO/RPO) | **مؤكَّد** | F8-01 `[~]` (DEC-17) · F8-02 `[~]` · F8-07 `[ ]` | F8-01/02/07 · DEC-17 | P1 | لا — بنيةٌ تحتيّة |
| D-06 | `SEC-10` RLS جزئيٌّ (`force RLS = 0`، والاتصالُ كـDB owner يتجاوزُ RLS) | **مُؤكَّد — محجوزٌ بـADR 0006** | `scripts/lib/security-controls-registry.ts`: SEC-10 = `partial` · `docs/evidence/security/SEC-10-20260917.md`: 68/69 جدولًا عليها RLS، استثناءٌ واحدٌ (`spatial_ref_sys`) · **محجوزٌ بـADR 0006**: يرفضُ `force RLS` بلا سياساتٍ (يوقفُ التطبيقَ) ويرفضُ كتابةَ سياساتٍ لا يمرُّ بها اتصالٌ (أمنٌ ورقيٌّ) · الحارسُ `check-row-security-condition.ts` يمنعُ `force RLS` ما لم يُسجَّلْ في `POLICY_EXCEPTIONS` (فارغٌ) · التغييرُ يتطلَّبُ تغييرَ نموذجِ الاتصالِ (بنيةٌ تحتيّةٌ) أو تجاوزَ ADR 0006 (قرارُ مالكٍ) | SEC-10 · F8-08 · ADR 0006 | P1 | **لا — محجوزٌ بـADR 0006** |
| D-07 | `SEC-15` (حداثةُ عقودِ CORE) غيرُ قابلٍ للتحقُّقِ آليًّا من CI | **مؤكَّد** | `scripts/lib/security-controls-registry.ts`: SEC-15 = `partial` — المستودعُ الخاصُّ المصدرُ لا يُقرأ من CI | SEC-15 · F8-08 | P1 | لا — يحتاجُ صلاحيّةً آمنةً أو artifact |
| D-08 | `SEC-16` (إدارةُ الأسرارِ) غيرُ مبنيٍّ | **مُجلَد — قرارُ مالكٍ** | SEC-16 = `not-built` · قرارُ المالكِ (2026-09-17): تأجيلٌ مؤقتٌ بلا بديلٍ قائمٍ · `DEC-16-20260917-secrets-deferral.md` · الأسرار تبقى في متغيرات بيئة Render | SEC-16 · F8-08 · O-6 | P1 | **مُجلَد بقرارِ المالكِ** |
| D-09 | لا يوجدُ فحصُ ثغراتِ اعتمادياتٍ آليٌّ (Dependabot/Trivy/Snyk) | **مُغلَقٌ جزئيًّا** | `.github/dependabot.yml` يُغطّي npm (جذرٌ + miniapp) وgithub-actions وdocker · جدولةٌ أسبوعيّةٌ · PR ينتظرُ المراجعةَ · بقيَ Trivy/Snyk لفحصِ الصورِ العميقِ | — | P1 | نعم — تمَّ التنفيذُ |
| D-09 (إضافةٌ — 2026-09-17) | **الفحصُ يعملُ ولا يُثمِرُ دمجاً**: Dependabot لا يُحدِّثُ `bun.lock`، فكلُّ طلبٍ يفتحُه يسقطُ في `bun install --frozen-lockfile` قبلَ أيِّ حاجزٍ — ستةُ طلباتٍ (#90…#95) حمراءُ بسببٍ واحدٍ | **مُغلَقٌ جزئيًّا — السببُ الجذريُّ مُعالَجٌ آليًّا** | ADR 0142 · `docs/evidence/toolchain/D-09-20260917.md` · `.github/workflows/dependabot-lockfile.yml` يُعيدُ توليدَ القفلِ على فروعِ `dependabot/**` ويتحقّقُ أنَّ المتغيّرَ هوَ القفلُ وحدَه ثمَّ يُجمِّدُه ويدفعُه ويستدعي CI · `workflow_dispatch` في `ci.yml` لأنَّ دفعةَ `GITHUB_TOKEN` لا تُشعِلُ زناداً · **ولم يُخفَّفْ `--frozen-lockfile`** · `typescript@7.0.2` دُمِجَ بقياسٍ (`typecheck`/`lint`/`build`/ميزانيةُ الأداءِ/`bun test` كلُّها `0`) · **دورةُ السيرِ الكاملةُ لم تُقَسْ بعدُ** (تنتظرُ أوّلَ دفعةٍ من Dependabot) | D-23 | P1 | نعم — تمَّ التنفيذُ |
| D-10 | `actions/checkout@v4` يستعملُ Node.js 20 (منتهيُ الدعمِ) | **مُغلَقٌ** | حُدِّثَتْ جميعُ الإجراءاتُ في `.github/workflows/*.yml`: `actions/checkout@v7` · `actions/upload-artifact@v7` · `actions/setup-node@v7` · `oven-sh/setup-bun@v2` (حاليٌّ) · Dependabot (D-09) يُبقيها محدَّثةً | — | P2 | نعم — تمَّ التنفيذُ |
| D-11 | تباينُ إصدارِ Bun بين CI (1.3.14) وlocal (1.4.2) | **مُغلَقٌ** | وُحِّدَ الإصدارُ إلى 1.4.2 في كلِّ المواضعِ: `ci.yml` (4) · `core-contract-freshness.yml` · `db-rtt-diagnostic.yml` · `roadmap.yml` (كانَ `latest`) · `docker/upstash-rest-shim/Dockerfile` (كانَ 1.3.14) · `@types/bun: 1.4.2` (كانَ `latest`) · `engines: >=1.4.2` (كانَ `>=1.1.0`) | — | P2 | نعم — تمَّ التنفيذُ |
| D-12 | `vendored-pin-follows-bytes.test.ts` يفشلُ محليًّا وينجحُ في CI | **غيرُ متكرِّرٍ حاليًّا** | التشغيلُ المحلّيُّ على `main`@`832e4a3`: 14 نجاح / 0 فشل. التقريرُ الخارجيُّ ذكرَ فشلًا في بيئةٍ مختلفة | — | P2 | لا — غيرُ متكرِّرٍ |
| D-13 | لا يوجدُ سجلُّ هجراتٍ مُطبَّقةٍ (migration ledger) في القاعدة | **مؤكَّد** (دينٌ موثَّقٌ مقصودٌ) | `scripts/migrate.ts` يُصرِّحُ: «ولا سجلَّ هجراتٍ مُطبَّقةٍ في القاعدةِ» — السببُ: القاعدةُ السياديّةُ 0.4 تفرضُ `city_id` على كلِّ جدولٍ، وسجلُّ الهجراتِ ليسَ بياناً مدينيّاً. ADR 0068 يوثِّقُ القرارَ. المعوَّضُ: استرجاعٌ مفروضٌ + `--from` | F7-07 · ADR 0068 | P2 | لا — قرارٌ موثَّقٌ مقصودٌ |
| D-14 | `JOB_INTERVALS.everySeconds` يُقرأُ مرّةً عندَ البناءِ لا في كلِّ طلبٍ | **مؤكَّد** (دينٌ موثَّقٌ مقصودٌ) | `apps/workers/src/container.ts:235`: «يقرأُ `everySeconds` مرّةً عندَ البناءِ» — التعديلُ في القاعدةِ لا يُتبَعُ حتّى إعادةِ التشغيل | — | P2 | نعم — لكنَّهُ تنازلٌ مقصودٌ |
| D-15 | ~221 ملفًّا بنمطِ `export {};` (scaffold sprawl) | **مُغلَقٌ** | حاجزُ `scripts/check-scaffold-sprawl.ts` يفحصُ 222 ملفَّ قالبٍ فارغٍ ويتحقَّقُ أنَّ لا استيرادَ منها في الإنتاجِ · مُضافٌ إلى CI · سالباتٌ مزروعةٌ (ح-7) | — | P3 | نعم — تمَّ التنفيذُ |
| D-16 | `check-rate-limit-coverage.ts` موجودٌ في CI بلا اسمٍ في `package.json` | **مُغلَقٌ** | أُضيفَ `check:rate-limit-coverage` script مسمّىً في `package.json` · يُشغَّلُ ضمنَ `ci` ويُمكنُ تشغيلُهُ مستقلًّا | — | P3 | نعم — تمَّ التنفيذُ |
| D-17 | رسالةُ `driver-dialog` الخاطئةُ (الفائزُ يُبلَّغُ خسارةَ) | **غيرُ مؤكَّدٍ — أُصلِحَ** | BUG-008 `[x]` — أُغلِقَ 2026-09-03: `claim_ride` صارَ يميِّزُ `duplicate = true` من `ORDER_NOT_CLAIMABLE`، والفائزُ يرى `driver.offer_accepted` | BUG-008 | — | لا — مُغلَقٌ |
| D-18 | إزالةُ تكرارِ `update_id` في Telegram غيرُ مكتملة | **غيرُ مؤكَّدٍ — أُصلِحَ** | BUG-002 `[x]` — أُغلِقَ ضمنَ F6-01 `[x]`: `claim_telegram_update` الذرّيُّ + ingest نحيفٌ + طابورٌ + ACK سريع. `update-dedup.ts` يُصرِّحُ أنَّهُ ليسَ حلَّ الإنتاجِ النهائيَّ، لكنَّ BUG-002 وF6-01 مغلقان | BUG-002 · F6-01 | — | لا — مُغلَقٌ |
| D-19 | `SYSTEM_STATE.md` يحملُ snapshotًا قديمًا (عداداتُ اختباراتٍ/ملفاتٍ) | **مُغلَقٌ** | مُحدَّثٌ: 4720 اختباراً · 282 ملفَّ اختبارٍ · 1572 ملفَّ مصدرٍ · 222 ملفَّ قالبٍ · 8 ديونٍ مُغلَقةٌ (D-01/09/10/11/15/16/20/21) | — | P3 | نعم — تمَّ التنفيذُ |
| D-20 | `F8-09` البوّابةُ غيرُ محدَّدة (لا دليلٌ ولا سكربتٌ ولا ADR) | **مُغلَقٌ** | ADR 0141 يُعرِّفُ البوّابةَ · `docs/evidence/correctness/F8-09-20260917.md` دليلٌ · الحُكَّامُ الآليُّون قائمون (`check-skip-classification.ts` + `check-no-skipped-tests.ts`) | F8-09 | P3 | نعم — تمَّ التنفيذُ |
| D-21 | `deferred/` يحتوي تجاربَ وتكاملاتٍ غيرَ مُدمَجة | **مُغلَقٌ** | المراجعةُ والتصنيفُ قائمانِ: حاجزُ `check-deferred-area.ts` يمنعُ الاستيرادَ والتطبيقَ ويُلزِمُ README لكلِّ مجلَّدٍ · 2 مجلَّدانِ (`core-integration` و`field-experiments`) · 6 READMEs · 6 هجراتٍ مؤجَّلةً بلا توأمٍ · ADR 0094/0095/0097 | O-7 | P3 | نعم — تمَّ التنفيذُ |
| D-22 | لا يوجدُ رحلةُ راكبٍ/سائقٍ كاملةٌ مُثبَتةٌ على جهازٍ حقيقي | **مؤكَّد** | F1-09 `[~]`: 5 من 8 صفوفٍ تحتاجُ متصفّحًا/جهازًا · F2-04…F8 جزئيّة | F1-09 · F2 | P1 | لا — يحتاجُ جهازًا/متصفّحًا |
| D-23 (مُكتشَفٌ — 2026-09-17) | **هجرةُ الجامعِ إلى Rolldown مؤجَّلةٌ**: `vite@8` يستبدلُ Rollup بـRolldown و`@vitejs/plugin-react@6` يشترطُ `vite ^8` — والاثنانِ صفقةٌ واحدةٌ. وقد قِيسَ أثرُهما فسقطَت بوّابةُ ميزانيةِ الأداءِ: **صفرُ حزمةٍ مؤجَّلةٍ** (حزمةُ السائقِ تُنزَّلُ للراكبِ) · 10 طلباتٍ لأوّلِ رسمٍ والحدُّ 6 · 199.4 KB حملاً أوّلَ | **مؤجَّلٌ بقرارٍ مكتوبٍ — مُقيسٌ لا مُفترَضٌ** | ADR 0142 §٥ · `docs/evidence/toolchain/D-09-20260917.md` §٤ (القياسُ الكاملُ · الضبطُ السالبُ الذي حدَّدَ السببَ في طبقةِ توافقِ `manualChunks` · هجرةٌ مُجرَّبةٌ إلى `codeSplitting.groups` تُعيدُ التأجيلَ وتُبقي طلباً سابعاً من حزمةِ `runtime.js` القسريّةِ · ثلاثُ محاولاتٍ فاشلةٍ مُسجَّلةٍ كي لا تُعادَ) · `ignore` للإصدارَينِ الرئيسَينِ في `dependabot.yml` بقرارٍ مكتوبٍ · **لا ميزانيةَ رُفِعَت ولا جدولَ حِزَمٍ وُسِّعَ ولا حاجزَ عُطِّلَ** | F1-09 · F1-10 · ADR 0044/0045 · القسمانِ 9.4 و9.9 | P2 | نعم — عملٌ repo-local (يحتاجُ بندَ خارطةٍ مستقلًّا لا طلبَ تحديثِ اعتماديةٍ) |
| D-24 (مُكتشَفٌ — 2026-09-17) | **سلسلةُ `package.json → ci` بلا حاكمٍ يُثبِتُ صلاحيّتَها**: CI لا تُشغِّلُ السلسلةَ، وحاجزُ `ADR 0143` يقيسُ عضويّةَ الحاجزِ في الجانبَينِ لا أنَّه **يعملُ** حينَ يُنادى. فأمرٌ يُدرَجُ بوسيطٍ ناقصٍ يبقى أخضرَ في كلِّ مكانٍ | **مُغلَقٌ جزئيًّا — حاجزٌ ساكنٌ قائمٌ (ADR 0144)** | كُشِفَ بعطبٍ حقيقيٍّ: `check-no-skipped-tests.ts` أُدرِجَ في السلسلةِ بلا وسيطٍ فكانَ يُخفِقُ (خروج `1`) و**دُمِجَ خضراءَ** لأنَّ CI لا تُشغِّلُ السلسلةَ · صُحِّحَ بالإعفاءِ المنطوقِ لا بمحاكاةِ السجلِّ (تُخفي رمزَ خروجِ `bun test`) · الثمانيةُ والثمانونَ حاجزاً شُغِّلَت واحداً واحداً بلا إخفاقٍ · `docs/evidence/repo/GUARD-ENFORCEMENT-20260917.md` §٩ · **وحاجزٌ قائمٌ الآنَ**: `scripts/check-runner-command-validity.ts` بأربعِ قواعدَ يُبرهَنُ بإحياءِ العطبِ نفسِه (خروج `1`) ثمَّ إعادتِه (خروج `0`) · مُدرَجٌ في الجانبَينِ فتكافؤُ ADR 0143 أخضرُ · 13 حالةَ اختبارٍ سالبةً · `ADR 0144` · `docs/evidence/repo/RUNNER-COMMAND-VALIDITY-20260917.md` · **وما يبقى دَيناً:** الفحصُ ساكنٌ يُثبِتُ النداءَ لا النجاحَ، وسكربتٌ يشترطُ وسيطاً بلا سطرِ استعمالٍ لا يراهُ | ADR 0143 · ADR 0144 · D-23 | P2 | نعم — عملٌ repo-local (يحتاجُ بندَ خارطةٍ مستقلًّا) |
| D-25 (مُكتشَفٌ — 2026-09-24) | **التطبيقُ المصغَّرُ يُبنى أخضرَ ويُفتَحُ أبيضَ**: إضافةُ `inline-entry-script` (`D-23`) تُدمِجُ المدخلَ في `index.html` وتتركُ `./shell-*.js` نسبيّاً إلى المستندِ فيُطلَبُ `/shell-*.js` (404؛ وعلى Render يعودُ `index.html` بنوعِ HTML تحتَ `nosniff`) و`#root` فارغٌ — قِيسَ بمتصفّحٍ بلا رأسٍ على `main`@`571c891`؛ ومعَه `delete bundle[fileName]` بمفتاحٍ ناقصٍ لم يحذفْ شيئاً قطُّ | **مُصلَحٌ (فرعُ `fix/d-25-blank-app-and-browser-first-paint`)** — المُعيِّناتُ تُطلَقُ من بياناتِ الحزمةِ ومُعيِّنٌ نسبيٌّ باقٍ يُسقِطُ البناءَ؛ ووظيفةُ CI `browser-first-paint` تفتحُ `dist` بمتصفّحٍ حقيقيٍّ وتُسقِطُ على شاشةٍ بيضاءَ (مُبرهَنٌ على العطبِ نفسِه) · `ADR 0183` · `docs/evidence/architecture/F1-09-20260924.md` | D-23 · F1-09 · DEC-19 | P0 | نعم — تمَّ التنفيذُ |
| D-27 (مُكتشَفٌ — 2026-09-25) | **التقديمُ الساكنُ مطويٌّ في وحدةِ المدخلِ فلا يتقدَّمُ**: `vite build` يطوي سكربتَ الوحدةِ المُضمَّنَ في المدخلِ بعدَ `import` الحزمِ، فطلبُ الجلسةِ يبدأُ بعدَ تنزيلِ `shell` (CI `35994981204`: 2060 ms)؛ واختبارُ المصدرِ مرَّ أخضرَ | **مُصلَحٌ (PR #261)** — سكربتٌ كلاسيكيٌّ، ومستخرِجُ بصماتٍ واحدٌ، وحاجزُ مُخرَجٍ يُسقِطُ البناءَ (مُبرهَنٌ على العطبِ نفسِه)؛ LCP وسيطُه 4084→3064 ms · `docs/evidence/architecture/F1-09-20260925-preboot-classic.md` | F1-09 · DEC-19 · D-23 | P1 | نعم — تمَّ التنفيذُ |
| D-28 (مُكتشَفٌ — 2026-09-25) | **التقديمُ الساكنُ يتجاهلُ أصلَ البوّابةِ**: يطلبُ `/v1/...` نسبيّاً بينما `api/client.ts` يحترمُ `VITE_WASLAH_API_BASE`؛ فعلى أصلٍ منفصلٍ (`render.yaml`) يذهبُ إلى الخدمةِ الساكنةِ | **مُصلَحٌ (PR #261)** — الأصلُ من `%VITE_WASLAH_API_BASE%` وإلّا نسبيٌّ · `docs/evidence/architecture/F1-09-20260925-preboot-classic.md` | F1-09 · DEC-19 | P1 | نعم — تمَّ التنفيذُ |
| D-29 (مُكتشَفٌ — 2026-09-25) | **حزمةُ `shell` تحملُ القواميسَ الثلاثةَ**: `en`/`ur` ≈ 52.7 KB مضغوطةً على المسارِ الحرجِ لكلِّ مستخدمٍ ولا يُعرَضُ إلّا لغةُ الحسابِ | **مُصلَحٌ (PR #262)** — تحميلٌ عندَ الطلبِ وحاجزُ بناءٍ · `ADR 0186` · `docs/evidence/architecture/F1-09-20260925-lazy-dictionaries.md` | F1-09 · DEC-19 | P1 | نعم — تمَّ التنفيذُ |
| D-30 (مُكتشَفٌ — 2026-09-25) | **حزمةُ `rider-home` تخالفُ تقسيمَ 9.4**: تحملُ الرحلةَ والبحثَ والملخّصَ و`socket.io` والدعمَ والحسابَ (40.2 KB) على المسارِ الحرجِ لأوّلِ سطحٍ | **مُصلَحٌ (PR #263)** — تحميلٌ عندَ الطلبِ وحاجزا بناءٍ · `docs/evidence/architecture/F1-09-20260925-rider-bundle-split.md` | F1-09 · §9.4 | P1 | نعم — تمَّ التنفيذُ |
| D-31 (مُكتشَفٌ — 2026-09-25) | **`rider-home` تنتظرُ الدورَ وهيَ لازمةٌ للراكبِ والسائقِ**: تبدأُ بعدَ `/v1/me` (1,877 ms) سلسلةً متتاليةً على المسارِ الحرجِ | **مُصلَحٌ (PR #264)** — تسخينٌ بعدَ الجلسةِ وحاجزُ بناءٍ · `ADR 0187` · `docs/evidence/architecture/F1-09-20260925-warm-rider-home.md` | F1-09 · DEC-19 | P1 | نعم — تمَّ التنفيذُ |
| D-32 (مُكتشَفٌ — 2026-09-25) | **السجلُّ والإشعاراتُ في `rider-home`** خلافاً لمحتواها في 9.4 (16.6 KB على المسارِ الحرجِ) | **مُصلَحٌ (PR #265)** — حزمةُ `rider-history` المؤجَّلةُ وحاجزُ `assert-rider-first-surface` · `docs/evidence/architecture/F1-09-20260925-first-surface-within-limits.md` | F1-09 · 9.4 | P2 | نعم — تمَّ التنفيذُ |
| D-33 (مُكتشَفٌ — 2026-09-25) | **القاموسُ العربيُّ كلُّه في `shell`** (29 KB مضغوطاً؛ منها `driver.*` ونصوصُ الشاشاتِ المؤجَّلةِ) | **مُصلَحٌ (PR #265)** — تقسيمٌ بالبادئةِ وقتَ البناءِ وحاجزُ `assert-dictionary-partitions` · `ADR 0188` | F1-09 · 9.9 | P1 | نعم — تمَّ التنفيذُ |
| D-34 (مُكتشَفٌ — 2026-09-25) | **تسخينُ `rider-home` ينتظرُ نجاحَ الجلسةِ** بلا اعتمادٍ على نتيجتِها (حلقةُ ذهابٍ وإيابٍ زائدةٌ) | **مُصلَحٌ (PR #265)** — التسخينُ معَ إرسالِ الجلسةِ · `ADR 0189` | F1-09 · DEC-19 | P1 | نعم — تمَّ التنفيذُ |
| D-35 (مُكتشَفٌ — 2026-09-25) | **سكونُ عدّاداتِ الصمودِ (≈4s) أقصرُ من `PGSTAT_IDLE_INTERVAL` (10s)**: إفراغُ الإحماءِ المتأخِّرُ يُلوِّثُ الكتلةَ الدافئةَ (14525 مقابلَ 1189 على الالتزامِ عينِه `c2c060f` · الجولتانِ `36122025751`/`36122019657`) | محجوزٌ — `fix/d-35-soak-settle-idle-flush` | DEC-18 · F9-06 | P1 | نعم |

---

## ملخَّصُ الديونِ حسبَ الأولويّة

### P0 — ديونٌ تمنعُ التقدُّمَ (تحتاجُ قرارَ مالكٍ أو بنيةً تحتيّة)

| # | الدين | نوعُ الحاجز |
|---|---|---|
| D-01 | توحيدُ authority إنشاءِ الطلب | عملٌ repo-local قابلٌ للتنفيذ |
| D-02 | قراراتُ المالكِ المحجوزة (DEC-11/14/16/17 + ADR 0099) | قرارُ مالكٍ |
| D-03 | `numInstances: 1` في `render.yaml` | قرارُ مالكٍ (DEC-14) + بنيةٌ تحتيّة |
| D-04 | لا إثباتَ أداءٍ أو سعةٍ على بيئةٍ مماثلة | بنيةٌ تحتيّة (F9/F10) |

### P1 — ديونٌ مهمّة (تشغيليّةٌ أو أمنيّة)

| # | الدين | نوعُ الحاجز |
|---|---|---|
| D-05 | المنظومةُ المراقبيّةُ غيرُ مكتملة | بنيةٌ تحتيّة (DEC-17) |
| D-06 | RLS جزئيٌّ (`force RLS = 0`) | عملٌ repo-local جزئيًّا |
| D-07 | حداثةُ عقودِ CORE غيرُ قابلٍ للتحقُّق | صلاحيّةٌ آمنة أو artifact |
| D-08 | إدارةُ الأسرارِ غيرُ مبنيّة | قرارٌ سياديٌّ (O-6) |
| D-09 | لا فحصَ ثغراتِ اعتمادياتٍ آلي | عملٌ repo-local |
| D-23 | هجرةُ الجامعِ إلى Rolldown (`vite@8` + `plugin-react@6`) | عملٌ repo-local — مؤجَّلٌ بقياسٍ (ADR 0142 §٥) |
| D-24 | سلسلةُ المشغِّلِ المحلّيِّ بلا حاكمِ صلاحيّةٍ | عملٌ repo-local (ADR 0143 §٧ — حدٌّ مُعلَنٌ) |
| D-22 | لا رحلةَ كاملةً على جهازٍ حقيقي | جهاز/متصفّح |

### P2 — ديونٌ متوسّطة (قابلِيّةُ إعادةِ الإنتاج والتنظيم)

| # | الدين | نوعُ الحاجز |
|---|---|---|
| D-10 | `actions/checkout@v4` على Node.js 20 منتهي | عملٌ repo-local |
| D-11 | تباينُ إصدارِ Bun | عملٌ repo-local |
| D-12 | فشلُ `vendored-pin-follows-bytes` محليًّا | غيرُ متكرِّرٍ حاليًّا |
| D-13 | لا migration ledger | تنازلٌ مقصودٌ (ADR 0068) |
| D-14 | `JOB_INTERVALS` يُقرأُ عندَ البناء | تنازلٌ مقصودٌ |

### P3 — ديونٌ منخفضة (تنظيفٌ وتوثيق)

| # | الدين | نوعُ الحاجز |
|---|---|---|
| D-15 | ~221 scaffold module | عملٌ repo-local |
| D-16 | `check-rate-limit-coverage` بلا script مسمّى | عملٌ repo-local |
| D-17 | رسالةُ `driver-dialog` الخاطئة | **مُغلَقٌ (BUG-008 `[x]`)** |
| D-18 | تكرارُ `update_id` في Telegram | **مُغلَقٌ (BUG-002 `[x]`)** |
| D-19 | `SYSTEM_STATE.md` قديم | عملٌ repo-local |
| D-20 | `F8-09` البوّابةُ غيرُ محدَّدة | عملٌ repo-local |
| D-21 | `deferred/` غيرُ مُصنَّف | عملٌ repo-local |

---

## ما رُفِضَ أو صُحِّحَ من التقريرِ الخارجيِّ

1. **رسالةُ `driver-dialog` الخاطئة** (D-17): التقريرُ ذكرَها كعيٍب قائم، لكنَّ BUG-008 أُغلِقَ `[x]` في 2026-09-03. الفائزُ يرى `driver.offer_accepted` والخاسرُ يرى `driver.offer_taken`. لا عيبَ قائم.
2. **تكرارُ `update_id`** (D-18): التقريرُ ذكرَه كدينٍ مفتوح، لكنَّ BUG-002 وF6-01 مغلقانِ `[x]`. الـingest النحيفُ + `claim_telegram_update` الذرّيُّ + الطابورُ + ACK مبنّيةٌ ومُختبَرة.
3. **فشلُ `vendored-pin-follows-bytes` محليًّا** (D-12): التقريرُ ذكرَ فشلًا محليًّا، لكنَّ التشغيلَ على `main`@`832e4a3` أعادَ 14 نجاح / 0 فشل. قد يكونُ بيئيًّا في بيئةِ التدقيق.
4. **migration ledger** (D-13): التقريرُ ذكرَه كدينٍ، لكنَّهُ تنازلٌ مقصودٌ موثَّقٌ في ADR 0068 — القاعدةُ السياديّةُ 0.4 تفرضُ `city_id` على كلِّ جدولٍ، وسجلُّ الهجراتِ ليسَ بياناً مدينيّاً.
5. **رسالةُ `ErrorBoundary` في `tsc`**: التقريرُ لم يعتبرها bug، وهذا صحيحٌ — السببُ missing types في بيئةٍ بلا `node_modules`.

---

## الإجراءاتُ المقترحةُ (مرتَّبةٌ بحسبِ الأولويّة)

### أوّلًا — عملٌ repo-local قابلٌ للتنفيذِ فورًا (لا يحتاجُ قرارَ مالكٍ)

1. **D-01**: توحيدُ authority إنشاءِ الطلب — حوّل `rider-dialog.ts` إلى استدعاءِ `RideRequestCommand`، وأضفْ حاجزًا يمنعُ الكتابةَ المباشرةَ لـ`orders` من خارجَ طبقةِ التطبيق.
2. **D-06**: فرضُ `force RLS` على الجداول الحسّاسة وتقليلُ `service_role using(true)` قدرَ الإمكان.
3. **D-09**: إضافةُ `dependabot.yml` لفحصِ ثغراتِ الاعتماديات.
4. **D-10**: تحديثُ `actions/checkout` إلى إصدارٍ يدعمُ runtime حديثًا.
5. **D-11**: توحيدُ إصدارِ Bun في كلِّ من Docker وCI و`@types/bun` و`engines`.
6. **D-15**: تصنيفُ الـ221 scaffold module إلى planned وrequired، وإضافةُ حاجزٍ يمنعُ الاستعمالَ الإنتاجيَّ.
7. **D-16**: إضافةُ `check:rate-limit-coverage` script مسمّى في `package.json`.
8. **D-19**: تحديثُ lقطة الحالة في `SYSTEM_STATE.md`.
9. **D-20**: كتابةُ تعريفٍ واضحٍ لبوّابةِ `F8-09`.
10. **D-21**: مراجعةُ `deferred/` وتصنيفُ ما يُدمَجُ وما يُحذَفُ.

### ثانيًا — قراراتُ مالكٍ مطلوبة

1. **D-02**: حسمُ DEC-11 (التسعير/الدفع) · DEC-14 (التوزيع المتعدِّد) · DEC-16 (النسخة التحليليّة) · DEC-17 (OTel collector) · ADR 0099 (النشر الإنتاجي).
2. **D-03**: اعتمادُ التكلفة والبنيةِ لرفع `numInstances`.
3. **D-07**: توفيرُ صلاحيّةٍ آمنةٍ للوصولِ إلى مستودعِ CORE.
4. **D-08**: اختيارُ مكانِ حفظِ الأسرار وآليّةِ تدويرها.
5. **D-22**: توفيرُ أجهزةٍ لتجربةِ التطبيق.

### ثالثًا — بنيةٌ تحتيّةٌ مطلوبة

1. **D-04**: بناءُ بيئة staging مماثلةٍ للإنتاج.
2. **D-05**: تركيبُ OTel collector + Prometheus/Grafana + تنبيهات + SLO.
3. **D-04**: تنفيذُ اختبارِ حملٍ موزَّع (p50/p95/p99).
