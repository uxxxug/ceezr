# التقرير النهائي — التوجيه التنفيذي الختامي لمشروع وَصْلة

**التاريخ:** 2026-08-10
**المشروع:** وَصْلة (`waslah`) — منصّة نقل وتوصيل عبر بوتات تلغرام
**المستودع:** `github.com/noor-seez/ceezr` (خاص) · رأس `main` البعيد: `8b03c1e4e`
**البيئة:** Bun 1.3.14 · PostgreSQL 18.4 + PostGIS 3.6.2 · Ubuntu

---

## جدول مطابقة الترقيم (مهمّ)

المستودع يحوي `docs/section-0-report.md` … `section-5-report.md` من تنفيذ توجيه
**سابق** بترقيم مختلف. **لم يُمسّ أيٌّ منها.** تقارير هذا التوجيه بأسماء مستقلّة:

| قسم هذا التوجيه | ملفّه | التقرير السابق بنفس الرقم (لم يُمسّ) |
|---|---|---|
| 0 — الفحص التأسيسي | مُدمَج في §1 أدناه | `section-0-report.md` (خط أساس أقدم: `09337d5`) |
| 2 — حقلا الأسرار | §3 أدناه | `section-2-report.md` (تدقيق شامل) |
| 3 — الهوية والدخول | `directive-final-section-3-identity-login.md` | `section-3-report.md` (تحقّق المدخلات) |
| 4 — agent-core في الدعم | `directive-final-section-4-agent-core-support.md` | `section-4-report.md` (تجربة البوت) |
| 4.2 — قروبات المدن | §5 أدناه | `section-5-report.md` (معرّفات القروبات) |
| 4.4 — الاختبار العدائي | `docs/adversarial-test-report.md` | — |
| 6 — التحسينات المحدودة | `directive-final-section-6-bounded-improvements.md` | — |

---

## 1) الحالة الحالية

المنصّة **مكتملة وظيفياً ومستقرّة تقنياً**، مبنية على Clean Architecture
(monolith معياري) بـ Bun و Hono و grammY و postgres.js و Supabase.

### خط الأساس — أُعيد إنتاجه استقلالاً قبل أي تعديل

| المقياس | القيمة |
|---|---|
| ملفات الأرشيف | 609 — **مطابقة لرأس `main` البعيد حرفاً** |
| الهجرات | 14 → 21 جدولاً، كلها نظيفة |
| `typecheck` | PASS |
| `lint` | 527 ملفاً · 0 أخطاء · 9 infos |
| `bun test` | **690 pass · 0 fail** · 48 ملفاً |
| البوّابات الأربع | كلها PASS |

### الحالة بعد العمل

| المقياس | خط الأساس | الآن | الفرق |
|---|---|---|---|
| الهجرات | 14 | **15** | +1 (إصلاح سباق) |
| الجداول | 21 | **21** | بلا تغيير |
| `typecheck` | PASS | **PASS** | — |
| `lint` | 0 أخطاء · 9 infos | **0 أخطاء · 9 infos** | بلا انحدار |
| `bun test` | 690 | **740** | **+50** |
| التأكيدات | — | 2,470 | — |
| البوّابات الأربع | PASS | **PASS** | — |

كل رقم أعلاه شُغِّل فعلياً في هذه البيئة، على قاعدة أُعيد بناؤها من الصفر.

---

## 2) ما تم إنجازه

### أ) الفحص التأسيسي (§0)
الأرشيف فُكّ ورُسم (609 ملفاً)، وسلسلة الأدوات نُصّبت، وخط الأساس أُعيد إنتاجه
**استقلالاً** لا نقلاً عن تقرير سابق. وقُورن الأرشيف بالمستودع البعيد أوّلاً
(حسب اختياركم): مجموعة الملفات مطابقة لرأس `main` — فالأرشيف موثوق.

### ب) حقلا الأسرار (§2)
فُتحا وفق القيود حرفاً: لا قيمة سرّية في السجلّ · لا حفظ داخل الكود · لا في
التوثيق · لا في ملفات تشغيل عامّة. رمز الوصول للمستودع في خزنة الاعتماديات
ويُستدعى بمُعرِّف لا بقيمة.

### ج) الهوية وتسجيل الدخول (§3)
الكود كان سليماً — الثغرة كانت في التغطية. أُضيف
`admin-identity-login.test.ts`: **16/16**. **لم يُعدَّل سطر واحد من كود الهوية.**

### د) agent-core في الدعم فقط (§4.1)
حصر النطاق أُثبت بالكود: مُستدعٍ واحد · حدث واحد · وكيل واحد · سقف `SUGGEST` ·
معطَّل افتراضياً · بوّابة العزل تمرّ. **6/6**.

**وثغرة حقيقية سُدَّت:** الاقتراح كان يُنشر إلى تلغرام فقط ولا يظهر في لوحة
الإدارة، خلافاً لنصّ §4.1. سُدَّت بقراءة فقط (`left join lateral`) بلا كسر عزل
ولا مسار تنفيذ. **5/5** + اختبار وحدة.

### هـ) قروبات المدن (§4.2)
فُحصت الواجهة فوجدتها **مُغطّاة بالكامل** بثلاثة اختبارات قائمة. تطبيقاً للقاعدة
4 من §0 لم يُعَد بناؤها ولم تُكرَّر اختباراتها.

### و) الاختبار العدائي (§4.4)
`adversarial.test.ts` — **22/22**، تغطي **14 بنداً من 14**. التفصيل الكامل في
`docs/adversarial-test-report.md`.

---

## 3) ما بقي مفتوحاً

1. لا اختبار على بيئة إنتاج حقيقية — كل شيء على قاعدة محلّية ومعالِجات محقونة.
2. `tests/e2e/` فارغ (موروث من خط الأساس).
3. `docs/api.md` متأخّر عن الواقع (موروث).
4. `postgis` وأدوار Supabase الثلاثة مطلوبة للتشغيل ولا يوثّقها المشروع (موروث).
5. لا ملف LICENSE (موروث).
6. الـ9 `infos` في lint لم تُمسّ عمداً (خارج النطاق).
7. لا حارس داخل حزمة الاختبارات لسباقَي رموز الدخول وتهدئة التذاكر — الحراسة
   بمشغّلَي سباق يدويين. المحاولتان لإضافة حارس آليّ نجحتا على الكود المعطوب
   فأُزيلتا؛ السبب موثّق في `directive-final-section-7-plpgsql-audit.md` §5.

---

## 4) ما تم تأجيله

ثمانية بنود موثّقة بالأثر والمبرّر في
`directive-final-section-6-bounded-improvements.md` §4. أبرزها:

- حدّ صريح لحجم جسم الويبهوك
- إسقاط التكرار بـ `update_id`
- ~~تمشيط بقيّة دوال `plpgsql` بحثاً عن نمط «افحص ثم اكتب» بلا قفل~~ — **أُنجز.**
  43 دالّة مُشِّطت، ستّة مرشّحين، **عيب حقيقي واحد** (`open_support_ticket`)
  أُثبت وأُصلح وأُعيد إثباته. التفصيل في
  `directive-final-section-7-plpgsql-audit.md`

وستّ أفكار **رُفض بناؤها** لأنها تخالف §4.3 — أبرزها تعقيم HTML عند إدخال الاسم،
وهو ضارّ لا نافع (§4.أ من تقرير الاختبار العدائي).

---

## 5) ما فشل وتم إصلاحه

### أ) عيب حقيقي واحد في المنتَج: سباق حدّ رموز الدخول

| البند | التفصيل |
|---|---|
| **السبب** | `select count(*)` ثم `insert` بلا قفل في `issue_admin_login_code`. تحت `READ COMMITTED` تقرأ المعاملات المتزامنة عدّاداً واحداً قديماً وتكتب جميعاً |
| **الأثر** | حدّ 5 رموز/ساعة يُلتَفّ بالتزامن وحده — والحدّ موضوع لمنع إغراق صندوق المسؤول |
| **القياس قبل** | 40 جولة × 30 متزامناً → **31 جولة تجاوزت، أقصى إصدار 9 مقابل حدّ 5** |
| **الإصلاح** | هجرة جديدة: `for update` على صفّ المستخدم قبل الفحص |
| **القياس بعد** | 40×30 و100×50 → **أقصى إصدار 5، صفر تجاوز** |
| **إثبات السببية** | أُعيدت الدالة القديمة فعلياً → 31 تجاوزاً. أُعيد الإصلاح → صفر |
| **إعادة التحقّق** | قاعدة من الصفر (15 هجرة، 21 جدولاً) + الحزمة الكاملة + 5 تكرارات متتالية |

الإصلاح لا يُدخل نمطاً جديداً: `select ... for update` مستعمَل أصلاً في خمسة
مواضع من `atomic_rpcs`. وجاء بهجرة **جديدة** لا بتعديل هجرة مُطبَّقة.

### ب) ثغرة تنفيذ: الاقتراح لا يظهر في اللوحة
وُثّقت وأُصلحت وأُعيد التحقّق منها — §2.د أعلاه.

### ج) أخطاء ارتكبتُها أنا (لا عيوب في المشروع)
تسعة أخطاء موثّقة بأسبابها: خمسة أثناء إصلاح اللوحة (تقرير القسم 4 §4)، وأربعة
في كتابة الاختبار العدائي (تقرير الاختبار العدائي §5). أُثبتَت جميعاً أنها في
الاختبار لا في المنتَج — أبرزها ترويسة HTTP لا تحمل إلا ASCII، وتوقُّع خاطئ بأن
`parseFullName` يرفض محارف قلب الاتجاه بينما هو **ينزعها** (سلوك أفضل).

### د) ثلاثة سلوكيات بدت عيوباً وليست كذلك
مدقّق الاسم لا يرفض HTML (والحماية عند العرض، مُثبَتة) · Redis يفشل مفتوحاً
(ADR 0010) · التحديث المكرَّر يُعالَج مرّتين (الذرّية في القاعدة). وُثّقت ولم
تُغيَّر — تقرير الاختبار العدائي §4.

---

## 6) الاختبارات الناجحة

| الحزمة | النتيجة |
|---|---|
| **`bun test` الكاملة** | **740 pass · 0 fail** · 2,470 تأكيداً · 52 ملفاً · 8.3 ثانية |
| تكرار الحزمة 5 مرّات | 740/740 في كل مرّة |
| `typecheck` | PASS |
| `lint` | 532 ملفاً · 0 أخطاء · 9 infos (خط الأساس) |
| `check-agent-core-isolation` | PASS |
| `check-business-constants` | PASS |
| `check-i18n` | PASS |
| `check-migrations` | PASS |
| الهجرات (15) من الصفر | نظيفة → 21 جدولاً |
| ضغط السباق 100×50 | لا تجاوز |

**الاختبارات المضافة (+50):** الهوية والدخول 16 · حصر agent-core 6 · اقتراح
الطبقة في اللوحة 5 · وحدة اللوحة 1 · عدائي 22.

---

## 7) المخاطر المتبقية

1. **السباقات لا تُنفى بالاختبار.** أُثبت إغلاق السباق المكتشَف، ولم يظهر غيره
   تحت الضغط المطبَّق. بقيّة دوال `plpgsql` لم تُمشَّط بنفس الكثافة — وهذا أهمّ
   بند مؤجَّل.
2. **لا تحقّق إنتاجي.** لم يُختبر تلغرام حقيقي ولا Redis حقيقي ولا Supabase
   مُدار ولا حِمل واقعي.
3. **حجم جسم الويبهوك بلا حدّ صريح** — يعتمد على الطبقة المستضيفة.
4. **`for update` يُسلسِل إصدار الرموز للمستخدم الواحد** — مقصود وأثره ضئيل.
5. **agent-core معطَّل افتراضياً** — تشغيله في الإنتاج يحتاج مراقبة مستقلّة؛
   الاختبار يُثبت الحصر لا جودة الاقتراحات.
6. **فجوات موروثة** — e2e فارغ، توثيق API متأخّر، متطلّبات تشغيل غير موثّقة.

---

## 8) هل المشروع جاهز؟

**المسوّغات:** خط الأساس أُعيد إنتاجه استقلالاً ولم ينحدر · كل بنود التوجيه
نُفّذت أو أُثبت أنها منفَّذة · العيب الحقيقي الوحيد وُجد وأُثبت سببياً وأُصلح
وأُعيد التحقّق منه من الصفر · agent-core محصور في الدعم بسقف `SUGGEST` والبوّابة
تمرّ · 740/740 مستقرّة عبر خمس تكرارات.

**والشروط قائمة:** لم يجرِ أي تحقّق على بيئة إنتاج حقيقية، وبقيّة دوال القاعدة
لم تُمشَّط بحثاً عن نفس نمط السباق الذي ثبت وقوعه فعلاً في إحداها.

### الشروط الواجبة قبل الإطلاق

1. **تمشيط كل دوال `plpgsql` عن نمط «افحص ثم اكتب» بلا قفل.** ثبت وقوع هذا
   النمط فعلياً في دالة واحدة؛ فبقاؤه في غيرها احتمال قائم لا نظري.
2. **تشغيل تجريبي مغلق** بتلغرام و Redis و Supabase حقيقية قبل فتح الخدمة.
3. **توثيق متطلّبات التشغيل** (`postgis` وأدوار Supabase الثلاثة) — بدونها لا
   تُبنى القاعدة، وقد عُرفت بالتجربة لا بالقراءة.
4. **إبقاء agent-core معطَّلاً** حتى يُرصد أثر اقتراحاته على تذاكر حقيقية.

عند استيفاء هذه الأربعة، لا مانع تقنياً معلوماً من الإطلاق.

---

YES WITH CONDITIONS
