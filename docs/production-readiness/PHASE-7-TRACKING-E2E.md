# المرحلة ٧ — اختبار التتبّع من الطرف إلى الطرف

> **الغرض:** إثبات أن سلسلة التتبّع تعمل **موصولةً** لا كأجزاءٍ صحيحة كلٌّ على
> حِدَة. المراحل ٣ و٤ و٥ و٦ بنت الحلقات (تحقّق GPS، الموقع القانوني، آلة حالة
> الجلسة، النقل اللحظي) وكلٌّ منها مُختبَرٌ وحدةً وتكاملاً. وهذه المرحلة لا
> تُضيف حلقةً: تُثبت أن الحلقات مُتماسكة تحت مسارٍ واحدٍ حقيقي، وتُبيّن بالقياس
> ما لم يكن مُختبَراً وما لا يزال غير مُثبت.

**الحالة:** البوابة مُغلقة. ولا يُدّعى فيها اكتمالٌ زائد: انظر البند ١٣ و١٤.

---

## ١. ما تم فحصه قبل أي تعديل

القاعدة الثامنة تُوجب فحص القائم قبل الكتابة. فُحِص:

| الملف | ما يُختبر فيه فعلاً | الثغرة التي بقيت |
|---|---|---|
| `tests/unit/tracking-realtime.test.ts` (٢٧ اختباراً) | التصريح في `visibility.ts`، الخنق في المُرحِّل بساعةٍ مصنوعة، توجيه الناقل | كلّه بساعةٍ وذاكرةٍ مصنوعتين: لا قاعدة، ولا بوت، ولا HTTP |
| `tests/integration/tracking-realtime.test.ts` (٩) | مستودع الجلسات على قاعدةٍ حقيقية، قيود الجدول، فهرس «جلسةٌ مفتوحةٌ واحدة» | الجلسات تُفتح بنداءٍ مباشر على المستودع، لا بإصلاحةٍ واردةٍ من سائق |
| `tests/integration/driver-location-visibility.test.ts` | الموقع القانوني والاستبعاد بلا موقع | لا جلسة ولا ناقل ولا عميل |
| `tests/e2e/ride-soak.test.ts` | ثلاثون رحلة متتابعة عبر المسار الحقيقي | لا موقع بعد البدء إطلاقاً: الرحلة تنتهي بلا إصلاحةٍ واحدة |
| `tests/integration/admin-live.test.ts` | مجرى SSE بحقن لقطة | لا يُقارَن المعروض بما في القاعدة |

**الخلاصة المقيسة:** لا يوجد في المستودع اختبارٌ واحد يمرّ فيه موقعٌ من تحديث
تلغرام إلى `drivers.last_location` إلى `tracking_sessions` إلى الناقل إلى خريطة
العميل ولوحة العمليات في **سياقٍ واحد**. فكل ما سبق يشهد لأجزاءٍ لا لسلسلة.
وهذا هو بالضبط ما تسدّه المرحلة ٧.

## ٢. ما تم تغييره

1. **أُضيف `tests/e2e/tracking-e2e.test.ts`** — سبعة اختبارات، كلّها عبر المسار
   الحقيقي: تسجيل السائق بالخطوات (اسم، رقم، مدينة، خدمة، مركبة، لوحة، هوية،
   صورة) ثم توثيق ثم `/available`، وتسجيل العميل، وطلب رحلةٍ بموقعَي الانطلاق
   والوصول، ثم `offer:accept` و`ride:start` و`ride:complete`. **لا صفَّ طلبٍ
   واحد مبذورٌ باليد** — لأن بذر الصفوف يشهد للقاعدة لا للتطبيق.
2. **عُرِض المُرحِّل على الحاوية**: `container.tracking.relay` في
   `apps/gateway/src/container.ts`. وهو **نفس النسخة** المُشتركة على الناقل لا
   نسخةٌ ثانية — فلا مصدرَ ثانٍ للحقيقة (القاعدة الخامسة). والسبب في البند ٣.

## ٣. الملفات التي تغيّرت

| الملف | التغيير | لماذا هذا أقلّ تغييرٍ صحيح |
|---|---|---|
| `apps/gateway/src/container.ts` | `TrackingWiring.relay: CustomerLiveRelay` + تمرير `customerRelay` في الحاوية + استيراد النوع | عدد البثوث المفتوحة (`openBroadcasts`) حالةٌ في الذاكرة لا أثر لها في القاعدة. فبلا عرضها لا يستطيع اختبارٌ أن يشهد على تسريبٍ فيها إلا بالاستدلال من عدد رسائل تلغرام — وهو استدلالٌ يُخفي الحالة العالقة التي لا تُرسل شيئاً. والبديل (بناء مُرحِّلٍ ثانٍ في الاختبار) كان سيقيس نسخةً لا تعمل في الإنتاج. |

## ٤. الملفات التي أُضيفت

| الملف | السطور | المحتوى |
|---|---|---|
| `tests/e2e/tracking-e2e.test.ts` | ٦٩٥ | سبعة اختبارات، ٦٧ توكيداً |
| `docs/production-readiness/PHASE-7-TRACKING-E2E.md` | هذا الملف | تقرير البوابة |

## ٥. الملفات التي لم تُلمس — ولماذا

- **`packages/domain/tracking/*` و`packages/application/tracking/*` و
  `packages/infrastructure/tracking/*`**: لم يُعدَّل فيها منطقٌ واحد. وهذه نتيجةٌ
  لا هدف: القياس لم يكشف خطأً في السلوك المُختبَر، فأي تعديلٍ «تحسيني» هنا كان
  سيكون تغييراً بلا دليلٍ يطلبه.
- **`supabase/migrations/**`**: لا هجرة في هذه المرحلة. الاختبار لا يحتاج عموداً
  ولا جدولاً جديداً — والحاجة إلى مخططٍ جديدٍ لأجل اختبارٍ علامةُ أن الاختبار
  يقيس شيئاً غير موجود.
- **`tests/unit/tracking-realtime.test.ts` و`tests/integration/tracking-realtime.test.ts`**:
  لم يُحذف منهما توكيدٌ ولم يُضعَّف. الاختبار الأسرع يبقى هو الذي يُحدِّد موضع
  الخطأ؛ واختبار الطرف إلى الطرف يشهد للتوصيل لا يُغني عنهما.
- **`tests/integration/driver-location-visibility.test.ts`**: الاختبار الفاشل فيه
  بوّابةٌ مقصودةٌ للمرحلة ١٤ (انظر البند ٨). لم يُلمس — تعديله ليمرّ هو بالضبط
  الممنوع.

## ٦. الاختبارات المضافة — وماذا يقيس كلٌّ منها

| # | الاختبار | الواقعة المُثبتة |
|---|---|---|
| ١ | رحلةٌ كاملة في سياق واحد | إصلاحةُ التوافر تفتح جلسةً بـ`trip_id = null` ولا تُنشئ نداءَ خريطةٍ لأي عميل؛ وبعد الإسناد والبدء تُربط الرحلة **بنفس صفّ الجلسة** لا بصفٍّ جديد؛ والإنهاء يُغلقها بـ`TRIP_COMPLETED` ويُصدر `stop` واحداً بالضبط |
| ٢ | ما تراه العمليات هو ما خُزِّن في المصدر القانوني | `listLiveDriverPositions` تُطابق `drivers.last_location` المقروء بـSQL خامّاً، ولقطةُ مجرى SSE تحمل مُعرِّف السائق ومُعرِّف الرحلة |
| ٣ | ينقطع السائق ثم يعود | بعد إرجاع `last_fix_at` خلف حدّ التقادم يُقرأ `sessionStateAt` منقطعاً و`ended_at` لا يزال `null`؛ وإصلاحةٌ جديدة تُعيده `ACTIVE` **في نفس مُعرِّف الجلسة** |
| ٤ | إصلاحةٌ متأخّرة بعد نهاية الرحلة | إصلاحتان بعد الإنهاء ⇒ صفرُ نداءات خريطة؛ جلستان لا ثلاث؛ الثانية مفتوحةٌ بـ`trip_id = null`؛ ولا جلسةَ مفتوحةٍ تُشير إلى الرحلة المنتهية |
| ٥ | رحلتان متزامنتان في نفس المدينة | بإصلاحاتٍ متناوبةٍ بين سائقين، كل عميلٍ يستقبل نداءً واحداً بإحداثيات سائقه هو؛ وإنهاء إحدى الرحلتين يُوقف بثّ عميلها وحده |
| ٦ | حدثٌ لسائقٍ غير سائق الرحلة يُسقَط | حدث `location_updated` يحمل رحلةَ العميل ومُعرِّفَ سائقٍ آخر يُسقَط ولا يفتح بثّاً؛ ثم إصلاحةُ السائق الحقيقي تفتحه على إحداثياته |
| ٧ | إصلاحات متلاحقة لا تُغرِق تلغرام | ثلاث إصلاحاتٍ متلاحقة ⇒ صفرُ تعديلات؛ وبعد فاصلٍ حقيقي (٥٫١ ثانية بساعة النظام) ⇒ تعديلٌ واحد بالضبط؛ والموقع القانوني تقدّم مع كل إصلاحةٍ رغم كتم القناة |

## ٧. فحص التحوير — إثبات أن الاختبار يقيس شيئاً

اختبارٌ ناجحٌ لا يُثبت أنه يقيس. فأُبطِل كل حَرَسٍ يُدّعى أن الاختبار يشهد له،
وأُعيد التشغيل:

| الحَرَس المُبطَل | الملف | النتيجة | الحكم |
|---|---|---|---|
| `and status in ('matched','in_progress')` ← أُضيف `'completed'` | `infrastructure/tracking/tracking-queries.ts` | **فشل اختبار ٤** (٥ نجاح / ١ فشل) | له أسنان |
| `if (nowMs - open.sentAtMs < minIntervalMs) return;` ← أُبطِل | `application/tracking/customer-live-relay.ts` | **فشل اختبار ٧** (٥ نجاح / ١ فشل) | له أسنان |
| `if (target.driverId !== event.driverId) return;` ← أُبطِل | `application/tracking/customer-live-relay.ts` | **فشل اختبار ٦** (٦ نجاح / ١ فشل) | له أسنان |
| نفس الحَرَس + `if (event.tripId !== subscription.tripId)` في الناقل | المُرحِّل + `event-bus.ts` | **٦ نجاح / ٠ فشل** قبل إضافة اختبار ٦ | **لا أسنان — نتيجةٌ سلبية** |

**النتيجة السلبية أهمّ من الموجبة.** اختبار «رحلتان متزامنتان» (٥) بقي ناجحاً
بعد إبطال حَرَس الهويّة — لأن البثّ مُفهرسٌ بالرحلة وكل حدثٍ يحمل رحلته، فالفصل
هناك صحيحٌ **بالبنية لا بالحَرَس**. أي أن توكيده حقيقي لكنه لا يشهد لذلك السطر.
فأُضيف اختبار ٦ يُحاكي السباق الذي يُغلقه الحَرَس (إعادة إسنادٍ بين النشر
والاستهلاك) بالنشر المباشر على الناقل، وأُعيد فحص التحوير فسقط. والبديل — قبولُ
اختبار ٥ شاهداً — كان سيُنتج بالضبط ما نهى عنه التوجيه: سطرَ أمانٍ يُحذف يوماً
بلا أن يصرخ شيء، وتقريراً يدّعي تغطيةً لا وجود لها.

## ٨. الاختبارات المُشغَّلة والنتائج

```
bun test  →  1165 pass / 1 fail / 4364 expect  •  1166 tests / 84 files / 24.92s
bun test tests/e2e/tracking-e2e.test.ts  →  7 pass / 0 fail / 67 expect / 5.92s
```

**الفشل الوحيد سابقٌ لهذه المرحلة ولا علاقة له بها**:
`tests/integration/driver-location-visibility.test.ts` › «إرسال الموقع بعده يُنهي
الاستبعاد فوراً» عند السطر ٢٩٨ (`0` مقابل `> 0`). أُثبِت سبقُه في المرحلة ٦
بـ`git stash` وإعادة تشغيل، وهو بوّابةٌ مقصودةٌ تنتظر المرحلة ١٤ (تكامل
الإسناد). والفرق عن المرحلة ٦: ١١٥٨ ← ١١٦٥ نجاحاً (+٧)، و٤٢٩٧ ← ٤٣٦٤ توكيداً
(+٦٧)، و٨٣ ← ٨٤ ملفاً.

## ٩. typecheck

```
bun run typecheck  →  tsc --noEmit  →  0 أخطاء
```

## ١٠. lint

```
npx biome check .  →  606 ملفاً  •  8 أخطاء + 1 تحذير + 9 معلومات
```

مطابقٌ لخطّ الأساس تماماً (٦٠٥ ← ٦٠٦ ملفاً بالملف المضاف، والأعداد لم تتغيّر).
لم يُصلَح أيٌّ من الثمانية في هذه المرحلة: هي في ملفاتٍ لا تخصّ التتبّع،
وإصلاحها هنا يخلط تغييرَين في مرحلةٍ واحدة.

## ١١. build

**لا سكربت `build` في المستودع** — وهذه واقعةٌ لا نقص: `package.json` يحمل
`typecheck, lint, lint:fix, format, test, test:integration, test:e2e, ci, dev,
start`، والتشغيل ببن مباشرةً على TypeScript بلا خطوة حزمٍ (ADR-0002). فبديل
البناء هنا هو `typecheck` وقد مرّ بصفر أخطاء. وسكربت الحزم يُقرَّر في المرحلة
٢٥ (النشر) لا يُخترَع هنا.

### فحص المخططات

```
bun scripts/check-migrations.ts  →  مخالفتان، كلتاهما سابقة:
  db_backups (20260811140000)   — لا عمود city_id
  webhook_events (20260811150000) — لا عمود city_id
```

بلا زيادة. ولا هجرةَ في هذه المرحلة.

## ١٢. النتائج الأمنية

| الواقعة | الحكم |
|---|---|
| فصلُ خرائط العملاء عن بعضها | **مُثبت** باختبار ٥ عبر المسار الحقيقي، وبفحص تحويرٍ على حَرَس الهويّة في اختبار ٦ |
| حدثٌ لسائقٍ غير سائق الرحلة | **مُثبت** أنه يُسقَط قبل أي نداءِ قناة (اختبار ٦) |
| بثٌّ يبقى مفتوحاً بعد نهاية الرحلة | **مُثبت** أنه لا يبقى: `stop` واحدٌ عند الإنهاء، وصفرُ نداءاتٍ للإصلاحات بعده، ولا جلسةَ مفتوحةٍ تُشير إلى رحلةٍ منتهية (اختبار ١ و٤) |
| كتمُ البوت بحدود معدّل تلغرام | **مُخفَّف ومُثبت التوصيل** بساعة النظام لا بساعةٍ مصنوعة (اختبار ٧) |
| تسريبُ موقعٍ إلى مشغّلٍ خارج نطاقه | **غير مُختبَرٍ هنا**. التصريح مُختبَرٌ وحدةً في `tracking-realtime.test.ts`، ومسار SSE في هذه المرحلة يُفحص بمشغّلٍ نطاقه كل المدن. مشغّلٌ محدود المدينة يُفحص في المرحلة ١٣ حيث تُبنى واجهة اللوحة |

## ١٣. المخاطر الباقية

الجديد في هذه المرحلة:

- **R-21 — اختبار الخنق ينام ٥٫١ ثانية بساعةٍ حقيقية.** و`ContainerOverrides`
  لا تحمل بذرة `clock`، فلا سبيل إلى ساعةٍ مصنوعةٍ عبر الحاوية. والنوم هنا
  **مقصود** لأن المُراد إثبات التوصيل بزمن التشغيل نفسه؛ لكنه يُضيف ٥ ثوانٍ إلى
  الحزمة ويجعل الاختبار حسّاساً لبطء المُشغِّل. يُعاد النظر في المرحلة ٢٢ (بذرة
  ساعةٍ في الحاوية تخدم اختباراتٍ أخرى أيضاً).
- **R-22 — `openBroadcasts` صار سطحاً عاماً على الحاوية.** الخطر أن يُبنى عليه
  منطقُ تشغيلٍ يقرأ حالةَ ذاكرةٍ لنسخةٍ واحدة ويظنّها حقيقةً عامة. الحدّ مكتوب في
  ترويسة الحقل، ويُراقَب في المرحلة ٢٣ (الرصد).

والباقي من قبل بلا تغيير: **R-9** (لا تاريخ مواقع ← ٢٧)، **R-13** (الحزمة
حسّاسة لتنازع القاعدة ← ٢٢)، **R-16** (`TrackingService` غير موصول)، **R-17**
(صحّة الناقل معلّقةٌ على `numInstances: 1` بلا إنفاذ ← ٢٥)، **R-18** (خريطة
العمليات بلا واجهة ← ١٣)، **R-19** (الموقع الحيّ على تلغرام غير مُجرَّبٍ على
جهازٍ حقيقي)، **R-20** (حدّ الـ٢٤ ساعة للموقع الحيّ في تلغرام).

## ١٤. ما لا يزال غير مُثبت — بلا تجميل

- **جهازٌ حقيقي.** كل شيءٍ هنا يُقاس بقناةٍ مُستبدَلة تُسجِّل النداءات. أن تلغرام
  يقبل `editMessageLiveLocation` بهذا التتابع من بوتٍ حقيقي إلى محادثةٍ حقيقية
  **لم يُقَس** (R-19).
- **نسخٌ متعدّدة.** الناقل في العملية. كل اختبارات هذه المرحلة على نسخةٍ واحدة،
  فهي لا تشهد للسلوك عند رفع النسخ (R-17).
- **الحِمل.** سبعة اختباراتٍ بسائقين وعميلين لا تقول شيئاً عن مئة سائق يُرسلون
  كل ثانية. هذا موضوع المرحلة ٢٤ (اختبار الحِمل)، ولا يجوز الاستدلال عليه من
  هنا.
- **تصريح المشغّل محدود المدينة على مسار SSE** (البند ١٢، آخر صف).
- **الشبكة والانقطاع الحقيقي.** «انقطاع السائق» في اختبار ٣ يُحاكى بإرجاع
  الطوابع في القاعدة، لا بشبكةٍ تسقط فعلاً. والحالة المُقاسة صحيحة، لكن سبيلها
  إليها مُصطنع.

## ١٥. المرحلة التالية

**المرحلة ٨ — `DriverLocator`**: اختيار السائق بالقرب المكاني على الموقع
القانوني الذي أُثبِت في هذه المرحلة أنه يتقدّم مع كل إصلاحة. وشرط الدخول متحقّق:
سلسلةُ التتبّع مُثبتٌ توصيلها، فالباحث سيقرأ حقيقةً لا صفراً.
