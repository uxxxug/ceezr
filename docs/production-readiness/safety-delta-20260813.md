# دلتا السلامة — 2026-08-13

## ما أُغلق

كانت `trigger-sos.ts` و`resolve-safety-incident.ts` فجوتين من الفئة ٣ في
`skeleton-classification-20260812.md`. أصبحتا الآن تنفيذين حيّين موصولين
بحاوية البوابة والعامل، لذلك لم تعودا فجوتين صامتتين.

| البند السابق | التنفيذ الحي |
|---|---|
| زر SOS غير موجود | `/sos` في حوار الراكب والسائق، بمفاتيح i18n في العربية والإنجليزية والأردية |
| لا سجل موثوق للحادث | `trigger_sos` ينشئ `safety_incidents` و`safety_incident_deliveries` في معاملة واحدة مع منع تكرار مقفل |
| فشل Telegram يفقد البلاغ | عامل `deliver-safety-incidents` يسحب outbox ويعيد الفشل إلى `pending` بموعد الإعادة من `platform_settings` |
| لا قرار إنساني مقفل | `claim_safety_incident` ثم `resolve_safety_incident` يفرضان `open → received → closed` ويسجلان الموظف والوقت والقرار |
| حجب موازٍ أو آلي | قرار `block_reporter` وحده يستدعي `admin_set_user_blocked` الإداري القائم، بعد قرار موظف مسجل |

## الدليل

يوثق `docs/evidence/safety-20260813.txt` إعادة بناء قاعدة
`postgres://postgres@127.0.0.1:5432/waslah_safety`، واختبارات التزامن،
وفشل الإرسال ثم إعادة تسليم واحدة، والبوابات التشغيلية.

لا يُفعّل هذا التغيير مشاركة موقع حيّ جديداً أو تصنيفاً آلياً؛ البطاقة تعرض
آخر موقع معروف فقط، وكل إغلاق أو حجب يبدأ بضغطة موظف مخوّل في قروب الإسناد.
