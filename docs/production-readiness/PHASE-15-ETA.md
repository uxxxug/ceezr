# المرحلة ١٥ — زمنُ الوصول التقديري (ETA)

- **الحالة:** مُغلَقة
- **قرارٌ حاكم:** ADR 0024 — زمنُ الوصول مدّةُ توجيهٍ أو لا شيء
- **يُغلِق:** الشقَّ الأوّل من R-28 (محرّك التوجيه غيرُ موصولٍ في الإنتاج)
- **يفتح:** R-54 · R-55

## ١. ما فُحِص قبل أي تعديل

| المفحوص | النتيجة المقيسة |
| --- | --- |
| مواضعُ نداء `createOsrmProvider` | ثلاثةٌ، **كلّها في `tests/`** |
| ‏`grep RoutingProvider` خارج `packages/maps` | **لا شيء** |
| ‏`OSRM_BASE_URL` في الضبط | مذكورٌ في `.env.example:129` و`render.yaml:91,178` و**لا يُقرأ في `packages/shared/config` قط** |
| ما يراه السائق | مسافةُ هافرساين موسومةً `distanceKind: "STRAIGHT_LINE"` — ولا زمنَ وصولٍ إطلاقاً |
| ‏`packages/domain/tracking/driver-trip-view.ts` | تعليقُه يذكر المرحلة ١٥ صراحةً، و`DistanceKind` بُني اتحاداً تحضيراً لها |
| مستهلكو البطاقة | `driver-dialog.ts` (`/trip` و بعد قبول العرض) و`rating-dialog.ts` (بعد بدء الرحلة) — و`tripCardReplies` نقطةُ التركيب المشتركة |
| جودةُ المزوّد القائم | مهلةٌ إجماليّةٌ ٣ ثوانٍ للمحاولتين، تراجعٌ ١٥٠ م.ث، `deservesRoutingRetry` يُعيد على العابر وحده، `checkCode()` قبل قراءة الحقول لأنّ OSRM يردّ ‎200‎ برموزٍ غير `Ok` — **جيّدٌ ولم يُعَد بناؤه** |

**الخلاصة:** لم تكن المشكلة عيباً في حسابٍ قائم، بل أنّ الحساب **غيرُ موجود**
والمحرّكُ الذي يُنتجه **غيرُ قابلٍ للبناء في الإنتاج**.

## ٢. القياس على محرّك حقيقي (قبل التعديل)

القياس أُجري مقابل خادم OSRM حقيقي على أزواجٍ حقيقيّة في جدّة ومكّة. التفاصيلُ
الكاملة في ADR 0024. وخلاصةُ ما حكم القرار:

1. **نسبةُ الالتفاف** على ١٥ زوجاً: ١.١٢٤ … ٢.٨٣٤ (وسيط ١.٤٢٥). معاملُ المتوسط
   يُخطئ من **−٤٥.٩٪** إلى **+٣٦.٦٪** ⇒ لا تقديرَ من مسافةٍ مستقيمة.
2. **السرعةُ المتضمَّنة** ٢٧.٣ … ٨٠.٦ كم/س، وأبطأُ زوجٍ أقصرُها ⇒ لا سرعةَ ثابتة.
3. **الإلصاقُ الصامت:** إحداثيةٌ في البحر الأحمر أعادت `Ok` و٥٦ دقيقةً بإلصاقِ
   **٥١.٧ كم** ⇒ `code:"Ok"` ليس دليلاً على أنّ الجواب يخصّ الموضعَ المسؤولَ عنه.
4. **أبعدُ موضعٍ مشروعٍ قيس:** مطار جدّة ٥١٣.٢ م. **وأصغرُ زائفٍ:** ٢٥٠٠ م ⇒ السقفُ
   ١٠٠٠ م يقع بينهما.
5. **مَزلَقُ شكل الإحداثية:** المجالُ `{latitude, longitude}` والخرائطُ
   `{lat, lng}`. وتمريرُ الشكل الخطأ **لا يرفع خطأً**: يُنتج `NaN` بصمتٍ لأنّ Bun
   يشغّل TypeScript بلا تحقّقٍ من الأنواع ⇒ الجسرُ صار دالّةً واحدةً مختبَرةً.

بعد التعديل، أُعيد القياسُ على نفس المحرّك عبر السلسلة الكاملة:
`البلد ← الروضة` = ‏١٢ دقيقة/١١.٧ كم `ROUTED` · `البحر الأحمر` = `OFF_ROAD` ·
`مصر` = `OFF_ROAD` · `الصحراء` = ‏١٩٥ دقيقة `ROUTED` (بعيدٌ لا خاطئ) · خادمٌ ميت =
`PROVIDER_DOWN` · بلا مزوّد = `NOT_CONFIGURED` · بلا مقصد = `NO_INPUT`.

## ٣. الملفّات المُضافة

| الملف | الدور |
| --- | --- |
| `packages/domain/eta/index.ts` | السياسةُ نقيّةً: الحدود، التصنيف، ترتيب الفحص. بلا استيرادات |
| `packages/application/tracking/estimate-arrival.ts` | الوصلة: جسرُ الإحداثية، ترجمةُ أصناف الخطأ، فكُّ اتحاد الإلصاق |
| `tests/unit/eta-policy.test.ts` | ٢٥ اختباراً للسياسة |
| `tests/unit/eta-estimate-arrival.test.ts` | ١٩ اختباراً للوصلة والجسر |
| `tests/unit/eta-osrm-snap.test.ts` | ٨ اختباراتٍ لقراءة الإلصاق مقابل خادمٍ مُزيَّفٍ يُصغي على منفذٍ حقيقيّ |
| `docs/adr/0024-eta-is-routing-duration-or-nothing.md` | القرار |
| `docs/production-readiness/PHASE-15-ETA.md` | هذا الملف |

## ٤. الملفّات المُعدَّلة

| الملف | التغيير |
| --- | --- |
| `packages/shared/config/index.ts` | ‏`routingProvider` و`osrmBaseUrl` يُقرآن ويُتحقَّق منهما: اسمٌ مجهولٌ يُرفَض، و`osrm` بلا عنوانٍ يُرفَض، وعنوانٌ بلا `http/https` يُرفَض |
| `packages/maps/core/types.ts` | ‏`RouteSnap` اتحادٌ مميَّز، و`RouteResult.snap` **إلزاميّ** |
| `packages/maps/providers/osrm/osrm-provider.ts` | يقرأ `waypoints[].distance` لأوّل نقطةٍ وآخرها بشرط تطابق العدد، وإلاّ `{known:false}` |
| `packages/application/tracking/driver-trip-card.ts` | يعيد `{view, eta}`، و`routing` تبعيّةٌ إلزاميّةٌ قابلةٌ للعدم |
| `packages/application/bots/driver-trip-reply.ts` | ‏`driverTripText(view, eta, tr)` + قاعدةُ كتمان الامتناع غير المُفيد |
| `packages/application/bots/driver-dialog.ts` | تبعيّةُ `routing`، و`tripCardReplies` تُمرّرها |
| `packages/application/bots/rating-dialog.ts` | تبعيّةُ `routing` |
| `apps/gateway/src/container.ts` | **يبني المزوّد من الضبط** — أوّلُ موضعِ بناءٍ إنتاجيّ في تاريخ المستودع |
| `packages/shared/i18n/{ar,en,ur}.json` | مفتاحان جديدان (٢٨٩ مفتاحاً في كلٍّ منها) |
| `.env.example` · `render.yaml` | ‏`ROUTING_PROVIDER` مُضاف، وافتراضُه `none` |
| ‏٢٥ ملفَّ اختبار | تجهيزةُ `AppConfig` نالت الحقلين الجديدين |

### تصحيحٌ عرضيّ في `.env.example`

كان الملفُّ يضبط `ROUTING_PROVIDER=osrm` مع `OSRM_BASE_URL=` **فارغاً**، ويعرض
`valhalla` و`google` قيمتَين مدعومتين ولا وجودَ لهما في الكود. ولمّا صار المتغيّرُ
مقروءاً كان نسخُه حرفياً **يمنع الإقلاع**. فصار الافتراضُ `none`، والقيمُ المعروضة
هي المُنفَّذة وحدها.

## ٥. الملفّات التي لم تُلمَس ولماذا

| الملف | السبب |
| --- | --- |
| `packages/domain/tracking/driver-trip-view.ts` | المشهدُ يقول الحقيقةَ عن المسافة بوسمه، وزمنُ الوصول حُكمٌ منفصلٌ لا حقلٌ فيه. ودمجُه كان سيُلزم المجالَ النقيَّ بانتظار شبكة |
| `packages/application/tracking/customer-live-relay.ts` | زمنُ الوصول في خريطة العميل خارج النطاق: المُرحِّل يقرأ `orders` كلَّ ٥ ثوانٍ لكلّ بثّ (R-42)، فإضافةُ نداءِ شبكةٍ إلى تلك الحلقة قرارُ حملٍ لا قرارُ ميزة |
| مزوّد OSRM — منطقُ المهلة والتراجع | مقيسٌ وجيّد. القاعدةُ ٦: لا يُحذَف كودٌ يعمل بلا سببٍ مُثبَت |
| `packages/tracking/*` | الشجرةُ الموازية غيرُ الموصولة (R-35). وصلُها قرارٌ معماريٌّ مستقلّ |
| ‏`nearest` و`table` | لا يستخدمهما زمنُ الوصول. والمرحلةُ ٨ منعت `nearest` في تحديد الموقع صراحةً |

## ٦. الاختبارات

| البند | العدد |
| --- | --- |
| اختباراتٌ أُضيفت | **٦٦** (٢٥ سياسة + ١٩ وصلة + ٨ مزوّد + ٨ ضبط + ٣ توصيل + ٣ تعديلُ توقيعٍ في اختباراتٍ قائمة) |
| المجموع قبل المرحلة | ‏١٣٣١ ناجحاً |
| المجموع بعدها | **‏١٣٩٧ ناجحاً / ٠ فاشل** — ٥١٨٧ توكيداً، ٩٨ ملفاً |

### إثباتُ التوصيل (جوهر R-28)

ثلاثةُ اختباراتِ تكاملٍ في `tests/integration/tracking-realtime.test.ts` تمرّ من
`buildContainer` بضبطٍ حقيقيّ، إلى خادم OSRM مُزيَّفٍ يُصغي على منفذٍ حقيقيّ، إلى
**نصّ الرسالة التي يقرؤها السائق**:

1. ‏`ROUTING_PROVIDER=osrm` ⇒ السائق يقرأ «١٢ دقيقة»، ولا يفقد الترويسةَ ولا سطرَ
   المسافة المستقيمة (زمنُ الوصول إضافةٌ لا استبدال).
2. عنوانٌ لا يُصغي عليه شيء ⇒ سطرُ «غير متاح» **مع** بقيّة البطاقة سليمة.
3. ‏`ROUTING_PROVIDER=none` ⇒ **لا رقمَ ولا شكوى**: منصّةٌ لم تُضبَط لا تملك ما
   تقوله، وسطرُ الشكوى فيها ضجيجٌ دائمٌ في كلّ بطاقةٍ لكلّ سائق.

واختبارُ وحدةٍ يبني المزوّدَ بيده كان سيُثبت أنّ المزوّد يعمل، **لا أنّ الحاوية
توصله** — وهو بالضبط ما كان مكسوراً.

### فحص التحوير

‏١٣ مُحوِّراً في المصادر (السياسة، الوصلة، المزوّد، الجسر، الضبط، العرض، الحاوية).
**١٣ قُتِل. صفرُ ناجين.** ومنهم `config.routingProvider === "osrm"` ⟶ `false` (عودةُ
R-28 نفسها) ⇒ قُتِل باختبارَي تكامل.

وفي الجولة الأولى **نجا مُحوِّرٌ واحد**: تبديلُ مفتاح الخطأ المُبلَّغ عنه
(`OSRM_BASE_URL` ⟶ اسمٌ آخر). والاختبارُ كان يقيس رمزَ الخطأ لا مفتاحَه، فقُوِّيت
ثلاثةُ توكيداتٍ لتقيس المفتاح — لأنّ المشغّلَ يقرأ سجلَّ الإقلاع ليعرف **أيَّ**
متغيّرٍ يُصلح، ورمزٌ صحيحٌ بمفتاحٍ خطأ يُرسله إلى المتغيّر الخطأ. ثمّ قُتِل.

### عيبٌ كشفه اختبارٌ فأُصلح المصدرُ لا الاختبار

`estimateArrival` كان تعليقُها يوثّق «لا تُلقي أبداً» وكانت **تُلقي** إذا ألقى
المزوّد. وعقدُ `Result` يُلزم الكودَ المكتوب لا طبقةَ الشبكة تحته، و`fetch` يُلقي
في حالاتٍ لا يمرّ بها المسارُ الملتقَط داخل المزوّد. والمستدعي بطاقةُ رسالةٍ في
تلغرام، فاستثناءٌ واحدٌ هناك يُسقط **كلَّ** البطاقة — أي يُخفي نقطةَ الانطلاق
والمسافة لأجل حقلٍ تكميليّ. فأُغلِق الحدُّ عند آخر موضعٍ نملكه.

## ٧. البوابة

| الفحص | النتيجة | خط الأساس |
| --- | --- | --- |
| `bunx tsc --noEmit` | ✅ نظيف | نظيف |
| `bunx biome check .` | ‏٨ أخطاء · ١ تحذير · ٩ معلومات | **مطابقٌ تماماً** |
| `bun test` | ‏**١٣٩٧ ناجحاً / ٠ فاشل** | ‏١٣٣١ / ٠ |
| `bun scripts/check-i18n.ts` | ✅ ٣ قواميس متطابقة، ٢٨٩ مفتاحاً | ٢٨٧ |
| `bun scripts/check-migrations.ts` | مخالفتان سابقتان (`db_backups`, `webhook_events`) | نفسهما |
| فحص التحوير | ‏١٣/١٣ قُتِل | — |

لا سكربتَ `build`: ‏Bun يشغّل TypeScript مباشرةً، والتصريفُ هو بديلُه.

## ٨. نتائجُ الأمن

- **لا سطحَ جديد.** لا مسارَ HTTP جديد، ولا صلاحيّةً جديدة، ولا قراءةً جديدةً من
  القاعدة. زمنُ الوصول يُحسَب من موقع السائق القانوني ومن هدفٍ **قرّره المجال**
  (`view.target`) لا من `facts.trip.pickup` مباشرةً — فلا مصدرَ حقيقةٍ ثانٍ.
- **لا تسريبَ إحداثيّاتٍ عبر رسالةٍ لا تملكها.** حدُّ الصلاحيّة لم يتغيّر: البطاقةُ
  تُبنى بمفتاح السائق نفسه كما كانت.
- **مُدخَلٌ خارجيٌّ لا يُوثَق به.** ردُّ OSRM يُتحقَّق منه حقلاً حقلاً: `Number.isFinite`
  على المدّة والمسافة والإلصاق، ورفضُ السالب، وتطابقُ عدد نقاط الطريق. ورقمٌ
  غيرُ عدديٍّ من خادمٍ مُخترَقٍ يُنتج `PROVIDER_DOWN` لا «NaN دقيقة» في رسالة.
- **متغيّرٌ خطأٌ يمنع الإقلاع** بدلاً من خدمةٍ صامتةٍ موعودٍ بها.
- **تنبيه:** ‏`OSRM_BASE_URL` يقبل `http` عن قصدٍ (نشرٌ داخل شبكةٍ خاصّة). ونشرُه
  على `http` عبر شبكةٍ عامّة يعرّض إحداثيّاتِ السائق للاعتراض — قرارُ مشغّلٍ لا
  قرارُ كود، ومذكورٌ في `.env.example`.

## ٩. المخاطر المتبقّية

| # | الوصف | الأولويّة |
| --- | --- | --- |
| **R-54** (جديد) | ‏`MAX_SNAP_METERS = 1000` مُعايَرٌ على ١٥ نقطةً في جدّة ومكّة فقط. غيرُ مُعايَرٍ للرياض والمدينة والطائف؛ شبكةٌ أخفُّ قد تُنتج `OFF_ROAD` لموضعٍ مشروع | P2 |
| **R-55** (جديد) | مهلةُ ٣ ثوانٍ الإجماليّة غيرُ مُعايَرةٍ على خادمٍ محمَّل، و**لا قاطعَ دائرة**: خادمٌ بطيءٌ يُضيف حتّى ٣ ثوانٍ إلى كلّ بطاقةِ رحلةٍ وكلّ ردٍّ على قبول عرض | P1 |
| **R-28** (شقٌّ باقٍ) | قاطعُ الدائرة لم يُبنَ. الشقُّ الأوّل (التوصيل) مُغلَقٌ ومُثبَتٌ باختبارَي تكامل | P1 |
| **R-42** | مُرحِّلُ خريطة العميل يقرأ `orders` كلَّ ٥ ثوانٍ لكلّ بثّ — وهو سببُ تأجيل زمن الوصول للعميل | P1 |
| مهمّةُ مشغّل | ‏OSRM إنتاجيٌّ غيرُ مُستضاف. `router.project-osrm.org` تجريبيٌّ وسياستُه تمنع الإنتاج | P0 لتشغيل الميزة |

## ١٠. خارج النطاق صراحةً

| البند | السبب |
| --- | --- |
| زمنٌ يراعي الازدحام | يستدعي مصدرَ بياناتٍ حركيّاً غيرَ موجود |
| زمنٌ يتعلّم من الرحلات السابقة | طبقةُ تعلّمٍ لم يُطلَب بناؤها؛ والمرحلةُ تنصّ «مدّةُ توجيهٍ فقط، بلا ذكاءٍ اصطناعيّ» |
| مطابقةُ المسار (Map Matching) | المرحلة ١٦ |
| زمنُ الوصول في خريطة العميل الحيّة | مؤجَّلٌ إلى قياس الحمل في المرحلة ٢٤ (R-42) |
| زمنُ الوصول في التوزيع وترتيب المرشّحين | التوزيعُ يستخدم هافرساين عن قصد (المرحلة ٨). وإدخالُ نداءِ شبكةٍ في حلقة الترشيح قرارُ حملٍ مستقلّ |
| استضافةُ OSRM | مهمّةُ مشغّلٍ لا مهمّةُ كود |
| ‏`valhalla` و`google` | كانا موعوداً بهما في `.env.example` ولم يُنفَّذا. صارا مرفوضَين صراحةً في الضبط بدلاً من قبولٍ صامت |

## ١١. الحالة النهائية

| البند | STATUS | EVIDENCE | TEST | RISK |
| --- | --- | --- | --- | --- |
| سياسةُ زمن الوصول في المجال | مُنفَّذة | `packages/domain/eta/index.ts` نقيٌّ بلا استيرادات | ‏٢٥ اختباراً، ٣ محوّراتٍ قُتِلت | R-54 (سقفُ الإلصاق مُعايَرٌ على مدينتين) |
| وصلُ المحرّك في الإنتاج | مُنفَّذ ومُثبَت | `apps/gateway/src/container.ts` يبني من الضبط | ‏٣ اختباراتِ تكاملٍ من الحاوية إلى نصّ الرسالة | R-55 (لا قاطعَ دائرة) |
| قراءةُ بعد الإلصاق | مُنفَّذة | `waypoints[].distance` بشرط تطابق العدد | ‏٨ اختباراتٍ مقابل خادمٍ يُصغي فعلاً | — |
| التحقّقُ من الضبط | مُنفَّذ | ‏`InvalidEnvVarError` بالمفتاح الصحيح | ‏٨ اختباراتٍ، ومُحوِّرُ المفتاح قُتِل | — |
| زمنُ الوصول للعميل | **خارج النطاق** | ADR 0024 §«ما بقي مفتوحاً» | — | R-42 |
| تشغيلُ الميزة فعلياً | **يتطلّب مشغّلاً** | الافتراضُ `none`؛ لا خادمَ OSRM مُستضاف | — | P0 لتشغيل الميزة لا للإطلاق |

**المرحلة التالية:** ١٦ — مطابقةُ المسار (Map Matching).
