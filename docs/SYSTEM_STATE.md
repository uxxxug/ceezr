# وثيقة حالة النظام — وَصْلة (Waslah)

> **هذه الوثيقة هي المرجع الحاكم لحالة المشروع الهندسية.**
> `docs/MASTER_DIRECTIVE.md` يحكم *قواعد العمل*؛ وهذه الوثيقة تحكم *وصف الحالة*.
> عند التعارض بين أي وثيقة أخرى في `docs/` وهذه الوثيقة بشأن **حالة النظام الحالية**، فهذه الوثيقة هي الصحيحة، وتُصحَّح الأخرى أو تُوسَم كلقطة تاريخية.

| | |
|---|---|
| **آخر تحديث** | 2026-09-20 — **`PD-051` (بطاقةُ عرضِ السائقِ) أُغلِقَت بـ`[x]` بعدَ ثلاثِ جولاتٍ خضراءَ متتالياتٍ على `main` (`ح-4`): `claim_notification_delivery` يضمُّ بياناتِ الطلبِ (الخدمةُ، الانطلاقُ، الوصولُ، الملاحظاتُ) إلى حمولةِ العرضِ، والناشرُ يبني البطاقةَ بالترتيبِ: خدمةٌ ← من ← إلى ← مسافة/وقتٌ ← ملاحظاتٌ (squash دمجِ PR #179، التزامُ الدمجِ `0a6b665`). والتشغيلاتُ `35535038207` + جولتانِ بتزامِ توثيقٍ. قبله 2026-09-20 — **`PD-040` (أهليّةُ التجربةِ) أُغلِقَت بـ`[x]` بعدَ ثلاثِ جولاتٍ خضراءَ متتالياتٍ على `main` (`ح-4`): `start_trial` يرفضُ بدءَ التجربةِ قبلَ توثيقِ السائقِ وتفعيلِ مدينتِهِ، و`admin_set_driver_verification` يبدأُ التجربةَ تلقائيًّا عندَ التوثيقِ (squash دمجِ PR #177، التزامُ الدمجِ `8529c43`). والتشغيلاتُ `35530589273` و`35530897764` و`35531169916`. قبله 2026-09-20 — **`PD-020` (قناةُ السلامةِ) أُغلِقَت بـ`[x]` بعدَ ثلاثِ جولاتٍ خضراءَ متتالياتٍ على `main` (`ح-4`): مدخلُ استغاثةٍ ظاهرٌ من كلِّ سطحِ راكبٍ، وفصلُ «استُقبِلَ» عن «اطّلعَ»، وفعلُ «تعذَّرَ الإكمالُ» لمسارِ السائقِ (squash دمجِ PR #175، التزامُ الدمجِ `0fba809`). والتشغيلاتُ `35524330151` و`35524806536` و`35525387994`. واكتشافٌ جانبيٌّ أُصلحَ أثناءَ الإغلاقِ: تجاوزٌ غيرُ مُصنَّفٍ في `tests/integration/driver-cannot-complete.test.ts` أُضيفَ إلى سجلِّ OPS-009 وخطوةِ CI بلا تخفيفٍ للحاجزِ. قبله 2026-09-20 — **`PD-020` (قناةُ السلامةِ) دُمِجَت في `main` (squash دمجِ PR #175، التزامُ الدمجِ `0fba809`). ثلاثةُ شقوقٍ: مدخلُ استغاثةٍ ظاهرٌ من كلِّ سطحِ راكبٍ، وفصلُ «استُقبِلَ» عن «اطّلعَ»، وفعلُ «تعذَّرَ الإكمالُ» لمسارِ السائقِ. والجولةُ الخضراءُ الأولى على `main` (التشغيلُ `35524330151` — كلُّ الوظائفِ خضراءُ). والإغلاقُ معلَّقٌ على ثلاثِ جولاتٍ خضراءَ متتالياتٍ (`ح-4`). قبله 2026-09-20 — **`ECO-008` (الشقُّ المملوكُ للمستودَعِ) نُفِّذَ كزيادةٍ لا إغلاقاً للبندِ: حسّاسيّةُ شكلِ التكلفةِ عندَ ١٠x من حجمِ الرحلاتِ صارت مقيسةً على السِلكِ (عشرُ رحلاتٍ متتابعةٍ بالشكلِ المُعلَنِ في حاويةٍ واحدةٍ) بسقوفٍ حجميّةٍ مُستوردةٍ من سقوفِ الرحلةِ الواحدةِ (`ECO-004`) لا مكتوبةٍ رقماً — حَكَمٌ نقيٌّ بـ12 قاعدةً وحاجزٌ ساكنٌ بـ13 قاعدةً وسالباتٌ مبذورةٌ لكلٍّ منهما (ح-7) وستُّ خطواتٍ مُسمّاتٍ في CI (ADR 0156). والرمزُ يبقى `[ ]`: السعرُ في فاتورةِ مزوّدِ سحابةٍ (REQ-09) والسعةُ عملُ مختبرِ النشرِ (DEC-17).** قبله 2026-09-20 — **حاجزُ حداثةِ وثيقةِ الحالةِ (check-system-state.ts) أُضيفَ لمنعِ تَقادُمِها أكثرَ من 14 يومًا، مع وحدةٍ نقيةٍ واختبارِ سالبٍ (ح-7) وخطوتين في CI. قبله ECO-004 دُمِجَ وأُصلِحَ قياسُه: pg_stat_database صارَ مستقرًّا بـpg_stat_clear_snapshot وآليّةِ استقرارٍ، وفُجوةُ order_offers في الحاجزِ الساكنِ أُغلقَت (القاعدةُ 11)، وحدودُ القياسِ وُضِّحَت. قبله ECO-002 وECO-003 وECO-004 وF1-09 وSEC-15 دُمِجَت في main بينَ 2026-09-19 و2026-09-20.** قبله 2026-09-08 — **اختبارُ الإغلاقِ الرشيقِ (`F5-05`) صارَ حتميّاً: مهلةُ التمهيدِ رُفِعَت من حافّةِ الإخفاقِ، وتفريغُ الأنبوبِ صارَ إلى آخرِه، والمنفذُ صارَ فريداً حتماً لا عشوائيّاً؛ **ثمَّ كشفَت الجولةُ الثانيةُ فجوةً في الإنتاجِ نفسِه فأُضيفَت نافذةُ إعلانٍ صريحةٌ (`announceMs`) إلى دورةِ الحياةِ حتى يرى المُوجِّهُ «مُصرِّف» قبلَ إغلاقِ المقبسِ** — والتوكيداتُ كلُّها باقيةٌ حرفاً بحرفٍ. التفصيلُ في الملحقِ `ي` أدناه.** قبله **اختبارُ خطّةِ PostGIS (`CAP-003` §٧) صارَ حتميّاً بـ`analyze drivers` قبلَ `EXPLAIN`؛ فبوّابةُ CI لم تعُدْ تُخفِقُ لإحصاءٍ بائدٍ — التفصيلُ في الملحقِ `ط` أدناه. ولا توكيدَ أُرخِيَ ولا بندَ قُلِبَ (`CAP-003`/`F7-01` كانا `[x]` وبقيا).** قبله **حارسُ الإنتاجِ لرفضِ الحمولةِ بلا `update_id` صالحٍ نُفِّذَ ودُمِجَ (سدُّ فجوةِ [ADR 0054](adr/0054-telegram-webhook-durable-ingest-and-dedup.md) §٦ الذي كشفَه تدقيقُ إغلاقِ `CAP-001`/`F6-01`) — التفصيلُ في الملحقِ `ح` أدناه. ولا بندَ في `ROADMAP-MASTER` قُلِبَ بهذا التغييرِ (`ح-4` غيرُ مُستحَقٍّ: إصلاحُ عيبٍ داخلَ بندٍ مُغلَقٍ لا قلبُ حالةٍ).** قبله **قُلِبَ `F5-01` إلى `[x]` بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) على الفرعِ `audit/f5-01-bugs-closure` — الجولاتُ `34163094674` و`34163610568` و`34164056016` كلُّ وظيفةٍ فيها `success`، التفصيلُ في الملحقِ الرابعِ والأربعينَ أدناه.** تدقيقٌ مركَّبٌ بلا جديدِ كود (`ح-1`): كلُّ عيبٍ من BUG-001 إلى BUG-009 يستوفي العناصرَ الأربعةَ المطلوبةَ (اختبارُ إعادةِ إنتاجٍ → إسلامٌ → اختبارُ انحدارٍ → اختبارٌ توزيعيٌّ). CI محلّيًّا: `lint`=0 · `typecheck`=0 · ٢١٧٧ ناجحًا / ٦٢٥ متجاوَزًا / ٠ فاشلاً / ٦٨٦٩ توكيدًا في ٢١٢ ملفًّا. قبله `F6-01`/`CAP-001` قُلِبا إلى `[x]` بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ على الفرعِ `audit/f6-01-cap-001-thin-ingest` (`ح-4`) — الجولاتُ `34149393266` و`34149836063` و`34150009212` كلُّ وظيفةٍ فيها `success`، التفصيلُ في الملحقِ الثالثِ والأربعينَ أدناه.** ثبَتَ قراءةً من الكودِ الفعليِّ أنَّ مسارَ الـingest النحيفَ بترتيبِهِ الستّيِّ منفَّذٌ فعلاً ضمنَ عملِ `BUG-002`/`F6-02` المُغلَقَينِ بـ`[x]` (سرّ ← حدّ معدّل ← `update_id` ← استلامٌ صامدٌ ذرّيٌّ + طابور ← ACK ← عاملٌ منفصلٌ) — إغلاقٌ مركَّبٌ لا جديدَ كودٍ فيه. قبله في اليومِ نفسِه نُفِّذَ الشطرُ المفقودُ من `BUG-002`/`F6-02` (حاملُ الحمولةِ المقيدُ `telegram_update_jobs` + أربعُ دوالَّ ذرّيّةٍ + عاملُ درينرٍ منفصلٌ) ونُقِلَتْ `handler.handle` خارجَ طلبِ `HTTP`، لكنّ القلبَ إلى `[x]` كان معلَّقًا على ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`)، ولم تكتملْ بعدُ سوى الجولةُ الأولى (التشغيلُ `34127610123` على `c3d6e71`) — التفصيلُ في الملحقِ الأربعينَ أدناه.** قبله `BUG-006` أُغلقَ بـ`[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها (`ح-4`): حدُّ المعدّلِ على Redis صار ذرّيًّا بسكربتِ Lua واحدٍ (نافذةٌ منزلقةٌ بـZSET عبرَ `EVAL`) بدلَ `INCR`+`EXPIRE` المنفصلَينِ، فلا تُفقدُ المهلةُ ولا تُتجاوزُ الحدودُ عندَ التزامنِ؛ وسياسةُ الفشلِ «مفتوحٌ» صارت صريحةً ومعتمدةً بـADR 0055. مقيسٌ باختبارٍ توزيعيٍّ على Redis حقيقيٍّ في CI (٢٠ طلبًا متزامنًا ⇒ ٥ مسموحةٍ بالضبطِ، `PTTL > 0`، `ZCARD = الحدّ). والملحقُ الحادي والثلاثونَ أعلاه يُقرأُ من الآنَ مقيَّداً بهذا الملحقِ كما كانَ الثلاثونَ مقيدًا بالحادي والثلاثينَ. **ثمَّ `BUG-007` أُغلقَ بـ`[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ في التشغيلِ `34053661017` على `c646b6a` لا قبلَها (`ح-4`): جلساتُ الحوارِ صارت تحملُ مراجعةً رمزيّةً وكتابةً شرطيّةً (CAS ذرّيٌّ بسكربتِ Lua `EVAL`) مع إعادةِ محاولةٍ محدودةٍ في المحوّل، فلا تُفقدُ خطوةُ الحوارِ عندَ الكتابةِ المتزامنةِ؛ ولا سطرٌ في أيِّ حوارٍ غُيِّر.** وكان `BUG-002` وقتَ تلك اللقطةِ مفتوحًا (يعتمدُ على `REQ-03` المحجوبِ للمالكِ، ثم أُغلقَ اليومَ بـ`[x]` بعدَ ثلاثِ جولاتٍ خضراءَ — الملحقُ 41 أدناه)، و`SCL-002` أُغلقتْ بعدَها بـ`[x]` في التشغيلِ `34054604942` على `619942f`، و`SCL-003` أُغلقتْ بعدَها بـ`[x]` كإغلاقٍ مُركَّبٍ (BUG-006 + SCL-002) في التشغيلِ `34054852982` على `667c01f`، و`F5-03` أُغلقتْ بعدَها بـ`[x]` كإغلاقٍ مُركَّبٍ (SCL-002) في التشغيلِ `34055155911` على `0821ece`، **ثمَّ `F5-05`/`CAP-008` أُغلقا بعدَها بـ`[x]` (تصريفٌ رشيقٌ عند `SIGTERM` + `/ready` متوازٍ بمهلات + بوّابةُ رفضِ الجديدِ أثناءَ التصريف) في الملحقِ السابعِ والثلاثينَ أدناه**، **ثمَّ `CAP-004` أُغلِقَ بعدَها بـ`[x]` (اشتقاقُ `prepare` من رابطِ الاتصالِ + قرارُ pooler موثَّقٌ بـADR 0056 + نموذجُ ميزانيّةِ اتصالاتٍ `5N+10M`) في الملحقِ الثامنِ والثلاثينَ أدناه**، و`F6-03` يبقى `[ ]`، و`DEC-11` محجوبٌ، وبوابةُ `F0` `[~]`.** التفصيلُ في الملاحقَ الثاني والثلاثينَ والثالثِ والثلاثينَ والرابعِ والثلاثينَ والخامسِ والثلاثينَ والسادسِ والثلاثينَ والسابعِ والثلاثينَ والثامنِ والثلاثينَ والرابعِ والأربعينَ أدناه |
| **الـ commit الذي تصف هذه الوثيقة حالته** | التزامُ قلبِ `F6-01`/`CAP-001` إلى `[x]` بعدَ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) على الفرعِ `audit/f6-01-cap-001-thin-ingest` (الجولاتُ `34149393266` و`34149836063` و`34150009212`، الملحقُ 43)؛ وقبلَه `ab2d009` — التزامُ توثيقِ إغلاقِ `BUG-002`/`F6-02` بـ`[x]` بعدَ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) (squash دمجِ PR #17 `docs/close-bug-002-f6-02` إلى `main`، الملحقُ 41)؛ وقبلَه `a1b2e3d` — رأسُ `main` بعدَ التزامِ الكودِ الذي أغلقَ `CAP-004` بـ`[x]` (اشتقاقُ `prepare` من رابطِ الاتصالِ + قرارُ pooler موثَّقٌ بـADR 0056 + نموذجُ ميزانيّةِ اتصالاتٍ `5N+10M`) في الملحقِ الثامنِ والثلاثينَ؛ وقبلَه `6395ead` — تسجيلُ نتيجةِ CI للإغلاقِ في الدليلِ والملحقِ 37؛ وقبلَه `628ae97` (تسجيلُ hashِ الالتزامِ `8880333`)، وقبلَه `8880333` — التزامُ الكودِ الذي أغلقَ `F5-05`/`CAP-008` بـ`[x]` (تصريفٌ رشيقٌ عند `SIGTERM` + `/ready` متوازٍ بمهلات + بوّابةُ رفضِ الجديدِ أثناءَ التصريف) في الملحقِ السابعِ والثلاثينَ، وقبلَه `0821ece` الذي كانت تصفُه بإغلاقِ `F5-03` في الملحقِ السادسِ والثلاثينَ، وقبلَه `667c01f` الذي كانت تصفُه بإغلاقِ `SCL-003` في الملحقِ الخامسِ والثلاثينَ، وقبلَه `619942f` الذي كانت تصفُه بإغلاقِ `SCL-002` في الملحقِ الرابعِ والثلاثينَ، وقبلَه `c646b6a` الذي كانت تصفُه بإغلاقِ `BUG-007` في الملحقِ الثالثِ والثلاثينَ، وقبلَه `5f082cb` الذي كانت تصفُه بإغلاقِ `BUG-006` في الملحقِ الثاني والثلاثينَ، وقبلَه `90b0413` الذي كانت تصفُه بإغلاقِ `BUG-004` في الملحقِ الحادي والثلاثينَ (`2f4ca1c` سابقَه)، وقبلَه `e9ae9dc` الذي كانت تصفُه بشطرِ العرضِ وحدَه (`[~]`، الملحقُ الثلاثون)، وقبلَه `8969a5dad3645a6ae0687daaf82150568409eb10` |
| **المرجع السابق (لقطة تاريخية)** | كانت هذه الوثيقة تصف `b61ae98` (`Feat/system state baseline (#13)`) مع تغييرات وحدة 2-3. وآخرُ لمسةٍ لها قبل اليوم كانت في `8638f7f`. **النصُّ القديم محفوظٌ في مواضعه ولم يُحذَف منه شيء**، وما صار متجاوزاً منه موسومٌ `Historical Snapshot` حيث هو |
| **تصحيحٌ مُلحَق — 2026-08-27** | رأس `main` صار **`c4149f69`** بعد ثلاثة التزامات (`03749902` مسبار SSE · `e7874fdd` مسبار WebSocket · `2123547e` مسبار Socket.IO) ثم `c4149f69` (هيكل `F1-01`). **فاللقطة أدناه محرَّرةٌ تصحيحاً جزئياً في مواضع `DEC-06` وMini App و`F1` فقط؛ وإعادةُ بنائها الكاملة على الرأس الجديد تجري في وحدة تدقيق إغلاق `F1-01`** |
| **تصحيحٌ مُلحَق ثانٍ — 2026-08-27 (وحدة إغلاق `F1-01`)** | **`main` لم يكن قابلاً للبناء إطلاقاً منذ `c4149f69`:** `bun install --frozen-lockfile` يسقط لأنّ `bun.lock` الجذريَّ لا يحوي مساحةَ عمل `apps/miniapp` ولا اعتمادياتِها، و`bun run lint` يسقط بأربعةِ أخطاء، و`bun run typecheck` يسقط بخمسةِ أخطاءَ في ملفّات Mini App، **وحاجزُ `verify-container-build.sh` يسقط أيضاً بعد إصلاح القفل** لأنّ ملفَّي الصورة ينسخان بيانَ الجذر وحده. أُصلحت الأسبابُ الأربعة في هذه الوحدة. **ونطاقُ الوحدة حُصِر بأمرِ العمل في `F1-01` وحده، فإعادةُ بناءِ اللقطة كاملةً على الرأس الجديد — الموعودةُ في الصفّ السابق — لم تُنفَّذ بعد وتبقى فجوةَ حَوكمةٍ معلنة**، والمصحَّحُ ههنا مواضعُ `F1-01` وMini App وبوابةِ CI وحدها |
| **تصحيحٌ مُلحَق ثالثٌ — 2026-08-27 (وحدة `F1-02`)** | بندُ `F1-02` صار `[x]` بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-02-20260827.md`) وسطرٌ في سجل القسم 25. **والمُختبَرُ فيه مضيفٌ مُصطنَعٌ لا جهازٌ حقيقي**: الطبقةُ تُمرِّر وتُبوِّب وتصمُد أمامَ غيابِ تيليجرام، **وسلوكُ عميلِ تيليجرامَ الحقيقيِّ لم يُقَس** — بوابةُ `F1` لا تُفتح بهذا البند. وعددُ الحرّاسِ المسمَّى في `verify` صار **17** بحاجزِ عزلِ الطبقة، وسلسلةُ `bun run ci` صارت **12** سكربتَ حراسة. و`F1-03`…`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق رابعٌ — 2026-08-27 (وحدة `F1-03`)، ودرجتُه رُفِعت 2026-08-28** | بندُ `F1-03` صار `[x]` بدرجةِ **مُنفَّذ · مُختبَر · مُتحقَّق منه** — تشغيلُ CI `33109904661` نجح في الوظيفتَين معاً على `dc40dde2` (ومنها الحرّاسُ الثمانيةَ عشرَ)، **ورُفِعت الدرجةُ بعدَ اكتمالِ التشغيلِ لا قبلَه، والمُتحقَّقُ منه التشغيلُ لا مصداقيةُ المدخل، ولا `مَقيس` ولا `مُثبَت`** (`ح-5`). وله ملفُ دليلٍ (`docs/evidence/architecture/F1-03-20260827.md` — و§٨ فيه وحدةُ رفعِ الدرجة) وسطرٌ في سجل القسم 25. **والمُختبَرُ فيه `initData` مُصطنَعٌ نولّده بأنفسِنا وفقَ الخوارزميةِ الموثَّقةِ**، فلو كان فهمُنا للوثيقةِ خاطئاً لَاتَّفَق المُولِّدُ والفاحصُ على الخطأ ومرَّت الاختباراتُ خضراء؛ **ولا ادّعاءَ تحقّقٍ على عميلِ تيليجرامَ حقيقيٍّ ولا على جهاز** — بوابةُ `F1` لا تُفتح بهذا البند. **ولا استمرارَ ولا إبطالَ ولا تجديدَ للجلسةِ (`F1-04`)، ولا مسارَ منتَجٍ واحدٌ يستهلك الرمزَ الصادر.** وعددُ الحرّاسِ المسمَّى في `verify` صار **18** بحاجزِ منعِ تسريبِ الأسرارِ إلى السجلّات، وسلسلةُ `bun run ci` صارت **13** سكربتَ حراسة. و`F1-04`…`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق خامسٌ — 2026-08-28 (وحدة `F1-04`)** | بندُ `F1-04` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-04-20260828.md`) وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33128888809` بنتيجةِ `success` في الوظيفتَين على `d388bf2d` لا قبلَه** (§٩ من ملفِّ الدليل)، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **13** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ خطوةً مسمّاةً في وظيفةِ `verify`. **والمُختبَرُ رموزٌ ومخزنٌ مُصطنَعان في العملية**: لا جهازَ ولا عميلَ تيليجرامَ حقيقيٌّ، والمُتحقَّقُ منه أنّ الطبقةَ تنادي المخزنَ الصحيحَ ولا تنادي غيرَه — **بوابةُ `F1` لا تُفتح بهذا البند**. و`F1-05`…`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب، ولا مسارَ منتَجٍ واحدٌ يستهلك رمزَ الجلسةِ بعد |
| **تصحيحٌ مُلحَق سادسٌ — 2026-08-28 (وحدة `F1-05`)** | بندُ `F1-05` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-05-20260828.md`) وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33162315073` بنتيجةِ `success` في الوظيفتَين على `27caef7c` لا قبلَه** (§٩ من ملفِّ الدليل)، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **14** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ خطوةً مسمّاةً في وظيفةِ `verify`. **والمُختبَرُ رموزٌ وقارئُ حسابٍ مُصطنَعان في العملية: لا جهازَ ولا عميلَ تيليجرامَ حقيقيٌّ، ولا قاعدةَ حقيقيةَ في اختباراتِ هذا البند** — فالاستعلامُ الفعليُّ على `users` غيرُ مُشغَّلٍ على PostgreSQL ههنا، والمُتحقَّقُ منه أنّ المحوّلَ يبني استعلامَ قراءةٍ مُمَعلَماً ولا يكتب. **بوابةُ `F1` لا تُفتح بهذا البند.** **وحدودٌ وظيفيةٌ معلَنةٌ لا مُخفَّفة:** لا ربطَ/إنشاءَ مستخدمٍ عندَ أوّلِ جلسةٍ (القسم 9.8 خطوة 4) فالتسجيلُ يبقى على البوت، ولا تفويضَ على مستوى الكائنِ، ولا حدَّ معدّلٍ على المسارِ (العقدُ يوجبه بلا أرقامٍ فلم تُخترَع أرقامٌ)، ولا سطحَ `support` (مؤجَّلٌ والدورُ يُعاد صادقاً)، **وسطحُ المشرفِ داخلَ Mini App جذرٌ فارغٌ لا أداةَ إشرافٍ فيه، وأدواتُ الإشرافِ تبقى في لوحةِ الإدارةِ على الخادمِ (ADR 0007)، والتوفيقُ بينهما قرارٌ معماريٌّ لاحقٌ مطلوبٌ ومُعلَنٌ**. و`F1-06`…`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق سابعٌ — 2026-08-28 (وحدة `F1-06`)** | بندُ `F1-06` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-06-20260828.md`) وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33165448602` بنتيجةِ `success` في الوظيفتَين على `7f77be4f` لا قبلَه** (§٩ من ملفِّ الدليل)، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. **ولا حاجزَ جديدٌ في هذه الوحدة**: سلسلةُ `bun run ci` تبقى **14** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وحاجزُ عزلِ طبقةِ تيليجرامَ القائمُ **يُفرِض أنّ كلَّ نداءٍ للسمةِ داخلَ `tg/`** وأنّ `Shell` يستوردها من `tg/index.ts` وحدَه. **والمُختبَرُ مضيفٌ ومستندٌ مُصطنَعان في العملية: لا جهازَ ولا عميلَ تيليجرامَ حقيقيٌّ ولا iOS ولا أندرويد ولا شاشةَ ذاتِ نتوءٍ ولا مُصيِّرَ CSS يحسب `env()`/`max()`/`calc()`** — فالمُتحقَّقُ منه أنّ القيمَ تصل إلى موضعِها المتفَقِ عليه، **لا أنّ الرأسَ لُوِّن فعلاً على جهازٍ ولا أنّ التخطيطَ المقلوبَ سليمٌ بالعين**. **بوابةُ `F1` لا تُفتح بهذا البند.** **وحدودٌ معلَنةٌ لا مُخفَّفة:** لا تصميمَ بصريَّ كاملاً (لا مكوّناتٍ ولا سلّمَ خطوطٍ ولا ألوانَ هويةٍ للتمييز)، ولا فحصَ تباينٍ آلياً فتمامُ UX-6 نقطةٌ مفتوحةٌ، ولا ربطَ بقواميسِ `packages/shared/i18n` (رموزُ اللغاتِ الثلاثِ مُعلَنةٌ محلياً بلا نصوصٍ والافتراضُ متطابقٌ، والتوحيدُ وشاشةُ اختيارِ اللغةِ `F2-11`)، ولا ميزانيةَ أداءٍ مفروضةً في بوابةٍ (`F1-09`)، ولا شاشاتِ `SS-01…SS-05` ولا حدودَ أخطاءٍ (`F1-07`) ولا شريطَ تنقّلٍ (القسم 9.3). و`F1-07`…`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق ثامنٌ — 2026-08-28 (وحدة `F1-07`)** | بندُ `F1-07` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-07-20260828.md`) وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33171122193` بنتيجةِ `success` في الوظيفتَين على `ba0d92c7` لا قبلَه** (§٩ من ملفِّ الدليل)، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **15** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ (`check-system-screens-policy`) خطوةً مسمّاةً في وظيفةِ `verify`. **والمُختبَرُ ردودٌ مُصطنَعةٌ باستبدالِ `globalThis.fetch` وتبعياتٌ محقونةٌ وعرضٌ نصيٌّ من `react-dom/server`: لا بيئةَ DOM ولا مكتبةَ اختبارِ مكوّناتٍ ولا متصفّحَ ولا مُصيِّرَ CSS ولا جهازَ ولا عميلَ تيليجرامَ حقيقيٌّ** — فالمُثبَتُ **بنيةُ الخَرْجِ الأوّلِ وحدَها**: لا ضغطَ زرٍّ، ولا أثرٌ (`useEffect`) يعمل، ولا انتقالُ حالةٍ بعدَ ردٍّ، ولا دورةُ إعادةِ مصادقةٍ حتى ظهورِ السطح، ولا `GET /health` حقيقيٌّ أُجيب. **ووظيفةُ «تكامل على PostgreSQL حقيقي» نجحت ولا أثرَ لها في هذا البند**: لا اختبارَ من هذه الوحدةِ يلمس قاعدةً. **بوابةُ `F1` لا تُفتح بهذا البند.** **وحدودٌ معلَنةٌ لا مُخفَّفة:** `SS-03` غيرُ منفَّذةٍ (لا عقدَ إصدارٍ: لا الخادمُ يُصدِر أدنى إصدارٍ مقبولٍ ولا العميلُ يحمل إصداراً) · `SS-04` مكتوبةٌ وغيرُ موصولةٍ (`GET /v1/cities` غيرُ منفَّذٍ) وبلا زرِّ «أبلغني» لأنه حالةُ أعمالٍ · `Retry-After` بصيغةِ تاريخِ HTTP لا تُقرأ · مسارُ التجديدِ يفقد الحالةَ الأصليّةَ فيُشخَّص بفحصِ الحياةِ لا بالردِّ · التجاوبُ ومقاسُ اللمسِ والتباينُ بلا قياسٍ ولا فحصٍ آليٍّ (تمامُ `UX-6` و`UX-10` نقطةٌ مفتوحةٌ) · لا ربطَ بقواميسِ `packages/shared/i18n` (`F2-11`) · لا شريطَ تنقّلٍ (القسم 9.3) · لا ميزانيةَ أداءٍ مفروضةً في بوابةٍ (`F1-09`) · ولا `request-id` ولا قياسَ واجهةٍ (`F1-08`). **وتوتُّرُ ترتيبِ ADR 0035 §3 «البنيةُ قبلَ الشاشات» مُعلَنٌ ولم يُحسَم ولم يُخالَف**: الشاشاتُ لا تحمل بياناً حيّاً ولا تستقصي، والحاجزُ الجديدُ يمنع أن تصير كذلك — **والقرارُ لصاحبِه**. **وصفُّ «المرحلة الحالية» أدناه كان متقادماً** (كان يذكر `F1-01`…`F1-03` وحدَها بعدَ إغلاقِ سبعةِ بنودٍ) **فصُحِّح ههنا وصفاً للحالةِ لا تغييراً للخارطة**. و`F1-08` و`F1-09` و`F1-10` كلُّها `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق تاسعٌ — 2026-08-29 (وحدة `F1-08`)** | بندُ `F1-08` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-08-20260829.md`) وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33219280511` بنتيجةِ `success` في الوظيفتَين على `d18e4290` لا قبلَه** (§٩ من ملفِّ الدليل)، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **16** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ (`check-telemetry-policy`) خطوةً مسمّاةً في وظيفةِ `verify` (الخطوةُ 30). **وأُنشئ ADR 0043** لأنّ ههنا قراراً معماريّاً: معرّفُ الطلبِ يولّده الخادمُ وحدَه في رأسٍ لا في جسمٍ، والقياسُ لا يخرج من الجهاز. **والحدودُ معلَنةٌ لا مُخفَّفة:** لا لوحةَ عرضٍ للأحداثِ ولا مصدِّرَ ولا سجلَّ يُستعلَم — **فلا أحدَ يقرأ الأحداثَ اليومَ بقرارٍ صريحٍ، وقيمةُ ما بُنِي بنيويّةٌ لا تشغيليّةٌ** · `traceparent` وW3C Trace Context **لم يُنفَّذا ولا نصفُهما** (بندُ الرصدِ في `F8`) · معرّفُ الطلبِ **لا يُسجَّل في سجلٍّ مبنيَّ للاستعلامِ على الخادمِ** بعد · قراءةُ الدورِ (`GET /v1/me`) وشاشاتُ `SS-01…SS-05` **غيرُ مقيسةٍ** لأنّ لا مُراقِبَ يُمرَّر من هناك · ولا قياسَ أداءٍ ولا `FCP` ولا `LCP` ولا حجمَ حزمةٍ مقيساً (`F1-09` لم يبدأ). **والمُختبَرُ عقدٌ ووَصْلٌ لا سلوكُ جهازٍ**: لا بيئةَ DOM ولا متصفّحَ ولا عميلَ تيليجرامَ حقيقيٌّ — `componentDidCatch` يُنادى نداءً مباشراً، والردودُ مصنوعةٌ بـ`Response`، وإصدارُ الرأسِ مُثبَتٌ في اختبارِ خادمٍ مستقلٍّ يُنادي تطبيقَ Hono في العمليةِ نفسِها — **ولم يُرَ الطرفانِ موصولَينِ عبرَ شبكةٍ حقيقيةٍ**. **ووظيفةُ «تكامل على PostgreSQL حقيقي» نجحت ولا أثرَ لها في هذا البند.** **بوابةُ `F1` لا تُفتح بهذا البند.** و`F1-09` و`F1-10` `[ ]` بعد، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق عاشرٌ — 2026-08-29 (وحدة `F1-09`)** | بندُ `F1-09` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر في نطاقِ ثلاثةِ صفوفٍ من ثمانيةٍ** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-09-20260829.md`) وسطرٌ في سجل القسم 25 وADR 0044. **والبندُ حالتُه `[~]` لا `[x]` بقرارٍ معلَنٍ**، بنصِّ سطرِ المفتاحِ «قيد التنفيذ فعلياً الآن»، **والسببُ نقصُ إمكانٍ لا نقصُ عملٍ**: `FCP` و`LCP` وزمنُ التفاعلِ تحتاج متصفّحاً بلا رأسٍ وشبكةً مُقيَّدةً وجهازاً، **وصفَّا بياناتِ جلسةِ عشرِ دقائقَ وإعادةِ رسمِ الخريطةِ يقيسان ميزةً غيرَ مبنيّةٍ بعدُ** (لا حزمةَ `map` ولا رحلةَ نشطةً) — **فهذه تبعيّةٌ عبرَ المراحلِ لا دَينٌ يُسدَّد في `F1`**، وفحصُهما اليومَ يمرُّ لأنّ لا شيءَ يُقاس. **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **17** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ (`check-performance-budget`) خطوةً مسمّاةً في وظيفةِ `verify` **بعدَ خطوةِ البناءِ** لأنّه يقرأ `apps/miniapp/dist`. **وأُنشئ ADR 0044.** **وتغييرٌ في السلوكِ لم يحدث**: كلُّ ما جرى في شيفرةِ التطبيقِ إعادةُ توزيعِ شيفرةٍ على حزمٍ وقلبُ اتجاهِ اعتمادٍ ودمجُ أنماطٍ في المستند — ولم تُبنَ ميزةٌ ولا شاشةٌ ولا نداءُ شبكةٍ. **والحدودُ معلَنةٌ لا مُخفَّفة:** الخمسةُ غيرُ المفحوصةِ لا يُدَّعى شيءٌ عنها · **لا زمنَ يُقاس ههنا إطلاقاً ولا يجوز قراءةُ خُضرةِ هذه البوّابةِ إثباتاً لأيِّ حدٍّ زمنيٍّ** · `brotli` غيرُ مقيسٍ (لا أداةَ في بيئةِ التنفيذِ) وgzip هو المُلزِمُ وهو الأشدُّ · المفحوصُ مُخرَجُ بناءٍ لا أصلٌ منشورٌ (**لا مضيفَ للتطبيقِ المصغَّرِ في `render.yaml`**) فلا CDN ولا رؤوسَ ضغطٍ · طلباتُ أوّلِ رسمٍ **عندَ الحدِّ بالضبطِ 6/6 بلا هامشٍ** فأيُّ إضافةٍ في الحملِ الأوّلِ تُسقِط البناءَ وذلك مقصودٌ · والأرقامُ بعيدةٌ عن الحدودِ لأنّ **83٪ من الحملِ `vendor-react` والحزمُ المؤجَّلةُ جذورٌ فارغةٌ لا شاشاتٌ** — فالسعةُ ليست دليلَ انضباطٍ. **وتوتُّرٌ معلَنٌ لم يُحسَم**: حزمةُ `admin` ليست في جدولِ القسم 9.4 وADR 0007 يجعل لوحةَ المشرفِ مُصيَّرةً من الخادمِ — مُسجَّلٌ في ADR 0044 §٦ ومطبوعٌ في استثناءِ الحاجزِ، **ولا يُحسَم في بندِ أداءٍ**. **ووظيفةُ «تكامل على PostgreSQL حقيقي» نجحت ولا أثرَ لها في هذا البند.** **بوابةُ `F1` لا تُفتح بهذا البند**: `F1-09` `[~]` و`F1-10` `[ ]`، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق حادي عشر — 2026-08-29 (وحدة `F1-10`)** | بندُ `F1-10` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر في نطاقِ النصِّ والبناءِ وسقوطِ الحاجزِ وحدَه** وله ملفُ دليلٍ (`docs/evidence/architecture/F1-10-20260829.md`) وسطرٌ في سجل القسم 25 وADR 0045. **وحالتُه `[~]` لا `[x]` بقرارٍ معلَنٍ، والسببُ نقصُ إمكانٍ لا نقصُ عملٍ**: لا متصفّحَ ولا DOM في بيئةِ التنفيذِ فلا دليلَ سلوكيَّ على أنّ متصفّحاً يُطبِّق السياسةَ ولا أنّ التطبيقَ يعمل تحتَها، **ولا خدمةَ للتطبيقِ المصغَّرِ في `render.yaml`** فشقُّ رأسِ الاستجابةِ غيرُ منفَّذٍ ولا يمكن تنفيذُه اليومَ. **و`TG-005` قُلِب إلى `[~]` لا `[x]` لهذا بعينِه.** **ونصُّ البندِ لم يُمَسّ (`ح-1`).** وسلسلةُ `bun run ci` صارت **18** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ، وأُضيف الحاجزُ الجديدُ (`check-single-origin-assets`) خطوةً مسمّاةً في وظيفةِ `verify` **بعدَ حاجزِ الأداءِ** لأنّه يقرأ `apps/miniapp/dist`. **وأُنشئ ADR 0045** (45 قراراً الآن). **وتغييرٌ في سلوكِ التطبيقِ لم يحدث إطلاقاً**: وسمٌ يُحقَن في وقتِ البناءِ ووحدتانِ نقيّتانِ وحاجزٌ — لا ميزةَ ولا شاشةَ ولا نداءَ شبكةٍ ولا حالةَ أعمالٍ ولا كيانَ ولا جدولَ ولا هجرةَ (ADR 0035 §2). **والعيبُ الذي أوقعه برهانُ السقوطِ يُسجَّل بلا تلوينٍ**: ثلاثةُ خروقٍ محقونةٍ مرَّت بخروجِ صفرٍ لأنّ دالّةَ تفريغِ التعليقاتِ كانت تفرّغ كلَّ عنوانٍ في المستودعِ، **فكلُّ قاعدةٍ تطابق عنواناً في أيِّ حاجزٍ يستعملها كانت ميتةً وهي خضراءُ** — وأُصلِح السببُ بوحدةٍ مشتركةٍ مُختبَرةٍ وُصِل بها هذا الحاجزُ و`check-telemetry-policy`، **وبقيّةُ الحواجزِ تُوصَل عندَ أوّلِ مساسٍ بها ولا يُعاد كتابةُ حاجزٍ سليمٍ بلا سببٍ — وهذا دَينٌ معلَنٌ**. **والحدودُ معلَنةٌ لا مُخفَّفة:** لا رأسَ استجابةٍ ولا `frame-ancestors` ولا `sandbox` ولا `report-to` (**والأخيرُ يخالف قرارَ مالكِ المنتجِ في `F1-08` أنّ القياسَ لا يخرج من الجهازِ — ADR 0043**) · لا `Trusted Types` · لا MapLibre ولا حزمةَ `map` · لا مضيفَ ولا CDN ولا أصلاً منشوراً · وقاعدةُ الأصلِ الخارجيِّ تحرس شيفرةَ الإنتاجِ وحدَها **وما بقي بلا حِراسةٍ مُعلَنٌ في §٩ من ملفِّ الدليلِ لا مسكوتاً عنه**. **وتناقضُ القسم 9.2 (MapLibre من CDN ضدَّ لا أصولَ تنفيذيةً من نطاقٍ ثالثٍ) مُعلَنٌ ولم يُحسَم ولم يُخالَف**، والحسمُ عندَ بناءِ حزمةِ الخريطةِ بـADR يُصحِّح القسمَ صراحةً لا بلَيِّ أحدِ الصفَّين (ADR 0045 §٦). **والمُختبَرُ نصُّ السياسةِ وموضعُها ومطابقةُ بصمتِها لأنماطِ المُخرَجِ وسقوطُ الحاجزِ، لا سلوكُ عميلٍ**: لا متصفّحَ ولا DOM ولا جهازَ ولا عميلَ تيليجرامَ حقيقيٌّ — **فلا `مَقيس` ولا `مُثبَت` (`ح-5`)، ولا يجوز قراءةُ خُضرةِ هذه البوّابةِ إثباتاً لأنّ حمايةَ الأصولِ المتقاطعةِ فعّالةٌ في متصفّحٍ**. **ووظيفةُ «تكامل على PostgreSQL حقيقي» نجحت ولا أثرَ لها في هذا البند.** **بوابةُ `F1` لا تُفتح بهذا البند**: `F1-09` و`F1-10` `[~]`، و`R-17` قائم، و`R-24` مفتوح، و`DEC-11` محجوب |
| **تصحيحٌ مُلحَق ثاني عشر — 2026-08-29 (وحدة `OPS-009`)** | بندُ `OPS-009` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر · مُتحقَّق منه** وله ملفُ دليلٍ (`docs/evidence/correctness/OPS-009-20260829.md`) و`ADR 0046` وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33253286342` بنتيجةِ `success` في الوظيفتَين على `100827cb` لا قبلَه**، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **19** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ. **والمُتحقَّقُ منه أنّ التصنيفَ محروسٌ وأنّ ما زُعِم أنّه يعمل عمل فعلاً في وظيفةٍ مُدارةٍ، لا أنّ الاختباراتَ صحيحةٌ ولا كافيةٌ.** **وأُوقِعت ثلاثةُ عيوبٍ حقيقيةٍ ويُسجَّل كلُّها بلا تلوينٍ**: خطوتانِ عميانِ في CI كانتا تبحثانِ نصّاً عربيّاً يطبعه المشروعُ بنفسِه (**26 ملفّاً من 55 لا تطبعه — 214 حالةً كان يمكن أن تمرَّ صامتةً**) · **إحدى عشرةَ حالةً لم تعمل في CI قطُّ** لانعدامِ `BENCH_DATABASE_URL` · **وعيبٌ ثالثٌ في قارئِ سجلِّ الحاجزِ نفسِه أسقط التشغيلَ `33252715645` بمخالفتَين مُختلَقتَين** لأنّ `bun` في بيئةٍ مُدارةٍ يطبع بادئةَ `::group::` **ويجمع كلَّ المتجاوَزاتِ في كتلةٍ واحدةٍ في آخرِ التشغيلِ** — **وأُصلِح السببُ لا العَرَضُ ولم يُخفَّف الحاجزُ بل أُضيفت قاعدةٌ أشدُّ** (تفرُّدُ عنوانِ الحزمةِ). **والدَينُ المُعلَنُ قائمٌ لا مُخضَّرٌ: إحدى عشرةَ حالةً في منصّةِ القياسِ لا تعمل في أيِّ مكانٍ اليومَ** ويطبعها الحاجزُ تحذيراً في كلِّ تشغيلٍ. **وبوابةُ `F1` لا تُفتح بهذا البند** ولا علاقةَ له بـ`REQ-04`، **و`F8-09` يبقى `[ ]` وبوّابتُه غيرُ بوّابةِ هذا البندِ** | 
| **تصحيحٌ مُلحَق ثالث عشر — 2026-08-30 (وحدة `OPS-010`)** | بندُ `OPS-010` نُفِّذ بدرجةِ **مُنفَّذ · مُختبَر · مُتحقَّق منه** وله ملفُ دليلٍ (`docs/evidence/correctness/OPS-010-20260830.md`) و`ADR 0047` و`docs/rollback.md` وسطرٌ في سجل القسم 25. **وقُلِب البندُ إلى `[x]` بعدَ اكتمالِ تشغيلِ CI `33279634961` بنتيجةِ `success` في الوظيفتَين على `d810df9f` لا قبلَه**، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. وسلسلةُ `bun run ci` صارت **20** سكربتَ فحصٍ (`check-*.ts`) إلى جانبِ حاجزِ بناءِ الصورةِ. **والمُتحقَّقُ منه أنّ الهجراتِ السبعَ والخمسينَ تُطبَّق واحدةً واحدةً على PostgreSQL حقيقيٍّ في وظيفةٍ مُدارةٍ وأنّ كلَّ فقدٍ يُطبَع، وأنّ الحاجزَ يُسقِط البناءَ على تضييقٍ لا يُعلِن مسارَ عودتِه — لا أنّ عودةً جرت على إنتاجٍ ولا أنّ القراءةَ الساكنةَ تُصيب كلَّ تضييقٍ.** **والحدودُ معلَنةٌ لا مُخفَّفةٌ:** أربعةٌ وعشرونَ تضييقاً مُعلَناً **منها عشرةٌ تكسر نسخةً سابقةً وتُطبَع تحذيراً دائماً في كلِّ تشغيلٍ ولا تُخضَّر** · **لا عودةَ نُفِّذت على إنتاجٍ قائمٍ** (لا إنتاجَ — `DEC-11`) · **`RTO`/`RPO` لم يُقاسا** فالبوّابةُ H تبقى `[ ]` · **الشيفرةُ السابقةُ لم تُشغَّل على المخطّطِ الجديدِ** وهو المقياسُ الأصدقُ ومؤجَّلٌ بـADR 0047 §٣ · **وكائنٌ لا تُنشئه هجرةٌ في المستودعِ غيرُ مرئيٍّ لخطِّ الزمنِ** (`record_webhook_event(text,text,text)` تُوقَف في `20260812120000` ولم تُنشَأ بأيّةِ هجرةٍ) **وهذا واقعٌ مُعلَنٌ لا مُعالَجٌ** · و`scripts/rollback-schema-drill.ts` **لا مُشغِّلَ له خارجَ CI** (لا PostgreSQL في بيئةِ التطويرِ ههنا) فلا يُقرَأ غيابُه نجاحاً. **وأُوقِع عيبٌ حقيقيٌّ ويُسجَّل بلا تلوينٍ**: التشغيلُ `33279160200` على `40649912` سقط في خطوةِ التمرينِ بثمانيةَ عشرَ إخفاقاً **فأُصلِح السببُ ولم تُستثنَ هجرةٌ ولم يُخفَّف حاجزٌ**. **وأُعيد القياسُ في هذه الوحدةِ على مُشغِّلٍ آخرَ و`bun 1.4.0` فجاء مطابقاً حرفاً** (2020 ناجحةً · 514 متجاوَزةً · 0 فاشلةً · 6363 تحقُّقاً في 186 ملفّاً · `lint`=0 · `typecheck`=0 · حاجزُ العودةِ خروج 0) — **فالأرقامُ ليست رهنَ جهازٍ ولا إصدارٍ**، ومُسجَّلٌ في §٨-ب من ملفِّ الدليلِ. **ونقصٌ حَوكميٌّ في سطرِ `OPS-009` صُحِّح بسطرٍ جديدٍ لا بتعديلِ سطرٍ سابقٍ**: كان بلا عمودَي «الدرجة» و«الـcommit». **وبوابةُ `F1` لا تُفتح بهذا البند** ولا علاقةَ له بـ`REQ-04`، **و`F11-09` و`F13-04` والبوّابةُ H تبقى `[ ]` ولم تُمَسّ** (`ح-1`) |
| **تصحيحٌ مُلحَق رابع عشر — 2026-08-30 (وحدة `OPS-005`)** | بندُ `OPS-005` صار **`[~]` لا `[x]`** وله ملفُ دليلٍ (`docs/evidence/correctness/OPS-005-20260830.md`) و`ADR 0048` وسطرٌ في سجل القسم 25، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. **المُنفَّذُ شطرُ التغطيةِ وحدَه**: `scripts/check-coverage-gate.ts` (الحاجزُ **الحادي والعشرونَ** في سلسلةِ `bun run ci`) يقرأ `lcov` ويحكم على **تسعةِ المساراتِ الحرجةِ** — القائمةُ المغلقةُ نفسُها من `scripts/lib/skip-registry.ts` — **بمقياسَين لا بواحدٍ**: أرضيّةُ نسبةِ الأسطرِ **وسقفُ عددِ ملفّاتِ المصدرِ التي لم يُحمِّلها اختبارٌ قطُّ**. **والمقياسُ الثاني ضروريٌّ لعيبٍ مقيسٍ لا مُتوقَّعٍ**: bun لا يُدرِج في `lcov` ملفّاً لا يستورده اختبارٌ، **فغيابُه يرفع النسبةَ لا يُنقِصها** — وشاهدُه أنّ «توثيقَ السائق» نسبتُه **100% على ملفَّين مقيسَين من 23**. المقيسُ على `bun 1.3.14` (إصدارُ CI): **381 ملفّاً · 18391/29584 سطراً · 62.165%**، **ومطابقٌ حرفاً على `bun 1.4.0`** فلا هامشَ إصدارٍ مضروبٌ؛ والأرضيّاتُ التسعُ **مقيسةٌ لا مستحسَنةٌ** (72 · 44 · 47 · 57 · 59 · 100 · 48 · 7 · 60). **وبُرهِن سقوطُ الحاجزِ بأربعِ حقناتٍ والحكمُ على رمزِ الخروجِ لا على النصِّ**، ومنها **انحدارٌ حقيقيٌّ**: إخراجُ ملفِّ اختبارِ الإسنادِ أهبطَ «دورةَ الرحلةِ والإسناد» إلى 43.81% دونَ أرضيّةِ 44% — **ثلاثةُ أسطرٍ من 1349 كفت**. **والسببُ في بقاءِ البندِ `[~]` نقصُ إمكانٍ لا نقصُ عملٍ**: **شطرُ «انحدارِ الأداء» لم يُبنَ ولم يُلفَّق له بديلٌ** — محجوبٌ بـ`OPS-004` (`[ ]`) و`REQ-06` (`[!]`)، و`bench/` لا يعمل في CI (`BENCH_DATABASE_URL` غيرُ مضبوطةٍ)، وعارضُ GitHub مشترَكُ الموردِ فزمنُه ضجيجٌ لا إشارةٌ؛ **وميزانيةُ الحِزَمِ (ADR 0044) تحرس حجمَ المُخرَجِ لا زمنَ الاستجابةِ فلا تُقرَأ بوابةَ أداءٍ**. **والحدودُ معلَنةٌ لا مُخفَّفةٌ:** **85 ملفَّ مصدرٍ في مساراتٍ حرجةٍ لا يلمسها اختبارٌ** — منها مطالبةُ السائقِ بالطلبِ والاستغاثةُ كاملةً — **تُطبَع تحذيراً في كلِّ تشغيلٍ والحاجزُ يمنع زيادتَها ولا يُصلِحها** · **سطرٌ منفَّذٌ ليس سطراً مُتحقَّقاً منه** · **لا قياسَ تفرُّعٍ** (`BRDA:` غيرُ مُصدَرٍ من bun) · **تغطيةُ اختباراتِ التكاملِ غيرُ مقيسةٍ** (لا PostgreSQL في `verify`) فتغطيةُ محوِّلاتِ البنيةِ التحتيّةِ **أدنى من حقيقتِها** · **السقفُ عددٌ خامٌ** يشمل ملفَّ الأنواعِ كما يشمل ملفَّ المنطقِ · **وأرضيّةُ «توثيقُ السائق» 100% هشّةٌ بالبناءِ** فأوّلُ ملفٍّ يدخل القياسَ ناقصَ التغطيةِ يُسقِط البناءَ — **وذلك مقصودٌ: يُصلَح السببُ ولا تُخفَّض الأرضيّةُ**. **وبوابةُ `F1` لا تُفتح بهذا البند، و`F9-06` تبقى `[ ]` ولم تُمَسّ** لأنّ بوّابتَها غيرُ بوّابتِه (`ح-1`) |
| **تصحيحٌ مُلحَق خامس عشر — 2026-08-30 (وحدة `OPS-006`)** | بندُ `OPS-006` صار **`[x]`** وله ملفُ دليلٍ (`docs/evidence/correctness/OPS-006-20260830.md`) و`ADR 0049` وسطرٌ في سجل القسم 25، **ونصُّ البندِ لم يُمَسّ (`ح-1`)**. **والمُنفَّذُ**: مسارُ اختبارٍ جديدٌ `tests/real-redis/` ووظيفةٌ ثالثةٌ في CI «تكامل على Redis حقيقي» بـ`REQUIRE_REAL_REDIS=1` — **فغيابُ السرِّ في وظيفةٍ وُجدت للسرِّ إخفاقٌ لا خُضرةٌ صامتةٌ**، وهو العيبُ المقصودُ بالإزالةِ — و`scripts/check-real-redis-proof.ts` (**الحاجزُ الثانيَ والعشرونَ**) بوحدةٍ نقيّةٍ تحكم على دليلٍ مبنيٍّ: عميلٌ حقيقيٌّ · قائمةٌ **مغلقةٌ** من عشرةِ فحوصٍ بلا نقصٍ ولا زيادةٍ ولا تكرارٍ · حدٌّ أدنى للأوامرِ (30) · صفرُ مفاتيحَ متروكةٍ · **ولا أثرَ سرٍّ في أيِّ حقلٍ** (رابطٌ · رمزٌ طويلٌ يخلط حرفاً ورقماً · `Bearer `). **والمُثبَتُ على الخادمِ الحقيقيِّ عشرُ حالاتٍ**: ذهابٌ وعودةٌ · حفظُ الجلسةِ وقراءتُها · مهلةٌ مضبوطةٌ في الخادمِ · **انتهاءُ مهلةٍ بزمنٍ فعليٍّ** · عزلُ فضاءَي البوتَين · قيمةٌ مشوَّهةٌ تُمحى في الخادمِ · `SCAN` لا يخرج عن البادئةِ · **حدُّ المعدَّلِ يعدُّ في الخادمِ لمُستهلِكَين منفصلَين** · نافذتُه تنتهي بمهلتِه · **حوارُ تسجيلٍ كاملٌ إلى PostgreSQL حقيقيّةٍ**. المقيسُ من تشغيلِ CI **33313714833** (ثلاثُ وظائفَ `success`): **62 أمراً · 10/10 فحوصٍ · صفرُ مفاتيحَ متروكةٍ**؛ ومحليّاً **2056 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6506 توكيداً في 189 ملفّاً**، ومدخلٌ **سادسٌ وخمسون** في سجلِّ التجاوزِ بشرطِ تفعيلٍ ومالكٍ ومسارٍ حرجٍ ومُشغِّلٍ مُتحقَّقٍ. **والسرُّ**: يُقرأ في موضعٍ واحدٍ (`tests/support/real-redis.ts`) ويُكتَم في كلِّ تفصيلِ فشلٍ قبلَ مخرجاتِ المُشغِّلِ، **ولم تُقرأ له قيمةٌ محليّاً قطُّ** ولا كُتِب في ملفٍّ ولا commit ولا دليلٍ ولا توكيدٍ. **وحدودٌ معلَنةٌ لا مُخفَّفةٌ:** **انقطاعُ Redis لا يُحاكى على خادمٍ حقيقيٍّ** فسلوكُ الانقطاعِ يبقى مُثبَتاً على مزدوجِ `tests/integration/redis-sessions.test.ts` · **وهُويّةُ النقطةِ بيانُ مديرِ المشروعِ لا نتيجةُ قياسٍ** (والحرزُ بادئةٌ فريدةٌ لكلِّ تشغيلٍ وتنظيفٌ مُتحقَّقٌ بصفرٍ، **ولا `FLUSHDB` قطُّ** لأنّ القاعدةَ قد تكون مشتركةً) · **والكتمُ يمنع الأنماطَ المعروفةَ لا كلَّ صياغةٍ ممكنةٍ** · **وطلبُ دمجٍ من نسخةٍ متشعّبةٍ لا يُمنَح أسرارَ المستودعِ فتُتخطّى الوظيفةُ فيه** ولا يُتخطّى شيءٌ في الدفعِ إلى `main`. **والأوامرُ مَعدودةٌ لا مُقاسةُ زمنٍ: لا قياسَ أداءٍ ولا سعةٍ ههنا، فلا `مَقيس` ولا `مُثبَت`** (`ح-5`). **وبوابةُ `F1` لا تُفتح بهذا البند، و`F9-02` تبقى `[ ]` لأنّ بوّابتَها غيرُ بوّابتِه (`ح-1`)، و`OPS-004` و`OPS-005` و`REQ-06` و`R-17` و`DEC-11` على حالِها** |
| **تصحيحٌ مُلحَق سادس عشر — 2026-08-31 (وحدةُ `R-17` — قرارٌ لا تنفيذٌ)** | **أُنشئ `ADR 0050` وحدَه: الناقلُ داخلَ العمليةِ آليةُ التوزيعِ الوحيدةُ المُقرَّرةُ، و`numInstances: 1` شرطُ صحّةٍ يُفرَض في الشيفرةِ وبحاجزٍ آليٍّ لا تعليقٌ يُقرَأ.** وثُبِّت فيه **عقدُ خمسةَ عشرَ دلالةً** ميزاناً لأيِّ محوِّلٍ يُختار مستقبلاً، **ولا محوِّلَ يُختار الآنَ**: لا Redis Pub/Sub ولا Redis Streams ولا PostgreSQL `LISTEN/NOTIFY` ولا محوِّلَ توزيعٍ لـSocket.IO — **والتوزيعُ على نسخٍ متعدّدةٍ مؤجَّلٌ لا مرفوضٌ**، يُفتَح بـADR ناسخٍ يومَ يُشترى تعدُّدُ النسخِ أو يُغلَق `REQ-07`. **و`R-17` يبقى `مفتوحاً` كما هو ولم يُمَسَّ نصُّه (`ح-1`)**: المُثبَّتُ **اتجاهٌ ودلالاتٌ وإيجابُ إنفاذٍ**، **والإلزامُ عندَ بدءِ التشغيلِ والحاجزُ الآليُّ لم يُكتَبا** — فالكسرُ يبقى صامتاً كما وصفه سطرُ `R-17` حتى يُنفَّذ الدَينُ الأربعةُ في `ADR 0050` §٦. **ولا سطرَ شيفرةٍ تغيّر ولا هجرةَ ولا اختبارَ ولا ملفَّ دليلٍ ولا سطرَ في سجلِّ القسم 25** — **وذلك مقصودٌ مُعلَنٌ**: لم يُقلَب بندٌ إلى `[x]` فلا موضعَ لـ`ح-4` ولا لسلّمِ الأدلةِ. **و`TrackingEvent` لم يُمَسّ ولا يحمل `version` ولا `sequence`، فكشفُ الفجوةِ غيرُ منفَّذٍ ولا يزعم القرارُ أنّه نُفِّذ** — وشكلُ الرقمِ المتزايدِ نقطةٌ مفتوحةٌ. **ولا realtime إنتاجيٌّ بُنِيَ**، ولا شيفرةَ Socket.IO في `main`. **ولا قياسَ سعةٍ ولا أداءٍ ههنا فلا `مَقيس` ولا `مُثبَت`** (`ح-5`): سعةُ النسخةِ الواحدةِ غيرُ مقيسةٍ (`OPS-004` محجوبٌ بـ`REQ-06`) والاختباراتُ الجهازيّةُ غيرُ مكتملةٍ (`R-24`). **ولم تُمَسَّ `ADR 0011` ولا `0016` ولا `0042` ولا ورقةُ `DEC-06`، ولم يُفتَح `DEC` جديدٌ ولا `DEC-11`، ولا بوّابةُ `F1`، ولا `OPS-004`/`OPS-005`، ولا الأجرةُ ولا التسعيرُ ولا الدفعُ.** **وفجوةُ حَوكمةٍ تبقى مُعلَنةً غيرَ محسومةٍ: لا بندَ مُرقَّماً في جدولِ `F1` يحمل عملَ الناقلِ** (`F1-01`…`F1-10` كلُّها هيكلٌ وهويّةٌ وسمةٌ وقياسٌ ونطاقٌ)، **وسطرُ `R-17` يُحيل إلى «المرحلة ٢٥» من إطارٍ استبدله `F0`…`F12`** — وحسمُها لمديرِ المشروعِ |
| **تصحيحٌ مُلحَق سابع عشر — 2026-08-31 (وحدةُ `R-17` — شطرُ الإنفاذِ)** | **شرطُ `numInstances: 1` صار مفروضاً في الشيفرةِ لا موصوفاً في تعليقٍ** — وهو **الشطرُ الأوّلُ وحدَه** من `R-17` (العيبُ الأوّل في `ADR 0050` §١). إنفاذانِ لا واحدٌ: **(أ) عندَ الإقلاعِ** — وحدةُ حكمٍ نقيّةٌ في `packages/shared/config/single-instance.ts` تقرأ **الإشارةَ القائمةَ `SESSION_STORE`** (لا متغيّرَ بيئةٍ جديداً ولا تحليلَ YAML وقتَ التشغيلِ)، فإن أُعلِنت طوبولوجيا متعدّدةُ العملياتِ (`redis`) مع ناقلٍ داخلَ العمليةِ **رُفِض الإقلاعُ برمزِ خروجٍ `1` ورمزِ خطأٍ `SINGLE_INSTANCE_INVARIANT`** قبلَ تركيبِ أيِّ محوِّلٍ — **لا تراجعَ صامتاً ولا تجاوزَ بمتغيّرِ بيئةٍ**؛ **(ب) في المستودعِ** — حاجزٌ مستقلٌّ (`scripts/check-instance-invariant.ts`) يقرأ `render.yaml` ويُلزِم `numInstances == 1` لكلِّ خدمةٍ **مصنَّفةٍ**، ويسقط كذلك على **خدمةٍ جديدةٍ غيرِ مصنَّفةٍ** أو **حقلٍ غائبٍ** أو **قيمةٍ غيرِ عدديّةٍ** أو **بنيةٍ لا تُقرَأ** — بثمانيةِ رموزِ خرقٍ في قائمةٍ مغلقةٍ. **وبُرهِن السقوطُ بخرقٍ مزروعٍ أوّلاً ثمّ النجاحُ بإزالتِه، والحكمُ برمزِ الخروجِ لا بالنصِّ**: خرقٌ ⇒ `1` · المستودعُ الحقيقيُّ ⇒ `0` · ملفٌّ غائبٌ ⇒ `1`. **ولم يُغيَّر عددُ النسخِ في `render.yaml`** — الزرعُ في نسخةٍ خارجَ المستودعِ وفي نصٍّ داخلَ الاختبارِ. **والرسالةُ لا تكشف سرّاً ولا تستطيع**: مُدخلُ الحكمِ حقلانِ لا يحملانِ سرّاً — قيدُ نوعٍ لا اتفاقٌ — وفُحِص ذلك بتمريرِ تسعةِ أسرارٍ صناعيّةٍ والبحثِ عن قيمةِ كلٍّ منها في المخرَجِ. وله ملفُ دليلٍ (`docs/evidence/architecture/R-17-ENFORCEMENT-20260831.md`) وسطرٌ في سجلِّ القسم 25. محليّاً `lint`=0 · `typecheck`=0 · `build:miniapp` بُنِي · **2079 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6569 توكيداً في 191 ملفّاً** (منها **23 فحصاً و61 توكيداً** لهذه الوحدةِ) · بوابةُ التغطيةِ 9 مساراتٍ كلُّها فوقَ أرضيّتِها. **والبندُ `R-17` يبقى «مفتوح» ولم يُقلَب `[x]`، وسطرُه (السطر 198) لم يُعدَّل** (`ح-1`): **شطرُ القدرةِ — توزيعُ الأحداثِ بين النسخِ — لم يُنفَّذ بتمامِه** (لا Socket.IO إنتاجيٌّ ولا محوِّلَ Redis ولا Pub/Sub ولا Streams ولا `LISTEN/NOTIFY`)، **و`version`/`sequence` في `TrackingEvent` لم يُحسَم تنفيذياً ولا كشفَ فجوةٍ**، **والتوسّعُ الأفقيُّ غيرُ محقَّقٍ ومقصودٌ كذلك اليومَ**. **ولا رقمَ سعةٍ ولا زمنَ استجابةٍ قِيسَ في هذه الوحدةِ ولا يُستخرَج منها** (`ح-5`). **ولم تُمَسَّ `ADR 0042` ولا `DEC-06` ولا `ADR 0050`، ولم يُفتَح `DEC` جديدٌ، ولا `R-16` ولا `OPS-004`/`OPS-005` ولا `F9` ولا الأجرةُ ولا التسعيرُ ولا الدفعُ.** **وثلاثُ نقاطٍ مفتوحةٍ مُعلَنةٌ لا مسكوتٌ عنها: (١) شرطٌ مسبقٌ تشغيليٌّ** — إن كانت البيئةُ الحيّةُ على `SESSION_STORE=redis` (كما يسجّل `docs/directive-item-0-live-diagnosis.md` السطر 81) **فسيرفض الإقلاعُ حتّى تُضبَط `memory`**، وهذا مقصودٌ لا عرَضٌ، **ولم تُفحَص البيئةُ الحيّةُ ههنا**؛ **(٢) تنافرٌ مع `SCL-002` (السطر 634) و`F5-03` (السطر 780)** وكلاهما `[ ]` يطلب العكسَ (إفشالُ الإقلاعِ عندَ `memory`) — فلا تعارضَ اليومَ، **وتنفيذُ أيِّهما يوجب حسمَ التنافرِ بقرارٍ** (متغيّرُ طوبولوجيا مستقلٌّ أو آليةُ توزيعٍ)، **ولم يُحسَم ههنا ولا ضمناً**؛ **(٣) `SESSION_STORE=redis` على نسخةٍ واحدةٍ ضبطٌ مشروعٌ منطقياً يرفضه هذا الإنفاذُ** لأنّ اتّساقَه غيرُ قابلٍ للإثباتِ من داخلِ العمليةِ — **اختيارٌ مُعلَنٌ لا سهوٌ** |
| **تصحيحٌ مُلحَق ثامن عشر — 2026-09-01 (وحدةُ `R-17` — تصحيحُ الإنفاذِ)** | **إنفاذُ الأمسِ كان يحكم حكماً مقلوباً، وصُحِّح: الطوبولوجيا تُعلَن ولا تُستنتَج من مخزنِ الجلساتِ.** الشيفرةُ المدمَجةُ في `d398ca8e` كانت تقرأ `SESSION_STORE=redis` دليلاً على تعدُّدِ العملياتِ، وهو **عكسُ لازمِ `ADR 0011`**: اللازمُ `multi-process ⇒ redis` وحدَه، ولا يصحّ عكسُه. و`SESSION_STORE` يجيب سؤالاً واحداً — **أين تُخزَّن الجلسةُ** — ولا يجيب **كم عمليةً تعمل**. فكان الحاجزُ **يرفض إقلاعَ `single-process + redis`** وهي حالةٌ صحيحةٌ ومفيدةٌ (بقاءُ الجلساتِ عبرَ إعادةِ النشرِ)، **بل هي أوّلُ خطوةٍ في مسارِ التوسّعِ الذي يأمر به `docs/render-deployment-vars.md` نفسُه** (الأسطر 86–88: اضبط `redis` → انشر → تحقّق → **ثمّ** ارفع النسخَ) — أي أنّ الحاجزَ كان يمنع الصوابَ لا الخرقَ، وشاهدُه وثيقتانِ متعارضتانِ في المستودعِ لا رأيٌ. **فأُنشئ `ADR 0051` ناسخاً لشطرِ الاستنتاجِ وحدَه، و`ADR 0050` لم يُعدَّل حرفاً** (قرارٌ منشورٌ لا يُحرَّر)، **وسبعةُ بنودٍ منه باقيةٌ بحرفِها**: `numInstances: 1` شرطُ صحّةٍ · الناقلُ داخلَ العمليةِ آليةُ التوزيعِ المُقرَّرةُ الوحيدةُ · تعدُّدُ العملياتِ ممنوعٌ · Redis ليس مصدرَ الحقيقةِ · Socket.IO ليس مصدرَ الحقيقةِ · `ADR 0011` و`ADR 0042` بلا مساس. **والتصميمُ محورانِ مستقلّانِ لا محورٌ واحدٌ يُحمَّل معنيَين:** `SESSION_STORE ∈ {memory, redis}` لموضعِ الجلسةِ وحدَه، و`PROCESS_TOPOLOGY ∈ {single-process, multi-process}` **إعلاناً صريحاً** يُقرأ بالطريقةِ القياسيةِ للضبطِ، افتراضُه `single-process`، **وقيمتُه الباطلةُ خطأُ ضبطٍ حتميٌّ (`InvalidEnvVarError`) لا تجاهلٌ ولا ردٌّ صامتٌ إلى الافتراضِ**. **ولم يُضَف `INSTANCE_COUNT` ولا متغيّرٌ تخمينيٌّ آخرُ**: Render لا يعطي العمليةَ عددَ النسخِ بقيمةٍ موثوقةٍ، فرقمٌ كهذا يكتبه إنسانٌ ويكذّبه الواقعُ صامتاً. والحكمُ صار على `topology + distribution`: **`single+memory` يُسمَح · `single+redis` يُسمَح — وهي الحالةُ الإنتاجيةُ الراهنةُ ولم يعد النظامُ يمنعها · `multi+redis` يُرفَض (`MULTI_PROCESS_WITHOUT_DISTRIBUTION`) · `multi+memory` يُرفَض بخرقَين**. ورمزُ الخطأِ `SINGLE_INSTANCE_INVARIANT` لم يتغيّر. **والحاجزُ القائمُ وُسِّع ولم يُنشَأ حارسٌ مكرَّرٌ**: أربعةُ رموزِ خرقٍ جديدةٍ (`MISSING_PROCESS_TOPOLOGY` · `INVALID_PROCESS_TOPOLOGY` · `TOPOLOGY_INSTANCES_MISMATCH` · `MULTI_PROCESS_WITHOUT_DISTRIBUTION`) تفرض التكافؤَ **في الاتّجاهَين**: `numInstances == 1 ⟺ single-process`. وقائمةُ القيمِ مكرَّرةٌ في الحاجزِ عن قصدٍ (أداةُ مستودعٍ لا تستورد شيفرةَ إنتاجٍ) **والتكرارُ نفسُه محروسٌ باختبارٍ يطابقه بقائمةِ الضبطِ**. **وبُرهِن السقوطُ برمزِ الخروجِ لا بالنصِّ**: خمسُ صورٍ من خرقِ ملفِّ النشرِ ⇒ `1` · المستودعُ الحقيقيُّ ⇒ `0` · وأربعُ حالاتِ إقلاعٍ فاشلةٍ برمزٍ غيرِ صفريٍّ، **ومعها فحوصُ قبولٍ تُثبِت أنّ العمليةَ بقيت حيّةً بعدَ موضعِ الحارسِ** فلا يمرُّ سقوطٌ أبكرُ مرورَ نجاحٍ. **ولم يُرفَع `numInstances` ولا زُرِع خرقٌ في `render.yaml`** — الزرعُ في نسخٍ خارجَ المستودعِ وفي نصوصٍ داخلَ الاختبارِ. **والرسالةُ لا تكشف سرّاً**: مُدخَلُ الحكمِ ثلاثةُ حقولٍ مقيَّدةٍ بالأنواعِ، وفُحِص المُخرَجُ بأسرارٍ صناعيّةٍ فصفرُ مطابقاتٍ. وله ملفُ دليلٍ (`docs/evidence/architecture/R-17-CORRECTION-20260901.md`) وسطرٌ في سجلِّ القسم 25. محليّاً `lint`=0 · `typecheck`=0 · `check-adr-numbering` **51 قراراً بأرقامٍ فريدةٍ** · `check-env-drift`=0 · `check-secret-logging`=0 · `check-session-storage-policy`=0 · `check-instance-invariant`=0 · **2095 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6615 توكيداً في 191 ملفّاً** (منها **39 فحصاً و107 توكيداً** لهذه الوحدةِ) · بوابةُ التغطيةِ خروج `0` والمساراتُ التسعةُ فوقَ أرضيّتِها. **والبندُ `R-17` يبقى «مفتوح» ولم يُقلَب `[x]` وسطرُه لم يُعدَّل** (`ح-1`): **المُغلَقُ عيبُ الإنفاذِ الخاطئِ لا البندُ**. **ولا توزيعَ بين النسخِ نُفِّذ ولا محوِّلَ اختير** (لا Redis Adapter ولا Pub/Sub ولا Streams ولا `LISTEN/NOTIFY`)، **ولا سعةَ ولا زمنَ استجابةٍ قِيسَ ولا يُستخرَج من هذه الوحدةِ** (`ح-5`)، **ولا realtime إنتاجيٌّ صار جاهزاً**، **ولم تُفتَح `F2` ولا `SCL-003` ولا `F5-03` ولا `DEC-11`**، **ولم تُمَسَّ الأجرةُ ولا التسعيرُ ولا الدفعُ**. **واقترانُ اختيارِ مُحدِّدِ المعدَّلِ بـ`SESSION_STORE` لم يُمَسّ بأمرٍ صريحٍ ويُسجَّل دَيناً مملوكاً لـ`SCL-003`/`F5`** — وأثرُه سليمٌ اليومَ لأنّ الطوبولوجيا `single-process`. **والتنافرُ الذي أعلنه تصحيحُ الأمسِ مع `SCL-002` و`F5-03` زال**: `redis` على نسخةٍ واحدةٍ صارت مسموحةً فلا يصطدم البندانِ بهذا الإنفاذِ. **ونقطةٌ مفتوحةٌ مُعلَنةٌ:** `PROCESS_TOPOLOGY` إعلانٌ يكتبه إنسانٌ ولا تؤكّده المنصّةُ للعمليةِ وقتَ التشغيلِ — والحاجزُ يربطه بـ`numInstances` **في المستودعِ** لا في وقتِ التشغيلِ، **وهذا حدُّ الدليلِ لا سهوٌ** **وتشغيلُ CI `33568001488` على `ea107e55e7` انتهى `success` في الوظائفِ الثلاثِ.** **وحدُّ الدليلِ مُعلَنٌ: وظيفةُ `verify` لا خطوةَ صريحةً فيها تنادي الحاجزَ** — يعمل في CI عبرَ خطوةِ `Test` التي تُشغِّله على `render.yaml` الحقيقيِّ وتُوجِب خروجاً صفريّاً، **وإضافةُ خطوةٍ صريحةٍ خارجُ نطاقِ هذه الوحدةِ ولم تُنفَّذ**. |
| **تصحيحٌ مُلحَق تاسع عشر — 2026-09-02 (وحدةُ `R-16` — الحذفُ)** | **البندُ `R-16` أُغلِق بالحذفِ لا بالتركيبِ وفق `ADR 0052`، وحالتُه في `docs/production-readiness/PHASE-6-REALTIME.md` §١٠ صارت «مغلق بالحذف» — ونصُّ البندِ لم يُمَسّ حرفاً (`ح-1`)، إنّما عمودُ الحالةِ وحدَه.** البندُ كان يصف مأزقاً بين طرفَين — التركيبُ يُنشئ مصدرَ حقيقةٍ ثانياً، والحذفُ يُسقِط «~٤٠» اختباراً — **و`ADR 0052` حسم أنّ الثانيَ ثمنٌ لا مانعٌ**. **والبرهانُ أنّ `TrackingService` كانت غيرَ موصولةٍ مقطوعٌ في موضعَين معاً**: صفرُ ذِكرٍ لها في `apps/gateway/src/container.ts`، ومستهلِكُها الوحيدُ `apps/gateway/src/routes/tracking.ts` **لم يكن مركَّباً في `apps/gateway/src/index.ts`** — لا `app.route` ولا استيرادَ. **وحجّةُ الحسمِ `ADR 0015` نفسُه**: علّق صحّةَ `previousPositions` على أن تُقرأ الإصلاحةُ السابقةُ من المصدرِ القانونيِّ، **والمسارُ الحيُّ استوفاه**: `handleLocation` (`packages/application/bots/driver-dialog.ts:2050`) يقرأ `driver.lastFix` من `drivers.last_location` ثمّ `assessGpsFix` ثمّ `drivers.updateLocation` — **الكاتبُ الوحيدُ** — ثمّ `packages/application/tracking/live-tracking.ts` الموصولُ في `container.ts:641`. **المحذوفُ خمسةُ ملفّاتٍ**: `packages/tracking/tracking-service.ts` · `packages/tracking/tracking-auth.ts` (**مخزنُ رموزٍ بلا تنفيذٍ ولا مُصدِرٍ ولا مستهلِكٍ**، وتتبّعُ العميلِ منفَّذٌ بنظامٍ آخرَ تماماً: `trip_tracking_tokens` والمرحلة ١١) · `apps/gateway/src/routes/tracking.ts` · `tests/unit/tracking-route.test.ts` · `tests/unit/tracking.test.ts`؛ **ورموزٌ أثبت المُترجِمُ ورسمُ الاستيرادِ موتَها بالحذفِ**: `resolveTrackingConfig` و`DEFAULT_TRACKING_CONFIG` و`GpsUpdate` و`ValidationResult` وإعاداتُ التصديرِ من `packages/tracking/index.ts`. **وانحرافٌ مُعلَنٌ عن حرفِ `ADR 0052` §٨** الذي قال «`types.ts` يبقى بلا مساسٍ»: المساسُ وقع في رمزَين ميتَين لا في `TrackingEvent`، وسندُه أمرُ الوحدةِ الصريحُ بألّا تبقى `dead exports` — **و`TrackingEvent` لم يُمَسّ حرفاً**. **و`TrackingEventPublisher` نُقِل ولم يُحذف**: كشف `typecheck` أنّ مستهلِكَيهِ `packages/application/tracking/live-tracking.ts` و`packages/infrastructure/tracking/event-bus.ts` أي المسارُ الحيُّ وناقلُه، فنُقِل إلى `packages/tracking/types.ts` بجوارِ `TrackingEvent` **بلا تغييرِ توقيعٍ** — **ولم يُنشَأ بديلٌ لـ`TrackingService` ولا صنفٌ ولا وحدةٌ**. **والباقي بسندٍ**: `packages/tracking/session-store.ts` (ليس من الخدمةِ في شيءٍ، وتنفيذُه على PostgreSQL في `packages/infrastructure/tracking/session-repository.ts`) · `resolveGpsPolicy` (`container.ts:142,677`) · `packages/domain/geo/gps-fix.ts` · `packages/domain/tracking/session.ts`. **والجردُ الحقيقيُّ 36 اختباراً في ثلاثةِ ملفّاتٍ لا «~٤٠» كما قدّر نصُّ البندِ**، **والمحذوفُ منها 26 اختباراً تختبر الشيفرةَ المحذوفةَ نفسَها** (10 `tracking: service` + 16 `tracking-route`) — **فسقوطُها حذفُ ادّعاءِ تغطيةٍ لا فقدُ تغطيةٍ**. **والقيميّانِ الوحيدانِ في الملفِّ المحذوفِ — اختبارا `haversine` — مكرَّرانِ حرفاً في `tests/unit/geo.test.ts:53-73`** (نقطتانِ متطابقتانِ ⇒ صفرٌ · جدّة←مكّة 65–73 كم · جدّة←الرياض · التماثلُ)، **فلم يُنقَل شيءٌ لأنّه موجودٌ أصلاً ولم تضِع قيمةٌ واحدةٌ**. و`tests/unit/tracking-config-resolution.test.ts` عُدِّل ولم يُحذف وبقيَ فيه كلُّ ما يختبر `resolveGpsPolicy` الحيَّ. محليّاً `lint`=0 · `typecheck`=0 · **2071 ناجحاً · 526 متجاوَزاً · 0 فاشلاً · 6564 توكيداً في 189 ملفّاً** — **والفرقُ ‎−29 يُطابِق الحسابَ بالضبطِ** (26 + 2 مكرَّرَين + 1 توكيدُ ضبطٍ) — **وأُعيدَت السلسلةُ مرّتَينِ فجاءت مطابقةً فلا لاحتميّةَ** · بوابةُ التغطيةِ خروج `0` والمساراتُ التسعةُ فوقَ أرضيّتِها ومسارُ «التتبّعُ وموقعُ السائق» **47.24% · 15/16 ملفّاً مقيساً** · وحواجزُ `bun run ci` العشرةُ الأولى خضراءُ ومنها `check-env-drift` و`check-adr-numbering` (**52 قراراً بأرقامٍ فريدةٍ**) و`check-secret-logging` و`check-session-storage-policy` و`check-instance-invariant` و`check-migrations`. **وحدُّ الدليلِ المحلّيِّ مُعلَنٌ ولا يُلوَّن**: `bash scripts/verify-container-build.sh` سقط محليّاً لأنّ `bun` في بيئةِ التنفيذِ **1.4.0** والصورةَ مثبَّتةٌ على **1.3.14** — **قيدُ بيئةٍ سابقٌ لهذه الوحدةِ لا أثرٌ لها**، ومرّ في CI حيثُ الإصدارُ مطابقٌ. **والحكمُ النهائيُّ من CI لا من المحلّيِّ**: التشغيلُ `33572493851` على `66a5ed0f29` انتهى **`success` في الوظائفِ الثلاثِ** (`verify` · «تكامل على PostgreSQL حقيقي» · «تكامل على Redis حقيقي»)، **وقُرئ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ لا من نصِّ السجلِّ**. وله ملفُ دليلٍ (`docs/evidence/architecture/R-16-TRACKING-SERVICE-DELETION-20260902.md`) وسطرٌ في سجلِّ القسم 25. **وما لم يُنفَّذ يُقرأ حرفيّاً**: **لا `version` ولا `sequence` ولا `eventId` — و`BUG-009` مفتوحٌ بحرفِه ولم يُقلَب `[x]`** · **لا replay ولا تاريخَ مواقعَ** (`R-9` على حالِه) · **لا Socket.IO إنتاجيٌّ ولا مُحوِّلَ ولا Pub/Sub** · **لا توزيعَ بين النسخِ — و`R-17` مفتوحٌ بحرفِه ولم يُقلَب `[x]`** · **لا realtime إنتاجيٌّ صار جاهزاً** (`R-19`) · **ولا سعةَ قِيسَت ولا زمنَ استجابةٍ ولا حِملَ — فلا `مَقيس` ولا `مُثبَت`** (`ح-5`). **ولم يُقلَّم نوعُ الحدثِ** رغمَ أنّ ثلاثةً من أحدَ عشرَ نوعاً وحدَها تُنشَر في الإنتاجِ وأنّ `teleport_detected` و`speeding_detected` فقدا ناشرَهما الوحيدَ — **تغييرُ عقدٍ لا حذفُ شيفرةٍ ميتةٍ، وخارجٌ عن نطاقِ الوحدةِ**؛ **والإشارةُ لم تضِع**: هي في `location_updated.metadata.quality` و`.findings`، **ولم يكن للنوعَين مستهلِكٌ أصلاً** (`event-bus.ts` لا يفرّق بالنوعِ، و`customer-live-relay.ts:129,134` يقرأ `session_ended` و`location_updated` وحدَهما). **ومتغيّراتُ البيئةِ الثلاثةُ** (`TRACKING_GPS_INTERVAL_SECONDS` · `TRACKING_GPS_IDLE_INTERVAL_SECONDS` · `TRACKING_MIN_DISTANCE_METERS`) **تبقى مقروءةً عندَ الإقلاعِ بلا مستهلِكٍ إنتاجيٍّ، وتُسجَّل دَيناً مُعلَناً ببندٍ مستقلٍّ** لأنّ حذفَها يمسُّ `render.yaml` و`docs/render-deployment-vars.md` ولم تأمر به هذه الوحدةُ — **ولا تبتلعُه**. **ولم تُمَسّ `ADR 0015` ولا `ADR 0042` ولا `ADR 0052`.** و`DEC-11` محجوبٌ على حالِه |
| **تصحيحٌ مُلحَق عشرون — 2026-09-02 (وحدةُ `BUG-009` — قرارٌ لا تنفيذٌ)** | **أُنشئ `ADR 0053` وحدَه — «ترتيبُ أحداثِ التتبُّعِ رقمٌ من الخادمِ لكلِّ جلسةٍ» — ولا سطرَ شيفرةٍ تغيَّر ولا هجرةَ ولا اختبارَ ولا ملفَّ دليلٍ ولا سطرَ في سجلِّ القسم 25 ولا قلبَ بندٍ.** القرارُ يُثبِّت عقدَ `BUG-009` ولا يُغلقه: التسلسلُ صادرٌ من الخادمِ، رتيبٌ متزايدٌ **داخلَ جلسةِ التتبُّعِ الواحدةِ** لا كلّيّاً، محلُّه صفُّ الجلسةِ في `tracking_sessions` فيبقى صحيحاً بعدَ إعادةِ التشغيلِ، **ويُثبَّت في نفسِ الكتابةِ الذرّيّةِ التي تُثبِّت تقدُّمَ الجلسةِ — ولا يُنشَر حدثٌ إذا رُفِضت تلك الكتابةُ**؛ وكلُّ حدثٍ يحمل `sessionId + sequence`، واللقطةُ المرجعيّةُ تحمل آخرَ رقمٍ يمثِّل حالةَ القناةِ، والمستهلكُ يحفظ `lastAppliedSeq` **في الذاكرةِ وحدَها**: `stale` يُسقَط · `duplicate` غيرُ ضارٍّ · `gap` و`reconnect` ⇐ لقطةٌ عبرَ `HTTP` — **ولا `replay` ولا `event store` ولا `Redis` ولا عدَّادٌ في الذاكرةِ**. **وحُسِمت نقطتانِ من الثلاثِ المعلَّقةِ:** `session_started` و`session_ended` يأخذانِ رقمَهما من **عدَّادِ الجلسةِ نفسِه** (سندُه أنّ `session_ended` حدثُ تحكُّمٍ ذو أثرٍ ترتيبيٍّ في `event-bus.ts:155` و`customer-live-relay.ts:129`، وأنّ مواضعَ الكتابةِ الخمسةَ في `session-repository.ts` كلَّها `insert/update … returning`)؛ و**إصلاحتانِ في الثانيةِ نفسِها تُقبَلانِ كلتاهما** بأرقامٍ مختلفةٍ — التساوي ليس قِدَماً، والمرفوضُ الأقدمُ قِدَماً صارماً، **والطابعُ الزمنيُّ يكفُّ عن كونِه مفتاحَ ترتيبٍ** لأنّ `telegram-mapper.ts:90` يضعه `message.date * 1000` أي بدقّةِ ثانيةٍ وبساعةِ جهازِ السائقِ. **والثالثةُ مؤجَّلةٌ صراحةً `deferred decision`:** قناةُ `F4-04` غيرُ منشأةٍ فلا نصَّ يحسم استقلالَ نطاقِ ترتيبِها، ويُقيَّد أيُّ تصميمٍ لاحقٍ بقيدَين — لا حَكَمَ ثانٍ لـ«أيُّهما أحدثُ»، وتبدُّلُ الجلسةِ داخلَ رحلةٍ يُعامَل معاملةَ الفجوةِ. **والمُغلَق نقطةُ `ADR 0050` §٧/٢ المفتوحةُ وحدَها** («شكلُ الرقمِ المتزايدِ … لكلِّ رحلةٍ أم لكلِّ جلسةٍ؟ من القاعدةِ أم من الناقلِ؟») — **ولم يُعدَّل من `ADR 0050` حرفٌ ولا من `ADR 0015` ولا `0042` ولا `0051` ولا `0052`**، وآليةُ التوزيعِ تبقى الناقلَ داخلَ العمليةِ. **والمفتوحُ باقٍ مفتوحاً:** `BUG-009` **بحرفِه** — ويُغلقه تنفيذُه بالحالاتِ التسعِ في §٨ وبدليلِه بعدَ خضرةِ CI لا قبلَها · `BUG-001` مفتوحٌ **ومقيَّدٌ بشرطِ توافقِ المُسنَدِ** (لا حَكَمانِ على «الأحدثِ») · `R-17` مفتوحٌ ولم يُنقَص من دَينِه شيءٌ ولا محوِّلَ يُختار (ستٌّ من الدلالاتِ الثمانِ في `ADR 0050` §٣-ح باقيةٌ) · `R-9` مفتوحٌ ولا سجلَّ مواقعَ يُنشَأ · `F4-01` و`F4-04` و`F4-08` تبقى `[ ]` · ولا Socket.IO إنتاجيٌّ ولا توزيعَ على نسخٍ متعدّدةٍ. **وحتى تُنفَّذ الحالاتُ التسعُ، الخريطةُ ما زالت قابلةً للتراجعِ كما يصفُ `BUG-009` بحرفِه** |
| **تصحيحٌ مُلحَق حادٍ وعشرون — 2026-09-02 (وحدةُ `BUG-009` — تنفيذٌ لا قرارٌ)** | **نُفِّذ `BUG-009` وفق `ADR 0053` بلا تعديلِ حرفٍ منه وبلا ADR جديدٍ، في خمسِ دفعاتٍ لا تُترك `main` بينها غيرَ قابلٍ للبناءِ.** **والعيبُ لم يكن غيابَ حكمٍ على القِدَمِ** — `recordFix` و`DEFAULT_GPS_POLICY` كانا يحكمانِ — **بل أنَّ الحكمَ لا يبلغُ المستهلِكَ**: حدثٌ بلا رقمٍ ولا جلسةٍ، ونشرٌ يسبق قرارَ الكاتبِ، ومستهلِكٌ يطبّق بترتيبِ الوصولِ؛ وسببُ التراجعِ **بنيويٌّ** لأنَّ `message.date * 1000` ساعةُ جهازٍ بدقّةِ الثانيةِ عبرَ طابورِ ويبهوكٍ. **المخطَّطُ:** هجرةٌ **واحدةٌ** `20260902000000_tracking_session_sequence.sql` — `last_sequence integer not null default 1` وقيدُ `>= 1`. **عمودٌ في صفٍّ لا `create sequence`** لأنَّ كائنَ السلسلةِ عالميٌّ وغيرُ معامليٍّ فيتقدَّم وإن انسحبَت المعاملةُ، **و`integer` لا `bigint`** لأنَّ `postgres.js` يُرجِع `int8` **نصّاً** فتصير `"10" < "9"` صحيحةً وتكسر كلَّ موازنةٍ صامتاً، **والبدءُ من ١** كما نصَّ القرارُ فـ`session_started` يحمل ١. وسطرُ الرجوعِ مكتوبٌ في رأسِ الهجرةِ **ومُجرَّبٌ**، والهجرةُ مقيسةٌ في الاتجاهاتِ الثلاثةِ: من الصفرِ (٥٨ هجرةً) · على قاعدةٍ قائمةٍ فيها صفُّ جلسةٍ سابقٌ (صارَ `last_sequence = 1` وبقيَ مفتوحاً) · ورجوعاً. و`rollback-schema-drill` أخضرُ. **وموضعُ التوليدِ واحدٌ لا غيرُه**: `session-repository.advance` جملةٌ واحدةٌ `set last_fix_at = $at, last_sequence = t.last_sequence + 1 … returning` — **بلا `Math.max` وبلا read-then-write وبلا `Date.now()` وبلا عدّادِ ذاكرةٍ**، والذرّيّةُ **خاصّةُ الجملةِ** لا دعوى تصميمٍ: القراءةُ والزيادةُ والكتابةُ والحكمُ على القِدَمِ (`last_fix_at < $at` صارماً فالمتساوي يُقبَل) في جملةٍ واحدةٍ يُسلسِلها قُفلُ الصفِّ. **و`recordedAtMs` بقيَ metadata زمنيّاً فقط.** **والدليلُ ١٩ حالةً**: ٨ على PostgreSQL حقيقيٍّ — **ومنها الخمسُ نبضاتٍ مختلطةِ الترتيبِ اختبارَ مسارٍ حقيقيٍّ لا اختبارَ دالّةٍ**، تدخل من `container.handler.handle` وهو نفسُ ما يُنادى من موجِّهِ الويبهوكِ، طوابعُها أ<ب<ج<د<هـ ووصولُها أ·ج·ب·هـ·د، فقِيسَ أنَّ `last_fix_at` = **هـ لا د** وأنَّ `location_updated` **ثلاثةٌ لا خمسةٌ** وأنَّ أرقامَها متزايدةٌ صارماً وآخرُها = `last_sequence` في الصفِّ، **ولا رقمَ مصطنعٌ في توقُّعٍ: كلُّ رقمٍ مقروءٌ من القاعدةِ أو محمولٌ في حدثٍ أصدرَته** — واستئنافُ الرقمِ بـ`createSql` **ومستودعٍ جديدَين** لا يُشاركانِ الأوّلَ ذاكرةً **فعدّادُ ذاكرةٍ يُخفق ههنا حتماً** — والأقدمُ يُرَدُّ بلا استهلاكِ رقمٍ · والمتساوي يُقبَل · وجلستا سائقَينِ ترقّمانِ استقلالاً · والدورةُ بعدّادٍ واحدٍ · واللقطةُ مرساةٌ ولا تابعَ إعادةِ بثٍّ · وساعةٌ منحرفةٌ إلى الأمامِ لا تُفسِد ترتيبَ القناةِ. **و١١ حالةً بلا قاعدةٍ، ومنها ثلاثةُ امتناعاتٍ بنيويّةٍ تُقرأ من نصِّ المصدرِ** لأنَّ عدّاداً في الذاكرةِ **ينجح** في كلِّ اختبارِ سلوكٍ داخلَ عمليةٍ واحدةٍ ولا يُخفق إلَّا في الإنتاجِ. **والحالاتُ التسعُ في `ADR 0053 §٨` مُستوفاةٌ كلُّها**، والحالتانِ ١ و٧ على قاعدةٍ حقيقيّةٍ كما شُرِطَ نصًّا. **والحواجزُ أوقعَت العملَ ثلاثَ مرّاتٍ ويُسجَّل بلا تبييضٍ**: بوّابةُ التغطيةِ سقطت في الوحدتَينِ ١ و٢ (44.91% دونَ 47%) لأنَّ الشيفرةَ سبقَت دليلَها **فأُصلِح السببُ بالدليلِ** فصارت **48.04% (515/1072)** ولم تُخفَّض الأرضيّةُ · وحاجزُ `OPS-009` ردَّ تجاوزاً مشروطاً غيرَ مُصنَّفٍ **فسُجِّل في `skip-registry` بسببٍ ومالكٍ ومُشغِّلٍ مُتحقَّقٍ** · ثمَّ ردَّ العددَ المُثبَّتَ في `skip-audit` **فرُفِع إلى ٥٧ ملفّاً و٥٣٤ حالةً رفعاً لا تخفيضاً** ونُسِبت الزيادةُ إلى قياسِ يومِها لا دُمِجت في رقمِ `OPS-009`. **وحدودُ الدليلِ مُعلَنةٌ لا مُخفّفةٌ: `BUG-001` مفتوحٌ بحرفِه ولم يُمَسّ** (`packages/infrastructure/identity/directories.ts:162-175` ما زال يكتب `drivers.last_location` بلا CAS) **ولم يُحَلَّ بالتخفّي داخلَ `BUG-009`** و`ADR 0053 §٦` يفصل الطبقتَين نصًّا فلا تعارضَ يوجب التوقُّفَ · **ومسارُ الراكبِ العامُّ لم يُمَسَّ عن قصدٍ** (`get_tracking_position` يقرأ `drivers.last_location` لا `tracking_sessions`، واستطلاعٌ كلَّ ٥ ثوانٍ بلا اشتراكٍ، **واقترانُ رقمٍ صاعدٍ بحقلٍ غيرِ محروسٍ يُوهِم ترتيباً لا يملكه**) · **وعيبٌ قائمٌ يُسجَّل ولا يُبيَّض**: `currentSession` يُغلِق بـ`EXPIRED` **بلا نشرِ `session_ended`** فيُستهلَك رقمٌ بلا حدثٍ يقابله — لا يكسر عدمَ التراجعِ ويظهر فجوةً تُصحَّح باللقطةِ، وليس من نطاقِ البندِ · **و`packages/application/tracking/tracking-session.ts` لا وجودَ له في المستودعِ** (آلةُ الحالةِ `packages/domain/tracking/session.ts`) ولم يُنشَأ ملفٌّ باسمٍ لا يقابله شيءٌ · **ولا `replay` ولا مخزنَ أحداثٍ ولا Redis ولا ترتيبَ عالميَّ ولا Adapter ولا `F4` ولا Socket.IO إنتاجيٌّ** · **و`R-17` و`R-9` مفتوحةٌ** والرقمُ لكلِّ جلسةٍ في القاعدةِ لا يمنع نسختَين ومنعُهما ليس ههنا، و`numInstances: 1` شرطُ صحّةٍ على حالِه · **ولا سعةَ قِيسَت ولا حِملَ ولا تزامنَ إنتاجيٍّ فلا `مَقيس` ولا `مُثبَت` (`ح-5`)** · **ونصُّ البندِ لم يُغيَّر حرفاً (`ح-1`)** ولا رُقِّم شيءٌ ولا حُذِف (`ح-2`) **ولا عُدِّل `ADR 0053`** (`ح-6`) **ولا أُنشئ ADR جديدٌ لأنَّ التنفيذَ مطابقٌ له**. محليّاً: `lint`=0 · `typecheck`=0 · وحدةً **1818 ناجحاً · 0 فاشلاً** · تكاملاً على PostgreSQL حقيقيٍّ **454 ناجحاً · 0 فاشلاً · 11 متجاوَزاً** (دَينُ `BENCH_DATABASE_URL` القائمُ) · وبوّابةُ التغطيةِ خروج `0`. **وحدُّ البيئةِ المحلّيّةِ مُعلَنٌ**: `verify-container-build.sh` يسقط محليّاً لتباينِ إصدارِ `bun` (1.4.0 مقابل 1.3.14 في الصورةِ) — **قيدُ بيئةٍ سابقٌ** ويمرّ في CI. **والبندُ `[x]` بعدَ خضرةِ الوظائفِ الثلاثِ لا قبلَها** |
| **تصحيحٌ مُلحَق ثانٍ وعشرون — 2026-09-02 (وحدةُ `BUG-001` — الكتابةُ الشرطيّةُ)** | **نُفِّذ `BUG-001` وحدَه وقُلِب `[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها، في أربعِ دفعاتٍ لا تُترك `main` بينها غيرَ قابلٍ للبناءِ.** **والجردُ قبلَ التعديلِ أثبتَ كاتباً إنتاجيّاً واحداً لا اثنَين** — `DriverDirectory.updateLocation` في `packages/infrastructure/identity/directories.ts` — **وسائرُ إصاباتِ `set last_location` تجهيزاتُ اختبارٍ** (`live-sequence-validation:211` · `admin-dashboard:895` · `canonical-driver-location:231,242` · `driver-location-freshness:248,375`)، **ونداءُها واحدٌ** من `packages/application/bots/driver-dialog.ts:2079`، **وقُرّاؤها ثمانيةٌ** (`toLastFix` · `dispatch-adapters:135` · `order-adapters:220` · `tracking-queries:158` · `admin/queries` أربعةُ مواضعَ · دالّةُ `get_tracking_position` · `bench/ride-dispatch:177`) — **فلم يوجد تناقضٌ يوجب التوقُّفَ، وأُعيدَ الجردُ بعدَ التعديلِ فبقيَ واحداً**. **والعيبُ:** الكتابةُ كانت `update … set last_location = … where id = $1` بلا شرطٍ زمنيٍّ، **فأقدمُ نبضةٍ تصل متأخّرةً تكتب فوقَ الأحدثِ** — وسببُه بنيويٌّ لا احتماليٌّ لأنَّ الطابعَ `message.date * 1000` ساعةُ جهازٍ بدقّةِ الثانيةِ عبرَ طابورِ ويبهوكٍ. **والمُنفَّذُ في الموضعِ نفسِه لا في طبقةٍ أخرى:** جملةٌ واحدةٌ فيها `with current as (select id from drivers where id = $1)` و`written as (update … and (d.last_location_recorded_at is null or d.last_location_recorded_at <= $ts) returning d.id)` ثمَّ `select true from written union all select false from current where not exists (select 1 from written)` — **والذرّيّةُ خاصّةُ الجملةِ لا دعوى تصميمٍ**: الحرزُ مكتوبٌ في `where` **تُعيد القاعدةُ تقييمَه على الصفِّ بعدَ فكِّ القفلِ في `READ COMMITTED`**، وقُفلُ الصفِّ يُسلسِل الكاتبَين، و`current` يفرّق «رُفِضَت» من «لا سائقَ بهذا المعرِّفِ». **ولا `Math.max` ولا `greatest` ولا `Date.now()` ولا `read-then-write` ولا `Map` ولا عدّادَ ذاكرةٍ ولا ساعةَ جهازٍ حَكَماً** — والطابعُ **معاملٌ مُمَعلَمٌ** مبنيٌّ من `quality.recordedAtMs` الذي صارَ **إلزاميّاً** في العقدِ. **و`<=` لا `<`** فالمتساويانِ لا يُرفَضان لمجرَّدِ التساوي (`ADR 0053 §4-ج`)، **والحكمُ على `last_location_recorded_at`** — وقتُ القياسِ — لا `last_location_at` وهو وقتُ الكتابةِ. **ولا حَكَمَ ثانياً على «الأحدثِ»**: الطابعُ هو الذي يحكم به `BUG-009` نفسُه، **و«عمودُ التسلسلِ التصاعديِّ» في نصِّ الحلِّ لم يُنشَأ عن قصدٍ** لأنَّه حَكَمٌ ثانٍ يمنعُه `ADR 0053 §٦` — **مُعلَنٌ لا مُخفىً**. **والجوابُ صارَ مُسمّىً** `accepted \| stale \| no_driver` يُقرأ من القاعدةِ لا من ترتيبِ الوعودِ، **والمرفوضُ لا يُبَثّ ولا يُعيد الإسنادَ** (يتخطّى `tracking.onFix`) **ولا يُرجِع `drivers.last_location` إلى الوراءِ**، **وعطلُ الاتّصالِ يبقى عطلاً لا `stale`**. **ولا هجرةَ جديدةً** فالعمودُ قائمٌ في `20260812020000_phase_5_fix_recorded_at.sql`. **والدليلُ ٢٤ حالةً**: ٨ على **PostgreSQL حقيقيٍّ** بسباقٍ حقيقيٍّ بكاتبَين متزامنَين على اتّصالَين منفصلَين، **والبرهانُ الفاصلُ على غيابِ read-then-write** معاملةٌ ثالثةٌ تحبس الصفَّ فيُقاسُ أنَّ الكتابةَ الأقدمَ **معلَّقةٌ** بعدَ `Bun.sleep(300)` ثمَّ تُرجِع `stale` بعدَ الإفراجِ؛ و٨ وحدةً تحرسُ **شكلَ الجملةِ** (واحدةٌ لا اثنتانِ) وموضعَ الحرزِ وتمعيلَ الطابعِ وترجمةَ الجوابِ؛ و٨٧ حالةَ حوارٍ لم تُخفِق واحدةٌ. **والحالاتُ التسعُ التي شرطَها التوجيهُ مُستوفاةٌ كلُّها.** **وثلاثةُ حواجزَ سقطَت ويُسجَّل بلا تبييضٍ**: تصنيفُ التجاوزِ في `skip-registry` · وحرزُ عددِه في `skip-audit` **رُفِع إلى ٥٨ ملفّاً و٥٤٤ حالةً رفعاً لا تخفيضاً** · وبوّابةُ التغطيةِ سقطَت في التشغيلِ ٢٢٦ على مسارِ «الهويةِ والجلسةِ والصلاحياتِ» (72.24% فوقَ أرضيّةِ 72% بفرقٍ لا يحتمل سطرين، و`directories.ts` مُغطّى **6/214** في `verify` لأنَّ تغطيتَه كلَّها من تكاملٍ يتجاوز هناك — **حدٌّ مُعلَنٌ في `coverage-registry.ts` نفسِه** — والمقياسُ يعُدُّ أسطرَ التوثيقِ أسطراً غيرَ مغطّاةٍ) **فأُصلِح سببُها بتغطيةٍ حقيقيّةٍ ببديلِ `sql` مُسجِّلٍ على نمطٍ قائمٍ في المستودعِ، فصارَ المسارُ 76.66% أعلى ممّا كان قبلَ العملِ — لا أرضيّةٌ خُفِّضَت ولا توثيقٌ حُذِف ولا استثناءٌ أُضيفَ**. محليّاً: `lint`=0 · `typecheck`=0 · بقاعدةٍ **٢٥٢٤ ناجحاً · ٠ فاشلاً · ٢٣ متجاوَزاً** · بلا قاعدةٍ **٢٠٩١ ناجحاً · ٠ فاشلاً** · وبوّابةُ التغطيةِ خروجُ `0` في الحالَين. **وحدُّ البيئةِ المحلّيّةِ مُعلَنٌ**: `verify-container-build.sh` يسقط لتباينِ إصدارِ `bun` (1.4.0 مقابل 1.3.14) — **قيدُ بيئةٍ سابقٌ** ويمرّ في CI. **والحكمُ من CI**: التشغيلُ **٢٢٨** على `1570b4311b` انتهى **`success` في الوظائفِ الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ**. **وما لم يُمَسّ يُقرأ حرفيّاً: `TrackingEvent` ولا `sessionId`/`sequence` ولا SSE ولا Socket.IO ولا محوِّلَ Redis ولا `R-17` ولا `R-9` ولا `F4` ولا `BUG-002…008` ولا `F2` ولا الأجرةُ ولا التسعيرُ ولا الدفعُ ولا `DEC-11` ولا `ADR 0015` ولا `ADR 0053`** — **و`BUG-009` لم يُعَد فتحُه ولا وُسِّع، ولم يُقلَب بندٌ آخرُ (`ح-1` نصُّ البندِ لم يُغيَّر حرفاً · `ح-6` لا ADR منشورٌ عُدِّل · ولا ADR جديدٌ لأنَّ القرارَ قائمٌ في `ADR 0053` ولم يظهر جديدٌ).** **ولا سعةَ قِيسَت ولا أداءَ ولا تحقُّقَ جهازٍ ولا صحّةَ تعدُّدِ نسخٍ ولا جهوزيّةَ realtime — فلا `مَقيس` ولا `مُثبَت` (`ح-5`): المُثبَتُ أنَّ القاعدةَ ترفض الأقدمَ عندَ سباقٍ حقيقيٍّ وأنَّ المرفوضَ لا يُرجِع المخزَّنَ، لا أنَّ طبقةَ التتبُّعِ صارت جاهزةً للإنتاجِ الفوريِّ** |
| **تصحيحٌ مُلحَق ثالثٌ وعشرون — 2026-09-03 (وحدةُ `BUG-010` — تعيينُ نطاقٍ ثمَّ تنفيذٌ)** | **عيبٌ كان بلا بندٍ فسُجِّل بنداً قبلَ أن يُمَسَّ سطرٌ:** جردُ مواضعِ الإغلاقِ في `packages/application/tracking/live-tracking.ts` أثبتَ ثلاثةَ مواضعَ لا أكثرَ (`:154` داخلَ `closeAndAnnounce` · `:360` في `closeByTrip` وينشر لكلِّ جلسةٍ أُغلقت · `:189`)، **والثالثُ وحدَه كان يُنادي `deps.sessions.close(driverId, "EXPIRED", nowMs)` مباشرةً ويُهمل عائدَها** — فيُغلق الصفُّ في القاعدةِ ويُستهلَك رقمُ التسلسلِ (`session-repository.ts:209`: «الإغلاقُ يستهلك رقماً لأنَّه ينشر `session_ended`») **بلا حدثٍ يقابله**: رسالةُ الموقعِ الحيِّ عندَ العميلِ لا تُغلق، وتمثيلُ السائقِ على خريطةِ العمليات/الإدارةِ قد يبقى مفتوحاً إلى أن تُصحّحه لقطةٌ. **ولم يُربَط بـ`F5-01`**: نصُّه «إغلاق `BUG-001` … `BUG-009`» مدىً مُغلقٌ حرفاً، وإدخالُ عاشرٍ فيه تغييرٌ لمعنى بندٍ يمنعُه `ح-1` — **فسُجِّل `BUG-010` مستقلّاً** (ورقمُه حرٌّ بالفحصِ: لا `BUG-010` في `docs` ولا `apps` ولا `packages` ولا `tests` قبلَ هذا). **والمُنفَّذُ سطرٌ واحدٌ:** `await closeAndAnnounce(driverId, existing.facts.tripId, "EXPIRED", nowMs);` بدلَ النداءِ المباشرِ — **ولا رقمَ جديداً ولا حدثاً جديداً ولا مساراً ولا هجرةً ولا تعديلَ `TrackingEvent` ولا تعديلَ `ADR 0053` ولا إعادةَ كتابةِ `closeAndAnnounce`** (ونصُّ تعليقَيها حُدِّث ليقولَ «ثلاثةُ مواضعَ» بعدَ أن صار كذلك، بلا تغييرِ منطقٍ). **فهذا إنفاذُ عقدٍ قائمٍ لا تغييرُ عقدٍ:** `ADR 0053` §٤-أ كان يُلزم بالنشرِ لكلِّ إغلاقٍ، والشيفرةُ كانت تخالفُه في موضعٍ واحدٍ. **وأثرٌ ظاهرٌ يُعلَن لا يُخفى:** رحلةٌ تعبُر حدَّ الاثنَي عشرةَ ساعةً يُغلق بثُّها ثمَّ يُفتَح بثٌّ جديدٌ لجلسةِ الخَلَفِ — وهو ما تُوجبه دلالةُ «الجلسةُ قناةٌ» في `ADR 0053` §٤-ب/٢ وما يفعله المُرحِّلُ أصلاً عندَ كلِّ نهايةٍ، لا سلوكٌ استُحدثَ هنا. **والدليلُ أربعُ حالاتٍ أُضيفَت لا أكثرَ**: ثلاثٌ في `tests/unit/tracking-realtime.test.ts` (النشرُ بسببِ `EXPIRED` ومعرّفُ الجلسةِ المنتهيةِ · **أنَّ `sequence` في الحدثِ هو عائدُ `close` نفسُه** مُلتقَطاً من المخزنِ لا رقماً مكتوباً في التوقُّعِ · أنَّ النهايةَ تسبق بدايةَ الخَلَفِ · أنَّ إغلاقاً عائدُه `null` لا يُنتج حدثاً كاذباً · أنَّ بثَّ العميلِ يتوقّف لأنَّ بوّابةَ `BUG-009` في المُرحِّلِ لا تُغلق إلَّا إن كان `sessionId` في الحدثِ هو معرّفُ جلسةِ البثِّ) وواحدةٌ في `tests/integration/tracking-sequence.test.ts` **على PostgreSQL حقيقيٍّ**: البدايةُ تُبعَد خلفَ السقفِ في القاعدةِ نفسِها لا بساعةٍ مزوّرةٍ، فيُقاسُ `end_reason = EXPIRED` و`ended_at` على الصفِّ، **وأنَّ الرقمَ المنشورَ هو `last_sequence` المستقرُّ في `tracking_sessions`**. **واثنتانِ من الأربعِ أُخفِقتا قبلَ الإصلاحِ وأُثبِتَ ذلك بإرجاعِ السطرِ ثمَّ إعادةِ التشغيلِ** (٢ فاشلاً في الوحدةِ · ١ فاشلاً في التكاملِ)، **والاختبارُ القائمُ في `tests/e2e/tracking-e2e.test.ts` لم يُعدَّل حرفاً وبقيَ ناجحاً**، وكذلك الاختبارُ القائمُ «جلسة تجاوزت سقفها تُغلق EXPIRED وتُستبدل بلا ماسحٍ دوري» في اختباراتِ الوحدةِ. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **الحُرّاسُ الواحدُ والعشرون كلُّهم خروجُ `0`** · بلا قاعدةٍ **٢٠٩٤ ناجحاً · ٠ فاشلاً** · وبوّابةُ التغطيةِ خروجُ `0` (مسارُ «التتبّعُ وموقعُ السائق» 48.04%) · وبقاعدةٍ حقيقيّةٍ `tests/integration` و`tests/e2e` **٤٧١ ناجحاً · ٠ فاشلاً**. **ولا أرضيّةَ تغطيةٍ خُفِّضَت ولا `skip` أُضيفَ ولا استثناءَ في حارسٍ.** **وحدُّ البيئةِ المحلّيّةِ مُعلَنٌ**: Redis غيرُ متاحٍ محليّاً و`verify-container-build.sh` يسقط لتباينِ إصدارِ `bun` — والحكمُ فيهما من CI. **وما لم يُمَسّ يُقرأ حرفيّاً: `BUG-001` ولا `BUG-009` ولا دلالةُ `sequence` ولا دلالةُ اللقطةِ ولا `R-17` ولا Socket.IO ولا Redis ولا SSE ولا `F4-01` ولا `ADR 0042/0052/0053` ولا بندٌ آخرُ قُلِب.** **ولا سعةَ قِيسَت ولا أداءَ ولا تحقُّقَ جهازٍ ولا صحّةَ تعدُّدِ نسخٍ ولا جهوزيّةَ realtime — فلا `مَقيس` ولا `مُثبَت` (`ح-5`).** **والحكمُ من CI**: التشغيلُ **٢٣٠** على `d10472e06e` انتهى **`success` في الوظائفِ الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ**. **وبعدَ الخضرةِ لا قبلَها (`ح-4`) قُلِب `BUG-010` إلى `[x]` وله ملفُ دليلٍ مستقلٌّ** (`docs/evidence/correctness/BUG-010-20260903.md`) **ولم يُقلَب بندٌ آخرُ** |
| **تصحيحٌ مُلحَق رابعٌ وعشرون — 2026-09-03 (وحدةُ `BUG-005` — ذرّيّةُ فتحِ الدورةِ)** | **نُفِّذ `BUG-005` وحدَه وقُلِب `[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها (`ح-4`)، في وحدتَينِ لا تُترك `main` بينهما غيرَ قابلةٍ للبناءِ.** **والعيبُ TOCTOU بحرفِه**: `matchOrder` (`packages/application/dispatch/match-order.ts:150`) تقرأ `status` وتقارنُه **في JavaScript** ثمَّ تحسبُ `round = broadcastRound + 1`، و`openRound` (`dispatch-adapters.ts:197-217`) بعدَها تفتحُ معاملةً وتُدخِلُ `N` صفَّ عرضٍ في حلقةٍ وترفعُ `orders.broadcast_round` **بجملةٍ بلا شرطٍ ولا قفلِ صفٍّ**. فبينَ القراءةِ والكتابةِ نافذةٌ كاملةٌ: استدعاءانِ متزامنانِ يقرآنِ `searching` و`broadcast_round = N` معاً فيفتحُ كلاهما `N+1` — **«جولةٌ مزدوجةٌ» بحرفِ أثرِ البندِ** — وطلبٌ أُسنِد أو أُلغيَ بينَ اللحظتَينِ تُبَثُّ عروضُه إلى سائقينَ **لطلبٍ لم يعُد يبحثُ**. **والجردُ قبلَ التعديلِ وأُعيدَ بعدَه أثبتَ كاتباً واحداً لا ثانيَ له لـ`orders.broadcast_round` هو `openRound` نفسُها** — لا في `packages` ولا `apps` ولا دوالِّ الهجراتِ — **فلا كاتبَ خفيٌّ أُخفيَ داخلَ الإصلاحِ**؛ و`orders.status` تكتبُه **دوالُّ SQL وحدَها** (`claim_ride` ⇐ `matched` · بدءُ الرحلةِ · التقييماتُ · `cancel_order_fanout` ⇐ `cancelled` · مفاوضةُ غيرِ المشتركِ · `claim_ride_returns_rider`) **ولم تُمَسَّ واحدةٌ منها**. **والمُنفَّذُ دالّةٌ واحدةٌ** `open_offer_round(uuid, integer, timestamptz, jsonb) returns jsonb` في هجرةٍ **واحدةٍ** `20260903090000_open_offer_round_is_atomic.sql`، `language plpgsql security definer set search_path = public`: حارسُها **تحديثٌ شرطيٌّ واحدٌ** `update orders set broadcast_round = p_round, updated_at = now() where id = p_order_id and status = 'searching' and broadcast_round = p_round - 1 returning * into v_order`، ثمَّ **في المعاملةِ نفسِها** إدخالُ العروضِ بجملةٍ واحدةٍ من `jsonb_array_elements(p_entries)` ثمَّ `get diagnostics`. **والذرّيّةُ خاصّةُ الجملةِ لا دعوى تصميمٍ، واختيرَ الأدنى الذي يطابقُ العقدَ لا الأشهرُ**: ١) الجملةُ الشرطيّةُ **تأخذُ قفلَ الصفِّ الحصريَّ نفسَه عندَ نجاحِها** فالفائزُ يُمسِكُ الصفَّ إلى نهايةِ المعاملةِ تماماً كما لو قَفَلَه صراحةً · ٢) **وتحتَ `read committed` تنتظرُ الخاسرةُ ثمَّ تُعيدُ تقييمَ شرطِها على الصورةِ الجديدةِ للصفِّ (`EvalPlanQual`)** فلا يعودُ `broadcast_round = N` صادقاً فتُصيبُ **صفراً من الصفوفِ** وتخسرُ **حسماً** — **بلا `retry loop` ولا `sleep` ولا `Map` ولا قفلِ ذاكرةٍ ولا اعتمادٍ على ترتيبِ الوصولِ** · ٣) **وفي مسارِ الرفضِ لا تأخذُ قفلاً أصلاً**، بخلافِ `select … for update` الذي يقفلُ قبلَ أن يُعلَمَ هل سيُكتَبُ شيءٌ — **وهذا فرقٌ عمليٌّ لا تجميليٌّ لأنَّ `claim_ride` القائمةَ تستعمل `for update skip locked`، فصفٌّ مقفولٌ عندَها ليس صفّاً تنتظرُه بل سائقٌ يُبلَّغُ `ORDER_NOT_CLAIMABLE`؛ فكلُّ قفلٍ زائدٍ ههنا يتحوَّلُ عندَ سائقٍ متزامنٍ إلى رفضِ قبولٍ كاذبٍ**. **فالأقلُّ قفلاً هو الأصحُّ سلوكاً لا الأخفُّ حِملاً فحسبُ.** **وانحرافٌ عن حرفِ عمودِ «الحلّ» في البندِ يُعلَنُ ولا يُخفى**: النصُّ اقترحَ `FOR UPDATE`، والمُنفَّذُ يستوفي كلَّ ما اشترطَه العمودُ (**RPC ذرّيّةٌ واحدةٌ · تحقُّقُ الحالةِ · إنشاءُ الجولةِ والعروضِ · كلُّها في معاملةٍ**) بآليةِ قفلٍ أدنى لا تُفسِدُ `claim_ride` — **ونصُّ البندِ لم يُعدَّل ليطابقَ التنفيذَ (`ح-1`)، بل سُجِّل الانحرافُ في ملفِّ الدليلِ**. **ولا ADR جديدٌ**: هذا **تنفيذُ عقدٍ منصوصٍ في البندِ نفسِه** لا قرارٌ معماريٌّ جديدٌ، **ونمطُه مسبوقٌ في المستودعِ** بـ`BUG-001` (`directories.updateLocation`) وبـ`claim_ride`؛ **و`ADR 0053` لم يُمَسّ** لأنَّه لا تناقضَ ظهرَ (`ح-6`). **وحراسةُ الرقمِ لازمةٌ ولا تُغني عنها حراسةُ الحالةِ**: `openRound` **لا تُغيِّرُ `status`**، فالطلبُ يبقى `searching` بينَ دورتَينِ **وحارسُ الحالةِ وحدَه يسمحُ لسباقَينِ بفتحِ الرقمِ نفسِه مرّتَينِ** — **فالمقارنةُ-والتبديلُ على `broadcast_round` هي التي تُغلِقُ «الجولةَ المزدوجةَ»، وحارسُ الحالةِ هو الذي يُغلِقُ «عروضٌ لطلبٍ لم يعُد يبحثُ»، وكلاهما لازمٌ**. **ولم يُضَف قيدٌ فريدٌ ولا عُدِّل قائمٌ ولا مُسَّ نموذجُ الجولةِ ولا معنى `round`**، و`on conflict (order_id, driver_id, round) do nothing` **احتياطٌ ضدَّ تكرارِ سائقٍ داخلَ الدفعةِ لا حارسٌ للسباقِ**. **و`city_id` يُقرأُ من `v_order` العائدِ من التحديثِ لا من معاملٍ يُصدَّقُ**، فلا يبثُّ المُنادي طلباً في مدينةٍ ليست مدينتَه. **والرفضُ مُسمّىً بلا تسريبٍ**: `ORDER_NOT_FOUND` · `ORDER_NOT_SEARCHING` (ومعه `status` وحدَه) · `ROUND_ALREADY_OPENED` — **ولا رسالةَ قاعدةٍ ولا تفصيلَ داخليَّ في خطأٍ عامٍّ**؛ والأخيرُ **صُنِّف تزاحُماً لا فشلاً** في `redispatch-searching-orders.ts` وإلَّا سُجِّل خطأً كاذباً، **وعدَّادُ العروضِ في `observability/dispatch.ts` لا يعُدُّ إلَّا عندَ الفتحِ**. **والدليلُ سبعُ حالاتٍ في `tests/integration/dispatch-open-round-atomicity.test.ts` على PostgreSQL حقيقيٍّ — سبعٌ ناجحةٌ صفرُ فاشلةٍ**: طلبٌ `searching` ⇐ 3 عروضٍ والرقمُ 1 · **استدعاءانِ متزامنانِ على اتّصالَينِ منفصلَينِ (`createSql` مرّتَينِ) ⇐ فائزٌ واحدٌ لا اثنانِ، والخاسرةُ `ROUND_ALREADY_OPENED` حسماً، والرقمُ 1، و3 عروضٍ لا 6، ودورةٌ متمايزةٌ واحدةٌ** · **وحاجزٌ بقفلِ معاملةٍ مفتوحةٍ لا بنومٍ** — **والنومُ 300ms يُقاسُ به أنَّها محبوسةٌ لا يُصنَعُ به السباقُ** — ⇐ رُفِضت بعدَ التثبيتِ و**0 عروضٍ** · وطلبٌ `matched` ⇐ `ORDER_NOT_SEARCHING` و`status = matched` والرقمُ **بقيَ 0** و**0 عروضٍ** · ومعرِّفٌ لا وجودَ له ⇐ `ORDER_NOT_FOUND` · ودورةٌ ثانيةٌ تُفتَحُ بعدَ الأولى **وإعادةُ الأولى تُرفَضُ** ⇐ 6 عروضٍ ودورتانِ متمايزتانِ. **والتراجعُ مقيسٌ لا مُدَّعىً**: دفعةٌ فيها **سائقٌ شبحٌ** تُفشِلُ الإدخالَ على المفتاحِ الأجنبيِّ، فيُقرأُ **من الصفِّ بعدَها** أنَّ `result.ok === false` و**`broadcast_round` بقيَ 0** و**0 عروضٍ** و**0 دوراتٍ متمايزةٍ** و**`status` بقيَ `searching`** — **فلا جولةٌ ولا نصفُ جولةٍ ولا عرضٌ يتيمٌ ولا انتقالُ حالةٍ جزئيٌّ**. **وحاجزٌ أوقعَ العملَ ويُسجَّل بلا تبييضٍ**: اختبارا سطحِ الصلاحياتِ (`الطبقة ٢` والفحصُ الهجوميُّ) سقطا لأنَّ PostgreSQL **يمنحُ تنفيذَ كلِّ دالّةٍ جديدةٍ لـ`public` بحكمِه**، **فأُصلِح السببُ** بـ`revoke all … from public` و`grant execute … to service_role` على **نمطِ `revoke_order_tracking_tokens` القائمِ** — **لا باستثناءٍ في الحاجزِ ولا بتخفيفٍ**. **و`skip` مُعلَنٌ صراحةً (`§14`)**: الملفُّ الجديدُ يستعملُ `describeIf(TEST_DATABASE_URL)` وهو **عقدُ المستودعِ القائمُ لكلِّ اختبارِ تكاملٍ** لا استثناءٌ استُحدثَ هنا، فسُجِّل في `skip-registry` بمالكٍ وسببٍ ومُشغِّلٍ مُتحقَّقٍ في `ci.yml` **ورُفِعَ حرزُ `skip-audit` إلى ٥٩ ملفّاً و٥٥١ حالةً رفعاً لا تخفيضاً**. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **الحُرّاسُ الواحدُ والعشرون كلُّهم خروجُ `0`** · بلا قاعدةٍ **٢٠٩٧ ناجحاً · ٠ فاشلاً · ٥٥٦ متجاوَزاً** · بقاعدةٍ حقيقيّةٍ (`tests/integration` و`tests/e2e`) **٤٧٨ ناجحاً · ٠ فاشلاً · ١١ متجاوَزاً** (دَينُ `BENCH_DATABASE_URL` القائمُ) · **وبوّابةُ التغطيةِ خروجُ `0` والمساراتُ التسعةُ فوقَ أرضيّتِها ولا أرضيّةَ خُفِّضَت**. **وحدُّ البيئةِ المحلّيّةِ مُعلَنٌ**: Redis غيرُ متاحٍ محليّاً و`verify-container-build.sh` يسقطُ لتباينِ إصدارِ `bun` (1.4.0 مقابل 1.3.14) — **قيدُ بيئةٍ سابقٌ**، والحكمُ فيهما من CI. **والحكمُ من CI**: التشغيلانِ `33720416457` (على `c555958baf`) و`33720425445` (على `9adcc8022d`) انتهيا **`success` في الوظائفِ الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ لا من نصِّ السجلِّ**. **وما لم يُمَسّ يُقرأُ حرفيّاً: `BUG-008` مفتوحٌ بحرفِه، لم يُقلَب ولم يُعدَّل ولم يُحَلَّ بالتخفّي داخلَ هذا البندِ** — هذا يُؤَمِّنُ **فتحَ** الدورةِ وحدَه · **ولا `BUG-002/003/004/006/007` مُسَّ** · **ولا `R-17`** (لا توزيعَ بينَ نسخٍ ولا مُحوِّلَ اختير) · **ولا `F4` ولا Socket.IO ولا Redis ولا `F2`/`F3`** · **ولا الأجرةُ ولا التسعيرُ ولا الدفعُ** · **ولا `DEC-11`** وهو محجوبٌ على حالِه · **ولا قاعدةُ أعمالٍ غيرُ متعلّقةٍ بالسباقِ ولا رسالةُ خطأٍ إلَّا بالحدِّ الأدنى ولا عقدٌ خارجيٌّ** · **ولا دَينُ حكومةٍ غيرُ متّصلٍ بهذا البندِ نُظِّف**. **ودَينٌ لُوحِظَ ويُبلَّغُ ولا يُمَسُّ**: توثيقٌ يذكرُ مهمّةً دوريّةً لإنهاءِ `EXPIRED` لا وجودَ لها (`apps/gateway/src/admin/queries.ts:1573` · `packages/domain/tracking/operations-status.ts:114`)، و`ADR 0050 §٦` يقولُ «لم يُنفَّذ» عن أمرٍ نُفِّذ، و`PHASE-6-REALTIME.md:198` صيغةٌ منسوخةٌ عن `R-17`. **ولا سعةَ قِيسَت ولا حِملَ ولا تزامنَ إنتاجيٍّ — فلا `مَقيس` ولا `مُثبَت` (`ح-5`): المُثبَتُ أنَّ القاعدةَ نفسَها لا تسمحُ بفتحِ دورتَينِ للطلبِ الواحدِ عندَ سباقٍ حقيقيٍّ وأنَّ الفشلَ الجزئيَّ يتراجعُ كاملاً، لا أنَّ طبقةَ التوزيعِ صارت جاهزةً للإنتاجِ.** |
| **تصحيحٌ مُلحَق خامسٌ وعشرون — 2026-09-03 (وحدةُ `BUG-008` — تمييزُ التسليمِ المكرَّرِ)** | **العطلُ الموصوفُ في §١١ من هذا الملفِّ — «الفائزُ يُبلَّغُ سبقك سائق آخر» — أُغلِق، وعمودُ حالةِ `BUG-008` في `docs/ROADMAP-MASTER.md` §11-أ صارَ `[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفاً (`ح-1`).** **والسببُ الجذريُّ كما وُصِفَ ههنا حرفاً**: `claim_ride` تبدأُ بـ`select … where status = 'searching' for update skip locked`، فمتى أُسنِدَ الطلبُ لم يبقَ `searching` فلا يجدُ النداءُ الثاني — **من الفائزِ نفسِه** — صفّاً فيخرجُ `ORDER_NOT_CLAIMABLE` **وهو جوابُ الخاسرِ عينُه**، والحوارُ يُترجِمُ كلَّ `claimed = false` إلى `driver.offer_taken`. **والحالةُ التجاريّةُ كانت سليمةً تماماً والعطلُ في ما يُقال وحدَه** — وهو ما كان مُسجَّلاً ههنا، **فالمُصلَحُ هو التمييزُ الذي قيلَ إنّه مفقودٌ: «سُبِقتُ» من «أنا صاحبُ الإسنادِ أصلاً»**. **والمُنفَّذُ `create or replace function claim_ride` بجسمٍ مطابقٍ** (`supabase/migrations/20260903120000_claim_ride_distinguishes_duplicate_delivery.sql`)، والزيادةُ في فرعِ `if not found` وحدَه: قراءةٌ ثانيةٌ بثلاثةِ شروطٍ `id` و`assigned_driver_id = p_driver_id` و`is_driver_engaged_order_status(status)` تُخرِجُ `ok = true, duplicate = true`، وإلّا `ORDER_NOT_CLAIMABLE` كما كان. **ومفتاحُ التكرارِ قائمٌ لا مُختَرَعٌ**: النقرةُ لا تحملُ إلّا `(order_id, driver_id)` وهما بعينُهما `orders.id` + `orders.assigned_driver_id` — **لا معرِّفٌ جديدٌ ولا عمودٌ ولا جدولُ إزالةِ تكرارٍ ولا قيدٌ فريدٌ، و`BUG-003` لم يُمَسّ**. **وبلا `for update` عن قصدٍ**: الفحصُ بعدَ إخفاقِ `skip locked`، ولو حُبِسَ على القفلِ لانتظرَ الخاسرُ الفائزَ في كلِّ سباقٍ — وهو ما تجنَّبَه `skip locked` أصلاً — ولقطةُ `read committed` كافيةٌ لأنَّ **الإسنادَ لا يُنقَلُ إلى سائقٍ آخرَ**. **وطلبٌ `completed` أو `cancelled` ليس تكراراً** فيخرجُ `ORDER_NOT_CLAIMABLE` — لا نجاحٌ كاذبٌ. **وفرعُ التكرارِ لا يكتبُ شيئاً ألبتّةَ** (لا `update orders` · لا `update order_offers` · لا صفَّ تدقيقٍ ثانياً)، **و`matched_at` يُرجَعُ المُخزَّنَ لا `now()`**، **و`rider: null` عن قصدٍ ومعه `if (!claim.value.duplicate)` حولَ `notifyRiderOfAcceptance`**، **و`recordDispatchOfferAccepted()` صارَ مشروطاً بـ`!duplicate`** — فلا انتقالُ حالةٍ ولا عرضٌ ولا إسنادٌ ولا إخطارٌ ولا قياسٌ يتكرَّرُ. **وردُّ الحوارِ للفائزِ `driver.offer_accepted` بمفتاحٍ قائمٍ ولا مفتاحَ `i18n` جديدٌ**، **ورسالةُ نجاحٍ واحدةٌ**. **والحكمُ من المغلَّفِ لا من غيابِ حقلٍ** (`raw.duplicate === true`)، **فمغلَّفٌ قديمٌ بلا الحقلِ يُقرأُ إسناداً أوّلَ كما كان**. **ولا صلاحياتٌ تغيَّرت** فـ`create or replace` يحفظُ `ACL` القائمةَ. **والدليلُ سبعُ حالاتٍ على PostgreSQL حقيقيٍّ باتّصالاتٍ منفصلةٍ** (`tests/integration/claim-ride-duplicate-delivery.test.ts`)، ومنها **سائقانِ متزاحمانِ ⇐ فائزٌ واحدٌ وخاسرٌ صريحٌ ولا واحدٌ منهما `duplicate`**، و**إعادةُ مطالبةِ الفائزِ ⇐ `duplicate = true` والحالةُ كلُّها `toEqual` ما كانت بعدَ الأولى حرفاً**، و**تسليمٌ ثانٍ قبلَ التثبيتِ بحاجزِ معاملةٍ ⇐ حاسمٌ ثمَّ تكرارٌ بعدَ التثبيتِ** — **والسباقُ لم يُصنَع بنومٍ ولا بترتيبِ `Promise`**. **وسبعُ حالاتٍ لحدِّ المحوِّلِ** في `tests/unit/claim-ride-envelope.test.ts` **الذي لم يكن يُحمِّلُه اختبارٌ قطُّ**. **والممنوعُ لم يُستعمَل**: لا قفلٌ في العمليّةِ · لا `Map` · لا مخزنُ إزالةِ تكرارٍ في الذاكرةِ · لا `sleep` يُخفي سباقاً · لا مقارنةٌ في العميلِ ثمَّ تحديثٌ غيرُ مشروطٍ. **و`apps/gateway/src/routes/update-dedup.ts` ليس الحلَّ ولم يُمَسّ** (يُفقَدُ عندَ إعادةِ التشغيلِ ولا يُشارَكُ، والنقرةُ الثانيةُ تحملُ `update_id` آخرَ أصلاً)، **والإزالةُ الموزَّعةُ بندٌ آخرُ (`BUG-002`) ولم يُبدَأ**. **ولا تسجيلَ أُضيفَ ألبتّةَ** فلا سطرَ سجلٍّ جديدٌ يُفشي سرّاً ولا حمولةَ `callback` خامّةً. **وحاجزٌ أوقعَ العملَ ويُسجَّلُ بلا تبييضٍ**: بوّابةُ التغطيةِ سقطت أوّلَ مرّةٍ (٤٣٫٩٢٪ دونَ ٤٤٪)، **فأُصلِحَ السببُ بتغطيةٍ حقيقيّةٍ فصارت ٤٧٫٦٠٪ لا بتخفيضِ أرضيّةٍ**؛ **وحرزُ `skip-audit` رُفِعَ إلى ستّينَ ملفّاً و٥٥٨ حالةً رفعاً لا تخفيضاً**. **وإخفاقٌ في الطريقِ يُسجَّلُ**: التشغيلُ `33746358782` على الوحدةِ الأولى سقطَ في `Typecheck` لأنَّها وسَّعت `ClaimRideResult` بحقلٍ إلزاميٍّ ومزدوجاتُ الاختبارِ لم تُطابِقْه إلّا في الثانيةِ. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · بلا قاعدةٍ **٢١٠٧ ناجحاً · ٠ فاشلاً · ٥٦٥ متجاوَزاً** · بقاعدةٍ حقيقيّةٍ **٤٨٥ ناجحاً · ٠ فاشلاً · ١١ متجاوَزاً**. **وRedis غيرُ متاحٍ محليّاً و`verify-container-build.sh` يسقطُ لتباينِ إصدارِ `bun` — قيدُ بيئةٍ سابقٌ والحكمُ من CI.** **وما لم يُمَسّ**: `BUG-002` و`BUG-003` و`BUG-004` و`BUG-006` و`BUG-007` مفتوحةٌ بحرفِها · ولا `BUG-001` ولا `BUG-005` مُسَّ **ولم يُعتبَر إغلاقُ `BUG-005` حلّاً لهذا** · ولا `R-17` ولا `F4` ولا Socket.IO ولا توزيعُ Redis ولا `F2`/`F3` · ولا الأجرةُ ولا التسعيرُ ولا الدفعُ · ولا `DEC-11` · ولا `ADR 0042/0050/0051/0052/0053` · **ولا ADR منشورٌ عُدِّل (`ح-6`) ولا ADR جديدٌ أُنشئ** لأنَّ هذا تنفيذُ عقدٍ منصوصٍ في البندِ نفسِه و`ADR 0023` قُرئ كاملاً فوافقَه · **ولا بندٌ تالٍ بُدِئ**. **ولا سعةَ قِيسَت ولا حِملَ ولا تزامنَ إنتاجيٍّ ولا سلوكَ تلغرام الحقيقيَّ — فلا `مَقيس` ولا `مُثبَت` (`ح-5`).** والوحدتانِ `f85f3da621` و`6a8ef041e7`، **وCI أخضرُ في التشغيلِ `33746376273` بوظائفِه الثلاثِ و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ**. الدليلُ `docs/evidence/correctness/BUG-008-20260903.md`. |
| **تصحيحٌ مُلحَق سادسٌ وعشرون — 2026-09-04 (وحدةُ حوكمةٍ — ملحقٌ حاكمٌ لا تنفيذٌ)** | **أُضيف إلى `docs/MASTER_DIRECTIVE.md` ملحقٌ حاكمٌ واحدٌ (2026-09-04) يُقرُّ صنفاً واحداً مغلقاً — «سجلُّ مدخلٍ سابقٍ للمجالِ» (`domain-ingress receipt`) — يجوز له وحدَه — ولا سواه — أن لا يحمل `city_id` لحظةَ الإنشاءِ، لأنَّ الانتماءَ إلى مدينةٍ غيرُ متاحٍ بصورةٍ صادقةٍ عندَ لحظةِ الاستلامِ.** **والقاعدةُ 0.4 تبقى مطلقةً خارجَ هذا الصنفِ**: لا تُخفَّف لأيِّ جدولِ مجالٍ آخرَ، و`RLS` لا تسقط، ولا يُملأ `city_id` بقيمةٍ مُلفَّقةٍ، **وأيُّ صنفٍ ثانٍ يتطلّب ملحقاً حاكماً جديداً قبلَ التنفيذِ**. **وهذا تغييرٌ حوكميٌّ فقط**: لا هجرةَ ولا سطرَ شيفرةٍ ولا اختبارَ ولا ملفَّ دليلٍ ولا سطرَ في سجلِّ القسم 25، و`scripts/check-migrations.ts` و`apps/gateway/src/routes/telegram-webhook.ts` و`docs/ROADMAP-MASTER.md` لم تُمَسَّ حرفاً. **و`ADR 0054` لم يُنفَّذ بعدُ ولم يُمَسَّ نصُّه** (`ح-1`) — ولا جدولَ استلامٍ أُنشئ. **ولم يُغلَق بندٌ ولا قُلِبَ إلى `[x]`**: `BUG-002` و`F6-01` و`F6-02` و`SCL-001` و`CAP-001` وغيرُها تبقى `[ ]` كما هي، فلا موضعَ لـ`ح-4` ولا لسلّمِ الأدلةِ. **ولا قياسَ سعةٍ ولا أداءٍ ههنا فلا `مَقيس` ولا `مُثبَت`** (`ح-5`) |
| **تصحيحٌ مُلحَق سابعٌ وعشرون — 2026-09-04 (وحدةُ `BUG-002` — الاستلامُ الصامدُ وترتيبُ الحدِّ قبلَ منعِ التكرارِ)** | **نُفِّذ `BUG-002` وحدَه وقُلِب `[x]` بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها (`ح-4`).** **والعيبُ عطبانِ لا واحدٌ**: منعُ التكرارِ كان خريطةً في ذاكرةِ العمليةِ (`apps/gateway/src/routes/update-dedup.ts`) فإعادةُ الإقلاعِ تمحو القرارَ والنسخةُ الأخرى لا تراه؛ **والأسوأُ** أنَّ `dedup.admit()` — **وهي تفحصُ وتَسِمُ في نداءٍ واحدٍ بحكمِ توقيعِها** — كانت تُنادى **قبلَ** حدِّ المعدَّلِ، فطلبٌ يُرفَض بـ`429` **يكونُ قد استهلكَ `update_id` في الوسمِ**، فإعادةُ تيليجرام للتحديثِ نفسِه تُبتلَع بوصفِها «مكرَّراً» — **فقدٌ دائمٌ للأصلِ لا تكرارُ معالجةٍ**، وهو بعينِه عمودُ الأثرِ في البندِ: «ضياع أحداث حقيقية = رحلات معلّقة». **والترتيبُ صارَ**: سرٌّ ← حجمُ الجسمِ والتحليلُ ← **حدُّ المعدَّلِ** ← `update_id` ← **الحجزُ الصامدُ في القاعدةِ** ← معالجةٌ ← ختمٌ ← `ACK`. **والمُنفَّذُ هجرةٌ واحدةٌ** `20260904090000_telegram_update_receipts.sql`: جدولُ `telegram_update_receipts` بمفتاحٍ أوّليٍّ `(bot, update_id)` — **فالتفرُّدُ ذرّيٌّ على الزوجِ لا فحصٌ في التطبيقِ** — و`update_id bigint` لا `integer`، وحالاتٌ أربعٌ (`pending`/`claimed`/`failed`/`done`) وقيدانِ يمنعانِ المستحيلَ (`done_is_sealed` · `claim_is_paired`) وفهرسٌ جزئيٌّ على `claimed_at where status = 'claimed'`. **ولا حمولةَ خامّةً ولا نصَّ رسالةٍ ولا معرِّفَ مستخدمٍ** (`ADR 0054 §٧/١`)، **والوفاءُ بـ«مرّةً على الأقلّ» يبقى ممكناً بلا حمولةٍ لأنَّ إعادةَ تيليجرام نفسَها هي ناقلُ الحمولةِ** — فالسجلُّ يحفظُ **القرارَ** لا **المحتوى**. **ولا `city_id`** بإذنِ **الملحقِ الحاكمِ 2026-09-04** وحدَه، لأنَّ قرارَ الاستلامِ **يسبقُ أيَّ تفسيرٍ للحمولةِ** فلا مدينةَ معلومةً بعدُ ونسبتُه إلى مدينةٍ اشتقاقٌ اصطناعيٌّ منهيٌّ عنه في الملحقِ نفسِه — **و`RLS` مُفعَّلةٌ ولم تسقط**، و`revoke all from public/anon/authenticated` مع `grant to service_role`. **والدالّتانِ**: `claim_telegram_update` قرارٌ واحدٌ ذرّيٌّ — `insert … on conflict (bot, update_id) do nothing … returning` ⇒ `claimed`، **لا `select` ثمَّ `insert` منفصلَينِ فلا نافذةَ تُقتنَصُ** — وإلَّا `CTE` بـ`for update skip locked` ⇒ `reclaimed` (ومعه `CLAIM_ABANDONED` عندَ تجاوزِ المهلةِ)، وإلَّا `done` ⇒ `duplicate`، وإلَّا ⇒ `in_progress`؛ **و`skip locked` مقصودٌ لا تجميليٌّ: مسارُ الإقرارِ السريعِ لا يجوزُ أن ينتظرَ قفلاً**. و`finish_telegram_update` **مطابقةُ رمزِ الحجزِ شرطٌ في `where`** وصفرُ صفوفٍ ⇒ `NOT_CLAIMED_BY_CALLER`. **والحارسُ إعفاءٌ مغلقٌ لا `bypass`**: `scripts/check-migrations.ts` لا يُعفي جدولاً من `city_id` إلَّا **باجتماعِ شرطَينِ** — اسمٌ في القائمةِ المغلقةِ في `packages/shared/config/domain-ingress.ts` **و**التصريحُ الحرفيُّ `-- domain-ingress-receipt: <name>` في ملفِّ الهجرةِ نفسِه — **ويسقطُ على ثلاثِ صورٍ من الالتفافِ** (تصريحٌ لاسمٍ خارجَ القائمةِ · تصريحٌ بلا `create table` · مُدخلٌ ميّتٌ)، **و`RLS` تبقى مفروضةً ولا حارسٌ أُضعِف**، ومخرجُه يقولُ ذلك عن نفسِه. **والمهلةُ (٣٠ث) حدٌّ تقنيٌّ لا تجاريٌّ** فموضعُها `packages/shared/config/` وتُمرَّرُ وسيطاً فلا رقمَ مدفونٌ في الهجرةِ؛ **و«عددُ المحاولاتِ قبلَ اليأسِ» مسألةٌ غيرُ محسومةٍ** (`ADR 0054 §١٠/٤`) فلم تُخترَع لها سياسةٌ. **وعجزُ القاعدةِ ⇒ `503 INTAKE_UNAVAILABLE` لا `200`** فتيليجرام يُعيدُ، **ولا يُنتظَرُ نجاحُ أثرٍ خارجيٍّ حتى الإقرارِ**. **والدليلُ خمسُ حالاتٍ على PostgreSQL حقيقيّةٍ — خمسٌ ناجحةٌ صفرُ فاشلةٍ · ٤٨ توكيداً**: مكرَّرٌ ⇒ معالجةٌ واحدةٌ وصفٌّ واحدٌ `done`/`attempts = 1` · **مرفوضٌ بـ`429` لا يُودِعُ الرقمَ ثمَّ الإعادةُ بعدَ النافذةِ تُقبَلُ فلا فقدَ للأصلِ** · **عمليتانِ منفصلتانِ فعلاً بـ`Bun.spawn`** ⇒ `claimed` ثمَّ `in_progress` ثمَّ `duplicate` بعدَ الختمِ وصفٌّ واحدٌ — **فخريطةُ ذاكرةٍ تُخفِقُ ههنا حتماً** · حجزٌ ناجحٌ ومنعُ حجزٍ مزدوجٍ ورمزٌ غريبٌ يُرفَضُ واستعادةُ حجزٍ مهجورٍ ⇒ `reclaimed` و`attempts = 2` · **ومكرَّراتٌ وتجاوزُ حدٍّ معاً ⇒ المعالَجُ `[الأصلُ، المُتضوِّرُ]` بالضبطِ** وهو نصُّ عمودِ الاختبارِ في البندِ حرفاً. **وسيناريو القياسِ الموزَّعُ `cross-process-replay` قُلِبَت توقُّعاتُه لتُثبِتَ الإصلاحَ** (`ADR 0054 §١١/٣`) وشُغِّلَ على عنقودٍ حقيقيٍّ من **بوّابتَينِ مستقلّتَينِ** فخرجَ **`نجاحُ عمل` بثلاثةٍ وعشرينَ توكيداً**، ومنها «لم تُستشَر خريطةُ الذاكرةِ في أيِّ نسخةٍ — `gw1=0` · `gw2=0`» و«إيصالُ استلامٍ واحدٌ بالضبطِ مهما تعدّدت التسليماتُ». **وسياسةُ إعفاءِ الإعاداتِ من الحصّةِ لم تُخترَع** (`§١٠/٥`) — بل الحالةُ الخامسةُ توثِّقُ صراحةً أنَّ التسليمَ المكرَّرَ **يستهلكُ** الحصّةَ، حتى لا يُقرأَ سكوتٌ إقراراً. **وحواجزُ أوقعت العملَ وتُسجَّل بلا تبييضٍ**: `check-schema-contract` (كُتِبَ العقدُ بـ`--write` ⇐ 80 دالّةً و23 جدولاً) · `check-skip-classification` مرّتَينِ — تجاوزٌ غيرُ مُصنَّفٍ، ثمَّ **مسارٌ حرجٌ خارجَ القائمةِ المغلقةِ فلم تُوسَّع القائمةُ لأجلِ ملفٍّ** بل صُنِّفَ على «دورةُ الرحلةِ والإسناد» وهو نصُّ أثرِ البندِ حرفاً — **ورُفِعَ حرزُ `skip-audit` إلى ٦١ ملفّاً و٥٦٣ حالةً رفعاً لا تخفيضاً**. **ولا أرضيّةَ تغطيةٍ خُفِّضَت ولا حارسٌ أُسكِتَ ولا استثناءٌ أُضيفَ لتمريرِ فحصٍ.** محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **الحُرّاسُ الواحدُ والعشرون كلُّهم خروجُ `0`** · بلا قاعدةٍ **٢١٠٧ ناجحاً · ٠ فاشلاً · ٥٧٢ متجاوَزاً** · بقاعدةٍ حقيقيّةٍ **٢٥٦٠ ناجحاً · ٠ فاشلاً · ٢٣ متجاوَزاً · ٩٤٦٦ توكيداً** · **وبوّابةُ التغطيةِ خروجُ `0` والمساراتُ التسعةُ فوقَ أرضيّتِها**. **وحدُّ البيئةِ المحلّيّةِ مُعلَنٌ**: Redis غيرُ متاحٍ محليّاً و`verify-container-build.sh` يسقطُ لتباينِ إصدارِ `bun` (1.4.0 مقابل 1.3.14) — **قيدُ بيئةٍ سابقٌ**، والحكمُ فيهما من CI. **والحكمُ من CI**: التشغيلُ `33818910358` على `4a30c625356e39636aa6b0e82b667944bddbe115` انتهى **`success` في الوظائفِ الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ لا من نصِّ السجلِّ**. **وما لم يُمَسّ يُقرأُ حرفيّاً**: **`SCL-001` يبقى `[ ]`** لأنَّ نصَّه يطلبُ «dedup موزَّع **في Redis**» والمُنفَّذُ في PostgreSQL وفقَ `ADR 0054`، **والخريطةُ في الذاكرةِ لم تُحذف** بل بقيت مسارَ تدهورٍ مُعلَناً حينَ لا يُركَّبُ المنفَذُ — **ونصُّ البندِ لم يُعدَّل ليطابقَ التنفيذَ (`ح-1`)** · **`CAP-001` و`F6-01` يبقيانِ `[ ]`** لأنَّ كليهما يشترطُ طابوراً ومعالجةً في عاملٍ، **والمعالجةُ ما زالت داخلَ الطلبِ**، وعاملٌ منفصلٌ حقيقيٌّ يوجبُ تخزينَ الحمولةِ التي يمنعُها `§٧/١` — **قيدٌ مُعلَنٌ لا تعارضٌ** · **`F6-02` يبقى `[ ]`** فالمُنفَّذُ حجزٌ وإيجارٌ واستعادةٌ على سجلِّ الاستلامِ لا طابورٌ بمستهلِكٍ منفصلٍ · **ولا `R-17` ولا شرطُ `numInstances: 1` مُسَّ** — البرهانُ أنَّ القرارَ لا يسكنُ الذاكرةَ لا إذنٌ بتعدُّدِ النسخِ · **ولا `DEC-06`/Socket.IO ولا `BUG-003…007` ولا `F5-03` ولا `SCL-003`** · **ولا سياسةُ حدِّ المعدَّلِ عامّةً غُيِّرَت** — ما تغيَّر موضعُها في الترتيبِ لا قواعدُها · **ولا `ADR` منشورٌ عُدِّل (`ح-6`) ولا `ADR` جديدٌ أُنشئ**: `ADR 0054` قائمٌ من جولةٍ سابقةٍ والتنفيذُ مطابقٌ له ولم يُعدَّل منه حرفٌ · **ولا دَينُ حكومةٍ غيرُ متّصلٍ نُظِّف**. **ولا `exactly-once` خارجيٌّ يُدَّعى** (`§٥/٤`): المضمونُ أثرٌ تجاريٌّ واحدٌ لكلِّ `(bot, update_id)` بحكمِ القاعدةِ، لا ضمانةُ تسليمٍ واحدةٍ عبرَ الشبكةِ. **وسقوطُ العمليةِ بعدَ الحجزِ وقبلَ الختمِ يتركُ صفّاً `claimed` واستئنافُه معلَّقٌ على إعادةِ تيليجرام للتحديثِ نفسِه** لأنَّ الحمولةَ لا تُخزَّن — **وفاءٌ بـ«مرّةً على الأقلّ» لا استعادةٌ ذاتيّةٌ**. **ولا سعةَ قِيسَت ولا حِملَ ولا زمنَ ولا سلوكَ تيليجرام الحقيقيَّ في الإنتاجِ — فلا `مَقيس` ولا `مُثبَت` (`ح-5`).** |
| **تصحيحٌ مُلحَق ثامنٌ وعشرون — 2026-09-04 (وحدةُ تصحيحِ حالةٍ — `BUG-002` يُعاد إلى `[ ]`)** | **الملحقُ السابعُ والعشرونَ أعلاه يُقرأُ من الآنَ مقيَّداً بهذا الملحقِ: كلُّ ما فيه عن الشيفرةِ والهجرةِ والاختباراتِ والحرّاسِ وCI قائمٌ ولم يتغيّر منه حرفٌ، وما فيه من إعلانِ إغلاقٍ لـ`BUG-002` باطلٌ.** **والسببُ الحاكمُ** أنَّ عمودَ الحلِّ في البندِ يعدُّ ستَّ خطواتٍ — «تحقّق سرّ → حدّ معدّل → dedup موزَّع → استلام صامد → **طابور** → ACK سريع» — **والخطوةُ الخامسةُ لم تُنفَّذ**. **وما بُني حجزٌ وإيجارٌ (`claim`/`lease`) على سجلِّ الاستلامِ نفسِه يُستدعى داخلَ الطلبِ — وهو ليس طابوراً ولا مستهلِكاً منفصلاً، ولا يُسمّى طابوراً**، والمعالجةُ ما زالت تجري داخلَ طلبِ `HTTP`. **والحكمُ السابقُ كان تقديريّاً خاطئاً**: بُنيَ على عمودَي العيبِ والاختبارِ وحدَهما، **وعمودُ الحلِّ جزءٌ من البندِ لا زينةٌ فيه**؛ **و[`ADR 0054 §٦/د`](adr/0054-telegram-webhook-durable-ingest-and-dedup.md) يحكمُ صراحةً أنَّ ما دونَ «موزَّع» و«صامد» و«طابور» معاً «مرفوضٌ إغلاقاً لـ`BUG-002`»، و`§٣/٦` منه يشترطُ أن لا يُنفَّذَ العملُ التالي للإيصالِ داخلَ طلبِ `HTTP`** — شرطٌ غيرُ مستوفىً — **و`§٧/١` منه يقول إنَّ البندَ «يُغلقه تنفيذُه بدليلِه»**. **فلا تعارضَ نشأ عن هذا التصحيحِ، بل التصحيحُ يوافقُ القرارَ المنشورَ ولم يُعدَّل منه حرفٌ.** **والمُنفَّذُ فعلاً — ويبقى صحيحاً وقائماً في `4a30c625356e`**: حدُّ المعدَّلِ صارَ يُستهلَك قبلَ إيداعِ `update_id` · ومنعُ التكرارِ صارَ موزَّعاً بقيدِ تفرُّدٍ ذرّيٍّ `(bot, update_id)` في القاعدةِ لا بخريطةٍ في ذاكرةِ العمليةِ · والاستلامُ صارَ صامداً يصمُد أمامَ إعادةِ الإقلاعِ ويُرى من عمليةٍ أخرى · والإقرارُ يعقبُ الإيداعَ لا الأثرَ الخارجيَّ، وعجزُ القاعدةِ ⇒ `503` لا `200`. **والدليلُ المقيسُ لم يُحذَف ولم يُخترَع له بديلٌ**: خمسُ حالاتٍ على PostgreSQL حقيقيّةٍ بـ٤٨ توكيداً، وسيناريو `cross-process-replay` على بوّابتَينِ مستقلَّتَينِ. **لكنَّ استيفاءَ عمودِ الاختبارِ دليلُ صحّةٍ على ما نُفِّذ لا شهادةُ اكتمالٍ للبندِ.** **ووثيقةُ `docs/evidence/correctness/BUG-002-20260904.md` صُحِّحَت صياغتُها** فصارت «دليلَ إصلاحٍ جزئيٍّ في نطاقِ `BUG-002`» لا دليلَ إغلاقٍ، و§٩ منها صارَ يفصلُ «ما ثبتَ فعلاً» عن «ما لم يُنفَّذ» — **بلا حذفِ نتيجةٍ مقيسةٍ ولا اختراعِ دليلٍ جديدٍ**. **ولم يُمَسّ في هذه الوحدةِ**: **لا شيفرةَ ولا هجرةَ ولا اختبارَ ولا حارسَ** — والإصلاحُ البرمجيُّ **لم يُرجَع ولم يُنقَض** · و`F6-01` و`F6-02` و`CAP-001` و`SCL-001` تبقى `[ ]` بنصوصِها حرفاً ولم يُعدَّل نطاقُها · ولا `ADR` عُدِّل · ولا `R-17` ولا `DEC-06` ولا `BUG-003…007` ولا `F5-03` ولا `SCL-003`. **ولا طابورَ بُدئ في هذه الوحدةِ ولا يُبدأ بعدَها إلَّا بأمرٍ مستقلٍّ.** **والحالةُ الصادقةُ للنظامِ**: مدخلُ تيليجرام صارَ لا يفقدُ التحديثَ الأصليَّ عندَ تجاوزِ الحدِّ، وقرارُ منعِ التكرارِ صارَ يصمُد ويُرى عبرَ العملياتِ — **وتزامنُ المعالجةِ بلا طابورٍ (`CAP-001`) قائمٌ كما كان**. |
| **تصحيحٌ مُلحَق تاسعٌ وعشرون — 2026-09-05 (وحدةُ `BUG-003` — رفضٌ يصوبُ على عرضٍ واحدٍ بمعرّفِهِ لا على كلِّ عرضٍ معلَّقٍ للسائقِ)** | **نُفِّذ `BUG-003` وحدَه وقُلِب `[x]` في `docs/ROADMAP-MASTER.md` §11-أ بعدَ خضرةِ وظائفِ CI الثلاثِ لا قبلَها (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفاً (`ح-1`).** **والعيبُ بحرفِهِ**: الرفضُ كان يُصيبُ بالاسمِ `(order_id, driver_id)` كلَّ عرضٍ معلَّقٍ للسائقِ مهما اختلفتْ جولتُه — `update order_offers set status='rejected' where order_id=$ and driver_id=$ and status='pending'` — **فضغطةُ رفضٍ على عرضِ الجولةِ الثانيةِ تُلغي عرضَ الجولةِ الأولى المعلَّقَ** الذي ما زال محتمِلاً لم تنتهِ مهلتُه بعدُ. **والأصلُ الجذريُّ** أنَّ `offerId` لم يكن يتدفّقُ من القاعدةِ إلى الزرِّ أصلاً: `OfferNotification` بلا `offerId`، و`open_offer_round` تُرجعُ `offers` عدداً وحدَه. **والمُنفَّذُ هجرةٌ واحدةٌ** `20260905020000_open_offer_round_returns_offer_ids.sql` (`create or replace function open_offer_round`): صارتْ تُرجعُ `offer_ids: [{offer_id, driver_id}]` من CTE `insert…returning` + `jsonb_agg`، **والعددُ والمصفوفةُ من المصدرِ نفسِه** — لا `get diagnostics` بعدَ `jsonb_agg` (فهو يَعُدُّ صفَّ النتيجةِ واحدةً لا المُدرَجةَ، خطأٌ التقطَه المراجِعُ وصُلِّحَ قبلَ الدفعِ). و`broadcastOffers` يبني `insertedByDriver` ويُمرّرُ `offerId` في كلِّ إشعارٍ، و`offered`/`notified` صارا من الصفوفِ المُدرَجةِ فعلاً (الإدخالُ الفاشلُ ⇒ `unreachable`). **والرفضُ صار** `where id=${offerId}::uuid and driver_id=${driverId}::uuid and status='pending' returning id` — **المفتاحُ الأساسيُّ `id` يُثبِتُ `order_id` و`round`** فيُحقِّقُ اشتراطَ الأربعةِ معرّفاتٍ دونَ حشرِها في `where`، فيبقى عرضُ الجولةِ الأخرى معلَّقاً. وزرُّ الرفضِ `offer:reject:${offerId}` (٥٠ بايتاً < حدِّ تلغرام ٦٤). **والقبولُ لم يُمَسَّ**: `claim_ride` تلتقطُ `round desc limit 1` وتُلغي العروضَ المعلَّقةَ الأخرى ذرّيّاً فالقبولُ غيرُ ملتبسٍ — **ولم يُوسَّعْ نطاقُ `BUG-003` إلى القبولِ**. **ولا صلاحياتٌ تغيَّرت** فـ`create or replace` يحفظُ `ACL` القائمةَ، وسطحُ الصلاحياتِ يُعادُ تأمينُه (`revoke … from public` و`grant … to service_role`) ومُسجَّلٌ في `rollback-registry`. **والدليلُ ثلاثُ حالاتٍ على PostgreSQL حقيقيّةٍ** (`tests/integration/dispatch-reject-offer-scoped.test.ts`): جولتانِ متتاليتانِ للسائقِ نفسِه، رفضُ عرضِ الجولةِ الثانيةِ **يتركُ عرضَ الجولةِ الأولى معلَّقاً** · سائقٌ أجنبيٌّ ⇒ صفرُ صفوفٍ · عرضٌ قُبِلَ لا يُرفَض بأثرٍ رجعيٍّ. **والقيدُ الحاسمُ** (`tests/integration/dispatch-open-round-atomicity.test.ts`): `offers` العدديُّ يساوي طولَ `offer_ids` — لا تُفكِّرُ الدالّةُ عدداً لا تطابقُه المصفوفةُ. **وحاجزٌ أوقعَ العملَ ويُسجَّل بلا تبييضٍ**: التشغيلُ `33931717692` على `0616db5` سقطَ في تكاملِ PostgreSQL لأنَّ `full-ride.test.ts` كان يمرّرُ `orderId` في زرِّ الرفضِ (العقدُ القديمُ) فيبقى العرضُ معلَّقاً، **فصُلِّحَ السببُ الجذريُّ** بجلبِ المعرّفِ الفعليِّ من القاعدةِ كما يبنيه المغلَّفُ في الإنتاجِ — **لا تضعيفُ العقدِ ولا تخطّي الاختبارِ ولا حذفُه**. **وحرزُ `skip-audit` رُفِعَ إلى ٦٢ ملفّاً و٥٦٦ حالةً رفعاً لا تخفيضاً، ولا أرضيّةَ تغطيةٍ خُفِّضَت ولا حارسٌ أُسكِتَ**. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **الحُرّاسُ الـ٢٢ خروجُ `0`** · بلا قاعدةٍ **١٨٤٣ ناجحاً · ٠ فاشلاً**. **ولا PostgreSQL محليّاً فالتكاملُ يُتجاوَزُ محليّاً ويُقاسُ في CI، والحكمُ من CI** (`ح-5`). **وما لم يُمَسّ يُقرأُ حرفيّاً**: `BUG-002`/`BUG-004`/`BUG-006`/`BUG-007` مفتوحةٌ بحرفِها · ولا `BUG-001`/`BUG-005` مُسَّ في هذه الجولةِ · ولا `R-17` (لا توزيعَ بينَ نسخٍ ولا مُحوِّلَ) · ولا `F4`/Socket.IO/Redis/`F2`/`F3` · ولا الأجرةُ/التسعيرُ/الدفعُ · ولا `DEC-11` · **ولا ADR منشورٌ عُدِّل (`ح-6`) ولا ADR جديدٌ أُنشئ** فالنمطُ مسبوقٌ بـ`BUG-005` · **ولا بندٌ تالٍ بُدِئ، ولا دَينُ حكومةٍ غيرُ متّصلٍ نُظِّف**. **ولا سعةَ قِيسَت ولا حِملَ ولا سلوكَ تلغرام الحقيقيَّ — فلا `مَقيس` ولا `مُثبَت` (`ح-5`): المُثبَتُ أنَّ القاعدةَ نفسَها تُغيّرُ صفَّ عرضٍ واحدٍ بالمعرّفِ وتتركُ عرضَ الجولةِ الأخرى معلَّقاً عندَ رفضٍ على قاعدةٍ حقيقيّةٍ، لا أنَّ البثَّ قِيسَ في الإنتاجِ.** والوحدتانِ `0616db5` و`255c8e7`، **وCI أخضرُ في التشغيلِ `33932032090` بوظائفِه الثلاثِ و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ**. الدليلُ `docs/evidence/correctness/BUG-003-20260905.md`. |
| **تصحيحٌ مُلحَق ثلاثون — 2026-09-06 (وحدةُ `BUG-004` — صندوقُ صادرٍ معاملاتيٌّ لمسارِ العرضِ وحدَه، والبندُ لا يُغلَق)** | **نُفِّذ شطرُ مسارِ العرضِ من `BUG-004` وصارت حالتُه في §11-أ `[~]` لا `[x]`** — **والسببُ الحاكمُ** أنَّ نصَّ البندِ يُلزِمُ «Outbox **موحّد**» وموضعَه «متعدّد»، والمُنفَّذُ صندوقٌ واحدٌ لمسارٍ واحدٍ، ورأسُ الهجرةِ نفسُه يقولُ «`BUG-004` يكتفي بإشعارِ العرضِ». ونصُّ البندِ لم يُمَسّ حرفاً (`ح-1`). **والمُنفَّذُ:** جدولُ `notification_outbox` بـ`city_id` و`kind check in ('offer')` و`offer_id` فريدٍ و`claim_token` و`attempts` و`next_attempt_at` وقيدَي `delivered_pair`/`claim_pair` و`RLS` لـ`service_role`، وثلاثُ دوالَّ ذرّيّةٍ (`claim_notification_delivery` بـ`for update skip locked` ورمزِ حجزٍ · `finish_notification_delivery` بالرمزِ لا بالصفِّ · `abandon_notification_delivery`)، **و`open_offer_round` صارت تكتبُ صفَّ الإشعارِ في معاملةِ إدراجِ العرضِ نفسِها**، والإرسالُ الفعليُّ انتقلَ إلى عاملٍ كلَّ 30 ثانيةً. **ولا رقمَ تجاريّاً في الشيفرةِ**: ثلاثةُ مفاتيحَ في `platform_settings` لكلِّ مدينةٍ. **والدليلُ ثمانِ حالاتٍ على PostgreSQL حقيقيّةٍ في CI** ومنها ما يطلبُه البندُ حرفاً: «فشلُ الإرسالِ بعد نجاحِ المعاملةِ يُعادُ بلا تكرارِ أثر»، ومعها «المُسلَّمُ لا يُلتقطُ ثانيةً» و«الحجزُ المتروكُ يُستعادُ» و«العرضُ الميّتُ يُتخلى عنه». **وأسبابٌ جذريّةٌ أُصلحت لا حُوِّلت**: `main` كان أحمرَ و`biome` يحجبُ خطأَ نوعٍ حقيقيّاً تحتَه؛ وإعادةُ تسميةِ `notified`→`offered` لم تُنشَرْ إلى `rider-dialog.ts` فكان `TypeError` يخنقُ كلَّ ردٍّ للراكبِ ويُسقِطُ `full-delivery.test.ts:227` على قاعدةٍ حقيقيّةٍ؛ و`DriverNotifier` نُقِلَ من `BroadcastDependencies` إلى `RiderBotDependencies.matching` فسقطَ التوصيلُ الميّتُ؛ ومسارُ عودةِ هجرةِ `20260905030001` أُعلِنَ (`OPS-010`)؛ وحاجزُ تفعيلِ المدينةِ أُرضِيَ بضبطِ القروباتِ الثلاثةِ في عبارةِ التفعيلِ نفسِها لا بترتيبِ ملفّاتٍ؛ والدوالُّ الثلاثُ أُدرِجت في عقدِ المخطّطِ فصار **83 دالّةً و23 جدولاً**؛ وتجاوزُ اختبارِ الصندوقِ صُنِّفَ فرُفِعَ حرزُ `OPS-009` إلى **63 ملفّاً و574 حالةً رفعاً لا تخفيضاً**. **والباقي مُعلَنٌ لا مسكوتٌ عنه**: `register-unsubscribed-claim` و`rotate-negotiation-turn` و`sweep-unmatched-orders` و`rider-dialog` (إبلاغُ الإلغاءِ) و`resolve-dispute` ما زالت تُغيّرُ حالةً ثمّ تُشعِرُ خارجَ المعاملةِ، وصناديقُ البثِّ والاشتراكِ والسلامةِ **ثلاثةٌ لا واحدٌ**، **و`F6-03` يبقى `[ ]`**، **وخطرُ §4-أ رقمُ 4 يبقى قائماً بشطرِه الأوسعِ**. محليّاً: `lint`=0 · `typecheck`=0 · **الحرّاسُ الثمانيةَ عشرَ خروجُ `0`** · بوابةُ تغطيةِ `OPS-005` تمرُّ بأرضيّاتِها التسعِ · **2110 ناجحاً · 587 متجاوَزاً · 0 فاشلاً**. **ولا PostgreSQL محليّاً فالحكمُ من CI** (`ح-5`): **التشغيلُ `34001077954` على `e9ae9dc` أخضرُ بوظائفِه الثلاثِ و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ**، وسبقَه سقوطانِ حقيقيّانِ (`34000606524` و`34000718617`) يُسجَّلانِ بلا تبييضٍ. **وما لم يُمَسّ يُقرأُ حرفيّاً**: `BUG-002`/`BUG-006`/`BUG-007`/`BUG-010` مفتوحةٌ · ولا `R-17` ولا `F4` ولا Socket.IO ولا Redis ولا `F2`/`F3` · ولا الأجرةُ ولا التسعيرُ ولا الدفعُ · ولا `DEC-11` · **ولا ADR منشورٌ عُدِّل (`ح-6`) ولا ADR جديدٌ أُنشئ** فالخارطةُ نفسُها تنصُّ على الحلِّ · **ولا حاجزٌ أُسكِتَ ولا اختبارٌ عُطِّلَ ولا مهلةٌ زيدَتْ ولا أرضيّةُ تغطيةٍ خُفِّضَت ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`**: المُثبَتُ سلوكُ القاعدةِ والعاملِ لا سلوكُ Bot API فالمُرسِلُ في الاختبارِ مزدوجٌ. الدليلُ `docs/evidence/correctness/BUG-004-20260906.md` |
| **تصحيحٌ مُلحَق الحادي والثلاثون — 2026-09-06 (وحدةُ إغلاقِ `BUG-004` — الصندوقُ الموحَّدُ يكتملُ، والبندُ يُغلَقُ بـ`[x]`)** | **هذا الملحقُ يَفوقُ الملحقَ الثلاثينَ أعلاه لا يُلغيه**: كلُّ ما فيه عن الشطرِ الأوّلِ وحكمِه `[~]` صحيحٌ في زمنِه ويبقى كما كُتب. **وههنا يُسجَّلُ اكتمالُ الأشطرِ الأربعةِ الباقيةِ التي بها صارَ الصندوقُ موحَّداً حرفاً:** لم يبقَ في المستودعِ موضعٌ يُغيِّرُ حالةً ثمَّ يُشعِرُ خارجَ معاملتِه. **والقلبُ إلى `[x]` وقعَ بعدَ خضرةِ وظائفِ CI الثلاثِ في التشغيلِ `34007820674` على `2f4ca1c` لا قبلَها (`ح-4`)** — ونصُّ البندِ لم يُمَسّ حرفاً (`ح-1`)، وملفُّ الدليلِ الجديدُ `docs/evidence/correctness/BUG-004-20260906-complete.md` **يُضافُ ولا يُلغي** سابقَه. **والأنواعُ الثمانيةُ في صندوقٍ واحدٍ** (`offer` · `dispute_resolution` من `resolve_support_ticket` · `negotiation_turn_opened`/`_closed`/`negotiation_agreed` من `register_unsubscribed_claim` و`rotate_negotiation_turn` و`settle_negotiation` · `wider_circle_opened` في الدورةِ الأولى وحدَها من `open_unsubscribed_cycle` · `no_driver_found` عندَ أوّلِ تسليمٍ وحدَه من `mark_escalation_delivered` · `order_cancelled` **صفٌّ لكلِّ سائقٍ** من `cancel_order_by_rider`)، **وأربعُ هجراتٍ** (`20260906010000`/`020000`/`030000`/`040000`) و`create or replace` يحفظُ `ACL` ولا مفتاحَ نُقِصَ من مُرجَعٍ منشورٍ. **والقراراتُ الحاكمةُ بحُجَجِها**: صفٌّ لكلِّ مُستلِمٍ لا للحادثةِ · منعُ التكرارِ بقيدٍ فريدٍ `(kind, dedup_key)` و`on conflict do nothing` · الإغناءُ لحظةَ الالتقاطِ في `claim_notification_delivery` · ولا حمولةَ حسّاسةً خامّاً. **والمسارُ المتزامنُ المُستبدَلُ حُذِفَ لا عُطِّلَ** (`DriverNotifier` و`matching.notifier` و`notifyCancelled` و`CancelNotifyTarget` ومزدوجاتُها). **والدليلُ الذي يطلبُه البندُ حرفاً قِيسَ لكلِّ نوعٍ**: أربعةُ ملفّاتِ تكاملٍ (٦+٧+٧+٦ حالاتٍ) يقيسُ كلٌّ منها الإيداعَ في المعاملةِ ورجوعَها بلا صفٍّ يتيمٍ ومنعَ الصفِّ الثاني وإعادةَ الفاشلِ وحدَه، وزادَ ملفُّ الإلغاءِ هجرَ صفِّ سائقٍ حُذِفَ سجلُّه بلا تعطيلِ غيرِه. **وإخفاقٌ حقيقيٌّ في الطريقِ يُسجَّلُ بلا تبييضٍ**: التشغيلُ `34006197622` على `7b52e2a` سقطَ بخمسِ حالاتٍ، وسببُه الجذريُّ واحدٌ — **مُفرِّغُ الاختباراتِ `drainNotificationOutbox` كان يبتلعُ إخفاقَ الدفعةِ ويعودُ صامتاً** — فأُصلِحَ السببُ: المُفرِّغُ يرفعُ الإخفاقَ بنصِّه، واختبارُ الإلغاءِ يُفرِّغُ بمعالجِ الإلغاءِ كما يفعلُ العاملُ، واختبارُ الدورةِ يربطُ معالجَي صاحبِ الطلبِ — **ولا اختبارٌ عُطِّلَ ولا حاجزٌ خُفِّفَ ولا مهلةٌ زِيدَت ولا عقدٌ غُيِّرَ ليمرَّ اختبارٌ**. **وبوّابةُ التغطيةِ سقطت** (٤٣٫٠٧٪ دونَ ٤٤٪) **فأُصلِحَت بتغطيةٍ حقيقيّةٍ** — اختبارُ وحدةٍ لقرارِ الهجرِ — فصارت **٤٤٫٦٠٪ (٦٧٣/١٥٠٩)**، **ولا أرضيّةَ خُفِّضَت**. **وحرزُ `skip-audit` رُفِعَ إلى ٦٧ ملفّاً و٦٠٠ حالةٍ رفعاً لا تخفيضاً**. **وما لم يُفتَح ولم يُغيَّرْ يُقرأُ حرفيّاً**: سقفُ المحاولاتِ الأقصى و`DLQ` والضغطُ العكسيُّ وسياسةُ الجدولةِ والإنتاجيّةُ الكلّيّةُ **لم تُفتَحْ ولم تُغيَّرْ**؛ **و`F6-03` يبقى `[ ]`** لأنَّ الصناديقَ المعاملاتيّةَ الثلاثةَ السابقةَ (`safety_incident_deliveries` · `subscription_notices` · صفوفُ البثِّ) **داخلَ معاملاتِها أصلاً فليست من عيبِ BUG-004**، وتوحيدُها إعادةُ تصميمٍ لا يأذنُ بها نطاقُ هذا البندِ؛ **ولا `DEC-11` ولا `F0`** · **ولا ADR منشورٌ عُدِّلَ (`ح-6`) ولا جديدٌ أُنشئَ** فالنصُّ نفسُه ينصُّ على الحلِّ ولا عقدَ مخطّطٍ غُيِّرَ (`check-schema-contract` بلا فارقٍ) · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: `typecheck`=0 · `biome`=0 · الحرّاسُ خروجُ `0` · **٢١١٦ ناجحاً · ٦٢١ متجاوَزاً · ٠ فاشلاً**؛ وبقاعدةٍ حقيقيّةٍ في CI **٥١٩ ناجحاً · ٠ فاشلاً · ١١ متجاوَزاً**. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — لا حِملَ قِيسَ ولا سلوكَ Bot API؛ المقيسُ سلوكُ القاعدةِ والعاملِ. **وCI أخضرُ بوظائفِه الثلاثِ في التشغيلِ `34007820674` على `2f4ca1c` و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيّةِ** (وسبقَه خضرةُ التشغيلِ `34007227717` على `dd08aee`). | `docs/evidence/correctness/BUG-004-20260906-complete.md` | مُنفَّذ · مُختبَر على قاعدةٍ حقيقيةٍ في CI | `2f4ca1c` |
| **تصحيحٌ مُلحَق الثاني والثلاثون — 2026-09-06 (وحدةُ `BUG-006` — حدُّ المعدّلِ على Redis يصيرُ ذرّيًّا بسكربتِ Lua واحدٍ، والبندُ يُغلَقُ بـ`[x]`)** | **`BUG-006` أُغلِقَ بـ`[x]` بعدَ تنفيذِ البندِ بحرفِه وقياسِ الدليلِ الذي يطلبُه على Redis حقيقيٍّ لا قبلَها (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفًا (`ح-1`).** **والعيبُ بحرفِهِ**: حدُّ المعدّلِ على Redis كان أمرَينِ منفصلَينِ عبرَ `command()` — `INCR` ثمَّ `EXPIRE` — **فيبقى المفتاحُ بلا انتهاءٍ بينَ الأمرَينِ** ويتجاوزُ العدُّ الحدَّ عندَ التزامنِ قبلَ ضبطِ المهلةِ؛ **وسياسةُ الفشلِ «مفتوحٌ» كانت ضمنيّةً لا معتمدةً بقرارٍ**. **والمُنفَّذُ سكربتُ Lua واحدٌ ذرّيٌّ عبرَ `EVAL`** على منفذِ Upstash REST نفسِه: نافذةٌ منزلقةٌ بـZSET تنفِّذُ `ZREMRANGEBYSCORE` (حذفُ المنتهية) + `ZCARD` (العدّ) + `ZADD` (الإضافة) + `PEXPIRE` (ضبطُ المهلةِ بالميلي ثانية) في خطوةٍ واحدةٍ على الخادمِ، فالعدُّ وضبطُ المهلةِ غيرُ قابلَينِ للفصلِ، ويعيدُ السكربتُ ثلاثيَّ `[مسموحٌ، متبقّي، مللي ثانية]` في جوابٍ واحد. **وسياسةُ الفشلِ صارت صريحةً ومعتمدةً بـADR جديدٍ** (`docs/adr/0055-redis-rate-limit-is-atomic-lua-and-fails-open.md`) مبنيّةً على مبدأِ ADR 0010 — حمايةٌ لا مصادقةٌ. **والدليلُ الذي يطلبُه البندُ حرفًا قِيسَ**: اختبارٌ توزيعيٌّ على Redis حقيقيٍّ في CI — عشرونَ طلبًا متزامنًا على نفسِ المفتاحِ (limit=5) ⇒ **خمسةٌ مسموحةٌ بالضبطِ وخمسةَ عشرَ مرفوضةٌ**، `PTTL > 0` (المهلةُ مضبوطةٌ في الخادمِ لا مفقودةٌ)، `ZCARD = 5` (لا تُضافُ العناصرُ المرفوضةُ). **واختبارُ الوحدةِ يُثبتُ أنّ كلَّ طلبٍ أمرُ `EVAL` واحدٌ فقط** (لا `INCR`/`EXPIRE`)، والجوابُ المشوَّهُ يُعَدُّ عجزًا، والفشلُ مفتوحٌ عندَ الخطأ. **و`nowMs` خيارٌ قابلٌ للحقنِ للقابليةِ للاختبار**، و`EVAL` مباشرةٌ بلا `EVALSHA` فلا حالةُ تحميلِ سكربتٍ تُكسرُ إعادةَ الإدخال. **ولا واجهةً جديدةً لـ`RedisClient`** ولا اسمَ ملفٍ غُيِّر (`fixed-window.ts` بقي كما هو في مواضعِ الاستيرادِ الأربعة)؛ التعليقُ الداخليُّ والـdocstring وحدهما يُشيرانِ إلى النافذةِ المنزلقةِ. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ غُيِّر** (`check-schema-contract` بلا فارقٍ: 83 دالّةً و23 جدولاً) · **ولا ADR منشورٌ عُدِّل (`ح-6`)** فالجديدُ 0055 وحدَه · والـmemory limiter وحدُّ المعدّلِ على مسارِ الويبهوك لم يُمَسّا · **ولا `R-17` ولا Socket.IO ولا توزيعُ Redis ولا `DEC-11` ولا `F0`** · ولا `BUG-002`/`BUG-007` مُسَّ في هذه الوحدةِ · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: `lint`=0 · `typecheck`=0 · اختباراتُ الوحدةِ **١٨٥٦ ناجحًا · ٠ فاشلاً · ٥٧٨٥ توكيدًا** · بوّابةُ التغطيةِ تمرُّ (المساراتُ التسعةُ فوقَ أرضيّاتِها) · **الحرّاسُ الـ١٨ خروجُ `0`** · marker جديد `rate-limit-atomic-under-concurrency` في `REQUIRED_CHECKS` (٩ أسطرٍ). **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه سلوكُ Upstash REST في CI لا حمولةُ الإنتاجِ ولا سلوكُ Bot API. **وCI أخضرُ في التشغيلِ `34047912228` على `5f082cb` بوظائفِه الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · **تكاملٌ على Redis حقيقيٍّ** الذي مرَّ فيه الاختبارُ الذرّيُّ التوزيعيُّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيّةِ لا من نصِّ السجلِّ ولا من الـbadge**. الدليلُ `docs/evidence/correctness/BUG-006-20260906.md`. | `docs/evidence/correctness/BUG-006-20260906.md` | مُنفَّذ · مُختبَر على Redis حقيقيٍّ في CI | `5f082cb` |
| **تصحيحٌ مُلحَق الثالث والثلاثون — 2026-09-06 (وحدةُ `BUG-007` — جلساتُ الحوارِ تحملُ مراجعةً وكتابةً شرطيّةً، والبندُ يُغلَقُ بـ`[x]`)** | **`BUG-007` أُغلِقَ بـ`[x]` بعدَ تنفيذِ البندِ بحرفِه وقياسِ الدليلِ الذي يطلبُه على Redis حقيقيٍّ لا قبلَها (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفًا (`ح-1`).** **والعيبُ بحرفِهِ**: مخزنُ الجلساتِ كان يكتبُ الحالةَ بأمرِ `SET` غيرِ مشروطٍ بلا مقارنةِ إصدارٍ ولا CAS، فإذا وردَ تحديثانِ متزامنانِ لنفسِ المستخدمِ كتبَ الثاني فوقَ الأوّلِ صامتاً فضاعَتْ خطوةُ الحوار. **والمُنفَّذُ مراجعةٌ رمزيّةٌ (Symbol `SESSION_REVISION`) على كائنِ الحالة + مغلفٌّ `{revision, state}` في Redis + CAS ذرّيٌّ بسكربتِ Lua `EVAL` واحد (يقرأُ المغلفَ ويُقارنُ المراجعةَ ويكتبُ الأحدثَ ويردُّها، أو يردُّ 0 عندَ التعارض) + إعادةُ محاولةٍ محدودةٍ (MAX_CAS_RETRIES=3) في محوّلَي السائقِ والراكبِ تُعيدُ تحميلَ الحالةِ وإعادةَ حسابِ الردودِ قبلَ إرسالِ أيِّ رسالة.** ولا توقيعُ `SessionStore` مسَّهُ تغيير، **ولا سطرٌ في أيِّ حوارٍ (`packages/application/bots/*-dialog.ts`) غُيِّر** — الحلُّ كلُّه في المخزنِ والمحوّل. **والهجرةُ صامتة**: القيمُ الإرثيةُ (نصٌّ خامٌّ للحالة) تُقاسُ كمراجعةٍ 0 فيُغلَّفُها أوّلُ حفظٍ بعدها. **والدليلُ الذي يطلبُه البندُ حرفًا قِيسَ**: على Redis حقيقيٍّ في CI — كتابتانِ متزامنتانِ بمراجعةٍ قديمةٍ ⇒ **الأولى تنجحُ والثانية تتعارضُ بلا فقدانِ خطوة**، والإرثُ يُهاجَرُ صامتاً، والمغلفُ يُحفَظُ بحقولِه كاملةً (بلا فقدِ `null`). **ومراجعةُ التكراريّة** أثبتتْ أنَّ كلَّ عمليّاتِ القاعدةِ داخلَ حدِّ إعادةِ المحاولة تكراريّةٌ (register=upsert · updateLocation=CAS بـWHERE · setPreferredArea=تحديثٌ مباشر · offers.reject=حراسةُ status · changeCity=RPC+clear) فإعادةُ المحاولةِ آمنة. **وخطآنِ جذرّيّانِ في الطريقِ عُولِجَا لا رُقِّعَا**: الأوّلُ فقدانُ حقولِ `null` في `cjson.decode`/`encode` (أُصلِحَ بدمجِ `stateJson` نصّاً في المغلف) اكتُشفَ على Redis حقيقيٍّ، والثاني تعارضُ إعادةِ التعيينِ (الحالاتُ الطازجةُ بلا مراجعة كانت تُعامَلُ كمراجعةٍ 0 فيتتعارضُ CAS الصارم) أُصلِحَ بالتمييزِ بين «حالةٍ محمَّلة» (CAS) و«حالةٍ طازجة» (كتابةٌ غيرُ مشروطة) عبرَ الحارسِ `-1`، ومزدوجُ `fakeRedis` في التكاملِ أُضيفَ له فرعُ `EVAL`. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ غُيِّر** (`check-schema-contract` بلا فارق) · **ولا ADR منشورٌ عُدِّل (`ح-6`) ولا جديدٌ أُنشئ** فالحلُّ تنفيذيٌّ لا قرارٌ معماريٌّ جديد · **ولا `R-17` ولا Socket.IO ولا توزيعُ Redis ولا `DEC-11` ولا `F0`** · ولا `BUG-002` مُسَّ في هذه الوحدة · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: `lint`=0 (على الملفّات المُغيَّرة) · `typecheck`=0 · `build:miniapp` ناجح · اختباراتُ الوحدةِ **٢١٣٢ ناجحاً · ٠ فاشلاً · ٦٢٢ متجاوَزاً** · الحرّاسُ ذوو الصلة خروجُ `0`. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — لا حِملَ قِيسَ ولا سلوكَ Bot API؛ المقيسُ سلوكُ Upstash REST في CI ووحدةُ المخزنِ والمحوّل. **وCI أخضرُ في التشغيلِ `34053661017` على `c646b6a` بوظائفِه الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · **تكاملٌ على Redis حقيقيٍّ**)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيّةِ** (وسبقَه فشلُ التشغيلَينِ `34053045238` و`34053404677` قُرئَ سببُهما الجذريُّ من السجلِّ الفعليِّ وأُصلِحَ). الدليلُ `docs/evidence/correctness/BUG-007-20260906.md`. | `docs/evidence/correctness/BUG-007-20260906.md` | مُنفَّذ · مُختبَر على Redis حقيقيٍّ في CI | `c646b6a` |
| **تصحيحٌ مُلحَق الرابع والثلاثون — 2026-09-06 (وحدةُ `SCL-002` — مخزنُ الجلساتِ في الذاكرةِ مرفوضٌ في الإنتاج، والبندُ يُغلَقُ بـ`[x]`)** | **`SCL-002` أُغلِقَ بـ`[x]` بعدَ تنفيذِ البندِ بحرفِه وقياسِ الدليلِ الذي يطلبُه لا قبلَها (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفًا (`ح-1`).** **والعيبُ بحرفِهِ**: مخزنُ الجلساتِ يفترضُ `SESSION_STORE=memory` حين يغيبُ المفتاحُ، ولا حارسَ يرفضُ `memory` في الإنتاج، فيعملُ النظامُ بذاكرةٍ صامتةً (جلساتٌ تُمحى بإعادةِ التشغيلِ، بلا مشاركةٍ بينَ النسخِ، بلا CAS) دونَ إنذار. **والمُنفَّذُ حارسُ إقلاعٍ واحدٌ في `packages/shared/config/index.ts`**: بعدَ التحقّقِ من صلاحيةِ `SESSION_STORE`، إن كان `env === "production"` و`rawSessionStore === "memory"` يُعادُ `InvalidEnvVarError` برسالةٍ تأمرُ بـ`SESSION_STORE=redis`، فيفشلُ الإقلاعُ في `tryLoadConfig` قبلَ أن يبدأَ الخادمُ ويخرجُ `index.ts` بـ`process.exit(1)` برسالةٍ معلنة. **و`redis` وحدهُ مسموحٌ في الإنتاج**، ومتغيراتُه (`UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`) مفروضةٌ أصلاً في `REQUIRED_ENV_KEYS` فلا فحصٌ إضافيٌّ يُحتاج. **و`memory` تبقى مسموحةً في التطويرِ والاختبار** فاختباراتُ الوحدةِ والتكاملِ التي تستعملُ الذاكرةَ لا تنكسر. **والدليلُ الذي يطلبُه البندُ قِيسَ**: الإنتاجُ + `memory` ⇒ فشلٌ · الإنتاجُ + غيابُ `SESSION_STORE` (يفترضُ memory) ⇒ فشلٌ · الإنتاجُ + `redis` ⇒ نجاحٌ · التطويرُ/الاختبارُ + `memory` ⇒ مسموح. **وخمسُ اختباراتٍ انكسرتْ أولَ دفعة** (ثلاثٌ في `config-loading.test.ts` تستعملُ `FULL` بلا `SESSION_STORE` معَ `NODE_ENV=production`، واثنتانِ في `single-instance-invariant.test.ts` تقلعانِ بـ`memory`+إنتاجٍ متوقِّعةً خطأَ طوبولوجيا) **فأُصلِحَتْ بفصلِ الاهتماماتِ لا بضعفِ التوكيدات**: `FULL` صار يضبطُ `redis` ليكونَ صالحاً في كلِّ بيئةٍ، واختبارُ رفضِ `memory` بقيَ يُؤكِّدُ الرفضَ، واختبارُ الطوبولوجيا صار يستعملُ `redis` ليصلَ إلى الفحصِ الذي يختبرُه — **ولا اختبارٌ عُطِّلَ ولا حاجزٌ خُفِّفَ ولا تأكيدٌ ضُعِّفَ**. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ غُيِّر** (`check-schema-contract` بلا فارق) · **ولا ADR منشورٌ عُدِّل (`ح-6`) ولا جديدٌ أُنشئ** فالحارسُ تنفيذيٌّ يُطبِّقُ نصَّ البندِ · **ولا `BUG-002` ولا الطابورُ (`REQ-03`) ولا الويبهوكُ ولا `container.ts` ولا `createUpstashRedis` مُسَّ** — التغييرُ كلُّه في طبقةِ الإعدادِ والاختبارات · **ولا `R-17` ولا Socket.IO ولا توزيعُ Redis ولا `DEC-11` ولا `F0`** · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp` ناجح · **٢١٣٨ ناجحاً · ٠ فاشلاً · ٦٢٢ متجاوَزاً** · الحرّاسُ الـ٢٢ خروجُ `0` (ومنها `check-session-storage-policy` و`check-env-drift` و`check-instance-invariant`). **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه سلوكُ `tryLoadConfig` في CI لا حمولةُ الإنتاج. **وCI أخضرُ في التشغيلِ `34054604942` على `619942f` بوظائفِه الثلاثِ** (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ)، **و`conclusion` قُرئ لكلِّ وظيفةٍ من الواجهةِ البرمجيّةِ**. الدليلُ `docs/evidence/correctness/SCL-002-20260906.md`. | `docs/evidence/correctness/SCL-002-20260906.md` | مُنفَّذ · مُختبَر في CI | `619942f` |
| **تصحيحٌ مُلحَق الخامس والثلاثون — 2026-09-06 (وحدةُ `SCL-003` — حدُّ المعدّلِ في الإنتاجِ صارَ Redis Lua مشتركًا، والبندُ يُغلَقُ بـ`[x]` كإغلاقٍ مُركَّب)** | **`SCL-003` أُغلِقَ بـ`[x]` كإغلاقٍ مُركَّبٍ لا جديدُ كودٍ فيه (`ح-1`): حاجزُهُ انحلَّ كنتيجةٍ مباشرةٍ لبندَينِ سابقَينِ، وقُرئَ من الكودِ الفعليِّ لا من الافتراض.** **والعيبُ بحرفِهِ**: حدُّ المعدّلِ في الذاكرةِ يعدُّ كلُّ نسخةٍ طلباتِها وحدَها فلا يكونُ الحدُّ مشتركًا بينَ النسخِ ولا ذرّيًّا على مستوى الخادمِ. **والمُركَّبُ الذي يُغلقه**: (١) `BUG-006` (مُغلَق بـ`[x]` في `5f082cb`) جعلَ `createRedisRateLimiter` ذرّيًّا بسكربتِ Lua واحدٍ عبرَ `EVAL` (نافذةٌ منزلقةٌ بـZSET) بدلَ `INCR`+`EXPIRE`، وسياسةُ الفشلِ «مفتوحٌ» صارت صريحةً بـADR 0055؛ (٢) `SCL-002` (مُغلَق بـ`[x]` في `619942f`) ألزمَ الإنتاجَ بـ`SESSION_STORE=redis` بفحصِ إقلاعٍ يفشلُ قبلَ أن يبدأَ الخادمُ. **والتدقيقُ الشاملُ للكودِ** (`rg -rn "createMemoryRateLimiter" apps packages`) أثبتَ أنَّ `createMemoryRateLimiter` يُستدعى في **موضعٍ إنتاجيٍّ واحدٍ وحيدٍ**: `apps/gateway/src/index.ts:229` داخلَ دالةِ `limiter()`، ولا يُreachableُ إلّا حينَ `rateRedis === null` (أي `config.sessionStore !== "redis"`)، ولا يوجدُ في المستودعِ كلِّهِ موضعٌ إنتاجيٌّ آخرَ يُنشئُ حدَّ معدّلٍ في الذاكرةِ خارجَ هذه الدالة. **والنتيجةُ المنطقيّة**: بما أنَّ `config.sessionStore` في الإنتاجِ لا يمكنُ أن يكونَ إلّا `"redis"` (SCL-002)، فـ`rateRedis` لا يمكنُ أن يكونَ null في الإنتاج، فدالةُ `limiter()` لا تُعيدُ إلّا `createRedisRateLimiter` (Redis Lua المشترك من BUG-006) في الإنتاج. **و`createMemoryRateLimiter` يبقى قائمًا للتطويرِ والاختبارِ فقط** (نسخةٌ واحدةٌ، في الذاكرةِ، بلا عبورِ عمليةٍ) ولا يخالفُ SCL-003 لأنَّ البندَ يتحدّثُ عن الإنتاج. **ولا حاجةَ إلى كودٍ جديدٍ أو حارسٍ إضافيٍّ**: حارسُ الإقلاعِ في SCL-002 يضمنُ بالفعلِ أنَّ الإنتاجَ لا يقلعُ بـmemory، فلا يُreachableُ مسارُ `createMemoryRateLimiter` في الإنتاج، وإضافةُ حارسٍ ثانٍ تكرارٌ لما يضمنُهُ حارسُ الإعداداتِ قبلَه؛ وإزالةُ `createMemoryRateLimiter` تكسرُ بيئةَ التطويرِ والاختبارِ. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ ولا ADR جديدٌ (`ح-6`)** — الإغلاقُ مُركَّبٌ من بندَينِ منفَّذَينِ سابقًا · **ولا `R-17` ولا Socket.IO ولا توزيعُ Redis ولا `DEC-11` ولا `F0` ولا `BUG-002`/`REQ-03` مُسَّ** · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: لا تغييرَ في الكودِ الإنتاجيِّ في هذا الإغلاق (الاختباراتُ خضراءٌ في `619942f`/`667c01f`)، والتدقيقُ الشاملُ (`rg`) على الكودِ الإنتاجيِّ يؤكّدُ انفرادَ `index.ts:limiter()` بالمسار. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — هذا الإغلاقُ توثيقيٌّ يُقرُّ بنتيجةِ بندَينِ سابقَينِ لا قياسُ حملٍ جديد. **وCI أخضرُ في التشغيلِ `34054852982` على `667c01f` بوظائفِه الثلاثِ**، وهو نفسُ الشجرةِ التي يُضافُ إليها هذا الدليل. الدليلُ `docs/evidence/correctness/SCL-003-20260906.md`. | `docs/evidence/correctness/SCL-003-20260906.md` | مُنفَّذ · مُختبَر في CI | `667c01f` |
| **تصحيحٌ مُلحَق السادس والثلاثون — 2026-09-07 (وحدةُ `F5-03` — Redis إلزاميٌّ في الإنتاج بفحصِ إقلاعٍ يفشل، والبندُ يُغلَقُ بـ`[x]` كإغلاقٍ مُركَّب)** | **`F5-03` أُغلِقَ بـ`[x]` كإغلاقٍ مُركَّبٍ لا جديدُ كودٍ فيه (`ح-1`): متطلبه النصّيَّ مُنفَّذٌ بالكاملِ عبر `SCL-002` (`[x]`)، والقلبُ جاءَ بعدَ التحقّقِ من الكودِ الفعليِّ والاختباراتِ القائمةِ، لا بعدَ تنفيذٍ جديد.** **ونصُّ البندِ**: «Redis إلزاميٌّ في الإنتاج: فحصُ إقلاعٍ يفشلُ إذا كان `SESSION_STORE=memory`». **والدليلُ من الكودِ الفعليِّ**: `packages/shared/config/index.ts` (السطر 491) داخلَ `tryLoadConfig` — `if (env === "production" && rawSessionStore === "memory")` يُعيدُ `InvalidEnvVarError("SESSION_STORE", ...)` قبلَ تجميعِ `AppConfig`، فيخرجُ `apps/gateway/src/index.ts` بـ`process.exit(1)` برسالةٍ معلنةٍ قبلَ أن يبدأَ الخادمُ. **ومطابقةُ المتطلبِ بالنصّ**: «Redis إلزاميٌّ في الإنتاج» ← الإنتاجُ لا يقلعُ إلّا بـ`redis` و`memory` مرفوضٌ · «فحصُ إقلاعٍ يفشلُ» ← `tryLoadConfig` يُعيدُ `InvalidEnvVarError` · «إذا كان `SESSION_STORE=memory`» ← الشرطُ يشملُ القيمةَ الصريحةَ والغيابَ (الافتراضُ `memory`). **والاختباراتُ القائمةُ** (`tests/unit/config-session-store.test.ts` من SCL-002): الإنتاجُ+memory⇒فشلٌ · الإنتاجُ+غيابٌ⇒فشلٌ · الإنتاجُ+redis⇒نجاحٌ · التطويرُ/الاختبارُ+memory⇒مسموحٌ — كلُّها خضراءُ في `34054604942`. **ولا حاجةَ إلى كودٍ جديدٍ أو حارسٍ إضافيٍّ**: حارسُ الإقلاعِ في `tryLoadConfig` يضمنُ المتطلَّبَ بالكامل، وإضافةُ فحصٍ ثانٍ تكرارٌ. **وهذا إغلاقٌ لبندِ F5-03 وحدَه لا فتحٌ لبوابةِ المرحلةِ F5** — بوابةُ F5 تتطلّبُ عدّةَ gateways وworkers وقتلَ مثيلٍ أثناءَ رحلةٍ بلا فقدِ خطوةٍ (SCL-004/005/007/008 + فصلُ العاملِ + ≥3 مثيلات)، وكلُّها `[ ]` بعد، **فبوابةُ F5 تبقى مغلقةً**. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ ولا ADR جديدٌ (`ح-6`)** — الإغلاقُ مُركَّبٌ من `SCL-002` المنفَّذِ سابقًا · **ولا `R-17` ولا Socket.IO ولا توزيعُ Redis ولا `DEC-11` ولا `F0` ولا `BUG-002`/`REQ-03` مُسَّ** · **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: لا تغييرَ في الكودِ الإنتاجيِّ في هذا الإغلاق (الاختباراتُ خضراءٌ في `0821ece`)، وحارسُ `tryLoadConfig` مُتحقَّقٌ منه في الكودِ. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه سلوكُ `tryLoadConfig` في CI لا حمولةُ الإنتاج. **وCI أخضرُ في التشغيلِ `34055155911` على `0821ece` بوظائفِه الثلاثِ**، وهو الشجرةُ التي يُضافُ إليها هذا الدليل. الدليلُ `docs/evidence/correctness/F5-03-20260907.md`. | `docs/evidence/correctness/F5-03-20260907.md` | مُنفَّذ · مُختبَر في CI | `0821ece` |
| **تصحيحٌ مُلحَق السابع والثلاثون — 2026-09-07 (وحدةُ `F5-05` / `CAP-008` — تصريفٌ رشيقٌ عند `SIGTERM` و`/ready` متوازٍ بمهلات، والبندان يُغلَقان بـ`[x]`)** | **`F5-05` و`CAP-008` أُغلِقا بـ`[x]` بعدَ تنفيذِ البندينِ بحرفِهما وقياسِ الدليلِ الذي يطلبانِه لا قبلَهما (`ح-4`)، ونصُّ البندينِ لم يُمَسّ حرفًا (`ح-1`).** **والعيبُ بحرفِهِ**: لا تصريفٌ رشيقٌ عندَ `SIGTERM` (إغلاقٌ فوريٌّ يقطعُ الجاري)، و`/ready` يفحصُ تسلسليّاً بلا مهلاتٍ (فحصٌ بطيءٌ أو معلَّقٌ يُعلِّقُ الجاهزيةَ كلَّها). **والمُنفَّذُ ثلاثةُ ملفّاتٍ:** (١) `apps/gateway/src/lifecycle.ts` (جديد) — `createLifecycle` بحقنِ الوقتِ (`now`/`sleep`)، إعلانُ `isDraining` أوّلاً، انتظارُ فراغِ الجاري (`inFlight()===0`) ضمنَ `graceMs` (10ث)، ثمّ `forceClose` عندَ انقضاءِ المهلة، ثمّ `close` ترتيبيّاً، **idempotent**، والخروجُ ليسَ في الوحدةِ (مسؤوليّةُ المنادي)؛ (٢) `apps/gateway/src/routes/health.ts` — `/ready` يفحصُ `isDraining` أوّلاً (503 فوراً بلا فحص)، ويُشغّلُ الفحوصَ **بالتوازي** (`Promise.all`) مع `runProbe` تُسابقُ `Promise.race` بينَ الفحصِ ومهلةٍ `probeTimeoutMs` (2000ملّي) لكلِّ فحص، فلا يُنتظرُ إلى الأبد؛ (٣) `apps/gateway/src/index.ts` — `Bun.serve` فعليٌّ مع عدّادِ `inFlightRequests` (لا يعدُّ `/health` و`/ready`)، و**بوّابةُ تصريفٍ** في `fetch`: عندَ `isDraining` يُسمَحُ لـ`/health` و`/ready` فقط (حتى يرى المُوجِّهُ «مُصرِّف») ويُرفَضُ ما عداهما بـ`503 {status:"draining"}` قبلَ بلوغِ التطبيق، و`process.on(SIGTERM/SIGINT)` → `lifecycle.requestShutdown(signal).then(()=>process.exit(0))`. **ومطابقةُ المتطلَّبِ بالنصّ**: «توقّف عن قبول الجديد» ← بوّابةُ `fetch` ترفضُ غيرَ الصحيِّ بـ503 · «أنهِ الجاري بمهلة» ← `lifecycle` ينتظرُ `inFlight===0` ضمنَ `graceMs` · «أغلق الاتصالات» ← `close` ترتيبيّاً + `forceClose` قَسريٌّ · «`/ready` متوازٍ بمهلة قصوى» ← `Promise.all` + `Promise.race` على `probeTimeoutMs`. **والدليلُ ثلاثةَ عشرَ اختباراً في ثلاثةِ ملفّاتِ اختبار**: `tests/unit/lifecycle.test.ts` (5: حالةُ التصريف، فراغُ الجاري، الإغلاقُ القسريُّ عندَ المهلة، idempotent، فشلُ الإغلاقِ لا يُسقطُ النتيجة) · `tests/unit/health-ready.test.ts` (5: **التوازي** — فحصانِ بطيئانِ 40ملّي ينتهيانِ في 43ملّي < 80ملّي مع تداخلٍ مُثبَت، **المهلة** — فحصٌ معلَّقٌ يُعدُّ فاشلاً في 30ملّي بلا تعليق، التصريفُ 503 بلا فحص، التصنيفُ، التبعيّةُ غيرُ الحرجة) · `tests/unit/gateway-graceful-shutdown.test.ts` (3: **عمليّةٌ فرعيّةٌ حقيقيّةٌ** تُطلقُ البوابةَ ببيئةٍ وهميّة، تنتظرُ `/health`، ثمّ `SIGTERM` ⇒ خروجٌ برمزِ صفر؛ وأثناءَ التصريفِ `/ready` يردُّ 503 «draining»؛ و**بوّابةُ الجديد**: مسارٌ غيرُ صحيٍّ يردُّ 404 قبلَ الإشارة و503 «draining» بعدها — برهانُ رفضِ الجديدِ لا صحّةَ الفحصِ وحده). **وقرارٌ تصميميٌّ مُعلَن**: تُركَ الخادمُ يستقبلُ أثناءَ التصريفِ (لا `server.stop(false)` فوراً) حتى يُجيبَ `/ready` بـ«مُصرِّف»، والجديدُ يُرفَضُ عندَ بابِ `fetch` لا TCP، فيرى المُوجِّهُ `503 draining` ويوقفُ التوجيهَ بينما تُنهي الجاريَّ والمواردُ. **وهذا إغلاقٌ لِـ`F5-05`/`CAP-008` وحدَهما لا فتحٌ لبوابةِ المرحلةِ F5** — بوابةُ F5 تتطلّبُ عدّةَ gateways وworkers وقتلَ مثيلٍ أثناءَ رحلةٍ بلا فقدِ خطوة (SCL-004/005/007/008 + فصلُ العاملِ + ≥3 مثيلات) وكلُّها `[ ]` بعد، **فبوابةُ F5 تبقى مغلقةً**. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه سلوكُ التصريفِ في اختبارِ عمليّةٍ فرعيّة لا حمولةُ الإنتاجِ. **ولا هجرةَ قاعدةٍ ولا عقدَ مخطّطٍ ولا ADR جديدٌ (`ح-6`)** — التغييرُ كلُّه في طبقةِ البوابة. **ولا `BUG-002` ولا الطابورُ (`REQ-03`) ولا `SCL-001` ولا `F5-01`/`F5-02`/`F5-04` مُسَّ** (`ح-1`). **ولا `force-push` ولا تاريخٌ أُعيدَ كتابتُه**. محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **٢١ حارساً كلُّهم خروجُ `0`** · `test`=**2151 ناجحاً · 622 متجاوَز · 0 فاشل** (٢٧٧٣ اختباراً في ٢١٠ ملفّاً) · `check:coverage`=0 فوقَ أرضيّاتِه التسع. **وقيدُ بيئةٍ محلّيٌّ مُعلَن**: `verify-container-build.sh` يسقطُ لتباينِ إصدارِ `bun` (1.3.14 في `docker/Dockerfile.*` مقابل 1.4.2 محلّياً) — قيدٌ سابقٌ لا علاقةَ لتغييراتِ هذا الإغلاقِ به. **وGitHub Actions (الحكمُ النهائيُّ): التشغيلُ `34065347798` على `628ae97` اكتملَ بنتيجةِ `success` (2m5s).** الدليلُ `docs/evidence/correctness/F5-05-20260907.md`. | `docs/evidence/correctness/F5-05-20260907.md` | مُنفَّذ · مُختبَر في CI | `8880333` |
| **تصحيحٌ مُلحَق الثامن والثلاثون — 2026-09-07 (وحدةُ `CAP-004` — اشتقاقُ `prepare` من رابطِ الاتصالِ + قرارُ pooler + ميزانيّةُ الاتصالاتِ، والبندُ يُغلَقُ بـ`[x]`)** | **`CAP-004` أُغلِقَ بـ`[x]` بعدَ تنفيذِ البندِ بحرفِه وقياسِ الدليلِ الذي يطلبهُ لا قبلَهُ (`ح-4`)، ونصُّ البندِ لم يُمَسّ حرفًا (`ح-1`).** **والعيبُ بحرفِهِ**: `createSql` كان يُمرِّرُ `prepare: options.prepare ?? true`، فالبوابةُ `apps/gateway/src/container.ts:324` تستدعي `createSql({ connectionString })` **بلا `prepare`** فترثُ الافتراضيَّ `true` — فيتعارضُ مع Supabase transaction pooler (المنفذُ 6543) الذي لا يدعمُ الجُملَ المُحضَّرة. والعاملُ كان ضبطَ `prepare: false` صراحةً فحُمِيَ، **لكنّ البوابةَ لم تضبطْه**. **والعيبُ جذرُهُ معماريٌّ**: ضبطُ `prepare` كان مسؤوليّةَ كلِّ مستدعٍ، فمتى ظهرَ مستدعٍ جديدٌ دونَه عادَ العيبُ. **والمُنفَّذُ ملفٌّ إنتاجيٌّ واحدٌ + ADR + اختبارُ وحدة:** (١) `packages/infrastructure/db/client.ts` — دالتانِ مركزيّتانِ: `detectDbPoolerMode(connectionString)` تُرجِعُ `"transaction"` (المنفذُ 6543 أو `?pgbouncer=true`) · `"session"` (pooler.supabase المنفذُ 5432) · `"direct"` (اتصالٌ مباشرٌ أو محليٌّ) · `"unknown"` (رابطٌ غيرُ قابلٍ للتحليلِ)؛ و`resolvePrepare(options)` — **قاعدةُ السلامةِ**: transaction ⇒ `false` حتماً حتى لو طُلِبَ `prepare: true` صراحةً، لأنّ transaction pooling لا يدعمُ الجُملَ المُحضَّرة فيعطّلُ الإعدادَ آمناً قبلَ أن يفشلَ في وقتِ التشغيلِ؛ ولغيرِ transaction يُحترَمُ التمريرُ الصريحُ أو يُرجَعُ الافتراضيُّ `true`. وصارَ `createSql` يستخدمُ `resolvePrepare` مركزيّاً بدلَ `options.prepare ?? true`، **فحُمِيَ كلُّ مستدعٍ تلقائيّاً** — البوابةُ التي كانت تنسى `prepare: false` صارَت تأخذُه من السياسة، والعاملُ يُحقَّقُ له آليّاً، ولا يعودُ العيبُ في مستدعٍ جديد؛ (٢) `docs/adr/0056-supabase-transaction-pooler-and-prepared-statements.md` — يوثِّقُ القرارَ: transaction pooler هو مسارُ الإنتاجِ المعتمد فتُعطَّلُ الجُملُ المُحضَّرةُ آليّاً عندَه، والسياسةُ المركزيّةُ تُشتقُّ من الرابطِ لا من كلِّ مستدعٍ، ونموذجُ الميزانيّةِ `5N + 10M` (مُقاسٌ من الثوابتِ الفعليّةِ: بوابة `max:5`، عامل `max:5 + max:5` حيثُ `MAX_JOB_CONCURRENCY=4`)؛ (٣) `tests/unit/db-pooler-policy.test.ts` — **18 توكيداً** تُثبتُ قاعدةَ السلامةِ: transaction pooler ⇒ `prepare: false` حتماً حتى مع `prepare: true` صريح. **ومطابقةُ المتطلَّبِ بالنصّ**: «نموذجُ ميزانيّةِ اتصالاتٍ معلنٌ» ← ADR 0056 §٣-ج · «قرارُ pooler موثَّقٌ بـADR (transaction vs session)» ← ADR 0056 §٢-٣ · «تعطيلُ الجُملِ المُحضَّرةِ عندَ pooling المعاملات» ← `resolvePrepare`: transaction ⇒ `false` حتماً · «`packages/infrastructure/db/client.ts:8,20-21`» ← التعديلُ في الملفِّ نفسِه. **وهذا إغلاقٌ لِـ`CAP-004` وحدَه لا فتحٌ لبوابةِ المرحلةِ F5** — بوابةُ F5 تبقى مغلقة. **ولا `مَقيس` ولا `مُثبَت` ولا `VERIFIED`** — المُتحقَّقُ منه منطقُ الاشتقاقِ في اختبارِ وحدة، لا throughputُ الإنتاجِ ولا latencyُ القاعدةِ. **ولا هجرةَ قاعدةٍ ولا schema ولا `numInstances` يُرفَعُ ولا عاملٌ يُنقَلُ** — نموذجُ الميزانيّةِ مُعلَنٌ من الثوابتِ القائمة لا مُغيَّر. **ولا `BUG-002` ولا `F5-01`/`F5-02`/`F5-04` ولا أيُّ بندٍ آخر مُسَّ، ولا ADR منشورٌ عُدِّل (`ح-6`).** محليّاً: `lint`=0 · `typecheck`=0 · `build:miniapp`=0 · **٢١ حارساً كلُّهم خروجُ `0`** · `test`=**2169 ناجحاً · 622 متجاوَز · 0 فاشل** (٢٧٩١ اختباراً في ٢١١ ملفّاً) · `check:coverage`=0. قيدُ بيئةٍ محلّيٌّ مُعلَن**: `verify-container-build.sh` يسقطُ لتباينِ إصدارِ `bun` (1.3.14 في `docker/Dockerfile.*` مقابل 1.4.2 محلّياً) — قيدٌ سابقٌ لا علاقةَ لتغييراتِ هذا الإغلاقِ به. **وGitHub Actions (الحكمُ النهائيُّ): التشغيلُ `34072012637` على `a1b2e3d` اكتملَ بنتيجةِ `success`.** الدليلُ `docs/evidence/correctness/CAP-004-20260907.md`. | `docs/evidence/correctness/CAP-004-20260907.md` | مُنفَّذ · مُختبَر في CI | `a1b2e3d` |
| **تصحيحٌ مُلحَق التاسع والثلاثون — 2026-09-07 (وحدةُ حوكمةٍ — ملحقٌ حاكمٌ ثانٍ + `ADR 0057` لا تنفيذٌ)** | **أُضيف إلى `docs/MASTER_DIRECTIVE.md` ملحقٌ حاكمٌ ثانٍ (2026-09-07) يُقرُّ أنَّ «حاملَ الحمولةِ المقيدَ» (`bound payload carrier`) عضوٌ ثانٍ في الصنفِ المغلقِ `domain-ingress receipt` — يجوز له — كالإيصالِ — أن لا يحمل `city_id` لحظةَ الإنشاءِ، لأنَّه يُنشَأ في لحظةِ الاستلامِ نفسِها قبلَ معرفةِ مدينةٍ، ولأنَّ [ADR 0054](adr/0054-telegram-webhook-durable-ingest-and-dedup.md) §٧/١ منعَ تخزينَ التحديثِ الخامِّ في الإيصالِ، فالحاملُ هو موضعُ الحمولةِ الخارجيُّ الذي يتيحُ للعاملِ معالجةَ التحديثِ لاحقاً بعدَ «ACK سريع».** **والقاعدةُ 0.4 تبقى مطلقةً خارجَ عضوَي الصنفِ** (الإيصالُ + الحاملُ): لا تُخفَّف لجدولِ مجالٍ ولا لصندوقِ صادرٍ عامٍّ (`notification_outbox` يحملُ `city_id` صادقاً لأنَّه يُودَعُ في معاملةِ تغييرِ حالةٍ تُعرفُ فيها المدينةُ)، و`RLS` لا تسقط، ولا يُملأ `city_id` بقيمةٍ مُلفَّقةٍ، **والصنفُ يبقى مغلقًا بعضوَّينِ، فإضافةُ ثالثٍ تتطلّب ملحقًا حاكمًا جديداً**. **وأُنشئَ `ADR 0057`** (`docs/adr/0057-telegram-ingress-bound-payload-carrier-and-worker.md`) **قرارًا معماريًّا فقط** يُقرِّرُ: جدولُ `telegram_update_jobs` منفصلٌ عن الإيصالِ ومقيدٌ إليه ١:١ بمفتاحِ `(bot, update_id)`، والحاملُ يخزِّنُ تحديثَ grammY كاملاً كـ`jsonb` (لأنَّ إعادةَ بناءِ التحديثِ من معرّفاتٍ وحدَها غيرُ ممكنةٍ)، والإيداعُ يُجرى في معاملةٍ ذرّيّةٍ واحدةٍ معَ الالتقاطِ، و`handler.handle` ينتقلُ إلى عاملٍ منفصلٍ يلتقطُ بـ`claim`/`lease`/`finish`/`reclaim` كنمطِ `notification_outbox` القائم، والاستبقاءُ قيمةٌ تشغيليّةٌ تقنيّةٌ لا في `platform_settings`، ولا أسرارَ ولا توكنَ في الحمولةِ. **وهذا تغييرٌ حوكميٌّ وقرارٌ معماريٌّ فقط**: لا هجرةَ ولا سطرَ شيفرةٍ ولا اختبارَ ولا ملفَّ دليلٍ، و`scripts/check-migrations.ts` و`apps/gateway/src/routes/telegram-webhook.ts` و`packages/shared/config/domain-ingress.ts` و`docs/ROADMAP-MASTER.md` لم تُمَسَّ حرفاً. **ولم يُغلَق بندٌ ولا قُلِبَ إلى `[x]`**: `BUG-002` و`F6-01` و`F6-02` و`SCL-001` و`CAP-001` تبقى `[ ]` كما هي، فلا موضعَ لـ`ح-4` ولا لسلّمِ الأدلةِ. **ولا قياسَ سعةٍ ولا أداءٍ ههنا فلا `مَقيس` ولا `مُثبَت`** (`ح-5`) |
| **تصحيحٌ مُلحَق الأربعون — 2026-09-07 (وحدةُ `BUG-002`/`F6-02` — حاملُ الحمولةِ المقيدُ والعاملُ المنفصلُ — الجولةُ الخضراءُ 1/3)** | **`BUG-002` و`F6-02` يبقيانِ `[ ]` — نُفِّذَ الشطرُ الذي كان مفقودًا (الطابورُ والعاملُ المنفصلُ) لكنّ القلبَ معلَّقٌ على ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`)، ولم تكتملْ بعدُ سوى الجولةُ الأولى.** **والعيبُ بحرفِهِ**: كان `handler.handle(bot, update)` يُنادى **داخلَ طلبِ `HTTP`** بعدَ الإيصالِ (`apps/gateway/src/routes/telegram-webhook.ts:298`)، فزمنُ المعالجةِ محجوزٌ في استجابةِ الويبهوكِ. **والمُنفَّذُ هجرةٌ واحدةٌ + أربعُ دوالَّ + خمسةُ ملفّاتٍ كودٍ + اختباراتٌ**: (١) `supabase/migrations/20260907090000_telegram_update_jobs.sql` — جدولُ `telegram_update_jobs` بمفتاحٍ `(bot, update_id)` مقيدٍ ١:١ إلى الإيصالِ بـ`FK on delete cascade`، و`payload jsonb` (تحديثُ grammY كاملٌ)، وحالاتٌ أربعٌ (`pending`/`claimed`/`done`/`dead`)، وقيدانِ (`done_is_sealed`/`dead_is_sealed`)، و`next_attempt_at`، و`RLS` + `revoke` + `grant service_role`، والتصريحُ الحرفيُّ `-- domain-ingress-receipt: telegram_update_jobs`؛ (٢) الدوالُّ الذرّيّةُ الأربعُ: `claim_and_enqueue_telegram_update` (إيداعٌ والتقاطٌ في معاملةٍ واحدةٍ بـ`on conflict do nothing` + كشفِ `xmax`، يَختُمُ الحاملَ والإيصالَ برمزِ حجزٍ واحد) · `claim_telegram_update_job` (يُسترجعُ المهجورُ **قبلَ** الالتقاطِ: المُعادُ⇒`pending` والمُنهَكُ⇒`dead`+`failed` عبرَ `CTE returning`، ثمَّ `for update skip locked`+`gen_random_uuid()`) · `finish_telegram_update_job` (سبعةُ وسطاءَ، `select for update` يَمنعُ تَزاحُمَ الالتقاطِ/الختمِ، والتسليمُ مشروطٌ بـ`claim_token` في الحاملِ والإيصالِ معاً، و`next_attempt_at = now()+retry_delay` للفشلِ القابلِ لإعادةِ المحاولةِ) · `abandon_telegram_update_job` (موتٌ صريحٌ مشروطٌ بـ`claim_token`)؛ (٣) `packages/infrastructure/db/telegram-update-queue.ts` (المنفذُ، `finish` يُمرِّرُ `retryDelay` وسيطًا سابعًا) · `apps/gateway/src/routes/update-intake.ts` (`TelegramUpdateEnqueuer` و`claimAndEnqueue`، **تصحيحٌ حاسمٌ**: `${sql.json(payload as never)}` لا `${JSON.stringify(payload)}::jsonb`) · `apps/gateway/src/routes/telegram-webhook.ts` (مسارُ الاستلامِ صار `claimAndEnqueue`+`ACK`، **لا `handler.handle` داخلَ الطلبِ**) · `apps/gateway/src/background/telegram-update-drainer.ts` (درينرٌ بـ`setInterval` ٥٠٠ملّي وممانعِ تَزاحُمٍ و`stop()` نظيف) · `apps/gateway/src/index.ts` (توصيلُ الدرينرِ والتصريفُ قبلَ `embeddedWorker`/`container`). **والقيودُ التي عُولجَت**: إعادةُ التسليمِ المكرَّرةُ لا تُعيدُ إنشاءَ حاملٍ قائمٍ (`on conflict do nothing`)؛ و`finish` يَختُمُ الحاملَ والإيصالَ ذرّيًّا برمزِ حجزِ الحاملِ لا الإيصالِ؛ والاسترجاعُ المهجورُ يُعالجُ المحاولاتِ المنهَكةَ (لا تُتركُ عالقةً `pending`). **ولا `city_id`** بإذنِ الملحقِ الحاكمِ الثاني 2026-09-07 (الحاملُ عضوٌ ثانٍ في الصنفِ المغلقِ `domain-ingress receipt`)، **و`RLS` لم تسقط**. **والدليلُ**: **٨ مجموعاتِ تكاملٍ** على PostgreSQL 18 حقيقيّةٍ (`tests/integration/telegram-durable-intake.test.ts` — الويبهوكُ لا يُنادي `handler.handle`، والدرينرُ هو من يُعالجُ، و`skip locked` يَمنعُ الحجزَ المزدوجَ، والمهلةُ المهجورةُ مع المحاولاتِ المنهَكةِ⇒`dead`) + **٨ اختباراتِ وحدةٍ** للمنفذِ بمحاكاةِ `Sql` (`tests/unit/telegram-update-queue.test.ts`) رفعتْ تغطيةَ مجموعةِ «المخطّطُ وتفعيلُ المدن» من ٥٥٫٢١% إلى **٦٨٫٥١%** فوقَ الأرضيّةِ ٦٠. **والحُرّاسُ**: `check-migrations` (٣٩ جدولاً، عضوانِ مُعفيانِ) · `check-schema-contract` (٨٧ دالّةً و٢٣ جدولاً) · `biome` · `tsc` · بوّابةُ التغطيةِ خروجُ `0`. **ولا أرضيّةَ خُفِّضَت ولا حارسٌ أُسكِتَ ولا استثناءٌ أُضيفَ.** **وGitHub Actions (الحكمُ النهائيُّ): التشغيلُ `34127610123` على `c3d6e71` اكتملَ بنتيجةِ `success` في الوظائفِ الثلاثِ (`verify` · تكاملٌ على PostgreSQL حقيقيٍّ · تكاملٌ على Redis حقيقيٍّ) — الجولةُ الخضراءُ 1/3.** **والقلبُ معلَّقٌ**: `BUG-002`/`F6-02` يبقيانِ `[ ]` حتى ثلاثِ جولاتٍ خضراءَ متتاليةٍ (`ح-4`)؛ الجولةُ 2/3 بعدَ دفعِ التوثيقِ، و3/3 بعدَ الدمجِ إلى `main`، **عندئذٍ فقط** يُقلَبانِ إلى `[x]`. **ولا نصُّ بندٍ عُدِّل (`ح-1`) ولا `ADR` منشورٌ عُدِّل (`ح-6`)** — `ADR 0057` قائمٌ من جولةِ الحوكمةِ والتنفيذُ مطابقٌ له. **ولا `مَقيس` ولا `مُثبَت`** — المُتحقَّقُ منه سلوكُ الإيداعِ والالتقاطِ في اختباراتِ تكاملٍ على قاعدةٍ حقيقيّةٍ، لا throughputُ الإنتاجِ ولا latencyُ الويبهوكِ. الدليلُ `docs/evidence/correctness/BUG-002-20260907.md`. | `docs/evidence/correctness/BUG-002-20260907.md` | مُنفَّذ · مُختبَر في CI — 1/3 | `c3d6e71` |
| **تصحيحٌ مُلحَق الحادي والأربعون — 2026-09-07 (إغلاقُ `BUG-002`/`F6-02` — اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`BUG-002` و`F6-02` قُلِبا إلى `[x]` في `docs/ROADMAP-MASTER.md` §11-أ و§11-و بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) لا قبلَها، ونصُّ البندينِ لم يُمَسَّ حرفًا (`ح-1`) — عمودُ الحالةِ وحدَه قُلِبَ من `[ ]` إلى `[x]`.** **وما نُفِّذ في الملحقِ الأربعينَ قائمٌ ولم يُمَسَّ ولم يُعَد**: الهجرةُ `20260907090000_telegram_update_jobs.sql` والدوالُّ الأربعُ والمنفذُ والدرينرُ والمسارُ والاختباراتُ كلُّها باقيةٌ كما هي. **والجولاتُ الثلاثُ المتتاليةُ كلُّ وظيفةٍ فيها `success` (قُرِئَ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ):** (1/3) التشغيلُ `34127610123` على `c3d6e71` في الفرعِ `feat/telegram-update-jobs-durable-intake` · (2/3) التشغيلُ `34128261550` على `311bed7` في الفرعِ نفسه · (3/3) التشغيلُ `34128675257` على `26eeb41` في `main` (دمجُ PR #16). والوظائفُ في الثلاثةِ كلِّها: `verify` و`تكاملٌ على PostgreSQL حقيقيٍّ` و`تكاملٌ على Redis حقيقيٍّ` — كلُّها `success`. **وزيادةً في الإثباتِ** مرَّتْ جولتانِ خضراوانِ إضافيّتانِ (`34127613929` و`34128265983`) فائضٌ عن المطلوبِ. **وهذا الـ commitُ تغييرُ حالةٍ وتوثيقٌ وحدَه**: لا شيفرةَ ولا هجرةَ ولا اختبارَ ولا حاجزٌ — عمودُ الحالةِ في الخارطةِ وملفُ الدليلِ `docs/evidence/correctness/BUG-002-20260907.md` (§٧ و§٨ و§٩ والترويسةُ حُدِّثَت من «1/3» إلى الإغلاقِ) وحدَهما. **وما لم يُقلَب يُقرأُ حرفيًّا**: **`CAP-001` و`F6-01` يبقيانِ `[ ]`** — هذه الوحدةُ تُغلقُ البندينِ المُسمَّينِ في الملحقِ 40 وحدَهما؛ و`CAP-001`/`F6-01` لهما عمودُ حلٍّ ونصٌّ وعبءُ دليلٍ خاصٌّ، ولم يُعلِنِ الملحقُ 40 أنَّهما «مُنفَّذانِ بانتظارِ CI»، فلا يُقلبانِ هنا ويُتركانِ لتدقيقِ إغلاقٍ لاحقٍ منفصلٍ · **`SCL-001` يبقى `[ ]`** (dedup موزَّعٌ في Redis) · **`R-17`/Socket.IO/`DEC-11`/`F0`/`DEC-06` لم تُمَسَّ** · **ولا `ADR` منشورٌ عُدِّل (`ح-6`)** — `ADR 0057` و`ADR 0054` قائمانِ والتنفيذُ مطابقٌ لهما · **ولا قلبُ بندٍ آخرَ**. **ولا `مَقيس` ولا `مُثبَت`** (`ح-5`) — المُتحقَّقُ منه اكتمالُ الجولاتِ الخضراءِ في CI، لا throughputُ الإنتاجِ ولا latencyُ الويبهوكِ ولا سلوكُ تيليجرامَ الحقيقيَّ. الدليلُ `docs/evidence/correctness/BUG-002-20260907.md`. | `docs/evidence/correctness/BUG-002-20260907.md` | مُنفَّذ · مُختبَر · مُتحقَّق منه | `ab2d009` |
| **تصحيحٌ مُلحَق الثاني والأربعون — 2026-09-07 (وحدةُ تدقيقِ `F6-01`/`CAP-001` — إغلاقٌ مركَّبٌ، الجولةُ الخضراءُ 1/3 `ح-4`)** | **`F6-01` و`CAP-001` يبقيانِ `[ ]` — نُفِّذَ تدقيقُ الإغلاقِ المنفصلُ المُترَكِ في الملحقِ 41، فثبتَ قراءةً من الكودِ الفعليِّ أنَّ مسارَ الـingest النحيفَ المطلوبَ بترتيبِهِ الستّيِّ منفَّذٌ فعلاً ضمنَ عملِ `BUG-002`/`F6-02` المُغلَقَينِ بـ`[x]`.** **ولا جديدَ كودٍ في هذه الوحدةِ**: لا شيفرةَ ولا هجرةَ ولا اختبارَ ولا حاجزٌ — هذا إغلاقٌ مركَّبٌ كنمطِ `SCL-003` (الملحقِ 35) و`F5-03` (الملحقِ 36): القلبُ جاءَ بعدَ التحقّقِ من الكودِ الفعليِّ والاختباراتِ القائمةِ، لا بعدَ تنفيذٍ جديد. **والمُتحقَّقُ منه بحرفِ نصِّ البندينِ**: (١) تحقّقُ السرّ بمقارنةٍ زمنٍ ثابتٍ في `apps/gateway/src/routes/telegram-webhook.ts:207` · (٢) حدُّ المعدَّلِ **بعدَ** السرّ و**قبلَ** استهلاكِ `update_id` في `:254-262` · (٣) استخراجُ `update_id` في `:274` · (٤) الاستلامُ الصامدُ + منعُ التكرارِ + إدراجُ الطابورِ **بنداءٍ ذرّيٍّ واحدٍ** `claim_and_enqueue_telegram_update` عبرَ `update-intake.ts:108-122` · (٥) ACK فوريٌّ `200` في `:294` قبلَ أيِّ معالجةٍ · (٦) المعالجةُ في عاملٍ منفصلٍ `telegram-update-drainer.ts:73` (يُبدَأُ في `apps/gateway/src/index.ts:703`) — فـ`handler.handle` **لا يُنادى داخلَ طلبِ HTTP** في المسارِ المتينِ. **والاختبارُ `tests/integration/telegram-durable-intake.test.ts` يُغطّي المسارَ بسبعةِ أقسامٍ على PostgreSQL حقيقيٍّ**. **والجولاتُ الخضراءُ الثلاثُ لِ`BUG-002`/`F6-02`** (`34127610123` و`34128261550` و`34128675257`) **تشملُ هذا المسارَ بعينِهِ**. **والقلبُ إلى `[x]` معلَّقٌ على ثلاثِ جولاتِ CI خضراءَ متتاليةٍ على هذا الفرعِ (`ح-4`) لا قبلَها** — هذا الـcommitُ يُضيفُ الدليلَ وحدَه. **و`SCL-001` تبقى `[ ]`** (Redis صراحةً) · **و`F6-03` يبقى `[ ]`** (Outbox موحَّد) · **ولا فصلُ خدمةِ عاملٍ** (الدرينرُ داخلَ العمليةِ وفقَ `ADR 0057`) · **ولا `R-17` ولا `DEC-11`/`F0` ولا `ADR` منشورٌ عُدِّل (`ح-6`)**. **ولا `مَقيس` ولا `مُثبَت`** (`ح-5`). الدليلُ `docs/evidence/correctness/F6-01-CAP-001-20260907.md`. |
| **تصحيحٌ مُلحَق الثالث والأربعون — 2026-09-07 (إغلاقُ `F6-01`/`CAP-001` — اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`F6-01` و`CAP-001` قُلِبا إلى `[x]` في `docs/ROADMAP-MASTER.md` §11-ج و§12 (مرحلةُ F6) بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) على الفرعِ `audit/f6-01-cap-001-thin-ingest` لا قبلَها، ونصُّ البندينِ لم يُمَسَّ حرفًا (`ح-1`) — عمودُ الحالةِ وحدَه قُلِبَ من `[ ]` إلى `[x]`.** **وما وُثِّق في الملحقِ الثاني والأربعينَ قائمٌ ولم يُمَسَّ ولم يُعَد**: التدقيقُ قراءةً من الكودِ والاختباراتُ القائمةُ كلُّها باقيةٌ كما هي — **ولا جديدَ كودٍ في هذه الوحدةِ**: لا شيفرةَ ولا هجرةَ ولا اختبارَ ولا حاجزٌ. **والجولاتُ الثلاثُ المتتاليةُ كلُّ وظيفةٍ فيها `success` (قُرِئَ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ):** (1/3) التشغيلُ `34149393266` على `b0416f3` · (2/3) التشغيلُ `34149836063` على `407eda2` · (3/3) التشغيلُ `34150009212` على `8af5668` — كلُّها في الفرعِ `audit/f6-01-cap-001-thin-ingest`. والوظائفُ في الثلاثةِ كلِّها: `verify` و`تكاملٌ على PostgreSQL حقيقيٍّ` و`تكاملٌ على Redis حقيقيٍّ` — كلُّها `success`. **وهذا الـ commitُ تغييرُ حالةٍ وتوثيقٌ وحدَه**: لا شيفرةَ ولا هجرةَ ولا اختبارَ ولا حاجزٌ — عمودُ الحالةِ في الخارطةِ وملفُ الدليلِ `docs/evidence/correctness/F6-01-CAP-001-20260907.md` (§٧ و§٨ والترويسةُ حُدِّثَت من «1/3» إلى الإغلاقِ) وحدَهما. **وما لم يُقلَب يُقرأُ حرفيًّا**: **`SCL-001` تبقى `[ ]`** (Redis صراحةً) · **`F6-03` يبقى `[ ]`** (Outbox موحَّد) · **ولا فصلُ خدمةِ عاملٍ** (الدرينرُ داخلَ العمليةِ وفقَ `ADR 0057`) · **ولا `R-17` ولا `DEC-11`/`F0` ولا `ADR` منشورٌ عُدِّل (`ح-6`)** — `ADR 0057` و`ADR 0054` قائمانِ والتنفيذُ مطابقٌ لهما · **ولا قلبُ بندٍ آخرَ**. **ولا `مَقيس` ولا `مُثبَت`** (`ح-5`) — المُتحقَّقُ منه اكتمالُ الجولاتِ الخضراءِ في CI، لا throughputُ الإنتاجِ ولا latencyُ الويبهوكِ ولا سلوكُ تيليجرامَ الحقيقيَّ. الدليلُ `docs/evidence/correctness/F6-01-CAP-001-20260907.md`. | `docs/evidence/correctness/F6-01-CAP-001-20260907.md` | مُنفَّذ · مُختبَر · مُتحقَّق منه | التزامُ هذا السطرِ نفسُه |
| **تصحيحٌ مُلحَق الرابع والأربعون — 2026-09-08 (إغلاقُ `F5-01` — اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`F5-01` قُلِب إلى `[x]` بعدَ تدقيقٍ مركَّبٍ بلا جديدِ كود، ونصُّ البندِ لم يُمَسَّ حرفًا (`ح-1`) — عمودُ الحالةِ وحدَه قُلِبَ من `[ ]` إلى `[x]`.** كلُّ عيبٍ من BUG-001 إلى BUG-009 مُغلَقٌ بـ`[x]` في `§11-أ` وله ملفُ دليلٍ في `docs/evidence/correctness/`. التدقيقُ يُثبِتُ أنَّ كلَّ عيبٍ يستوفي العناصرَ الأربعةَ المطلوبةَ (اختبارُ إعادةِ إنتاجٍ → إسلامٌ → اختبارُ انحدارٍ → اختبارٌ توزيعيٌّ): BUG-001 سباقٌ بكاتبَين متزامنَين على PostgreSQL حقيقي · BUG-002 عمليتان منفصلتان بـ`Bun.spawn` · BUG-003 عزلُ جولتين على PostgreSQL حقيقي · BUG-004 معاملةٌ ذرّيّةٌ وإرسالٌ من عاملٍ منفصل · BUG-005 استدعاءان متزامنان تحت `read committed` · BUG-006 اختبارٌ توزيعيٌّ صريحٌ على Redis حقيقي (٢٠ طلبًا متزامنًا) · BUG-007 اختبارٌ توزيعيٌّ صريحٌ على Redis حقيقي (كتابتان متزامنتان) · BUG-008 تمييزٌ تحت التسليمِ المكرَّر · BUG-009 ٥ نبضاتٍ مختلطةِ الترتيب. **والجولاتُ الثلاثُ المتتاليةُ كلُّ وظيفةٍ فيها `success` (قُرِئَ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ):** (1/3) التشغيلُ `34163094674` على `2b3d32c` · (2/3) التشغيلُ `34163610568` على `c564333` (verify via rerun — اختبارُ توقيتٍ هشٌّ في graceful-shutdown) · (3/3) التشغيلُ `34164056016` على `81f2cff` — كلُّها في الفرعِ `audit/f5-01-bugs-closure`. والوظائفُ في الثلاثةِ كلِّها: `verify` و`تكاملٌ على PostgreSQL حقيقيٍّ` و`تكاملٌ على Redis حقيقيٍّ` — كلُّها `success`. CI محلّيًّا: `lint`=0 · `typecheck`=0 · ٢١٧٧ ناجحًا / ٦٢٥ متجاوَزًا / ٠ فاشلاً / ٦٨٦٩ توكيدًا في ٢١٢ ملفًّا. **و`F5-02` يبقى `[ ]`** (SCL-001 إلى SCL-005 بنودٌ منفصلةٌ) · **و`BUG-010` لم يُمَسَّ** (نصُّ البندِ يقولُ «BUG-001 … BUG-009» — تسعةٌ لا عشرة) · **ولا `ADR` منشورٌ عُدِّل (`ح-6`)** · **ولا `R-17`/Socket.IO/`DEC-11`/`F0`/`DEC-06` مُسَّ** · **ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** — المُتحقَّقُ منه اكتمالُ العناصرِ الأربعةِ لكلِّ عيبٍ والجولاتِ الخضراءِ الثلاثِ في CI، لا throughputُ الإنتاجِ ولا سلوكُ تيليجرامَ الحقيقيَّ. الدليلُ `docs/evidence/correctness/F5-01-20260908.md`. | `docs/evidence/correctness/F5-01-20260908.md` | مُنفَّذ · مُختبَر · مُتحقَّق منه | التزامُ هذا السطرِ نفسُه |
| **تصحيحٌ مُلحَق الخمسون — 2026-09-19 (`F9-06`/`OPS-005` — ميزانيّةُ عملٍ مطلقةٌ، **أُعيدَ فتحُها للتدقيقِ المستقلِّ**)** | **`F9-06` عُكِسَ من `[x]` إلى `[~]`** و**`OPS-005` عُكِسَ من `[x]` إلى `[~]`** بعدَ تدقيقٍ مستقلٍّ. **والأدلّةُ السابقةُ محفوظةٌ (`ح-8`)**: الكودُ منفَّذٌ في PR #135 · ثلاثُ جولاتِ CI خضراءَ على `main` · البوّاباتُ محروسةٌ. **لكنَّ التدقيقَ كشفَ فجوةً**: ١٦ فشلاً في اختباراتِ التكاملِ على قاعدةٍ نظيفةٍ (PG18) — ٨ في `mutual-rating` (قيدُ مفتاحٍ خارجيٍّ في `beforeEach`) و٤ في `BUG-004` (مهلةٌ) و١ في `F7-07` (مهلةٌ) و١ في `F7-02` (خطّةٌ) و٢ في سعةِ المدينةِ. **وهذهِ لا تُثبَتُ على صورةِ CI (PG17) بعدُ**. والتحقُّقُ المحلّيُّ: ١٦٧ هجرةً · ٨٠ جدولاً · ١٠٧١ دالّةً · عقدُ المخطَّطِ ٣/٣ · `check:coverage` ٥٢٩٠/٠. |
| **تصحيحٌ مُلحَق الثامن والأربعون — 2026-09-10 (إغلاقُ `F7-07`/`CAP-007` — اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`F7-07` و`CAP-007` قُلِبا إلى `[x]` بعَقدٍ وثائقيٍّ بحتٍ لا شيفرةَ فيه، ونصُّ البندِ لم يُمَسَّ حرفاً (`ح-1`).** والمُتحقَّقُ: ملفُّ شاهدٍ `docs/evidence/architecture/F7-07-20260909.md` §٨ · سطرُ سجلٍّ في §25 · **وثلاثُ تشغيلاتِ CI خضراءَ متتاليةٍ على `main`** بعدَ الدمجِ في `524f266`: `34355053288` · `34356336027` · `34361792319` بوظائفِها الثلاثِ. **والقسمانِ 2141 و2190 أدناهُ يبقيانِ كما كُتِبا — ومنهما «لا يُقلَبُ `F7-07` ولا `CAP-007` إلى `[x]`» — وهذا الملحقُ يُقيِّدُهما بتاريخِهما لا يمحوهما (`ح-8`).** **والدرجةُ «مُنفَّذ · مُختبَر · مُتحقَّق منه» ولا `مَقيس` ولا `مُثبَت` (`ح-5`)**: لا هجرةَ على إنتاجٍ · والحاجزُ على ما بعدَ `20260909050000` وحدَه · والموروثةُ ليست مُسترجَعةً · ولا سجلَّ هجراتٍ في القاعدةِ. **و`F7-02`/`CAP-005` باقيانِ `[ ]`** — جولتانِ لا ثلاثٌ. |
| **المرحلة الحالية** | **بوابة `F0` = NO-GO بسبب `DEC-11` وحده، والبوابةُ `[~]`. وأساسُ `F1` قائمٌ: بنودُ `F1-01`…`F1-08` كلُّها `[x]`، لكلٍّ منها ملفُ دليلٍ وسطرٌ في سجل القسم 25 وتشغيلُ CI ناجحٌ، و`F1-09` `[~]` (**مفروضٌ منه ثلاثةُ صفوفٍ من ثمانيةٍ، وصفّانِ من الباقي يقيسان ميزةً غيرَ مبنيّةٍ بعدُ — تبعيّةٌ عبرَ المراحلِ**)، **و`F1-10` `[~]` (النصُّ والبناءُ والحاجزُ منفَّذةٌ، وشقُّ رأسِ الاستجابةِ غيرُ منفَّذٍ لانعدامِ مضيفٍ في `render.yaml`، ولا دليلَ سلوكيَّ لانعدامِ متصفّحٍ — و`TG-005` `[~]` لهذا)** — فبوابةُ `F1` لم تُفتح**، ولا يفتحها بندٌ: **لا جهازَ حقيقيٌّ ولا iOS ولا أندرويد ولا سطحَ مكتبٍ ولا شبكةٌ بطيئةٌ في أيِّ دليلٍ حتى الآن**، ودرجةُ كلِّ بنودِ `F1` **مُنفَّذ · مُختبَر** (و`F1-03` وحدَه **مُتحقَّق منه**) **لا `مَقيس` ولا `مُثبَت`**. **ومن القسم 11-د صار `OPS-005` `[~]` (أُعيدَ فتحُه للتدقيقِ المستقلِّ — البوّاباتُ محروسةٌ وCI أخضرُ، لكنَّ ١٦ فشلاً في اختباراتِ التكاملِ على قاعدةٍ نظيفةٍ بانتظارِ الإصلاحِ والتحقُّقِ على صورةِ CI عينِها)**، **و`F9-06` صار `[~]` (أُعيدَ فتحُه للتدقيقِ المستقلِّ)** **و`OPS-010` `[x]`** (مسارُ العودةِ موثَّقٌ ومحروسٌ بحاجزٍ وممرَّنٌ على قاعدةٍ حقيقيةٍ في CI، والدَينُ المُعلَنُ: عشرُ هجراتٍ تكسر نسخةً سابقةً بتحذيرٍ دائمٍ، ولا عودةَ على إنتاجٍ ولا `RTO`/`RPO` مقيسانِ فالبوّابةُ H `[ ]`) **و`OPS-009` `[x]`** (تصنيفُ كلِّ تجاوزٍ محروسٌ بحاجزَين، والدَينُ المُعلَنُ: إحدى عشرةَ حالةَ منصّةِ قياسٍ لا تعمل في أيِّ مكانٍ) **وهو لا يفتح بوّابةَ `F1` ولا يلمس `REQ-04`، و`F8-09` يبقى `[ ]` لأنّ بوّابتَه غيرُ بوّابتِه**. والإطارُ الحاكمُ للمراحل هو `F0`…`F12` في `docs/ROADMAP-MASTER.md` الإصدار 1.3. التفصيلُ في قسم **CURRENT STATE SNAPSHOT — 2026-08-26** أدناه |
| **إطار «المراحل 0…9»** (§9 و§10) | **لقطة تاريخية — Historical Snapshot**، استبدلها `F0`…`F12`. محفوظةٌ كما هي بلا حذف |
| **الهدف النهائي المعلن** | نظام يخدم 2,000,000 مستخدم متزامن بسلاسة، مع صحة البيانات والعمليات والتواصل، وقابلية التوسع والاستمرارية والتعافي |
| **هل يدعم النظام 2M concurrent users اليوم؟** | **لا. ولا يوجد دليل يقربه من ذلك.** التفصيل في §6 و§13 و§15 |

---

## CURRENT STATE SNAPSHOT — 2026-08-26

> **هذا القسم هو الوصفُ النافذُ لحالةِ النظام.**
> وهو **لقطةٌ (snapshot) لا سجلُّ تغييرات (changelog)**: يقول ما هو قائمٌ اليوم، لا ما جرى.
> وكلُّ ما أسفلَه من أقسامِ هذه الوثيقة **محفوظٌ كما هو ولم يُحذَف منه شيء**؛ وما صار متجاوزاً موسومٌ `Historical Snapshot` في موضعه.
> **عند التعارض بين هذا القسم وأيِّ قسمٍ أقدمَ في هذه الوثيقة نفسِها — هذا القسم هو الصحيح.**
> والقاعدةُ الحاكمةُ لتحديثِ هذه الوثيقة في §0 أدناه.

### S1. المرجع

| | |
|---|---|
| الفرع | `main` |
| الـ commit | `6c4cd25cf5bb0b93a5b608e86b565e721f283a7a` — `style(lint): format three files with Biome` — 2026-08-26T17:16:00Z |
| أبوه | `e6591ed1` — `قرار بوابة F0: NO-GO بسبب DEC-11 وحده — ADR 0033..0041 وخارطة v1.3` |
| طلبات الدمج المفتوحة | **لا يوجد (صفر)** — وهذا يُبطِل ما في §12 عن انتظار دمجِ `#1` و`#12` |
| الإطار الحاكم للمراحل | **`F0`…`F12`** في `docs/ROADMAP-MASTER.md` **الإصدار 1.3** (حالتُها «نافذة وحاكمة»). وإطارُ «المراحل 0…9» في §9 و§10 **لقطةٌ تاريخيةٌ مستبدَلة** |
| توزيع الولاية | `MASTER_DIRECTIVE` يحكم قواعد العمل · **هذه الوثيقة تحكم وصف الحالة** · `ROADMAP-MASTER` تحكم ما يجب أن يُبنى وبأيِّ ترتيبٍ وبأيِّ دليل |

### S2. حالة المرحلة والقرارات

**بوابة `F0` = NO-GO.** القرارُ رسميٌّ من مدير المشروع، **وسببُه `DEC-11` وحده**. بنود `F0-01`…`F0-06` منجَزة، **وحالةُ البوابة تبقى `[~]` ولا تُحوَّل إلى `[x]`**.

**والـNO-GO نطاقُها محدَّدٌ وليست إعادةَ فتحٍ لما حُسِم:** لا يُعاد بحثُ نموذج الحمل، ولا رقمُ 2M، ولا Mini App، ولا ADR 0033/0034.

| القرار | الحالة النافذة | السند |
|---|---|---|
| `DEC-09` | **APPROVED** | [ADR 0037](adr/0037-load-mix-structural-equality-and-unmeasured-baseline.md) |
| `DEC-10` | **APPROVED** | [ADR 0033](adr/0033-launch-gate-splits-capacity-from-commercial-demand.md) |
| `DEC-11` | **BLOCKED — Legal/Regulatory** | [ADR 0039](adr/0039-fare-mechanism-is-blocked-pending-regulatory-decision.md) |
| `DEC-12` | **APPROVED ومُنفَّذ** | [ADR 0038](adr/0038-adr-numbers-are-unique-and-enforced.md) |
| `DEC-13` | **APPROVED** | [ADR 0034](adr/0034-capacity-target-versus-expected-commercial-load.md) |
| `DEC-06` | **APPROVED — مغلَق 2026-08-27** · Socket.IO فوق WebSocket لـMini App؛ HTTP = أوامر + لقطة مرجعية؛ مصدر الحقيقة القاعدة/المجال؛ SSE للإدارة لا لـMini App. **والإغلاق قرارٌ لا تنفيذ؛ و`R-17` قائم** | [ADR 0042](adr/0042-miniapp-realtime-transport-is-socketio.md) · `docs/decisions/DEC-06-realtime-transport-20260827.md` · `docs/evidence/architecture/DEC-06-CLOSURE-20260827.md` · ملحق v1.3 م13-10 |

**توضيحاتٌ ملزمةٌ لمنع التأويل:**

1. `DEC-11` **ليس «منتظراً لاعتماد مدير المشروع».** نطاقُه معتمدٌ، والمحجوبُ هو **آليةُ الأجرة والنشاطُ الترخيصي**. ومصدرُ الإغلاق المقبول **حصراً**: قرارٌ مكتوبٌ من مستشارٍ نظاميٍّ سعوديٍّ مؤهَّل، أو تأكيدٌ رسميٌّ من الجهة التنظيمية المختصة. **ولا تُقبل إجابةٌ تخمينيةٌ من الفريق**، ويشمل ذلك التحليلَ الداخليَّ في `docs/decisions/DEC-09-11-12-20260826.md` — فهو **مادةُ إحاطةٍ لا سند**.
2. **مسألة 2,400,000 حُسِمت ورُفِضت** كشرطٍ ثالثٍ للإطلاق (ADR 0033)، وأُعيد توصيفُها «اختبارَ هامشٍ فوق الهدف». فلا يُقرأ بعد اليوم أنّ السؤال «2M أم 2.4M؟» مفتوح.
3. **`Concurrent Capacity Target` = 2,000,000، و`Expected Commercial Load` لم يُعتمَد بعد** (ADR 0034). والرقم 150,000 في `bench/capacity/profile-v2-market.json` **سيناريو أوَّليٌّ غيرُ معتمد**. ويُمنع استخدام «2M متزامن» وحدها في أيِّ benchmark.
4. **كلُّ النسب العشرة** في `bench/capacity/profile-v2.json` تُوصَف `capacity baseline assumption — unmeasured` — **لا النسبةُ 6% وحدها** — ويُمنع تقديمُها سلوكاً متوقَّعاً (ADR 0037).
5. **مُجمَّد حتى `DEC-11 = APPROVED`:** `Fare` و`Ride Pricing` و`Passenger Payment` وأيُّ فرضيةِ تسويةٍ نقدية — **ومعها الهياكلُ التمهيدية**: لا أعمدة، ولا ترحيلات، ولا واجهات، ولا موضعَ عرضٍ محجوز. و`SR-04` تُبنى **بلا السعر التقديري وبلا وسيلة الدفع** (ADR 0039).
6. **التكامل التنظيمي معماريةٌ حرجة** (ADR 0040)، والمواصفةُ الفنيةُ للجهة **غيرُ معلومة ولا تُخمَّن**.

### S3. ما هو مُقَرَّرٌ وما هو مُنفَّذ — الفرق الحاسم

**قرارٌ معتمدٌ ليس تنفيذاً.** وهذا موضعُ أكبرِ سوءِ فهمٍ محتملٍ اليوم:

| العنصر | حالة القرار | حالة التنفيذ على `main` |
|---|---|---|
| **Mini App** | **معتمدٌ معمارياً ومغلقٌ لا يُعاد فتحُه** — ADR 0028 + ADR 0035 | **هيكلٌ أوّليٌ مدمَج ولا منتج.** مسار `apps/miniapp` **قائمٌ فعلاً في `main`** بعد `c4149f69` (`F1-01` scaffold: Vite + React + TypeScript)، ولا شاشاتٍ ولا حدودَ API ولا ناقلَ زمنٍ حقيقي. **وهويةُ تيليجرامَ صارت متحقَّقاً منها على الخادمِ في وحدة `F1-03`** (`POST /v1/session/telegram` + جلسةٌ داخليةٌ منفصلةٌ عمرُها 600 ثانية) — **على `initData` مُصطنَعٍ لا على عميلٍ حقيقيٍّ، وبلا تجديدٍ ولا استمرارٍ ولا مسارِ منتَجٍ يستهلكها**؛ ولا سطرَ عميلٍ في `apps/miniapp` أُضيف لها بعد. **وقد صار الهيكلُ مبنيّاً ومفحوصاً فعلاً في وحدة إغلاق `F1-01`:** القفلُ الجذريُّ يشمل مساحةَ العمل، و`bun install --frozen-lockfile` ينجح من نسخةٍ نظيفة، و`vite build` يُخرج الأصولَ الثابتة، وفحصُ الأنواع يغطّي `.tsx`، و`biome` يفحص `.tsx`، وCI يبني التطبيقَ المصغَّر بخطوةٍ صريحة. **والنصُ السابق ههنا («غيرُ منفَّذ إطلاقاً — لا وجود لمسار `apps/miniapp`») لقطةٌ تاريخية — Historical Snapshot حتى `2123547e`** |
| تيليجرام ليست تبعيةً وجودية | **معتمد** — ADR 0031 | غير منفَّذ — الواجهةُ الوحيدةُ القائمةُ فعلاً هي بوتات تيليجرام |
| تعريف المستخدم المتزامن | **معتمد** — ADR 0029 | تعريفٌ قابلٌ للقياس — **ولم يُقس بعد** |
| أهداف مستوى الخدمة (SLO) | **معتمدةٌ وملزمة** — ADR 0030 | لا تعريفاتٍ قابلةً للنشر داخل المستودع (§3 البند 12 قائم) |
| ناقل الزمن الحقيقي للرحلة النشطة | **محسوم — Socket.IO فوق WebSocket** (ADR 0042) | **لم يُبنَ.** لا شيفرة إنتاجية للناقل في `main`، والقائم مسابرُ قياس معزولة في `probe/`. **ولا يُبنى قبل إغلاق `R-17` معمارياً** |
| تفرُّد أرقام القرارات | **معتمد** — ADR 0038 | **مُنفَّذ ومربوطٌ في البوابة** — `scripts/check-adr-numbering.ts` داخل `bun run ci` وداخل GitHub Actions |

**بنودُ `F1-01` و`F1-02` و`F1-03` `[x]` في `ROADMAP-MASTER`، وبنودُه السبعةُ الباقيةُ `F1-04`…`F1-10` كلُّها `[ ]` بلا استثناء** — و`F1` **لم يبقَ فارغاً**: هيكل `F1-01` مدمَج في `main` (`c4149f69`) وله ملفُ دليلٍ في `docs/evidence/architecture/F1-01-20260827.md`، **وقد أُغلقت فجوةُ الحَوكمةِ هذه: بندُ `F1-01` صار `[x]` وله سطرٌ في سجل القسم 25 وملفُ دليلِ إغلاقٍ مستقلٌّ** (`docs/evidence/architecture/F1-01-CLOSURE-20260827.md`) — **ثمّ صار `F1-02` `[x]` كذلك في وحدةٍ مستقلّةٍ بدليلها (`docs/evidence/architecture/F1-02-20260827.md`) — مُختبَرٌ على مضيفٍ مُصطنَعٍ لا على جهازٍ حقيقي؛ ثمّ صار `F1-03` `[x]` في وحدةٍ مستقلّةٍ بدليلها (`docs/evidence/architecture/F1-03-20260827.md`) — تحقّقُ `initData` وفحصُ `auth_date` على الخادمِ وجلسةٌ داخليةٌ منفصلةٌ قصيرةُ العمر، **مُختبَرٌ على `initData` مُصطنَعٍ لا على عميلِ تيليجرامَ حقيقيٍّ، وبلا استمرارٍ ولا تجديدٍ ولا مسارِ منتَجٍ يستهلك الرمزَ (`F1-04`)**؛ و`F1-04`…`F1-10` كلُّها `[ ]` بلا استثناء.** والعبارةُ السابقة «`F1` لم يبدأ» لقطةٌ تاريخية — Historical Snapshot حتى `2123547e`. وترتيبُ تنفيذه (ADR 0041) غيرُ قابلٍ للإعادة: هوية → جلسة → حدود API → ناقل الزمن الحقيقي → الهيكل → الرحلة → التتبع → الإشعارات → الدفع → الدعم. **وبندُ الناقل لا يُعَدُّ منجَزاً قبل حسم `DEC-06` ومعالجة `R-17`** — و`DEC-06` حُسِم بـADR 0042 في 2026-08-27، **و`R-17` لم يُعالَج بعد، فالبند ما زال غيرَ منجَز.**

وعقدُ ADR 0035 نافذٌ على `F1` كلِّه: **Mini App لا يُنشئ حالةَ عملٍ جديدة** — لا `Users` ولا `Rides` ولا `Drivers` ولا `Offers` ولا `Sessions` ولا `Tracking` مكرَّرة؛ ومسارُ الهوية الواحدُ لا يُختصر؛ والبنيةُ قبل الشاشات.

### S4. الطوبولوجيا القائمة فعلاً — لم تتغير

**لا يظهر في سجلِ التغييرات بين `b61ae98` و`6c4cd25` أيُّ تغييرٍ في ملفات `apps/` أو `packages/`؛ والتغييراتُ في هذا المدى محصورةٌ بحسب الجرد في `docs/` و`bench/` و`scripts/` و`tests/` و`.github/` وملفات الضبط.**

> **حدُّ الادّعاء:** هذا **استنتاجٌ من جردِ مدىٍ محدّد، لا خاصيةٌ (invariant) يفرضُها حاجزٌ دائمٌ في البوابة**؛ فلا يُقرَأ ضماناً ملزِماً لما بعد `6c4cd25`.

وعليه فوصفُ §1.2 و§1.3 قائمٌ بحرفه، وأُعيد التحقُّق منه بفحصِ ملفاتِ `main` عند `6c4cd25`. الدرجة: **مُنفَّذ** (فحصُ ملفات، لا تشغيلٌ في الإنتاج).

Bun 1.3.14 + TypeScript · Hono · grammY · PostgreSQL/Supabase + PostGIS عبر `postgres.js` · Upstash Redis عبر REST **اختياري** · monolith معياري · Render Frankfurt `starter` · **`numInstances: 1`** · `autoDeploy: false` · `SESSION_STORE=memory` · العاملُ داخل البوابة.

**ما لا وجود له في المستودع اليوم — منفيٌّ صريحاً:**

* **لا WebSocket** — ولا أيُّ تنفيذٍ له.
* **لا Supabase Realtime.**
* **لا تنفيذَ إنتاجياً لـMini App** — ولا مشروعَ واجهةٍ منفصلاً، ولا `apps/miniapp`.
* **لا ناقلَ زمنٍ حقيقيٍ جديداً** من أيِّ نوع.

ووسائطُ «الزمن الحقيقي» القائمةُ فعلاً ثلاثٌ فقط، كما في §1.3: تعديلُ رسالة Live Location في تيليجرام · استقصاءُ HTTP كلّ 5 ثوان لرابط التتبع العام · SSE لخريطة الإدارة يُغذيها **ناقلُ أحداثٍ داخل العملية**.

**و`R-17` قائمٌ وغيرُ معالَج:** صحةُ الناقل الحالي مشروطةٌ بـ`numInstances: 1`، **ولا شيءَ في الشيفرة يفرضُه**، والتوسّعُ الأفقيُّ يكسرُه **صامتاً**.

### S5. الحجم الفعلي — مَقيس بالعدَّ على شجرة `main`

طريقةُ القياس: عدٌّ على شجرةِ Git الكاملة لـ`6c4cd25` (**1,048 blob**، `truncated = false`)، وعدُّ أسطرٍ على نسخةِ عملٍ **مُتحقَّقٍ من مطابقةِ أحجامِ ملفاتِها بالبايت** لأحجامِ الـblobs في الشجرة. الدرجة: **مَقيس**.

| القياس | القيمة عند `6c4cd25` |
|---|---|
| ملفات TypeScript (`apps` + `packages` + `scripts` + `tests`) | **744** |
| أسطر TypeScript في المسارات نفسِها | **93,465** |
| ملفات TypeScript في `bench/` (**خارج تعريف §1.1**) | **33** ملفاً / **7,535** سطراً |
| هجرات SQL في `supabase/migrations/` | **57** |
| ملفات اختبار `*.test.ts` (كلُّها تحت `tests/`) | **151** |
| قرارات معمارية موثقة `docs/adr/*.md` | **41** |
| ملفات دليل تحت `docs/evidence/` | **67** |
| اعتماديات مباشرة | **6** — `grammy`, `hono`, `postgres`, `@biomejs/biome`, `@types/bun`, `typescript` (لا يوجد `package.json` آخر في المستودع) |
| ملفات workflow | **1** — `.github/workflows/ci.yml` (352 سطراً) |

> **تصحيحٌ واجبُ التسجيل (§0 البند 6 — الفشل يُسجَّل ولا يُلوَّن):** أرقامُ §1.1 لم تكن صحيحةً **عند الـcommit الذي نُسِبت إليه**. العدُّ على شجرةِ `b61ae98` يعطي **741** ملف TypeScript و**149** ملف اختبار، لا 734 و144. فالرقمان كانا مقيسَين على أساسِ المرحلة 0 ولم يُعاد عدُّهما. والرقمُ الوحيد الذي صمد دون تغيير هو الهجرات: **57**.

### S6. حالة بوابة CI — التشغيل المرجعي

| | |
|---|---|
| التشغيل المرجعي | **GitHub Actions run `33102818831`** — على `7e149819` (رأسُ `main`). والتشغيلُ الأقدمُ `32994052498` على `6c4cd25` لقطةٌ تاريخية؛ وبينهما أخفق الرأسُ في خطوة `Install` في `c4149f69` (run `33096721405`) و`65691094` (run `33100294275`) |
| النتيجة | **`completed / success`** — الوظيفتان معاً: `verify` و«تكامل على PostgreSQL حقيقي». والتفصيلُ الكاملُ للخطوات في `docs/evidence/architecture/F1-01-CLOSURE-20260827.md` §٤.٢ |
| الخطوات | كلُّ خطوةٍ `success`، عدا خطواتِ تقريرِ الإخفاق فهي `skipped` — وهو المتوقّع عند النجاح |
| ما عبر في `verify` | `Install` · `Lint` · `Typecheck` · **`بناء تطبيق تِلِغرام المصغَّر`** · `Test` · **والحواجزُ المعماريةُ الستةَ عشر كلُّها**، ومنها **تفرُّد أرقام القرارات المعمارية (ADR 0038)** و**قابليةُ إعادةِ إنتاجِ بناءِ الصورتَين** |
| ما عبر في وظيفة التكامل | أدوار Supabase · تطبيق الهجرات بالترتيب · تطابق أدوات العميل مع الخادم · اختبارات التكامل · اختبارات e2e · **وحاجزا منعِ التخطيّ الصامت عبرا أيضاً** |
| تنبيه قائم (ليس فشلاً) | `actions/checkout@v4` يستهدف Node.js 20 المتوقَّف ويُجبَر على Node.js 24 — في الوظيفتين |
| بنية البوابة | `.github/workflows/ci.yml` وحده — وظيفتان، و**18 حاجزاً معمارياً مسمَّى** في `verify` (السابعَ عشرَ عزلُ طبقةِ تيليجرام في وحدة `F1-02`، والثامنَ عشرَ منعُ تسريبِ الأسرارِ إلى السجلّات في وحدة `F1-03`)، **وخطوةُ بناءٍ صريحةٌ لتطبيق تِلِغرام المصغَّر** (`bun run build:miniapp`) مضافةٌ بعد `Typecheck` في وحدة إغلاق `F1-01`. و`bun run ci` يسلسل: `lint` → `typecheck` → **`build:miniapp`** → **13 سكربت حراسة** → `verify-container-build.sh` → `test` |

**حدُّ ما يُدَّعى هنا — صريحاً:**

* المُثبَت هو **أنّ الـworkflow نجح كاملاً**، وأنّ كلَّ خطوةٍ عبرت.
* **ولا يُكتب هنا عددُ اختباراتٍ ناجحة ولا عددُ توكيداتٍ من هذا التشغيل**، لأنّ السجلات الخام لم تُحفَظ كملفِ دليلٍ في `docs/evidence/`. وأيُّ رقمٍ من هذا النوع في هذه الوثيقة يجب أن يُسنَد إلى ملفِ دليلٍ محفوظ.
* ولم يُشغَّل `bun run ci` ولا `typecheck` ولا الاختبارات محلياً في إعدادِ هذه اللقطة.

**وسياقٌ يجب أن يُقال:** التشغيلاتُ الثلاثةُ قبلَه — على `8638f7f` و`f0f2981` و`e6591ed` — **فَشِلت عند خطوة `Lint`**، فتُخُطّيت `Typecheck` و`Test` **وكلُّ الحواجز المعمارية** بحالة `skipped`. وأمّا وظيفةُ التكامل على PostgreSQL فقد كانت تنجح في تلك التشغيلات. وأوّلُ رؤيةٍ لنتيجةِ الحواجز الستةَ عشر منذ ثلاثة commits كانت في `6c4cd25`.

**سببُ فشل `Lint`** وإصلاحه: ثلاثةُ ملفاتٍ خالفت تنسيقَ Biome — `bench/capacity/profile-v2.json` و`bench/capacity/profile-v2-market.json` و`scripts/check-adr-numbering.ts`. و`6c4cd25` **تنسيقٌ بحتٌ لا يغير سلوكاً ولا قراراً ولا نطاقاً**: الملفان JSON متطابقان دلالياً قبل وبعد، وتغييرُ `.ts` **مسافاتٌ فقط** (tabs → مسافتان، وفق `biome.json`).

### S7. الأدلة القائمة في `docs/evidence/`

**67 ملف دليل.** والبنيةُ صارت مصنَّفةً وفق `docs/evidence/README.md`: تسعةُ تصانيف (`architecture`, `capacity`, `chaos`, `compliance`, `correctness`, `distributed`, `recovery`, `release`, `security`).

أدلةُ `F0` والقرارات — كلُّها تحت `docs/evidence/architecture/`:

| الملف | ما يحتويه |
|---|---|
| `F0-01-to-F0-05-20260826.md` | إغلاق البنود `F0-01`…`F0-05` مع ADR 0028–0032 |
| `F0-06-20260826.md` | بنية مجلدات الأدلة |
| `F0-GATE-DECISION-20260826.md` | قرار بوابة `F0` |
| `DEC-10-DEC-13-F1-CONTRACT-20260826.md` | عقد `F1` وقرارا `DEC-10` و`DEC-13` |

وورقةُ `docs/decisions/DEC-09-11-12-20260826.md` **مادةُ إحاطةٍ لا سند** — وهي الملفُ الوحيد في `docs/decisions/`. **ولا توجد في المستودع ورقةُ قرارٍ لـ`DEC-06`.**

وأُنشئت `docs/capacity/capacity-profile-v2.md` و`capacity-profile-v2-market.md` **مُوَلَّدتَين بـ`bench/capacity/derive.py`** — ولا يُكتب رقمٌ مشتقٌّ بيدٍ في أيِّ وثيقة.

### S8. درجات الأدلة للقطة اليوم — لا خلط

وفق سلّم §0 البند 3 والقسم 1.4 من `ROADMAP-MASTER`:

| الادّعاء | الدرجة | الدليل |
|---|---|---|
| أرقام الحجم في S5 | **مَقيس** | عدٌّ على شجرة Git وعلى نسخةِ عملٍ مُتحقَّقٍ من أحجامِها بالبايت |
| الطوبولوجيا في S4، ونفيُ WebSocket/Realtime/Mini App | **مُنفَّذ** (وصفٌ من فحصِ ملفات) | شجرة `main` + `render.yaml` + ملفات المسارات |
| تفرُّد أرقام القرارات | **مُختبَر** | `scripts/check-adr-numbering.ts` عبر على 41 قراراً في run `32994052498` |
| مسار التكامل على PostgreSQL + PostGIS حقيقي | **مُتحقَّق منه** | وظيفةُ التكامل في run `32994052498` على حاوية `postgis/postgis:17-3.5` |
| أيُّ رقمٍ للسعة، وأيُّ اقترابٍ من 2M | **غيرُ مَقيس وغيرُ مُثبَت** | لا يوجد قياسٌ واحدٌ على طوبولوجيا الإنتاج — §3 البند 1 قائمٌ بحرفه |
| سلوك Upstash وSupabase الحقيقيَين تحت الضغط | **غيرُ مُتحقَّق منه** | §3 البند 7 و§12 |
| أرقام §13 كلُّها | **مَقيسةٌ على جهازٍ واحد لا مُثبَتة** | محفوظةٌ كما هي، وبيئاتُها معلنةٌ في مواضعِها |

### S9. المخاطر — لم تسقط واحدة

بما أنّ **الجرد لا يظهر تغييراً في `apps/` أو `packages/` في هذا المدى** (القسم S4 وحدُّ الادّعاء فيه)، فـ**§4 بكلِّ بنودِه قائمٌ بلا استثناء**، ومعه §3 و§5 و§6. وتُضاف إليها اليوم ثلاثٌ:

1. **`R-17`** — صحةُ ناقل الزمن الحقيقي مشروطةٌ بـ`numInstances: 1` ولا شيءَ في الشيفرة يفرضُه.
2. **التكامل التنظيمي** — معماريةٌ حرجةٌ (ADR 0040) ومواصفتُها الفنيةُ غيرُ معلومة.
3. **دينٌ في الـworkflow** — `actions/checkout@v4` على Node.js 20 المتوقَّف.

ومن المعروف القائم الذي لم يُصلَح: **سلوكٌ خاطئٌ في `driver-dialog`** يخبر الظافرَ أنّه خسر (§13.10) — الحالةُ التجاريةُ سليمةٌ والنصُّ خطأ؛ و**تجميعُ العدّادات عبر النسخ** شرطٌ سابقٌ لمرحلة اكتشاف السعة.

### S10. مواضع موسومة `Historical Snapshot` في هذه الوثيقة

محفوظةٌ كلُّها بلا حذف، وتُقرَأ تاريخاً لا حالةً:

| الموضع | لماذا |
|---|---|
| §1.1 | أرقامُه متجاوزةٌ، ولم تكن صحيحةً عند `b61ae98` أصلاً — البديل في S5 |
| §2 | صفوفُ `lint`/`typecheck`/الاختبارات/بوابات CI تعود لجلسةِ قياسٍ منتهية — البديل في S6 |
| §9 و§10 | إطارُ «المراحل 0…9» استبدله `F0`…`F12` — البديل في S2 وفي `ROADMAP-MASTER` |
| §11 و§12 | يتوقفان عند وحدة 2-3؛ ووقائعُ `F0` تُسجَّل في القسم 25 من `ROADMAP-MASTER` |
| §14 و§15 و§16 | قراراتُ المرحلة 0 وافتراضاتُها — منها ما حُسِم بـADR 0028–0041 |
| §17 | ينسب الحالة إلى `ecb4518`/`b61ae98` وإلى فرعٍ وPR لم يعودا قائمَين |
| ملحق ج — الجملة عن `DEC-09`…`DEC-13` | تقول إنّها **«ما زالت منتظرةً الاعتماد»** — وهذا لم يعد صحيحاً. البديل في S2 وفي تصحيح ملحق ج أدناه |

### S11. ما لم يُفعَل في إعداد هذه اللقطة

لم يُعدَّل `ROADMAP-MASTER.md`، ولا أيُّ ADR، ولا أيُّ كود. ولم تُنشَأ ADR 0042 ولا ورقةُ `DEC-06`. ولم تُغَيَّر حالةُ `F0` ولا `DEC-11` ولا `F1`. والتحريرُ هنا **وصفٌ لا قرار**.

---

## 0. قاعدة حاكمة: الوثيقة تُحدَّث قبل أي دفع

**هذه القاعدة مُلزِمة لكل من يعمل على هذا المستودع بعد هذا التاريخ — إنساناً كان أو وكيلاً برمجياً (agent) أو عملية آلية.**

1. **لا يجوز وجود commit يغيّر حالة المشروع دون أن تكون هذه الوثيقة مُحدَّثة في نفس الـ commit (أو في commit يسبقه مباشرة داخل نفس الـ PR) لتعكس هذا التغيير.**
2. قبل أي `git push` يجب أن تُسجَّل في هذه الوثيقة:
   - ما نُفِّذ فعلاً.
   - ما بقي.
   - الاختبارات التي شُغّلت ونتائجها الفعلية (مع أرقام، لا أوصاف).
   - القرارات المعمارية المتخذة وأسبابها.
   - المشاكل والمخاطر المعروفة التي لم تُحَل.
3. **مصطلحات الحالة إلزامية ولا يجوز الخلط بينها.** كل ادّعاء في هذه الوثيقة يجب أن يحمل واحدة من هذه الدرجات:

   | الدرجة | المعنى الدقيق | ما يكفي كدليل |
   |---|---|---|
   | **مُنفَّذ** (Implemented) | الكود موجود ويُترجَم ويُدمَج في مسار حقيقي. | مسار الملف والسطر. |
   | **مُختبَر** (Tested) | يوجد اختبار آلي يغطي السلوك وينجح. | اسم ملف الاختبار + نتيجة تشغيل. |
   | **مُتحقَّق منه** (Verified) | شُغِّل على بيئة حقيقية (قاعدة بيانات فعلية/خدمة فعلية) وتحقّقت الافتراضات. | سجل تشغيل مؤرَّخ. |
   | **مَقيس** (Measured) | يوجد رقم كمّي مأخوذ من تشغيل فعلي، مع وصف بيئة القياس. | مخرجات القياس + بيئتها. |
   | **مُثبَت** (Proven) | القياس أُجري على طوبولوجيا مماثلة للإنتاج، بمعايير نجاح مُعلَنة مسبقاً، وقابل لإعادة الإنتاج. | سكربت + بيئة + معايير + إعادة تشغيل. |

4. **وجود الكود ليس إثباتاً.** ونجاح اختبار وحدة ليس قياساً. وقياس على جهاز محلي ليس إثباتاً لسعة الإنتاج.
5. **لا يُكتب في هذه الوثيقة أن شيئاً «جاهز» أو «صحيح» أو «يدعم كذا» بغير دليل مُشار إليه.**
6. **الفشل يُسجَّل، لا يُخفى ولا يُلوَّن.** إذا فشل اختبار: يُسجَّل الفشل وسببه، ثم يُعالَج، ثم يُعاد التشغيل، وتُسجَّل النتيجة الجديدة. لا يُسمى الفشل «نجاحاً جزئياً».
7. **لا تُجمَع تغييرات غير مترابطة في commit واحد.** كل وحدة عمل مترابطة = commit + دفع. تاريخ Git يجب أن يعكس التقدم الحقيقي.
8. **أيُّ commit يغير حالةَ المشروع أو القرار أو النطاق يجب أن يُحدَّث `SYSTEM_STATE.md` في نفسِ الـcommit أو في commit سابق.**
   - **والاستثناءُ الوحيد: التنسيقُ البحتُ الذي لا يغير سلوكاً ولا قراراً ولا نطاقاً** — مثالُه `6c4cd25`.
   - ويدخل في «تغيير القرار أو النطاق»: إضافةُ ADR أو تعديلُه أو إعادةُ ترقيمِه، وتغييرُ حالةِ أيِّ `DEC-*`، وتغييرُ حالةِ أيِّ بوابةِ مرحلة، وتعديلُ `ROADMAP-MASTER`، وإضافةُ حاجزٍ في الـCI.
   - **وقد خُرِقت هذه القاعدة فعلاً** في `e6591ed1` — 1,084 إضافةً تشمل تسعةَ ADR وإعادةَ ترقيمٍ وخارطةً بإصدار جديد وحاجزاً جديداً في الـCI، **بلا أيِّ تغييرٍ في `SYSTEM_STATE.md`**. يُسجَّل وفق البند 6 ولا يُلوَّن.

---

## 1. الحالة الحالية للمشروع (وصفٌ لا حكم)

**وَصْلة** منصة مشاوير وتوصيل تعمل **بالكامل عبر بوتات تيليجرام** (بوت سائق + بوت راكب)، لخمس مدن سعودية تُفعَّل معاً: جدة، مكة، الرياض، الطائف، المدينة المنورة. النطاق التجاري المُفعَّل هو **اشتراك السائق الشهري فقط** (ADR 0027).

### 1.1 الحجم الفعلي (مقيس بالعدّ على الـ commit أعلاه) — **Historical Snapshot**

> **لقطةٌ تاريخية — لا تُقرَأ كحالةٍ حالية. الأرقامُ النافذةُ في `CURRENT STATE SNAPSHOT — 2026-08-26` القسم S5.**
> وتسجيلاً واجباً (§0 البند 6): الجدولُ أدناه **لم يكن مطابقاً للـcommit الذي نُسِبَ إليه** (`b61ae98`)، إذ العدُّ على شجرةِ `b61ae98` يعطي 741 ملف TypeScript و149 ملف اختبار لا 734 و144.

| القياس | القيمة (تاريخية) |
|---|---|
| ملفات TypeScript (`apps` + `packages` + `scripts` + `tests`) | 734 |
| أسطر TypeScript | 92,477 |
| هجرات SQL في `supabase/migrations/` | 57 |
| ملفات اختبار (`*.test.ts`) | 144 |
| قرارات معمارية موثقة (`docs/adr/`) | 28 |
| اعتماديات npm | ~20 حزمة فقط |

### 1.2 الطوبولوجيا الفعلية

* **وقت التشغيل:** Bun 1.3.14، TypeScript، بلا مترجم إلى JS مُصغَّر.
* **HTTP:** Hono. **البوتات:** grammY.
* **قاعدة البيانات:** PostgreSQL/Supabase + PostGIS، عبر `postgres.js`.
* **Redis:** Upstash **عبر REST**، اختياري (للجلسات وحد المعدل).
* **الشكل المعماري:** monolith معياري — `packages/domain | application | infrastructure | shared` + `packages/agent-core` + `packages/maps`، وتطبيقات `apps/gateway | workers | admin-dashboard`.
* **النشر:** Render، منطقة Frankfurt، خطة `starter`، **`numInstances: 1` لكل خدمة**، `autoDeploy: false`، فحص الجهوزية `/ready` (`render.yaml:16-27`, `render.yaml:161-169`).
* **الجلسات في الإنتاج:** `SESSION_STORE=memory` (`render.yaml:33-35`).
* **العامل الخلفي:** يعمل **داخل البوابة** (`RUN_WORKER_IN_GATEWAY=true`) لأن خدمة العامل المستقلة لم تكن منشأة فعلاً على Render حسب توثيق المستودع (`render.yaml:60-69`, `docs/render-deployment-vars.md:98-105`).

### 1.3 وسائط «الزمن الحقيقي» الفعلية (مؤكَّدة بالفحص)

لا يوجد WebSocket ولا Supabase Realtime في هذا النظام. الوسائط الثلاثة هي:

1. **خريطة الراكب الحيّة** = تعديل رسالة Live Location في تيليجرام (خانق 5 ثوان / نافذة 15 دقيقة).
2. **رابط التتبع العام** = استقصاء HTTP كل 5 ثوان (`GET /api/track/:token/position`).
3. **خريطة الإدارة** = SSE يُغذّيها **ناقل أحداث داخل العملية** + لقطة من القاعدة كل 20 ثانية.

> النتيجة الهندسية: الوسيطان 1 و3 يعتمدان على حالة داخلية في العملية، وهذا يمنع التوسع الأفقي (§4 البند ب).

> **تحديث 2026-08-26 (يُقرَأ مع القسم S4):** هذا الوصفُ **قائمٌ بحرفه** وأُعيد التحقُّق منه على `6c4cd25`. ويُضاف إليه أمران:
> 1. **`DEC-06` (ناقل الزمن الحقيقي للرحلة النشطة) ما زال مفتوحاً، وصار حاجزاً معلناً داخل `F1`.** ولا يُبنى ناقلٌ جديدٌ قبل حسمِه.
> 2. **`R-17`:** صحةُ هذه الوسائط الثلاث مشروطةٌ بـ`numInstances: 1`، **ولا شيءَ في الشيفرة يفرضُ هذا الشرط**؛ فرفعُ عدد النسخ يكسرُ السلوك **صامتاً بلا خطأ ولا تنبيه**.

---

## 2. ما يعمل فعلاً (بدرجة الدليل) — **Historical Snapshot جزئياً**

> **ما زال قائماً:** كلُّ الصفوف الوصفية عن البنية والسلوك (الإسناد، idempotency، القفل الاستشاري، الجلسات، outbox) — **فالجرد لا يظهر تغييراً في `apps/` أو `packages/` بين `b61ae98` و`6c4cd25`** (القسم S4).
> **وما صار لقطةً تاريخية:** صفوفُ `lint` و`typecheck` ومجموعةِ الاختبارات و«بوابات CI الصارمة» — فأرقامُها تعود لجلسةِ قياسٍ منتهية على `b61ae98`، ولا تُنسَب لـ`main` اليوم. **والبديلُ النافذُ في القسم S6.**
> وتصحيحاتٌ واجبة: عددُ سكربتات الحراسة في `verify` صار **17** لا «~14» (كان 16 قبلَ وحدةِ `F1-02`) · ومراجعُ الأسطر `ci.yml:13-145` و`:147-349` **لم تعد صحيحة** فالملفُ اليوم 352 سطراً · وصورةُ التكامل `postgis/postgis:17-3.5` **ما زالت صحيحة** (`ci.yml:159`) · وعددُ ملفات الاختبار صار 151 لا 144.

| العنصر | الدرجة | الدليل |
|---|---|---|
| بناء المشروع وفحص الأنماط (`lint`) | **مَقيس** | `bun run lint` → exit 0، 39 ملاحظة معلوماتية. شُغِّل في هذه الجلسة على الـ commit أعلاه. |
| فحص الأنواع (`typecheck`) لـ `apps/packages/tests` | **مَقيس** | `bun run typecheck` (`tsc --noEmit`) → exit 0. |
| مجموعة الاختبارات الكاملة | **مَقيس** | `bun run test` → **1,772 نجاح، 0 فشل، 6,938 استدعاء `expect()`، 144 ملفاً، 156.92 ثانية.** |
| الهجرات الـ57 تُطبَّق على PostgreSQL 18.4 + PostGIS نظيفة | **مُتحقَّق منه** | طُبِّقت كلها بترتيب اسم الملف على قاعدة `waslah` محلية في هذه الجلسة، بلا خطأ. |
| **عدم إسناد الرحلة لسائقين — منع الإسناد المزدوج** | **مَقيس + مُتحقَّق منه** | `claim_ride` يستخدم `FOR UPDATE SKIP LOCKED` + فهرس فريد جزئي على العرض المقبول. أُعيد تشغيل `scripts/match-race-test.ts` في هذه الجلسة على قاعدة حقيقية: **1,000 مطالبة متزامنة على طلب واحد → 0.36s، 2,745 مطالبة/ث، فائز واحد بالضبط، 999 خسارة بسبب `ORDER_NOT_CLAIMABLE`، صفر استثناءات، كل تحققات الصحة نجحت.** وسيناريو 100 طلب × 10 سائقين → **0.21s، 4,816 مطالبة/ث، 100/100 فائز واحد، لا عرضان مقبولان لطلب واحد.** |
| عدم تكرار الأثر المالي (idempotency للدفع) | **مُنفَّذ + مُختبَر** | مفتاح idempotency فريد في `payment_transactions`، وفريد `(provider, event_id)` في `webhook_events`، وإعادة قراءة المعاملة من المزود قبل أي أثر مالي. يغطيه `tests/integration/payment-real-flow.test.ts`. |
| حصرية العامل الخلفي (عدم تشغيل نفس المهمة مرتين) | **مُنفَّذ + مُختبَر** | قفل استشاري (advisory lock) في PostgreSQL على اتصال مخصص. `tests/integration/distributed-lock.test.ts`. |
| جلسة تتبع واحدة مفتوحة لكل رحلة، وتقدّم زمني رتيب | **مُنفَّذ** | فهرس فريد جزئي على الجلسة المفتوحة + `GREATEST` على آخر إصلاحة موقع. |
| صناديق صادر (outbox) للبث والإشعارات والسلامة، مع رموز مطالبة وإعادة مطالبة | **مُنفَّذ** | جداول outbox + claim token + reclaim. |
| بوابات CI الصارمة | **مُتحقَّق منه** | `verify` يشغّل lint + typecheck + الاختبارات + ~14 سكربت حراسة؛ و`integration` يبني `postgis/postgis:17-3.5` حقيقياً ويطبّق كل الهجرات ويمنع التخطي الصامت (`.github/workflows/ci.yml:13-145`, `:147-349`). |
| إجراءات نسخ احتياطي واستعادة موثقة مع تدريب (drill) | **مُنفَّذ + مُختبَر** | `scripts/backup-restore-drill.ts` يقارن المخطط وRLS والقيود والفهارس والصفوف. `docs/runbook.md:45-83`. |

---

## 3. ما هو غير مكتمل أو غير مثبت

1. **لا يوجد قياس واحد على الطوبولوجيا الحقيقية.** كل الأرقام المنشورة أُخذت على جهاز واحد (2 vCPU / 7 GiB) مع PostgreSQL محلي، جلسات ذاكرة، **ومُرسِل تيليجرام مُستبدَل بمُرسِل صامت**، وحد معدل مرفوع عملياً إلى اللانهاية (`docs/load-test-report.md:19-51`, `scripts/load-test.ts:41-77`). لا Render، لا Supabase pooler، لا Upstash، لا TLS، لا إنترنت.
2. **أداة قياس الحمل: أُصلحت في وحدة 2-1 لكنها ليست منصّة القياس المعتمدة.** الوصف التالي يوثّق ما كان على الـ commit `ecb4518` وسببَه الجذريّ، لأن الدرس منه هو أساس تصميم منصّة القياس. `scripts/load-test.ts` كان لا يعمل: `buildContainer` أصبح يقرأ `config.tracking` لكن السكربت يبني `AppConfig` ناقصاً 9 حقول، فيسقط بـ `TypeError: undefined is not an object (evaluating 'overrides.maxAccuracyMeters')` عند `packages/tracking/config.ts:26`. **السبب الجذري:** `tsconfig.json` يستثني `scripts/**` من `include`، فأدوات القياس خارج محيط فحص الأنواع وخارج CI، فتعطّبت صامتة. عند إضافة `scripts/**/*.ts` مؤقتاً ظهرت **4 أخطاء أنواع في 3 ملفات** (`load-test.ts`, `backup-restore-drill.ts`, `race-subscription-changes.ts`). **الأثر:** الأرقام المنشورة في `docs/load-test-report.md` **غير قابلة لإعادة الإنتاج على هذا الـ commit.**

   **ما تغيّر في وحدة 2-1:** `scripts/**` و`bench/**` صارا داخل `include`، والأخطاء الأربعة أُصلحت من جذرها، وضبط الأداة صار يُبنى من `loadConfig` الحقيقي لا بكائن مكتوب باليد، والحوار أُكمل إلى تسع خطوات، وصار الحكم من جسم الرد ومن صفوف قاعدة البيانات. الأداة الآن **تعمل وتنجح ونتائجها صحيحة الدلالة** (§13.3). ومع ذلك **تبقى غير صالحة كـbaseline** لأسبابٍ بنيوية: مولّدها داخل عملية الخادم، وحملها مغلق الحلقة بلا معدّل ورود، ونتائجها لا تُحفَظ بصيغة تُقارَن. فهي محفوظة كأداة إعادة إنتاج للأرقام التاريخية، والقياس المعتمد يُبنى في الوحدات 2-3-4.
3. **لا خدمة عامل مستقلة فعلياً.** الـ Blueprint يعرّفها، والتوثيق يقول إنها لم تُنشأ على Render، والحل الحالي هو دمجها في البوابة — أي أن المهام الخلفية تنافس معالجة الويبهوك على نفس الـ CPU وتموت بموتها.
4. **لا بيئة staging مماثلة للإنتاج، ولا IaC يصفها.** فلا يوجد مكان يمكن فيه إعادة القياس على طوبولوجيا حقيقية.
5. **لا مولّد حمل موزّع.** المولّد الحالي `Promise.all` داخل نفس عملية Bun التي تشغّل الخادم — فهو يتنافس على نفس المعالج ويُشبِع نفسه قبل أن يُشبِع النظام.
6. **لا تغطية اختبار (coverage) مقيسة، ولا عتبة، ولا بوابة أداء في CI.** ولا يُشغَّل `load-test.ts` ولا `match-race-test.ts` في CI ولا في `package.json`. فحص الأنواع صار يغطّيهما بعد وحدة 2-1، وهذا يمنع انجراف *الأنواع* وحده؛ انجرافُ *السلوك* لا يمنعه إلا التشغيل الفعلي في CI (مؤجَّل إلى ما بعد وحدة 3).
7. **لا Redis حقيقي في CI.** فسلوك الجلسات وحد المعدل على Upstash تحت الضغط أو التعطل غير مُتحقَّق منه.
8. **لا chaos ولا failure injection** على مستوى الشبكة/القاعدة/Redis/المزودين، ولا اختبار قتل نسخة أو إعادة نشر متدرج مع طلبات حيّة.
9. **لا تقسيم جداول (partitioning) ولا سياسة استبقاء/أرشفة** لأي جدول ينمو بلا حد: `audit_log`, `attendance_log`, `order_offers`, جلسات التتبع المغلقة، صفوف outbox، السجلات المالية.
10. **لا تتبّع موزّع (tracing) ولا `request-id`** يعبر الطبقات.
11. **لا قواطع دائرة (circuit breakers) ولا فواصل (bulkheads)** لأي مزود خارجي.
12. **لا تعريفات مراقبة أو تنبيه قابلة للنشر داخل المستودع** — التوثيق يصف ما *ينبغي* مراقبته، لكن لا يوجد ملف monitor/alert فعلي، ولا SLO، ولا RTO/RPO، ولا ملكية on-call (`docs/render-deployment-vars.md:223-293`, `docs/runbook.md:85-92`).
13. **انجراف وثائق تاريخية:** `docs/production-verification.md` يقول `autoDeploy` مفعّل ويذكر 4 هجرات و813+ اختباراً، والواقع الآن `autoDeploy: false` و57 هجرة و1,772 اختباراً. هذه الوثائق **لقطات تاريخية لا وصف حالة**، وهذه الوثيقة تنسخ حكمها عند التعارض.

---

## 4. المخاطر الحالية (مرتَّبة بالأثر على صحة النظام أولاً)

### أ) مخاطر صحة البيانات والعمليات (الأولوية القصوى)

1. **تحديث موقع السائق غير محروس — فقدان تحديث وترتيب مقلوب.**
   `drivers.last_location` يُحدَّث بـ `UPDATE` غير مشروط بلا مقارنة زمنية (`packages/infrastructure/identity/directories.ts:162-175`). نبضة موقع قديمة تصل متأخرة تكتب فوق موقع أحدث. الأثر: مطابقة على موقع خاطئ، وخريطة راكب ترتد للخلف. **لا حماية اليوم.**
2. **إزالة التكرار (dedup) تسبق حد المعدل — فقدان تحديثات تيليجرام.**
   في `apps/gateway/src/routes/telegram-webhook.ts:219-237` يُسجَّل التحديث كـ«مرئي» ثم يُرفض بـ429. تيليجرام يعيد الإرسال، فيُهمَل التحديث نهائياً بوصفه مكرراً. **الأثر: فقدان عملية مستخدم صامت.**
3. **رفض العرض يشمل كل الصفوف المعلَّقة لـ`(order, driver)` بلا تحديد الجولة أو المُعرِّف** — يخلط جولات الإرسال.
4. **آثار جانبية للإشعارات خارج المعاملة** — لا صندوق صادر موحّد لكل الآثار، فقد ينجح الالتزام ويفشل الإشعار أو العكس.
5. **`openRound` يُدخل N صفوف تسلسلياً بلا قفل على صف الطلب ولا شرط `status='searching'`** (`packages/infrastructure/dispatch/dispatch-adapters.ts:197-217`) — فتحُ جولتَي إرسال متزامنتين لنفس الطلب غير ممنوع بنيوياً.
6. **حد المعدل على Redis غير ذرّي** — `INCR` ثم `EXPIRE` بلا Lua، ويفتح على الفشل (fail-open) عند تعطل Redis. وجلسات Redis بلا compare-and-set، فآخر كاتب يفوز ويضيع تقدّم الحوار.

### ب) مخاطر تمنع التوسع الأفقي بنيوياً

النظام **لا يمكن تشغيله بأكثر من نسخة بوابة واحدة اليوم** بدون فقدان صحة، لأن هذه الحالة كلها داخل العملية:

| الحالة داخل العملية | الأثر عند نسختين |
|---|---|
| `Map` إزالة تكرار التحديثات | كل نسخة ترى تكراراتها فقط → معالجة مزدوجة للتحديث نفسه |
| جلسات الحوار في الذاكرة (الافتراضي في الإنتاج) | خطوة الحوار تُفقَد إذا وصلت النسخة الأخرى |
| حد المعدل في الذاكرة | الحد الفعلي يُضرَب في عدد النسخ |
| ناقل أحداث التتبع | خريطة الإدارة ترى أحداث نسختها فقط |
| خريطة البثّ الحيّ للراكب (`tripId → messageId`) | تحديث الموقع الحيّ ينكسر عبر النسخ |
| سجل Prometheus | مقاييس مجزّأة لكل نسخة |
| جدول العامل | تكرار جدولة المهام (يخفّفه القفل الاستشاري جزئياً) |

### ج) مخاطر السعة والاعتماديات الخارجية

7. **معالجة ويبهوك تيليجرام متزامنة بالكامل داخل الطلب HTTP.** `telegram-webhook.ts:219-246` ينتظر `handler.handle()` كاملاً — عمل قاعدة بيانات + إرسال رسائل تيليجرام **متسلسل** — قبل الرد على تيليجرام. **لا طابور.** أي بطء في مزود خارجي يتحول فوراً إلى زمن استجابة ويبهوك ثم إلى إعادة إرسال من تيليجرام ثم إلى ضغط مضاعف.
8. **الإرسال الصادر إلى تيليجرام بلا طابور ولا إعادة محاولة ولا مهلة ولا حد معدل عام**، و`retry_after` **يُتجاهَل** (`packages/infrastructure/notification/telegram-api-sender.ts:47-94`).
9. **الإرسال (dispatch) يحمّل كل سائقي المدينة إلى الذاكرة** ثم يُرشّح ويُقيّم في Bun (`dispatch-adapters.ts:119-162`) — لا `ST_DWithin` ولا شرط `is_available` ولا `LIMIT` في SQL، وفهرس GiST غير مُستَخدَم. **التكلفة تنمو خطياً مع عدد سائقي المدينة لكل طلب.**
10. **ميزانية اتصالات القاعدة ≈ `5N + 10M`** (البوابة 5 افتراضياً، العامل 5 عمل + 5 قفل)، و`prepare: true` **يتعارض مع Supabase transaction pooler** بحسب تعليق الكود نفسه (`packages/infrastructure/db/client.ts:8,20-21`). لا متغير بيئة لضبط السقف.
11. **فهارس ناقصة لمسارات فعلية:** `orders(city,status,created_at)`، `orders(assigned_driver_id,status,matched_at)` للـ LATERAL في خريطة الإدارة، طلبات الراكب النشطة `(rider_id,status,created_at)`، العروض المعلَّقة على نطاق المدينة، حداثة `audit_log`. ولوحة الإدارة تنفّذ `COUNT`/`AVG` على التاريخ الكامل وخريطة حرارية بلا فهرس.
12. **مهل بلا قواطع:** OSRM 3 ثوان لمحاولتين، والمزودان الماليان 10 ثوان × 3 محاولات بلا backoff مُتدرِّج. تعطّل مزود يحتجز عاملين ويُطيل الطلبات.
13. **الهجرات ليست آمنة أونلاين:** لا `CREATE INDEX CONCURRENTLY`، ولا نمط backfill ثم `SET NOT NULL`، وقيود `CHECK` تُضاف بلا `NOT VALID`. أي هجرة على جدول كبير في الإنتاج = قفل طويل.
14. **لا تصريف رشيق (graceful drain) للـ HTTP عند `SIGTERM`**، و`/ready` يشغّل فحوصه **بالتسلسل وبلا مهل**.
15. **نقطة فشل واحدة معلنة:** نسخة بوابة واحدة تحمل الويبهوك والعامل ولوحة الإدارة، بلا autoscaling، على خطة `starter`.

### د) الخطر الأكبر: سقف تيليجرام نفسه

هذا ليس عنق زجاجة داخلياً — بل **سقف خارجي صلب لا يُتجاوَز بالكود**:

* Bot API لتيليجرام يحدّ الإرسال الصادر عند **~30 رسالة/ثانية لكل بوت**، و**~20 رسالة/دقيقة لكل مجموعة**.
* النظام يملك **بوتين** → سقف نظري ~60 رسالة/ثانية صادرة **للنظام كله**.
* مسار البثّ إلى «مجموعة السائقين» في كل مدينة يخضع لسقف المجموعة (~20/دقيقة)، وهو الأقسى.

**النتيجة الهندسية الصريحة: بالطوبولوجيا الحالية (بوتان)، لا يمكن لأي قدر من التحسين الداخلي أن يخدم 2M مستخدم متزامن،** لأن كل تفاعل يحتاج رسالة صادرة واحدة على الأقل. هذا **ليس رأياً في الكود** بل حدٌّ في المنصة المضيفة. ومعالجته قرار على مستوى المنتج لا مستوى التنفيذ، وسيُطرح في المرحلة 1 بخيارَين رئيسيَّين (تجزئة البوتات / نقل المسارات عالية التردد إلى Telegram Mini App). **لا يجوز الاستمرار في مراحل التوسع الداخلي دون حسم هذا القرار، وإلا صُرِف جهد على عنق زجاجة غير حاكم.**

---

## 5. أعناق الزجاجة المحتملة (مرتَّبة بترتيب الوصول المتوقَّع)

| # | عنق الزجاجة | الدرجة | لماذا |
|---|---|---|---|
| 1 | **Telegram Bot API** (~30 رسالة/ث لكل بوت، ~20/دقيقة لكل مجموعة) | **غير مقيس** — سقف موثَّق من المنصة | سقف خارجي صلب، يُبلَغ قبل أي حدٍّ داخلي |
| 2 | **نسخة البوابة الواحدة** (starter، عامل مضمن) | **غير مقيس على Render** | لا autoscaling، والعامل ينافس الويبهوك |
| 3 | **معالجة الويبهوك المتزامنة** | **مقيس محلياً فقط**: 2,045 تسجيل/ث محلياً بمُرسِل صامت | مع مُرسِل تيليجرام حقيقي، ينقلب الرقم إلى سقف تيليجرام |
| 4 | **استعلام الإرسال O(سائقي المدينة)** | **غير مقيس** | يتضخّم مع نمو المدينة، لا مع نمو الطلبات فقط |
| 5 | **تجمّع اتصالات القاعدة (5 لكل نسخة)** وسقف Supabase | **غير مقيس** | أول حد داخلي عند رفع النسخ |
| 6 | **كتابات موقع السائق** (كل 3 ثوان لكل سائق افتراضياً) | **غير مقيس** | 100 ألف سائق نشط ⇒ ~33 ألف كتابة/ث على `drivers` |
| 7 | **جداول بلا تقسيم ولا استبقاء** | **غير مقيس** | تدهور تدريجي غير قابل للتراجع بسهولة |
| 8 | **Upstash Redis عبر REST** (رحلة شبكة لكل جلسة/حد) | **غير مقيس** | يضيف زمناً لكل تحديث عند التوسع |
| 9 | **OSRM ومزودو الدفع** بلا قواطع | **غير مقيس** | تعطّلهم يحتجز عاملي المعالجة |
| 10 | **لوحة الإدارة**: تجميعات على التاريخ الكامل | **غير مقيس** | استعلام إداري واحد قد يُشبِع القاعدة |

---

## 6. ما يجب إثباته قبل أي ادّعاء بخدمة 2M مستخدم متزامن

**أولاً: تعريف الرقم.** «2M مستخدم متزامن» في نظام بوت تيليجرام **ليس 2M اتصالاً مفتوحاً** — تيليجرام يحمل اتصالات المستخدمين، ونحن نستقبل تحديثات HTTP. لذا يجب ترجمة الهدف إلى نموذج حمل قابل للقياس. هذا **لم يُعرَّف بعد** وهو أول مخرج المرحلة 1. النموذج المبدئي المطروح للنقاش:

| البُعد | الترجمة المبدئية (افتراض يحتاج إقراراً) |
|---|---|
| تحديثات واردة | 2M مستخدم × تفاعل كل ~30 ثانية ⇒ **~66,000 تحديث/ثانية** |
| رسائل صادرة | ≥ تحديث واحد صادر لكل وارد ⇒ **~66,000 رسالة/ثانية** مقابل سقف ~60/ث ببوتين ⇒ الحاجة إلى **أكثر من 1,000 رمز بوت** أو نقل المسار إلى Mini App |
| نبضات موقع السائق | 100–200 ألف سائق نشط × كل 3 ثوان ⇒ **33,000–66,000 كتابة موقع/ثانية** |
| ذروة الطلبات | تُشتق من النموذج بعد إقراره |

**ثانياً: قائمة ما يجب إثباته (كلها اليوم غير مثبتة):**

1. **صحة أولاً:** صفر عمليات حساسة مفقودة أو مكرَّرة تحت الحمل الكامل — لا في المطابقة، ولا في الدفع، ولا في الاشتراك، ولا في الإشعارات.
2. صفر حالة تسابق (race condition) قابلة للإظهار في أي مسار حساس، مع اختبار يُظهرها لو رجعت.
3. سلامة المعاملات: لا أثر جانبي خارج المعاملة يمكن أن ينفصل عنها.
4. صحة الإرسال والتتبع والمصادقة والجلسة **عبر أكثر من نسخة** في وقت واحد.
5. سلامة التواصل: كل رسالة ينبغي إرسالها تُرسَل مرة واحدة بالضبط، مع احترام `retry_after`.
6. تحمّل الأعطال: فقدان نسخة، فقدان Redis، فقدان مزود خارجي، إعادة نشر متدرج — بلا فقدان بيانات وبلا توقف كامل.
7. التعافي: زمن ومعيار استعادة معلنان (RTO/RPO)، مُثبَتان بتدريب دوري.
8. أداء مقبول: p95/p99 معلنان مسبقاً كمعيار نجاح، لا مُستخرَجان بعد القياس.
9. قابلية التوسع: علاقة موثَّقة بين عدد النسخ والسعة (لا سعة واحدة مقيسة على نسخة واحدة).
10. الاستقرار تحت حمل مستمر (soak) لساعات، بلا تسرّب ذاكرة أو اتصالات أو انزلاق تأخير.
11. **قياس على طوبولوجيا مماثلة للإنتاج** بمولّدات حمل موزَّعة، مع telemetry مركزية.

**حتى تتحقق كل بنود هذه القائمة، تبقى الجملة الصحيحة الوحيدة عن هذا النظام هي:** لم تُقَس سعته على الإنتاج، والحد الحاكم المعروف هو سقف Telegram Bot API.

---

## 7. ما تم التحقق منه فعلاً في هذه المرحلة

كل ما يلي شُغِّل فعلاً على الـ commit `ecb4518` في بيئة هذه الجلسة (Bun 1.3.14، PostgreSQL 18.4 + PostGIS محلي، قاعدة `waslah`):

| # | ما شُغِّل | النتيجة الفعلية |
|---|---|---|
| 1 | `bun run lint` | exit 0 — 39 ملاحظة معلوماتية، 0 خطأ |
| 2 | `bun run typecheck` | exit 0 |
| 3 | `bun run test` (المجموعة الكاملة) | **1,772 نجاح / 0 فشل**، 6,938 `expect()`، 144 ملفاً، **156.92 ثانية** |
| 4 | تطبيق الهجرات الـ57 بترتيب اسم الملف | نجحت كلها، بلا خطأ |
| 5 | `scripts/match-race-test.ts` سيناريو 1 (1,000 مطالبة → طلب واحد) | 0.36s، **2,745 مطالبة/ث**، وسيط 310.3ms، p95 355.4ms، أقصى 363.8ms — **فائز واحد**، 999 `ORDER_NOT_CLAIMABLE`، 0 استثناء، عرض مقبول واحد، 0 معلَّق، 999/999 مُلغى، سجل تدقيق واحد |
| 6 | `scripts/match-race-test.ts` سيناريو 2 (100 طلب × 10) | 0.21s، **4,816 مطالبة/ث**، وسيط 139.1ms، p95 202.7ms — 100/100 فائز واحد، لا عرضان مقبولان، لا طلب يتيم |
| 7 | `scripts/load-test.ts` | **فشل — لم يعمل.** `TypeError` في `packages/tracking/config.ts:26` لأن السكربت يبني `AppConfig` ناقصاً 9 حقول |
| 8 | إضافة `scripts/**/*.ts` إلى `tsconfig.json` تجريبياً | **4 أخطاء أنواع في 3 ملفات** — أُعيدت `tsconfig.json` إلى أصلها؛ الإصلاح مؤجَّل إلى المرحلة 2 |
| 9 | تدقيق قراءة ساكنة كامل لثلاث طبقات (القاعدة، وقت التشغيل، البنية التحتية والاختبار) بأرقام أسطر | مُنجَز — نتائجه هي مادة §3 و§4 و§5 |

**تصنيف هذه النتائج:** البنود 1–4 **مَقيسة/مُتحقَّق منها** لبيئة التطوير. البندان 5–6 **مَقيسان** لصحة التزامن على قاعدة حقيقية، **وليسا** إثباتاً لسعة إنتاج (بلا HTTP ولا TLS ولا تيليجرام ولا نسخ متعددة). البندان 7–8 **فشل مُسجَّل**.

---

## 8. ما لم يُتحقَّق منه (بصراحة)

1. **أي شيء على Render أو Supabase أو Upstash الفعلية.** لم يُشغَّل شيء على الإنتاج أو على بيئة مماثلة له في هذه المرحلة.
2. **سلوك النسخ المتعددة.** لم تُشغَّل نسختا بوابة في وقت واحد قطعياً. *(زال هذا الحدُّ جزئيّاً في وحدة 2-6 — §13.10: شُغِّلت بوّابتان في عمليتين مستقلّتين بجلسةٍ مشتركةٍ وقاعدةٍ واحدة، وأُعيدت سيناريوهاتُ العملِ عبرهما. وما يبقى: نسختان على **مضيفَين** وعلى Upstash/Supabase الحقيقيّتين.)*
3. **الحلقة الكاملة مع تيليجرام الحقيقي** — لا زمناً ولا سقفاً ولا سلوك `retry_after`.
4. **سعة العامل الخلفي** ومقدار تنافسه مع الويبهوك.
5. **سقف تجمّع الاتصالات الفعلي** وسلوك القاعدة عند إشباعه.
6. **لوحة الإدارة تحت حمل.**
7. **المزودون الخارجيون** (Tap، Moyasar، OSRM، Google Drive، الترجمة) — لا throughput ولا سلوك عند 429/تعطّل.
8. **حمل مستمر (soak)** لساعات، ولا telemetry للموارد (CPU/RSS/GC/FD/اتصالات/أقفال).
9. **Chaos**: قتل نسخة، فقدان Redis، تأخير شبكة، إشباع اتصالات، نشر متدرج مع طلبات حيّة. *(جُرِّب منها في وحدة 2-6 — §13.10: قتلُ عمليّةِ بوّابةٍ بـ`SIGKILL` وسطَ مسارِ عمل، وسقوطُ اتّصالِ حاملِ قفلٍ استشاريّ. وما يبقى: فقدانُ Redis، وتأخيرُ الشبكة، وإشباعُ الاتصالات، والنشرُ المتدرّج.)*
10. **صحة كتابات الموقع تحت تزامن** — أعلى مخاطر صحة البيانات المذكورة في §4-أ-1 **لم يُبنَ لها اختبار سباق بعد**.
11. **الأرقام المنشورة في `docs/load-test-report.md`** — رقم التسجيل منها **مُبطَل صراحةً** (التحذير الثالث في §13.2)، وبقيّة الأرقام لم تُعَد إنتاجها بعد على الـ commit الحالي: الأداة صارت تعمل، لكن إعادة الإنتاج تنتظر منصّة القياس (الوحدات 2-3-4).

---

## 9. المراحل المختارة (وأسباب هذا الترتيب) — **Historical Snapshot — superseded by ROADMAP-MASTER v1.3 (F0–F12)**

> **لقطةٌ تاريخية — لا تُقرَأ كخطةٍ نافذة.**
> إطارُ «المراحل 0…9» أدناه **استبدله إطارُ `F0`…`F12`** في `docs/ROADMAP-MASTER.md` الإصدار 1.3، وهو **الإطارُ الحاكمُ الوحيدُ للمراحل ومعاييرِ القبول والترتيب اليوم**.
> ويُحفَظ هذا القسم كما هو لأنّه يوضّح **لماذا** رُتّبت الأولويات على هذا النحو، ولأنّ منطقَ الأولويات (الصحة قبل التوسع قبل الرقم) **ما زال نافذاً**.
> **وتصحيحٌ واجبٌ للمرحلة 1 أدناه:** مسألةُ «سقف تيليجرام — تجزئةُ بوتات مقابل Mini App» **لم تعد مفتوحة**: حُسِمت بـADR 0028 (Mini App معتمدٌ معمارياً) وADR 0031 (تيليجرام ليست تبعيةً وجودية). وحالةُ التنفيذ: **غيرُ منفَّذ إطلاقاً** (القسم S3).

الترتيب يتبع أولويات المشروع المعلنة: **الصحة، ثم سلامة البيانات، ثم الفهم، ثم القابلية للقياس، ثم التوسع، ثم إثبات الأداء، ثم الاستقرار، ثم التعافي، ثم الرقم.** ولا تُصرَف جهود توسع قبل أن يوجد ما يقيسها وما يحسم سقفها الخارجي.

| المرحلة | العنوان | المخرج القابل للتحقق | معيار الانتقال |
|---|---|---|---|
| **0** | **الفهم ونقطة البداية الموثقة** | هذه الوثيقة + baseline مقيس + تدقيق ثلاث طبقات | ✅ **مكتملة** — دُفعت مع هذا الـ commit |
| **1** | **نموذج الحمل وحسم السقف الخارجي** | وثيقة نموذج حمل رقمية لـ«2M متزامن»، ومعايير نجاح مُعلَنة مسبقاً، وADR يحسم مسألة سقف تيليجرام (تجزئة بوتات مقابل Mini App) | إقرار المالك للنموذج والقرار — بلا هذا الإقرار، بقية المراحل تبني على رقم غير معرَّف |
| **2** | **استعادة محيط القياس** | `scripts/**` داخل `typecheck` وCI، إصلاح `load-test.ts` وأخطاء الأنواع الأربعة، ربط سكربتات القياس بـ`package.json`، وحفظ نتائج القياس كـartifact | `load-test.ts` و`match-race-test.ts` يعملان ويُشغَّلان من CI، وأرقام `load-test-report.md` تُعاد إنتاجها أو تُصحَّح |
| **3** | **إصلاح صحة البيانات (قبل أي توسع)** | مقارنة-وتعيين (CAS) لموقع السائق، نقل dedup إلى ما بعد حد المعدل، تحديد الجولة في رفض العرض، قفل صف الطلب في `openRound`، حد معدل Redis ذرّي بـLua، جلسات Redis بـCAS، صندوق صادر موحّد للآثار الجانبية | اختبار سباق **يفشل قبل الإصلاح وينجح بعده** لكل بند — لا إصلاح بلا اختبار يُظهر الخلل |
| **4** | **إزالة الحالة داخل العملية** | نقل الجلسات وdedup وحد المعدل وناقل الأحداث وخريطة البثّ الحيّ إلى مخزن مشترك | تشغيل **نسختَي بوابة** فعلاً خلف موزّع، وإثبات صحة الحوار والتتبع والحد عبرهما |
| **5** | **فصل الاستقبال عن المعالجة** | طابور دائم: الويبهوك يُقر بالاستلام سريعاً ويعالج لاحقاً؛ وطابور صادر لتيليجرام باحترام `retry_after` وإعادة محاولة ومهلة وحد معدل عام | إثبات: صفر تحديث مفقود عند إسقاط النسخة أثناء المعالجة، وصفر تجاوز لسقف تيليجرام |
| **6** | **توسّع طبقة البيانات** | دفع تصفية الإرسال إلى SQL (`ST_DWithin` + `is_available` + `LIMIT`)، الفهارس الناقصة، تقسيم الجداول النامية + استبقاء، ميزانية اتصالات معلنة + متغير بيئة، حسم وضع pooler و`prepare` | قياس قبل/بعد يُظهر تحوّل زمن الإرسال من O(سائقي المدينة) إلى ~ثابت |
| **7** | **الصمود والتعافي** | قواطع دائرة وفواصل للمزودين، تصريف رشيق عند `SIGTERM`، `/ready` بمهل ومتوازٍ، `request-id` وتتبّع موزّع، هجرات آمنة أونلاين، مراقبة وتنبيهات فعلية داخل المستودع، SLO وRTO/RPO | تجارب chaos موثقة وقابلة للتكرار: قتل نسخة، فقدان Redis، تعطّل مزود — بلا فقدان بيانات |
| **8** | **بنية القياس الحقيقية** | بيئة مماثلة للإنتاج بـIaC، مولّدات حمل موزَّعة بـramp/pacing/traffic-mix، telemetry مركزية زمنية، بوابات أداء في CI | إعادة إنتاج قياس واحد مرتين بنتيجة متقاربة على الطوبولوجيا الحقيقية |
| **9** | **إثبات السعة الحقيقية والإعلان الصادق** | تشغيل تصعيدي مُوثَّق حتى نقطة الانهيار، وتحديد الرقم الحقيقي | يُعلَن **الرقم المقيس فعلاً**. إن كان 500 ألف أو مليوناً أو 1.7 مليون، يُقال ذلك حرفياً. ولا تُكتب كلمة «2M» إلا مع اختبار قابل لإعادة الإنتاج ومعايير نجاح مُعلَنة |

**قاعدة الانتقال:** لا تبدأ مرحلة قبل أن يكون معيار انتقال سابقتها مُحقَّقاً ومُسجَّلاً في §11 من هذه الوثيقة بدليل.

---

## 10. المرحلة الحالية — **Historical Snapshot — superseded by ROADMAP-MASTER v1.3 (F0–F12)**

> **لقطةٌ تاريخية — هذا لم يعد وصفَ المرحلة الحالية.**
> **المرحلةُ الحاليةُ النافذة: بوابة `F0` = NO-GO بسبب `DEC-11` وحده، و`F1` لم يبدأ** — القسم S2 و`ROADMAP-MASTER` v1.3.
> ووحداتُ «المرحلة 2» أدناه تُقرَأ **سجلاً لما أُنجِز فعلاً** — وهي إنجازاتٌ حقيقيةٌ لم تُلغَ — **لا خطةً قادمة**. وعبارةُ «المرحلة 1 موقوفةٌ على قرار المالك» **متجاوزة**: فقراراتُ السقف ونموذجِ الحمل صدرت بـADR 0028–0041، والمحجوبُ الوحيدُ اليوم هو `DEC-11` وسببُه **قانونيٌّ تنظيميٌّ لا إداري**.

**المرحلة 0 — الفهم ونقطة البداية الموثقة: مكتملة.**

**المرحلة 2 — استعادة محيط قياس موثوق: جارية.** تقدّمها بالوحدات:

| الوحدة | ما فيها | الحالة |
|---|---|---|
| 1 | إدخال `scripts/**` و`bench/**` في محيط فحص الأنواع، وإصلاح أعطاب `load-test.ts` من جذرها حتى صار **يعمل ويحكم على صحة ما قاسه** | **مكتملة ومقيسة** (§13.3) |
| 2-2 | قلعُ تكرار عقد `AppConfig` من ٢٩ ملف اختبار إلى مُعينٍ واحد — شرطٌ سابق لمنصّة القياس، لأن كلّ حقلٍ جديد في الضبط كان يكلّف ٢٩ تعديلاً | **مكتملة ومُختبَرة** (§13.4) |
| 2-3 | ناقلُ صادرٍ مُعلَن في الضبط (`silent` مرفوض في الإنتاج) وعدّادُ الرسائل الصادرة في المقاييس | **مكتملة ومُختبَرة** (§13.5) |
| 2-4 | **أساسُ منصّة القياس**: عزلُ قاعدةٍ مخصّصة، ٥٧ ترحيلاً، بذرٌ حتميّ، وإثباتُ أنّ `reset → seed → reset → seed` يُعطي الحالةَ المنطقيّةَ نفسَها | **مكتملة ومُختبَرة** (§13.6) |
| 2-4.1 | تحقّقٌ مستقلٌّ من الأساس على بيئةٍ من الصفر، وإصلاحُ قابليّةِ نقلِ البصمة | **مكتملة ومُختبَرة** (§13.7) |
| 2-5 | سيناريوهاتُ عملٍ حقيقيّة بمعايير نجاحٍ على مستوى العمل، تحقّقٌ من حالة القاعدة، ومقاييسُ وأدلّةٌ محفوظةٌ قابلةٌ للمقارنة | **مكتملة ومقيسة، وبشاهدٍ سالب** (§13.9) |
| 2-6 | **تحقُّقٌ موزَّع**: طبولوجيا أقربُ للإنتاج (بوّابتان في عمليتين، جلسةٌ وحدُّ معدّلٍ مشتركان، عاملان مستقلّان، قاعدةٌ واحدة)، وإعادةُ سيناريوهاتِ 2-5 عبرها، وتزاحمٌ وحتميّةٌ عبر العمليات، واختبارُ فشلٍ بقتلِ عمليّة | **مكتملة ومقيسة** (§13.10) |
| 3 | مولّد حمل مفتوح الحلقة بمعدّل ورود، في عملية منفصلة عن الخادم، يكشف انزلاق جدولة المولّد ويُبقي الطلبات البطيئة قيد التنفيذ | **مكتملة ومُختبَرة** (§13.11) |
| 4 | تشغيل أصغر baseline موثوق وإثبات قابليته لإعادة الإنتاج | لم تبدأ |
| 5 | وثيقة نموذج الحمل ومسودّة ADR لسقف تيليجرام (**بلا قرار**) | لم تبدأ |

**المرحلة 1 (نموذج الحمل وحسم السقف الخارجي)** ما تزال **موقوفة على قرار المالك**، لأن مخرجها الأساسي قرارُ منتج لا قرارُ تنفيذ. المرحلة 2 **لا تتوقف على هذا القرار**.

---

## 11. ما تم إنجازه (سجل تراكمي)

> **ملاحظةُ نطاق (2026-08-26):** هذا السجلُ **محفوظٌ وصحيحٌ تاريخياً**، ولكنّه **يتوقف عند وحدة 2-3**، ولا يحتوي وقائعَ `F0`. ووقائعُ `F0-01`…`F0-06` وقرارُ البوابة **تُسجَّل في سجلِ التنفيذ في `docs/ROADMAP-MASTER.md`** وفي `docs/evidence/architecture/`، لا هنا. وملخّصُ الحالة النافذ في القسم S2.

| التاريخ | الـ commit | المرحلة | ما أُنجز | الدليل |
|---|---|---|---|---|
| 2026-08-26 | (وحدة 2-3) | 2 | **مولّد وصول مفتوح مستقل عن البوابة:** نواةٌ تحدّد الوصول من معدل/مدة ولا تنتظر إكمال الطلب السابق؛ تقريرٌ يضم معدل الإطلاق، `maxInFlight`، انزلاق الجدولة، زمن النقل، حالات HTTP وأخطاء النقل؛ CLI لا يستورد التطبيق ولا يقلع بوابةً ويحفظ JSON وMarkdown؛ اختبارٌ تشغيلي قلَع خادماً حقيقياً ثم شغّل المولّد في PID مختلف عبر TCP وقرأ دليله؛ حزمة CI كاملة: **1,443 نجاح / 514 تخطٍّ / 0 فشل / 4,617 توكيداً / 151 ملفاً** | §13.11 |
| 2026-08-19 | (مصالحةُ Git — الثانية) | 1 | **حلُّ تعارضِ الطلب #13 من جذرِه لا من ظاهره:** تشخيصٌ بالأوامر أثبت أنّ `main` بعد دمجِ #1 و#12 بـsquash يساوي `45818e1` زائدَ وثيقةِ #1 وحدَها، وأنّ ما ينقصه ٢٠ ملفاً كلُّها وحدة 2-5؛ فحُلَّ الملفّان المتعارضان باعتمادِ نسختِنا لأنّها تحتوي نسختَ `main` وتزيد، مع إثباتِ أنّ إضافاتِ #1 محفوظةٌ بفرقٍ صفريّ؛ والبوّاباتُ كلُّها خضراءُ بعد الدمج (1,862 نجاحاً / 0 فشل) | §13.8 |
| 2026-08-19 | (وحدة 2-6) | 2 | **تحقُّقٌ موزَّع للنتائجِ التجاريّة:** طبولوجيا من عمليّاتٍ حقيقيّة (بوّابتان بمنفذَين ومقبسِ TCP حقيقيّ، مزدوجُ Upstash REST لجلسةٍ وحدِّ معدّلٍ مشتركين، عمليّتا عاملٍ على حاجزٍ زمنيّ، قاعدةٌ واحدةٌ معزولة)؛ منفذُ `ScenarioEnvironment` جعل سيناريوهاتِ 2-5 تعمل موزَّعةً **بلا تعديلِ حرفٍ فيها**؛ **١١٢ توكيداً ناجحاً و0 إخفاق** في ٥ سيناريوهاتٍ وفحصَين؛ ثابتُ «ظافرٌ واحدٌ بالضبط» صحيحٌ والمتنافسون موزَّعون على عمليتين (جدولُ التوجيه محفوظ)؛ حتميّةٌ عبرَ العمليات بإعادةِ نفسِ `update_id` إلى عمليّةٍ أخرى (تعبُر مانعَ التكرارِ غيرِ المشترك ومع ذلك لا تكرارَ في الحالة)؛ ازدواجُ عاملين على ٤٨ مهمّةً بلا تقاطعٍ زمنيٍّ و٤٥ امتناعاً بقفلٍ، وإثباتُ أنّ الحاجزَ في القاعدةِ وأنّ سقوطَ الاتّصالِ يحرّر القفل؛ ضبطٌ سالبٌ يُثبت أنّ الجلسةَ المشتركةَ شرطٌ فعليّ؛ اختبارُ فشلٍ بـ`SIGKILL` لبوّابة؛ **وثلاثُ نتائجَ مكشوفةٌ موثَّقة**: عطبٌ في أداةِ القياس حكم بالإخفاق على نظامٍ سليم (أُصلح ومُختبَرٌ وحدةً)، وسلوكٌ خاطئٌ في `driver-dialog` يخبر الظافرَ أنّه خسر (مُبلَّغٌ لا مُصلَح)، وتصحيحُ ثابتٍ خاطئٍ كتبتُه عن معنى القفلِ الاستشاريّ | §13.10 |
| 2026-08-19 | (وحدة 2-5) | 2 | **سيناريوهاتُ عملٍ حقيقيّةٌ يُحكَم فيها بنتيجةِ العمل لا بكودِ HTTP:** عقدٌ من ١٣ بنداً واجبَ الإعلان يرفض السيناريوَ الناقص قبل تشغيله؛ مشغّلٌ `decideVerdict` **لا يستقبل كودَ HTTP في توقيعه أصلاً**؛ ٣ سيناريوهات (تسجيلُ سائق 10 فاعلين، طلبُ رحلةٍ وبثُّه 5، **تسابقُ ٤ سائقين على طلبٍ واحد**) بـ**٦٦ توكيداً تجاريّاً ناجحاً و0 إخفاق**؛ إثباتُ الذرّية بثلاثةِ شواهدَ (عرضٌ مقبولٌ واحد، إسنادٌ لصاحبِه، وعدّادٌ تحرّك ١ لا ٤) وحتميّةٌ عند إعادةِ الضغط؛ **شاهدٌ سالبٌ** بثلاثةِ إفساداتٍ مقصودة: اثنان كُشِفا والثالثَ رفضته القاعدةُ بقيدِ `order_offers_single_accepted`؛ ٢٣ اختباراً للإطارِ نفسِه بلا قاعدةٍ فتجري في CI؛ وتصحيحُ توكيدٍ خاطئٍ كتبتُه (سطرُ الانتظارِ أربعةُ بدائلَ لا مفتاحٌ واحد) | §13.9 |
| 2026-08-19 | (مصالحةُ Git) | 2 | **تصفيرُ حالة المستودع:** فحصُ الفروع الثمانية ومراجعِ `refs/pull/*` وحالةِ طلبات الدمج من واجهة GitHub؛ وحلُّ تعارضَي `add/add` في الطلبَين المفتوحَين #12 (`docs/SYSTEM_STATE.md` — أثرُ دمج squash، حُلَّ بصفرِ تغييرٍ في المحتوى بإثباتِ تطابقِ الفرق) و#1 (`PHASE-0-BASELINE.md` — حُلَّ سطراً سطراً: نثرُ `main` صحيحٌ والاقتباسُ البرمجيُّ أُعيد إلى نصّ الكود، ووُسمت الوثيقةُ لقطةً تاريخيّة)؛ والحزمةُ الكاملة تنجح على الفرعين بلا تخطٍّ؛ وصار الطلبان `mergeable = true` | §13.8 |
| 2026-08-19 | (وحدة 2-4.1) | 2 | **تحقّقٌ مستقلٌّ من وحدة 2-4 على بيئةٍ بُنيت من الصفر** (١٥/١٥ فحصاً، الحزمةُ الكاملة، lint، typecheck) ⇒ الوحدةُ صحيحةٌ في جوهرها؛ **وكشفُ عيبٍ حقيقيّ**: بصمةُ الحالة كانت مرتبطةً بالقاعدة لأنّ الترحيلاتَ تبذر `cities`/`platform_settings` بمعرّفاتٍ عشوائيّة، فحالتان منطقيّتان متطابقتان تُعطيان بصمتين. أُضيفت `portableDigest` بتقييس المعرّفات المُسنَدة، و`identityOnly` في المقارنة، و`bench/compare-snapshots.ts` مع `BENCH_STATE_JSON`؛ وأُثبتت قابليّةُ النقل بإسقاط قاعدة القياس وإعادة ترحيلها (٧/٧ بصماتٍ منقولةٍ متطابقة، فروقٌ منطقيّة = 0)؛ وتصحيحُ انحرافٍ في `bench/README.md` (سكربتُ ترحيلاتٍ غيرُ موجود) | §13.7 |
| 2026-08-19 | (وحدة 2-4) | 2 | إنشاء `bench/`: حرسُ عزلٍ بقائمة سماحٍ يرفض الإنتاجَ والمضيفَ المُدارَ والبعيدَ وأيَّ قاعدةٍ غير `waslah_bench`، ويسأل `current_database()` بعد الاتّصال لا الوجهةَ وحدها؛ قاعدةُ `waslah_bench` بـ**٥٧ ترحيلاً مطبَّقاً**؛ بذرٌ حتميّ بلا `random` ولا `now()` ولا `on conflict`؛ `reset` بثلاثة حروسٍ تُغلَق على الفشل ولا تحذف عند الشكّ؛ بصمةُ حالةٍ قابلةٌ للمقارنة الآليّة تشمل الجداولَ المحفوظة؛ **١٥/١٥ فحصاً في سلسلة الإثبات، وتطابقُ البصمات بين عمليّتين منفصلتين**؛ ٣٢ اختباراً جديداً | §13.6 |
| 2026-08-19 | (وحدة 2-3) | 2 | إضافة `TELEGRAM_TRANSPORT` إلى الضبط بناقلين (`real` افتراضاً، و`silent` **مرفوض في الإنتاج** رفضاً قاطعاً عند الإقلاع)؛ إضافة مقياس `waslah_telegram_messages_sent_total{bot,kind}` — أوّل رؤية للنظام لما **يخرج** منه من رسائل؛ لفُّ العدّاد حول الناقل الحقيقي أيضاً لا الصامت وحده؛ 11 اختباراً جديداً | §13.5 |
| 2026-08-19 | (وحدة 2-2) | 2 | استبدال ٢٩ نسخةً يدويّةً من عقد `AppConfig` في الاختبارات بمُعينٍ واحد `tests/support/config.ts`؛ حَفِظ كلَّ فرقٍ حقيقيّ صريحاً في موضع الاستدعاء (المنفذ، سرّ الويبهوك، معرّف المشرف، مخزن الجلسات، تشغيل العامل)؛ **لا تغيير سلوك**: ١٧٧٢ اختباراً تنجح كما كانت | §13.4 |
| 2026-08-18 | (وحدة 2-1) | 2 | إدخال `scripts/**` و`bench/**` في `tsconfig.json`؛ إصلاح 4 أخطاء أنواع في 3 ملفات؛ إعادة بناء ضبط `load-test.ts` من محمّل الضبط الحقيقي؛ إكمال حوار التسجيل إلى خطواته التسع؛ جعل الأداة تحكم من جسم الرد وحالة قاعدة البيانات لا من كود HTTP؛ **إبطال رقم التسجيل التاريخي** | §13.3 و§13.2 |
| 2026-08-18 | `0a4b243` | 0 | تدقيق قراءة ساكنة كامل لثلاث طبقات؛ تشغيل lint/typecheck/الاختبارات الكاملة/الهجرات/سباق المطابقة؛ اكتشاف عطب `load-test.ts` وسببه الجذري؛ تحديد سقف Telegram كحدٍّ حاكم؛ إنشاء هذه الوثيقة وخطة المراحل والقاعدة الحاكمة للتوثيق | §7 من هذه الوثيقة |

---

## 12. ما بقي

> **تصحيحاتٌ واجبة (2026-08-26) — تسبق ما أدناه عند التعارض:**
> * **«المراحل 1 إلى 9» تُقرَأ اليوم `F1`…`F12`** في `ROADMAP-MASTER` v1.3.
> * **«حسم سقف Telegram Bot API» لم يعد موقوفاً على المالك — حُسِم بـADR 0028 وADR 0031.** وما بقي منه **تنفيذٌ لا قرار**.
> * **«وحدة 2-4 التالية» لم تعد الخطوةَ التالية.** الخطوةُ التاليةُ محكومةٌ بـ`ROADMAP-MASTER`، والبوابة `F0` = NO-GO بسبب `DEC-11`.
> * **«دمج طلبَي الدمج #1 و#12» متجاوز: لا توجد اليوم أيُّ طلبات دمجٍ مفتوحة (صفر)**، و`main` هو المرجع الوحيد.
> * **وما زال قائماً بحرفه من هذا القسم:** سلوكُ `driver-dialog` الخاطئ لم يُصلَح · وتجميعُ العدّادات عبر النسخ لم يُنجَز · والحملُ الحقيقيُّ لم يُقس · وقابليةُ نقل البصمة لم تُقس · وإصلاحاتُ صحة البيانات في §4-أ لم تبدأ.

* **المراحل 1 إلى 9 بكاملها** (§9).
* **بند موقوف على المالك:** حسم سقف Telegram Bot API — لا يمكن للجهد الهندسي أن يتجاوزه.
* **إصلاحات صحة بيانات محددة لم تبدأ بعد:** كل بنود §4-أ.
* **تصحيح انجراف الوثائق التاريخية** أو وسمها صراحةً كلقطات (§3 البند 13).
* **وحدة 2-5: أُنجزت** (§13.9). وما بقي منها موصوفٌ صريحاً: السيناريوهاتُ لا تجري في CI بعد (تحتاج قاعدةَ قياسٍ في الوظيفة؛ اختباراتُ الإطارِ نفسِه تجري)، ولا تغطّي بعد: الإلغاءَ، والتفاوضَ، والتصعيدَ إلى مجموعاتٍ، والدفعَ والاشتراكَ المنتهي، ورحلةً كاملةً حتى التقييم.
* **وحدة 2-6: أُنجزت** (§13.10). وما بقي منها صريحٌ: لا تجري في CI (تحتاج قاعدةَ قياسٍ في الوظيفة؛ واختباراتُ مكوّناتِ الطبولوجيا الـ١٨ تجري)، وبوّابتان على حاسوبٍ واحدٍ ليستا عنقودَ إنتاج، ولا شيءَ فيها يُثبت سلوكَ Upstash ولا Supabase الحقيقيَّين.
* **وحدة 2-3: أُنجزت** (§13.11). وما بقي منها صريحٌ: المولّد لا يعرّف مزيج حركة المنتج ولا معيار نجاحٍ ولا خط أساسٍ قابل للمقارنة؛ ونجاح HTTP ليس حكماً تجارياً، ولم يُشغّل على بوابة المنتج ولا على طوبولوجيا إنتاج.
* **الوحدة التالية 2-4 لم تبدأ:** أصغر baseline موثوق بمعيار نجاحٍ معلن، تشغيلين قابلين لإعادة الإنتاج، ومزيج حركةٍ لا يخلط صحة العمل بسعة النقل.
* **سلوكٌ خاطئٌ مكشوفٌ لم يُصلَح:** `driver-dialog` يخبر الظافرَ «سبقك سائق آخر» عند تسليمٍ مكرَّرٍ لضغطتِه (§13.10 والدليل) — الحالةُ التجاريّةُ سليمةٌ والنصُّ خطأ.
* **تجميعُ العدّاداتِ عبرَ النسخ** شرطٌ سابقٌ لمرحلةِ اكتشافِ السعة: العدّاداتُ في ذاكرةِ كلِّ عمليّة، وقراءةُ نسخةٍ واحدةٍ في عنقودٍ رقمٌ خاطئٌ بالبناء (§13.10).
* **حملٌ حقيقيٌّ لم يُقَس بعد:** صار مولّد الوصول يملك نوافذ إطلاق بمعدل معلن، لكن أكبرَ تشغيلٍ تجاري ما زال ١٩ فاعلاً وأكبرَ تزاحمٍ ٤ على موردٍ واحد؛ لم يُعلن مزيج حركة أو baseline ولا يُقفَز إلى 2M (§13.11 و§15).
* **قابليّةُ نقل البصمة بين مضيفين وإصدارَي PostgreSQL/PostGIS مختلفين** لم تُقَس بعد (§13.7).
* **دمجُ طلبَي الدمج #1 و#12 إلى `main`** — صارا قابلَين للدمج (§13.8)، والقرارُ للمالك.

---

## 13. النتائج والقياسات

### 13.1 قياسات هذه الجلسة (بيئة: Bun 1.3.14، 2 vCPU، PostgreSQL 18.4 + PostGIS محلي، بلا HTTP خارجي، بلا تيليجرام)

| القياس | القيمة | الدرجة |
|---|---|---|
| المجموعة الكاملة | 1,772 نجاح / 0 فشل / 144 ملفاً / 156.92s | مَقيس |
| مطالبة متزامنة على طلب واحد | 2,745 مطالبة/ث، p95 355.4ms، فائز واحد | مَقيس (صحة) |
| مطالبة موزَّعة على 100 طلب | 4,816 مطالبة/ث، p95 202.7ms، 100/100 صحيح | مَقيس (صحة) |
| `load-test.ts` | **فشل التشغيل** | فشل مُسجَّل |

### 13.2 قياسات تاريخية في المستودع (سياقها إلزامي، ولا تدل على سعة الإنتاج)

من `docs/load-test-report.md` — جهاز واحد 2 vCPU / 7 GiB، PostgreSQL محلي على نفس الجهاز، `SESSION_STORE=memory`، **مُرسِل تيليجرام صامت**، حد معدل مرفوع، بلا شبكة:

| المسار | الرقم |
|---|---|
| تسجيل سائق كامل (5 خطوات) | 2,045 طلب/ث — **مُبطَل، انظر التحذير الثالث** |
| `/status` | 3,412 طلب/ث |
| رفض سرّ ويبهوك خاطئ | 27,741 طلب/ث |
| `/ready` | 10,986 طلب/ث، ثابت عند 8.6ms تحت الحمل |
| 300 تسجيل متزامن | 0.73s |
| أخطاء 5xx | 0 من 7,600 طلب |

**تحذير إلزامي:** تقدير التقرير بأن نسخة Render `starter` تخدم **300–600 تحديث/ثانية** هو **تقدير صريح لا قياس** (`docs/load-test-report.md:125-136`). لا يُستشهد به كسعة.

**تحذير ثانٍ:** الأرقام أعلاه **غير قابلة لإعادة الإنتاج على الـ commit الحالي** حتى تُنجَز المرحلة 2.

**تحذير ثالث (اكتُشف في وحدة 2-1، وهو الأخطر):** رقم «تسجيل سائق كامل» **لم يكن يقيس تسجيلاً**. حوار التسجيل الحقيقي تسع خطوات لا خمس، والقياس كان يتوقّف عند الخطوة الخامسة، فلا يُكتب صفُّ سائقٍ واحد. وأسوأ من ذلك: القياس كان يتعطّل فعلياً عند **الخطوة الثالثة** (الجوال) لأنه يرسل بطاقة اتصال بلا `user_id`، والحوار يرفض بطاقةً لا يملكها المُرسِل — وهو قيدٌ أُضيف بعد كتابة القياس. فكان الجريان يمرّ في مسار «لم أفهم هذه الرسالة» ويُجيب `200` في كل مرّة، ويطبع «تسجيل كامل: 2,045 طلب/ث» و«0 أخطاء 5xx». أي أن الرقم يقيس **سرعة رفض الرسائل**، لا سرعة التسجيل. الدرس المعماري: **قياسٌ يحكم من كود HTTP وحده يعدّ الفشل الكامل نجاحاً كاملاً**، ولذلك صار الحكم في الأداة من جسم الرد ومن حالة قاعدة البيانات بعد الجريان.

### 13.3 قياسات وحدة 2-1 (بيئة: Bun 1.3.14، 2 vCPU، PostgreSQL 18.4 + PostGIS محلي، `SESSION_STORE=memory`، `RUN_WORKER_IN_GATEWAY=false`، مُرسِل تيليجرام صامت، حدّ المعدّل مرفوع)

| القياس | القيمة | الدرجة |
|---|---|---|
| `bun run lint` | خروج 0 (41 ملاحظة إرشادية) | مَقيس |
| `bun run typecheck` **بعد إدخال `scripts/**`** | خروج 0 | مَقيس |
| المجموعة الكاملة | 1,772 نجاح / 0 فشل / 6,938 تأكيداً / 144 ملفاً / 178.94s | مَقيس |
| `scripts/race-subscription-changes.ts` | خروج 0، كل السباقات سليمة | مَقيس (صحة) |
| `scripts/load-test.ts --users 20 --rounds 2` | خروج 0، `drivers = 20/20` مطابق، 0 خطأ 5xx، 0 خطأ جسمٍ داخل 200 | **مَقيس (صحة)** |

الأرقام الزمنية لهذا الجريان (20 مستخدماً × 2 دورات) **لا تُعتمد كـbaseline**، وسببُ ذلك بنيويٌّ لا ظرفيّ: المولّد يسكن عملية الخادم نفسها، والحمل دفعةٌ مغلقة الحلقة بلا معدّل ورود، والعيّنة صغيرة، والنتائج لا تُحفَظ بصيغة تُقارَن. هذه بالضبط وظيفة الوحدات 2 و3 و4.

**ما صار مضموناً بعد هذه الوحدة:** لا يمكن لحقلٍ جديد في `AppConfig` أن يكسر أدوات القياس صامتاً — فحص الأنواع صار يغطّي `scripts/**`. **وما لا يزال غير مضمون:** انجرافُ *سلوكٍ* لا *نوع* — كقيد `user_id` الذي كسر القياس بلا أي خطأ ترجمة. لا يمنعه إلا **تشغيل** القياس في CI، وهو مؤجَّل إلى ما بعد وحدة 3 حتى لا يُدخَل في CI مولّدُ حملٍ معروفُ العطب.

---

### 13.4 وحدة 2-2 — قلعُ تكرار عقد الضبط من الاختبارات

لماذا قبل منصّة القياس لا بعدها؟ لأن المنصّة تحتاج أن تُعلِن topology النظام، ومنها بديلٌ واحد مُعلَن للمُرسِل إلى تيليجرام؛ وإعلانه يعني حقلاً جديداً في `AppConfig`. وقياسُ أثر ذلك أعطى **٢٩ خطأ فحصٍ في ٢٩ ملف اختبار**: كلٌّ منها يكتب العقد بيده (٢٤ حقلاً)، متطابقةً إلا في حقلٍ أو حقلين.

وهذا **نفس صنف العطب** الذي أبطل قياس `load-test.ts` (§13.2)، بفارقٍ واحد حاسم: هذه النسخ التسع والعشرون **داخل محيط فحص الأنواع**، فلا تتعفّن صامتةً بل تسقط معاً. فالخطر ليس خطرَ كذبٍ في النتيجة، بل خطرُ أن تصير كلفةُ إضافة حقلٍ إلى الضبط ٢٩ تعديلاً — وكلفةٌ كهذه تدفع نحو الحلول الرخيصة (حقلٌ اختياري، قاسر `as`) التي تُضعف العقد نفسه.

| المقياس | قبل | بعد |
|---|---|---|
| مواضع تعريف عقد `AppConfig` للاختبار | ٢٩ | ١ (`tests/support/config.ts`) |
| أسطر الضبط في ملف اختبارٍ معتاد | ٢٤ | ٢–٤ (الفروق وحدها) |
| كلفة إضافة حقلٍ جديد إلى `AppConfig` | ٢٩ ملفاً | ١ ملف |

وما حفِظ التحويلُ من التسطيح: فحصٌ قبليّ لقيم كلّ نسخة أظهر أن `WEBHOOK_SECRET` **يختلف فعلاً** بين الملفات (١٢ قيمةً متمايزة)، وأن كلّ تجربةٍ تُرسل سرّها في ترويستها؛ فتوحيدُه كان سيجعل اختباراتٍ تتحقّق من سرٍّ غير الذي أُرسِل. فبقي صريحاً في موضع الاستدعاء، وكذلك المنفذ ومعرّف المشرف ومخزن الجلسات (`redis` في ملفٍ واحد) وتشغيل العامل (`true` في ملفٍ واحد) وحدود التتبّع.

| الفحص | النتيجة | الدرجة |
|---|---|---|
| `bun run typecheck` | 0 خطأ | مُتحقّق منه |
| `bun run lint` | خروج 0، 41 ملاحظة معلوماتية — **نفس عددها قبل التغيير** | مُتحقّق منه |
| `bun run test` (قاعدة حقيقية) | **1772 ناجح / 0 فاشل**، 6938 `expect()`، 144 ملفاً، 179.41ث | **مُختبَر** |

والعدد مطابقٌ لما قبل التغيير (§13.3)، وهو موضع الإثبات: إعادةُ هيكلةٍ لا تُغيّر سلوكاً لا يجوز أن تُغيّر عددَ ما ينجح ولا عددَ ما يُتحقّق منه.

**وما لا يزال غير مضمون:** لا حارس يمنع ملفاً جديداً من أن يكتب عقد `AppConfig` بيده مرّةً أخرى فيعود التكرار يزحف. ولم يُكتب حارسٌ له الآن لأن حارساً بلا استثناءاتٍ مدروسة يُعرقل اختبارات `tryLoadConfig` نفسها، وهي التي يجب أن تبني العقد يدوياً.

---

### 13.5 وحدة 2-3 — ناقلُ الصادر المُعلَن وعدّادُه

المشكلة التي حلّتها هذه الوحدة ليست مشكلة قياسٍ بل **عمىً في النظام نفسه**: كان يقيس ما *يدخل* عليه من تحديثات (`waslah_telegram_webhook_updates_total`) ولا يرى ما *يخرج* منه من رسائل. وهذا العمى في أخطر موضع، لأن الحدّ الخارجي الحاكم للمنصّة كلّها هو **معدّل الإرسال** لا معدّل الورود (§7)، فتخطيطُ السعة كان يجري على رقمٍ لا يُقاس أصلاً. فأُضيف `waslah_telegram_messages_sent_total{bot,kind}`، ويسري **في الإنتاج** لا في القياس وحده.

والقرار المعماري الثاني: كيف يُقاس النظام بلا أن يُرسل إلى تيليجرام فعلاً؟ البديل الذي رُفض هو **استنساخ نقطة الدخول** (515 سطراً) في ملفّ قياسٍ يُبدِّل فيها المُرسِل — وهو حرفياً صنف العطب الذي أبطل قياس `load-test.ts` (§13.2): نسخةٌ تتعفّن بصمتٍ خلف أصلها. فصار البديل حقلاً واحداً مُعلَناً في الضبط، والقياس يشغّل **الخادم الحقيقي نفسه**، ولا يختلف عنه إلا في بديلٍ واحدٍ مذكورٍ في topology كل تجربة.

وهذا الحقل خطرٌ بطبيعته: نظامٌ يقبل إسكات إشعاراته بمتغيّر بيئةٍ واحد يستطيع أن يخدع مستخدميه بخطأ ضبط. فالحرس ليس توصيةً في وثيقة بل **رفضٌ في `loadConfig` عند الإقلاع**: `TELEGRAM_TRANSPORT=silent` مع `NODE_ENV=production` يُسقط الإقلاع، وله اختبارٌ يحكم عليه.

| القرار | البديل المرفوض | السبب |
|---|---|---|
| العدّ **بعد** نجاح النداء | العدّ قبله | العدُّ قبل النداء يقيس «رسائل قرّرنا إرسالها»، وهو رقمٌ يكذب عند أوّل رفضٍ من تيليجرام أو تجاوزٍ لحدّ المعدّل — ونحن نقيس السقف الخارجي |
| العدّاد يلفّ الناقل الحقيقي أيضاً | يلفّ الصامت وحده | لو لفّ الصامت وحده لصار العدّاد أداةَ قياسٍ مُلحقة، والإنتاج يبقى أعمى عن صادره |
| معرّف مُصطنع ببادئة `silent-` | إعادة `null` | `null` يعني «لا أعرف المعرّف» فتمتنع فوقه تعديلاتُ الرسالة، فيقيس القياسُ نظاماً **أخفَّ** من الحقيقي — تشويهٌ صامت للنتيجة. والبادئة تمنع الالتباس، والناقل مرفوضٌ في الإنتاج فلا يصل معرّفٌ مُصطنع إلى مستخدم |
| وسم النوع (`message`/`photo`/`location`) | عدّاد واحد مجموع | إرسال الصور أثقل من النصّ زمناً وحملاً، وجمعهما يطمر فرقاً يهمّ من يقيس السقف |

| الفحص | النتيجة | الدرجة |
|---|---|---|
| `bun run typecheck` | 0 خطأ | مُتحقّق منه |
| `bun run lint` | خروج 0، 41 ملاحظة معلوماتية — نفس عددها قبل التغيير | مُتحقّق منه |
| حرّاس CI (`check-business-constants`, `check-env-drift`, `check-agent-core-isolation`, `check-i18n`, `check-schema-contract`) | كلّها OK | مُتحقّق منه |
| `bun run test` | **1783 ناجح / 0 فاشل**، 6967 `expect()`، 145 ملفاً، 190.66ث | **مُختبَر** |

الزيادة 11 اختباراً على 1772: خمسةٌ على الضبط (الافتراض، القبول خارج الإنتاج، رفض المجهول، **رفض الصامت في الإنتاج**، قبول الحقيقي فيه) وستةٌ على الناقل (العدّ بالنوع والبوت، الفصل بين البوتين، **عدم عدّ ما فشل مع إعادة رفع الاستثناء**، انعدام النداء الشبكي، تمايز المعرّف، قياس الصامت كالحقيقي).

**وما لا يزال غير مضمون:** العدّاد يقيس ما خرج من **هذه العملية**، ولا يعرف ما رفضه تيليجرام بحدّ معدّله (429) لأن المحوّلات فوقه تبتلع الفشل وتُعيد `false`. فقياسُ السقف الخارجي الفعلي يحتاج تمييز `429` عن غيره، وهو مؤجَّل إلى وحدة 5 حيث يُدرَس السقف نفسه.

---

### 13.6 وحدة 2-4 — أساسُ منصّة القياس: عزلٌ، ترحيلات، بذرٌ حتميّ، وإثباتُ إعادة الضبط

**السؤالُ الذي وُجدت الوحدةُ لتُجيبه:** هل أستطيع تنفيذ `reset + seed` مرّتين
والحصول على الحالة المنطقيّة نفسِها؟ ما لم يكن الجوابُ نعم بدليل، فكلُّ رقمٍ
يُقاس بعده قابلٌ للتشكيك — وذلك خطأٌ أخطرُ من خطأ ٢٠٤٥ طلب/ث السابق، لأن ذاك
اكتُشف بينما هذا يُصدَّق.

**ولماذا هذا الترتيب:** عزل → ترحيلات → تهيئة → بذر → إعادة ضبط. كلُّ حلقةٍ تعتمد
على ما قبلها، فبذرٌ حتميٌّ على قاعدةٍ ناقصةِ الترحيلات لا معنى له، و`reset` موثوقٌ
على قاعدةٍ غير معزولةٍ خطرٌ لا إنجاز. ولذلك توقف سلسلةُ الإثبات عند أوّل فشل:
نتيجةُ فحصٍ بُني على فحصٍ فاشلٍ قبله ليست نتيجة.

#### القرارات وبدائلها المرفوضة

| # | القرار | السبب الهندسي | البديلُ المرفوض ولماذا |
|---|---|---|---|
| 1 | **قاعدةٌ منفصلة `waslah_bench`** لا مخطّطٌ داخل قاعدة التطوير | فصلُ القاعدة يجعل خطأَ الوجهة مستحيلاً لا مستبعَداً | مخطّطٌ منفصل: `search_path` خاطئٌ واحدٌ يُوصل المحوَ إلى جداول التطوير |
| 2 | **قائمةُ سماحٍ** (`waslah_bench` على مضيفٍ محليّ) لا قائمةَ منع | قائمةُ المنع تُنسى: مضيفٌ سحابيٌّ جديدٌ لم يُدرَج فيها يمرّ | قائمةُ منعٍ للمضيفات المُدارة: تحرس الماضيَ لا المستقبل |
| 3 | **سؤالُ `current_database()` بعد الاتّصال** لا الاعتماد على الوجهة | الوجهةُ نيّةٌ لا واقع: مجمّعُ اتّصالاتٍ قد يُوصل إلى غيرِ ما كُتب | تحليلُ الوجهة وحده: يفحص ما نُوي لا ما جرى |
| 4 | **`truncate` بلا `cascade`** | `cascade` تعني «امحُ ما لم أذكره»، وهو نقيضُ نطاقٍ مُعلَنٍ محسوب | `cascade`: يُخفي جدولاً خارجَ الحساب بدل أن يكشفه |
| 5 | **جداولُ التشغيل تُستنبط من القاعدة**، والمُستثنى قائمةٌ صغيرةٌ يُتحقَّق منها | قائمةٌ مكتوبةٌ باليد لجدولٍ يُضاف لاحقاً تعني صفوفاً باقيةً بلا أن يشتكي أحد | تعدادُ الجداول يدويّاً: ينحرف عن المخطّط بأوّل ترحيلٍ جديد |
| 6 | **`verifyMigrationOwned`** يُثبت أنّ قائمةَ المُستثنى هي بالضبط ما تبذره الترحيلات | الاستثناءُ بلا إثباتٍ بابٌ لإبقاء صفوفٍ يظنّها الجميعُ محفوظة | الثقةُ بالقائمة: تصير خطأً صامتاً لحظةَ يتغيّر ترحيل |
| 7 | **ثلاثةُ حروسٍ قبل المحو**: الوجهة، ثمّ الملكيّة (`telegram_id ≥ 700000`)، ثمّ الاتّساق | الأداةُ لا تحذف إلّا ما تعرف أنّها أنشأته؛ وما لا تعرفه يوقفها | محوٌ بلا حرسٍ: يعمل حتى اليوم الذي تُشير فيه الوجهةُ إلى قاعدةٍ فيها بياناتُ ناس |
| 8 | **الشكُّ يُوقف ولا يمحو** (`FOREIGN_ROWS`, `UNEXPLAINED_ROWS`, `INCOMPLETE`) | فشلٌ مغلقٌ لا مفتوح: قياسٌ لم يجرِ مشكلةٌ تُحَل، وبياناتٌ مُحيت لا | تخطّي الصفوف الغريبة: يجعل الحرسَ إخباريّاً لا مانعاً |
| 9 | **البذر حتميّ**: `uuid` v5 مُشتقّ، وقتٌ ثابت `2026-01-01`، لا `random`، لا `now()` | حتميّةُ الهويّات شرطٌ لمقارنة تجربتين، ولحكمِ الحرس على الملكيّة | `gen_random_uuid()` و`now()`: تجربتان لا تُقارَنان أبداً |
| 10 | **لا `on conflict` في البذر** | التصادمُ خبرٌ يجب أن يُسقط البذر | `do nothing`: يجعل بذراً فوق بذرٍ يبدو ناجحاً وقد أنتج غيرَ المطلوب |
| 11 | **تفعيلُ المدن في التهيئة لا في البذر** | `cities` جدولٌ لا يمحوه `reset`، فتغييرُه كلَّ دورةٍ يُفقد سؤالَ «من أين بدأنا؟» جوابَه | التفعيلُ في البذر: يُبقي جدولاً محفوظاً متحرّكاً بين التجارب |
| 12 | **البصمةُ تشمل الجداولَ المحفوظة** (`cities`, `platform_settings`) | مدينةٌ عُطّلت أو إعدادٌ تجاريٌّ عُدِّل يُغيّر النتيجةَ جذريّاً ولا يظهر في جدولٍ تشغيليّ | تصويرُ التشغيليّ وحده: تجربتان «متطابقتان» بنتيجتين مختلفتين بلا تفسير |
| 13 | **`created_at`/`updated_at` مُستثنيان من البصمة** | يتغيّران لمرور الوقت لا لاختلافٍ منطقيّ | إدخالُهما: فشلٌ دائمٌ لا يُميّز شيئاً، أي عمليّاً لا فحص |
| 14 | **متغيّراتُ القياس ليست في `.env.example`** | `.env.example` عقدُ متغيّرات التطبيق، وحرسُ الانحراف يفحص `apps,packages,scripts,docker` فقط | إضافتُها: تُنتج «مُعلَنٌ ولا يُقرأ» فتُسقط الحرس |

#### درجات التحقّق

| الشيء | الدرجة | الدليل |
|---|---|---|
| حرسُ العزل يرفض الإنتاجَ والمضيفَ المُدارَ والبعيدَ والقاعدةَ غير المسموحة | **مُختبَر** — ١٢ حالةَ رفضٍ مستوفيةٌ لكلّ رمزِ فشلٍ مُعلَن | `tests/unit/bench-isolation.test.ts` (٢٣ اختباراً، بلا قاعدة، يجري في CI) |
| ترتيبُ الفحص: بيئةُ الإنتاج قبل كلّ شيء | **مُختبَر** | نفس الملفّ |
| قاعدةُ القياس منفصلةٌ فعلاً، والاتّصالُ عليها لا على نيّتها | **مُتحقَّق منه** — `current_database() = waslah_bench` | فحص 3 في سلسلة الإثبات |
| ٥٧ ترحيلاً مطبَّقاً بنجاح | **مُتحقَّق منه** — `applied=57`، خروج 0، ٣٧ جدولاً في `public` | فحص 4 + سجلّ التهيئة |
| قائمةُ ما تملكه الترحيلاتُ مطابقةٌ للواقع | **مُختبَر** | فحص 7 + اختبار التكامل |
| `reset` يُفرّغ كلَّ جدولٍ تشغيليٍّ ويُبقي المحفوظ | **مُختبَر** — ٠ صفٍّ تشغيليّ، ٥ مدنٍ باقية | فحص 11 + اختبار التكامل |
| صفٌّ لا يملكه القياس ⇒ رفضٌ **ولا يُحذف صفٌّ واحد** | **مُختبَر** — الاستثناءُ رُفع **والبصمةُ قبل وبعد متطابقة** | فحص 10 + اختبار التكامل |
| صفوفٌ تشغيليّةٌ بلا هويّةِ قياسٍ ⇒ `UNEXPLAINED_ROWS` | **مُختبَر** | `tests/integration/bench-reset-seed.test.ts` |
| البذرُ حتميّ: `reset+seed` ثلاثَ مرّاتٍ ⇒ الحالةُ نفسُها | **مُختبَر ومُتحقَّق منه** — فحصان ١٤ و١٥ + ثلاثُ دوراتٍ في الاختبار | سلسلةُ الإثبات + اختبار التكامل |
| الحتميّةُ **عبر عمليّاتٍ منفصلة** لا داخل عمليّةٍ واحدة | **مُتحقَّق منه** — بصمتا تشغيلين مستقلّين متطابقتان حرفيّاً، **على القاعدة نفسِها**؛ وقابليّةُ النقل بين قاعدتين موضوعُ 13.7 | `…-foundation-run1.txt` مقابل `…-run2.txt` |
| لا تضخّمَ في إعادة البذر | **مُختبَر** — ١٢٠ صفّاً ثابتةً بعد كلّ دورة | فحص 12 + اختبار التكامل |
| بذرٌ فوق بذرٍ يفشل ولا يترك أثراً | **مُختبَر** — المعاملةُ تُلغى والبصمةُ كما كانت | اختبار التكامل |
| الحزمةُ الكاملة | **مُختبَر** — **1,815 نجاح / 0 فشل**، 7,032 `expect()`، ١٤٧ ملفاً، 189.78ث | `…-tests.txt` |
| lint و typecheck والحرّاسُ التسعة | **مُتحقَّق منه** — خروج 0، و41 ملاحظةً معلوماتيّة (**نفسُ العدد**، فلا انحراف) | `…-guards.txt` |

#### ما لم يُثبَت، وما يجب ألّا يُقرأ من هذه الوحدة

1. **لا شيءَ عن القدرة أو الأداء.** لم يُقَس في هذه الوحدة طلبٌ واحدٌ ولا زمنُ
   استجابةٍ واحد. أزمنةُ `reset` (41ms) و`seed` (27ms) أزمنةُ أدواتٍ لا أزمنةُ نظام،
   ولا يجوز الاستشهادُ بها في باب الأداء.
2. **لا سيناريو عملٍ بعد.** البذرُ يُنشئ سائقين وعملاءَ واشتراكاتٍ مباشرةً في
   القاعدة، وهو **ليس** تسجيلاً عبر مسار المنتج. الحالةُ الابتدائيّةُ ليست سيناريو،
   والتحقّقُ على مستوى العمل موضوعُ وحدة 2-5.
3. **الاختبارُ المرتبطُ بقاعدةٍ لا يجري في CI** لعدم وجود قاعدةِ قياسٍ فيه، ويُتخطّى
   بإعلانٍ صريحٍ في السجلّ — واختبارٌ لم يجرِ ليس اختباراً نجح. أمّا منطقُ الحرس
   نفسُه فيجري في CI مع كلّ تغيير، لأن من يكسره لن يكون قد نوى تشغيل قياس.
4. **الحتميّةُ مُثبَتةٌ لخُطّةِ البذر الافتراضيّة** (٢٠ سائقاً، ١٠ عملاء) على
   PostgreSQL 18 محليّ. لم تُثبَت لخُططٍ أكبرَ ولا على مضيفٍ مُدار، وكلاهما قد
   يُدخل فروقاً (التزامن، الترتيبُ الافتراضيّ، إضافاتٌ مختلفة).
5. **`spatial_ref_sys`** جدولُ PostGIS، مُدرَجٌ في المحفوظ لأنّه فعلاً كذلك، لا
   لأنّه من بيانات المشروع.

---

### 13.7 وحدة 2-4.1 — تحقّقٌ مستقلٌّ من الأساس، وإصلاحُ عيبٍ في قابليّة نقل البصمة

**السياق:** جلسةُ تسليمٍ جديدةٌ لم تثق بما وُثِّق: أُعيدت البيئةُ من الصفر (Bun
1.3.14، PostgreSQL 18.4 + PostGIS 3.6.2، ٥٧ ترحيلاً على قاعدتَي `waslah` و
`waslah_bench`) وأُعيد تشغيلُ كلّ ما يدّعي الأساسُ إثباتَه. ونتيجةُ التحقّق أنّ
**وحدة 2-4 صحيحةٌ في جوهرها**: سلسلةُ الإثبات ١٥/١٥، والحتميّةُ ولا-التضخّمُ
ورفضُ المحو عند الهويّة الدخيلة كلُّها أُعيد إنتاجُها في تشغيلين مستقلّين.
وكُشف مع ذلك **عيبٌ واحدٌ حقيقيّ** في تفسير ما تقيسه البصمة.

**العيب:** بصماتُ الحالة التي حُسبت في هذه الجلسة اختلفت عن البصمات المحفوظة في
`docs/evidence/phase2-unit4-20260819-foundation-run1.txt` لكلّ جدولٍ إلّا
`spatial_ref_sys`، **بينما عددُ الصفوف مطابقٌ تماماً**. والسببُ مُقاسٌ لا
مُقدَّر: الترحيلاتُ تبذر `cities` و`platform_settings` بمعرّفات
`gen_random_uuid()`، فتُسنِد كلُّ قاعدةٍ معرّفاتٍ مختلفة، ويتسرّب اختلافُها إلى
كلّ صفٍّ يحمل `city_id`. فالبصمةُ كانت **مرتبطةً بالقاعدة التي حُسبت فيها**، لا
قابلةً للنقل بين بيئتين كما أوحى الوصفُ بأنّها «قابلةٌ للمقارنة الآليّة». وهذا
أخطرُ صنفٍ من الفروق: فرقٌ يظهر بلا معنى، فيُدرّب من يقرؤه على تجاهُل الفروق —
وهو نفسُ نمطِ الخطأ الذي أنتج رقمَ ٢٠٤٥ طلب/ث.

**ما لم يكن معيباً:** معرّفاتُ البذر نفسُها (`benchUuid`) مُشتقّةٌ اشتقاقاً
UUIDv5 فهي ثابتةٌ بين القواعد؛ العيبُ محصورٌ في المعرّفات التي تُسنِدها
الترحيلاتُ عشوائياً. وحتميّةُ البذر **داخل** قاعدةٍ واحدة كانت وما زالت مُثبَتة.

**الإصلاح:** لكلّ جدولٍ بصمتان لا واحدة — `digest` كما هي (تُقارِن داخل القاعدة
نفسِها وتكشف أيّ إعادةِ بذرٍ للجداول المملوكة للترحيلات)، و`portableDigest`
تُحسَب بعد استبدال كلّ معرّفٍ تُسنِده الترحيلاتُ باسمه المنطقيّ الثابت
(`«city:JED»`، `«setting:JED:<key>»`) المُستنبَط من القاعدة لا من قائمةٍ مكتوبة.
و`compareStates` تقارن المنقولة، وتضع اختلافَ الخامِ وحده في `identityOnly` بلا
أن يُعلَن فرقاً. وأُضيف `bench/compare-snapshots.ts` و`BENCH_STATE_JSON` لأنّ
المقارنةَ بين بيئتين لا تجري في عمليّةٍ واحدة.

**ولماذا لم تُستبدَل معرّفاتُ البذر أيضاً:** تقييسُ ما هو محمولٌ أصلاً كان
سيطمر فروقاً حقيقيّة — سائقان مختلفان يصيران صفّاً واحداً في البصمة. فالتقييسُ
مقصورٌ على ما تُسنِده القاعدةُ عشوائياً، والباقي يُقارَن حرفاً بحرف.

#### درجات التحقّق (وحدة 2-4.1)

| الشيء | الدرجة | الدليل |
|---|---|---|
| سلسلةُ إثبات الأساس أُعيد إنتاجُها على بيئةٍ بُنيت من الصفر | **مُتحقَّق منه** — ١٥/١٥ فحصاً، خروج 0، تشغيلان في عمليّتين منفصلتين | `phase2-unit41-20260819-foundation-db1.txt` |
| البصمةُ الخامُ **ليست** قابلةً للنقل بين قاعدتين — العيبُ مُثبَتٌ لا مُدَّعى | **مُثبَت** — ٧ جداولَ اختلفت بصمتُها الخام وعددُ الصفوف مطابقٌ (١٢٠) | `phase2-unit41-20260819-portability.txt` |
| البصمةُ المنقولة متطابقةٌ بين قاعدتين مُهيّأتين من الترحيلات نفسِها | **مُثبَت** — إسقاطُ `waslah_bench` وإعادةُ ترحيلها ⇒ معرّفاتُ مدنٍ جديدة، و٧/٧ بصماتٍ منقولةٍ متطابقة، وفروقٌ منطقيّة = 0 | `phase2-unit41-20260819-portability.txt` + `…-foundation-db2-rebuilt.txt` |
| منطقُ التقييس مُختبَرٌ بلا قاعدةِ بيانات (يجري في CI) | **مُختبَر** — ٦ اختباراتٍ جديدة: استبدالٌ في كلّ مواضع الصفّ، عدمُ مسّ ما ليس في الخريطة، خريطةٌ فارغة، حالةُ الأحرف | `tests/unit/bench-isolation.test.ts` (٢٩ اختباراً) |
| اختلافُ المعرّفات المُسنَدة وحدَه لا يُسقِط `identical`، ويُذكَر في بابه | **مُختبَر** | نفس الملفّ |
| الحزمةُ الكاملة بعد الإصلاح | **مُختبَر** — **1,821 نجاح / 0 فشل / 0 تخطٍّ**، 7,046 `expect()`، ١٤٧ ملفاً، 190.76ث | `phase2-unit41-20260819-tests.txt` |
| lint و typecheck بعد الإصلاح | **مُتحقَّق منه** — خروج 0، و41 ملاحظةً معلوماتيّة (**نفسُ العدد**، فلا انحراف) | نفس ملفّ الاختبارات + تشغيلٌ منفصل |

#### تصحيحاتٌ على ما كان مكتوباً

| ما كان مكتوباً | الواقعُ المقيس | التصحيح |
|---|---|---|
| البصمةُ «قابلةٌ للمقارنة الآليّة» بلا تقييدٍ للنطاق | كانت قابلةً للمقارنة **داخل القاعدة نفسِها** فقط | أُضيفت `portableDigest`، وقُيِّد نطاقُ كلّ بصمةٍ صراحةً في `bench/README.md` و`bench/state.ts` |
| `bench/README.md`: `bun scripts/apply-migrations.ts` | **الملفُّ غيرُ موجودٍ في المستودع**؛ CI يطبّق الترحيلاتَ بحلقة `psql` بترتيب الاسم | استُبدل بالحلقة نفسِها التي يستخدمها CI |
| الحزمةُ الكاملة: 7,032 `expect()` | التشغيلُ على `df09f33` أعطى 7,034 قبل أيّ تغييرٍ منّي | فرقٌ صغيرٌ لكنّه يعني أنّ الرقمَ نُقل لا نُسخ؛ والرقمُ المُعلَن الآن (7,046) من تشغيلٍ محفوظٍ بدليله |

#### ما لم يُثبَت في هذه الوحدة

1. **قابليّةُ النقل مُثبَتةٌ بين قاعدتين على المضيف نفسِه وبإصدارِ PostgreSQL/PostGIS
   نفسِه.** لم تُقَس بين إصدارين مختلفين ولا بين مضيفين، وفي `spatial_ref_sys`
   خاصّةً يجوز أن تختلف لاختلاف إصدار PostGIS — وذلك فرقٌ حقيقيٌّ يجب أن يُعلَن لا
   أن يُقيَّس.
2. **لا شيءَ عن القدرة أو الأداء.** لم يُقَس في هذه الوحدة طلبٌ واحد.
3. **البصمةُ المنقولة تُحسَب في العمليّة لا في القاعدة**، فلها سقفٌ مُعلَن
   (`PORTABLE_DIGEST_ROW_LIMIT = 250,000` صفّاً للجدول) **يُرفَع عنده خطأٌ ولا
   تُحسَب بصمةٌ ناقصة**. ولم يُقَس أثرُ ذلك على الذاكرة عند حملٍ كبير، وهو ما
   ستكشفه وحدةُ 2-5 إن اقتربت من السقف.
4. **لا سيناريو عملٍ بعد.** ما زال التحقّقُ على مستوى العمل موضوعَ وحدة 2-5. *(زال هذا الحدُّ في وحدة 2-5 — §13.9؛ ويُترك نصُّه كما كُتب لأنّه لقطةُ حالةٍ لوقتِها.)*

---

### 13.8 تصفيرُ حالة Git — حلُّ تعارضَي طلبَي الدمج المفتوحَين (2026-08-19)

**السبب:** المستودعُ هو المصدرُ الفعليُّ للحالة المدفوعة، وكان فيه طلبا دمجٍ
مفتوحان يرفضهما GitHub بـ`mergeable_state = dirty` — أي أنّ عملاً موثَّقاً كـ«تمّ»
لم يكن قابلاً للوصول إلى `main`. فجوةُ ما تقوله الوثيقةُ عمّا في المستودع هي بذاتُها
عيبٌ، لا مسألةً إجرائيّة.

**ما فُحص فعلاً:** `git status` (شجرةٌ نظيفة، ولا `stash`)، وسائرُ الفروع الثمانية،
والـ`commits` غير المدفوعة (لا شيء)، ومراجعُ `refs/pull/*` الاثنا عشر، وحالةُ كلّ
طلبٍ من واجهة GitHub لا من الظنّ، ثمّ **دمجٌ تجريبيٌّ محليّ** (`git merge-tree`)
لتحديد الملفّ المتعارض بعينه.

#### طلب الدمج #12 — `feat/system-state-baseline` → `main`

| | |
|---|---|
| **التعارض** | `add/add` على `docs/SYSTEM_STATE.md` وحده |
| **السببُ الجذريّ** | طلبُ الدمج #11 دُمج بـ**squash**، فأنشأ في `main` الـcommit `562b129` يحمل نفسَ محتوى `0a4b243` الموجود في فرعنا. فالملفُّ الواحدُ صار له أصلان لا جدَّ مشترك بينهما، فأبى Git الاختيارَ. **ليس تعارضَ محتوى، بل أثرُ إعادةِ كتابةِ التاريخ.** |
| **الإثباتُ قبل الحلّ** | `git diff origin/main 2ea7a0b` و`git diff 0a4b243 2ea7a0b` **متطابقتان بصمةً** (`28055ef7…`) ⇒ `main` لا يحمل سطراً واحداً غائباً عن فرعنا، فأخذُ نسختنا لا يُفقِد شيئاً — وهذا قياسٌ لا اطمئنان |
| **الحلّ** | دمجُ `main` في الفرع بـ`--no-ff` مع اعتماد نسختنا للملفّ المتعارض. **وشجرةُ الدمج مطابقةٌ بايتاً ببايت لشجرة `2ea7a0b`** (`git diff --stat 2ea7a0b` فارغ) ⇒ صفرُ تغييرٍ في المحتوى، ومحضُ مصالحةِ تاريخ |
| **لماذا دمجٌ لا rebase** | الـ`rebase` كان سيُعيد كتابة خمسة commits تمثّل وحداتِ المشروع (2-1 … 2-4.1) ويحوّل `0a4b243` إلى commit فارغٍ أو متعارض؛ والدمجُ يحفظ الوحدات كما وقعت ويُسجّل سببَ المصالحة في رسالته |

#### طلب الدمج #1 — `production-hardening` → `main`

| | |
|---|---|
| **التعارض** | `add/add` على `docs/production-readiness/PHASE-0-BASELINE.md` وحده |
| **السببُ الجذريّ** | الفرعُ أُنشئ في 2026-08-11 من `19ae72e`، ووصل الملفُّ نفسُه إلى `main` من طريقٍ آخر في `2148fc9` بعد نحو ٢٠ دقيقة. والاختلافُ الفعليُّ **ثلاثةُ أسطرٍ** فقط |
| **الحلّ سطراً سطراً — لا اختيارَ جانب** | (١) النثرُ الذي يذكر المتغيّرَ الواجبَ تعيينُه: نسخةُ `main` (`TEST_DATABASE_URL`) **صحيحة**؛ الكودُ يقرأ `process.env.TEST_DATABASE_URL` في ٦٤ ملفاً ولا وجودَ لـ`DATABASE_URL` في حرّاس الاختبارات. (٢) المقطعُ البرمجيُّ المقتبَس: نسخةُ `main` «صحّحته» فأفقدته مطابقةَ الكود — الثابتُ المحليُّ اسمُه `DATABASE_URL` فعلاً وهو يقرأ `TEST_DATABASE_URL`؛ فأُعيد الاقتباسُ إلى نصّ الكود حرفيّاً مع ملاحظةٍ تفكّ الالتباس. **فلا الجانبُ القديمُ صحيحٌ بتمامه ولا الجديد** |
| **قرارٌ إضافيّ** | وُسمت الوثيقةُ في رأسها **لقطةً تاريخيّة** لتاريخ 2026-08-11 لا وصفاً للحالة الراهنة، لأنّ أرقامها (866 نجاحاً · 223 تخطّياً · 14 خطأَ lint · 26 ترحيلاً) أبطلتها الحالةُ الفعليّة (٥٧ ترحيلاً، وحزمةٌ كاملةٌ بلا تخطٍّ)؛ ولقطةٌ غيرُ موسومةٍ تُقرأ حالةً راهنة، وذلك أصلُ انجراف الوثائق. وهذا يُنجِز بنداً كان مؤجَّلاً في §12 |
| **ما لم يُفعَل** | **لم يُغلَق الفرعُ ولم يُحذَف محتواه بصمت.** أثرُه الصافي على `main` بعد الحلّ هو ١٣ سطراً مضافاً وسطرٌ محذوف في ملفِّ وثائق واحد، وهو ما يجب أن يبقى؛ ولا سطرَ كودٍ فيه |

#### النتائجُ بعد الحلّ (لا قبله)

| الفرع | typecheck | lint | الحزمة الكاملة | الدليل |
|---|---|---|---|---|
| `production-hardening` (`73fdfec`) | خروج 0 | 0 خطأ، 39 ملاحظة | **1,772 نجاح / 0 فشل / 0 تخطٍّ**، 6,940 `expect()`، ١٤٤ ملفاً، 193.67ث | `phase2-pr1-20260819-checks.txt` |
| `feat/system-state-baseline` (الدمج) | خروج 0 | 0 خطأ، 41 ملاحظة | **1,821 نجاح / 0 فشل / 0 تخطٍّ**، 7,046 `expect()`، ١٤٧ ملفاً | `phase2-unit41-20260819-tests.txt` + `phase2-merge-20260819-checks.txt` |

وفرقُ الرقمين (1,772 مقابل 1,821) ليس انحرافاً: فرعُ `production-hardening` عند شجرة
`main` التي **لا تحتوي** وحداتِ المرحلة 2 ولا اختباراتِها الجديدة. وهو أوّلُ ما يجب
أن يُفسَّر لا أن يُبرَّر.

#### حالةُ GitHub بعد الدفع (فُحصت من الواجهة، لا من الظنّ)

| PR | قبل | بعد | CI على عدّاء GitHub |
|---|---|---|---|
| #1 | `mergeable = false`, `dirty` | **`mergeable = true`, `clean`** (رأس `73fdfec`) | `verify` نجح · `تكامل على PostgreSQL حقيقي` نجح |
| #12 | `mergeable = false`, `dirty` | **`mergeable = true`, `clean`** (رأس `a105760`) | `verify` نجح · `تكامل على PostgreSQL حقيقي` نجح |

وهذه شهادةُ طرفٍ ثالثٍ لا شهادةُ بيئتي: الوظيفتان جرَيتا على عدّاء GitHub بقاعدةِ
`postgis/postgis:17-3.5` و`CI_REQUIRE_DB=1`، أي على إصدارِ PostgreSQL مختلفٍ عن
المحلّي (17 مقابل 18.4) — فالنجاحُ فيهما يضيف ما لا تضيفه إعادةُ التشغيل عندي.

**ما بقي على المالك:** قرارُ دمجِ الطلبَين إلى `main` قرارُ مالكٍ لا قرارُ وكيل؛
وكلاهما الآن قابلٌ للدمج وقد جرى على شجرته ما يُثبِت أنّه لا يكسر شيئاً.


#### التكرارُ الثاني لنفسِ السبب — دمجُ `main` مرّةً أخرى بعد إتمامِ #1 و#12 (نفسُ اليوم)

بعد أن دُمج الطلبان فعلاً بـ**squash**، صار في `main` الـcommit `e5ac705` يحمل نفسَ
محتوى `45818e1` من فرعنا بهويّةٍ مختلفة، فعاد الطلبُ #13 `dirty` — **نفسُ العطبِ لا
عطبٌ جديد**، وهو الثمنُ المتكرِّرُ لدمجِ الـsquash على فرعٍ يواصل العمل.

| ما فُحص | النتيجة المقيسة |
|---|---|
| هل شجرةُ `main` جديدةٌ فعلاً؟ | `git diff --name-only 45818e1 origin/main` ⇒ ملفٌّ واحدٌ فقط: `PHASE-0-BASELINE.md` (إضافاتُ PR #1). أي **`main` = `45818e1` + وثيقةُ #1** |
| هل ينقص `main` شيءٌ من عملنا؟ | ٢٠ ملفاً، كلُّها **وحدة 2-5** — لأنّ #12 دُمج من نقطةٍ قبل `eaa4bdd` |
| التعارضُ الفعليّ | ملفّان: `docs/SYSTEM_STATE.md` (محتوى) و`bench/README.md` (`add/add`) — وفي كلِّ فرقٍ منهما جانبُنا **يحتوي** جانبَ `main` ويزيد عليه سطورَ 2-5 و2-6 |
| القرار | اعتمادُ نسختنا في الملفَّين المتعارضَين لأنّها المجموعةُ الأشمل، **مع إبقاء إضافاتِ #1 كما هي**: `git diff origin/main -- PHASE-0-BASELINE.md` ⇒ **صفر** |
| لماذا دمجٌ لا rebase (كما في الحالةِ الأولى) | الـrebase يُعيد كتابةَ commit-اتِ الوحدات 2-5 و2-6 ويُحوِّل ما دُمج أصلاً إلى فارغٍ أو متعارض؛ والدمجُ يحفظ الوحداتَ كما وقعت |
| البوّاباتُ بعد الحلّ | typecheck خروج 0 · lint 0 خطأ / 41 ملاحظة · 9/9 حرّاس · **1,862 نجاح / 0 فشل** في ١٤٩ ملفاً — الدليل `phase2-merge2-20260819-checks.txt` |

وما لم يُفعَل: **لم يُحذَف سطرٌ من `main`** ولا من الفرع؛ والأثرُ الصافيُّ للدمج على
الشجرةِ هو إضافةُ وثيقةِ #1 وحدَها.

---

### 13.9 وحدة 2-5 — سيناريوهاتُ عملٍ حقيقيّةٌ يُحكَم فيها بنتيجةِ العمل لا بكودِ HTTP (2026-08-19)

**المشكلةُ التي حلّتها هذه الوحدة.** كان في المستودع أداةُ حملٍ (`bench/load-test.ts`)
وأساسُ قياسٍ (وحدة 2-4)، لكن لم يكن فيه شيءٌ يقول: **ما معنى أن ينجحَ الطلب؟**
وحدة 2-1 كانت قد أبطلت رقمَ التسجيل التاريخيَّ لأنّه قِيس من كودِ HTTP، والدرسُ
نفسُه لم يكن قد صار بنيةً بعد. هذه الوحدةُ تجعله بنية: عقدٌ يمنع كتابةَ سيناريو
بلا إعلانِ نتيجتِه، ومشغّلٌ **لا يستقبل كودَ HTTP في توقيعِ حكمه أصلاً**.

#### ما بُني (ملفّاتٌ جديدة، لا تعديلَ على كودِ الإنتاج)

| الملف | ما فيه |
|---|---|
| `bench/scenarios/contract.ts` | عقدُ السيناريو: ١٣ بنداً واجبَ الإعلان (حالةُ البداية، الخطوات، مدخلاتُ المستخدم، استجابةُ النظام، نتيجةُ العمل، حالةُ القاعدة، الانتقالات، الثوابت، الحتميّةُ عند التكرار، التزامن، شروطُ الفشل، المقاييس، ما يُثبته وما **لا** يُثبته)، و`validateScenario` يرفض القائمةَ الفارغةَ كما يرفض الحقلَ المفقود |
| `bench/scenarios/harness.ts` | بيئةُ تشغيل: خادمٌ حقيقيٌّ على الويبهوك، قاعدةُ `waslah_bench` وحدها، مُرسِلٌ ملتقِطٌ مُعلَنٌ كمزدوج، إعادةُ ضبطٍ بين سيناريو وآخَر عبر `bench/reset.ts` (لا نسخةَ ثانيةً من قائمةِ الجداول) |
| `bench/scenarios/runner.ts` | المشغّل: يتحقّق من العقد، يُعيد الضبط، يفحص الشروطَ الابتدائيةَ **ولا يفترضها**، يُطلق الفاعلين، ثم يُوكّد بعد اكتمالِهم؛ و`decideVerdict(checks, failures, actorErrors)` — بلا كودِ HTTP في التوقيع |
| `bench/scenarios/metrics-text.ts` | قراءةُ عدّادٍ من نصِّ Prometheus بجمعِ كلِّ سلاسلِه الموسومة |
| `bench/scenarios/updates.ts` | حمولاتُ ويبهوك تيليجرام الحقيقية (نصّ، صورة، موقع، جهةُ اتصال، زرّ) |
| `bench/scenarios/catalog/*.ts` | ثلاثةُ سيناريوهاتِ عمل + مُعينُ تهيئةٍ مشترك |
| `bench/run-scenarios.ts` | مُشغِّلٌ من سطرِ الأوامر: `--scenario`, `--actors`, `--list`؛ يحفظ الدليلَ ويخرج بغيرِ صفرٍ على أيِّ `business_failure` |
| `bench/negative-control.ts` | شاهدٌ سالب: يُفسِد الحالةَ عن قصدٍ ليُثبت أنّ التوكيداتَ ترى الإخفاق |
| `tests/unit/bench-scenario-contract.test.ts` | ٢٣ اختباراً للإطارِ نفسِه، **بلا قاعدةِ بيانات** فتجري في CI |

#### نتائجُ التشغيل الفعليّ (٣ سيناريوهات · ٦٦ توكيداً · 0 إخفاق)

| السيناريو | الفاعلون | النمط | التوكيدات | p50 | p95 | الحكم |
|---|---|---|---|---|---|---|
| `driver-onboarding` — تسجيلُ سائقٍ حتى بدءِ التجربة | 10/10 | مستقلّ | 20 ✅ | 124.9ms | 130.3ms | `business_success` |
| `ride-dispatch` — طلبُ رحلةٍ وبثُّه على المؤهّلين | 5/5 | مستقلّ | 21 ✅ | 43.7ms | 45.4ms | `business_success` |
| `contended-accept` — تسابقُ سائقين على طلبٍ واحد | 4/4 | **متزاحم** | 25 ✅ | 6.8ms | 8.0ms | `business_success` |

وهذه الأزمنةُ **مقيسة** لا مُثبَتة، ومقيسةٌ لمسارٍ داخليّ: قاعدةٌ محلّيّةٌ في نفسِ
الحاسوب، وعمليّةٌ واحدة، ومُرسِلُ تيليجرام مزدوجٌ لا يخرج إلى الشبكة. وهي متفائلةٌ
مقابل الإنتاج بمقدارٍ **غيرِ معروف**، ولا تُقرأ سعةً ولا تُقارَن بأيِّ رقمٍ لغيرِ
هذه البيئة (§8 و§10 من الأمر الحاكم).

**أهمُّ ما أُثبت (سيناريو التزاحم):** أربعةُ سائقين ضغطوا «قبول» على طلبٍ واحدٍ معاً،
فكانت النتيجةُ عرضاً مقبولاً واحداً، وطلباً واحداً `matched` مُسنَداً **لصاحبِ ذلك
العرضِ نفسِه**، ورسالةَ ظفرٍ واحدةً وثلاثَ رسائلِ «الطلبُ أُخِذ»، وبطاقةَ رحلةٍ
واحدةً ودبّوسَ موقعٍ واحداً، وأثرَ تدقيقٍ واحداً `order.claimed` — و**عدّادَ
`waslah_dispatch_offers_accepted_total` تحرّك بمقدارِ ١ لا ٤**. ثلاثةُ شواهدَ على
الواقعةِ نفسِها (صفوفُ القاعدة، الرسائلُ المصوغة، العدّاد) لا شاهدٌ واحدٌ يُصدَّق.
ثم أعاد الظافرُ الضغطَ فلم يتغيّر شيءٌ من الثلاثة — وهذه هي الحتميّةُ عند التكرار
مقيسةً لا مُدَّعاة.

#### الشاهدُ السالب: هل التوكيداتُ ترى الإخفاق أصلاً؟

تقريرٌ أخضرُ من مقياسٍ أعمى لا قيمةَ له، فأُفسدت حالةُ الإسنادِ ثلاثَ مرّاتٍ عن قصدٍ
بعد انتهاءِ التسابقِ وقبل أوّلِ توكيد:

| الإفساد | النتيجة |
|---|---|
| رفعُ عرضٍ خاسرٍ إلى `accepted` (أي ما يحدث لو لم تكن `claim_ride` ذرّية) | **رفضته القاعدةُ نفسُها**: `duplicate key value violates unique constraint "order_offers_single_accepted"` |
| تحويلُ الإسنادِ إلى سائقٍ غيرِ صاحبِ العرضِ المقبول | كُشِف: `business_failure` + شرطُ `ASSIGNMENT_MISMATCH` |
| إرجاعُ الطلبِ إلى `searching` بلا سائقٍ بعد وقوعِ الإسناد | كُشِف: `business_failure` + شرطُ `LOST_ASSIGNMENT` |

والنتيجةُ الأولى فائدةٌ لم تكن مقصودةً: منعُ الإسنادِ المزدوجِ **ليس** متروكاً للدالّةِ
الذرّيةِ وحدها، بل تحته قيدُ فرادةٍ جزئيٌّ في المخطَّط يجعل الحالةَ المستحيلةَ غيرَ
قابلةٍ للكتابة أصلاً — حتى بيدٍ تكتب SQL مباشرةً على القاعدة.

#### تصحيحٌ لتوكيدٍ خاطئٍ كتبتُه أنا (§3: الكودُ هو المرجع)

أوّلُ تشغيلٍ لـ`ride-dispatch` أعطى `business_failure` على توكيدِ «كلُّ عميلٍ أُبلِغ
ببدءِ البحث: المتوقّع=5 · الملاحَظ=0». والسببُ لم يكن في النظام: `rider-dialog`
يُرسل أحدَ أربعةِ بدائلَ من عائلةِ `waiting.rider_search_*` تُختار ببذرةٍ مستقرّة
(`packages/application/bots/waiting-lines.ts`)، لا مفتاحاً واحداً اسمُه
`rider.searching`. فالتوكيدُ هو الذي أُصلح — يقرأ قائمةَ البدائلِ من مصدرِها عبر
`waitingVariants` ولا ينسخُها — والسلوكُ لم يُلمَس. وهذا مُدرَجٌ هنا لا مطموسٌ: فشلٌ
أوّلُ سببُه المقياسُ لا المقيس يجب أن يُقرأ، لأنّه يُخبِر عن جودةِ التوكيدِ نفسِه.

#### المزدوجاتُ المُعلَنة (§7) — وما لا تُثبته هذه الوحدة

ثلاثةُ مزدوجاتٍ في كلِّ تشغيل، مُعلَنةٌ في `HARNESS_MOCKS` وتُطبَع في كلِّ تقرير:
مُرسِلُ تيليجرام ملتقِطٌ (يُثبت أنّ النظامَ **صاغ** الرسالةَ الصحيحةَ للمُخاطَبِ
الصحيح، ولا يُثبت التسليم)، وحدُّ معدّلٍ متساهلٌ (١٠⁹)، وعاملٌ خارجَ البوابة.

* **عدّادُ `waslah_telegram_messages_sent_total` بقي 0 في كلِّ التشغيلات، وهذا صحيحٌ لا عطب** (§9): العدّادُ يُحسب بعد نجاحِ نداءٍ حقيقيّ، والمُرسِلُ هنا مزدوجٌ يتقدّم على `measuredTelegramSender`. ولو تحرّك لكان ذلك هو الخطأ.
* **لا يُثبت هذا العملُ**: تسليمَ رسالةٍ عبر تيليجرام، ولا سعةً تحت حمل (١٩ فاعلاً في المجموع ليس حملاً — §15)، ولا ذرّيةً عبر عملياتٍ أو خوادمَ متعدّدة (كلُّ الطلباتِ من عمليّةٍ واحدة؛ الضمانُ في القاعدة لكنّ القياسَ لم يعبر الشبكة)، ولا سلوكاً على مُدارِ Supabase، ولا أثرَ حدِّ المعدّلِ الحقيقيّ.

#### تصنيفُ الأرقام (§10)

| الرقم | التصنيف |
|---|---|
| ٦٦ توكيداً تجاريّاً ناجحاً في ٣ سيناريوهات، 0 إخفاق | **مقيس** (بيئةٌ محلّيّة، مزدوجاتٌ مُعلَنة) |
| عرضٌ مقبولٌ واحدٌ بالضبط تحت تسابقِ ٤ سائقين، وعدّادٌ تحرّك ١ | **مقيس بثلاثةِ شواهدَ مستقلّة**؛ ولا يُقال «مُثبَت» على مستوى الإنتاج لأن التزاحمَ ٤ لا آلاف |
| كشفُ الإفسادَين المصطنعَين، ورفضُ القاعدةِ للثالث | **مُثبَت** لهذه الثوابتِ بعينها: الشاهدُ السالبُ جرى وأُوثِق |
| أزمنةُ p50/p95 أعلاه | **معلوماتيّة/مقيسة داخلياً** — لا تُقرأ سعةً |
| 1,844 اختباراً / 0 فشل / 0 تخطٍّ · 0 خطأ نمطٍ · 9/9 حرّاس | **مقيس ومحفوظٌ حرفيّاً** في `phase2-unit5-20260819-checks.txt` |

#### شهادةُ طرفٍ ثالث: CI على عدّاءِ GitHub

الـcommit `eaa4bdd` (رأسُ `feat/system-state-baseline`) جرى على عدّاءِ GitHub لا على
بيئتي:

| الوظيفة | النتيجة |
|---|---|
| `verify` (typecheck · lint · حرّاس · الحزمةُ الكاملة) | **نجحت** |
| `تكامل على PostgreSQL حقيقي` (`postgis/postgis:17-3.5`، `CI_REQUIRE_DB=1`) | **نجحت** |

وقيمةُ هذه الشهادةِ أنّها على **إصدارِ PostgreSQL مختلفٍ عن المحلّي** (17 مقابل
18.4) وعلى شجرةٍ مستنسخةٍ من الصفر، فتضيف ما لا تضيفه إعادةُ التشغيل عندي. ولا
تُثبت السيناريوهاتَ نفسَها: هي لا تجري في CI بعد لعدمِ وجود قاعدةِ قياسٍ في
الوظيفة، والذي جرى فيها من وحدة 2-5 هو اختباراتُ الإطارِ الـ٢٣ بلا قاعدة.

#### تغييرٌ صغيرٌ واحدٌ خارجَ `bench/`

`biome.json`: استُثني `docs/evidence` من فحصِ التنسيق. الدليلُ مُخرَجُ تشغيلٍ لا كودُ
مصدر، وإعادةُ تنسيقِه تُعدّل الأثرَ المحفوظَ نفسَه — وهو أوّلُ ما يجب أن يبقى كما
خرج. ولا يمسّ هذا أيَّ ملفِّ مصدر: lint بقي **0 خطأ / 41 ملاحظة**، وهو نفسُ خطِّ
الأساس المُسجَّل في §13.4.

---

### 13.10 وحدة 2-6 — تحقُّقٌ موزَّع: هل تبقى النتيجةُ التجاريّةُ صحيحةً عبر أكثرِ من عملية؟ (2026-08-19)

**السؤالُ الذي تجيبه هذه الوحدة.** وحدة 2-5 أثبتت النتائجَ التجاريّةَ في **عمليّةٍ
واحدة**، وقالت ذلك صريحاً في «ما لا تُثبته». وفي عمليّةٍ واحدةٍ تُجيب أشياءُ كثيرةٌ
بالمجّان: الجلسةُ في ذاكرةٍ واحدة، ومانعُ التكرارِ خريطةٌ واحدة، والمُرسِلُ ملتقِطٌ
واحد. فكان لا بدَّ من سؤالٍ منفصل: **أيُّ توكيدٍ من تلك الـ٦٦ كان صحيحاً بسببِ
النظام، وأيُّها كان صحيحاً بسببِ وحدةِ العملية؟**

#### الطبولوجيا التي بُنيت (§2 من أمرِ الوحدة)

| المكوِّن | ما هو فعلاً |
|---|---|
| البوّابات | **عمليتان مستقلّتان** (`Bun.spawn`)، كلٌّ بمنفذٍ حقيقيّ ومحتوىً كامل من `buildContainer`، وكومةٍ وبِركةِ اتصالاتٍ ومانعِ تكرارٍ خاصٍّ بها |
| النقل | HTTP على **مقبسِ TCP حقيقيّ** إلى عمليةٍ أخرى — لا `app.fetch` في نفسِ العمليّة كما في 2-5 |
| مخزنُ الجلسات وحدُّ المعدّل | `SESSION_STORE=redis` على **مزدوجِ Upstash REST** في عمليّةِ حالةٍ ثالثة؛ وعميلُ الإنتاج `createUpstashRedis` يُستخدم بلا تعديل |
| القاعدة | `waslah_bench` واحدةٌ مشتركة — وهي المنسِّقُ الحقيقيُّ للثوابت |
| العاملون | عمليّتا عاملٍ مستقلّتان تُقلَعان بحاجزٍ زمنيٍّ مشترك؛ والعاملُ داخلَ البوّاباتِ **مُطفأ** كي لا يختلط مصدرُ الأثر |
| العزل | حروسُ وحدة 2-4 كما هي: `assertConnectedToBenchDatabase` في **كلِّ** عمليّةِ بوّابةٍ وعاملٍ بعد الاتّصال؛ ولا مساسَ بـSupabase ولا باعتمادِ إنتاج |

وعنقودٌ **جديدٌ لكلِّ سيناريو**: أحدُ السيناريوهاتِ يقتل بوّابةً قسراً، وإعادةُ
استخدامِ عنقودٍ نصفِ ميّتٍ كانت ستُنتج نتائجَ صحيحةً لسببٍ غيرِ الذي يظنّه القارئ.

#### ما بُني (لا تعديلَ على كودِ الإنتاج — ولا سطرَ واحد)

| الملف | ما فيه |
|---|---|
| `bench/scenarios/env.ts` | منفذُ `ScenarioEnvironment`: تجريدُ ما يحتاجه السيناريو (إرسالُ تحديث، قراءةُ رسائل، نصُّ المقاييس، إعادةُ ضبط) — فصار السيناريوُ الواحدُ يعمل على عمليّةٍ واحدةٍ أو على عنقودٍ **بلا تعديلِ حرفٍ فيه** |
| `bench/topology/redis-shim.ts` | مزدوجُ بروتوكولِ Upstash REST: `PING/GET/SET…EX/DEL/INCR/EXPIRE`، مصادقةُ `Bearer`، وعدّادُ أوامرَ يُثبت أنّ المشاركةَ جرت |
| `bench/topology/gateway-process.ts` | عمليّةُ بوّابةٍ حقيقيّة + مسارُ قياسٍ (`/bench/messages?since=`، `/bench/metrics-text`، `/bench/state`) |
| `bench/topology/cluster.ts` | إقلاعُ العنقودِ وتوجيهُ الطلباتِ بالتناوب، وجمعُ الرسائلِ والمقاييسِ من كلِّ نسخة، و`kill(i)` بـ`SIGKILL` |
| `bench/topology/serialize.ts` | مُسلسِلُ السحبِ لكلِّ نسخة — أُنشئ لإصلاحِ عطبِ قياسٍ حقيقيّ (§ أدناه) |
| `bench/topology/worker-process.ts` + `worker-duel.ts` | عمليّتا عاملٍ على حاجزٍ زمنيٍّ مشترك، وتسجيلُ مدى كلِّ تنفيذٍ **داخلَ** القفل لفحصِ التقاطع |
| `bench/topology/probes.ts` | ضبطٌ سالبٌ للجلسةِ المشتركة: نفسُ الحوارِ على `redis` ثمّ على `memory` |
| `bench/topology/scenarios/cross-process-replay.ts` | إعادةُ نفسِ `update_id` إلى عمليّةٍ أخرى (وحدة 2-5 لم تُختبر التكرارَ أصلاً: بُناةُ التحديثاتِ لم تكن تُصدِر `update_id`) |
| `bench/topology/scenarios/gateway-death.ts` | قتلُ بوّابةٍ وسطَ المسار وإكمالُ القبولِ على الباقية |
| `bench/run-distributed.ts` | مُشغِّلٌ واحد: `--scenario/--actors/--gateways/--list`، يحفظ الدليلَ ويخرج بغيرِ صفرٍ على أيِّ `business_failure` |
| `tests/unit/bench-topology-contract.test.ts` | ١٨ اختباراً لمكوّناتِ الطبولوجيا نفسِها **بلا قاعدةٍ ولا عمليّات** فتجري في CI |

#### النتائج (٥ سيناريوهات · ١٠٢ توكيداً · فحصان بـ١٠ توكيدات · 0 إخفاق)

| ما جرى | الفاعلون | التوكيدات | p50 | الحكم |
|---|---|---|---|---|
| `driver-onboarding` — **مُعادٌ من 2-5 كما هو** | 10/10 | 20 ✅ | 231.6ms | `business_success` |
| `ride-dispatch` — **مُعادٌ من 2-5 كما هو** | 5/5 | 21 ✅ | 131.6ms | `business_success` |
| `contended-accept` — **مُعادٌ من 2-5 كما هو**، والمتسابقون موزَّعون | 4/4 | 25 ✅ | 27.0ms | `business_success` |
| `cross-process-replay` — نفسُ `update_id` إلى عمليّةٍ أخرى | 1/1 | 20 ✅ | 13.8ms | `business_success` |
| `gateway-death-midflight` — `SIGKILL` لبوّابةٍ ثمّ إكمالُ القبول | 1/1 | 16 ✅ | 13.6ms | `business_success` |
| فحصٌ: الجلسةُ المشتركةُ شرطٌ (ضبطٌ سالب) | — | 3 ✅ | — | نجاحُ عمل |
| فحصٌ: عاملان في عمليتين على نفسِ المهامّ | — | 7 ✅ | — | نجاحُ عمل |

**أهمُّ ما أُثبت (§4 من أمرِ الوحدة).** أربعةُ سائقين ضغطوا «قبول» معاً و**هم موزَّعون
على عمليتين** — مُوثَّقٌ بجدولِ التوجيهِ المحفوظِ في الدليل: `callback:offer:accept`
= gw1: 3 (منها إعادةُ الحتميّة) وgw2: 2 — فكانت النتيجةُ عرضاً مقبولاً واحداً،
وطلباً `matched` مُسنَداً لصاحبِ ذلك العرضِ نفسِه، ورسالةَ ظفرٍ واحدةً وثلاثَ رسائلِ
«الطلبُ أُخِذ»، وأثرَ تدقيقٍ واحداً، و`waslah_dispatch_offers_accepted_total` **+1
مجموعاً على النسختين**. أي أنّ الثابتَ **لا يعتمد** على وجودِ المتنافسين في نفسِ
العمليّة: الحاكمُ هو `claim_ride` في القاعدة.

**الحتميّةُ عبر العمليات (§5).** نفسُ `update_id` سُلِّم ثلاثَ مرّات: الأصلُ إلى gw1،
ثمّ إعادةٌ إلى gw1 **حُجِبت** في مانعِ التكرار (`waslah_telegram_webhook_duplicates_total`
+2)، ثمّ إعادةٌ إلى gw2 **عبرت** إلى منطقِ العمل — لأنّ لكلِّ عمليّةٍ خريطتَها
(gw1=1 مفتاح، gw2=1 مفتاح). ومع ذلك: عرضٌ مقبولٌ واحد، أثرُ تدقيقٍ واحد، وعدّادٌ
تحرّك واحداً. فالحتميّةُ **ليست** من مانعِ التكرارِ في البوّابة — وهو غيرُ مشتركٍ
أصلاً — بل من ذرّيّةِ العمليّةِ في القاعدة. وهذا بالضبط ما لم يكن ممكناً معرفتُه
من عمليّةٍ واحدة.

**حمايةُ القاعدةِ من عملياتٍ متعدّدة (§6).** عاملان في عمليتين مستقلّتين، على حاجزٍ
زمنيٍّ مشترك، على ٤٨ مهمّة: **لا تنفيذَين متقاطعَين زمنيّاً** لنفسِ المهمّة، و٤٥
امتناعاً بـ`skipped_locked_elsewhere` (أي أنّ التزاحمَ وقع فعلاً ولم تكن النتيجةُ
توقيتاً سعيداً)، و0 فشل. ثمّ حُجِز القفلُ من **جلسةٍ خارجيّةٍ** لا من التطبيق فامتنع
العاملُ — فالحاجزُ في القاعدةِ لا في الكود؛ ثمّ أُسقط اتّصالُ حاملِ القفل فحُرِّر
القفلُ واشتغل العاملُ التالي — فلا قفلٌ يتيمٌ يُجمِّد المهامَّ إلى الأبد.

**اختبارُ الفشل (§8).** `SIGKILL` لبوّابةٍ بعد بثِّ العروضِ وقبلَ القبول: النسخةُ
الميتةُ لا تستجيب (مُوكَّد)، والقبولُ يتمّ على الباقية، والنتيجةُ التجاريّةُ كاملةٌ
بلا طلبٍ عالقٍ في `searching` وبلا عرضٍ منتظِرٍ يتيم.

#### ثلاثُ نتائجَ غيرِ متوقّعةٍ — بالتفصيلِ في `phase2-unit6-20260819-findings.md`

1. **عطبٌ في أداةِ القياس حكم بالإخفاق على نظامٍ سليم (أُصلح).** أوّلُ تشغيلٍ
   لـ`ride-dispatch` أعطى `business_failure`: «٤٠ رسالةَ عرضٍ مقابلَ ٢٥ عرضاً في
   القاعدة». والسببُ أنّ خمسَ عمليّاتِ سحبٍ متوازيةٍ من نفسِ البوّابةِ قرأت نفسَ
   المؤشّرِ فاستوعبت نفسَ الرسائلِ مرّتين. النظامُ كان سليماً والعدُّ وحدَه كذب.
   الإصلاحُ `bench/topology/serialize.ts` مع أربعةِ اختباراتِ وحدةٍ أحدُها يُعيد بناءَ
   العطبِ نفسِه. **ولو كان الكذبُ في الاتّجاهِ المعاكس لكان تقريراً أخضرَ على عطبٍ
   حقيقيّ** — ولهذا يُسجَّل هنا لا يُطمَس.
2. **سلوكٌ خاطئٌ في كودِ الإنتاج، مُبلَّغٌ ولم يُصلَح في هذه الوحدة.** الظافرُ نفسُه
   يُخبَر «سبقك سائق آخر إلى هذا الطلب.» عند تسليمٍ مكرَّرٍ لضغطتِه — مقيسٌ حرفيّاً.
   الموضع: `packages/application/bots/driver-dialog.ts` يردُّ `driver.offer_taken`
   على كلِّ `claimed=false` بلا تمييزِ «سُبِقتُ» من «أنا صاحبُ الإسنادِ أصلاً». الحالةُ
   التجاريّةُ سليمةٌ تماماً؛ الخطأُ في **ما يُقال للسائق**. وهو سابقٌ للتوزيعِ لا
   ناتجٌ عنه — الطبولوجيا أظهرته فقط. ولم يُكتَب توكيدٌ «ينجح» على السلوكِ الحاليّ،
   ولم يُخلَط إصلاحٌ سلوكيٌّ بوحدةِ تحقُّق.
3. **تصحيحُ ثابتٍ كتبتُه أنا خطأً.** كتبتُ أوّلاً «لا مهمّةَ نفّذها العاملان كلاهما»
   ففشل الثابت. والصحيحُ: القفلُ الاستشاريُّ يمنع **التقاطعَ الزمنيَّ** لا التكرارَ
   في نفسِ النافذة (كلُّ عمليّةٍ تجدول بـ`lastRunMs` في ذاكرتِها)، وأمانُ التكرارِ
   من ذرّيّةِ مطالبةِ الصفوفِ في المهامِّ نفسِها. فأُصلح الثابتُ وأُعلِن العددُ صريحاً
   في التقرير بدلاً من إخفائه.

#### المزدوجاتُ المُعلَنة (§7) وما لا تُثبته هذه الوحدة

المزدوجاتُ تُطبَع في كلِّ تقرير: مُرسِلُ تيليجرام ملتقِطٌ في كلِّ بوّابة، ومُرسِلاتُ
العاملِ الخارجةُ صامتة، ومزدوجُ Redis محلّيّ، وتوثيقُ المشرفِ مُختصَر.

* **لا تُثبت** تسليمَ رسالةٍ عبر تيليجرام، ولا سلوكَ Upstash الحقيقيّةِ (زمنُها،
  حدودُها، انقطاعُها، دقّةُ انتهاءِ صلاحيّتِها — كلُّها خارجَ المزدوج)، ولا سلوكاً
  على مُدارِ Supabase، ولا **سعةً** بأيِّ معنى: ٢١ فاعلاً في المجموعِ ليس حملاً،
  وبوّابتان على نفسِ الحاسوبِ ليستا عنقودَ إنتاج، وشبكةُ المِقبَسِ المحلّيِّ ليست
  شبكةً حقيقيّة.
* **حدُّ قياسٍ مُعلَنٌ يخصُّ المرحلةَ القادمة:** عدّاداتُ البوّابةِ في ذاكرةِ كلِّ
  عمليّة، والقراءةُ الصحيحةُ هي مجموعُ كشطِ كلِّ النسخ (وهو ما يفعله المشغّل)؛
  وعمليّةٌ تموت تفقد ما عدَّته — وهذا مُوكَّدٌ صريحاً في سيناريو موتِ البوّابة.

#### تصنيفُ الأرقام (§10)

| الرقم | التصنيف |
|---|---|
| ١١٢ توكيداً ناجحاً (١٠٢ في السيناريوهات + ١٠ في الفحصَين)، 0 إخفاق | **مقيس** على عنقودٍ محلّيٍّ متعدّدِ العمليات بمزدوجاتٍ مُعلَنة |
| بقاءُ ثابتِ «ظافرٌ واحدٌ بالضبط» مع توزيعِ المتنافسين على عمليتين | **مُثبَتٌ لهذه الحالة**: التوزيعُ موثَّقٌ بجدولِ التوجيه، والثابتُ مُقاسٌ بثلاثةِ شواهد. ولا يُقال «مُثبَتٌ للإنتاج» لأنّ التزاحمَ ٤ لا آلاف |
| «لا تقاطعَ زمنيَّ لتنفيذِ نفسِ المهمّة» عبر عمليتين | **مقيسٌ بمدى تنفيذٍ فعليٍّ داخلَ القفل**، مع ٤٥ امتناعاً يُثبت وقوعَ التزاحم |
| أزمنةُ p50 أعلاه | **معلوماتيّة** — وأكبرُ من 2-5 لأنّ الطلبَ يعبر مقبساً وحالةً مشتركةً الآن، ولا تُقرأ سعةً ولا تُقارَن ببيئةٍ أخرى |
| 1,862 اختباراً / 0 فشل · 0 خطأ نمطٍ · lint 0 خطأ / 41 ملاحظة · 9/9 حرّاس | **مقيسٌ ومحفوظٌ حرفيّاً** في `phase2-unit6-20260819-checks.txt` |

#### الدليل

| الملف | ما فيه |
|---|---|
| `docs/evidence/phase2-unit6-20260819-distributed.txt` | التقاريرُ الخمسةُ والفحصان بنصِّهما، مع الطبولوجيا وجدولِ توجيهِ الطلباتِ على النسخ |
| `docs/evidence/phase2-unit6-20260819-distributed.json` | نفسُها بنيويّاً: كلُّ توكيدٍ بنوعِه ونتيجتِه، والعدّادات، وتصنيفُ القياس |
| `docs/evidence/phase2-unit6-20260819-findings.md` | الثلاثةُ المكشوفةُ أعلاه بالتفصيلِ وسببِها الجذريِّ وموضعِها في الكود |
| `docs/evidence/phase2-unit6-20260819-checks.txt` | مخرجاتُ بوّاباتِ الجودةِ حرفيّاً |

#### نقطةُ التوقّف (§12 من أمرِ الوحدة)

**لم تبدأ** أيُّ قياسِ سعة: لا req/s، ولا مستخدمين متزامنين، ولا 2M. والمرحلةُ
التاليةُ المُعلَنة هي **Rate-Controlled Capacity Discovery**، وشرطُها الأوّلُ الآن
معروف: تجميعُ العدّاداتِ عبرَ النسخ، وإلّا فكلُّ رقمٍ سيُقاس من نسخةٍ واحدةٍ في
عنقودٍ من نسخٍ عدّة.

---

### 13.11 وحدة 2-3 — مولّد وصول مفتوح: معدل ورود منفصل عن الإكمال وعن عملية البوابة (2026-08-26)

**السؤال الذي تجيبه هذه الوحدة.** كان أي تشغيل سابق دفعةً مغلقة: لا يبدأ طلباً تالياً إلا عبر المسار الذي انتهى إليه السابق، فيصير معدل الإطلاق نتيجةً لزمن النظام لا حملاً مفروضاً عليه. لذلك بُني مولّد يعلن `rate` و`duration-ms` ويحوّلهما إلى مواعيد وصول، ثم يطلق كل محاولة في موعدها حتى إن بقيت المحاولات السابقة معلّقة. يسجّل التقرير انزلاق الجدولة، لأن مولّداً لا يلحق بالمواعيد لا يحق له أن ينسب فشله إلى الهدف.

| الملف | ما نُفّذ فعلاً |
|---|---|
| `bench/open-loop.ts` | جدول وصول حتمي من المعدل والمدة، إطلاق لا ينتظر الإكمال، وعدّادات الإطلاق/التزامن، ومئينات لانزلاق الجدولة وزمن النقل، وحالات HTTP وأخطاء النقل. |
| `bench/run-open-loop.ts` | واجهة CLI مستقلة لا تستورد تطبيق البوابة ولا تقلعه؛ تستهدف URL عبر HTTP، تقرأ حمولة ثابتة مرةً قبل القياس، وتحفظ تقريرَي JSON وMarkdown مع PID للمولّد وتصنيف الدليل. |
| `tests/unit/bench-open-loop.test.ts` | ٦ اختبارات حتمية للجدول، رفض مدخلات القياس غير المعقولة، فتح الحلقة مع طلبات بطيئة، كشف الانزلاق والأخطاء، والمئينات. |
| `tests/unit/bench-open-loop-cli.test.ts` | اختبار تشغيلي: خادم HTTP حقيقي في عملية الاختبار، مولّد Bun فرعي عبر TCP، **PID مختلف**، وملف JSON يثبت طلبين مكتملين بحالة 204. |
| `package.json` و`bench/README.md` | أمر `bun run bench:open-loop` ووصف الاستعمال، الدليل، وما لا تثبته الأداة. |

**النتائج المقاسة.** `bun run ci` نجح: **1,443 نجاح / 514 تخطٍّ / 0 فشل / 4,617 توكيداً / 151 ملفاً**. كما نجحت اختبارات المولّد السبعة: الستة الحتمية واختبار CLI التشغيلي، مع **0 فشل**. الدليل النصي الكامل محفوظ في `docs/evidence/phase2-unit3-20260826-checks.txt`.

**حدود الدليل لا تُخفى.** نتيجة HTTP تعني أن الناقل عاد بحالة، لا أن رحلةً أو تسجيلاً نجحا تجارياً. لا يوجد في هذه الوحدة مزيج حركة معتمد أو معيار أداء أو baseline، ولا قياس على Render أو Supabase أو Upstash أو تيليجرام حقيقي أو موزع حمل. لذا تصنيفها **مُنفّذ ومُختبَر** لا «مُقاس كسعة» ولا «مُثبت في الإنتاج».

**قرار الوحدة.** لا تُدخل وحدة 2-4 أرقام req/s كحكم قبل إعلان مزيج حركة وعتبات p95/p99، ولا تُستخدم واجهة المولّد ضد إنتاج أو ببيانات اعتماد إنتاجية. الناتج التالي الواجب هو baseline أصغر قابل لإعادة التشغيل مرتين وبشروط فشل معلنة.

---

## 14. القرارات الهندسية المهمة في هذه المرحلة (وأسبابها)

| # | القرار | السبب الهندسي |
|---|---|---|
| 1 | **عدم تغيير أي سطر من معمارية المشروع في المرحلة 0** | الأمر الحاكم يمنع تغييراً معمارياً قبل إكمال الفهم؛ والتغيير قبل وجود قياس يجعل أثره غير قابل للإثبات. |
| 2 | **جعل «استعادة محيط القياس» المرحلة 2 — قبل أي تحسين أداء** | أداة القياس الوحيدة معطوبة. أي تحسين أداء الآن سيكون بلا قبل/بعد، أي بلا دليل. القياس شرطُ الحكم لا نتيجته. |
| 3 | **جعل «إصلاح صحة البيانات» (المرحلة 3) قبل «التوسع الأفقي» (المرحلة 4)** | التوسع يضاعف أثر حالات التسابق. توسيع نظام به فقدان تحديث يحوّل خطأ نادراً إلى خطأ منهجي. الصحة أولاً بحسب أولويات المشروع. |
| 4 | **جعل سقف Telegram Bot API قراراً على مستوى المنتج، وإيقاف مراحل التوسع الكبرى عليه** | السقف خارجي وصلب (~30 رسالة/ث لكل بوت). تحسين داخلي بلا حسمه = جهد على عنق زجاجة غير حاكم. |
| 5 | **الإبقاء على المونوليث المعياري ولغة/إطار المشروع بلا تغيير** | التدقيق لم يجد سبباً هندسياً لتبديلها: الفصل بين الطبقات محترم، والعقود واضحة، والاعتماديات قليلة (~20 حزمة). الحدود المكتشفة كلها في *حالة العملية* و*أنماط الوصول للبيانات* و*الاعتماديات الخارجية* — لا في اللغة ولا في شكل الحزم. إعادة البناء بلا سبب ممنوعة. |
| 6 | **الإبقاء على PostgreSQL/PostGIS كمخزن أساسي** | الصحة تحت التزامن مبنية على مقوّمات القاعدة نفسها (`SKIP LOCKED`، فهارس فريدة جزئية، أقفال استشارية) وهي مُثبَتة عملياً في §7. استبدالها يهدم الضمانة القائمة مقابل مكسب غير مُبرهَن. |
| 7 | **عدم اعتبار أي تقنية (Kubernetes/ECS/Redis/Supabase/Render) مفروضة ولا محرَّمة** | قرار الطوبولوجيا يُتخذ في المرحلة 8 على أساس أرقام المرحلة 1 ونتائج المراحل 3–7، لا على أساس تفضيل مسبق. |
| 8 | **حفظ تقارير التدقيق الثلاثة خارج المستودع، ونقل خلاصاتها فقط إلى هنا** | المستودع يوثّق حالته لا مسوَّدات التحليل. وهذه الوثيقة يجب أن تبقى مرجعاً واحداً قابلاً للقراءة. |

---

## 15. أي تغيير جوهري في المعمارية

> **تقييدٌ واجبٌ (2026-08-26):**
> جملةُ «لم يُجرَ أي تغيير معماري» و«ولا سطر واحد من كود الإنتاج تغيّر» **ما زالتا قائمتين على مستوى الكود بحسب جردِ المدى** — فلا يظهر تغييرٌ في `apps/` أو `packages/` بين `b61ae98` و`6c4cd25` (القسم S4 وحدُّ الادّعاء فيه).
> **لكنّهما لا تعنيان أن المعمارية لم تُقرَّر**: فقراراتٌ معماريةٌ مُلزِمةٌ صدرت فعلاً بـADR 0028–0041 (منها Mini App، وإلغاء `capacity-profile-v1`، وتجميد ADR 0039). **مُقَرَّر ≠ مُنفَّذ** — القسم S3.
> والبند 5 أدناه («قرار سقف تيليجرام» مرشّحٌ مؤجّلٌ للمرحلة 1) **متجاوزٌ كقرار**: حُسِم بـADR 0028 وADR 0031، والمتبقّي منه **تنفيذٌ لم يبدأ**. وبقيةُ البنود 1–4 ما زالت **مرشّحةً غيرَ منفَّذة**.

**لم يُجرَ أي تغيير معماري في هذه المرحلة.** ولا سطر واحد من كود الإنتاج تغيّر. هذا الـ commit **يضيف وثيقة ويربطها** فقط.

التغييرات المعمارية **المرشَّحة** والمؤجَّلة إلى مراحلها، مع سببها (لا تُعَدّ مُقرَّرة حتى تُنفَّذ وتُوثَّق):

1. **فصل استقبال الويبهوك عن معالجته بطابور دائم** (المرحلة 5) — لأن المعالجة المتزامنة تربط زمن ردّنا بزمن أبطأ مزود خارجي.
2. **إخراج كل حالة العملية إلى مخزن مشترك** (المرحلة 4) — لأنها المانع البنيوي الوحيد للتوسع الأفقي.
3. **طابور صادر لتيليجرام مع حد معدل عام** (المرحلة 5) — لأن السقف الخارجي يوجب تنظيماً مركزياً للإرسال.
4. **دفع منطق اختيار السائقين إلى SQL المكاني** (المرحلة 6) — لأن التصفية في الذاكرة تجعل التكلفة دالةً في حجم المدينة.
5. **قرار سقف تيليجرام (تجزئة بوتات أو Mini App)** (المرحلة 1) — وهو **التغيير الأكبر أثراً على شكل النظام كله**، وقد يُعيد تعريف معنى «المستخدم المتزامن» في هذا المشروع.

---

## 16. القيود والافتراضات

### قيود
1. **Telegram Bot API** هو السقف الحاكم: ~30 رسالة/ث لكل بوت، ~20 رسالة/دقيقة لكل مجموعة. لا يُتجاوَز بالكود.
2. **Render خطة `starter`، نسخة واحدة لكل خدمة، بلا autoscaling، ونشر يدوي** — الطوبولوجيا الحالية.
3. **`SESSION_STORE=memory` في الإنتاج** — يمنع رفع عدد النسخ فوق واحد قبل المرحلة 4.
4. **العامل الخلفي مضمَّن في البوابة** — يتنافس على نفس الموارد.
5. **قواعد المستودع الصارمة مُلزِمة:** لا كود وهمي، لا ثوابت تجارية في الكود (كلها في `platform_settings`)، `city_id` في كل جدول، كل عملية حساسة عبر RPC ذرّي، لا ميزة بلا اختبار حقيقي، لا دمج بلا CI.
6. **لغة المستودع عربية** في التعليقات والوثائق، والمعرّفات بالإنجليزية.
7. **النطاق التجاري = اشتراك السائق الشهري فقط** (ADR 0027).

### افتراضات (تحتاج إقراراً وقد تُبطِل جزءاً من الخطة إن تغيّرت)

> **تحديثٌ واجبٌ (2026-08-26) — على القيود أعلاه والافتراضات أدناه:**
> * **الافتراض 2 («أن تيليجرام يبقى الواجهة الوحيدة… إن قُبِل Mini App») لم يعد افتراضاً ولا سؤالاً مفتوحاً**: Mini App **معتمدٌ معمارياً** بـADR 0028، وتيليجرام ليست تبعيةً وجوديةً بـADR 0031. **ولا يُعاد فتحُه كقرار.** ومع ذلك **لا يوجد تنفيذٌ إنتاجيٌّ لـMini App** (القسمان S3 وS4)، فالواجهةُ المُنفَّذةُ فعلاً اليوم هي بوتُ تيليجرام وحده.
> * **القيد 7 (النطاقُ التجاريُّ = اشتراكُ السائق الشهريُّ فقط — ADR 0027) ما زال نافذاً**، **ويُقرَأ معه تجميدُ ADR 0039**: لا يُكتب أيُّ تنفيذٍ للأجرة أو الدفع — **ولا بنًى تحضيرية** — قبل `DEC-11`، و`SR-04` يُعرَض بلا سعرٍ تقديريٍّ ولا وسيلةِ دفع.
> * **القيد 1 (سقف Telegram Bot API) ما زال قائماً تقنياً** لأنّ البوت هو الواجهةُ المُنفَّذةُ الوحيدة؛ لكنّه **لم يعد سقفاً حاكماً للتصميم** بعد ADR 0028 وADR 0031.
> * **الافتراض 1 (معنى «2M متزامن») لم يعد معلّقاً**: له تعريفٌ تشغيليٌّ يُقاس في ADR 0029، وطبيعتُه حُسِمت في ADR 0034 بوصفه **هدفَ قدرةٍ هندسية لا حملاً تجارياً متوقّعاً** — وهو تأكيدٌ حرفيٌّ لما قاله الافتراض 3 أدناه.

1. **أن «2M مستخدم متزامن» يعني مستخدمين نشطين متفاعلين، لا 2M اتصال مفتوح.** إن كان المقصود غير ذلك، يتغيّر نموذج الحمل كله.
2. **أن تيليجرام يبقى واجهة المستخدم الوحيدة.** إن قُبل Mini App، يتغيّر السقف والمعمارية.
3. **أن المدن الخمس هي النطاق الجغرافي.** 2M متزامن على خمس مدن سعودية افتراضٌ حملي بعيد جداً عن أي طلب حقيقي متوقَّع، ويجب أن يُقال ذلك صراحةً: الهدف هنا **هدف قدرة هندسية**، لا توقّع سوقي.
4. **أن الميزانية تسمح بطوبولوجيا أكبر من `starter` بنسخة واحدة.** بلا ذلك، لا معنى لمراحل التوسع.
5. **أن PostgreSQL يبقى المخزن الأساسي** حتى يُثبت قياسٌ خلاف ذلك.

---

## 17. الـ commit المرتبط بهذه الحالة — **Historical Snapshot / superseded**

> **لقطةٌ تاريخية — متجاوزةٌ بالقسم S1.**
> الجدولُ أدناه يعود إلى `ecb4518` وإلى رأسِ فرعٍ **لم يعد مرجعاً حياً**، ويذكر `feat/system-state-baseline` وطلبَ دمجٍ إلى `main` — **ولا توجد اليوم أيُّ طلبات دمجٍ مفتوحة (صفر)**، والعملُ يسير مباشرةً على `main`.
> **المرجعُ النافذُ لهذه الوثيقة: `main = 6c4cd25`** — القسم S1. ويُحفَظ الجدولُ لأنّ وصفَ محتوى وحدتَي 2-3 و 2-5 فيه صحيحٌ تاريخياً.

| | |
|---|---|
| **الحالة الموصوفة هنا تعود إلى** | `ecb45185e00fd403a55c4a5522f07a593fca41e6` (`ecb4518`) كنقطةِ أساسٍ للمرحلة 0؛ وحالةُ المرحلة 2 تعود إلى رأس `feat/system-state-baseline` بعد دمج `main` فيه (§13.8) |
| **الـ commit الذي أضاف هذه الوثيقة** | يُحدَّث تلقائياً بواقع تاريخ Git — هذه الوثيقة هي أول محتوى الـ commit التالي مباشرةً لـ`ecb4518`، ولم يتغيّر معه أي كود إنتاج |
| **آخِرُ وحدةٍ أُضيفت في هذه الحالة** | وحدة 2-3 (§13.11): `bench/open-loop.ts` و`bench/run-open-loop.ts` و`tests/unit/bench-open-loop*.test.ts`، وأدلةُ الجودة في `docs/evidence/phase2-unit3-20260826-checks.txt` — **بلا تعديلٍ على كودِ الإنتاج**؛ المولّد لا يستورد البوابة ويستهدفها عبر HTTP من عملية مستقلة |
| **الوحدةُ التي قبلها** | وحدة 2-5 (§13.9): `bench/scenarios/**` و`bench/run-scenarios.ts` و`bench/negative-control.ts` و`tests/unit/bench-scenario-contract.test.ts`، وأدلّتُها الأربعةُ في `docs/evidence/phase2-unit5-20260819-*` — بلا تعديلٍ على كودِ الإنتاج، والتغييرُ الوحيدُ خارجَ `bench/` هو استثناءُ `docs/evidence` من فحصِ تنسيقِ Biome |
| **الفرع** | `feat/system-state-baseline` → PR إلى `main` (بحسب عرف المستودع: CI يعمل على `main` و`feat/**`) |

---

## ملحق أ: ملفات الدليل لقياسات المرحلة 0

مخرجات التشغيل الفعلية محفوظة حرفياً في المستودع، فلا يُطلَب التصديق بالثقة:

| الملف | ما يحتويه |
|---|---|
| `docs/evidence/state-baseline-20260818-lint.txt` | مخرج `bun run lint` كاملاً |
| `docs/evidence/state-baseline-20260818-typecheck.txt` | مخرج `bun run typecheck` |
| `docs/evidence/state-baseline-20260818-tests.txt` | مخرج المجموعة الكاملة وملخّصها (1,772 / 0 / 156.92s) |
| `docs/evidence/state-baseline-20260818-match-race.txt` | مخرج سباق المطابقة بسيناريويه |
| `docs/evidence/phase2-unit1-20260818-load-test-PASS.txt` | جريان `load-test.ts` ناجحٌ بعد إصلاح وحدة 2-1، بتأكيد `drivers = 20/20` — يُقرأ **مقابل** ملفّ الفشل أدناه |
| `docs/evidence/phase2-unit1-20260818-typecheck.txt` | فحص الأنواع بعد إدخال `scripts/**` و`bench/**` — خروج 0 |
| `docs/evidence/phase2-unit1-20260818-lint.txt` | فحص التنسيق بعد الإصلاح — خروج 0 |
| `docs/evidence/phase2-unit1-20260818-tests.txt` | المجموعة الكاملة بعد الإصلاح: 1,772 نجاح / 0 فشل |
| `docs/evidence/phase2-unit2-20260819-typecheck.txt` | فحص الأنواع بعد قلع تكرار عقد الضبط: 0 خطأ |
| `docs/evidence/phase2-unit2-20260819-lint.txt` | التحقّق الأسلوبي: خروج 0 و41 ملاحظة — نفس عددها قبل التغيير، فلا انحراف أُدخل |
| `docs/evidence/phase2-unit2-20260819-tests.txt` | المجموعة الكاملة بعد قلع التكرار: 1,772 نجاح / 0 فشل — **العدد نفسه**، وهو إثبات أن إعادة الهيكلة لم تُغيّر سلوكاً |
| `docs/evidence/phase2-unit3-20260819-tests.txt` | المجموعة الكاملة بعد إضافة الناقل المُعلَن وعدّاد الصادر: 1,783 نجاح / 0 فشل |
| `docs/evidence/phase2-unit4-20260819-foundation-run1.txt` | سلسلةُ إثبات أساس القياس، التشغيل الأوّل: 15/15 فحصاً |
| `docs/evidence/phase2-unit4-20260819-foundation-run2.txt` | التشغيل الثاني في **عمليّةٍ منفصلة** — بصمةُ الحالة مطابقةٌ حرفيّاً للأوّل، وهو إثباتُ الحتميّة عبر العمليّات لا داخل العمليّة الواحدة |
| `docs/evidence/phase2-unit4-20260819-guards.txt` | الحرّاس التسعة، lint، typecheck، الاختباراتُ الجديدة، والحزمةُ الكاملة، ونتيجةُ مقارنة البصمتين |
| `docs/evidence/phase2-unit4-20260819-tests.txt` | المجموعة الكاملة بعد بناء أساس القياس: 1,815 نجاح / 0 فشل |
| `docs/evidence/phase2-unit41-20260819-foundation-db1.txt` | سلسلةُ الإثبات مُعاد إنتاجُها على بيئةٍ بُنيت من الصفر في جلسة تسليمٍ جديدة: 15/15، وبصمتان لكلّ جدول |
| `docs/evidence/phase2-unit41-20260819-foundation-db2-rebuilt.txt` | نفسُ السلسلة بعد **إسقاط قاعدة القياس وإعادة ترحيلها**، فمعرّفاتُ المدن مختلفةٌ عن الأولى |
| `docs/evidence/phase2-unit41-20260819-portability.txt` | إثباتُ العيب وإثباتُ إصلاحه في ملفٍّ واحد: ٧ بصماتٍ خامٍّ اختلفت، و٧ بصماتٍ منقولةٍ تطابقت، وفروقٌ منطقيّة = 0 — مع خطوات إعادة التنفيذ حرفيّاً |
| `docs/evidence/phase2-unit41-20260819-tests.txt` | المجموعة الكاملة بعد الإصلاح: 1,821 نجاح / 0 فشل / 0 تخطٍّ، 7,046 `expect()` |
| `docs/evidence/phase2-unit5-20260819-scenarios.json` | تقريرُ السيناريوهات الثلاثة آليّاً: كلُّ توكيدٍ باسمِه ومتوقَّعِه وملاحَظِه، والمزدوجاتُ المُعلَنة، وفروقُ المقاييس، وأزمنةُ الفاعلين — صيغةٌ قابلةٌ للمقارنة بين تشغيلين |
| `docs/evidence/phase2-unit5-20260819-scenarios.txt` | التقريرُ نفسُه نصّاً مقروءاً بالعربيّة |
| `docs/evidence/phase2-unit5-20260819-negative-control.txt` | **الشاهدُ السالب**: ثلاثةُ إفساداتٍ مقصودةٍ لحالةِ الإسناد — اثنان كشفَهما التوكيدُ، والثالثَ رفضته القاعدةُ بقيدِ `order_offers_single_accepted` |
| `docs/evidence/phase2-unit5-20260819-checks.txt` | typecheck وlint (0 خطأ / 41 ملاحظة) والحرّاس التسعة واختباراتُ الإطار (٢٣) والحزمةُ الكاملة: **1,844 نجاح / 0 فشل / 0 تخطٍّ**، ١٤٨ ملفاً |
| `docs/evidence/phase2-unit3-20260826-checks.txt` | بوابة `bun run ci` كاملة بعد وحدة مولّد الوصول المفتوح: **1,443 نجاح / 514 تخطٍّ / 0 فشل / 4,617 توكيداً / 151 ملفاً**؛ وتشمل اختبارات النواة واختبار PID المنفصل عبر TCP |
| `docs/evidence/phase2-pr1-20260819-checks.txt` | فحوصُ فرع `production-hardening` بعد حلّ تعارض PR #1: typecheck وlint والحزمةُ الكاملة على شجرة `main` |
| `docs/evidence/phase2-merge-20260819-checks.txt` | فحوصُ فرع `feat/system-state-baseline` بعد دمج `main` فيه وحلّ تعارض PR #12 |
| `docs/evidence/phase2-merge2-20260819-checks.txt` | تشخيصُ تعارضِ الطلب #13 بالأوامرِ ونتائجِها، وبوّاباتُ الجودةِ بعد الدمجِ الثاني |
| `docs/evidence/phase2-unit1-20260818-race-subscriptions.txt` | سباقات الاشتراك بعد إصلاح أخطاء أنواعها — كلّها سليمة |
| `docs/evidence/state-baseline-20260818-load-test-FAILURE.txt` | **سجل فشل** `load-test.ts` مع الـ stack trace — محفوظ قصداً لأن الفشل جزء من الحالة لا شيء يُخفى |

**إضافةٌ مؤرَّخةٌ (2026-08-26 — على `main = 6c4cd25`):** مجلّدُ الأدلة أُعيدَ تنظيمُه بالبند `F0-06`، فصارت الأدلةُ الجديدةُ تُرفع في مجلداتٍ مصنّفة وفق `docs/evidence/README.md`. والملفاتُ أعلاه ما زالت في جذرِ `docs/evidence/` ولم تُنقَل.

| الملف | ما يحتويه |
|---|---|
| `docs/evidence/README.md` | قاعدةُ تسميةِ الأدلة وتصنيفِها وسلّمُ الإحالة إلى الخارطة (البند `F0-06`) |
| `docs/evidence/architecture/` | أدلةُ القرارات المعمارية — **أربعةُ ملفاتٍ** أُضيفت مع ADR 0033–0041 |
| بقيةُ المجلدات المصنّفة | **تسعةُ مجلداتٍ مُنشأةٌ وفارغةٌ بـ`.gitkeep`** — هيكلٌ جاهزٌ لا دليلٌ فيه بعد |

**وقياسٌ مأخوذٌ من شجرةِ `main` (لا من تشغيل):** إجمالُ ما في `docs/evidence/` اليوم **67 مدخلاً** = `README.md` واحد + تسعة `.gitkeep` + أربعة في `architecture/` + ثلاثةٌ وخمسون ملفاً في الجذرِ القديم.

---

## ملحق ب: أين توجد تفاصيل التدقيق

خلاصات هذه الوثيقة مستخرجة من تدقيق قراءة ساكنة بأرقام أسطر لثلاث طبقات: طبقة البيانات (المخططات، RPCs، المعاملات، الأقفال، الفهارس)، طبقة وقت التشغيل (البوابة، العامل، الإرسال، التتبع، المدفوعات، الزمن الحقيقي)، وطبقة البنية التحتية (Render، Docker، البيئة، السكربتات، الاختبارات، CI، التشغيل، الاعتماديات الخارجية). المسارات وأرقام الأسطر المشار إليها في §3 و§4 و§5 كافية لإعادة الوصول إلى كل موضع مذكور دون وسيط.

---

## ملحق ج: المرجع الحاكم للتنفيذ (إضافة 2026-08-26 — البند `F0-05`)

> **إضافةٌ فقط.** لم يُعدَّل ولم يُحذَف أيُّ نصٍّ سابقٍ في هذه الوثيقة.

صدرت `docs/ROADMAP-MASTER.md` بوصفها **الخارطةَ الحاكمةَ للتنفيذ**، وهي تُلغي وتستبدل `docs/roadmap.md` ومسوّدتَي خارطةِ الطريقِ المرفقتَين من مديرِ المشروع.

**توزيعُ الولاية بين الوثائقِ الثلاث** — لا تعارضَ بينها لأن كلَّ واحدةٍ تحكم بابَها:

| الوثيقة | ما تحكمه | مثالُ سؤالٍ تُجيبه |
| --- | --- | --- |
| `docs/MASTER_DIRECTIVE.md` | **قواعدُ العمل** | كيف يُكتَب الكودُ ويُراجَع ويُدمَج؟ |
| `docs/SYSTEM_STATE.md` (هذه) | **وصفُ ما هو قائمٌ فعلاً** | ما الذي يعمل اليومَ وما حالتُه؟ |
| `docs/ROADMAP-MASTER.md` | **ما يجب أن يُبنى، وبأيِّ ترتيب، وبأيِّ دليل** | ما البندُ التالي وما بوابتُه؟ |

**قاعدةٌ إجرائية:** من أنجزَ بنداً في الخارطةِ فعليه **أمران**: تعليمُ البندِ في القسم 25 من الخارطةِ مع ملفِّ دليلِه، ثم — إن غيّرَ الإنجازُ **حالةَ النظامِ القائمة** — إضافةُ سطرٍ في هذه الوثيقةِ يصف الحالةَ الجديدة. الخارطةُ لا تصف الحالةَ، وهذه الوثيقةُ لا تصف الخطة.

**قاعدةُ الأدلة:** أدلةُ الخارطةِ تُرفَع في `docs/evidence/<التصنيف>/` وفق `docs/evidence/README.md`. وسلّمُ الأدلةِ ذو الدرجاتِ الخمس في القسم 1.4 من الخارطةِ **نافذٌ على هذه الوثيقةِ أيضاً**: لا يُوصَف شيءٌ هنا بأنه «يعمل» إلا بدرجتِه في السلّم.

### حالةُ المرحلة `F0` (2026-08-26)

بنودُ `F0-01` إلى `F0-06` منجَزة، وقراراتُها:

| القرار | الموضوع |
| --- | --- |
| [ADR 0028](adr/0028-miniapp-is-the-product-bot-is-the-gateway.md) | Mini App هو المنتج، والبوتُ بوابةُ دخولٍ وإشعارٍ حرجٍ فقط |
| [ADR 0029](adr/0029-concurrent-user-definition-and-capacity-profile.md) | تعريفُ المستخدمِ المتزامن، وإلغاءُ `capacity-profile-v1` واستبدالُه بـ`v2` المُولَّد |
| [ADR 0030](adr/0030-slos-are-binding-with-owner-and-error-budget.md) | أهدافُ مستوى الخدمةِ مُلزِمةٌ بمالكٍ وميزانيةِ خطأ |
| [ADR 0031](adr/0031-telegram-is-not-an-existential-dependency.md) | تيليجرام ليست تبعيةً وجودية — التشغيلُ من متصفحٍ بهُويةٍ مستقلة |
| [ADR 0032](adr/0032-roadmap-master-v1_1-corrections.md) | تصحيحاتُ الخارطةِ الإصدارُ 1.1، وخمسةُ تناقضاتٍ حاكمةٍ مرفوعةٍ للاعتماد |

**وبوابةُ `F0` لا تُعَدُّ مفتوحةً بعد**: القراراتُ `DEC-09` إلى `DEC-13` في القسم 26 من الخارطةِ ما زالت منتظرةً اعتمادَ مديرِ المشروع، ومنها ما يمسُّ شرطَ الإطلاقِ نفسَه (2,000,000 أم 2,400,000؟).

---

### تصحيحٌ مؤرَّخٌ لملحق ج (2026-08-26 — على `main = 6c4cd25`)

> **إضافةٌ فقط، وفقاً لقاعدةِ هذا الملحق نفسِه:** لم يُحذَف أيُّ نصٍّ من ملحق ج أعلاه، وهو يُقرَأ **لقطةً تاريخيةً لحالةِ 8638f7f**. **وما في هذا التصحيحِ يسبقُ ما قبله عند أيِّ تعارض.**

**1) الجملةُ المتجاوزةُ صراحةً.** قولُ ملحق ج إنّ `DEC-09` إلى `DEC-13` «ما زالت منتظرةً اعتمادَ مديرِ المشروع»، وإنّ شرطَ الإطلاق «2,000,000 أم 2,400,000؟» — **لم يعد صحيحاً كوصفِ حالةٍ حالية، ولا يُستند إليه.** الحالةُ النافذةُ (`ROADMAP-MASTER` v1.3، القسم 26):

| القرار | الحالةُ النافذة | ملاحظة |
| --- | --- | --- |
| `DEC-09` | **APPROVED** | لا يُعاد فتحُه |
| `DEC-10` | **APPROVED** | لا يُعاد فتحُه |
| `DEC-11` | **BLOCKED — Legal/Regulatory** | لا يُغلَق إلا برأيٍ من مستشارٍ قانونيٍّ سعوديٍّ مؤهّل أو جهةٍ رسميةٍ مختصةٍ — **ليس قراراً إدارياً** |
| `DEC-12` | **APPROVED** | لا يُعاد فتحُه |
| `DEC-13` | **APPROVED** | لا يُعاد فتحُه |

**2) سببُ NO-GO واحدٌ لا خمسة.** بوابةُ `F0` = **NO-GO بسبب `DEC-11` وحده**، وتبقى علامتُها `[~]` و**لا تُكتب `[x]` ما دام `DEC-11` محجوباً**. ونطاقُ الحجب **محدودٌ** لا يوقف المشروع كلّه.

**3) شرطُ الإطلاق ليس سؤالاً مفتوحاً.** ADR 0033 يرفض **2,400,000** كشرطِ إطلاقٍ ثالث. وADR 0034 يفصل **هدفَ القدرة = 2,000,000** عن **الحملِ التجاريِّ المتوقّع**، ورقمُ 150,000 فيه **مبدئيٌّ غيرُ معتمد** — لا يُقتبَس كرقمٍ مُقرَّر.

**4) درجةُ أرقامِ القدرة.** في ADR 0037 **كلُّ حصصِ القطاعاتِ العشر موسومةٌ `capacity baseline assumption — unmeasured`** — فلا تُوصَف بـmeasured ولا verified في أيِّ موضعٍ من هذه الوثيقة.

**5) قيدان معماريان نافذان يجب أن يُقرَأا مع جدولِ ADR أعلاه:**

* **ADR 0035:** Mini App **لا يُنشئ حالةً تجاريةً جديدة**؛ وسلسلةُ الهُوية **واحدةٌ غيرُ مختصرة**؛ والبنيةُ قبل الشاشات.
* **ADR 0039 (تجميد):** لا أجرةَ ولا دفعَ — **ولا بنًى تحضيريةً** — قبل `DEC-11`؛ و`SR-04` بلا سعرٍ تقديريٍّ ولا وسيلةِ دفع.

**6) جدولُ ADR في ملحق ج ناقصٌ.** هو يتوقف عند ADR 0032، وعددُ ADR على `main` اليوم **41** — ومنها **0033–0041** المُلزِمةُ أعلاه، ومنها إعادةُ ترقيمِ `0016-subscription-wallet-ledger-source-of-truth.md` إلى **`0036-`** (بلا تغييرٍ في المحتوى). والمرجعُ الحاكمُ لقائمةِ ADR هو `docs/adr/` نفسُه، لا هذا الجدول.

### أثرٌ مباشرٌ على أرقامٍ مذكورةٍ في هذه الوثيقة

الهدفُ المعلَنُ «2,000,000 مستخدمٍ متزامن» صار له **تعريفٌ تشغيليٌّ يُقاس** في ADR 0029: جلسةُ Mini App مصادَقةٌ ونشِطةٌ أجرت طلباً واحداً على الأقلِّ خلال نافذةِ خمسِ دقائقَ متحرّكة. وكلُّ رقمِ حِملٍ مشتقٍّ يُحسَب آلياً بـ`bench/capacity/derive.py` من `bench/capacity/profile-v2.json`، ويُنشَر في `docs/capacity/`. **لا يُكتَب رقمٌ مشتقٌّ بيدٍ في أيِّ وثيقةٍ بعد اليوم.**

---

| **تصحيحٌ مُلحَق السادس والأربعون — 2026-09-08 (إغلاقُ `F7-01`/`CAP-003` — نقلُ المطابقةِ إلى استعلامِ PostGIS واحد، اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`F7-01` و`CAP-003` قُلِبا إلى `[x]` في `docs/ROADMAP-MASTER.md` §F7 و§CAP بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) على `main` لا قبلَها، ونصُّ البندينِ لم يُمَسَّ حرفًا (`ح-1`) — عمودُ الحالةِ وحدَه قُلِبَ من `[ ]` إلى `[x]`.** **وما نُفِّذ في PR #22 (دُمجَ في `72a61cb`):** نقلَ المطابقةَ من «حمْلِ كلِّ سائقي المدينةِ ثمَّ ترتيبِهم في الذاكرة» إلى استعلامِ PostGIS واحد يُضيّقُ النافذةَ بالبواباتِ الصلبةِ قبلَ الدومين: `ST_DWithin` (فهرسُ GiST) + `is_available` + `verified` + عمرُ الموقعِ + `ORDER BY distance LIMIT k` — كلُّها في `packages/infrastructure/dispatch/dispatch-adapters.ts:findNearbyAvailableForDispatch`. والدومينُ يحتفظُ بـ`evaluateCandidates` حارسًا نهائيًّا للخدمةِ والاشتراكِ والاستبعادِ فوقَ النافذة، ومسارٌ تشخيصيٌّ احتياطيٌّ (`findAvailableInCity`) يبني قائمةَ الرفضِ المُسبَّبةَ كاملةً عندَ فراغِ النافذة. وحدُّ النافذةِ (`matching_candidate_limit`، افتراضيٌّ ٥٠، `is_provisional`) في `platform_settings` لا في الكود. **والجولاتُ الثلاثُ المتتاليةُ كلُّ وظيفةٍ فيها `success` (قُرِئَ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ):** (1/3) التشغيلُ `34184258662` على `9d8dedf` · (2/3) التشغيلُ `34188780141` على `c10c3a0` · (3/3) التشغيلُ `34242921957` على `72a61cb` (HEAD الذي يحوي تنفيذَ CAP-003) — كلُّها في `main`. والوظائفُ في الثلاثةِ كلِّها: `verify` و`تكاملٌ على PostgreSQL حقيقيٍّ` و`تكاملٌ على Redis حقيقيٍّ` — كلُّها `success`. CI على فرعِ PR #22 قبلَ الدمج كان أخضرَ بالكامل (التشغيلُ `34240703786`) والحالةُ `CLEAN`/`MERGEABLE`. **وهذا الـcommitُ تغييرُ حالةٍ وتوثيقٌ وحدَه**: عمودُ الحالةِ في الخارطةِ وملفُ الدليلِ `docs/evidence/architecture/F7-01-CAP-003-20260908.md` وحدَهما. **وما لم يُقلَب يُقرأُ حرفيًّا**: **`F6-03` يبقى `[ ]`** (Outbox موحَّد — يجري تدقيقُه في وحدةٍ منفصلة) · **`CAP-002`/`F6-04` تبقى `[ ]`** (مُرسِل تيليجرام واعٍ بالحد) · **`F5-04`/`F5-06`/`F5-07`/`F5-08` لم تُمَسَّ** · **ولا `ADR` منشورٌ عُدِّل (`ح-6`)** · **ولا `R-17`/Socket.IO/`DEC-11`/`F0`/`DEC-06` مُسَّ** · **ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** — المُتحقَّقُ منه اكتمالُ الجولاتِ الخضراءِ في CI والاختباراتُ على PostgreSQL حقيقيٍّ، لا throughputُ الإنتاجِ ولا latencyُ المطابقةِ تحتَ حملٍ. الدليلُ `docs/evidence/architecture/F7-01-CAP-003-20260908.md`. | `docs/evidence/architecture/F7-01-CAP-003-20260908.md` | مُنفَّذ · مُختبَر · مُتحقَّق منه | `التزامُ هذا السطرِ نفسُه` |

| **تصحيحٌ مُلحَق السابع والأربعون — 2026-09-08 (إغلاقُ `F6-03` — Outbox معاملاتيٌّ موحّدٌ، اكتمالُ الجولاتِ الخضراءِ الثلاثِ `ح-4`)** | **`F6-03` قُلِبَ إلى `[x]` في `docs/ROADMAP-MASTER.md` §F6 بعدَ اكتمالِ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`) على `main` لا قبلَها، ونصُّ البندِ لم يُمَسَّ حرفًا (`ح-1`) — عمودُ الحالةِ وحدَه قُلِبَ من `[ ]` إلى `[x]`.** **وهذا إغلاقٌ مركَّبٌ بلا جديدِ كودٍ** — التوحيدُ نُفِّذَ في ثلاثِ هجراتٍ متعاقبةٍ (المراحلُ 1-3 على `2026-09-08`) دُمجتْ في `main` قبلَ هذا القلبِ: `20260908010000_unified_outbox_safety_incident.sql` (safety→`safety_incident`) · `20260908020000_unified_outbox_subscription_notice.sql` (subscription→`subscription_notice`) · `20260908030000_unified_outbox_broadcast_recipient.sql` (broadcast→`broadcast_recipient`). كلُّ هجرةٍ تُضيفُ نوعًا إلى `notification_outbox` وتجعلُ الدوالَّ القديمةَ أغلفةً عليه بنمطِ expand-then-contract. **والتدقيقُ قراءةً من الكودِ الفعليِّ أثبتَ**: (١) لا موضعَ واحدٌ في الكودِ الإنتاجيِّ (`apps/` و`packages/`) يكتبُ مباشرةً في الجداولِ القديمةِ الثلاثةِ (`safety_incident_deliveries` و`subscription_notices` و`broadcast_recipients`) — البحثُ عن `insert into` لها عادَ فارغًا؛ (٢) الكودُ الإنتاجيُّ يستدعي أغلفةَ RPC الموحّدةَ (`claim_broadcast_recipients` و`claim_subscription_notices` و`claim_safety_incident_delivery`) التي تعملُ على `notification_outbox`؛ (٣) قيدُ النوعِ على `notification_outbox` يشمَلُ كلَّ الأنواعِ الموحّدةَ — فالمصدرُ واحدٌ ظاهرٌ في القاعدةِ، و`dedup_key` فريدٌ `(kind, dedup_key)`، وحالةٌ موحّدةٌ. **والجولاتُ الثلاثُ المتتاليةُ كلُّ وظيفةٍ فيها `success` (قُرِئَ `conclusion` لكلِّ وظيفةٍ من الواجهةِ البرمجيةِ):** (1/3) التشغيلُ `34242921957` على `72a61cb` · (2/3) التشغيلُ `34244316453` على `1a16fc8` · (3/3) التشغيلُ `34244831884` على `0103b26` (HEAD الحاليُّ) — كلُّها في `main`. والوظائفُ في الثلاثةِ كلِّها: `verify` و`تكاملٌ على PostgreSQL حقيقيٍّ` و`تكاملٌ على Redis حقيقيٍّ` — كلُّها `success`. **وهذا الـcommitُ تغييرُ حالةٍ وتوثيقٌ وحدَه**: عمودُ الحالةِ في الخارطةِ وملفُ الدليلِ `docs/evidence/architecture/F6-03-20260908.md` وحدَهما. **وما لم يُقلَب يُقرأُ حرفيًّا**: **`F6-04`/`CAP-002` تبقى `[ ]`** (مُرسِل تيليجرام واعٍ بالحد) — الخطوةُ التاليةُ · **`F6-05`/`F6-06`/`F6-07` لم تُمَسَّ** · **ولا `ADR` منشورٌ عُدِّل (`ح-6`)** — `ADR 0061` قائمٌ والتنفيذُ مطابقٌ له · **ولا `R-17`/Socket.IO/`DEC-11`/`F0`/`DEC-06` مُسَّ** · **ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** — المُتحقَّقُ منه اكتمالُ الجولاتِ الخضراءِ في CI والاختباراتُ على PostgreSQL/Redis حقيقيٍّ، لا throughputُ الإنتاجِ ولا latencyُ التسليمِ تحتَ حملٍ. الدليلُ `docs/evidence/architecture/F6-03-20260908.md`. | `docs/evidence/architecture/F6-03-20260908.md` | مُنفَّذ · مُختبَر · مُتحقَّق منه | `التزامُ هذا السطرِ نفسُه` |

## ملحق د: تدقيقُ F5-02 وحالةُ SCL-001 … SCL-005 (2026-09-08)

> **إضافةٌ فقط:** هذا الملحقُ يصفُ نتيجةَ تدقيقٍ للقراءةِ من الكودِ الفعليِّ، لا تغييرَ حالةِ مشروعٍ فيه. لم تُقلَبْ حالةُ بندٍ واحد، ولم تُكتبْ شيفرةٌ ولا هجرةٌ ولا اختبار. والمرجعُ التفصيليُّ هو `docs/evidence/correctness/F5-02-20260908.md`.

**البنودُ الخمسةُ بعدَ التدقيق:**

| البندُ | الحالةُ | القراءةُ من الكودِ |
| --- | --- | --- |
| `SCL-001` | `[ ]` | خريطةُ `Map` للـdedup قائمةٌ في `apps/gateway/src/routes/update-dedup.ts:54` وتُنادى في `telegram-webhook.ts:304`؛ ومصدرُ القرارِ صار PostgreSQL (`claim_telegram_update`) من `BUG-002`/`F6-01`. لا يُغلَق: الخريطةُ قائمةٌ، ونصُّ الحلّ يقولُ Redis والمُنفَّذُ PostgreSQL (`ح-1`). |
| `SCL-002` | `[x]` | مُغلَقٌ سابقاً، لا يُمَسُّ. |
| `SCL-003` | `[x]` | مُغلَقٌ سابقاً، لا يُمَسُّ. |
| `SCL-004` | `[ ]` | الناقلُ داخلَ العمليةِ في `packages/infrastructure/tracking/event-bus.ts:112` (`Map<symbol, Entry>`)؛ والمجرى المشتركُ لم يُنفَّذ. وحاجزٌ تقنيٌّ موثَّق: Upstash REST لا يدعمُ `SUBSCRIBE` (`event-bus.ts:14`). |
| `SCL-005` | `[ ]` | خريطةُ `tripId → messageId` قائمةٌ في `packages/application/tracking/customer-live-relay.ts:120` (`Broadcast.messageId`)؛ والحاجةُ لم تُسقَطْ (انتقالُ Mini App مفتوحٌ في `F4-04`/`F4-05`/`F4-07`). |

**`F5-02` يبقى `[ ]`** — بندٌ ميتا لا يُغلقُ حتى تنغلقَ بنوده الخمسةُ كلُّها، وثلاثةٌ منها (`SCL-001`/`SCL-004`/`SCL-005`) تبقى `[ ]` لأنّها تحتاجُ تنفيذاً حقيقيّاً مطابقاً لنصِّ الحلّ، لا توثيقاً. ولا `مَقيس` ولا `مُثبَت` (`ح-5`). ولا نصُّ بندٍ عُدِّل (`ح-1`) ولا `ADR` منشورٌ عُدِّل (`ح-6`).

## ملحق هـ: تنفيذُ `SCL-004` — ناقلُ أحداثٍ مشتركٌ عبر Redis Streams (2026-09-08)

> **تنفيذٌ فعليٌّ لـ`SCL-004`، لا قلبُ حالةٍ بعد.** المُنفَّذُ ناقلُ أحداثٍ مشتركٌ عبر Redis Streams فوقَ عميلِ Upstash REST القائمِ (`XADD`/`XREAD` طلبٌ/استجابةٌ بلا مقبسٍ دائم)، يُغلِّفُ الناقلَ المحليَّ فيطبِّقُ «مشتركين متعدّدين» عبرَ النسخِ. والقلبُ إلى `[x]` معلَّقٌ على ثلاثِ جولاتِ CI خضراء (`ح-4`). والمرجعُ التفصيليُّ هو `docs/evidence/distributed/SCL-004-20260908.md` و`docs/adr/0058-tracking-event-carrier-is-redis-streams.md`.

**ما تغيّر:**

- **`packages/infrastructure/tracking/redis-stream-event-bus.ts`** (جديد) — `createRedisStreamTrackingEventBus` غلافٌ يُحقنُ الناقلَ المحليَّ ويُضيفُ فوقهُ Streams: `publish` ← `XADD` (بـ`MAXLEN ~ 1000` وحقلِ `origin`) ثمّ `local.publish`، وماسحٌ كلَّ 250ms `XREAD` من مؤشّرٍ صاعدٍ يُعيدُ نشرَ ما ليسَ من مصدرِ هذه النسخةِ. تجاوزُ المصدرِ نفسِه، وتجاوزُ الإدخالِ السيّئِ بلا إسقاطِ الماسح.
- **`apps/gateway/src/container.ts`** — توصيلٌ مشروطٌ بوجودِ Redis: `distributedTrackingBus` يُنشأُ ويُبدأُ ماسحُه عندَ البناءِ ويُوقَفُ في `close`؛ وإن غابَ Redisُ يبقى الناقلُ المحليُّ كما كان.
- **`docs/adr/0058-tracking-event-carrier-is-redis-streams.md`** (جديد) — قرارٌ معماريٌّ يُلغي جزءَ «الناقلِ في العمليةِ» من ADR-0016 وحده (لا SSE ولا الموقعُ الحيُّ ولا رفضُ Pub/Sub). يختارُ Streams لكونِ `XADD`/`XREAD` طلبٌ/استجابةٌ تعملُ على عميلِ REST. لا `consumer group` (تُوزّعُ الحملَ لا تُكرّرُه).
- **اختبارات** — 5 اختباراتِ وحدةٍ بعميلِ Redis مزيفٍ يُثبتُ المنطقَ (نشرٌ من نسخةٍ وتسليمٌ في أخرى، تجاوزُ المصدرِ، تجاوزُ الإدخالِ السيّئِ، تفويضُ الاشتراكِ، دورةُ حياةِ `start`/`stop`)، واختبارُ تكاملٍ على Redis حقيقيّ (Upstash في CI) يُثبتُ `XADD`/`XREAD` عبرَ نسختين.

**حالةُ البنودِ بعدَ هذا التنفيذ:**

| البندُ | الحالةُ | السببُ |
| --- | --- | --- |
| `SCL-004` | `[x]` (مُغلَقٌ بعدَ ثلاثِ جولاتٍ خضراءَ) | التنفيذُ مكتملٌ ومُختبَرٌ. قُلِبَ `[ ]`→`[x]` بعدَ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`): 34169516237 · 34169514529 (إعادةُ تشغيلٍ) · 34169924470. |
| `SCL-001` | `[ ]` | لم يُمَسّ — يحتاجُ قراراً (Redis حرفيّاً أم اعتمادُ PostgreSQL durable dedup). |
| `SCL-005` | `[ ]` | لم يُمَسّ — خريطةُ `tripId → messageId` قائمةٌ، والحاجةُ لم تُسقَطْ. |

**`F5-02` يبقى `[ ]`** — بقيَ منه بنودٌ `SCL-001` و`SCL-005` (بنودٌ منفصلةٌ). **ونصُّ حلّ `SCL-004` لم يُمَسَّ حرفًا (`ح-1`)**. ولا `مَقيس` ولا `مُثبَت` (`ح-5`) — اختبارُ Redis الحقيقيّ أخضرُ في CI. ولا `R-17`/Socket.IO/`DEC-11`/`F0` مُسَّ.

## ملحق و: تنفيذُ `SCL-001` — تبنّي PostgreSQL durable dedup + حاجزُ إقلاعٍ للإنتاج (2026-09-08)

**`SCL-001` قُلِبَ من «Redis» إلى «PostgreSQL durable dedup» بقرارٍ حوكميٍّ صريحٍ** من صاحبِ
المشروع — **استثناءٌ من ح-1** (التي تمنعُ تعديلَ نصِّ الحلّ وحدَها)، لأنَّ النصَّ الأصليَّ صارَ
مُتجاوَزاً بتنفيذٍ أفضلَ (PostgreSQL `claim_telegram_update`، ADR-0054، أُضيفَ في
`BUG-002`/`F6-01`) بعدَ كتابتِه. الإبقاءُ على «Redis» كان سيُوجِبُ طبقةً زائدةً لا تُستخدمُ في
الإنتاج. التفاصيلُ في [ADR-0059](../adr/0059-scl-001-postgres-durable-dedup.md).

**المُنفَّذ:**

1. **تعديلُ نصِّ الحلّ** في `ROADMAP-MASTER` §11-ب — «Redis» ← «PostgreSQL durable dedup».
   **بموافقةٍ حوكميّةٍ صريحة** (استثناءُ ح-1).
2. **حاجزُ إقلاعٍ للإنتاجِ** — `telegram-webhook.ts`: حقلٌ `requireDurableIntake?: boolean` في
   `WebhookDependencies`، والمصنعُ يرمي `Error` عند الإقلاعِ إن كان `requireDurableIntake === true
   && intake === undefined`. و`index.ts:481` يضبطُهُ `config.env === "production"`. فلا يصيرَ
   `dedup` الذاكرةُ مساراً صامتاً في الإنتاج.
3. **إعادةُ تسميةِ الخريطة** — `update-dedup.ts`: تعليقٌ يُوثّبَّتُ أنّها اختباريّةٌ وحدَها.
4. **اختباراتُ الوحدةِ (4)** — `tests/unit/telegram-webhook-production-guard.test.ts`: يرمي في
   الإنتاجِ بلا إيداع · لا يرمي مع وجوده · التوافقُ مع مسارِ الاختبارِ بلا قاعدة · لا يكفي
   تعطيلُ الحاجزِ لتمريرِ الإنتاج.

**حالةُ البنودِ بعدَ هذا التنفيذ:**

| البندُ | الحالةُ | السببُ |
| --- | --- | --- |
| `SCL-001` | `[x]` (مُغلَقٌ بعدَ ثلاثِ جولاتٍ خضراءَ) | قُلِبَ `[ ]`→`[x]` بعدَ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`): 34173000774 · 34173178653 · 34173381531. |
| `SCL-005` | `[ ]` | لم يُمَسّ — خريطةُ `tripId → messageId` قائمةٌ، والحاجةُ لم تُسقَطْ. |

**`F5-02` يبقى `[ ]`** — بقيَ منه البندُ `SCL-005` (بندٌ منفصلٌ). **ونصُّ حلّ `SCL-001` عُدِّلَ بقرارٍ
حوكميٍّ صريحٍ (استثناءُ ح-1)** موثَّقٌ في [ADR-0059](../adr/0059-scl-001-postgres-durable-dedup.md)
وسجلِّ التنفيذِ §25. ولا `مَقيس` ولا `مُثبَت` (`ح-5`) — المُتحقَّقُ منه وجودُ الحاجزِ ونجاحُ
اختباراتِه. ولا `R-17`/Socket.IO/`DEC-11`/`F0` مُسَّ.

## ملحق ز: تنفيذُ `SCL-005` — مخزنُ بثٍّ مشتركٌ في Redis + ادّعاءٌ ذرّيٌّ لبدءِ البثّ (2026-09-08)

> **تنفيذٌ فعليٌّ لـ`SCL-005`، مُغلَقٌ بـ`[x]` بعدَ أربعِ جولاتِ CI خضراء.** المُنفَّذُ مخزنُ بثٍّ مشتركٌ (`LiveBroadcastStore`) في Redis فوقَ عميلِ Upstash REST القائمِ، مع ادّعاءٍ ذرّيٍّ لبدءِ البثّ (`SET NX`) وتحريرٍ آمنٍ بـLua. يُغلِقُ المسارَ «مفتاحٌ مشترك» من نصِّ الحلّ (بلا تعديلِ نصٍّ — `ح-1`). والمرجعُ التفصيليُّ هو `docs/evidence/distributed/SCL-005-20260908.md` و`docs/adr/0060-scl-005-redis-shared-broadcast-store.md`.

**اعتمدَ صاحبُ المشروعِ المسارَ «مفتاحٌ مشترك»** من نصِّ حلّ `SCL-005` (لا «إسقاطُ الحاجةِ»
— فـ`F4-07` المستقبليُّ `[ ]` يعتمدُ انتقالَ Mini App غيرَ المُنجَز، فلا يُغلَقُ بندٌ
بـ«التزامٍ مستقبليٍّ» يقلبُ حالةً بلا تحقق). التفاصيلُ في
[ADR-0060](../adr/0060-scl-005-redis-shared-broadcast-store.md).

**المُنفَّذ:**

1. **واجهةُ `LiveBroadcastStore`** في `application/tracking` (`get`/`save`/`delete`/
   `claimStart`/`releaseClaim`) + تنفيذانِ: الذاكرةُ (`createInMemoryLiveBroadcastStore`)
   للاختبارِ افتراضيّاً، وRedis (`createRedisLiveBroadcastStore`) للإنتاج عبرَ عميلِ Upstash
   REST نفسِه.
2. **الادّعاءُ الذرّيُّ لبدءِ البثّ** — `claimStart(tripId, token, ttl)` بـ`SET NX PX`: ينجحُ
   مرّةً واحدةً فقط لكلِّ رحلةٍ. والتحريرُ بـLua (`if get == token then del`) آمنٌ ضدَّ
   الإطلاقِ الخاطئ. فلا تفتحُ نسختانِ رسالتَي بثٍّ للرحلةِ الواحدة.
3. **إعادةُ هيكلةِ المُرحِّلِ** (`customer-live-relay.ts`) — استبدالُ `Map` بالمخزنِ المُحقَن.
   وبدءُ البثّ يُدّعى ذرّيّاً. والكتابةُ عندَ البدءِ والإغلاقِ والتشوهِ فقط — لا لكلِّ إصلاحة.
   و`openBroadcasts` صارَ عدّاداً محليّاً. وكلُّ الثوابتِ القائمةِ حُفِظَت (`BUG-009`،
   الخنط، حرّاسُ الجلسة، `isTripLive`).
4. **التوصيلُ في `container.ts`** — Redis حين يكونُ متاحاً (`sessionStore === "redis"`)،
   وإلّا الذاكرةُ (تنازلٌ موثَّقٌ كنشرِ نسخةٍ واحدةٍ). و`newClaimToken` مُحقَنٌ بـ
   `crypto.randomUUID()` حتى لا تستوردَ طبقةُ التطبيقِ `node:crypto`.
5. **اختباراتُ الوحدةِ (15)** — `tests/unit/customer-live-relay-distributed.test.ts`:
   مخزنُ الذاكرة + Redis بعميلٍ مزوّر + مُرحِّلانِ يتشاركانِ مخزناً (بدءٌ واحدٌ، تعديلُ
   الرسالةِ نفسِها، إغلاقٌ مرّةً، تسلسلٌ قديمٌ مُتجاهَل).
6. **اختبارُ تكاملِ Redis حقيقيّ** — `scl-005-broadcast-shared-store` في `REQUIRED_CHECKS`:
   ادّعاءٌ ذرّيٌّ تحتَ التسابقِ، ذهابٌ وعودةٌ، تحريرٌ آمنٌ، حذف.

**حالةُ البنودِ بعدَ هذا التنفيذ:**

| البندُ | الحالةُ | السببُ |
| --- | --- | --- |
| `SCL-001` | `[x]` (مُغلَقٌ) | PostgreSQL durable dedup — مُغلَقٌ في ملحق و. |
| `SCL-004` | `[x]` (مُغلَقٌ) | Redis Streams — مُغلَقٌ في ملحق هـ. |
| `SCL-005` | `[x]` (مُغلَقٌ) | التنفيذُ مكتملٌ ومُختبَرٌ (15 اختبار وحدة + تكاملُ Redis حقيقيّ). قُلِبَ `[ ]`→`[x]` بعدَ أربعِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`). |

**`F5-02` قُلِبَ إلى `[x]`** — انغلقَت بنودُه الخمسةُ كلُّها (`SCL-001`…`SCL-005`). **ونصُّ حلّ `SCL-005` لم يُمَسَّ حرفًا (`ح-1`)** — «مفتاحٌ مشترك» موجودٌ في النصّ أصلاً. ولا `مَقيس` ولا `مُثبَت` (`ح-5`) — اختبارُ Redis الحقيقيّ أخضرُ في CI. ولا `R-17`/Socket.IO/`DEC-11`/`F0` مُسَّ.

---

## ملحق ح: حارسُ الإنتاجِ يرفضُ الحمولةَ بلا `update_id` صالحٍ — سدُّ فجوةِ ADR 0054 §٦ (2026-09-08)

> **إصلاحُ عيبٍ داخلَ نطاقِ `F6-01`/`CAP-001` المُغلَقَينِ بـ`[x]` — لا قلبُ حالةٍ ولا بندٌ جديدٌ.**
> المرجعُ التفصيليُّ `docs/evidence/correctness/webhook-invalid-update-guard-20260908.md`.

**العيبُ الذي سُدّ.** في مسارِ الإنتاجِ (`deps.intake` موصولٌ دائماً من `index.ts`) كان
الشرطُ الحاكمُ لفحصِ التكرارِ والإيداعِ يقتضي `updateId !== null`؛ فحمولةٌ بلا `update_id`
صالحٍ **تسقطُ إلى `handler.handle` داخلَ طلبِ HTTP** — نقضاً صريحاً لـ
[ADR 0054](adr/0054-telegram-webhook-durable-ingest-and-dedup.md) §٦ («العملُ الذي يلي
الإيصالَ… لا يُنفَّذ داخلَ طلبِ HTTP»). فكانت الفجوةُ **مسارَ التفافٍ على الـingest النحيفِ**
يُفتحُ بحمولةٍ مُشوَّهةٍ من أيِّ طرفٍ يعرفُ سرَّ الويبهوكِ. **والفجوةُ كشفَها تدقيقُ إغلاقِ
`CAP-001`/`F6-01` نفسُه** (الملحقانِ 42 و43 أعلاه) — فالتدقيقُ أثمرَ عيباً لا شهادةً فحسب.

**المُنفَّذ.** حارسٌ واحدٌ في `apps/gateway/src/routes/telegram-webhook.ts` بعدَ استخراجِ
`updateId` وقبلَ الإيصالِ الصامدِ: `if (deps.intake !== undefined && updateId === null)`
⇒ `400 { ok: false, error: "INVALID_UPDATE" }`. **والحصرُ بـ`intake !== undefined` مقصودٌ**
فمسارُ الاختبارِ/التشخيصِ يبقى سلوكَهُ المُعلَنَ بلا تغييرٍ ولا يُمَسُّ عقدُ الاختبارِ في
ثمانيةَ عشرَ ملفّاً. **وترتيبُ ADR 0054 محفوظٌ**: الحارسُ **بعدَ** تحقُّقِ السرِّ و**بعدَ**
حدِّ المعدَّلِ لا قبلَهما، فلا يصيرُ الحارسُ نفسُه بابَ تجاوزٍ لحدِّ المعدَّلِ.

**الحُجَّةُ المرجعيّةُ.** [Telegram Bot API — Update](https://core.telegram.org/bots/api#update)
يُعرِّفُ `update_id` (Integer) **الحقلَ الإلزاميَّ الوحيدَ** في كائنِ `Update`؛ فحمولةٌ بلا
`update_id` صالحٍ ليست تحديثَ تيليجرام، ولا مفتاحَ لها يُمنَعُ به التكرارُ، ولا يجوزُ
إيداعُها في طابورٍ مفتاحُ إيصالِه الأوّليُّ `(bot, update_id)`.

**الاختبارُ.** `tests/unit/telegram-webhook-production-guard.test.ts` — ثلاثُ حالاتٍ يُحكَمُ
فيها **بنتيجةِ العملِ لا بكودِ HTTP وحدَه**: (١) حمولةٌ بلا `update_id` في الإنتاج ⇒ `400`
**والمعالجُ لم يُنادَ والإيداعُ لم يُنادَ** (`0`/`0` بمتتبِّعَينِ صريحَينِ) · (٢) `update_id`
نصٌّ ⇒ `400` بالتوكيداتِ نفسِها · (٣) **ضبطٌ سالبٌ**: مسارُ الاختبارِ بلا `intake` ⇒ `200`
والمعالجُ نُوديَ مرّةً — يمنعُ تسرُّبَ الحارسِ خارجَ نطاقِه.

**المقيسُ محلّيًّا (Bun 1.4.2، 2 vCPU، بلا Redis ولا PostgreSQL حقيقيَّينِ):**
`lint`=0 · `typecheck`=0 · **٢٢١٠ ناجحاً · ٦٣٦ متجاوَزاً · ٠ فاشلاً · ٦٩٤٩ توكيداً في
٢١٧ ملفّاً**. **وحدُّ البيئةِ مُعلَنٌ** — المتجاوَزُ حزمةُ التكاملِ الحقيقيِّ وحكمُها في CI.
**وسببُ حمرةِ `verify` على الفرعِ قبلَ الإصلاحِ كان تنسيقَ ملفِّ الاختبارِ الجديدِ وحدَه**
(`biome format`) لا منطقاً ولا نوعاً؛ أُصلِح على ذاك الملفِّ وحدَه.

**حالةُ البنودِ بعدَ هذا الإصلاح — لا شيءَ قُلِب:**

| البندُ | الحالةُ | السببُ |
| --- | --- | --- |
| `F6-01` / `CAP-001` | `[x]` (كما كانا) | إصلاحُ عيبٍ **داخلَ** نطاقِ بندٍ مُغلَقٍ لا قلبُ حالةٍ — فـ`ح-4` غيرُ مُستحَقٍّ، والمُشترَطُ خضرةُ وظائفِ CI الثلاثِ قبلَ الدمجِ (القاعدةُ 0.7). |
| `F6-03` | `[x]` (مُغلَقٌ في PR #36) | غيرُ ذي صلةٍ بهذا الإصلاح. |

**ما لم يُمَسّ.** لا نصُّ بندٍ عُدِّل (`ح-1`) · ولا `ADR` منشورٌ عُدِّل (`ح-6`) — والإصلاحُ
**يوافقُ** ADR 0054 §٦ لا يخالفُه · ولا هجرةَ ولا حاجزَ ولا مخطَّطَ قاعدةٍ · ولا `city_id`
ولا `RLS` · ولا `SCL-*` ولا `R-17` ولا Socket.IO ولا `DEC-11` ولا `F0`.
**ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** — المُتحقَّقُ منه أنَّ الحمولةَ المُشوَّهةَ لا تصلُ المعالجَ
ولا الطابورَ في مسارِ الإنتاجِ، لا throughputُ الإنتاجِ ولا سلوكُ تيليجرام الحقيقيُّ.

**`PR #20` أُغلِقَ متجاوَزاً بـ`#34`** — سدَّ الفجوةَ نفسَها بحارسٍ غيرِ محصورٍ فاضطرَّ إلى
تعديلِ عشرينَ ملفّاً وصارَ `CONFLICTING` مع `main`. **ولا شيءَ من حُجَّتِه فُقِد** — محفوظةٌ في
§٢ من وثيقةِ الدليلِ.

---

## ملحق ط: اختبارُ خطّةِ PostGIS صارَ حتميّاً — بوّابةُ CI لا تُخفِقُ لإحصاءٍ بائدٍ (2026-09-08)

**الموضعُ.** `tests/integration/dispatch-nearby-candidates-postgis.test.ts` §٧ —
الحالةُ `CAP-003 … خطةُ EXPLAIN تكشفُ استخدامَ فهرسِ drivers_location_gix`.

**الحادثةُ.** في التشغيلِ `34257974009` (الوظيفةُ `102168473835`) أخفقَت وظيفةُ
**«تكامل على PostgreSQL حقيقي»** بهذه الحالةِ وحدَها، **وإعادةُ تشغيلِ الوظيفةِ نفسِها بلا
تغييرِ سطرٍ واحدٍ أعطَت `success`** (الوظيفةُ `102169301336`). فاللاحتميّةُ **مُثبَتةٌ
بالتجربةِ** لا مُخمَّنةٌ. وبوّابةٌ تُخفِقُ عشوائيّاً تُفسِدُ القاعدةَ 0.7 من
`MASTER_DIRECTIVE`، لأنَّها تُعلِّمُ قارئَها أن يُعيدَ التشغيلَ حتى يخضَرَّ بدلَ أن يقرأَ
الإخفاقَ — وهذا أخطرُ من إخفاقٍ صريحٍ.

**السببُ الجذريُّ — مقروءٌ من الخطّةِ المرفوضةِ حرفاً.** سجَّلَ الإخفاقُ الخطّةَ كاملةً،
وفيها الشاهدُ: **`"Plan Rows": 1`**. الاختبارُ يُدخِلُ اثنَي عشرَ سائقاً حولَ نقطةِ
الاستلامِ **في اللحظةِ نفسِها**، و`autovacuum` لا يُحصي فوراً؛ فرأى المخطِّطُ جدولاً
يُقدَّرُ بصفٍّ واحدٍ، **فصارَ في تقديرِه B-tree على `city_id`
(`drivers_city_location_at_idx`) مع `Filter` على `st_dwithin` و`Sort` على مسافةِ `<->`
أرخصَ من KNN على GiST** — حكمٌ صحيحٌ لجدولٍ من صفٍّ واحدٍ، خاطئٌ للجدولِ الفعليِّ.
**و`set enable_seqscan = off` القائمُ في الاختبارِ لا يُداوي هذا**: هو يمنعُ المسحَ
التتابعيَّ وحدَه، **ولا يمنعُ فهرساً آخرَ** من أن يُختارَ.

**المُنفَّذُ سطرٌ واحدٌ.** `analyze drivers` قبلَ `EXPLAIN` وبعدَ الإدخالِ.

**ولا توكيدَ أُرخِيَ ولا فهرسٌ أُلزِم.** التوكيدُ باقٍ حرفاً —
`expect(planText).toMatch(/drivers_location_gix/)` — يُطابِقُ **اسمَ فهرسِ GiST بعينِه لا
أيَّ فهرسٍ**. والمُغيَّرُ أنَّ المخطِّطَ صارَ يرى **الحجمَ الحقيقيَّ** للجدولِ الذي يحكمُ
عليه، والحكمُ يبقى حكمَه هو. **فإن انحدرَ الفهرسُ فعلاً يُخفِقُ الاختبارُ كما يجب** —
وهذا هو الفرقُ بين تثبيتِ حتميّةٍ وتخفيفِ حارسٍ.

**حالةُ البنودِ بعدَ هذا التغيير — لا شيءَ قُلِب:**

| البندُ | الحالةُ | السببُ |
| --- | --- | --- |
| `CAP-003` / `F7-01` | `[x]` (كما كانا) | تثبيتُ حتميّةِ اختبارٍ **داخلَ** نطاقِ بندٍ مُغلَقٍ لا قلبُ حالةٍ — فـ`ح-4` غيرُ مُستحَقٍّ، والمُشترَطُ خضرةُ وظائفِ CI الثلاثِ قبلَ الدمجِ (القاعدةُ 0.7). |

**ما لم يُمَسّ.** لا نصُّ بندٍ عُدِّل (`ح-1`) · ولا `ADR` منشورٌ عُدِّل (`ح-6`) · ولا هجرةَ
ولا فهرسَ ولا مخطَّطَ قاعدةٍ ولا استعلامَ إنتاجٍ — **واستعلامُ `EXPLAIN` نفسُه لم يُمَسَّ
حرفاً** · ولا `city_id` ولا `RLS` · ولا حارسٌ أُسكِت ولا أرضيّةُ تغطيةٍ خُفِّضَت ولا
اختبارٌ وُسِم `skip`.

**حدُّ البيئةِ مُعلَنٌ.** لا PostgreSQL ولا PostGIS حقيقيَّانِ في صندوقِ العملِ، فحالةُ §٧
**متجاوَزةٌ محلّيّاً** ولا يجوزُ ادّعاءُ تشغيلِها ههنا؛ **وحكمُها في وظيفةِ «تكامل على
PostgreSQL حقيقي» في CI وحدَها**.

**ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** — المُتحقَّقُ منه سببُ اللاحتميّةِ وارتفاعُه، لا زمنُ
مطابقةٍ ولا سعةٌ.

---

## ملحق ي: اختبارُ الإغلاقِ الرشيقِ صارَ حتميّاً — مهلةُ التمهيدِ لا تُحاكِمُ الإشارةَ (2026-09-08)

> **الحالةُ**: تثبيتُ حتميّةِ اختبارٍ داخلَ `F5-05` المُغلَقِ بـ`[x]`. **لا قلبَ حالةٍ ولا
> بندَ جديدَ ولا توكيدَ أُرخِيَ.** والدليلُ الكاملُ في
> `docs/evidence/correctness/graceful-shutdown-test-determinism-20260908.md`.

**الحادثةُ.** في التشغيلِ `34259304939` (الوظيفةُ `102172954827`، فرعُ PR #38) أخفقَت
وظيفةُ `verify` بحالةٍ واحدةٍ من أصلِ `2846`:
`البوابةُ — الإغلاقُ الرشيقُ عند SIGTERM (F5-05) > SIGTERM ⇒ توقّفٌ عن القبولِ ثمّ خروجٌ برمزِ صفرٍ ضمنَ مهلة [15092.07ms]`.
**وأُعيدَ تشغيلُ الوظيفةِ نفسِها بلا تغييرِ سطرٍ واحدٍ فنجحَت** (`102175449334`)، فاللاحتميّةُ
مُثبَتةٌ بالتجربةِ لا مُخمَّنةٌ. وهو **الإخفاقُ الاحتماليُّ الثانيَ** في اليومِ بعدَ
`CAP-003` §٧ (الملحقُ `ط` أعلاه)، وبوّابةٌ تُخفِقُ احتماليّاً تُفسِدُ القاعدةَ `0.7` من
`MASTER_DIRECTIVE` من جِهةِ معناها: أخضرُ يُنالُ بالمحاولةِ الثانيةِ لا يَشهدُ بشيءٍ.

**السببُ الجذريُّ يدلُّ عليه الرقمُ بنفسِه.** الإخفاقُ استغرقَ `15092.07ms`، ودالّةُ
`bootGateway` كانت تنتظرُ `waitForHealth(port, 15_000)` — فالزمنُ المرصودُ على حافّةِ
المهلةِ نفسِها لا دونَها: العمليّةُ الفرعيّةُ لم تُجِب `/health` في خمسَ عشرةَ ثانيةً.
**وهذا الانتظارُ ليسَ المُختبَرَ**؛ المُختبَرُ ما يقعُ بعدَ `SIGTERM`. فحينَ يُخفِقُ
التمهيدُ يُنسَبُ الإخفاقُ إلى الإشارةِ ظُلماً ويُقرأُ كأنَّ الإغلاقَ الرشيقَ معطوبٌ وهو
سليمٌ. ووظيفةُ `verify` تُشغِّلُ `test:coverage`، وقياسُ التغطيةِ يُثقِلُ إقلاعَ عمليّةٍ
فرعيّةٍ تُحمَّلُ فيها حاويةُ البوابةِ كاملةً، ويُضافُ إليه ازدحامُ العاملِ؛ وفي المِحلّيِّ
يُجيبُ `/health` في `210ms` — ونسبةُ `210` إلى `15000` هي التي أخفَت العطلَ حتّى الآن.

**وعطبانِ ثانويّانِ كُشِفا في الطريقِ.** (أ) تفريغُ الأنبوبِ كان
`getReader().read()` **مرّةً واحدةً** — أي قطعةً واحدةً ثمّ يُترَكُ الأنبوبُ، فإن كتبَت
البوابةُ فوقَ سعتِه توقّفَت عندَ `write` **قبلَ أن تستمعَ**؛ وذاكَ إقلاعٌ لا يكتملُ أبداً
لا إقلاعٌ بطيءٌ، ولا تُنجيه زيادةُ مهلةٍ. (ب) والمنفذُ كان عشوائيّاً
(`30_000 + random*20_000`) فيجوزُ أن يصطدمَ بمشغولٍ على عاملِ CI.

**المُنفَّذُ.** `BOOT_TIMEOUT_MS = 40_000` (مهلةُ **تمهيدٍ** لا توكيدٍ) و
`CASE_TIMEOUT_MS = 70_000`؛ ودالّةُ `drain()` تقرأُ المجرى **إلى آخرِه** في الخلفيّةِ
وتحتفظُ بما قرأ؛ وآخرُ ألفَي حرفٍ من المخرجاتِ تُضمَّنُ في نصِّ الإخفاقِ بدلَ رسالةٍ
عمياءَ؛ والمنفذُ `31_000 + ((process.pid + n*7) % 9_000)` فريدٌ **حتماً** لا عشوائيّاً.

**وما لم يُمَسَّ وهو الأهمُّ.** توكيداتُ الحالاتِ الثلاثِ باقيةٌ حرفاً بحرفٍ:
`expect(exitCode).toBe(0)` **ونافذةُ الثمانِ ثوانٍ لم تُرفَع**، و`drainingSeen`
و`gateSeen` ونافذتاهما `3_000ms`. فزِيدَ زمنُ الوصولِ إلى نقطةِ القياسِ ولم يُزَد زمنُ
القياسِ نفسِه ولا خُفِّفَ شرطُه — وهو الفرقُ بينَ تثبيتِ الحتميّةِ وإرخاءِ التوكيدِ. ولا
شيفرةَ إنتاجٍ مُسَّت ولا هجرةَ ولا حارسٌ أُسكِتَ ولا أرضيّةُ تغطيةٍ خُفِّضَت ولا اختبارٌ
وُسِمَ `skip`، ولا نصُّ بندٍ عُدِّلَ (`ح-1`) ولا `ADR` منشورٌ (`ح-6`).

**وحدُّ ما يُدَّعى.** المِحلّيُّ يُنجِحُ الحالاتِ الثلاثَ (`210ms` · `5233ms` · `5213ms`)
لكنّه **لا يُعيدُ إنتاجَ العطلِ** (لا تغطيةَ فيه ولا ازدحامَ)، فهو يُثبِتُ سلامةَ الشِّفرةِ
الجديدةِ لا زوالَ اللاحتميّةِ؛ والحكمُ لـCI وحدَه. **ولا `مَقيس` ولا `مُثبَت` (`ح-5`)** —
وليسَ هذا برهانَ استحالةٍ بل توسيعُ هامشٍ من `1.0×` إلى `2.7×` مع إزالةِ عطبَينِ
بِنيويَّينِ كانا يجعلانِ الإخفاقَ ممكناً بلا حدٍّ.

**والجولةُ الثانيةُ كشفَت السببَ الأعمقَ — وكانَ عطلاً في الإنتاجِ لا في الاختبارِ.**
بعدَ إصلاحِ مهلةِ التمهيدِ أخفقَت `verify` في حالةٍ أخرى من الملفِّ نفسِه (التشغيلُ
`34261149314`، الوظيفةُ `102179119304`): `المسارُ غيرُ الصحيِّ يُرفَضُ بـ503 draining`
استهلكَ نافذةَ الترقُّبِ `3_000ms` ولم يرَ الإعلانَ قطُّ. **والسببُ أنَّ «الطلباتَ
الجاريةَ» في الاختبارِ لم تكن جاريةً أصلاً**: غلافُ `Bun.serve` في
`apps/gateway/src/index.ts` يستثني `/health` و`/ready` من عدّادِ الجاري بنصِّه («فحوصُ
الصحةِ لا تُعدُّ جاريةً — لا تُؤخِّرُ التصريفَ»)، فكانت الأربعةُ لا تُبطئُ التصريفَ
لحظةً، وكان `inFlight()` صفراً عندَ الإشارةِ دائماً فيُنتقَلُ إلى `close()` بلا
انتظارٍ — **وما أنجحَ الحالتَينِ سباقٌ محضٌ** بينَ `close()` وأولِ استطلاعٍ.

**وذاكَ فجوةٌ في `F5-05` نفسِه**: حينَ تأتي الإشارةُ ولا طلبَ جارٍ — وهو الغالبُ في
إعادةِ نشرٍ — يُرفَعُ `isDraining` ويُغلَقُ المقبسُ في دورةِ حدثٍ واحدةٍ، **فلا يرى
المُوجِّهُ `503 draining` أبداً** بل يرى رفضَ اتصالٍ؛ وهو عينُ ما جاءَ `F5-05` يمنعُه
بنصِّ `lifecycle.ts` نفسِه. **والمُنفَّذُ نافذةُ إعلانٍ صريحةٌ**: `announceMs?: number`
في `LifecycleOptions` (الافتراضُ `0` حفاظاً على سلوكِ من لا يطلبُها) يُنتظَرُ **بعدَ**
رفعِ `isDraining` و**قبلَ** انتظارِ فراغِ الجاري، و`index.ts` يضبطُها `500` سِلواً
مجاوراً لـ`graceMs: 10_000` المضبوطةِ هناكَ أصلاً؛ ويُقرأُ العدّادُ **بعدَ** النافذةِ
لا قبلَها فطلبٌ ينتهي خلالَها يُحسَبُ `drained` لا `force_closed`. وهي النمطُ
المُتَّبَعُ في المُنسِّقاتِ لا حيلةَ اختبارٍ. **وثلاثةُ اختباراتِ وحدةٍ** في
`tests/unit/lifecycle.test.ts` تُثبِتُها: النافذةُ أوّلُ نومٍ و`isDraining` مرفوعٌ
و`close` لم يُنادَ · وبلا `announceMs` لا يُنتظَرُ شيءٌ (السلوكُ السابقُ محفوظٌ حرفاً) ·
وطلبٌ ينتهي خلالَها يُنتِجُ `drained`. **والتوكيداتُ في اختبارِ العمليّةِ لم تُمَسَّ**،
والمحذوفُ سِقالةٌ كاذبةٌ لا تفعلُ شيئاً. **والأثرُ المرصودُ**: حالاتُ الملفِّ الثلاثُ
صارت `711ms` و`712ms` و`710ms` **متساويةً** بعدَ `210` و`5233` و`5213` — فالزمنُ محكومٌ
بنافذةٍ صريحةٍ لا بزمنِ إغلاقِ قاعدةٍ وهميّةٍ. **وهذا تعديلُ شيفرةِ إنتاجٍ داخلَ بندٍ
مُغلَقٍ (`F5-05` كانَ `[x]` وبقيَ)**، ولا `ADR` منشورٌ عُدِّلَ (`ح-6`) — والإصلاحُ
**يوافقُ** نصَّ `F5-05` لا يخالفُه.

## ملحق ك: تنفيذُ `CAP-002` / `F6-04` — مُرسِلٌ صادرٌ واعٍ بالحدِّ، وسقفُ محاولاتٍ يُنفَّذُ في القاعدةِ (2026-09-08)

> **الحالةُ**: `مُنفَّذ · مُختبَر · مُتحقَّق منه`. و**البندانِ قُلِبا إلى `[x]`**
> بعدَ استيفاءِ شرطَي `ح-4`: ملفُ دليلٍ بالتسميةِ الملزمةِ، وسطرٌ في سجلِّ التنفيذِ
> بأعمدتِه السبعِ. ولا نصَّ بندٍ عُدِّلَ (`ح-1`)، ولا `ADR` منشورٌ مُسَّ (`ح-6`).
>
> **وتصحيحُ حوكمةٍ يُعلَنُ صريحاً**: كانَ ههنا مكتوباً أنَّ `ح-4` «تُلزِمُ ثلاثةَ
> تشغيلاتِ CI خضراءَ متعاقبةٍ»، **وذاكَ تفسيرٌ خاطئٌ لا أصلَ له في نصِّ القاعدةِ**؛
> ونصُّها في `docs/ROADMAP-MASTER.md:66` حرفاً: «وضعُ `[x]` بلا ملفِ دليلٍ وبلا
> سطرٍ في سجلِّ التنفيذِ». **فالمانعُ كان توثيقيّاً محضاً لا انتظارَ جولةٍ**، وقد
> صُحِّحَ بسطرِ تصحيحٍ في القسمِ `25` **بلا تعديلِ السطرِ الخاطئِ ولا حذفِه** كما
> يُلزِمُ ذاكَ القسمُ نفسُه.
>
> **والدليلُ الحاكمُ** `docs/evidence/correctness/CAP-002-20260908.md`؛ والسابقُ
> `docs/evidence/architecture/CAP-002-outbound-rate-aware-sender-20260908.md`
> باقٍ للتاريخِ ولا يُحذَفُ. **والمُتحقَّقُ منه بحدِّه**: سقفُ المحاولاتِ والتراجعُ
> الأُسّيُّ على PostgreSQL 17.6 حقيقيّةٍ (٧٧ هجرةً بالترتيبِ بلا خطأٍ · ٦ حالاتٍ
> ناجحةً · ٢٠ حالةً قائمةً لم تنكسر) — **ولا حِمْلَ قِيسَ فلا `مَقيس` ولا `مُثبَت`
> (`ح-5`)**.

**العطبُ.** المُرسِلُ الصادرُ كانَ بلا مهلةِ نداءٍ ولا إعادةِ محاولةٍ ولا حدٍّ
عالميٍّ، و`retry_after` مُتجاهَلاً — وحدودُ Bot API المُعلَنةُ نحوُ ثلاثينَ رسالةً في
الثانيةِ للبوتِ وواحدةٌ للمحادثةِ، والبثُّ إلى مئةِ سائقٍ في دورةِ عرضٍ يتجاوزُها.
**وأخفاها وأخطرُها في القاعدةِ لا في الشيفرةِ**: `claim_notification_delivery` كانت
تقرأُ `notification_delivery_max_attempts` **وتُرجِعُه ولا أحدَ يُنفِّذُه**،
و`finish_notification_delivery` تُعيدُ الصفَّ إلى `pending` عندَ كلِّ إخفاقٍ **بلا
سقفٍ**؛ فصفٌّ إلى محادثةٍ حظرَتِ البوتَ (`403`) يُعادُ **أبدَ الدهرِ** ويُنفِقُ حصّةَ
الحدِّ على فشلٍ مضمونٍ.

**المُنفَّذُ — ثلاثُ طبقاتٍ.** (أ) **دلوٌ مشتركٌ** بنافذةٍ منزلقةٍ ونطاقَينِ
(`global` ٢٥/ثانيةً · `chat` ١/ثانيةً)، بنسختَينِ: ذاكرةٌ، وRedis بنَصِّ `Lua`
واحدٍ على `ZSET` يُنقّي ويعدُّ ويُضيفُ ذرّياً. **ويفشلُ مغلقاً — عكسَ حدِّ الوارِدِ
(ADR 0055) عن قصدٍ**: الوارِدُ لا يجوزُ أن يُسكِتَ البوتَ عن طلبِ راكبٍ، والصادرُ
الفاشلُ مفتوحاً يعني إرسالاً بلا حدٍّ فحظرَ البوتِ. (ب) **غلافُ صمودٍ**
(`withOutboundResilience`) يُغلِّفُ أيَّ `TelegramSender`: مهلةُ نداءٍ عشرُ ثوانٍ
بـ`AbortSignal` · تصنيفٌ ثلاثيٌّ (`throttled`/`permanent`/`transient`) **بشكلِ
الخطأِ لا بنوعِه** لأنَّه يعبُرُ `grammY` و`fetch` فيفقدُ نوعَه · `retry_after`
يُحترَمُ **حرفاً** · تراجعٌ أُسّيٌّ **كاملُ الرَّجرجةِ** · حرجٌ خمسُ محاولاتٍ وغيرُ
حرجٍ محاولتانِ و`sendLocation` حرجةٌ افتراضاً · **الدائمُ لا يُعادُ أبداً ولو كانَ
حرجاً** · طابورُ موتى بسعةٍ محدودةٍ (Redis: قائمةٌ ٥٠٠ وسبعةُ أيّامٍ بـ`EVAL` ذرّيٍّ)
**لا يرفعُ خطأً أبداً**. (ج) **سقفٌ في القاعدةِ**:
`20260908050000_notification_outbox_dead_letter.sql` يُضيفُ
`last_error`/`dead_reason`/`died_at` مع بذرٍ رجعيٍّ، وقيدَ تلازمٍ بينَ
`status='dead'` و`died_at`، وفهرساً جزئيّاً، ويُعيدُ تعريفَ
`finish_notification_delivery` بتوقيعٍ أوسعَ يُرجِعُ
`{ok, outcome, reason, attempts, max_attempts}` حيثُ `outcome ∈ {delivered, dead,
retried}` — **وهو شكلُ `finish_telegram_update_job` بعينِه، سابقةٌ في المستودعِ
تُتّبَعُ لا اصطلاحٌ سادسٌ يُضافُ**. والسقفُ من `platform_settings` لا من الشيفرةِ
(القاعدةُ `0.3`)، والمنحُ يُعادُ صراحةً بعدَ `drop` لأنَّها **لا تحفظُ** قائمةَ
التحكّمِ.

**وعطبُ بنيةٍ كُشِفَ في الطريقِ.** `apps/workers` كانت **بلا Redis أصلاً** وبلا
سطرِ استيرادٍ واحدٍ من `apps/gateway`، والاستيرادُ من تطبيقٍ إلى تطبيقٍ خرقُ طبقاتٍ.
فنُقِلَ عميلُ Upstash إلى `packages/infrastructure/redis/upstash.ts` بـ`git mv`
فبقيَ التأريخُ، وتُرِكَ في موضعِه القديمِ **قشرةُ إعادةِ تصديرٍ** فبقيَ المستوردونَ
**الستّةَ عشرَ** بلا تعديلِ سطرٍ. والصمودُ يُركَّبُ **خارجَ** طبقةِ القياسِ كي يَعُدَّ
القياسُ كلَّ نداءٍ حقيقيٍّ على Bot API لا نداءً منطقيّاً أُعيدَ خمساً.

**المُختبَرُ.** `tests/unit/outbound-rate-bucket.test.ts` (٥) ·
`tests/unit/rate-aware-telegram-sender.test.ts` (١١) ·
`tests/integration/notification-outbox-dead-letter.test.ts` (٦، PostgreSQL
حقيقيّةٌ). والنومُ والعشوائيّةُ والساعةُ محقونةٌ كلُّها فلا اختبارٌ ينامُ. محلّيّاً:
`lint`=**0** · `typecheck`=**0** · **2229 ناجحاً · 0 فاشلاً · 644 مُتخطّى** (كلُّ
المُتخطّى يلزمُه Redis أو PostgreSQL، فحُكمُ اختباراتِ التكاملِ من CI وحدَها).

**وسجلُّ العودةِ (`OPS-010`).** الحذفُ والإنشاءُ بتوقيعٍ مختلفٍ خطرُ عودةٍ يَرصُدُه
الحاجزُ الساكنُ، فأُعلِنَ مدخلانِ في `scripts/lib/rollback-registry.ts`
(`breaksPreviousRelease: true` · `forward-only` · `coupledDeploy: true`) وأُضيفَ
إجراءٌ في `docs/rollback.md`. وعِلّةُ الحذفِ أنَّ `create or replace function`
**لا تُغيِّرُ قائمةَ الوسائطِ**، فترْكُ القديمِ يُوجِدُ توقيعَينِ متعايشَينِ يجعلانِ
النداءَ **مُبهَماً** فيفشلُ الاثنانِ معاً.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** لا طابورَ أولويّةٍ حقيقيّاً (الحرجةُ تُعادُ أكثرَ ولا
تسبقُ في الانتظارِ) · لا سطحَ قراءةٍ إداريّاً لطابورِ الموتى · **ولا قياسَ حِمْلٍ**:
الأرقامُ إعداداتٌ وحدودٌ مُعلَنةٌ لا سعةٌ مَقيسةٌ، فلا يُقالُ «مَقيس» ولا «مُثبَت»
(`ح-5`) · والفشلُ المغلقُ للدلوِ خطرٌ مُعلَنٌ: انقطاعُ Redis يُوقِفُ الصادرَ بضغطٍ
عكسيٍّ، وهو المُختارُ عن قصدٍ.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F5-07` / `SCL-006`: مجموعُ النظامِ صار له موضعٌ يُقرأ منه

**ما كان مكتوباً قبلَ اليوم** أنَّ مسارَ قراءةِ المقاييسِ هو `GET /metrics`. وهذا
بقيَ صحيحاً، **لكنَّه لم يعد المسارَ الوحيدَ**، والسببُ أنَّه كان يحملُ عطباً
مُؤجَّلاً: نصُّ Prometheus الذي يُصيِّرُه هو مقاييسُ **العمليّةِ التي أجابت الطلبَ**
لا مقاييسُ النظام. وهذا لا يُرى اليومَ لأنَّ `numInstances: 1` — العمليّةُ
المُجيبةُ هي الوحيدةُ فمقاييسُها مقاييسُ النظام.

**ولكنَّ في الخارطةِ بندَينِ ينقضانِ هذا التطابقَ نقضاً.** `F5-06` يرفعُ النسخَ فوقَ
واحدةٍ، فيوجِّهُ مُوجِّهُ Render كلَّ كشطةٍ إلى نسخةٍ يختارُها هو: يُقرأُ عدّادُ
الأولى ثمَّ عدّادُ الثانيةِ، فلا هو مجموعٌ ولا هو ناقصٌ بمقدارٍ معلومٍ — بل
**منحنى عدّادٍ يهبطُ ويرتفعُ بحسبِ اختيارِ المُوجِّهِ**، ولا موضعَ في الدنيا يُقرأُ
منه المجموع. و`F5-04` يفصلُ العاملَ في خدمةٍ مستقلّةٍ **لا خادمَ HTTP فيها**: لا
مسارَ يُكشَطُ ولا منفذَ، فيصيرُ كلُّ ما يفعلُه العاملُ غيرَ مقيسٍ **إطلاقاً** لا
ناقصَ القياس. والأخطرُ أنَّ العطبَ في الحالتَينِ **صامتٌ**: لا خطأً يُرفَعُ ولا
اختباراً يسقطُ ولا سطرَ سجلٍّ يشكو — لوحةٌ تعرضُ أرقاماً معقولةَ الشكلِ خاطئةَ
المعنى. **فلا يجوزُ تنفيذُ `F5-04` ولا `F5-06` قبلَ هذا البند.**

**والمُصحَّحُ اليومَ (ADR-0062).** أُضيفَ مسارُ قراءةٍ **ثانٍ** لا بديلٌ: كلُّ عمليّةٍ
تدفعُ لقطةَ مسجِّلِها دوريّاً بصيغةِ OTLP/HTTP JSON إلى مُجمِّعٍ خارجيٍّ، بسماتِ
موردٍ تفصلُها عن أختِها. و**`service.instance.id` هي التي يقومُ عليها البندُ كلُّه**:
بلا هويّةٍ فارقةٍ تدهسُ سلاسلُ نسخةٍ سلاسلَ أختِها فيُقرأُ رقمُ آخرِ دافعٍ ويُظَنُّ
مجموعاً. و`pid` وحدَه لا يكفي — على مضيفَينِ يتكرَّرُ الرقمُ، وهو عينُ العطبِ الذي
جاء البندُ يمنعُه — فالمُولَّدُ `<الخدمة>-<pid>-<٨ محارف عشوائيّة>`.

**والزمنيّةُ تراكميّةٌ** (`aggregationTemporality: 2`): كلُّ دفعةٍ تحملُ العدَّ من
أوَّلِ الإقلاعِ، و`startTimeUnixNano` يُعلِمُ المُجمِّعَ فلا تُقرأُ إعادةُ النشرِ
هبوطاً. وهذا **يفرضُ على القارئِ قاعدةً**: أحدثُ جسمٍ لكلِّ نسخةٍ ثمَّ الجمعُ عبرَ
النسخِ. ومُجمِّعٌ يجمعُ دفعاتِ النسخةِ الواحدةِ بعضَها فوقَ بعضٍ يَنفخُ الرقمَ بعددِ
الدفعاتِ — **وهو خطأٌ وقعَ فعلاً في أوَّلِ تشغيلٍ** (قراءةُ ٣٦ حيثُ الصوابُ ١٨)،
فصارَ اختباراً مُصرَّحاً به لا ملاحظةً في تعليق.

**وحُزَمُ المُدرَّجِ تُحوَّلُ من التراكميِّ إلى الفرقيِّ**، وحدُّ `+Inf` يُحسَبُ من
`count` لا من آخرِ سلّةٍ: قيمةٌ فوقَ أكبرِ حدٍّ لا تدخلُ أيَّ سلّةٍ في المسجِّلِ،
فلو أُخِذَ الأخيرُ من السلالِ وحدَها ضاعت. ونسخُ المصفوفةِ كما هي — وهو أبسطُ ما
يُكتَبُ — يُعطي جسماً **صالحاً** بأرقامٍ منفوخةٍ نفخاً هائلاً بلا شكوى.

**والفشلُ مفتوحٌ لا صامتٌ.** المُصدِّرُ لا يرمي أبداً — لا يُسقِطُ إقلاعاً ولا
طلباً ولا إغلاقاً، إذ إسقاطُ منصّةِ نقلٍ لأنَّ لوحةَ مراقبةٍ تعطَّلت عطبٌ أسوأُ من
العطبِ الذي يُداوى — لكنَّه يدفعُ مقاييسَ نفسِه ضمنَ اللقطةِ نفسِها:
`waslah_metrics_export_attempts_total{outcome}` بثلاثةِ نواتجَ يفرِّقُ فيها بينَ
**`rejected`** (مُجمِّعٌ حيٌّ يردُّ برمزٍ غيرِ ناجحٍ — علّةُ اعتمادٍ أو صيغةٍ)
و**`failed`** (شبكةٌ أو مهلةٌ)، وبينَهما تشخيصانِ وإصلاحانِ مختلفانِ تماماً.
و`…_last_success_timestamp_seconds` **لا تتحرَّكُ بفشلٍ** فيصحُّ بناءُ تنبيهِ
«انقطعَ الدفعُ» عليها؛ ولو حُدِّثت في مسارِ الفشلِ لصارَ التنبيهُ لا يعملُ أبداً.
و`…_series_count` يُري انفجارَ العلاماتِ في اللوحةِ **قبلَ** أن يُرى في الفاتورة.

**ودفعةٌ أخيرةٌ عندَ الإيقافِ** في مسارِ الإغلاقِ الرقيقِ — في البوابةِ بعدَ الدرينرِ
والعاملِ المدمجِ وقبلَ إغلاقِ القاعدةِ (لأنَّ `beforeSnapshot` يستعلمُها)، وفي
العاملِ بعدَ `drain()` — وإلّا خسرت **كلُّ إعادةِ نشرٍ** ما يصلُ إلى فاصلٍ كاملٍ من
العدِّ، فيكونُ نقصُ المجموعِ بمقدارِ عددِ إعاداتِ النشرِ في اليوم.

**ومَعِيبانِ حقيقيّانِ كُشِفا في أثناءِ التنفيذِ لا في مراجعةٍ.** الأوَّلُ:
`parseInt("1.5")` يردُّ `1` و`parseInt("30s")` يردُّ `30`، فكانَ ضبطٌ خاطئٌ يُقبَلُ
بمعنىً غيرِ ما قصدَه المشغِّلُ ولا حرفَ يشكو — عولجَ بحارسِ `/^\d+$/` **قبلَ**
التحويل. والثاني: `apps/workers/src/index.ts` كانَ يُنشئُ الحاويةَ دونَ تمريرِ
`overrides.metrics`، فتعملُ محوّلاتُه بمسجِّلٍ غيرِ مقيسٍ — **عاملٌ يعملُ ولا
يُنتِجُ رقماً واحداً**. صارَ يُمرَّرُ مسجِّلاً حقيقيّاً.

**والسرُّ لا يُسجَّلُ ولا يُعادُ.** `METRICS_EXPORT_HEADERS` قيمتُه رمزُ اعتمادٍ،
فالمُسجَّلُ **أصلُ** النقطةِ وحدَه بلا مسارٍ ولا استعلامٍ، وإن تعذَّرَ التحليلُ
فـ`"invalid"` لا النصُّ الخام؛ ورسالةُ خطأِ التحليلِ تقولُ «المُدخَلُ رقم ٢ لا
يطابقُ الصيغةَ» ولا تقولُ ماذا كانَ فيه، إذ رسائلُ الإقلاعِ يقرؤُها في سجلِّ
المنصّةِ مَن لا يملكُ السرَّ؛ ونصُّ الاستثناءِ لا يُمرَّرُ بل `error.name` مقصوصاً،
فنصُّ `TypeError` من `fetch` قد يحملُ الرابطَ كاملاً برمزِه. وأُضيفَ الاسمُ إلى
`SECRET_NAMES` في `scripts/check-secret-logging.ts` فصارَ الحرسُ آليّاً لا بشريّاً.

**والغيابُ إطفاءٌ مُعلَنٌ لا فشلُ إقلاعٍ:** بلا نقطةٍ مضبوطةٍ يردُّ المُنشئُ `null`
ويُسجَّلُ `metrics_export.disabled` ويعملُ النظامُ كاملاً — ولو كانَ الغيابُ فشلاً
لصارَ نشرُ النظامِ موقوفاً على مُجمِّعٍ لم يُنشَأ بعدُ. و`https://` **إلزاميٌّ في
الإنتاجِ**: الجسمُ يحملُ أحجامَ الأعمالِ وأوقاتَ الذروةِ وعددَ السائقينَ والترويسةُ
تحملُ رمزَ الاعتمادِ، فنصٌّ صريحٌ كشفٌ مزدوج.

**المُختبَرُ.** `tests/unit/observability-otlp.test.ts` (١٢) ·
`tests/unit/observability-metrics-exporter.test.ts` (١٥) ·
`tests/unit/config-metrics-export.test.ts` (١٦) ·
`tests/integration/metrics-central-aggregation.test.ts` (٩) — **بعمليّتَينِ
حقيقيّتَينِ** بـ`Bun.spawn` تدفعانِ إلى `Bun.serve` واحدٍ، ونتيجةُ كلٍّ تُقرأُ من
**رمزِ خروجِها** لا من نصٍّ يُحلَّل. ومسجِّلانِ في عمليّةٍ واحدةٍ لم يكونا ليكفيا:
يتشاركانِ الكومةَ ورقمَ العمليّةِ فيمرَّانِ **ولو كانت الهويّةُ من `pid` وحدَه**.
محلّيّاً: `lint`=**0** (976 ملفاً) · `typecheck`=**0** · **2280 ناجحاً · 644
مُتخطّى · 1 فاشلاً** — والفاشلُ `gateway-graceful-shutdown` الخاصُّ بـ`F5-05`،
**ناجحٌ مشغَّلاً وحدَه في 18.2s من مهلةِ 40s**، هشاشةٌ زمنيّةٌ تحتَ ازدحامِ الحزمةِ
سابقةٌ لهذا البندِ ولا يمسُّه منطقيّاً (النقطةُ غيرُ مضبوطةٍ فيه فالمُصدِّرُ `null`)
— **ولا يُدَّعى أنَّه ليسَ مشكلةً**، بل يُترَكُ مرصوداً والحكمُ الفاصلُ من CI على
عتادٍ ثابت.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **لم يقبل جسمَنا مُجمِّعٌ حقيقيٌّ** — لا OTel
Collector ولا Grafana ولا لوحةٌ عرضته؛ المُستقبِلُ في الاختبارِ `Bun.serve` يفحصُ
البنيةَ لا مُجمِّعٌ كاملٌ · **ولم يُقَس دفعٌ من داخلِ شبكةِ Render**، وكلُّ ما جرى
على `127.0.0.1` · ولم يُشغَّل النظامُ بنسختَينِ فعليَّتَينِ (`numInstances` لا
يزالُ `1` — شأنُ `F5-06`) · ولم تُقَس كلفةُ الدفعِ ولا حجمُ الجسمِ عندَ سلاسلِ
الإنتاجِ الحقيقيّة. **فالدرجةُ «مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**،
ورفعُها مرهونٌ بـ`F5-06` حيثُ يُضبَطُ مُجمِّعٌ فعلاً وتُرفَعُ النسخُ فوقَ واحدة.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F5-04` / `SCL-007`: المهامُّ الدوريّةُ صار لها موضعٌ مُعلَنٌ لا مُفترَض

**ما كانَ يُقال.** «فصلُ العاملِ متاحٌ بمتغيّرِ بيئةٍ واحدٍ: `RUN_WORKER_IN_GATEWAY=false`».

**وهو صحيحٌ حرفاً وناقصٌ معنىً.** المتغيّرُ يُطفئ المهامَّ في البوّابةِ فعلاً،
**ولا شيءَ في النظامِ كلِّه كانَ يتحقّقُ من أنَّ أحداً غيرَها يُشغِّلُها.** فحالُ
«`false` بلا خدمةِ عاملٍ حيّةٍ» كانَ يمرُّ من كلِّ حاجزٍ: الإعدادُ يُقلعُ، والبوّابةُ
تُقلعُ، و`/health` يقولُ «سليم» — ولا مهمّةٌ دوريّةٌ واحدةٌ تعملُ في المنظومة. أي لا
انتهاءَ عرضٍ فيبحثُ الطلبُ إلى الأبد، ولا انتهاءَ اشتراكٍ، ولا نسخةَ احتياطيّة.
وليسَ فرضاً نظريّاً: هوَ الحالُ المُشخَّصُ في
`docs/directive-item-0-live-diagnosis.md` §0.2.

**والخطآنِ غيرُ متماثلَينِ، وعليهِ بُنيَ كلُّ ما فُعِل.** `true` معَ خدمةِ عاملٍ
قائمةٍ ⇒ ازدواجُ طلبٍ يبتلعُهُ القفلُ الموزَّعُ (ADR 0009) بـ
`skipped_locked_elsewhere`، لا ضرر. و`false` بلا عاملٍ ⇒ **توقّفٌ كاملٌ صامتٌ**.
فالحراسةُ كلُّها موجَّهةٌ إلى الثاني، و`true` تبقى مشروعةً عمداً كي لا يُسَدَّ طريقُ
التراجعِ الطارئِ بحاجزٍ من صنعِنا.

**النافذُ الآن (ADR 0063).** ثلاثةُ دفاعاتٍ مستقلّةٍ لا واحد: (١) `render.yaml`
يُفحَصُ في CI بـ`scripts/check-instance-invariant.ts` المُوسَّعِ **في موضعِه** —
`MISSING_RUN_WORKER_DECLARATION` و`INVALID_RUN_WORKER_VALUE` و**`JOBS_ORPHANED`**
(بوّابةٌ تُعلنُ الإطفاءَ ولا خدمةَ من نوعِ `worker` في الملفّ) · (٢) الإعدادُ يرفضُ
الإقلاعَ في `production` إن غابَ المتغيّرُ أو كُتِبَ بقيمةٍ خارجَ قائمةٍ مغلقةٍ —
فـ`fasle` كانت تُقرأُ «كاذبة» بصمتٍ، والافتراضُ `true` يبقى في التطويرِ وحدَه ·
(٣) `job_heartbeats` في `/ready` (§4.3) تبقى الخطَّ الثالثَ: تكشفُ عاملاً أُعلِنَ
ومات، وهوَ ما لا يكشفُهُ مانيفستٌ أبداً.

**و`render.yaml` نُقِلَ فعلاً:** البوّابةُ `"false"`، وخدمةُ `waslah-worker` هيَ
الموضعُ الوحيدُ للمهامّ. **و`PROCESS_TOPOLOGY` لم يُمَسّ**: هوَ يصفُ **عددَ نسخِ
الخدمةِ الواصفةِ** لا شكلَ المنظومةِ، ويبقى `single-process` للاثنتَين — وحقلٌ
واحدٌ بدلالتَينِ أسوأُ ما يُصنَعُ بحقلِ إعداد.

**المُختبَرُ.** `tests/unit/config-worker-placement.test.ts` (١١) ·
`tests/unit/check-instance-invariant.test.ts` (٣٧) ·
`tests/integration/worker-service-separation.test.ts` — **بعمليّاتٍ حقيقيّةٍ**
بـ`Bun.spawn` على PostgreSQL حقيقيّةٍ في CI، والشاهدُ **صفوفُ `job_heartbeats` في
القاعدةِ** لا سطرُ سجلٍّ (وهيَ نفسُها التي يقرؤها `/ready`). وثلاثُ حالاتٍ لا
واحدةٌ: بوّابةٌ بـ`true` ⇒ نبضاتٌ (**ضابطٌ موجبٌ**)، وبوّابةٌ بـ`false` ⇒ لا نبضةَ
في النافذةِ كلِّها، وعاملٌ مستقلٌّ ⇒ نبضاتٌ. والأولى ليست زينةً: بلاها تصدقُ
الثانيةُ على قاعدةٍ لم تُهاجَر وعلى نظامٍ ميّتٍ تماماً. محلّيّاً: `lint`=**0**
(978 ملفاً) · `typecheck`=**0** · **2304 ناجحاً · 649 مُتخطّى · 0 فاشلاً**
(القياسُ على `main`: 2281 · 644 · 0).

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **لم يُنشَر شيءٌ على Render** (`autoDeploy: false`
للخدمتَين) فلم تعملا معاً في الحياةِ قطُّ · ولم يُقَس سلوكُ نشرٍ متدرّجٍ يُعادُ فيهِ
نشرُ العاملِ وحدَه (المتوقَّعُ أنَّ `critical_jobs_freshness` غيرَ الحرجِ يلتقطُ
التأخّرَ ولا يُسقِطُ الجهوزيّةَ — **متوقَّعٌ لا مقيسٌ**) · ولم يُقَس أثرُ الفصلِ على
عددِ اتّصالاتِ القاعدةِ · و`JOBS_ORPHANED` يحرسُ الملفَّ وحدَه فمتغيّرٌ يُضبَطُ بيدٍ
في لوحةِ Render لا يراهُ — ولذلكَ وُجِدَ الدفاعانِ الثاني والثالث. **فالدرجةُ
«مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**، ورفعُها مرهونٌ بنشرٍ فعليٍّ
تعملُ فيهِ الخدمتانِ معاً.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F5-08` / `ARCH-011`: سطحُ الإدارةِ خرجَ من مسارِ الويبهوك

**ما كانَ يُقالُ ضِمناً** إنَّ المنظومةَ بعدَ `F5-04` خدمتانِ لكلٍّ عملُها. **والصوابُ**
أنَّ في البوّابةِ ساكناً ثالثاً لا علاقةَ له بالويبهوكِ ولا بالمهامِّ: **لوحةُ
الإدارةِ** بثلاثِ بادئاتٍ (`/admin` · `/admin/api` · `/admin/api/live`)، وحملُها
مناقضٌ لحملِ البوّابةِ نقيضاً: تجميعاتٌ ثقيلةٌ على نوافذَ زمنيّةٍ (خريطةٌ حراريّةٌ ·
خريطةٌ حيّةٌ · مدفوعات) تقبلُ دقيقةً، مقابلَ ويبهوكٍ **لا يجوزُ أن يتأخّرَ ثوانيَ**
وإلّا أعادَ تلغرام الإرسال. وهما يتقاسمانِ **بِركةَ اتّصالاتِ قاعدةٍ واحدةً في
عمليّةٍ واحدةٍ** — فاستعلامُ خريطةٍ يفتحُها مشرفٌ يحجزُ اتّصالاً من البِركةِ التي
يجب أن يُصدَّقَ بها الويبهوكُ. وهو خرقٌ مباشرٌ لـ`ARCH-013`: ما لا يجوزُ أن
يتأخّرَ لا يوضَعُ خلفَ ما يجوزُ أن يتأخّر.

**والفصلُ لم يحتَجْ هجرةً ولا تغييرَ سلوكٍ واحدٍ**: سطحُ الإدارةِ بلا حالةٍ في
الذاكرةِ أصلاً (ADR 0005) — الجلساتُ بـRPC في القاعدةِ ورمزُ CSRF مشتقٌّ اشتقاقاً
— والدلتا اللحظيّةُ تعبرُ العمليّاتِ بمجرى Redis منذُ `SCL-004` (ADR 0058).
**وهذا بالذاتِ ما يجعلُ خطرَه مقلوباً عن خطرِ `F5-04`.**

**وعدمُ التماثلِ مقلوبٌ — وهوَ لبُّ البند.** في `F5-04` كانَ الخطرُ في
`false` بلا عاملٍ: توقّفٌ كاملٌ صامتٌ. أمّا ههنا فالحالُ الأخطرُ هوَ **`true` مع
خدمةِ إدارةٍ قائمةٍ**: **لا شيءَ يُخفِقُ إطلاقاً** — `/health` أخضرُ في الخدمتَينِ،
واللوحةُ تعملُ من الأصلَينِ، ولا سطرُ خطأٍ واحدٌ — **بينما العزلُ الذي دُفِعت
فاتورتُه غيرُ موجودٍ**: التجميعاتُ الثقيلةُ ما زالت تسحبُ من بِركةِ البوّابةِ
نفسِها. فالخطرُ ليسَ الانقطاعَ بل **النجاحَ الكاذبَ**: بندٌ يُعلَنُ مُنجَزاً وخدمةٌ
ثالثةٌ تُدفَعُ تكلفتُها ولا شيءَ ممّا اشتُريَ مُلِك، ولا يكشفُه إلّا قياسٌ لا
يُجريه أحد. **ولذلكَ — خلافاً لـ`F5-04`** حيثُ بقيَ `true` مشروعاً في المانيفستِ
للتراجعِ — **يرفضُ الحاجزُ ههنا الحالَينِ معاً**: `ADMIN_DUPLICATED` و
`ADMIN_ORPHANED`. ويبقى التراجعُ ممكناً في دقيقةٍ بقلبِ القيمةِ وحذفِ الخدمةِ
**معاً**: مسموحٌ لكنَّه **مُعلَنٌ لا صامت**.

**والمُنفَّذُ.** `RUN_ADMIN_IN_GATEWAY` إعلانٌ إلزاميٌّ في الإنتاجِ بصيغةِ ADR 0063
حرفاً (والمُولِّدُ استُخرِجَ فصارَ واحداً للمتغيّرَينِ لا نصَّينِ يتباعدان) ·
و`mountAdminSurface` موضعُ تركيبٍ **واحدٌ** تُناديه نقطتا الدخولِ — البوّابةُ بشرطٍ
وخدمةُ الإدارةِ بلا شرطٍ، وبلاه كانَ الموضعانِ يتباعدانِ عندَ أوّلِ موجّهٍ جديدٍ
فيظهرُ مسارٌ في أحدِهما دونَ الآخر · وخدمةُ `waslah-admin` الثالثةُ من
`docker/Dockerfile.admin` بحاويةٍ ضيّقةٍ لا تبني بوتاً ولا مهامَّ دوريّةً وسقفُ
اتّصالاتِها `5`، وفحصُها **حياةٌ لا جهوزيّةٌ** لأنَّ لوحةً لا تُصدِّقُ ويبهوكاً
لا يُربَطُ إعادةُ تشغيلِها بتذبذبِ القاعدة · وأربعُ مخالفاتٍ في حاجزِ المانيفستِ
**تُطابِقُ الخدمةَ بالاسمِ لا بالنوعِ** (نوعُها `web` كالبوّابةِ، فالمطابقةُ
بالنوعِ كانت ستُنتِجُ حاجزاً لا يُخفِقُ أبداً — وهوَ أسوأُ من لا حاجز).

**وملفّاتُ الموجّهاتِ لم تُنقَل** — المُركَّبُ وحدَه انتقل. ونتيجتُه مُقالةٌ صريحةً
في رأسِ الصورةِ: صورةُ الإدارةِ تحوي كودَ بوّابةٍ لا تُشغِّلُه؛ والثمنُ المقابلُ
كانَ عشراتِ مساراتِ استيرادٍ تتغيّرُ بلا تغيّرِ سلوكٍ، وهيَ أكثرُ ما يُخفي عطلاً
حقيقيّاً وسطَ ضجيجِ الفروق.

**المُختبَرُ.** `tests/unit/config-admin-placement.test.ts` (٧) ·
`tests/unit/admin-mount-order.test.ts` (٥) ·
`tests/unit/check-instance-invariant.test.ts` (٣٧ ← ٥٠) ·
`tests/integration/admin-service-separation.test.ts` — **بعمليّاتٍ حقيقيّةٍ**
بـ`Bun.spawn` على PostgreSQL حقيقيّةٍ، والشاهدُ **استجاباتُ HTTP من عمليّاتٍ
مُقلَعةٍ** لا وجودُ دالّةٍ ولا سطرُ سجلٍّ. وثلاثُ حالاتٍ لا واحدةٌ: بوّابةٌ بـ`true`
⇒ `/admin/login` = **200** (**ضابطٌ موجبٌ**)، وبوّابةٌ بـ`false` ⇒ **404** على
البادئاتِ الثلاثِ **مع بقاءِ `/health` و`/ready` مُجيبَينِ في العمليّةِ نفسِها**،
وخدمةٌ مستقلّةٌ ⇒ **200** بصفحةٍ فيها نموذجٌ و`/health` يُسمّي `waslah-admin`.
والحالةُ الوسطى وحدَها ليست برهاناً: «404» تصدقُ على عمليّةٍ لم تُقلع وعلى منفذٍ
خطأٍ وعلى تطبيقٍ فارغ. **وخلافاً لـ`F5-04` نُفِّذَ البرهانُ محلّيّاً فعلاً لا في
CI وحدَها**: `3 pass · 0 fail · 6.37s` على قاعدةٍ حقيقيّةٍ بالهجراتِ مطبَّقةً.
محلّيّاً: `lint`=**0** (984 ملفاً) · `typecheck`=**0** · **2329 ناجحاً · 654
مُتخطّى · 0 فاشلاً** (القياسُ على `main`: 2304 · 649 · 0) · `check:coverage`
خروجٌ **0** · والصورُ الثلاثُ تمرُّ في `verify-container-build.sh`.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **العزلُ نفسُه لم يُقَس**: أنَّ استعلاماتِ اللوحةِ
لم تعُد تُزاحِمُ الويبهوكَ **مستنتَجٌ** من عمليّةٍ أخرى ببِركةٍ أخرى لا **مقيسٌ**
بحملٍ متزامنٍ — وهوَ الادّعاءُ الذي يُبرِّرُ البندَ كلَّه · ولم يُنشَر شيءٌ على
Render (`autoDeploy: false`) وخدمةُ `waslah-admin` مُعلَنةٌ ولم تُخلَق — **وهوَ
عينُ الحالِ المُشخَّصِ في `docs/directive-item-0-live-diagnosis.md` §0.2**، فمتى
نُشِرَ الفرعُ بلا إنشاءِ الخدمةِ صارَ `ADMIN_ORPHANED` حيّاً · ولم تُبنَ الصورةُ
(لا عفريتَ Docker) والمحروسُ حلُّ الاستيراداتِ وحدَه · ولم تُقَس الدلتا اللحظيّةُ
عبرَ العمليّاتِ ههنا (حالاتُ البرهانِ بلا Redis) وعبورُها مُثبَتٌ في موضعِه من
`SCL-004` · وسقفُ اتّصالاتِ اللوحةِ `5` **اختيارٌ لا قياسٌ** يُراجَعُ عندَ أوّلِ
قياسٍ حقيقيٍّ. **فالدرجةُ «مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**،
ورفعُها مرهونٌ بنشرٍ فعليٍّ تعملُ فيهِ الخدماتُ الثلاثُ معاً وبقياسِ العزلِ تحتَ
حمل.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F6-05` / `TG-002` / `SS-07`: الإشعاراتُ صارَ لها تصنيفٌ، والمستخدمُ صارَ له موجَزٌ

**ما كانَ يُقالُ ضِمناً** إنَّ صندوقَ الصادرِ الموحَّدَ (ADR 0047) حلَّ مسألةَ
الإشعاراتِ، وإنَّ دلوَ `TG-001` (ADR 0009) حلَّ مسألةَ الحدِّ. **والصوابُ** أنَّ
الاثنَينِ معاً يُجيبانِ عن «كيفَ نُرسِلُ الكثيرَ بلا حجبٍ» ولا يمسّانِ سؤالاً
أسبقَ منهما: **هل كلُّ ما نُرسِلُه يستحقُّ أن يُقاطِعَ المستخدمَ؟** فصفٌّ واحدٌ
اليومَ يحملُ أحدَ عشرَ نوعاً متساويةً في المعاملةِ: عرضُ رحلةٍ يُنتظَرُ في ثوانٍ،
وإخطارُ اشتراكٍ يُقرأُ في يومٍ، وبلاغُ استغاثةٍ يُقرأُ الآن — وكلُّها رسالةٌ في
محادثةٍ، وكلُّها من الدلوِ نفسِه. ونتيجةُ التسوية مزدوجةٌ: المستخدمُ يعتادُ
الإزعاجَ فيُطفئُ الإشعاراتِ — **فيفقدُ الحرجَ مع المعلوماتيِّ** — والدلوُ يُهدَرُ
على ما لا يُنتظَرُ فيتأخّرُ ما يُنتظَر.

**وعدمُ التماثلِ ههنا مقلوبٌ عن كلِّ ما سبقَ في الموجةِ — وهوَ لبُّ البندِ كلِّه.**
في `F5-04` كانَ الخطرُ توقّفاً صامتاً، وفي `F5-08` كانَ **النجاحَ الكاذبَ**؛ وههنا
الخطرُ أشدُّ من كلَيهما لأنَّه **نجاحٌ كاذبٌ في سِجِلٍّ يُدقَّقُ**. تصنيفُ إعلانٍ
`critical` يُهدِرُ الدلوَ — أثرٌ مرئيٌّ مَقيسٌ يُشتَكى منه. وتصنيفُ نوعٍ حرجٍ
`in_app` يعني أنَّ المستخدمَ **لم يُبلَّغْ ولا شيءَ يُخفِقُ**: صفُّ الصادرِ ينتهي
في حالةٍ نهائيّةٍ ناجحةٍ وصفُّ المركزِ مكتوبٌ، **فكلُّ سطحِ تدقيقٍ — عدّادُ
الميّتِ وسجلُّ الإخفاقِ ومقاييسُ التسليمِ — يقرأُ «سُلِّم»**، ولا يُكتشَفُ الخللُ
إلّا بشكوى مستخدمٍ لم يُخبَرْ بشيءٍ. **وأسوأُ الأحوالِ نوعٌ يُوجَّهُ إلى مجموعةٍ**:
`safety_incident` يُنشَرُ في قروبِ سلامةٍ لا في صندوقِ واردٍ شخصيٍّ، فتصنيفُه
`in_app` لا يُنتِجُ رسالةً **ولا مدخلَ مركزٍ** — **إعدامٌ صامتٌ لبلاغِ استغاثةٍ،
وصفُّه يُقرأُ نجاحاً**.

**ولذلكَ بُنيَ البندُ كلُّه بميلٍ واحدٍ: كلُّ إخفاقٍ في التصنيفِ يُخفِقُ نحوَ
الإرسالِ.** ثلاثةُ حرّاسٍ لا واحدٌ: غيابُ صفِّ سياسةٍ ⇒ `critical` (نوعٌ لم
يُصنَّفْ بعدُ يُزعِجُ ولا يصمتُ) · ونوعٌ مُوجَّهٌ إلى مجموعةٍ **يستحيلُ** تصنيفُه
داخلَ التطبيقِ بقيدِ جدولٍ يرفعُ خطأً لا بمراجعةٍ تنتبهُ · ومستقبِلٌ لا يُحلُّ ⇒
`critical`، ودالّةُ الحلِّ **لا ترفعُ استثناءً أبداً** فلا يُرجِعُ عطلُ دفترِ
إشعاراتٍ معاملةَ عرضِ رحلةٍ أو إلغاءِها — **الدفترُ تابعٌ لا شرطٌ**.

**والقرارُ في مُطلِّبِ `before insert` لا في طبقةِ تطبيقٍ** لأنَّ الأبوابَ إلى
الصندوقِ أكثرُ من بابٍ (`enqueue_notification` للأنواعِ العامّةِ، و`open_offer_round`
يُدرِجُ `offer` مباشرةً، وكلُّ بابٍ آتٍ): تصنيفٌ في التطبيقِ يعني بابَينِ مصنَّفَينِ
وبابَينِ لا، وهوَ فرقٌ لا يراه إلّا من يبحثُ عنه. **والكتمُ فعليٌّ لا وصفيٌّ**:
حالةٌ نهائيّةٌ جديدةٌ `in_app_only` — لا `delivered` (تلزمُها رسالةٌ لا وجودَ لها)
ولا `canceled` (قرارُ مشرفٍ) — ومُطالِبُ التسليمِ يمسحُ `pending` وحدَه، فالصفُّ
خارجَ مدى كلِّ مُطالِبٍ قائمٍ أو آتٍ بلا أن يُعدَّلَ مُطالِبٌ واحد. **والقناةُ
المُسجَّلةُ في المركزِ مشتقّةٌ من الحالةِ الناتجةِ لا من صفِّ السياسةِ**: موجَزٌ
يقولُ «داخلَ التطبيقِ» عن رسالةٍ أُرسِلَت سِجلٌّ كاذبٌ.

**والحدُّ خاصّيّةُ مخطَّطٍ لا انتباهُ مُراجعٍ**: `get_user_notifications` و
`mark_notification_read` تُحوِّلانِ معرّفَ تيليجرام إلى `users.id` **بنفسَيهما**،
ولا يقبلُ أيُّ سطحٍ مُعرِّفَ مستخدمٍ من العميلِ (ADR 0035). و«لا وجودَ له» و«ليسَ
لك» جوابٌ واحدٌ فلا عرّافَ يُستنطَق. والوسمُ متماثلُ الأثرِ بـ`coalesce(read_at,
now())` تحتَ قفلِ صفٍّ، **وفتحُ الموجَزِ لا يَسِمُ**: «مرَّ أمامَ العينِ» ليسَ
«قُرِئَ». والحدُّ مقصورٌ في القاعدةِ (`least(greatest(…,1),50)`) وفي التطبيقِ معاً،
قصراً لا إخفاقاً.

**المُختبَرُ.** `tests/integration/notification-classification-center.test.ts` —
**8 ناجحةً · 0 فاشلاً · 38 توكيداً · 23.82s على PostgreSQL حقيقيّةٍ** بالهجراتِ
مطبَّقةً، والشاهدُ **حالةُ الصفِّ في القاعدةِ ونتيجةُ نداءِ `claim_notification_delivery`
فعلاً** لا وجودُ دالّةٍ: الحرجُ يبقى `pending` ويُقيَّدُ (**ضابطٌ موجبٌ**) · و
`in_app` تُنتِجُ `in_app_only` **فلا يلتقطُها المُطالِبُ حينَ يُنادَى** · وحذفُ صفِّ
السياسةِ يُرسِلُ · وضبطُ `safety_incident` على `in_app` **تَرفُضُه القاعدةُ** ·
ومستقبِلٌ لا يُحلُّ لا يُسقِطُ الإيداعَ · و(ب) لا يقرأُ إشعارَ (أ) ولا يَسِمُه ·
و`p_limit=9999` يُقصَرُ إلى 50. و`tests/unit/notification-center-route.test.ts`
(٢٠) و`tests/unit/check-notification-classification.test.ts` (٨) — **28 ناجحةً ·
95 توكيداً**. محلّيّاً: `lint`=**0** (992 ملفاً) · `typecheck`=**0** · **2357
ناجحاً · 664 مُتخطّى · 0 فاشلاً** (القياسُ على `main`: 2329 · 654 · 0) ·
`check:coverage` خروجٌ **0** · وحاجزٌ جديدٌ `check-notification-classification`
موصولٌ في `package.json` وفي وظيفةِ `verify`.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **لا نوعَ أُعيدَ تصنيفُه فعلاً**: الأحدَ عشرَ كلُّها
مبذورةٌ `critical`، فالسلوكُ الظاهرُ بعدَ الهجرةِ مطابقٌ لما قبلَها حرفاً —
والمبنيُّ الآليّةُ لا القرارُ · **ولا شاشةَ لـ`SS-07`** (سطوحُ التطبيقِ المصغَّرِ
هياكلُ، وشاشاتُه بنودُ `F2`/`F3`)، وتصنيفُ نوعٍ `in_app` قبلَ وجودِ شاشةٍ تعرضُه
**إسكاتٌ لا تهدئةٌ** · **ولم يُقَسْ توفيرُ الدلوِ** الذي هوَ الغرضُ المُعلَنُ لـ
`TG-002`، ولا يُقاسُ ما دامَ لا كتمَ فعليّاً · **ولم يُقَسْ أثرُ المُطلِقَينِ
الجديدَينِ** على معدّلِ الإيداعِ تحتَ حملٍ — كلُّ إيداعٍ صارَ يقرأُ صفَّ سياسةٍ
بمفتاحٍ أوّليٍّ ويُدرِجُ صفَّ مركزٍ · وإشعارٌ لمستقبِلٍ لا يُحلُّ لا يظهرُ في
موجَزٍ (مُختبَرٌ صريحاً) · ولا `request-id` (`F1-08`) ولا حدَّ معدّلٍ لكلِّ
مستخدمٍ على مسارَي المركزِ · ولم تجرِ اختباراتُ التكاملِ على وظيفةِ CI بعدُ لحظةَ
كتابةِ هذا القسمِ. **فالدرجةُ «مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**،
ولا يُقلَبُ `F6-05` إلى `[x]` قبلَ استيفاءِ `ح-4`.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F7-02` / `CAP-005`: الفهارسُ صارَ لها سجلٌّ واحدٌ، وأثرُها صارَ يُقاسُ بالخطّةِ

**والمسألةُ لم تكن «أضِفْ فهارسَ».** نصُّ البندِ مراجعةٌ **بخططِ تنفيذٍ قبل/بعد**
لكلِّ استعلامٍ ساخنٍ، وإنشاءٌ بـ`CONCURRENTLY`. فجرى الحصرُ أوّلاً لا الإضافةُ:
**١٧٥ تعريفَ فهرسٍ** قائماً في ٧٨ هجرةً **بلا `drop index` واحدٍ** (فلا فهرسَ
أُلغيَ يوماً)، و**٧٦ استعلامَ TypeScript** فيهِ ترشيحٌ، و**٤٢ دالّةَ `plpgsql`**
في نسخِها النهائيّةِ؛ ومنها **٥٢ استعلاماً ساخناً**: ٣٨ مخدومٌ ببادئةٍ يسرى
صحيحةٍ، و١١ بلا فهرسٍ، و٣ ترشيحُها ناقصٌ. **وأخطرُ ما وُجِدَ** أنَّ
`orders.assigned_driver_id` و`orders.rider_id` **مفتاحانِ خارجيّانِ بلا فهرسٍ
ألبتّةَ**، والأوّلُ في مسارِ نبضةِ الموقعِ وفي شوطِ الخريطةِ الحيّةِ، والثاني في
مسارِ طلبٍ تفاعليٍّ.

**فأُنشئَت ستُّ فهارسَ لا أكثرَ**: `orders (assigned_driver_id, status)` جزئيّاً ·
`orders (rider_id, status)` · `orders (city_id, created_at) where
status='searching'` (البادئةُ القائمةُ تُرشِّحُ ولا تُرتِّبُ فتفرِزُ كلَّ شوطٍ) ·
`notification_outbox (created_at)` جزئيّاً على ثمانيةِ أنواعِ دورةِ الرحلةِ ·
`audit_log (action, entity_id)` · `attendance_log (changed_at desc)`. **ولكلِّ
فهرسٍ مُستدعٍ إنتاجيٌّ مُصرَّحٌ** (تسعةٌ مجموعاً) يُثبِتُ الحاجزُ وجودَ ملفِّه
وذكرَه لجدولِ الفهرسِ — القاعدةُ السياديّةُ 0.1؛ **وما لا مُستدعيَ لهُ لم يُنشأْ**
ومنهُ `order_offers (driver_id)`.

**ومصدرُ الحقيقةِ واحدٌ**: `scripts/lib/hot-query-indexes.ts` — الفهرسُ وهجرتُه
ومُستدعوهُ واستعلامُه المُمثِّلُ ورتبةُ أثرِه في سجلٍّ يقرؤُه ثلاثةٌ: الحاجزُ
`scripts/check-hot-query-index-coverage.ts` (موصولٌ في `package.json` وفي وظيفةِ
`verify`)، و`tests/unit/hot-query-indexes.test.ts` (**١٣ ناجحةً · ٦٣ توكيداً**)،
و`tests/integration/hot-query-index-plans.test.ts`.

**والأثرُ يُقاسُ بالخطّةِ لا بالوصفِ**: لكلِّ فهرسٍ بذرٌ ثمَّ `explain` **والفهرسُ
قائمٌ** فيلزمُ ظهورُه بالاسمِ، ثمَّ **إسقاطُه في المعاملةِ نفسِها** فيلزمُ انحدارُ
الخطّةِ إلى مسحٍ كاملٍ أو فرزٍ **وارتفاعُ كلفتِها**، ثمَّ **ارتدادُ المعاملةِ**
فلا صفَّ بذرٍ يبقى ولا فهرسَ يُفقَدُ. وستُّ هجراتٍ لا واحدةٌ لأنَّ ADR-0068 يمنعُ
`concurrently` مع غيرِها في ملفٍّ، وكلٌّ تُصرِّحُ `-- migration-phase: index`
وبـ`if not exists`.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **لا `enable_seqscan = off`**: إلزامُ المخطِّطِ
يُخفي الجدوى ولا يُثبِتُها · **ولا `explain analyze`**: زمنُ عتادِ CI متقلِّبٌ
فيُنتِجُ اختباراً يسقطُ بلا انحدارٍ، فالمُقارَنُ العُقدةُ والكلفةُ التقديريّةُ ·
**والبذرُ ٤٠٠٠ صفٍّ لا حجمُ إنتاجٍ**، فالمُدَّعى تغيُّرُ الخطّةِ لا رقمُ تحسُّنٍ ·
**ولم تُقَسْ كلفةُ الكتابةِ** التي زادتها ستُّ فهارسَ (ميزانُها `F7-06`) ·
**والحصرُ نصّيٌّ** لا من `pg_stat_statements` (يستلزمُ إنتاجاً بحملٍ وامتداداً
مُفعَّلاً: قرارُ مالكٍ وبيئةٌ غيرُ متاحةٍ) · **والرتبةُ إعلانٌ بشريٌّ** مُعلَّلٌ
بالتواترِ والنموِّ. ومحلّيّاً: `lint`=**0** (٩٩٦ ملفّاً) · `typecheck`=**0** ·
`bun test tests/unit`=**2097 ناجحةً · 0 فاشلاً · 6537 توكيداً** · وسجلُّ التجاوزِ
صارَ **٧٣ ملفّاً · ٦٤٠ حالةً**. **فالدرجةُ «مُنفَّذ · مُختبَر» ولا يُقالُ «مَقيس»
ولا «مُثبَت» (`ح-5`)**، ولا يُقلَبُ `F7-02` ولا `CAP-005` قبلَ استيفاءِ `ح-4`.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F7-02` / `CAP-005`: حكمُ CI، والأحمرُ الأوّلُ كانَ صادقاً

**جولةُ CI الأولى (`34320171469` على `c1d8558`)**: `verify` ✅ وRedis ✅ و
**PostgreSQL ❌** في ثلاثِ حالاتٍ من ستٍّ — المخطِّطُ لم يستخدمِ الفهرسَ ولو كانَ
قائماً، والكلفةُ قبلَ الإسقاطِ وبعدَه **متساويةٌ حرفاً** (24.63 · 24.63 · 115.38).

**والسببُ الجذريُّ في القياسِ لا في الفهارسِ**: (أ) استعلامانِ مُمثِّلانِ خالفا
مُستدعيَهما — سقطَ `order by created_at asc` من مُمثِّلِ `orders_rider_status_idx`
و`order by changed_at desc` من مُمثِّلِ `attendance_log_changed_at_idx`، و`limit`
بلا ترتيبٍ يُبيحُ للمخطِّطِ مسحاً متسلسلاً يتوقّفُ باكراً فقِيسَ ما لا يقعُ ·
(ب) توزيعُ البذرِ كذَبَ على الإنتاجِ — `ACTORS=20` تُعطي السائقَ ١٩٠ صفّاً من
٤٠٠٠ (نحوَ ٥٪)، فصارَ `ACTORS=400` ونصيبُ الواحدِ ٠٫٢٪. **ولم يُلمَسْ توكيدٌ ولم
يُعطَّلْ حاجزٌ ولم يُضَفْ `enable_seqscan=off` ولم يُصنَّفْ إخفاقٌ تخطّياً.**

**وجولةُ CI الثانيةُ (`34320818432` · `34320813864` على `db71e8c`) خضراءُ تامّاً**:
`verify` ✅ · «تكامل على PostgreSQL حقيقي» ✅ **567 ناجحةً · 11 مُتخطّاةً · 0
فاشلاً** · «تكامل على Redis حقيقي» ✅ **8 ناجحةً**. والحالاتُ التسعُ ناجحةٌ،
والكلفةُ بالفهرسِ ثمَّ بإسقاطِه في المعاملةِ نفسِها: صندوقُ الصادرِ **0.46 →
434.05** · سجلُّ التدقيقِ 8.42 → 346.01 · طلباتُ الراكبِ 43.01 → 234.18 ·
الباحثُ في المدينةِ 73.47 → 267.49 · طلبُ السائقِ الجاري 5.22 → 16.11 · سجلُّ
الحضورِ 235.79 → 403.17. **والارتدادُ تامٌّ**: الفهارسُ الستُّ قائمةٌ بعدَ القياسِ
كلِّه ولا صفَّ بذرٍ بقيَ.

**وحدودٌ مُعلَنةٌ**: الكلفةُ **تقديرُ مخطِّطٍ** لا زمنُ إنتاجٍ · والنسبُ عندَ ٤٠٠٠
صفٍّ لا عندَ مليونٍ · ولا كلفةَ كتابةٍ مقيسةً (ميزانُها `F7-06`) · **وجولتانِ لا
ثلاثٌ والثانيةُ وحدَها خضراءُ**. فالدرجةُ «مُنفَّذ · مُختبَر»، و`F7-02` و`CAP-005`
يبقيانِ `[ ]` حتّى `ح-4`.
## تصحيحٌ مُضافٌ — 2026-09-09 · `F7-07` / `CAP-007`: الهجرةُ صارَ لها طريقٌ واحدٌ محكومٌ لا حلقةَ صدفةٍ

**ما كانَ مكتوباً ههنا قبلَ اليومِ صحيحٌ ويبقى:** الجدولُ في §«ما عبرَ في وظيفةِ
التكاملِ» يقولُ «تطبيقُ الهجراتِ بالترتيبِ»، وهوَ ما كانَ يقعُ فعلاً. **وما لم
يكنْ مكتوباً**: أنَّ ذلكَ الترتيبَ كانَ حلقةَ `psql` عاريةً بلا مهلةِ قفلٍ ولا
ملكيّةِ معاملةٍ ولا حكمِ سلامةٍ، مكتوبةً **ثلاثَ مرّاتٍ** في ثلاثةِ مواضعَ
(وظيفتَي CI، وأمرِ الإنتاجِ في `docs/render-deployment-vars.md` §4). فكانَ
الأخضرُ في CI يشهدُ على تطبيقٍ لجدولٍ فارغٍ في ملّي ثانيةٍ، ولا يشهدُ بشيءٍ على
جدولٍ فيه مليونُ صفٍّ تحتَ قفلٍ حاجزٍ. وهوَ العائقُ `CAP-007` بنصِّه.

**والمقيسُ من الشيفرةِ قبلَ التنفيذِ:** ٧٨ هجرةً · ٩٦ `create index` **ولا واحدَ
`concurrently`** · `set not null` في ملفٍّ · عبارتانِ هادمتانِ · ولا ملفَّ
يُصرِّحُ طورَه.

**وما بُنيَ** (ADR-0068 · `docs/evidence/architecture/F7-07-20260909.md`): مكتبةُ
قواعدَ واحدةٌ يقرأُها الحاجزُ والمُطبِّقُ معاً (`scripts/lib/migration-safety.ts`)
— قاسمةُ عباراتٍ تحترمُ التعليقَ والاقتباسَ واقتباسَ الدولارِ، وستُّ قواعدَ
(الفهرسُ متزامنٌ ووحدَه وفي طورِه · القيدُ `not valid` · `set not null` ممنوعةٌ ·
الحذفُ في `contract` وحدَه · الاسترجاعُ · ولا معاملةَ في الملفِّ)، وستّةُ أطوارٍ
مُصرَّحةٍ في رأسِ الملفِّ. وحاجزٌ في `verify` وفي `bun run ci`. **ومُطبِّقٌ آمنٌ**
`scripts/migrate.ts` يحكمُ قبلَ أن يتّصلَ، ويضبطُ `lock_timeout=3s`، ويملكُ
المعاملةَ، ويُطبِّقُ طورَ الفهرسِ بلا معاملةٍ. **وحلقتا `psql` في CI أُزيلَتا**
واستُبدِلَتا بالمُطبِّقِ، وأمرُ الإنتاجِ في §4 صارَ الأمرَ نفسَه — فمصدرُ الحقيقةِ
واحدٌ، والإنتاجُ يُطبَّقُ بالطريقِ الذي يُقاسُ في CI حرفاً.

**ولا سجلَّ هجراتٍ في القاعدةِ** — القاعدةُ السياديّةُ 0.4 تُلزِمُ كلَّ جدولٍ
بـ`city_id`، وتوسيعُ صنفِ الاستثناءِ المُغلَقِ قرارُ مالكٍ. فبديلُه قاعدةُ
الاسترجاعِ، **وهيَ مقيسةٌ لا موصوفةٌ**: التكاملُ يُطبِّقُ السلسلةَ كلَّها مرّتَينِ
على قاعدةٍ حقيقيّةٍ ويُثبِتُ أنَّ البذرَ لا يتضاعفُ.

**والقياسُ محلّيّاً:** `migration-safety.test.ts` **٣٤ ناجحةً · ١٢٦٦ توكيداً** ·
`skip-audit` ٤٥ ناجحةً بأساسٍ مرفوعٍ ٧٢→٧٣ ملفّاً و٦٣١→٦٣٦ حالةً ·
`check-migration-safety` خروجٌ **٠** · `--dry-run` يطبعُ خُطّةَ ٧٨ ملفّاً بلا
اتّصالٍ · `lint`=**٠** · `typecheck`=**٠**.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **الحاجزُ يحكمُ اليومَ على صفرِ ملفٍّ**: الثمانُ
والسبعونَ كلُّها دَينٌ مُعلَنٌ مُجمَّدٌ بعددٍ مُقفَلٍ وحدِّ تقادُمٍ
`20260909050000`، فأولُ حكمٍ حقيقيٍّ يقعُ على أولِ هجرةٍ تُكتَبُ بعدَ الحدِّ. وهذا
حاجزٌ أخضرُ على مجموعةٍ فارغةٍ — عينُ الحالةِ التي أنتجَت الحاجزَ الأعمى مرّتَينِ
(ADR-0045 §٧ · ADR-0046 §٨) — فلذلكَ **لكلِّ قاعدةٍ من الستِّ برهانُ سقوطٍ** على
نصٍّ مخالفٍ، وبرهانانِ على ألّا يُصنَعَ حكمٌ من كلمةٍ في اقتباسٍ أو تعليقٍ ·
**ولا هجرةَ واحدةً كُتِبَت بالنمطِ بعدُ**، فالنمطُ آليّةٌ مُختبَرةٌ لا مساراً
عبرَه شيءٌ · **ولا PostgreSQL في بيئةِ التنفيذِ المحليّةِ**، فحالاتُ التكاملِ
الخمسُ لا حكمَ لها إلّا CI · ولا مُصادِقَ آليّاً للقيودِ غيرِ المُصادَقةِ
(التصديقُ هجرةُ طورٍ يكتبُها الإنسانُ) · ولا عودةَ إلى الوراءِ في المُطبِّقِ
(تمرينُ العودةِ قائمٌ بذاتِه في `scripts/rollback-schema-drill.ts`). **فالدرجةُ
«مُنفَّذ · مُختبَر» ولا يُقالُ «مُتحقَّقٌ منه» ولا «مَقيس» (`ح-5`)، ولا يُقلَبُ
`F7-07` ولا `CAP-007` إلى `[x]`.**

### تصحيحٌ بالقياسِ مُضافٌ إليه — 2026-09-09 · `F7-07`: حدُّ الاسترجاعِ لم يكنْ حيثُ ادُّعِيَ

**ما فوقَ يبقى مكتوباً كما كُتِبَ**، وهذا ما أبطلَه حكمُ CI منه.

كُتِبَ أعلاهُ أنَّ بديلَ سجلِّ الهجراتِ «قاعدةُ الاسترجاعِ **مقيسةً**: التكاملُ
يُطبِّقُ السلسلةَ كلَّها مرّتَينِ ويُثبِتُ أنَّ البذرَ لا يتضاعفُ». **وقد سقطَ
ذلكَ في CI** (التشغيلُ `34315907516` · وظيفةُ «تكامل على PostgreSQL حقيقي»):

```
PostgresError: trigger "cities_set_updated_at" for relation "cities" already exists  (42710)
```

**والسببُ الجذريُّ**: الهجراتُ الثمانُ والسبعونَ الموروثةُ **ليست مُسترجَعةً** —
عينُ الدَّينِ المُعلَنِ: القاعدةُ الخامسةُ لم تكنْ مفروضةً يومَ كُتِبَت. فالادّعاءُ
صحيحٌ على ما بعدَ حدِّ التقادُمِ، باطلٌ على ما قبلَه، ولم يكنِ الفرقُ مذكوراً.
**والخطأُ في الترتيبِ لا في المعرفةِ**: كُتِبَت «مقيسٌ في CI» قبلَ أن يُقرأَ حكمُ
CI.

**وما نجحَ في الجولةِ نفسِها ويبقى قائماً**: `verify` ✅ · Redis ✅ ·
**وخطوةُ تطبيقِ الهجراتِ بالمُطبِّقِ الجديدِ نجحَت في الوظيفتَينِ** فالحلقةُ
المُزالةُ لا يُفتقَدُ منها شيءٌ · وأربعُ حالاتٍ من الخمسِ نجحَت، ومنها
**`lock_timeout` بـ`55P03` في ١٣٢٠ms** وهوَ عينُ `CAP-007`.

**والمُعالَجةُ سبباً لا عرضاً**: `--from <طابعٌ>` في المُطبِّقِ (قاعدةٌ فارغةٌ ⇒
السلسلةُ كلُّها · قاعدةٌ مُهاجَرةٌ ⇒ ما بعدَ الطابعِ)، وإرشادٌ صريحٌ في المُخرَجِ
عندَ `42710`/`42P07`، **والحدُّ صارَ مقيساً في شقَّيهِ** (المستوفيةُ تُعادُ بلا
تضاعُفٍ · والموروثةُ تسقطُ بأحدِ الرمزَينِ · و`--from` يُقصِرُ القائمةَ)،
وتصحيحٌ **بالإضافةِ لا بالمحوِ** في ADR-0068 §«تصحيحٌ بالقياسِ»، وإجراءُ الإنتاجِ
صارَ يشترطُ `--from` على قاعدةٍ فيها المخطَّطُ. **ولا حاجزَ عُطِّلَ ولا اختبارَ
خُفِّفَ ولا دليلَ مُحيَ** — والحالاتُ ٥→٧، وأساسُ التجاوزِ ٦٣٦→٦٣٨.
---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F4-01`: استقبالُ الموقعِ صارَ حَكَماً واحداً لبابَينِ

**المُصحَّحُ.** كانَ عقدُ القسم 10 يُوجِبُ `POST /v1/driver/location` ولا وجودَ له
في البوابةِ قطُّ؛ وكانَ **قرارُ الاستقبالِ كلُّه** — تقييمُ الإصلاحةِ ثمَّ الكتابةُ
الشرطيّةُ ثمَّ الجلسةُ والبثُّ ثمَّ إعادةُ العرضِ — مكتوباً مرّةً واحدةً داخلَ
`handleLocation` في حوارِ السائقِ. فكانَ فتحُ البابِ الثاني على حالِه إمّا نسخاً
ثانياً للقرارِ (وهوَ ما ينهاهُ `ADR 0053 §٦`: حَكَمٌ واحدٌ على «الأحدثِ»)، وإمّا
باباً أعرَجَ. فنُقِلَ القرارُ إلى حالةِ استخدامٍ مشتركةٍ
`packages/application/geo/update-driver-location.ts`، وصارَ البابانِ يُناديانِها:
البوتُ (قائمٌ اليومَ) والمسارُ الجديدُ. والترتيبُ الخمسيُّ محفوظٌ في **موضعٍ واحدٍ**
لا في اثنَينِ، فلا يُنشَرُ الأقدمُ بعدَ رفضِ الحارسِ (`BUG-001`)، ولا يُرقَّمُ حدثٌ
لم يُكتَبْ (`BUG-009`)، ولا تُطلَبُ إعادةُ عرضٍ إلّا على انتقالٍ حقيقيٍّ إلى الحياةِ.

**المُنفَّذُ.** المسارُ لا يقرأُ هويّةً ولا دوراً من الطلبِ: جلسةٌ موقَّعةٌ
(`bearerTokenFrom` + `authorizeViewer`) ثمَّ صفُّ سائقٍ بمعرّفِ تيليجرام؛ ومعرّفٌ
مدسوسٌ في الجسمِ **مُختبَرٌ صريحاً** أنّه لا يُغيِّرُ مَن يُكتَبُ عليه. والحدودُ
قبلَ العملِ: حدُّ معدّلٍ على الجلسةِ بـ`USER_LIMIT` نفسِه، وسقفُ جسمٍ 1 KB
(`413`). و`stale` يُجابُ عنه بـ`200 {accepted:false, reason:"STALE"}` — صدقاً لا
خطأً؛ و`422 FIX_REJECTED` تُميَّزُ من `400` المشوَّهِ ومن `404` غيرِ المسجَّلِ ومن
`503` العطلِ. والتبعياتُ من الحاويةِ نفسِها (`container.driverLocation`) فلا سياسةَ
موقعٍ ثانيةً ولا ساعةَ ثانيةً، ومُنادي الإنتاجِ مركَّبٌ في `index.ts` (القاعدة 0.1).
وفرعُ المنطقةِ المفضَّلةِ (البند 2.4) بقيَ في الحوارِ بسلوكِه نفسِه حرفاً.

**المُختبَرُ.** `tests/unit/update-driver-location.test.ts` (**12**) —
والشاهدُ فيها **الترتيبُ لا وجودُ الدالّةِ**: لا كتابةَ قبلَ تقييمٍ، ولا بثَّ قبلَ
كتابةٍ، ولا بثَّ ولا إعادةَ عرضٍ عندَ `stale`، والمتدهوِّرُ يُخزَّنُ موسوماً لا
يُطرَحُ. و`tests/unit/driver-location-route.test.ts` (**19**) على خادمِ البوابةِ
الحقيقيِّ: كلُّ رمزٍ ورقمُه، ولا رمزَ ولا معرّفَ تيليجرام في أيِّ ردٍّ. و
`tests/integration/driver-location-intake.test.ts` (**6**) على PostgreSQL حقيقيّةٍ:
الطريقُ كلُّه من طلبٍ إلى الصفِّ القانونيِّ، ومنها **نبضاتٌ مختلطةُ الترتيبِ لا
تُرجِعُ الموضعَ إلى الوراءِ** — انحدارُ `BUG-001` مقيساً على السطحِ الجديدِ نفسِه.
محلّيّاً: `lint`=**0** (993 ملفاً) · `typecheck`=**0** · `build:miniapp`=**0** ·
وحدةٌ **2115 ناجحةً · 0 فاشلةً** (139 ملفاً) · الحرّاسُ الاثنانِ والعشرونَ **0** ·
`check:coverage` **2388 ناجحةً · 672 مُتخطّاةً · 0 فاشلةً** وبوابةُ التغطيةِ فوقَ
أرضيّاتِها التسعِ («التتبّعُ وموقعُ السائق» **56.56%**). وأرضيّاتُ `skip-audit`
**رُفِعَت** 72→73 ملفّاً و631→637 حالةً — لا تُخفَّضُ أرضيّةٌ.

**وحدودٌ مُعلَنةٌ لا مضمرةٌ.** **لا حملَ قِيسَ**: كلُّ نبضةٍ اليومَ كتابةٌ في
القاعدةِ، والحالةُ الساخنةُ المشتركةُ والاستمرارُ المجمَّعُ بندُ `F4-02`، فلا
يُقالُ `مَقيس` ولا `مُثبَت عند N` (`ح-5`) · و**لا عميلَ يستدعي المسارَ بعدُ** (بندُ
`F3-04`) والبابُ الأوّلُ هوَ العاملُ اليومَ · و`Idempotency-Key` غيرُ مطلوبٍ على
هذا المسارِ بحكمِ صنفِه في العقدِ («مسارٌ ساخنٌ» لا «أمرٌ») والامتناعُ محفوظٌ في
الحارسِ الشرطيِّ نفسِه — وهذا اجتهادٌ مُعلَنٌ في `ADR 0067` لا سكوتٌ عنه · و
الحالاتُ الستُّ التكامليّةُ **مُتخطّاةٌ محلّيّاً** لغيابِ قاعدةٍ في هذه البيئةِ،
فحكمُها من CI وحدَه. **فالدرجةُ «مُنفَّذ · مُختبَر»، و`F4-01` يبقى `[ ]`، والدمجُ
لمراجعٍ مستقلٍّ (0.7).**
## تصحيحٌ مُضافٌ — 2026-09-09 · `F6-06`: الطوابيرُ صارَ لها سقفٌ في كلِّ بعدٍ لا في العمقِ وحدَه

**ما كانَ مكتوباً في هذه الوثيقةِ قبلَ اليومِ صحيحٌ ولم يُمَسَّ حرفاً.** وهذا القسمُ إضافةٌ تُقيِّدُ فرضيّةً كانت قائمةً بالسكوتِ: أنَّ سقفَ إعادةِ المحاولةِ والرسالةَ الميتةَ يكفيانِ لحمايةِ طابورٍ.

**لا يكفيانِ.** كلُّ ما يحمياه هوَ الصفُّ الواحدُ الذي أخفقَ: ينتهي إلى `dead` بعدَ استنفادِ محاولاتِه فلا يُعادُ إلى الأبدِ. ولا يمسّانِ الطابورَ **جملةً**: عمقُه، وعمرَ أقدمِ حدثٍ مستحقٍّ فيه، وعددَ موتاه في نافذةٍ، ومعدَّلَ من يُدرِجُ فيه، وعددَ من يستهلكُ منه في وقتٍ واحدٍ. وطابورٌ يمتلئُ في هذه الأبعادِ **لا يُخفِقُ إخفاقاً يُقرأُ**: صفوفُه `pending` وهيَ حالةٌ مشروعةٌ، ولا صفَّ ميتاً فالمحاولاتُ لم تُستنفَدْ، والمستهلِكُ يعملُ بلا خطأٍ — والذي يسقطُ هوَ ما بعدَ الطابورِ: عرضُ رحلةٍ يُنتظَرُ في ثوانٍ يخرجُ بعدَ دقائقَ، وبلاغُ استغاثةٍ يقفُ خلفَ مئتَي رسالةِ حملةٍ.

### ما صارَ قائماً بعدَ `20260909120000`

**ستَّةُ أبعادٍ مُعلَنةٌ اسماً لكلِّ طابورٍ حيٍّ، ولكلِّ بعدٍ موضعُ حدٍّ واحدٌ.** الأبعادُ قائمةٌ مغلقةٌ في `packages/application/scheduling/queue-backpressure.ts`، ومواضعُ حدودِها خريطةٌ في `packages/shared/config/queue-backpressure.ts` — **ولا رقمَ في تلكَ الخريطةِ**: هيَ تقولُ «أينَ يسكنُ الحدُّ» لا «كم هوَ». وموضعُ الحدِّ حيثُ يسكنُ سقفُ إعادةِ المحاولةِ لذلكَ الطابورِ نفسِه: `platform_settings` لكلِّ مدينةٍ لِـ`notification_outbox` (ستَّةُ مفاتيحَ جديدةٍ **كلُّها `is_provisional = true`**)، وثوابتُ `packages/shared/config/domain-ingress.ts` لِـ`telegram_update_jobs` لأنَّه الاستثناءُ المغلقُ من قاعدةِ `city_id` فحدٌّ لكلِّ مدينةٍ لطابورٍ لا مدينةَ له يقتضي قراءةَ مدينةٍ عشواءَ.

**والحكمُ في المحرِّكِ لا في المستهلِكِ.** مُطلِّبُ `before insert` على `notification_outbox` يحكمُ على كلِّ بابٍ من أبوابِ المُنتِجِ الثلاثةِ (`enqueue_notification` · `open_offer_round` · `create_broadcast`) لِعينِ سببِ `F6-05`: بابٌ محكومٌ وبابٌ لا هوَ فرقٌ لا يُرى إلّا بمن يبحثُ عنه. والصنفُ الحرجُ يمرُّ ولو أُشبِعَ الطابورُ؛ والقابلُ للتأجيلِ — `broadcast_recipient` وحدَه اليومَ — يُؤجَّلُ **وحدةً واحدةً بموعدٍ واحدٍ** (الحكمُ محفوظٌ للمعاملةِ بـ`set_config`)، فحملةٌ تتشظّى في الزمنِ ليست تأجيلاً بل إعادةَ ترتيبٍ للإزعاجِ.

**والحدُّ الغائبُ خرقٌ لا سَعَةٌ.** لا قيمةَ افتراضيّةً في الشيفرةِ ولا في `SQL`: مفتاحٌ يُحذَفُ سهواً يُعيدُ الطابورَ إلى ما قبلَ هذا البندِ، فلو قُرِئَ غيابُه سَعَةً لا نهائيّةً لعادَ العطلُ صامتاً كما كانَ.

**وردُّ منتِجِ تلغرامَ يسبقُ إيصالَه.** `claim_and_enqueue_telegram_update` بخمسةِ وسائطَ (حملٌ زائدٌ توسيعيٌّ؛ والرباعيّةُ باقيةٌ بلا مسٍّ) يفحصُ عمقاً محدوداً **قبلَ** إدراجِ الإيصالِ ويردُّ `shed` فتردُّ البوابةُ `429` بـ`Retry-After`. ولو أُدرِجَ الإيصالُ أوّلاً لاستُهلِكَ رقمُ التحديثِ، ولصارَتْ إعادةُ تلغرامَ لاحقاً مكرَّراً يُهمَلُ — أي إسقاطَ تحديثٍ باسمِ حفظِه.

**والأثرُ مُلخَّصٌ لا مُفصَّلٌ.** `queue_backpressure_events` صفٌّ واحدٌ لكلِّ (مدينةٍ، طابورٍ، سببٍ، دقيقةٍ) و`occurrences` يتزايدُ، لأنَّ جدولَ أثرٍ يُكتَبُ صفّاً لكلِّ خرقٍ ينفجرُ حجماً وقتَ الضغطِ بعينِه فيصيرُ حِمْلاً على نظامٍ مُثقَلٍ.

### عطبٌ سابقٌ عُولِجَ في مصدرِه

`deliverNotificationBatch` كانَ يُحدِّدُ حجمَ شوطِه بـ`maxAttempts` — أي **بسقفِ إعادةِ المحاولةِ لصفٍّ واحدٍ** — لا بسعةِ شوطٍ. عددانِ لا علاقةَ بينَهما، وتساوَيا سهواً. صارَتِ القاعدةُ تُصدِرُ `batch_limit` في مظروفِ التسليمِ والشوطُ يُحدَّدُ به، ومقاييسُ الطابورِ الأربعةُ صارَتْ تعريفاتُها مطابقةً **حرفاً** لدوالِّ الحِمْلِ في القاعدةِ ويُقاطِعُها اختبارُ تكاملٍ.

### حدودُ ما يُقالُ ههنا

الحدودُ الستَّةُ **مبذورةٌ لا مقيسةٌ** (`is_provisional = true`)، ولم يُقَسْ أثرُ حدٍّ على حِمْلٍ إنتاجيٍّ، ولا رقمَ سَعَةٍ يُدَّعى في هذا القسمِ (ADR-0034). وثلاثَ عشرةَ حالةَ تكاملٍ تُقيمُ الدليلَ على اتّفاقِ حكمِ القاعدةِ وحكمِ الشيفرةِ على الحِمْلِ نفسِه، وهيَ تُجرى في وظيفةِ CI ذاتِ القاعدةِ الحقيقيّةِ وحدَها — فلا محرِّكَ PostgreSQL في بيئةِ التنفيذِ التي كُتِبَ فيها هذا القسمُ. **فالدرجةُ «مُنفَّذ · مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**، ولا يُقلَبُ `F6-06` إلى `[x]` قبلَ استيفاءِ `ح-4`.

وأثرُ ردِّ منتِجِ تلغرامَ لا يُكتَبُ في `queue_backpressure_events` لأنَّ الجدولَ لكلِّ مدينةٍ والطابورُ بلا مدينةٍ؛ فأثرُه مقاييسُه وسجلُّ البوابةِ. وسدُّ ذلكَ يقتضي استثناءً ثالثاً من قاعدةِ `city_id` ولم يُفتَحْ. والتفصيلُ في [ADR-0066](adr/0066-queue-backpressure-is-a-declared-limit-per-dimension.md) وشاهدُه في `docs/evidence/architecture/F6-06-20260909.md`.

---

## تصحيحٌ مُضافٌ — 2026-09-09 · `F5-06` / `SCL-008`: الرصةُ متعدّدةُ المثيلاتِ في CI، والحكمُ الأحمرُ الصادقُ، وانحدارُ `F7-07` المكشوفُ

**ما صارَ موجوداً**: رصةُ `docker-compose.f5-06.yml` — ثلاثُ بوّاباتٍ خلفَ HAProxy
يفحصُ `/ready` بنشاطٍ، وعاملانِ، وPostgres 17-PostGIS، وRedis حقيقيٌّ، ومحوّلُ
Upstash REST يُمكّنُ البوّابةَ من Redis **بلا مسِّ مسارِها الإنتاجيِّ** — ووظيفةُ CI
`chaos-multi-instance` تقودُ رحلةً واحدةً عبرَ المثيلاتِ الثلاثةِ ثمَّ تقتلُ
`gateway-2` أثناءَها وتُكمِلُها عبرَ الباقيةِ.

**وما لا يُقالُ**: لا شيءَ من ذلكَ «مُثبَتٌ» ولا حتّى «مُتحقَّقٌ منه» بعدُ. حكمُ CI
للوظيفةِ **أحمرُ في جولتَينِ** (`b40185b` ثمَّ `d87a52e`) عندَ خطوةِ رفعِ الرصةِ،
والوظائفُ الثلاثُ الأخرى خضراءُ في الجولتَينِ — فالعطبُ محصورٌ ولم يُنقَل.
`F5-06` و`SCL-008` باقيانِ `[ ]`.

**والسببُ الجذريُّ الثاني يُقيَّدُ لأنَّه صنفٌ لا حادثةٌ**: قيمةُ بيئةٍ في رصةِ
اختبارٍ (`SUPABASE_URL: http://…`) خالفَت شرطاً في `tryLoadConfig` لا يقبلُ إلّا
`https://`. الشرطُ صحيحٌ ولم يُلمَس؛ المُخالِفُ الرصةُ. **والصنفُ**: بيئةُ رصةٍ
تُكتَبُ بيدٍ ولا يُحاكِمُها الضبطُ إلّا داخلَ حاويةٍ لا تُرفَعُ إلّا في CI — فالعطبُ
يُكتشَفُ بعدَ دقائقَ من البناءِ لا بعدَ ثوانٍ من الكتابةِ. فصارَ
`tests/unit/compose-env-contract.test.ts` يُحاكِمُ كلَّ خدمةِ تطبيقٍ في الرصةِ
بـ`tryLoadConfig` نفسِه — لا بنسخةٍ ثانيةٍ من قواعدِه — ويُلزِمُ كلَّ بوّابةٍ بـ
`healthcheck` كي لا يُقرأَ `up --wait` أخضرَ على نسخةٍ ميّتةٍ.

**واكتشافٌ يُبطِلُ زعماً مُدمَجاً — يُقيَّدُ ولا يُمحى ما قبلَه**: طريقُ الهجراتِ في
CI **رجعَ** إلى حلقةِ `psql` العاريةِ بحلِّ تعارضٍ في عقدةِ الدمجِ `a16bf97`، بعدَ أن
كانَ `F7-07` قد استبدلَه بالمُطبِّقِ الآمنِ. فبقيَ قسمُ `F7-07` في هذا الملفِّ
وADR-0068 يقولانِ ما لا يفعلُه CI مدّةَ ستِّ عقدٍ في `main`. أُعيدَ المُطبِّقُ في
الوظيفتَينِ، وصارَت وظيفةُ الفوضى تُطبِّقُ به على منفذِ الرصةِ 5433، **وصارَ لهذا
القرارِ حاجزٌ ينفّذُه** — `scripts/check-migration-applier.ts` في `package.json`
وفي وظيفةِ `verify` — لأنَّ قراراً يُحرَسُ بالوثيقةِ وحدَها يُنقَضُ بحلِّ تعارضٍ لا
يراهُ مراجعٌ.

**وحدودُ ما يُقالُ ههنا**: لا Docker على جهازِ التنفيذِ، فرفعُ الرصةِ وقتلُ النسخةِ
**لا يُقاسانِ محلّيّاً ألبتّةَ** — حكمُهما CI وحدَه، والأخضرُ المحلّيُّ ليسَ بديلاً
عنه. والمقيسُ محلّيّاً هوَ الحاجزانِ الجديدانِ وحدَهما (`lint`=٠ · `typecheck`=٠ ·
٢٢١٢ حالةَ وحدةٍ ناجحةً · ٠ ساقطةً). وإغلاقُ `F5-06` يحتاجُ نشراً مُداراً متعدّدَ
المثيلاتِ (خطةٌ مدفوعةٌ — قرارُ مالكٍ) أو قرارَ حوكمةٍ صريحاً؛ و`render.yaml` لا
يزالُ `numInstances: 1`. والتفصيلُ في `docs/evidence/distributed/F5-06-20260909.md`.

---

## تصحيحٌ مُضافٌ ثانٍ — 2026-09-09 · `F5-06`: الحاجزُ الذي يمنعُ الرحلةَ، والرفضُ الذي قِيسَ بدلاً منها

**الحقيقةُ التي كشفَها CI**: رصةُ `F5-06` تُرفَعُ فعلاً، **والبوّاباتُ الثلاثُ ترفضُ
الإقلاعَ** برمزِ `SINGLE_INSTANCE_INVARIANT` — رفضاً **يوجبُه** `ADR 0050` §٣-ب لأنَّ
آليةَ توزيعِ الأحداثِ المُقرَّرةَ `in-process` لا تعبرُ حدودَ العمليةِ. فرحلةُ الفوضى
**مُتعذِّرةٌ بنيويّاً لا مُخفِقةٌ**، وما كانَ يُقرأُ «عطباً في CI» هوَ الحاجزُ يعملُ.

**وما فُعِلَ**: قِيسَ **الرفضُ نفسُه** في رصةٍ حقيقيّةٍ من ثلاثِ حاوياتٍ
(`scripts/f5-06-invariant-refusal.ts`): الثلاثُ معاً تخرجُ برمزٍ غيرِ صفريٍّ وبرمزِ
الخرقِ في سجلِّها، والبرهانُ يسقطُ على إقلاعٍ لم يُرفَض · خروجٍ بصفرٍ · **وسقوطٍ
لسببٍ آخرَ**. **وهذا سدُّ فجوةِ صدقٍ**: دَينُ `ADR 0050` §٦ كان مقيساً في عمليةٍ
واحدةٍ بالوحدةِ، فصارَ مقيساً حيثُ يقعُ الخرقُ.

**وما لم يُفعَل ولا يجوزُ**: لم يُلمَس ملفُّ الحكمِ، ولا أُضيفَ تجاوزٌ بمتغيّرِ بيئةٍ،
ولا أُعلِنَت `single-process` مع ثلاثِ نسخٍ (**أخضرُ كاذبٌ**: يقيسُ توازنَ حملٍ لا
صحّةَ حالةٍ موزَّعةٍ)، ولا اختِيرَ محوِّلُ توزيعٍ لتمريرِ وظيفةِ CI.

**ورحلةُ الفوضى محفوظةٌ لا محذوفةٌ ولا مُتخطّاةٌ**: شرطُها يُقرأُ من
`DECIDED_EVENT_DISTRIBUTION` في الشيفرةِ، فتُفتَحُ **تلقائيّاً** يومَ تُقرَّرُ آليةٌ
عابرةٌ للعمليةِ بـADR ناسخٍ — بلا لمسِ ملفٍّ ولا رفعِ رايةٍ.

**والحاجزُ القائمُ قرارُ مالكٍ لا عملٌ فنّيٌّ**: `ADR 0050` §٨ يشترطُ لفتحِ المحوِّلِ
**قرارَ رفعِ عددِ النسخِ أو إغلاقَ `REQ-07`** (`[!]` محجوبٌ)، وسطرُ الخارطةِ ١٥١٧
يمنعُ اختيارَ أيِّ محوِّلٍ قبلَ حسمِ دلالاتِ التسليمِ والترتيبِ ومنعِ التكرارِ
والتعافي على جدولِ `ADR 0050` §٢. **فـ`F5-06`/`SCL-008`/`R-17` مفتوحونَ**،
و`numInstances` لا يزالُ `1`، **وقتلُ نسخةٍ أثناءَ رحلةٍ جاريةٍ لم يُقَس قطُّ**.
التفصيلُ في `docs/adr/0070-f5-06-ci-measures-refusal-until-a-distribution-mechanism-is-decided.md`.

---

## قرارُ مالكٍ مُضافٌ — 2026-09-09 · `DEC-14`: النسخةُ الواحدةُ تبقى، و`F5-06` مُؤجَّلٌ صراحةً

**قرَّرَ مديرُ المشروعِ الخيارَ (٢)**: `numInstances: 1` يبقى، و`F5-06` مُؤجَّلٌ **صراحةً**،
و`R-17` مفتوحٌ — **ولا محوِّلَ توزيعٍ يُختارُ قبلَ قياسِ دلالاتِ التسليمِ والترتيبِ ومنعِ
التكرارِ والتعافي**، **والحاجزُ السياديُّ يبقى كما هو بدلاً من التحايلِ عليه أو إعلانِ
طوبولوجيا غيرِ صحيحةٍ**.

**فالحالةُ الرسميّةُ**: `F5-06` `[ ]` · `SCL-008` `[ ]` · `R-17` `[ ]` · `F7-07` `[ ]` ·
`REQ-07` `[!]` غيرُ مُغلَقٍ · `numInstances: 1` · `DECIDED_EVENT_DISTRIBUTION = "in-process"`
لا يُفعِّلُ مسارَ الرحلةِ. **و`F5-06` محجوبٌ بقرارٍ معلَنٍ: لا فاشلٌ ولا منجَزٌ** — وهذا وصفُ
المالكِ نفسِه.

**وما لا يُقرأُ**: لا جولةُ CI الخضراءُ على `5952afd` ولا `ADR 0070` إثباتٌ لرحلةٍ متعدّدةِ
النسخِ، **لأنَّ قتلَ نسخةٍ أثناءَ رحلةٍ جاريةٍ لم يُقَس** (`ح-5`). **وترتيبٌ لاحقٌ نصَّ عليه
المالكُ**: شكلُ الرقمِ المتزايدِ في `TrackingEvent` (`ADR 0050` §٨-ج) خطوةٌ سابقةٌ محتملةٌ قبلَ
اختيارِ القناةِ. **والإنفاذُ آليٌّ لا وصفيٌّ**: `tests/unit/f5-06-invariant-refusal.test.ts`
§«قرارُ المالكِ `DEC-14`» يُسقِطُ البناءَ على أيِّ نسخةٍ ثانيةٍ في `render.yaml` أو آليةٍ ثانيةٍ
في `EVENT_DISTRIBUTION_MECHANISMS` أو بوّابةِ رحلةٍ تُفتَحُ بلا قرارٍ. المرجعُ:
`docs/decisions/DEC-14-single-instance-deferral-20260909.md`.
## تصحيحٌ مُضافٌ — 2026-09-09 · `F6-07`: للطابورِ صارَ ترتيبٌ لا أقدميّةٌ وحدَها، ولدلوِ المعدّلِ نصيبٌ محجوزٌ للحرجِ

**ما كانَ مكتوباً في هذه الوثيقةِ قبلَ اليومِ صحيحٌ ولم يُمَسَّ حرفاً.** وهذا القسمُ إضافةٌ عليه (`ح-8`).

بعدَ `F6-06` صارَ لكلِّ طابورٍ حدٌّ مُعلَنٌ في كلِّ بعدٍ. **والحدُّ يقولُ متى نتوقّفُ، ولا يقولُ من يمرُّ أوّلاً.** وكانَ `claim_notification_delivery` يُرتِّبُ بـ`order by n.created_at` وحدَها: فدُفعةٌ من مئتَي `broadcast_recipient` أُودِعَت في الثانيةِ الأولى تتقدّمُ — **بحكمِ أقدميّةٍ صحيحٍ لا بعطبِ تنفيذٍ** — على `safety_incident` أُودِعَ في الثانيةِ التاليةِ. وكانَ دلوُ المعدّلِ الصادرُ يُشبَعُ فيَمنعُ **كلَّ** طالبٍ بالتساوي: حملةُ بثٍّ تُشبِعُ الدلوَ العامَّ فتمنعُ معَها عرضَ رحلةٍ وبلاغَ استغاثةٍ، لا لأنَّ حدَّ تلغرام خُرِقَ بسببِهما بل لأنَّهما وصلَا بعدَ من أشبعَ. والقسمُ 15 من الخارطةِ يقولُ خلافَ ذلكَ نصّاً.

### ما صارَ قائماً بعدَ `20260909150000` و`20260909151000`

**الرتبةُ خصيصةُ النوعِ في موضعٍ واحدٍ.** `packages/shared/config/traffic-priority.ts` سجلٌّ **مغلقٌ** يُسنِدُ لكلِّ نوعٍ من الأحدَ عشرَ صنفاً من أربعةٍ (`critical` · `high` · `medium` · `low`) برتبٍ `1..4`. ولا رتبةَ في مسارِ نداءٍ ولا وسيطاً يُمرَّرُ: وسيطٌ يُنسى في بابٍ من أبوابِ المُنتِجِ يجعلُ النوعَ الواحدَ رتبتَينِ بحسبِ من أودعَه — وهوَ فرقٌ لا يُرى.

**والمجهولُ يُقرأُ حرجاً لا منخفضاً**، في الطرفَينِ: `priorityClassOfKind` تردُّ `critical` لِما ليسَ في السجلِّ، و`notification_kind_priority` تردُّ `1` في `else`. **والسببُ عدمُ تماثلٍ في الظهورِ**: رتبةٌ منخفضةٌ خطأً يُشتَكى منها فتُصحَّحُ، ورتبةٌ مرتفعةٌ خطأً — أو نوعٌ بلا رتبةٍ — لا خطأَ يُرفَعُ لها ولا أثرَ يُكتَبُ، فقط بلاغٌ حرجٌ يقفُ في صفٍّ ولا أحدَ يقيسُ وقوفَه.

**والترتيبُ في المُطالِبِ داخلَ المحرِّكِ لا في العامِلِ.** `claim_notification_delivery` أُعيدَ تعريفُها **بالتوقيعِ نفسِه** والفرقُ سطرُ الترتيبِ وحدَه: `order by notification_kind_priority(n.kind), n.created_at`. ولماذا في المحرِّكِ؟ لأنَّ `for update skip locked` يختارُ الصفَّ **قبلَ** أن يرى العامِلُ شيئاً، فترتيبٌ في العامِلِ يُرتِّبُ ما التُقِطَ لا ما يُلتقَطُ — ترتيبُ نافذةٍ لا ترتيبُ طابورٍ؛ ولأنَّ نُسَخَ العامِلِ عدّةٌ وحكمُ المحرِّكِ واحدٌ عبرَها. **والأقدميّةُ تبقى حاكماً داخلَ الرتبةِ الواحدةِ** فلا جوعَ داخلَ الصنفِ، وسلوكُ ما قبلَ البندِ محفوظٌ حرفاً بينَ صفَّينِ من رتبةٍ واحدةٍ. وللترتيبِ فهرسٌ جزئيٌّ متزامنٌ (`notification_outbox_priority_due_idx`) في ملفٍّ منفصلٍ بطورِ `index`.

**وقائمةُ التأجيلِ صارت مُشتقّةً لا مكتوبةً مرّتَينِ.** `notification_kind_is_deferrable` = `priority >= 3`، والقائمةُ في `packages/shared/config/queue-backpressure.ts` تُشتَقُّ من السجلِّ. **وأثرُه اتّساعٌ مُعلَنٌ**: من نوعٍ واحدٍ (`broadcast_recipient`) إلى أربعةٍ (بإضافةِ `no_driver_found` · `dispute_resolution` · `subscription_notice`) — أي إنَّ هذه الثلاثةَ صارت تُؤجَّلُ تحتَ الضغطِ العكسيِّ ولم تكن تُؤجَّلُ.

**ودلوُ المعدّلِ يحجزُ نصيباً للحرجِ في نطاقِ `global` وحدَه.** الطالبُ القابلُ للتأجيلِ يرى حدّاً مضروباً في `lowPriorityShare` (افتراضُه `0.8`) فيُمنَعُ عندَ أربعةِ أخماسِ السَّعَةِ ويبقى الخُمسُ للحرجِ. **ولا حجزَ في نطاقِ المحادثةِ**: حدُّ تلغرام لكلِّ محادثةٍ حدٌّ على تلكَ المحادثةِ، وحجزٌ فيه يُنقِصُ سَعَتَها بلا مقابلٍ لاستغاثةٍ في محادثةٍ أخرى. **والحدُّ المضروبُ لا يبلغُ صفراً أبداً**: حصّةٌ تُقرأُ صفراً تُحوِّلُ التأجيلَ منعاً دائماً.

**والحاجزُ يُطابِقُ الطرفَينِ في CI.** `scripts/check-traffic-priority.ts` بستِّ قواعدَ: نوعٌ بلا رتبةٍ في أحدِ الطرفَينِ · `else` غيرُ حرجٍ · مُطالِبٌ لا يقرأُ الرتبةَ قبلَ الأقدميّةِ · ترتيبٌ بلا فهرسٍ. ولا نسخةَ ثالثةَ للحقيقةِ: الحاجزُ يقرأُ الطرفَينِ ولا يُعلِنُ ثالثاً.

### إغفالٌ سابقٌ عُولِجَ بالإضافةِ لا بالمحوِ

**حاجزُ `F6-06` (`scripts/check-queue-backpressure.ts`) كانَ مكتوباً ولا يُشغَّلُ في أيّةِ وظيفةٍ** — لا في سلسلةِ `bun run ci` ولا في `.github/workflows/ci.yml`. **فلم يكن حاجزاً بل ملفّاً يُقرأُ**، وسطرُ سجلِّ `F6-06` الذي يقولُ «وخطوةٌ مُسمّاةٌ في CI» يُصحَّحُ بهذا السطرِ لا بمحوِه. وقد وُصِلَ الاثنانِ ههنا بسلسلةِ `ci` وبوظيفةِ `verify`، والإغفالُ مُعلَنٌ في تعليقِ الخطوةِ نفسِها.

**وأثرُ اتّساعِ قائمةِ التأجيلِ على اختبارِ `F6-06`**: `tests/integration/queue-backpressure.test.ts` كانَ يستعملُ `no_driver_found` كـ«حِمْلٍ حرجٍ»، وقد صارَ قابلاً للتأجيلِ — فمقدّمةُ الحالةِ لم تبقَ صحيحةً. فبُدِّلَ النوعُ إلى `negotiation_turn_opened` (حرجٌ) **بتعليقِ تصحيحٍ مُضافٍ**، ولم تُحذَفْ حالةٌ ولا خُفِّفَ حكمٌ.

### حدودُ ما يُقالُ ههنا

**الجوعُ حدٌّ مُعلَنٌ لا مسألةٌ محلولةٌ.** الترتيبُ رتبيٌّ صرفٌ بلا حصّةٍ زمنيّةٍ في المُطالِبِ. وما يمنعُ الجوعَ اليومَ ثلاثةُ أمورٍ بنيويّةٍ: أنَّ الأنواعَ الحرجةَ حدثيّةٌ قليلةُ الحجمِ بطبعِها بخلافِ البثِّ الدُّفعيِّ، وأنَّ `broadcast_recipient` **ليسَ في مدى هذا المُطالِبِ أصلاً** (ADR-0061: ثمانيةُ أنواعِ دورةِ الرحلةِ وحدَها، وللبثِّ مُطالِبٌ آخرُ)، وأنَّ سقفَ تزامنِ المستهلِكِ من `F6-06` يجعلُ الشوطَ محدوداً. **ويومَ يدخلُ نوعٌ منخفضٌ دُفعيٌّ مدى هذا المُطالِبِ تُعادُ المسألةُ بحصّةٍ مقيسةٍ** (ADR-0071 §٥).

**والطابورُ الوارِدُ بلا رتبةٍ** (`telegram_update_jobs`) ولا يُدَّعى غيرُ ذلك: رتبةُ التحديثِ الوارِدِ غيرُ معروفةٍ وقتَ الالتقاطِ لأنَّ تصنيفَه يقتضي تنفيذَ الحوارِ نفسِه، وما يحميه اليومَ حدُّ المُنتِجِ من `F6-06` (ADR-0071 §٧).

**وخطّةُ تنفيذِ المُخطِّطِ لم تُقَسْ** على حجمٍ إنتاجيٍّ: المُثبَتُ **وجودُ** الفهرسِ و**صحّةُ** الترتيبِ، لا أنَّ المُخطِّطَ اختارَه — دَينُ قياسٍ من صنفِ ADR-0069. **وأثرُ الحصّةِ على تسليمٍ حقيقيٍّ عبرَ Bot API لم يُقَسْ** فالمُرسِلُ في الاختبارِ مزدوجٌ.

**ولا محرِّكَ PostgreSQL في بيئةِ التنفيذِ، والقاعدةُ الاختباريّةُ المتاحةُ متأخّرةٌ عن `main`** (لا `F6-05` ولا `F6-06` ولا فهارسُ `F7-02` عليها) **ولم تُهاجَرْ عن قصدٍ**: طرفُ سلسلتِها المُطبَّقُ غيرُ معلومٍ والهجراتُ الموروثةُ غيرُ مُسترجَعةٍ (دَينٌ مُعلَنٌ في `scripts/lib/migration-baseline.ts`)، فتطبيقُ ذيلٍ مجهولِ المبدأِ عليها يُفسِدُها ولا يُنتِجُ حكماً أصدقَ من وظيفةِ CI التي تُطبِّقُ السلسلةَ كلَّها على قاعدةٍ فارغةٍ بالمُطبِّقِ الآمنِ (`F7-07`). **فحكمُ الحالاتِ الستِّ التكامليّةِ من CI وحدَها. والدرجةُ «مُنفَّذ · مُختبَر»، و`F6-07` يبقى `[ ]`، والدمجُ لمراجعٍ مستقلٍّ (0.7).**
## تصحيحٌ مُضافٌ — 2026-09-09 · `F4-02` / `CAP-009`: موقعُ السائقِ صارَ له موضعُ استقرارٍ ساخنٌ، والقاعدةُ خرجَت من مسارِ كلِّ نبضةٍ

**ما كانَ مكتوباً في هذه الوثيقةِ قبلَ اليومِ صحيحٌ ولم يُمَسَّ حرفاً.** وهذا القسمُ إضافةٌ تُقيِّدُ فرضيّةً كانت قائمةً بالسكوتِ منذُ `F4-01`: أنَّ كتابةَ الموضعِ في القاعدةِ لكلِّ نبضةٍ وضعٌ يحتملُ التوسُّعَ. ولم تكنِ الفرضيّةُ مسكوتاً عنها في ADR-0067: عواقبُه تُعلِنُ صريحاً «هذا البندُ لا يدَّعي احتمالَ حملِ الذروةِ … وكلُّ نبضةٍ اليومَ كتابةٌ في القاعدةِ». وهذا القسمُ يُغلِقُ ذلكَ الحدَّ لا يُصحِّحُ خطأً.

**العطبُ كما هوَ.** صفٌّ واحدٌ لكلِّ سائقٍ في `drivers` يُكتَبُ كلَّ نبضةٍ، وهوَ الصفُّ نفسُه الذي يقرأُه المُرسِلُ في ترشيحِ القريبينَ وتقرأُه الخريطةُ. والقسم 6 يُقدِّرُ ٤٦٠٠٠ كتابةَ موقعٍ في الثانيةِ عندَ مليونَي مستخدمٍ — والعائقُ `CAP-009` مُسمّىً في القسم 11 لهذا بعينِه.

### ما صارَ قائماً بعدَ `20260909140000`

**حالةٌ ساخنةٌ في Redis، وحَكَمُ التسلسلِ فيها هوَ حَكَمُ القاعدةِ نفسُه حرفاً.** المُسنَدُ في سكربتِ Lua `newest > incoming ⇒ stale`، وفي دالّةِ القاعدةِ `last_location_recorded_at is null or last_location_recorded_at <= recorded_at` — وهما نفيُ بعضِهما تماماً، والمتساويُ يمرُّ في المخزنَينِ كما يُلزِمُ `ADR 0053 §٦`. **والتماثلُ محروسٌ آليّاً لا موصىً به**: `scripts/check-hot-location-state.ts` يقرأُ نصَّ السكربتِ ونصَّ الدالّةِ ونصَّ الكتابةِ المباشرةِ فيُسقِطُ البناءَ إن افترقا أو انقلبَ اتّجاهُ أحدِهما، ويُشغَّلُ في `bun run ci` وفي وظيفةِ `verify`.

**وأرضيّةُ الحكمِ أثرانِ لا أثرٌ واحدٌ.** كلُّ كتابةٍ ساخنةٍ تحملُ طابعَ الصفِّ المخزَّنِ (`previousRecordedAtMs`)، ويأخذُ السكربتُ الأكبرَ منه ومن المخزَّنِ ساخناً. وبلا ذلكَ كانَ انتهاءُ عمرِ مفتاحٍ يفتحُ نافذةً تُقبَلُ فيها إصلاحةٌ **أقدمُ** من الصفِّ فتُبَثُّ للراكبِ ثمَّ يرفضُها الاستمرارُ — قبولٌ ظاهرٌ ورفضٌ باطنٌ.

**والانتقالُ يُكتَبُ فوراً والنبضةُ تُجمَّعُ.** أوّلُ إصلاحةٍ لسائقٍ لا موقعَ له تُكتَبُ بكتابةِ `F4-01` الشرطيّةِ نفسِها، لأنَّ ظهورَه على الخريطةِ حدثُ حالةٍ يقرأُه المُرسِلُ فوراً؛ والنبضاتُ بعدَها تُؤجَّلُ. والمُخرَجُ صارَ يُعلِنُ **أينَ استقرَّ** الموضعُ (`persistence: "direct" | "deferred"`) فلا يُقرأُ التأجيلُ نجاحَ كتابةٍ.

**وثلاثةُ مفاتيحَ في Redis وسكربتٌ ذرّيٌّ لكلِّ عمليّةٍ مركَّبةٍ.** تجزيئةٌ ساخنةٌ لكلِّ سائقٍ (مرجعُ الحارسِ، عمرُها من الإعداداتِ)، ومجموعةٌ مُرتَّبةٌ للمدينةِ **عضوٌ واحدٌ لكلِّ سائقٍ** ودرجتُه أحدثُ طابعٍ — وهوَ التجميعُ نفسُه لا صفٌّ لكلِّ نبضةٍ — وتجزيئةُ حِمْلٍ تجعلُ السحبَ رحلةً واحدةً بمفاتيحَ مُعلَنةٍ كلِّها في `KEYS`. ولا أمرَ Redis مفرداً في المحوّلِ: «اقرأِ الأحدثَ ثمَّ اكتبْ إن كانَ أحدثَ» أمرانِ، ونسختانِ تُنفِّذانِهما متزاحمتَينِ تكتبانِ الأقدمَ آخراً — وهوَ `BUG-001` في مخزنٍ جديدٍ. والحارسُ يمنعُ الأمرَ المفردَ نصّاً.

**والإفراغُ نداءٌ ذرّيٌّ واحدٌ يأخذُ الدفعةَ.** `persist_driver_location_batch(uuid, jsonb)` تُنقّي الدفعةَ بـ`distinct on (driver_id) … order by recorded_at_ms desc` **في القاعدةِ** لا في الشيفرةِ، وتُحدِّثُ بمُسنَدِ الحارسِ، وتُقيِّدُ بالمدينةِ المُمرَّرةِ (`d.city_id = p_city_id`)، وتُرجِعُ `{applied, stale, missing}` — ثلاثةَ أصنافٍ لا عدداً واحداً، فيُميَّزُ الإفراغُ السويُّ من المعطوبِ. ودورةُ الإفراغِ مهمّةٌ لكلِّ مدينةٍ (`flush-driver-locations:<city>`) دَورتُها من الإعداداتِ، و**ليست في `CRITICAL_CITY_JOBS`**: غيابُها يُؤخِّرُ موضعاً ولا يُفسِدُ حالةً، وإدخالُها كانَ سيُسقِطُ `/ready` لمدينةٍ إعدادُها ناقصٌ.

**وموتُ Redis تدهوّرٌ مُعلَنٌ لا انقطاعٌ.** كلُّ عطلٍ في المسارِ الساخنِ يُسجَّلُ (`driver_location.hot_state_failed` · `hot_state_degraded`) ثمَّ **تُكتَبُ الإصلاحةُ مباشرةً بطريقِ `F4-01` المُثبَتِ**. فالنظامُ بلا Redis يعملُ كما كانَ قبلَ هذا البندِ حرفاً، ولا يُرَدُّ خطأٌ إلى سائقٍ لسببٍ في مخزنٍ ثانويٍّ.

**والأرقامُ الأربعةُ في `platform_settings` لكلِّ مدينةٍ** (`driver_location_hot_ttl_seconds` · `driver_location_flush_interval_seconds` · `driver_location_flush_batch_size` · `driver_location_backlog_limit`) **كلُّها `is_provisional = true`**، ولا قيمةَ افتراضيّةً في الشيفرةِ: مفتاحٌ غائبٌ أو غيرُ موجبٍ يُقرأُ **خرقاً مُسمّىً** فيُعطَّلُ المسارُ الساخنُ لتلكَ المدينةِ ولا تُسجَّلُ مهمّةُ إفراغِها ويُسجَّلُ الخرقُ (`driver_location.flush_not_registered`). ونمطُ `?? <رقم>` في وحدةِ الحدودِ ممنوعٌ بالحارسِ نصّاً (القاعدة 0.4).

### حدودُ ما يُقالُ ههنا

**الموضعُ القانونيُّ صارَ متأخّراً بمقدارِ دورةِ إفراغٍ** — رقمُها إعدادٌ يُضبَطُ لا ثابتٌ — ولذلكَ يُكتَبُ الانتقالُ فوراً. **ونبضاتٌ لم تُفرَغْ تُفقَدُ** بموتِ Redis أو بانتهاءِ عمرِ مفتاحٍ قبلَ الإفراغِ: المفقودُ نبضةٌ لا حالةٌ — الصفُّ يبقى على آخرِ موضعٍ استقرَّ والنبضةُ التاليةُ تُصحِّحُه — وهذا حدٌّ لا حلٌّ، ومَن أرادَ ضمانَ كلِّ نبضةٍ فطريقُه سجلُّ أثرٍ لا هذا البندُ. وتغييرُ إعدادٍ يسري خلالَ عمرِ حالةٍ ساخنةٍ واحدةٍ لا فوراً. والعنقودُ (`Redis cluster`) غيرُ مدعومٍ اليومَ.

**ولا رقمَ سَعَةٍ يُدَّعى في هذا القسمِ** (ADR-0034): لم يُقَسْ حملٌ، ولا كتابةٌ في الثانيةِ، ولا زمنُ استجابةٍ، ولا فرقٌ بينَ قبلَ البندِ وبعدَه. المُثبَتُ سلوكٌ: أنَّ الأقدمَ يُرفَضُ في الخادمِ نفسِه، وأنَّ خمسَ نبضاتٍ لسائقٍ تُبقي عضواً واحداً، وأنَّ نسختَينِ مستقلّتَينِ تَرَيانِ حالةً واحدةً، وأنَّ دفعةً مقلوبةَ الترتيبِ تُطبِّقُ الأحدثَ وحدَه. وذلكَ يُقاسُ على Redis حقيقيٍّ وPostgreSQL حقيقيّةٍ في وظيفتَي CI وحدَهما — فلا محرِّكَ منهما في بيئةِ التنفيذِ التي كُتِبَ فيها هذا القسمُ. **فالدرجةُ «مُنفَّذ · مُختبَر» ولا يُقالُ «مَقيس» ولا «مُثبَت» (`ح-5`)**، ولا يُقلَبُ `F4-02` إلى `[x]` قبلَ استيفاءِ `ح-4`. والتفصيلُ في [ADR-0072](adr/0072-hot-driver-location-state-and-batched-persistence.md) وشاهدُه في `docs/evidence/architecture/F4-02-20260909.md`.
| **تصحيحٌ مُلحَق التاسع والأربعون — 2026-09-10 (وحدةُ `F7-04` / `CAP-004` — ميزانيّةُ الاتّصالاتِ محسوبةٌ ومحروسةٌ، لا قلبَ حالةٍ)** | **`F7-04` و`CAP-004` باقيانِ `[ ]` ولم يُقلَبْ عمودُ حالةٍ ألبتّةَ (`ح-4`)**، ولم يُمَسَّ نصُّ بندٍ (`ح-1`)، ولم يُحرَّرْ ADR منشورٌ (`ح-6`) — **`ADR 0073` جديدٌ يُكمِّلُ `ADR 0056` ولا يُعدِّلُه**، ويُسجِّلُ تقادُمَ §٣-ج بالإضافةِ لا بالمحوِ (`ح-8`). **والمُصحَّحُ ههنا رقمٌ سبقَ أن ذُكِرَ في هذه الوثيقةِ وفي `ADR 0056`**: صيغةُ ميزانيّةِ الاتّصالاتِ `5N + 10M` **لم تعد تصفُ المنظومةَ**، وسببُها ثلاثةٌ مقروءةٌ من الشيفرةِ لا مُستنتَجةٌ: **(أ)** خدمةُ اللوحةِ فُصِلَت في `F5-08`/`ARCH-011` (`ADR 0064`) بتجمُّعٍ خاصٍّ (`ADMIN_DB_MAX_CONNECTIONS = 5`) **بعدَ** كتابةِ الصيغةِ، فالمُعلَنُ ١٥ والواقعُ **٢٠** عندَ `N=M=1`؛ **(ب)** `RUN_WORKER_IN_GATEWAY` **لا تُقرأُ في الصيغةِ ألبتّةَ** مع أنّها تبني حاويةَ عاملٍ كاملةً داخلَ عمليةِ البوّابةِ بتجمُّعَيها، فتحملُ البوّابةُ ١٥ اتّصالاً لا ٥ والمجموعُ **٣٠** — وهوَ طريقُ التراجعِ الطارئِ الذي أبقاهُ `ADR 0063` مشروعاً عن قصدٍ، أيْ أنّ الصيغةَ كانت تكذبُ في اللحظةِ التي تُستشارُ فيها فعلاً؛ **(ج)** الرقمُ «خمسةٌ» مكتوباً في **أربعةِ مواضعَ** بلا رابطٍ آليٍّ. **والمُنفَّذُ**: مصدرُ حقيقةٍ واحدٌ `packages/shared/config/connection-budget.ts` (`JOB_CONCURRENCY` · `DB_POOL_ROLES` قائمةً مغلقةً · `DB_POOL_MAX` مُجمَّدةً · `DEFAULT_DB_POOL_MAX = DB_POOL_MAX.gatewayRequest` · `TRANSIENT_POOL_ROLES` · `DECLARED_TOPOLOGY` · `computeConnectionBudget` · `describeConnectionBudget`)، **والصيغةُ صارت مُخرَجاً محسوباً**: `5×gateway(1) + 10×worker(1) + 5×admin(1) = 20` في الحالةِ المستقرّةِ **(+1 عابرٌ** عندَ التحقُّقِ من الاستعادةِ، لا يُجمَعُ في المستقرِّ لأنّه يُفتَحُ ويُغلَقُ)، و**30** إن صارَ `RUN_WORKER_IN_GATEWAY=true`. ومواضعُ الإنشاءِ الستّةُ كلُّها موصولةٌ بالنموذجِ، و`MAX_JOB_CONCURRENCY` يُعادُ تصديرُه بلا تغيُّرِ قيمةٍ ولا اسمٍ عندَ مُستدعٍ واحدٍ، وسطرُ إقلاعٍ `connection_budget.declared` يُحسَبُ من `runWorkerInGateway` **الفعليّةِ** لا المُعلَنةِ. **والإنفاذُ آليٌّ**: `scripts/check-connection-budget.ts` في سلسلةِ `bun run ci` (صارت **٢٦** حاجزاً) وخطوةً مُسمّاةً في وظيفةِ `verify`، بثلاثةَ عشرَ رمزَ خرقٍ في قائمةٍ مغلقةٍ — ومنها الاشتقاقُ عندَ المُستدعي (`DB_POOL_MAX.workerJobs + 1` مرفوضٌ كما يُرفَضُ `6`) ودورٌ مُعلَنٌ **بلا مُستدعٍ** (القاعدةُ السياديّةُ 0.1) وتنافرُ `DECLARED_TOPOLOGY` مع `render.yaml` في `numInstances` و`RUN_WORKER_IN_GATEWAY` في الاتّجاهَينِ. **والحاجزُ أثبتَ نفسَه أوّلَ تشغيلٍ فكشفَ خرقَينِ حقيقيَّينِ في العملِ نفسِه** — ثابتُ اللوحةِ الوسيطُ يُخفي مصدرَ السقفِ، ودورُ الاستعادةِ مُعلَناً بلا مُستدعٍ — **فأُصلِحَ الموضعانِ ولم يُخفَّفِ الحاجزُ**. **ولا قيمةَ واحدةً تبدَّلَت ولا سلوكَ إنتاجٍ تغيّرَ**: `5 · 5 · 5 · 5 · 1` قبلَ العملِ وبعدَه حرفاً؛ البندُ يجعلُ الرقمَ **معلوماً محروساً**، وتغييرُه قرارُ سعةٍ يملكُه المالكُ. **وحدودٌ مُعلَنةٌ لا مُخفَّفةٌ**: **المحسوبُ الطلبُ لا العرضُ** — لا يُقالُ إنّ العشرينَ تسعُ خطّةَ القاعدةِ، **ولم يُقارَنِ المجموعُ بسقفِ المزوِّدِ عن قصدٍ** لأنّه مُدخَلٌ من خارجِ المستودعِ وحاجزٌ أخضرُ على رقمٍ مُخترَعٍ يُطمئِنُ زوراً (`ح-5`) · **ولا قياسَ إنتاجيَّ**: لم يُقرأْ `pg_stat_activity` تحتَ حملٍ حقيقيٍّ (بيئةٌ غيرُ متاحةٍ، سُجِّلَت حدّاً) · والحاجزُ **يقرأُ النصَّ لا شجرةَ تحليلٍ** (مطابقةٌ على أقواسٍ متوازنةٍ من `createSql(` على نهجِ `check-instance-invariant.ts`)، فتجمُّعٌ يُفتَحُ بواجهةٍ أخرى لا يراهُ — ولذلكَ `DB_POOL_ROLES` قائمةٌ مغلقةٌ يسقطُ الحاجزُ على دورٍ فيها بلا مُستدعٍ · و`tests/` و`scripts/` و`bench/` خارجَ الحكمِ عن قصدٍ. محلّيّاً: `lint`=**0** (١٠١٨ ملفّاً) · `typecheck`=**0** · `build:miniapp`=**0** · الحاجزُ الجديدُ **0** (٦ مواضعِ إنشاءٍ) · الحواجزُ الخمسةُ والعشرونَ الأخرى **0** · `bun test` **٢٥٠٥ ناجحةً · ٧٠٧ مُتخطّاةً · ٠ ساقطةً · ٩١١٢ توكيداً** في ٢٤٦ ملفّاً؛ **و`bash scripts/verify-container-build.sh` لم يُشغَّلْ محلّيّاً** (bun ههنا 1.4.2 والصورةُ 1.3.14 — قيدُ بيئةٍ سابقٌ لهذا البندِ) **والحكمُ حكمُ CI وحدَه**. الشاهدُ: `docs/evidence/architecture/F7-04-20260910.md` · القرارُ: `docs/adr/0073-connection-budget-is-computed-not-narrated.md` · وسطرٌ في سجلِّ القسم 25. **والدمجُ لمراجعٍ مستقلٍّ (0.7).** |

---

## تصحيحٌ مُضافٌ — 2026-09-10 · قلبُ حالةِ `F7-02`/`CAP-005` و`F6-07` و`F4-02` إلى `[x]` بعدَ استيفاءِ `ح-4`

**ما تغيَّرَ في هذه العقدةِ عمودُ حالةٍ لا شيفرةٌ.** الثلاثةُ كانت `[ ]` لسببٍ
واحدٍ مكتوبٍ في أدلَّتِها حرفاً: **لم تكتملْ ثلاثُ جولاتِ `main` خضراءَ متتاليةً
بعدَ الدمجِ**. وقد اكتملَت بعدَ دمجِ الطلباتِ الخمسةِ (`#53`…`#57`) بالترتيب:

| البندُ | في `main` منذُ | جولاتٌ خضراءُ متتاليةٌ | الحالةُ الآنَ |
|---|---|---|---|
| `F7-02` · `CAP-005` | `5c9f2d2` | **٧** | `[x]` |
| `F6-07` | `a321bac` | **٤** | `[x]` |
| `F4-02` | `4d35ecc` | **٣** (الحدُّ الأدنى) | `[x]` |

والجولاتُ الخمسُ الأخيرةُ: `34413270745` · `34413815946` · `34415021164` ·
`34415614065` · `34416170548`، **وحكمُ كلِّ وظيفةٍ من الوظائفِ الأربعِ مقروءٌ من
الواجهةِ البرمجيّةِ (`conclusion`) لا من نصِّ سجلٍّ**.

**وما بقيَ `[ ]` عمداً، وهذا أهمُّ ما في هذا التصحيحِ:**

1. **`CAP-009` يبقى `[ ]`** رغمَ قلبِ `F4-02`. عمودُ حلِّها ثلاثيٌّ: حالةٌ ساخنةٌ
   في Redis (تمَّ) + استمرارٌ غيرُ متزامنٍ مجمَّعٌ (تمَّ) + **جدولُ تاريخٍ
   مقسَّمٌ** (لم يُبدَأْ — نطاقُ `F7-03`). فلا تُقلَبُ قدرةٌ بثُلثَيْ عمودِها.
2. **`F5-06` يبقى `[ ]`** رغمَ أنَّ جولاتِه الخضراءَ صارت خمساً. والمانعُ ليسَ
   `ح-4` بل نصُّ البندِ نفسُه: يشترطُ رصةً بثلاثةِ مثيلاتٍ عاملةٍ، و`DEC-14`
   أبقى `numInstances: 1`. **فالبندُ محجوبٌ بقرارٍ مُعلَنٍ لا ناقصُ دليلٍ**،
   والمُثبَتُ فيهِ أنَّ الحاجزَ يرفضُ الإقلاعَ صاخباً في رصةٍ حقيقيّةٍ.
3. **الدرجةُ لم تُرفَعْ إلى `مَقيس` ولا `مُثبَت` لأيٍّ من الثلاثةِ** (`ح-5`):
   الأخضرُ حكمٌ على السلوكِ تحتَ بوّابةٍ، لا حِملٌ إنتاجيٌّ قِيسَ. وكلُّ حدٍّ
   مكتوبٍ في الأدلَّةِ باقٍ بلا محوٍ، وأُضيفَ إليهِ قسمُ إغلاقٍ (`ح-8`).

---

## تصحيحٌ مُضافٌ — `2026-09-10` · `F4-08`: عقدُ التسلسلِ صارَ محكوماً ببوّابةٍ

**ولا يُمحى شيءٌ ممّا قبلَه؛ هذا سطرُ إضافةٍ (`ح-8`).**

`F4-08` نُفِّذَ وأُختُبِرَ ومرَّ حكمَ CI كاملاً — **وحالتُه تبقى `[ ]`**.

خمسةُ سباقاتٍ صارت مكتوبةً ومحكومةً: أربعةٌ على **PostgreSQL حقيقيٍّ**
(`tests/integration/location-race-conditions.test.ts`) وواحدٌ على **Redis
حقيقيٍّ** (`tests/real-redis/location-hot-state-outage-real.test.ts`)، كلُّها
بتزامنٍ فعليٍّ عبرَ `Promise.all` لا بتسلسلٍ مُقنَّعٍ. وأثرُها الأهمُّ أنَّ عقدَ
`ADR 0053` — الرقمُ خادميٌّ ذرّيٌّ لكلِّ جلسةٍ، والتكرارُ متماثِلٌ، ولا ترتيبَ
كلّيَّ بينَ قناتَينِ — **لم يعد نصّاً في وثيقةٍ بل سلوكاً تسقطُ عليه بوّابةٌ**.

**والسببُ الجذريُّ الذي كشفَه CI، مكتوباً لا ممحوّاً:** الجولةُ `34421891408`
أخفقت في وظيفةِ «تكامل على PostgreSQL حقيقي» على السباقِ الثالثِ وحدَه، بينما
مرَّ الثلاثةُ الأخرى ومرَّ سباقُ Redis. والسببُ أنَّ
`DEFAULT_GPS_POLICY.rejectOlderThanSeconds = 300`، فإصلاحةٌ عمرُها ساعةٌ تُرَدُّ
بـ`TIMESTAMP_TOO_OLD` عندَ آلةِ المجالِ **قبلَ أن تبلغَ الكاتبَ**، فلا جلسةَ
تُفتَحُ لها. **فالإخفاقُ كانَ صوابَ النظامِ لا عيبَه؛ والعيبُ في فرضيةِ
الاختبارِ** التي اختارت انحرافاً خارجَ نافذةِ القبولِ، فصارت تقيسُ رفضَ القِدَمِ
وهي تزعمُ قياسَ استقلالِ القناتَينِ.

**والعلاجُ كانَ عندَ السببِ وبالإضافةِ:** صارَ الانحرافُ مائتَي ثانيةٍ — داخلَ
النافذةِ — وأُضيفَ **شطرٌ ثانٍ يُثبِتُ الحدَّ نفسَه صراحةً**: نبضةٌ عمرُها ساعةٌ
تُرَدُّ فلا رقمَ جديدٌ ولا حدثَ منشورٌ. **بلا تخفيفِ توكيدٍ، ولا رفعِ سقفٍ، ولا
تعطيلِ بوّابةٍ، ولا تصنيفِ شيءٍ متجاوَزاً.** فما كشفَه CI صارَ توكيداً مكتوباً لا
درساً منسيّاً.

وحكمُ الجولتَينِ `34422481286` · `34422483389` على الالتزامِ `2f8d806`: الوظائفُ
الأربعُ **خضراءُ**، والسباقاتُ الخمسةُ مقروءةٌ فرداً فرداً من سجلِّ الوظيفةِ لا
من ملخَّصٍ.

**وما لا يُدَّعى، وهو أهمُّ ما في هذا السطرِ:**

1. **الحالةُ `[ ]` لا `[x]`.** `ح-4` يشترطُ بعدَ الدمجِ **ثلاثَ جولاتٍ خضراءَ
   متتاليةٍ على `main`** بحكمِ كلِّ وظيفةٍ من الواجهةِ البرمجيّةِ. ولم تقعْ بعدُ.
2. **الدرجةُ تقفُ عندَ `مُتحقَّقٌ منه`** ولا تبلغُ `مَقيس` ولا `مُثبَت` (`ح-5`):
   المقيسُ سلوكٌ بأربعةِ سائقينَ تحتَ بوّابةٍ، لا حِملٌ إنتاجيٌّ.
3. **سباقُ «المثيلَينِ» يُثبِتُ ذرّيّةَ الكاتبِ لا طوبولوجيا متعدِّدةَ
   المثيلاتِ.** التوزيعُ مُؤجَّلٌ بقرارِ المالكِ `DEC-14`، و`F5-06` يبقى `[ ]`
   لذلك — والتحفُّظُ مكتوبٌ في ملفِّ الاختبارِ نفسِه لا في الوثائقِ وحدَها.
4. **`F4-03`…`F4-06` تبقى محجوبةً** بـ`DEC-14` (مُهايِئُ التوزيعِ المُؤجَّلُ)،
   و`F4-07` يعتمدُ على قناةِ الرحلةِ في `F4-04`. فلا بناءَ حولَ محجوبٍ (`ح-7`).

---

## تصحيحٌ مُضافٌ — `F7-03`: أثرُ الموقعِ صارَ سجلَّ إلحاقٍ مقسَّماً (2026-09-10)

**ما تغيّرَ في حالِ النظامِ فعلاً:** قبلَ اليومِ **لم يكن للنظامِ ذاكرةُ موقعٍ
أصلاً**: `drivers.last_location` يُكتَبُ فوقَه في كلِّ نبضةٍ، فما مضى يُمحى في
اللحظةِ التي يصلُ فيها ما بعدَه. صارَ اليومَ في القاعدةِ جدولُ إلحاقٍ
`driver_location_history` مقسَّمٌ يوميّاً بالمدى على `recorded_at` (طابعُ
الجهازِ لا طابعُ الكتابةِ)، يُلحَقُ فيه من **فرعَي الكتابةِ كلَيهما في المعاملةِ
نفسِها** — لا مهمّةَ نسخٍ لاحقةً، فلا نافذةَ فقدٍ بينَ الساخنِ والتاريخيِّ.
والحاكمُ `ADR-0074` كُتِبَ قبلَ الشيفرةِ (`ح-6`).

**والموقعُ الساخنُ لم يُمَسَّ:** `drivers.last_location` يبقى يُكتَبُ فوقَه كما
كانَ. وذلكَ **عينُ الفصلِ** الذي جاءَ البندُ لأجلِه لا نقصٌ فيه: قارئُ «أينَ هوَ
الآنَ» يقرأُ صفّاً واحداً كما كانَ يفعلُ، وقارئُ «أينَ كانَ» يقرأُ قِسمَ يومِه.

**وحكمُ CI أسقطَ سبعةَ أشياءَ — وكلُّها عولجَت عندَ سببِها الجذريِّ:** أهمُّها
حقيقةٌ في المحرّكِ كادَت تمرُّ صامتةً: **تفعيلُ RLS على الأصلِ المُقسَّمِ لا
يسري على قِسمٍ يُقرَأُ باسمِه**، فأربعةَ عشرَ قِسماً كانَت باباً مفتوحاً من خلفِ
بابٍ مُقفَلٍ. ولم يكشفْها حاجزٌ ساكنٌ — كشفَها **اختبارُ سطحِ الصلاحيّاتِ على
قاعدةٍ حقيقيّةٍ**، وهوَ الفرقُ بينَ الأخضرِ المحلّيِّ وحكمِ CI حرفاً.

**وتصحيحٌ يمسُّ `F4-08`:** سباقُ «المثيلَينِ» كانَ يفترضُ أنَّ ستَّ نبضاتٍ
متزامنةٍ تُنتِجُ رقمَينِ فأكثرَ — و**ذلكَ غيرُ محتومٍ**: إنْ سبقَتِ الأحدثُ إلى
الصفِّ ردَّ حارسُ التسلسلِ الخمسَ الباقياتِ، وهوَ سلوكٌ صحيحٌ. فأُضيفَت نبضةٌ
متسلسلةٌ بعدَ السباقِ تُقبَلُ حتماً، **بلا إضعافِ توكيدٍ**: عدمُ التكرارِ
وتطابقُ الأقصى معَ الصفِّ والتزايدُ الصارمُ لكلِّ ناقلٍ تشملُ الجميعَ. وما كُتِبَ
في سطرِ `F4-08` قبلاً يبقى بلا محوٍ (`ح-8`)؛ هذا تصحيحٌ مُضافٌ عليه.

**وما لا يُدَّعى:**

1. **`CAP-010` لم تُستوفَ.** لا احتفاظَ ولا تقليمَ ولا حذفَ بعدَ مدّةٍ؛ ذلكَ
   `F7-06`. والجدولُ اليومَ **ينمو بلا سقفٍ**، وهذا حالٌ مُعلَنٌ لا مسكوتٌ عنه.
2. **لا قياسَ لكلفةِ الإلحاقِ.** لم يُقَسْ زمنُ نبضةٍ قبلَ التغييرِ وبعدَه تحتَ
   حِملٍ إنتاجيٍّ، فلا `مَقيس` في هذا الوجهِ (`ح-5`).
3. **نافذةُ الأقسامِ تنفَدُ إن سكتَ العامِلُ** أسبوعَينِ، فتهبطُ الصفوفُ في
   القِسمِ الافتراضيِّ: لا تُفقَدُ، لكنّها تفقدُ فائدةَ التقسيمِ. والمهمّةُ
   تُسجِّلُ ذلكَ بدرجةِ **خطأٍ** فلا يمرُّ صامتاً.
4. **لا قارئَ تاريخيّاً بُنِيَ.** المكتوبُ سجلٌّ صحيحٌ مفهرسٌ لقارئٍ لم يُكتَبْ.
5. **الحالةُ `[ ]` لا `[x]`، و`CAP-009` كما هيَ.** `ح-4` يشترطُ بعدَ الدمجِ ثلاثَ
   جولاتٍ خضراءَ متتاليةٍ على `main` بحكمِ كلِّ وظيفةٍ من الواجهةِ البرمجيّةِ.

---

## تصحيحٌ مُضافٌ — `F7-04` قُلِبَت `[x]`، وتصويبُ زعمٍ عن `CAP-004` (2026-09-10)

**قلبُ الحالةِ وقعَ اليومَ لا قبلَه، وبسببٍ واحدٍ:** العملُ نفسُه كانَ قد اكتملَ
ودُمِجَ عندَ `9b9af99`، لكنَّ الحالةَ بقيَت `[ ]` انتظاراً لحكمِ `main` لا لعملٍ
ناقصٍ. وقد وقعَت **أربعُ جولاتٍ خضراءَ متتاليةٍ** بعدَ الدمجِ، قُرِئَت وظيفةً
وظيفةً من الواجهةِ البرمجيّةِ: `34416170548` · `34418090108` · `34423493761` ·
`34429840706`. فالقلبُ حكمٌ مقروءٌ لا تقديرٌ.

**وتصحيحٌ يمسُّ ما كتبتُه أنا قبلَ ساعاتٍ:** سطرا التنفيذِ السابقانِ لـ`F7-04`
زعَما أنَّ `CAP-004` باقيةٌ `[ ]`. **وذلكَ خطأٌ**: هيَ `[x]` منذُ الالتزامِ
`a1b2e3d` الأقدمِ، الذي أغلقَها باشتقاقِ `prepare` من عنوانِ الاتّصالِ وبـ
`ADR-0056`. والفرقُ ليسَ لفظيّاً: القدرةُ أُغلِقَت يومَها **بقرارٍ موصوفٍ**،
وما زادَه عملُ اليومِ هوَ أنَّ شطرَها الأوّلَ — نموذجَ الميزانيّةِ — صارَ
**مُنفَذاً آليّاً** بحاجزٍ يُسقِطُ البناءَ، بعدَ أن كانَ صيغةً في نصِّ ADR لا
يقرؤها شيءٌ فتقادَمَت صامتةً حتّى صارَت تقولُ خمسةَ عشرَ والواقعُ عشرونَ.
والسطرانِ يبقيانِ منشورَينِ كما كُتِبا؛ هذا تصحيحٌ مُضافٌ عليهما لا محوٌ لهما
(`ح-8`).

**وما لا يُدَّعى:**

1. **المحسوبُ هوَ الطلبُ لا العرضُ.** لم يُقرَأْ `pg_stat_activity` تحتَ حِملٍ
   إنتاجيٍّ، فلا `مَقيس` ولا `مُثبَت` (`ح-5`).
2. **الحاجزُ لا يقارِنُ المجموعَ بسقفِ المزوِّدِ**، لأنَّ السقفَ مُدخَلٌ من
   خارجِ المستودعِ؛ ورقمٌ مُخترَعٌ لهُ يُنتِجُ أخضرَ يُطمئِنُ زوراً.
3. **`DEC-03` تبقى `[!]`** بيدِ المالكِ (نمطُ الـ`pooler`: معاملاتٌ أم جلسةٌ).
4. **لا قيمةَ سعةٍ واحدةً تبدَّلَت** بهذا البندِ؛ تغييرُها قرارُ مالكٍ.

---

## تصحيحٌ مُضافٌ — `F7-06` محجوبٌ `[!]` بـ`DEC-15`، وحقيقةٌ عن المخزنِ (2026-09-10)

**أوّلاً حقيقةٌ يجبُ أن تُقالَ صريحةً**: هذا النظامُ **لا يحذفُ صفّاً واحداً**، ولم
يفعلْ قطُّ. البحثُ عن `delete from` في الهجراتِ الثلاثِ والتسعينَ وفي
`packages/**` و`apps/**` كلِّها يُعيدُ **صفراً**. وما يُسمّى «تنظيفاً» في العامِلِ
يُبدِّلُ حالةً ولا يمحو. فكلُّ جدولٍ في هذا المخزنِ ملحَقٌ صافٍ ينمو إلى الأبدِ —
والبندُ التاسعُ في §٣ من هذه الوثيقةِ يقولُها منذُ أغسطسَ، والجديدُ أنَّ `F7-03`
أنشأَ **أضخمَ** هذه الجداولِ، فصارَ العائقُ ذا موضعٍ بعدَ أن كانَ معلَّقاً في
الهواءِ.

**ولماذا لم يُنفَّذْ `F7-06` اليومَ:** لا لعجزٍ فنّيٍّ. التقسيمُ الزمنيُّ قائمٌ،
ودالّةُ الصيانةِ قائمةٌ، وفصلُ القِسمِ وإسقاطُه عمليّةٌ لحظيّةٌ لا تمسحُ صفّاً
صفّاً. **الناقصُ رقمٌ لا أملكُه**: عمودُ حلِّ `CAP-010` يشترطُ «سياسةً معتمدةً»،
و`PDPL` يوجبُ الإتلافَ عندَ تحقُّقِ الغرضِ وإعلانَ المددِ **بلا تحديدِ رقمٍ**؛
فتعريفُ «الغرضِ» ومتى «يتحقّقُ» قرارُ متحكّمٍ في البياناتِ بمراجعةٍ قانونيّةٍ.
**وأشدُّ ما في الأمرِ أنَّ الخطأَ ههنا لا رجعةَ فيه**: كلُّ خطأٍ آخرَ في هذا
المستودعِ يُتراجَعُ عنه بهجرةٍ أو بنشرٍ سابقٍ، **وسياسةُ حذفٍ خاطئةٌ تُنتِجُ
خسارةً لا يُعيدُها تراجعٌ** — وهيَ تحذفُ كلَّ ليلةٍ بلا أن يراها أحدٌ حتّى
يُطلَبَ المحذوفُ.

**وتبعيّةٌ اكتُشِفَت في هذا الفحصِ ولم تكنْ مسجَّلةً**: السّجلّاتُ الماليّةُ
حدُّها الأدنى **ستُّ سنواتٍ** مُشتَقٌّ من متطلّباتِ الفوترةِ — فهيَ خارجَ سياسةِ
الحذفِ ألبتّةَ، **ويُشترَطُ حفظُها داخلَ المملكةِ**، فأيُّ «تخزينٍ باردٍ»
يُقترَحُ لا يجوزُ أن يختارَ موضعَه قبلَ حسمِ `F12-15`/`DEC-01`. أيْ أنَّ
`F7-06` محجوبٌ بقرارَينِ لا بقرارٍ واحدٍ إن اختيرَت الأرشفةُ.

**وما رُفِضَ عن قصدٍ**: تنفيذُ استبقاءِ الجداولِ التشغيليّةِ وحدَها
(`job_heartbeats` · `queue_backpressure_events`) — لا بياناتِ شخصيّةٍ فيها ولا
إلزامَ، وكانَ ممكناً اليومَ. ورُفِضَ لأنّه يُنشئُ آلةَ حذفٍ ومصدرَ حقيقةٍ
**يُعادُ فتحُهما وتغييرُ شكلِهما** عندَ وصولِ القرارِ، فيصيرُ للمشروعِ مصدرا
حقيقةٍ للاستبقاءِ بدلَ واحدٍ.

**وما لا يُدَّعى:**

1. **لا رقمَ في ملفِّ القرارِ مَقيسٌ.** حجمُ الصفِّ تقديرٌ تحليليٌّ لا
   `pg_column_size` على قاعدةٍ حقيقيّةٍ، والصفوفُ/اليومَ مشتقّةٌ من فرضيّاتٍ
   حالتُها المعلَنةُ `declared-assumptions-not-measured` (`ح-5`).
2. **القراءةُ النظاميّةُ ليست رأياً قانونيّاً**، بل تضييقٌ لسؤالِ المالكِ.
3. **`CAP-010` تبقى `[ ]`**: شطرُها الأوّلُ في `F7-03`، وشطراها الآخرانِ ههنا.

---

## تصحيحٌ مُضافٌ — `F7-06` والاستبقاءُ (2026-09-10، بعدَ حسمِ `DEC-15`)

مكتوبٌ في هذا الملفِّ قبلَ اليومِ أنَّ النظامَ **لا يُخرِجُ صفّاً قطُّ**: لا `delete from` واحدةً في ٩٣ هجرةً ولا في الشيفرةِ. **وذلكَ باقٍ صحيحاً حرفيّاً بعدَ `F7-06`** — ولا يُمحى (`ح-8`). فما بُنِيَ ليسَ حذفَ صفوفٍ بل **إسقاطُ قِسمٍ كاملٍ بعدَ إخراجِه مُتحقَّقاً منه**، و`driver_location_history` يبقى سجلّاً ملحَقاً كما نصَّ `ADR-0074`.

والمُستجِدُّ ثلاثةُ أمورٍ:

١. **`DEC-15` حُسِمَ بقرارِ مالكٍ**: نافذةٌ ساخنةٌ **١٤ يوماً** لأثرِ الموقعِ · مصيرُ ما يخرجُ **أرشفةٌ خارجَ القاعدةِ لا إتلافٌ** · `audit_log` **بلا حدٍّ** حتّى يُحسَمَ `F12-10`. فارتفعَ الحجبُ `[!]` عن `F7-06` وصارَ `[~]`.

٢. **صارَ للمستودعِ سياسةُ استبقاءٍ مكتوبةٌ ومحروسةٌ**: `packages/shared/config/retention-policy.ts` يُصنِّفُ **الأربعةَ والأربعينَ جدولاً كلَّها**، وحاجزٌ في `bun run ci` يمنعُ جدولاً جديداً من الدخولِ بلا صنفٍ. وأكثرُ الأصنافِ نصُّها «لا إخراجَ آليّاً»، والجدولُ **الوحيدُ** الذي يجوزُ إخراجُ صفوفِه آليّاً هوَ `driver_location_history`.

٣. **وجهةُ الأرشيفِ ليست حدّاً جديداً**: هيَ `BackupStoragePort` نفسُه الذي تعبرُه هذه الصفوفُ بعينِها يوميّاً في النسخِ الاحتياطيِّ منذُ `OPS-002`. فلم يُفتَحْ حدُّ إقامةِ بياناتٍ جديدٌ، و`DEC-01`/`F12-15` يبقيانِ حاكمَينِ للمخزنَينِ معاً حينَ يُحسَمانِ.

**وما لا يُدَّعى**: لا قياسَ على حجمٍ إنتاجيٍّ · لا استرجاعَ آليَّ (الاسترجاعُ يدويٌّ ودَينٌ مُعلَنٌ) · لا امتثالَ نظاميّاً مُدَّعى · و`CAP-010` يبقى `[ ]` وإن صارَ شطراهُ منفَّذَينِ.

## تصحيحٌ مُضافٌ — `F4-03` / `SCL-004`: المجرى المشتركُ كانَ مفتاحاً مكرَّراً ينشقُّ صامتاً (2026-09-10)

مكتوبٌ في هذا الملفِّ قبلَ اليومِ أنَّ `SCL-004` منفَّذٌ: ناقلُ التتبّعِ مُغلَّفٌ بـRedis Streams
(`ADR-0058`) فيرى الحدثُ النسخَ كلَّها. **وذلكَ باقٍ صحيحاً** — الشيفرةُ قائمةٌ وموصولةٌ في
العمليّتَينِ (البوّابةُ واللوحةُ)، ولا يُمحى منه حرفٌ (`ح-8`).

**والمُستجِدُّ أنَّ الفحصَ من الشيفرةِ كشفَ ثغرةً كانت تُبطِلُ ذلكَ صامتاً**: مفتاحُ المجرى
`waslah:tracking:events` كانَ **نصّاً حرفيّاً مكرَّراً** في `apps/gateway/src/container.ts`
و`apps/admin/src/container.ts`. وتعديلُه في واحدةٍ دونَ الأخرى **لا يُخفِقُ ترجمةً ولا
اختباراً ولا حاجزاً**: تُقلعُ العمليّتانِ خضراءَ، ويُسجِّلُ كلٌّ أنَّ المسارَ الموزَّعَ حاضرٌ،
وتنشرُ كلٌّ في مجراها — فلا يرى مشتركو اللوحةِ أحداثَ البوّابةِ أبداً. وهوَ **عينُ فشلِ
ناقلِ العمليةِ المنعزلِ** الذي بُنيَ البندُ لإزالتِه، يعودُ من بابِ الإعدادِ لا البنيةِ،
وصامتاً في قناةٍ حرجةٍ.

فصارَ المفتاحُ **ثابتاً واحداً مُصدَّراً** `TRACKING_EVENT_STREAM_KEY` في وحدةِ الناقلِ
نفسِها، والحاويتانِ تستورِدانِه؛ وصارَ للبندِ **حاجزٌ يُنفِذُه آليّاً**
`scripts/check-tracking-event-carrier.ts` في سلسلةِ `bun run ci` وخطوةً مُسمّاةً في `verify`،
بأربعِ قواعدَ: مصدرُ حقيقةٍ واحدٌ للمفتاحِ (والتعليقاتُ مُستثناةٌ صراحةً) · لا ناقلَ محليّاً
في حاويةٍ بلا غلافٍ موزَّعٍ و`.start()` · الناقلُ في الحاويةِ وحدَها ومرّةً واحدةً (وهوَ ما
كانَ تعليقُ الحاويةِ يوصي به نصّاً منذُ المرحلةِ ٦ فصارَ محروساً) · و`streamKey` هوَ الثابتُ
لا سواه. وغيابُ أيِّ ناقلٍ **خرقٌ لا نجاحٌ** (`ح-5`). ولهُ ١٤ حالةَ اختبارٍ **تسعٌ منها
سالبةٌ** تُشوِّهُ نصّاً واقعيّاً وتُطالِبُ الحاجزَ بخرقٍ.

**ولا قيمةَ تشغيليّةً واحدةً تبدَّلَت**: المفتاحُ نفسُه حرفاً، والاستطلاعُ والتقليمُ بلا مسٍّ.
العملُ يجعلُ المجرى **واحداً محروساً** لا يُغيِّرُ سلوكاً.

**وما لا يُدَّعى**: الحاجزُ يقرأُ النصَّ لا الأثرَ؛ عبورُ الحدثِ بينَ نسختَينِ يُثبِتُه اختبارُ
الوحدةِ واختبارُ Redis الحقيقيِّ في CI لا هوَ. ولا قياسَ تحتَ توزيعٍ حقيقيٍّ (`numInstances: 1`
بقرارِ `DEC-14`، و`F5-06` مُؤجَّلٌ) فلا زمنَ وصولٍ ولا معدَّلَ فقدٍ مقيسٌ — حدٌّ يُسجَّلُ.
وحدودُ `ADR-0058` الثلاثةُ (لا ضمانَ تسليمٍ · مرّةً على الأقلِّ · تقليمٌ مُقنَّنٌ) قائمةٌ بلا
تغييرٍ. و`F4-03` و`SCL-004` **لا تُقلَبُ حالتُهما بهذا العملِ**: `ح-4` تشترطُ حكمَ CI، والحجبُ
القائمُ على مُنفِّذي `main` (§٧ من ملفِّ الدليلِ) يمنعُ جمعَ الجولاتِ الخضراءِ. الدليلُ:
`docs/evidence/architecture/F4-03-20260910.md`.

---

## تصحيحٌ مُضافٌ — `F4-05` / `CAP-009`: `last_location_at` كانَ يدّعي حداثةً لا يملكُها (2026-09-10)

**السابقُ يبقى كما كُتِبَ** (`ح-8`)؛ وهذا يُضافُ إليه.

قسمُ `F4-02` أعلاه صادقٌ في كلِّ ما ذكرَه: الحالةُ الساخنةُ قائمةٌ، والقاعدةُ خرجَت من
مسارِ كلِّ نبضةٍ. **وما لم يُذكَرْ فيه أثرٌ جانبيٌّ لم يُلحَظْ حينَها**: الدالّةُ الذرّيّةُ
الجديدةُ `persist_driver_location_batch` كتبَت `last_location_at = now()`، و`now()` هناكَ
لحظةُ **الإفراغِ** لا لحظةُ **القبولِ**.

والعمودُ مُعرَّفٌ بتعليقٍ صريحٍ في هجرةِ `20260812020000` أنَّه «زمنُ الخادمِ لحظةَ قبولِ
الإصلاحةِ — ‹متى علِمنا›»، مقابلَ `last_location_recorded_at` الذي هوَ طابعُ **الجهازِ**.
فصارَ العمودُ يُبالِغُ في الحداثةِ بمقدارِ دورةِ إفراغٍ كاملةٍ وزمنِ طابورٍ وزمنِ تعافٍ
من تراكمٍ.

**والعطبُ دخلَ بلا سطرٍ خاطئٍ**: `F4-02` لم يمسَّ تعليقَ العمودِ ولا قارئيه ولا سطراً
قائماً — كتبَ دالّةً جديدةً فحسبُ. فلا ترجمةٌ تُخفِقُ ولا اختبارٌ يسقطُ ولا مراجعٌ يرى
تعارضاً؛ التناقضُ بينَ دالّةٍ جديدةٍ وتعليقٍ في هجرةٍ عمرُها شهرٌ.

**والضحيّتانِ**: صفحةُ التتبّعِ العامّةُ `SS-06` تطبعُ القيمةَ للعميلِ نصّاً («آخر تحديث:
…» عبرَ `get_tracking_position.updated_at`)، وحاجزُ `driver_location_max_age_seconds`
يقيسُ منها العمرَ. **واتّجاهُ الخطأِ هوَ الأسوأُ**: المبالغةُ في القِدَمِ تُرى وتُشتَكى؛
والمبالغةُ في الحداثةِ تُطمئِنُ كذباً بلا شاكٍ ولا إنذارٍ.

فصارَت لحظةُ القبولِ تُقيَّدُ في **موضعِ القبولِ** من قراءةِ الساعةِ نفسِها التي قُوِّمَت
بها الإصلاحةُ، وتُحمَلُ عبرَ Redis إلى حِمْلِ الدفعةِ فتكتبُها الدالّةُ. **ورُفِضَ الحلُّ
الأقصرُ** (الكتابةُ من طابعِ الجهازِ) لأنَّه مُدخَلٌ خارجيٌّ يُزوَّرُ ونهيُه صريحٌ في هجرةِ
`20260812040000`. والحاكمُ `ADR-0076`، والإنفاذُ آليٌّ بحاجزِ
`scripts/check-location-acceptance-stamp.ts` بخمسِ قواعدَ في `bun run ci` وخطوةً مُسمّاةً
في `verify`.

**وما لا يُدَّعى**: لا عطبَ وقعَ في الإنتاجِ — حاجزُ العمرِ مُعطَّلٌ (`0`، `is_provisional`)
فضررُه **كامنٌ** يقعُ يومَ يُفعَّلُ، والمُثبَتُ عرضٌ خاطئٌ على `SS-06`. ولا ضبطَ بالمِلِّي:
الخطأُ هبطَ من **دورةِ إفراغٍ** إلى **انحرافِ ساعتَينِ** لا إلى صفرٍ. ونافذةُ تراجعٍ
مُعلَنةٌ: مدخلاتٌ ساخنةٌ كُتِبَت قبلَ النشرِ لا طابعَ قبولٍ فيها، وتبقى قيمتُها كما كانَت
طيلةَ عمرِ TTL ساخنٍ واحدٍ. والحاجزُ يقرأُ النصَّ لا الأثرَ؛ وحالاتُ التكاملِ الثلاثُ
على PostgreSQL حقيقيٍّ واختبارُ Redis الحقيقيِّ **لم تُنفَّذْ محليّاً**. و`F4-05` **لا
تُقلَبُ حالتُها**: `ح-4` تشترطُ حكمَ CI، وحجبُ المُنفِّذين على مستوى الحسابِ قائمٌ.
و`CAP-009` عمودُه ثلاثيٌّ ولهذا البندِ منه شقٌّ واحدٌ. الدليلُ:
`docs/evidence/architecture/F4-05-20260910.md`.

---

## تصحيحٌ مُضافٌ — `F8-05`: مسارُ الاستغاثةِ كانَ معزولاً في تسليمِه لا في استقبالِه (2026-09-10)

**إضافةٌ لا محوٌ** (`ح-8`). ما سبقَ من وصفٍ لمسارِ الاستغاثةِ يبقى صحيحاً **في شقِّ
التسليمِ**: الحادثُ وصفُّ تسليمِه في معاملةٍ واحدةٍ (`ADR-0061`)، وأولويّةٌ حاكمةٌ في
الصندوقِ (`ADR-0071`)، وإخفاقُ تلغرام لا يفقدُ الحادثَ. **والناقصُ هوَ أنَّ ذلكَ لم يكنْ
يشملُ الاستقبالَ**، ومن قرأَ «مسارُ SOS معزولٌ» فَهِمَ الشمولَ.

**والمقروءُ من الشيفرةِ قبلَ هذا العملِ**: السائقُ ينتظرُ `drivers.findByTelegramId`
لكلِّ أمرٍ ومنها `/sos`، ثمَّ يشترطُ وجودَ منفذِ بطاقةِ الرحلةِ وإلّا ردَّ «لم أفهمْ هذه
الرسالةَ»، ثمَّ ينتظرُ `cardOf`. والراكبُ ينتظرُ `riders.findByTelegramId` ثمَّ
`activeOrdersOf` ليعرضَ **قائمةَ اختيارِ رحلةٍ** بأزرارٍ تحملُ مُعرِّفَ الطلبِ.
**وتلكَ القراءاتُ لا تُضيفُ ضماناً**: `trigger_sos` تُثبِتُ الملكيّةَ أصلاً تحتَ
`for update`، فكانت تحصيلَ مُعرِّفٍ لا حاجزَ أمنٍ.

**وأثرُها ثلاثةٌ**: إخفاقُ قراءةٍ يردُّ «حدثَ عطلٌ» **بلا حادثٍ ولا صفٍّ** — استغاثةٌ
تُصمَتُ بعطبٍ في قراءةٍ لا تلزمُها؛ وزرٌّ قديمٌ يحملُ مُعرِّفَ رحلةٍ **انتهَت** فتُنسَبُ
استغاثةُ اليومِ إلى طلبٍ `completed` بمَوضِعِه ومدينتِه **والملكيّةُ سليمةٌ ولا شيءَ
يُخفِقُ**؛ وضغطتانِ بدلَ واحدةٍ والثانيةُ في المسارِ المعطوبِ نفسِه.

### ما صارَ قائماً بعدَ `20260910210000`

- `trigger_sos` بالتوقيعِ ومفاتيحِ المردِّ نفسِها، و`p_order_id = null` يُحَلُّ **في
  القاعدةِ**: الراكبُ عبرَ `is_active_order_status` (و`searching` **داخلٌ**)، والسائقُ
  عبرَ `is_driver_engaged_order_status` (و`searching` **خارجٌ**) — والدالّتانِ تُنادَيانِ
  من هجرةِ `20260814140000` ولا تُكتَبُ قائمتُهما نصّاً.
- `order by created_at desc limit 1 for update` — **الأحدثُ لا الأقدمُ**. ورمزٌ جديدٌ
  `NO_ACTIVE_ORDER`. وحكمُ الدورِ مرفوعٌ إلى أوّلِ الدالّةِ. والقفلُ الاستشاريُّ على
  الطلبِ **المُحَلِّ** فتبقى الضغطتانِ المتزامنتانِ حادثاً واحداً.
- `/sos` مُلتقَطٌ في المُوزِّعَينِ **قبلَ أوّلِ `await`**، والانتظارُ الوحيدُ الباقي في
  مواضعِ الاستقبالِ الثلاثةِ هوَ `triggerSos`. والزرُّ لم يعدْ يُقرأُ منه مُعرِّفٌ.
- الإنفاذُ آليٌّ: `scripts/check-sos-intake-isolation.ts` بأربعِ قواعدَ في سلسلةِ
  `bun run ci` وخطوةً مُسمّاةً في `verify`، و**صفرُ مواضعَ خرقٌ لا نجاحٌ**.

### حدودُ ما يُقالُ ههنا

**لا يُدَّعى أنَّ استغاثةً سقطَت في الإنتاجِ**: المُثبَتُ **مسارٌ** كانَ يُسقِطُها عندَ
إخفاقِ قراءةٍ، ولا قياسَ لتكرارِ ذلكَ. **ولا يُدَّعى أنَّها لا تُخفِقُ الآنَ**: الانتظارُ
الباقي هوَ الدالّةُ نفسُها وإخفاقُها إخفاقٌ؛ المُدَّعى أنَّ نقاطَ الإخفاقِ نزلَت من أربعٍ
إلى **واحدةٍ لا يمكنُ إسقاطُها**. والحاجزُ يقرأُ النصَّ لا الأثرَ. وحالاتُ التكاملِ
العشرُ على PostgreSQL حقيقيّةٍ **لم تُنفَّذْ محليّاً** والهجرةُ **لم تُطبَّقْ**، فصحّةُ
صياغتِها بالقراءةِ لا بالتشغيلِ. ولا تحسينَ زمنٍ يُدَّعى فلم يُقَسْ. و`F8-05` **لا
تُقلَبُ حالتُها**: `ح-4` تشترطُ حكمَ CI، وحجبُ المُنفِّذين على مستوى الحسابِ قائمٌ.
الحاكمُ `ADR-0077`، والدليلُ `docs/evidence/correctness/F8-05-20260910.md`.

---

## تصحيحٌ مُضافٌ — `F8-03`: السجلُّ كانَ يعملُ ولا يُستعلَمُ، ويكتبُ مُعرِّفاً شخصيّاً خاماً (2026-09-10)

**إضافةٌ لا محوٌ** (`ح-8`). ما سبقَ من وصفٍ للرصدِ يبقى صحيحاً في شقِّ **المقاييسِ
التشغيليّةِ** وفي حاجزَي السرِّ وقياسِ الجهازِ (`F1-03` · `F1-08` · `ADR-0043`).
**والناقصُ هوَ السجلُّ نفسُه**: كانَ موجوداً وشغّالاً — ولذلكَ لم يشتكِ منه شيءٌ: لا
ترجمةٌ تُخفِقُ ولا اختبارٌ يسقطُ ولا حاجزٌ يُسقِطُ بناءً.

**والمقروءُ من الشيفرةِ قبلَ هذا العملِ**: **أربعةُ مُصدِراتٍ بثلاثةِ أشكالٍ** — البوابةُ
`{at, message, …}` بلا `level`، والعمّالُ `{level, message, …}` بلا `at`، والإدارةُ
كالبوابةِ، و`apps/gateway/src/container.ts` نداءُ `console` مباشراً — فلا استعلامَ
واحداً يمرُّ على السطحَينِ. و**٧١** رسالةً نصّاً عربيّاً إنشائيّاً ومعَها قالبٌ يُبنى
وقتَ التشغيلِ: لا يُعَدُّ ولا يُنبَّهُ عليه، **ويتغيَّرُ بتحريرٍ إنشائيٍّ** فمن صحَّحَ
همزةً أسقطَ كلَّ لوحةٍ تعتمدُ عليها ولا شيءَ يُخفِقُ. و**١٧** نداءَ `console` مباشراً
منها **إخفاقُ الإقلاعِ** نصّاً بأيقوناتٍ. و**أحدَ عشرَ موضعاً** تكتبُ مُعرِّفاً شخصيّاً
خاماً: `address` (٣) و`actorId` في `routes/telegram-webhook.ts`، و`telegramUserId` (٦)
في `bots/shared/language-middleware.ts`، و`chatId` في
`packages/infrastructure/notification/outbound-resilience.ts` — يبقى ما بقيَ
الاستبقاءُ (`ADR-0075`) ويُقرأُ بكلِّ من يقرأُ السجلَّ. **وتسجيلُ العنوانِ عندَ رفضِ
ويبهوكٍ يبدو تحقيقاً أمنيّاً** وهذا ما جعلَه يمرُّ في المراجعةِ. **والقراءةُ اليدويّةُ
رأت أربعةً من الأحدَ عشرَ فقط؛ والسبعةُ الباقيةُ أخرجَها الحاجزُ لمّا كُتِبَ.**

### ما صارَ قائماً

- `packages/infrastructure/observability/structured-log.ts` **المُصدِرُ الوحيدُ**
  والاستثناءُ الوحيدُ لـ`console` في `apps/**` و`packages/**`، بشكلٍ واحدٍ
  `{at, service, event, [level], …fields, [redacted]}` و`service` مُثبَّتٍ عندَ التركيبِ.
- رمزُ الحدثِ **حرفيٌّ لاتينيٌّ منقوطٌ** (`EVENT_CODE_PATTERN`)؛ ورمزٌ غيرُ مطابقٍ ⇦
  `log.invalid_event_code` بشدّةِ `error` **وبطولِ المطلوبِ لا نصِّه**.
- الشدّةُ **اختياريّةٌ وغائبةٌ عندَ الجهلِ** — لا `info` كذباً؛ وتصنيفُها `F8-02`.
- الحقلُ الشخصيُّ يُحجَبُ بقائمةٍ مغلقةٍ **وبمطابقةِ الاسمِ الكاملِ** بلا حساسيةِ حالةٍ
  ولا شُرطةٍ، **والتصفيةُ تنزلُ في الكائناتِ والمصفوفاتِ** إلى حدِّ عمقٍ مُعلَنٍ؛
  و`fileName`/`jobName`/`cityName` لا تُحجَبُ عن قصدٍ.
- المُعرِّفُ الشخصيُّ كنيةً `sha256(salt+value)` مقصورةً، **والمِلحُ عشوائيٌّ لكلِّ
  عمليةٍ** — فلا ربطَ بينَ مثيلَينِ ولا بعدَ إعادةِ تشغيلٍ، وذاكَ حدٌّ مُعلَنٌ يُقرَّرُ
  معَ `F8-07` لا قبلَه.
- النصُّ العربيُّ السابقُ **محفوظٌ حرفاً** في جدولِ مقابلةٍ من ٧٢ سطراً في `ADR-0078`
  (`ح-8`)، لا في السطرِ ولا في تعليقاتٍ متفرّقةٍ.
- الإنفاذُ آليٌّ: `scripts/check-structured-logging.ts` بأربعِ قواعدَ في سلسلةِ
  `bun run ci` **قبلَ** `verify-container-build.sh` وخطوةً مُسمّاةً في `verify`،
  **وقائمةُ الأسماءِ تُقرأُ من المُصدِرِ نفسِه** لا من نسخةٍ في الحاجزِ، **وشكلُ الرمزِ
  يُقارَنُ بينَ الاثنَينِ**، و**صفرُ مواضعَ خرقٌ لا نجاحٌ** (`ح-5`). والمقيسُ اليومَ
  **٢١١ موضعَ تسجيلٍ** و**صفرُ `console` خارجَ المُصدِرِ** (كانَت ١٧).
- `apps/miniapp` **خارجُ النطاقِ صريحاً**: حزمةُ المتصفِّحِ يحكمُها `F1-08` و`ADR-0043`
  بقواعدَ مختلفةٍ، وحاكمانِ لموضعٍ واحدٍ عيبٌ لا شمولٌ.

### حدودُ ما يُقالُ ههنا

**لا يُدَّعى أنَّ بياناً شخصيّاً وُجِدَ في سجلِّ إنتاجٍ**: المُثبَتُ أحدَ عشرَ موضعاً في
الشيفرةِ، ولا وصولَ إلى سجلاتِ الإنتاجِ في هذه البيئةِ ولا قياسَ لما جُمِعَ فعلاً.
**ولا يُدَّعى أنَّ الكنيةَ تمنعُ الاستدلالَ مطلقاً**: تمنعُ الردَّ المباشرَ بلا المِلحِ،
ومن ملكَ المِلحَ والفضاءَ ردَّها. **والحاجزُ يقرأُ النصَّ لا الأثرَ**؛ والتصفيةَ في
العمقِ تُثبِتُها إحدى وعشرونَ حالةً سلوكيّةً على المُصدِرِ. **ولا شدّةَ لأكثرِ
المواضعِ** وغيابُها مقصودٌ مُعلَنٌ. **ولا تصديرَ إلى جامعٍ خارجيٍّ ولا لوحةَ اليومَ**
(`F8-07` لم يُنفَّذْ) — فنفعُ التهيئةِ **مُؤجَّلٌ**، والمُنجَزُ ههنا شرطُه لا هوَ.
**وفرعُ فشلِ التسلسلِ في المُصدِرِ حرزٌ غيرُ مقيسٍ** ولا يُدَّعى أنَّه مُختبَرٌ.
و**`F8-03` لا تُقلَبُ حالتُها**: `ح-4` تشترطُ حكمَ CI، وحجبُ المُنفِّذين على مستوى
الحسابِ قائمٌ. الحاكمُ `ADR-0078`، والدليلُ
`docs/evidence/correctness/F8-03-20260910.md`.

---

## تصحيحٌ مُضافٌ — `F8-04`: لا قاطعَ ولا حدَّ تزامنٍ، ومهلةُ تلغرام كانَت خمسَمئةِ ثانيةٍ (2026-09-10)

هذا القسمُ **يُضافُ ولا يمحو** ما قبلَه (`ح-8`).

**ما كانَ مكتوباً ضِمناً** أنَّ صمودَ الصادرِ في
`notification/outbound-resilience.ts` كافٍ. **والمقروءُ من `main`@`e5de9f6`
خلافُه**: ذاكَ يحكمُ إعادةَ المحاولةِ وترتيبَ الرسائلِ **لمُتلقٍّ واحدٍ**، ولا
يحكمُ الاعتماديّةَ على مستوى العمليّةِ. فكانَ في المستودعِ **صفرُ قواطعِ دوائرَ
وصفرُ حدودِ تزامنٍ**، وسبعةُ مواضعَ تبني `new Api(token)` بلا خياراتٍ فتَرِثُ
مهلةَ grammY الافتراضيّةَ: **خمسَمئةَ ثانيةٍ**. **والتضخيمُ في التركيبِ لا في
ملفٍّ**: `callTimeoutMs: 10_000` و`criticalMaxAttempts: 5` فوقَها،
و`withTimeout` **يتخلّى عن النداءِ المتأخِّرِ ولا يُلغيه** — فرسالةٌ حرجةٌ واحدةٌ
تُخلِّفُ خمسةَ مقابسَ حيّةٍ بلا سقفٍ.

**والقائمُ الآنَ**: `packages/shared/resilience` (قاطعٌ · حدُّ تزامنٍ · حاجزٌ)،
و`DEPENDENCY_BUDGETS` **مصدرُ حقيقةٍ واحدٌ تُشتَقُّ منه ثوابتُ المهلةِ**،
ومصنعٌ وحيدٌ `createTelegramApi` بمهلةٍ **عشرِ ثوانٍ**، وحرسٌ في Redis
والخرائطِ والدفعِ، وحاجزٌ آليٌّ بخمسِ قواعدَ في `bun run ci` وفي `verify`.

**وما لا يُدَّعى**: الميزانيّاتُ **قراراتٌ مُعلَنةٌ لا قياساتٌ** ومعايرتُها
`F10`؛ **وخطرُ الخصمِ المزدوجِ ما زالَ قائماً** — `moyasar` و`tap` يُعيدانِ
`POST` خصمٍ بلا `Idempotency-Key`، **مُسجَّلٌ لا معالَجٌ** وعلاجُه معَ `F8-06`؛
**والتخلّي عن المقبسِ ما زالَ قائماً** وقد خفَّ أثرُه خمسينَ ضعفاً ولم يُزَلْ؛
ولا صمودَ للوكيلِ فلا خروجَ شبكيّاً له اليومَ.

و**`F8-04` لا تُقلَبُ حالتُها**: `ح-4` تشترطُ حكمَ CI، وحجبُ المُنفِّذين على
مستوى الحسابِ قائمٌ. الحاكمُ `ADR-0079`، والدليلُ
`docs/evidence/correctness/F8-04-20260910.md`.

---

## تصحيحٌ مُضافٌ — 2026-09-11 · وحدةُ `W-1` وأوّلُ حكمِ CI فعليٍّ في `uxxxug/ceezr`، و`OPS-011`

**أوّلُ ما يُصحَّحُ دعوى الحجبِ نفسُها.** آخِرُ ما في هذه الوثيقةِ يقولُ إنَّ
«حجبَ المُنفِّذين على مستوى الحسابِ قائمٌ» فلا يُقلَبُ بندٌ. **وهذا صحيحٌ لموضعِه
وباطلٌ لموضعِنا**: المستودعُ الحاكمُ اليومَ `uxxxug/ceezr`، وفيه الجَريانُ عاملٌ،
**وعددُ جولاتِ Actions فيه قبلَ هذا العملِ صفرٌ**. فالحجبُ كان مِلكَ حسابِ
`noor-seez`، **ولا يُمحى سطرٌ سابقٌ** — يُقيَّدُ نطاقُه بالإضافةِ.

**وحدةُ `W-1`.** جردُ الحدِّ بينَ MOVE وCORE صارَ **سجلّاً في الشيفرةِ يحرسُه
حاجزٌ** لا جدولاً في وثيقةٍ يتقادمُ: `scripts/lib/wasla-boundary-registry.ts`
مصدرَ حقيقةٍ واحداً لـ**٤٤ جدولاً** (KEEP=14 · REFACTOR=12 · MOVE_TO_CORE=18 ·
RETIRE=0) و**٧ خروقٍ على مستوى الأعمدةِ** و**٤ اعتماديّاتٍ على CORE**، و
`scripts/check-boundary-audit.ts` يستخرجُ الجداولَ من نصِّ الهجراتِ التسعينَ
فيرفضُ الناقصَ والزائدَ والمتقادمَ، ويولّدُ `docs/wasla/boundary-audit.md`
بـ`--write` فلا يُكتَبُ الجدولُ بيدٍ. والخطوةُ مسمّاةٌ في `verify` وفي سلسلةِ
`ci`. **وحدُّ المستخرِجِ مُعلَنٌ**: `driver_location_history_default` قسمٌ لا
جدولٌ فأُخرِجَ — **٤٤ لا ٤٥**. الحاكمُ `ADR-0080`، والدليلُ
`docs/evidence/architecture/W-1-20260911.md`. **ولا شيفرةَ تطبيقٍ ولا هجرةً ولا
حاجزاً قائماً مُسَّ، ولا صفَّ واحدٍ هاجرَ**: التصنيفُ ليسَ إذناً بالنقلِ، وقِسما
«Migrated» و«Retired» في `ROADMAP.md` خاليانِ.

**وحكمُ CI مختلطٌ لا أخضرُ، ويُقالُ كما هوَ (`ح-8`).** الفرعُ
`feat/w1-wasla-boundary-audit` عندَ `66d733d`، الطلبُ
[#1](https://github.com/uxxxug/ceezr/pull/1)، الجولاتُ `34617842924` و
`34617910690` و`34617842960`، و`conclusion` قُرِئ لكلِّ وظيفةٍ من الواجهةِ
البرمجيةِ: **`verify` `success` وفيها حاجزُ `W-1` نفسُه** · `فوضى متعدد
المثيلات` `success` · `طَرافةُ الخارطةِ` `success` · **`تكامل على PostgreSQL
حقيقي` `failure`** · **`تكامل على Redis حقيقي` `failure`**.

**والإخفاقانِ في وظيفةِ القاعدةِ سابقانِ للعملِ لا منه** — `W-1` لم يمسَّ شيفرةَ
تطبيقٍ ولا هجرةً ولا اختبارَ تكاملٍ — **ولم يُريا قبلَ اليومِ لسببَينِ**: صفرُ
جولاتٍ في هذا المستودعِ، واختباراتُ التكاملِ تتجاوزُ نفسَها بلا
`TEST_DATABASE_URL`. **فسُجِّلا بنداً مستقلّاً `OPS-011`** (§11-د): (أ) دفعةُ
الموقعِ كانت تُسلسَلُ مرّتَينِ (`JSON.stringify` معَ `::jsonb`، و`postgres.js`
يُسلسِلُ للمُعامِلِ الموسومِ `jsonb`) فتصلُ الدالّةَ `string` لا `array` فتردُّ
`BATCH_MUST_BE_ARRAY` حكماً صحيحاً على مُدخَلٍ خاطئٍ، ومعَها حقلُ حكمٍ مكتوبٌ
`quality` والدالّةُ تقرأُ `verdict`؛ فصارا `sql.json` و`verdict` — **وهوَ نفسُه
طريقُ المحوّلِ في الإنتاجِ**، فصارَ تراجُعُ الحملِ القديمِ إلى `now()`
(`ADR 0076`) مُقاساً بعدَ أن كانَ بلا دليلٍ. (ب) مُوجَبٌ على نصٍّ عربيٍّ حرٍّ
استبدلَه `F8-03`/`ADR 0078` بسجلٍّ مُهيكلٍ، فصارَ على الرمزِ
`embedded_worker.disabled` الذي يفرضُه `check-structured-logging` — **أقوى لا
أضعفُ**. **والتغييرُ في `tests/` وحدَها**، ولا مهلةَ زيدَت ولا `skip` أُضيفَ ولا
مُوجَبٌ خُفِّف. الدليلُ `docs/evidence/correctness/OPS-011-20260911.md`.

**وحاجزٌ بيئيٌّ يُسجَّلُ ولا يُتجاوَزُ**: وظيفةُ Redis الحقيقيِّ تُخفِقُ عندَ
`assertRealRedisWhenRequired` لغيابِ `UPSTASH_REDIS_REST_URL` و
`UPSTASH_REDIS_REST_TOKEN` في هذا المستودعِ — **والحارزُ عينُ إنفاذِ `OPS-006`
فلا يُخفَّفُ ولا يُسكَتُ ولا يُصنَّفُ تخطياً**، وضبطُ سرَّي المستودعِ ليسَ في
يدِ الوكيلِ. و`OPS-006` يبقى `[x]` بتصحيحٍ مُلحَقٍ يُقيِّدُ نطاقَه بالمستودعِ
الذي أُثبِتَ فيه.

**وما لم يُمَسّ يُقرأُ حرفيّاً**: لا `W-2` … `W-9` (و`W-5` محجوبٌ بـ
`DEP-CORE-001`) · ولا رسالةٌ واحدةٌ أُرسِلَت إلى CORE ولا استُقبِلَت · ولا
`wasla-core` عُدِّل حرفاً (قُرِئَ وحدَه عندَ `511624b`) · ولا MARKET · ولا ADR
منشورٌ عُدِّل (`ح-6`) · ولا `مَقيس` ولا `مُثبَت` (`ح-5`).
## حدُّ WASLA: المهمّةُ التشغيليّةُ وأحداثُ CORE — `W-4` · `W-5` (2026-09-11 · إضافةٌ)

**ما كانَ مكتوباً ضِمناً** أنَّ «المهمّةَ» في هذا المستودعِ هيَ `orders`، وأنَّ لا
حدَّ بينَه وبينَ CORE يحتاجُ عقداً. **والمقروءُ من `main`@`30e024e` خلافُه**: لا
جدولَ مهمّةٍ تشغيليّةٍ، ولا صندوقَ واردٍ، ولا نسخةَ عقدٍ من CORE، ولا حرفَ
يُستهلَكُ من `core.fulfillment.created` أو يُصدَرُ في `move.job.*`.

**والقائمُ الآنَ على الفرعِ** `feat/w4-w5-operational-job-and-core-lifecycle`:
`operational_jobs` مهمّةٌ قانونيّةٌ فريدةٌ لكلِّ تنفيذٍ بمراجعَ معتِمةٍ بلا اقترانٍ
بـ`orders`، وآلةُ حالاتٍ **في القاعدةِ** (خمسُ حالاتٍ · خمسةُ انتقالاتٍ · سبعةُ
قيودِ `check`)، و`core_event_inbox` يُقيِّدُ الواردَ بمفتاحِ `event_id`،
و`move_event_outbox` بحقولِ المغلَّفِ العشرةِ مفرودةً وقيدِ تفرُّدٍ على
`dedup_key` يُودَعُ فيه الحدثُ **في معاملةِ تغييرِ الحالةِ نفسِها**، وحاجزٌ
`check:core-contract-parity` يُطابِقُ حرفَ عقدِ CORE بالطبقةِ وبآلةِ الحالاتِ
وبالهجرتَينِ. والمقيسُ: 37 حالةً وحدةً و33 حالةَ تكاملٍ على PostgreSQL حقيقيّةٍ
بلا إخفاقٍ، للبنودِ العشرةِ المطلوبةِ.

**وما لا يُدَّعى**: **لا ناقلَ شبكيّاً إلى CORE** — `MoveEventShipper` منفذٌ بلا
محوّلٍ إنتاجيٍّ، و`DEP-CORE-001` قائمٌ؛ **ولا هجرةَ لصفٍّ واحدٍ** من `orders`
ولا إطفاءَ لمسارِه؛ **ولا طزاجةَ مُتحقَّقةً** للعقودِ المُستنسَخةِ
(`DEP-CORE-005`)؛ **والهجرتانِ ليستا جاهزتَينِ للدمجِ**:
`scripts/check-migrations.ts` يُخفِقُ بثلاثِ مخالفاتٍ للقاعدةِ 0.4 لأنَّ عقدَ
CORE لا يحملُ مدينةً، والحاجزُ لم يُضعَّف ولم يُستثنَ ولم يُجمَّد. الحسمُ
`DEP-CORE-006` أو ملحقٌ حاكمٌ جديدٌ من المالكِ يُوسِّعُ صنفَ
`domain-ingress receipt`.

وطابورُ الصادرِ **مُعلَنٌ** في سجلِّ الضغطِ العكسيِّ (`F6-06`) بحدودِه الستّةِ
ثوابتَ شيفرةٍ في `packages/shared/config/move-event-outbox.ts`، فصارَت الطوابيرُ
الحيّةُ ثلاثةً. وفهارسُ الجداولِ الثلاثةِ في ترحيلاتٍ مُفرَدةٍ بطورِ `index`
و`concurrently` (`CAP-007`).

و**`W-4` و`W-5` لا تُقلَبُ حالتُهما**: `ح-4` تشترطُ دليلاً وحكمَ CI، وحكمُ CI
أحمرُ بسببٍ مُعلَنٍ. الحاكمُ `ADR-0081`، والدليلُ
`docs/evidence/architecture/W-4-W-5-20260911.md`.

## ناقلُ أحداثِ CORE في الاتّجاهَينِ — `W-5` (2026-09-12 · إضافةٌ لا تمحو ما قبلَها)

كانَ المكتوبُ أعلاهُ أنَّ **لا ناقلَ شبكيّاً إلى CORE** لأنَّ `DEP-CORE-001`
قائمٌ. **والمقروءُ الآنَ من مستودعِ CORE عندَ**
`1231817757446560a0ecd061bd9c4f3a2a1c9fe4` **خلافُه**: البابُ الشبكيُّ صارَ
موجوداً (`d2c38e3`) — `POST /v1/events` بمصادقةِ حاملٍ، وقبولٌ `202` بإيصالٍ
يحملُ `first_delivery`، وإعادةُ إيداعِ المعرّفِ نفسِه آمنةٌ؛ وفي المقابلِ
اشتراكٌ صادرٌ (`event_subscription`) **يزرعُه المُشغِّلُ** لبادئةِ `core.*` على
نهايةٍ `https` بسرٍّ لا يقلُّ عن اثنَينِ وثلاثينَ محرفاً، وتوقيعٌ
`sha256=<hex>` على البايتاتِ عينِها. فأُغلِقَ `DEP-CORE-001` وسُجِّلَ إغلاقُه،
ولم يُعدَّل حرفٌ في CORE من هنا.

**والمُنفَّذُ**: محوّلٌ إنتاجيٌّ صاعدٌ (`core-event-shipper.ts`) بمهلةٍ صريحةٍ،
وتصنيفُ رمزِ الردِّ في **موضعٍ واحدٍ** يُحرسُ بحاجزٍ يقرأُ جدولَ الإعادةِ من
عقدِ CORE المنقولِ؛ ورفضٌ دائمٌ **يموتُ من محاولتِه الأولى بحكمٍ في القاعدةِ**
(`abandon_move_event_delivery` خمسيّةً بـ`p_permanent`)؛ وبابُ استقبالٍ
`/webhook/core-events` يتحقَّقُ من التوقيعِ **قبلَ** التحليلِ ويردُّ `503` عندَ
سرٍّ غائبٍ أو قصيرٍ ولا يُخفِّفُ التحقّقَ، ولا يُركَّبُ أصلاً بلا سرٍّ؛ ومنعُ
تكرارٍ في القاعدةِ وحدَها؛ وتصريفٌ مهمّةً بسقفٍ كلَّ خمسَ عشرةَ ثانيةً
(`ship-move-events`) لا تُسجَّلُ بلا عنوانٍ ورمزٍ.

**وحاجزٌ جديدٌ لسلامةِ المنقولِ**: `check:vendored-contract-integrity` يُعيدُ
حسابَ بصماتِ العقودِ المنقولةِ الثمانيةِ في كلِّ جريَةٍ، بعدَ أن بطلَت بصمتانِ
فعلاً بتنسيقِ المُنسِّقِ فبقيَت دعوى «حرفاً» مكتوبةً والبايتاتُ مختلفةً.
وسقوطُ الحاجزِ مقيسٌ بخرقٍ مزروعٍ.

**وما لا يُدَّعى**: أنَّ CORE الحقيقيَّ قَبِلَ. لا بيئةَ CORE مشتركةً ولا رمزَ
خدمةٍ لـMOVE (`DEP-CORE-007` · وقرارا المُشغِّلِ `O-3` و`O-4`)، فالمقيسُ
مطابقةُ العقدِ المكتوبِ وأثرُ الطريقَينِ في قاعدةٍ حقيقيّةٍ لا تسليمٌ إنتاجيٌّ.

و**`W-5` لا تُقلَبُ حالتُه**: `check-migrations` ما زالَ يُخفِقُ بثلاثِ
مخالفاتٍ للقاعدةِ 0.4 (`DEP-CORE-006` · `O-1`). الحاكمُ `ADR-0082`، والدليلُ
`docs/evidence/architecture/W-5-TRANSPORT-20260912.md`.

## مصفوفةُ الهجرةِ صارَت سجلّاً محروساً — `W-2` (2026-09-12 · إضافةٌ لا تمحو ما قبلَها)

الحاكمُ `ADR-0083`. والدليلُ `docs/evidence/architecture/W-2-20260912.md`.

**العطبُ المُغلَقُ**: `W-2` طلبَ «مصفوفةَ هجرةٍ لكلِّ كيانٍ» في `docs/migration/`،
وهذا المسارُ **لم يكنْ موجوداً ألبتّةَ**. ولو كُتِبَ وثيقةً بيدٍ لكانَ يتقادمُ
**بلا سطرٍ خاطئٍ فيه**: تُضافُ هجرةٌ بجدولٍ جديدٍ فلا خطّةَ له، أو تُبدَّلُ وجهةُ
جدولٍ في جردِ `W-1` فتبقى خطّتُه على الوجهةِ القديمةِ — فيقرأُ منفِّذٌ خطّةً
**صحيحةَ الشكلِ تُخالِفُ القرارَ النافذَ**. وأخطرُ من خطّةٍ ناقصةٍ خطّةٌ يُظَنُّ
أنَّها مطابقةٌ.

**والمُنفَّذُ**: `scripts/lib/wasla-migration-matrix.ts` سجلّاً واحداً — **سبعُ
آليّاتٍ مغلقةٍ** لكلٍّ وجهاتُها المقبولةُ وشكلُ عودتِها، و**سبعُ موجاتٍ** لكلٍّ شرطُ
دخولٍ مكتوبٌ، و**مُدخلٌ لكلِّ جدولٍ من ٤٧** فيه الآليّةُ والموجةُ والشروطُ السابقةُ
ومسارُ العودةِ والقياسُ الذي يُغلِقُ، و**٧ خططِ أعمدةٍ** تقابلُ سجلَّ الاختراقاتِ
واحداً بواحدٍ. و`scripts/check-migration-matrix.ts` حاجزاً بـ**عشرةِ فحوصٍ** يولِّدُ
`docs/migration/matrix.md` بـ`--write`، و**٢٧ حالةَ اختبارٍ أكثرُها سالبةٌ** تزرعُ
خرقَ كلِّ فحصٍ وتطلبُ من الحاجزِ أن يراه.

**والفحصُ الحاكمُ**: أيُّ مُدخلٍ يدّعي تنفيذاً وقسمُ «Migrated» في `ROADMAP.md`
خالٍ **يُسقِطُ البناءَ** — فلا يُقرأُ تخطيطٌ إنجازاً. وهوَ **يقيسُ ولا يُعانِدُ**:
إن صارَ في «Migrated» صفٌّ حقيقيٌّ سقطَ المنعُ، وذلكَ مُختبَرٌ صراحةً.

**ولا وجهةَ مُكرَّرةً ولا مالكاً** (القاعدةُ 0.6): `MatrixEntry` لا يحملُ الحقلَينِ
أصلاً، بل يُقرآنِ من سجلِّ `W-1` عندَ الفحصِ والتوليدِ — فلا مصدرَ حقيقةٍ ثانٍ
يفترقُ صامتاً.

**وموضعُ الخطوةِ في `verify` صُحِّحَ بعدَ قراءةِ الترتيبِ**: وُضِعَت أوّلاً بعدَ
خطوةِ القاعدةِ 0.4، وتلكَ **حمراءُ اليومَ** والوظيفةُ تنقطعُ عندَها — فما كانَ
الحاجزُ الجديدُ سيُشغَّلُ قطُّ ولا يُقرأُ لهُ حكمٌ. فنُقِلَت إلى ما **قبلَها**
مباشرةً. **ولم يُخفَّف حاجزُ 0.4 ولا أُخِّرَ**: هوَ الخطوةُ التاليةُ حرفاً ويُخفِقُ
بالرسالةِ عينِها، و`conclusion` الوظيفةِ يبقى `failure` — المتبدِّلُ أنَّ حاجزَ
`W-2` صارَ **مقيساً** بدلَ أن يكونَ ميّتاً حتّى يحسمَ المالكُ `O-1`.

**وما لا يُدَّعى**: **لم يُهاجَر صفٌّ واحدٌ**. كلُّ مُدخلٍ `executed: false` حرفاً،
و«Migrated» و«Retired» كلاهما «Nothing.». ولا موجةٌ قابلةٌ للتنفيذِ اليومَ: `B-1`
(الأحجامُ والمكرَّراتُ مجهولةٌ) · `B-2` (سياسةُ دمجِ الهويّاتِ) · `B-3` (بيئةُ CORE)
· و`DEP-CORE-002/003/004` **في CORE لا ههنا**. والمصفوفةُ **لا تُجيزُ شيئاً**: هيَ
خطّةٌ بحاجزٍ يقيسُ الشمولَ والتماسكَ وصدقَ الادّعاءِ، لا صوابَ اختيارِ آليّةٍ
لجدولٍ بعينِه (وذلكَ حكمٌ يُراجَعُ بالقراءةِ — `ADR-0083` §٦).

و**`W-2` لا يُعلَّمُ `[x]`**: `ح-4` تطلبُ حكمَ CI مقروءاً، و`verify` أحمرُ على
`main` بالقاعدةِ 0.4 (`O-1` قرارُ مالكٍ). الدرجةُ: **مُنفَّذ · مُختبَر**. لا
`مُتحقَّق منه` ولا `مَقيس` ولا `مُثبَت`.

**وحكمُ CI الفعليُّ قُرِئَ بعدَ الدفعِ** (`a587923` · الجولتانِ `34664552923`
و`34664586565` · الطلبُ [#3](https://github.com/uxxxug/ceezr/pull/3)): `verify`
**`failure`** · PostgreSQL حقيقي **`success`** · Redis حقيقي **`failure`** · فوضى
المثيلاتِ **`success`** · طَرافةُ الخارطةِ **`success`**. **وقُرِئَ سجلُّ الوظيفةِ
خطوةً خطوةً**: الخطوةُ ١٥ «مصفوفةُ الهجرةِ …» **`success`** — فحاجزُ البندِ مُثبَتٌ
عندَ CI لا محلّيّاً وحدَه — والخطوةُ ١٦ «منعُ أيِّ جدولٍ بلا `city_id`» `failure`،
**والخطوةُ ٢٤ حاجزُ `W-1` `skipped`**: وذلكَ هوَ البرهانُ على أنَّ نقلَ الخطوةِ لم
يكنْ تجميليّاً، إذ لولاه لكانَ حاجزُ `W-2` `skipped` فلا حكمَ لهُ. والأحمرانِ
**سابقانِ للبندِ لا منه**، بالوظيفةِ والخطوةِ عينِهما على `main` عندَ `8d031c2`.
**ولا يُدَّعى أنَّ الجولةَ خضراءُ.**

## 2026-09-12 — `W-6`: حدُّ الصادرِ سجلٌّ مغلقٌ محروسٌ

**العطبُ المُغلَقُ**: البندُ `W-6` يشترطُ ألّا يكونَ في MOVE اقترانٌ تجاريٌّ مباشرٌ
معَ MARKET وأن يمرَّ كلُّ ما بينَ النظامَينِ عبرَ CORE. وقِيسَ المستودعُ قبلَ أيِّ
تعديلٍ فكانَ الشرطُ **صحيحاً فعلاً**: ولا مضيفٌ واحدٌ في MARKET، وملفّاتُ
`*/marketplace/*` **خاويةٌ** من ثمانيةِ أسطرٍ لا اقترانٌ. **لكنَّه صحيحٌ
بالمصادفةِ لا بالإنفاذِ**: ولا سطرَ في المستودعِ يُخفِقُ لو أُضيفَ نداءٌ مباشرٌ
غداً. فالعملُ لم يكنْ إزالةَ اقترانٍ — لا اقترانَ ليُزالَ — بل **تحويلَ حالةٍ
عارضةٍ إلى حالةٍ مُنفَذَةٍ**.

**والمُنفَّذُ**: سجلٌّ مغلقٌ في `scripts/lib/wasla-egress-registry.ts` بخمسةَ عشرَ
مقصداً مصدرَ حقيقةٍ وحيداً، يحرسُه `scripts/check-egress-boundary.ts` في
`verify`. **ومنطقُه معكوسٌ عن القائمةِ السوداءِ**: لا يُسألُ أمنوعٌ هذا المضيفُ
بل **أمُعلَنٌ هوَ**، فمضيفٌ غيرُ مُعلَنٍ يُسقِطُ البناءَ أيّاً كانَ اسمُه،
وإعلانُه يقتضي `system`، و`system: "MARKET"` مرفوضٌ نصّاً. وذلكَ لازمٌ لأنَّ
نطاقَ MARKET **مجهولٌ لهذا المستودعِ** (`DEP-CORE-005`) فقائمةٌ سوداءُ بأسماءٍ لا
نعرفُها حاجزٌ بلا محروسٍ.

وثلاثةُ حاكماتٍ: لا نظامَ `MARKET`، وكلُّ مرورٍ بينَ الأنظمةِ صنفُه `CORE`، ولا
ادّعاءَ إزالةٍ ومضيفُها ما زالَ في الشيفرةِ. ومعَها سبعةُ فحوصٍ، ووثيقةٌ
مُولَّدةٌ محصورةٌ بعلامتَي توليدٍ (القاعدةُ 0.6)، و`ADR 0084`.

**والتكاملُ التجاريُّ المباشرُ دَينٌ مُعلَنٌ لا صمتٌ**: `api.moyasar.com` و
`api.tap.company` ليسا خرقاً لهذا البندِ — MARKET ليسَ طرفاً — لكنَّ صنفَهما يوجبُ
حقلَ `handover` يُسمّي `W-7` المُزيلَ و`DEP-CORE-002` الحاجبَ، ويتحقّقُ الحاجزُ
أنَّ المعرّفَينِ مُعلَنانِ فعلاً في `ROADMAP.md`.

**والحاجزُ أخفقَ على مؤلِّفِه**: أربعةُ إعفاءاتٍ وُضِعَت بالظنِّ ثمَّ قِيسَ
المسحُ منزوعَ التعليقاتِ فلم يظهرْ منها واحدٌ، فحُذِفَت كلُّها وأُضيفَ فحصٌ يمنعُ
**الإعفاءَ الميّتَ**؛ وثلاثةُ مواضعِ نداءٍ كُتِبَت بالاستنباطِ فأسقطَها الفحصُ ٨
فصُحِّحَت بالقياسِ.

**والقياسُ المحليُّ**: ٤٠ حالةً للحاجزِ · ٠ إخفاقٍ، والكلُّ **2966 نجاحاً · 821
تخطّياً · ٠ إخفاقٍ · 10496 توقُّعاً**. **وسقوطُ الحاجزِ مُثبَتٌ بخرقٍ مزروعٍ في
شيفرةٍ حقيقيّةٍ** لا في اختبارٍ وحدَه. **والخطوةُ موضوعةٌ قبلَ حاجزِ القاعدةِ 0.4**
لأنَّ ذاكَ أحمرُ بقرارِ مالكٍ (`O-1`) وما بعدَه `skipped` — كما قِيسَ في `W-2` —
**ولم يُضعَّف حاجزُ 0.4 ولا أُخِّرَ**.

**وما لا يُدَّعى**: **البندُ غيرُ مُنجَزٍ ولا يُعلَّمُ `[x]`**. نصفُ الصادرِ
مُنجَزٌ، ونصفُ **تسليمِ القناةِ** لا يُمكِنُ من ههنا: `DEP-CORE-004` يُسجِّلُ أنَّ
CORE لا يعرضُ محوّلَ قناةِ تِلغرام. ولا حجبَ صادرٍ في زمنِ التشغيلِ، والمسحُ
لفظيٌّ لا تحليلُ تدفُّقٍ، وقيمةُ متغيّرِ البيئةِ لا تُقاسُ — وكلُّ ذلكَ مُعلَنٌ
في `ADR 0084` لا مسكوتٌ عنه. الدرجةُ: **مُنفَّذ · مُختبَر**. لا `مُتحقَّق منه`
ولا `مَقيس` ولا `مُثبَت`.

## `W-8` — أدواتُ التشغيلِ الجافِّ والتسويةِ · 2026-09-12 (إضافةً لا محواً)

بُنيَت أداتانِ لهجرتَي المهمّةِ والهويّةِ: تشغيلٌ جافٌّ **يعجزُ عن الكتابةِ**
(`BEGIN TRANSACTION READ ONLY` على اتّصالٍ محجوزٍ، فالمنعُ خاصّةُ المحرِّكِ
بالرمزِ `25006`، ثمَّ `ROLLBACK` على كلِّ حالٍ)، وتسويةٌ **تعجزُ عن الأخضرِ
الزائفِ** (`RECONCILED` غيرُ قابلةٍ للبناءِ بلا إشهادِ مصدرٍ في CORE، و`DIVERGED`
كذلكَ، و`coreRows` تبقى غائبةً لا صفراً). ومحروسانِ بسبعةِ فحوصٍ في `verify`
**قبلَ** خطوةِ `city_id` الحمراءِ، وبـ٤٢ اختباراً سالباً، وبقياسٍ على PostgreSQL
حقيقيٍّ يُثبِتُ إخفاقَ الكتابةِ بالرمزِ `25006`. والقرارُ ADR 0085.

**والقياسُ كشفَ عطباً لم تكشفْهُ المراجعةُ**: التشغيلُ الجافُّ كانَ يفتحُ
`READ ONLY` على تجمُّعٍ غيرِ محجوزٍ، فالثابتُ مُعلَنٌ والسلوكُ مخالفٌ. أُصلِحَ في
السببِ بحجزِ اتّصالٍ، وأُضيفَ فحصٌ يمنعُ عودتَه صامتاً. **يُسجَّلُ ولا يُمحى.**

ولا موجةَ نُفِّذَت (`B-1`/`B-2`/`B-3`)، ولا تسويةَ تمَّت (`DEP-CORE-007`)، ولا
`[x]` للبندِ (`ح-4`).

## `W-9` — تمرينُ التحويلِ والعودةِ · 2026-09-12 (قياسُ حجبٍ لا إنجازٌ)

**محجوبٌ، والحجبُ أوسعُ من سطرِ الخارطةِ.** `B-5` (لا موافقةَ إصدارٍ للإنتاجِ)
حقيقيٌّ، ومعَهُ `B-3` (لا بيئةَ CORE مُهيَّأةً) و`DEP-CORE-007` (لا بيئةَ CORE
مشتركةً — وهوَ عينُ ما يُجبِرُ كلَّ سطرِ تسويةٍ في `W-8` أن يكونَ `UNVERIFIABLE`)
و`B-1` (جردُ الإنتاجِ مجهولٌ). فتمرينُ تحويلٍ إلى نظامٍ **غيرِ موجودٍ** بأحجامٍ
**مجهولةٍ** لا يُمرِّنُ شيئاً، وتسميتُه أخضرَ هيَ عينُ الأخضرِ الزائفِ الذي بُنيَ
`W-8` لِيَجعلَهُ غيرَ قابلٍ للبناءِ.

**والقائمُ اليومَ لا يُغطّيهِ**: `OPS-010` يُغطّي مسارَ عودةِ **المخطَّطِ** —
تدقيقٌ ساكنٌ وحاجزٌ وتمرينٌ على PostgreSQL حقيقيٍّ في CI (الخطوةُ ٩، `success`
عندَ `c80201c`) — أي **إعادةُ نشرِ صورةٍ سابقةٍ على مخطَّطٍ متقدِّمٍ**. أمّا هذا
البندُ فنقلُ ملكيّةِ الهويّةِ والدفعِ والمهمّةِ بينَ نظامَينِ ثمَّ العودةُ،
ويستلزمُ النظامَ الثاني.

ولا `[x]`، ولا إعادةَ تفسيرٍ للبندِ توثيقاً، ولا تمرينَ مُدَّعىً. الحالةُ:
**قيد التنسيق — محجوبٌ بقرارِ مالكٍ.**

## `W-6` الزيادةُ الثانيةُ — بوّابةُ الصادرِ وقتَ التشغيلِ · 2026-09-12 (إضافةً لا محواً)

**العطبُ المُغلَقُ**: الزيادةُ الأولى أعلنَت حدَّ نفسِها بنفسِها — **«لا منعَ
للصادرِ وقتَ التشغيلِ»** — فكانَ الإنفاذُ في البناءِ وحدَه. والحاجزُ الساكنُ يقرأُ
الشيفرةَ ولا يقفُ بينَ العمليّةِ والشبكةِ: مضيفٌ من **قيمةِ** مفتاحِ بيئةٍ، أو
مُركَّبٌ وقتَ النداءِ، أو ناقلٌ محقونٌ من الخارجِ — كلُّها كانت تمضي وحاجزُ البناءِ
أخضرُ. وخلطُ الأبوابِ (عميلُ دفعٍ يُنادي بابَ CORE) كانَ صامتاً لأنَّ المضيفَينِ
مُعلَنانِ كلاهما.

**والمُنفَّذُ**: `packages/shared/wasla/egress-gate.ts` بوّابةً وقتَ التشغيلِ تقرأُ
**السجلَّ عينَه**، وحكمُها مغلقٌ افتراضاً بستّةِ أسبابِ رفضٍ مُسمّاةٍ، ومنها
`HOST_OF_ANOTHER_PEER`. والبوّابةُ **قبلَ الناقلِ ولو كانَ محقوناً**، فلا يبقى
`fetchImpl` باباً جانبيّاً. و`assertEgressEnvironment` يُسقِطُ **الإقلاعَ** بعدَ
تحميلِ الضبطِ وقبلَ تركيبِ الحاويةِ، **وترتيبُه مقيسٌ في اختبارٍ**. والحاجزُ
الساكنُ كبِرَ بثلاثةِ فحوصٍ (١١، ١٢، ١٣) **ولم يُضعَفْ منهُ فحصٌ واحدٌ**. والقرارُ
`ADR 0086`.

**وتصحيحٌ بالإضافةِ لا بالمحوِ (`ح-8`)**: القسمُ السابقُ أعلاه يقولُ إنَّ السجلَّ
في `scripts/lib/wasla-egress-registry.ts`، وهوَ صحيحٌ لحظةَ كتابتِه. وقد نُقِلَ
بـ`git mv` إلى `packages/shared/wasla/egress-registry.ts` كي يقرأَه الحاجزُ
والبوّابةُ من مصدرٍ واحدٍ. **النصُّ السابقُ يبقى كما هوَ لقطةً تاريخيّةً**،
والنافذُ هوَ المسارُ الجديدُ.

**والمَقيسُ محلّيّاً** (ليسَ حكماً — `ح-8`): `bun test` **3035 نجاحاً · 829 تخطياً ·
0 إخفاقٍ** و10666 `expect()` في 3864 حالةً في 288 ملفّاً · `biome` 1114 ملفّاً بلا
إصلاحاتٍ · `typecheck` نظيفٌ · الحرّاسُ يمرّونَ. والزيادةُ عن قياسِ `main`
(**3008**) **٢٧** حالةً، أكثرُها يزرعُ الخرقَ ويقيسُ **الرفضَ**.

**والمنفيُّ صريحاً**: لا `[x]` للبندِ — نصفُ تسليمِ القناةِ محجوبٌ بـ`DEP-CORE-004`
و`ح-4` تطلبُ حكمَ CI و`verify` أحمرُ بالقاعدةِ 0.4 (`O-1`) — ولا إنفاذَ على طبقةِ
الشبكةِ (`B-1`/`B-5`)، ولا قياسَ أنَّ المضيفَ المضبوطَ هوَ CORE حقّاً
(`DEP-CORE-005`). ولم يُمَسَّ `ADR 0084` ولا حاجزُ `city_id` ولا اختبارُ Redis ولا
`MASTER_DIRECTIVE`، ولا سرَّ أُضيفَ إلى المستودعِ.

## 2026-09-12 — `W-8` زيادةٌ ثانيةٌ: الإشهادُ صارَ غيرَ قابلٍ للتلفيقِ، والحواجزُ صارت مقروءةً

`ADR 0087` · دليلٌ: `docs/evidence/architecture/W-8-attestation-20260912.md`.

**العطبُ قِيسَ ولم يُستَدَلَّ عليهِ.** الزيادةُ الأولى كتبَت أنَّ `RECONCILED` غيرُ
قابلةٍ للبناءِ إلّا بإشهادٍ، وكانَ `CoreAttestation` واجهةَ ثلاثِ سلاسلَ يكتبُها
أيُّ مُنادٍ. **والدليلُ**: اختبارُ البندِ نفسِه كانَ يكتبُ إشهاداً يذكرُ
`DEP-CORE-007` — وهوَ **مفتوحٌ** — ويحصلُ على `RECONCILED`. فالرفضُ كانَ قائماً على
غيابِ مُحاولةٍ، لا على آلةٍ.

**ما صارَ الآنَ.** وَسمٌ `unique symbol` غيرُ مُصدَّرٍ داخلَ
`scripts/lib/wasla-migration-dry-run.ts` ⇒ لا ملفَّ آخرَ يستطيعُ تسميةَ الحقلِ،
والاستحالةُ بِنيويّةٌ لا اتّفاقيّةٌ. و`issueCoreAttestation` مُصدِرٌ واحدٌ يرفضُ
تبعيّةً مفتوحةً ومعرّفاً غيرَ مُعلَنٍ ووصفَ قراءةٍ حشواً. وسبيلٌ ثانٍ مستقلٌّ:
`deriveReconciliation` تتحقَّقُ من الوَسمِ وقتَ التشغيلِ ⇒ الملفَّقُ
`UNVERIFIABLE`.

**وسجلٌّ واحدٌ مقروءٌ.** `scripts/lib/wasla-blockers.ts` يُفكِّكُ جداولَ
`ROADMAP.md` ⇒ 16 حاجزاً، 15 مفتوحاً، و`DEP-CORE-001` وحدَه مُغلَقٌ بدليلِ التزامٍ.
لا حالةَ محفوظةً (القاعدةُ 0.6)، والسؤالُ عن مجهولٍ **يرمي** ولا يُجيبُ «مُغلَقٌ».
وحارسٌ جديدٌ بثمانيةِ فحوصٍ في سلسلةِ `ci` وفي `verify` **قبلَ** خطوةِ `city_id`
الحمراءِ.

**قياسٌ محلّيٌّ** (`ح-8`: ليسَ حكماً): 3074 ناجحاً · 829 متخطّىً · 0 إخفاقٍ ·
3903 اختباراً / 290 ملفّاً · biome 1118 ملفّاً بلا إصلاحاتٍ · typecheck نظيفٌ ·
87 قراراً بأرقامٍ فريدةٍ.

**وانحرافٌ يُسجَّلُ**: `scripts/check-migration-dry-run.ts` كانَ محجوزاً و**لم
يُعدَّلْ** — الفحوصُ في حارسٍ مستقلٍّ ليُقرَأَ أيُّ حارسٍ أخفقَ. والحارسُ الجديدُ
أخفقَ أوّلاً على ملفِّ اختبارِه (يزرعُ الرموزَ سلاسلَ)، فلم يُخفَّفْ: تجاوزُ مسحِ
`tests/` مكتوبُ السببِ، ومُقيَّدٌ بفحصٍ ثامنٍ يشترطُ أن يبقى قياسُ التلفيقِ حيّاً،
وسقوطُ الاستثناءِ مَقيسٌ باختبارٍ.

**والمنفيُّ صريحاً**: لا تسويةَ تمَّت، ولا قراءةَ من CORE، و`DEP-CORE-007` مفتوحٌ،
و`O-1`/`O-2` أحمرانِ كما هُما ولم يُمَسّا، و`B-1`…`B-5` مفتوحةٌ فلا موجةَ
تُشغَّلُ، ولا `[x]` للبندِ (`ح-1`, `ح-4`). ولم يُمَسَّ `ADR 0085` (`ح-6`) ولا
اختبارُ Redis ولا `MASTER_DIRECTIVE`، ولا سرَّ أُضيفَ.

## سجلُّ خطواتِ التحوُّلِ والاسترجاعِ، وتمرينُ قراءةٍ فقط — قِيسَ 2026-09-12 (إضافةً؛ لا حذفَ)

`W-9` **ما زالَ محجوباً بـ`B-5`**، ولم يُتحايَلْ على الحَجبِ: نُقِلَ الرفضُ من
جملةٍ في وثيقةٍ إلى **إنشاءٍ في الشيفرةِ**. سجلُّ التحوُّلِ صارَ **مُشتَقّاً** من
`WASLA_MIGRATION_MATRIX` — ثلاثونَ خطوةً، لكلِّ واحدةٍ **عكسٌ** مُصرَّحٌ
و**مِسبارُ قراءةٍ** ورتبةٌ كلّيّةٌ وطورٌ وحواجزُ حَجبٍ؛ وترتيبُ الاسترجاعِ عكسُ
ترتيبِ التحوُّلِ حرفاً بحرفٍ.

ودالّةُ التمرينِ تقرأُ حواجزَها الأربعةَ (`B-5`، `B-3`، `DEP-CORE-007`، `B-1`) من
سجلِّ حواجزِ `W-8`، فما دامَ واحدٌ منها مفتوحاً فلا قيمةَ تُبنى غيرَ `REFUSED`،
ولا مُعامِلَ تجاوزٍ، **ومعرّفٌ مجهولٌ يُقرأُ مفتوحاً**. ولا حالةَ في النوعِ
تُمثِّلُ تحوُّلاً تامّاً ألبتّةَ.

وعلى قاعدةِ PostgreSQL حقيقيّةٍ قِيسَ **العجزُ** نفسُه: معاملةٌ `read only`،
مسابيرُ تُقرأُ، ثمَّ كتابةٌ **تُرفَضُ** — ويُخفِقُ المُنفِّذُ لو نجحَت. وهذا في
وظيفةِ PostgreSQL في CI بعدَ المُطبِّقِ الآمنِ، على وزنِ تمرينِ `OPS-010`،
و**ملفّاتُ `OPS-010` أُعيدَ استخدامُها ولم تُعدَّلْ** (`ح-6`).

وقِيسَ عطبٌ في الحاجزِ نفسِه فصُحِّحَ من جذرِه: مُتحقِّقُ «القراءةِ وحدَها» كانَ
يردُّ مِسبارَ `telegram_update_jobs` لأنَّ اسمَ الجدولِ يحملُ «update». ولم
يُخفَّفِ الحاجزُ ولا استُثنيَ جدولٌ — بل صارت المطابقةُ **برموزٍ كاملةٍ** بسببٍ
مكتوبٍ في الشيفرةِ، وقُيسَ الطرفانِ.

وما لا يُدَّعى: لا تمرينَ جرى، ولا موجةَ نُفِّذَت، ولا صفَّ كُتِبَ، ولا استرجاعَ
جُرِّبَ، ولا زمنَ قُدِّرَ (`B-1` مفتوحٌ). و`O-1`/`O-2` أحمرانِ كما هُما ولم
يُمَسّا، ولا `[x]` للبندِ (`ح-1`, `ح-4`). ولم يُمَسَّ `ADR 0085` ولا `0087`
(`ح-6`) ولا اختبارُ Redis ولا `MASTER_DIRECTIVE`، ولا سرَّ أُضيفَ.

## `DEP-CORE-005` — سندُ العقدِ المنقولِ صارَ مُفكَّكاً، وطزاجتُه صارَ لها مُقابِلٌ دلاليٌّ · 2026-09-12 (إضافةً لا محواً)

**القرارُ**: `ADR 0090` · **الدليلُ**:
`docs/evidence/architecture/DEP-CORE-005-20260912.md` · **الفرعُ**:
`feat/dep-core-005-contract-freshness` من `main`@`1d361ea`.

كلُّ ما سبقَ في هذه الوثيقةِ يبقى بحرفِه، ومنه قولُها إنَّ مقابلةَ العقودِ
داخليّةٌ. وهذه الزيادةُ تُضيفُ ما صارَ مقيساً، ولا تمحو سطراً.

### ما كانَ محروساً فعلاً قبلَ اليومِ — ولِمَ لم يكفِ

حاجزانِ قائمانِ، وكلاهما **داخليٌّ**:

1. `scripts/check-core-contract-parity.ts` يُقابِلُ **إعلانَ MOVE** بالنسخةِ
   المنقولةِ مقابلةً دلاليّةً تامّةً: مجموعةُ المطلوبِ في الاتّجاهَينِ، وكلماتُ
   كلِّ حقلٍ واحدةً واحدةً، ومنعُ الزائدِ، ورفضُ كلمةٍ لا يُنفِّذُها المُدقِّقُ.
2. `scripts/check-vendored-contract-integrity.ts` يُعيدُ حسابَ كلِّ بصمةٍ، فلا
   يُمَسُّ منقولٌ بعدَ نقلِه.

فالأوّلُ يُثبِتُ أنَّنا نوافقُ النسخةَ التي نحملُها، والثاني أنَّ النسخةَ لم
تُحرَّرْ، **ولا أحدَ منهما يرى CORE**. وهذا بعينِه ما جعلَ `W-5` ممكناً: بقيَ
`core.fulfillment.cancelled.v1` بائتاً من دورةِ نطاقِ المُستأجِرِ، والحاجزانِ
أخضرانِ، وMOVE يَرُدُّ كلَّ إلغاءٍ يَنشُرُه CORE.

### ما أُضيفَ

| المُضافُ | ما يفعلُه |
|---|---|
| سطرُ `pin` لكلِّ ملفٍّ في `PROVENANCE.md` | يُسمّي المستودَعَ والالتزامَ **كاملاً** ومسارَ المصدرِ والمسارَ المنقولَ. إضافةٌ في السجلِّ نفسِه لا سجلٌّ ثانٍ، ولا تُكرَّرُ فيه البصمةُ: السندُ يُجيبُ «من أينَ؟» والبصمةُ «هل تغيَّرَ؟» |
| `scripts/lib/vendored-contract-pins.ts` | قارئٌ **واحدٌ** للسجلِّ، يستورِدُه حاجزُ السلامةِ القائمُ بدلَ تعبيرِه الخاصِّ، فلا ينحرفُ تفكيكانِ لمصدرٍ واحدٍ. وسلوكُ الحاجزِ القائمِ وصادراتُه كما كانت، واختبارُه القائمُ يمرُّ بلا تعديلٍ |
| `scripts/check-vendored-contract-pins.ts` | ملفٌّ بلا سندٍ يُخفِقُ، وسندٌ لملفٍّ محذوفٍ يُخفِقُ، وسندانِ لملفٍّ واحدٍ يُخفِقانِ، والتزامٌ مختصرٌ يُرفَضُ. لا شبكةَ ولا سرَّ، فهوَ في سلسلةِ `ci` وفي `verify` |
| `scripts/lib/schema-semantic-diff.ts` | يُقابِلُ المخطَّطَ بمعناه (المطلوبُ · الحقولُ · كلماتُ كلِّ حقلٍ · حكمُ الزائدِ)، وYAML مُفكَّكاً، والنثرَ نصّاً **مع التصريحِ بأنَّه نصٌّ**. ويُصنِّفُ: ما يكسِرُ المستهلكَ، وما يكسِرُ المنتِجَ، وما هوَ زيادةٌ، وما هوَ وصفٌ. وكلمةٌ لا تصنيفَ لها تُقرأُ كاسرةً |
| `scripts/check-core-contract-freshness.ts` | يحكمُ على كلِّ ملفٍّ حكماً من خمسةٍ: مطابقٌ · نسختُنا مُحرَّرةٌ · المصدرُ تقدَّمَ · المصدرُ أُزيلَ · السندُ لا يُقرأُ. ويقرأُ البايتاتَ من **نسخةٍ محلّيّةٍ** من CORE بـ`git show`: لا رِمزَ ولا HTTP ولا بيئةَ نشرٍ |
| `.github/workflows/core-contract-freshness.yml` | جريَةٌ **بالطلبِ اليدويِّ وحدَه**: لا `schedule` فلا ضجيجَ إخفاقٍ يوميٍّ، ولا سرَّ في المستودَعِ |

### موضعُ الخطوتَينِ في `verify` — ولِمَ قبلَ الحمراءِ

كلُّ خطوةٍ بعدَ `منع أي جدول بلا city_id` تُتخطّى في كلِّ جريَةٍ ما بقيَت `O-1`
مفتوحةً. فحاجزٌ يُوضَعُ بعدَها **لا حكمَ له**، ويُعطي مظهرَ إنفاذٍ بلا إنفاذٍ.
ولذلكَ أُدرِجَت خطوتا هذه الزيادةِ — سندُ العقودِ، ومقياسُ سقوطِ المُقابِلِ —
**قبلَها**، للسببِ عينِه الذي قِيسَ ثلاثَ مرّاتٍ قبلَ اليومِ.

### ما قِيسَ على CORE الحقيقيِّ

شُغِّلَ المُقابِلُ على نسخةٍ من `uxxxug/wasla-core` عندَ رأسِها `9e6a636`
(2026-09-12T12:49:25Z)، فأخرجَ `1`: **ثلاثةٌ من ثمانيةٍ بائتةٌ** —
`core.fulfillment.cancelled.v1` (وأوّلُ انحرافٍ فيه `required.organization_id`
مُصنَّفاً «يكسِرُ المستهلكَ»، وهوَ عطبُ `W-5` بعينِه مكشوفاً آليّاً هذه المرّةَ)،
و`transport/core-v1.yaml` (٥٢ اختلافاً بنيويّاً)، و`transport/outbound-delivery.md`
(٨٢ سطراً مُضافاً، **ولا تُدَّعى دلالةٌ**). والخمسةُ الباقيةُ تطابقُ الرأسَ بايتاً
بايتاً.

**ولم يُعَدْ نقلُ الملفّاتِ الثلاثةِ ههنا**: هيَ في نطاقِ طلبِ الدمجِ المفتوحِ
`#11`، ونقلُ بايتاتِ عقدٍ في فرعَينِ يُنشئُ تعارُضاً في عقدٍ لا في نصٍّ.

### وما لا يُدَّعى — صريحاً

`DEP-CORE-005` **تبقى مفتوحةً**. CI لا تستطيعُ الحكمَ بالطزاجةِ لأنَّ CORE
مستودَعٌ خاصٌّ ورِمزُ جرياتِ هذا المستودَعِ لا يقرؤُه، وذاكَ قرارُ مالِكٍ
سُجِّلَ **`O-6`**. وفحصٌ لا تحكمُ به CI ليسَ بوابةً مُنفِذةً، فلا يُقفَلُ به
بندٌ، ولا يُوضَعُ `[x]` ولا `VERIFIED` — و`ح-4` لا يسمحُ أصلاً وهوَ يطلبُ ثلاثَ
جرياتٍ خضراءَ متتاليةً، وذاكَ مستحيلٌ ما بقيَت `O-1` حمراءَ.

وجملةُ `PROVENANCE.md` القائمةُ «**ولا يُدَّعى أنَّ التقادمَ محروسٌ آليّاً في
CI**» تبقى صادقةً بحرفِها بعدَ هذه الزيادةِ: المُقابِلُ موجودٌ ومُفكَّكٌ ومُختبَرٌ
وقد قِيسَ على CORE الحقيقيِّ، وCI لا تزالُ لا تحكمُ به. والذي يُقفِلُ البندَ
واحدٌ: `O-6` ثمَّ نقلُ الأمرِ خطوةً في `verify` قبلَ الحمراءِ.

## `DEP-CORE-005` الزيادةُ الثانيةُ — سندٌ يتبعُ بايتاتَه، أو يُخفِقُ · 2026-09-12 (إضافةً لا محواً)

**القرارُ**: `ADR 0091` — ويُبنى على `ADR 0090` ولا يُعدِّلُه.

الزيادةُ الأولى أعطَت كلَّ منقولٍ سندَاً مُفكَّكاً. وتشغيلُ المُقابِلِ كشفَ ثقباً
**في الزيادةِ الأولى نفسِها**، قِياساً لا ظنّاً: **إعادةُ نقلٍ تُحدِّثُ البايتاتَ
والبصمةَ وتنسى السندَ تمرُّ من الحُرّاسِ الثلاثةِ جميعاً.** فالمطابقةُ لا ترى
CORE، والسلامةُ تُقابِلُ البصمةَ بالملفِّ وهما متوافقانِ، وحاجزُ السندِ يُقابِلُ
الملفَّ بالسندِ **وجوداً لا مضموناً**.

وقد قِيسَ في المستودَعِ: طلبُ الدمجِ `#11` يُعيدُ نقلَ ثلاثةِ عقودٍ، وسندُ هذا
الفرعِ يُثبِّتُها عندَ التزاماتِ `main` — أي قبلَ إعادةِ النقلِ. فأيُّهما دُمِجَ
ثانياً لزمَ تحديثُ السندِ، ولم يكن ما يُلزِمُه: ذاكرةُ إنسانٍ لا حاجزٌ.

| المُضافُ | ما يفعلُه |
|---|---|
| `scripts/check-vendored-pin-follows-bytes.ts` | إن تغيَّرَت بايتاتُ منقولٍ في مدى الدفعِ فَلْيتغيَّرْ سطرُ سندِه في المدى نفسِه. ويُحَلُّ المدى بترتيبٍ مُعلَنٍ: مدى الدفعِ، ثمَّ أصلُ الفرعِ من `main`؛ وإن تعذَّرا خرجَ بـ`3` وسمّى السببَ فلا يُقرأُ تعذُّرٌ نجاحاً |
| خطوةٌ في `.github/workflows/roadmap.yml` | وهيَ الجريَةُ الوحيدةُ التي تملكُ مدىً حقيقيّاً و`fetch-depth: 0`. وإدراجُه في `verify` كانَ يعني تاريخاً كاملاً في كلِّ جريَةٍ أو سكوتاً دائماً — وحاجزٌ ساكتٌ أسوأُ من غيابِه |
| خطوةٌ في `verify` قبلَ الحمراءِ | تقيسُ **سقوطَ الحاجزِ نفسِه** على مستودَعِ `git` مزروعٍ بالتزامَينِ: بلا شبكةٍ ولا تاريخٍ ولا نسخةٍ من CORE |

والمقاييسُ: `14 pass · 0 fail · 27 expect()`. وتشغيلُه على مدى هذا الفرعِ يقولُ
إنَّه لا ملفَّ منقولاً تغيَّرَ، وذاكَ صادقٌ: هذا الفرعُ يُضيفُ إلى `PROVENANCE.md`
ولا يُعيدُ نقلَ بايتةٍ.

**وما لا يُدَّعى**: لا يُكشَفُ سندٌ كُتِبَ كاذباً في الالتزامِ نفسِه؛ ذاكَ يلزمُه
قراءةُ CORE، وهوَ المُقابِلُ و`O-6`. فهذا يسدُّ **النسيانَ** — وهوَ الطريقُ الذي
سُلِكَ فعلاً — ولا يسدُّ **الكذبَ المتعمَّدَ**. و`DEP-CORE-005` تبقى **مفتوحةً**:
المحروسُ ههنا صدقُ السندِ، لا طزاجةُ العقدِ.

## 2026-09-12 — `W-5` زيادةٌ ثالثةٌ: عقدُ الإلغاءِ كانَ منحرفاً فكانَ MOVE يرفضُ كلَّ إلغاءٍ

`ADR 0089` · دليلٌ: `docs/evidence/architecture/W-5-CANCELLATION-20260912.md`.
إضافةً لا محواً: لا سطرَ قبلَ هذا عُدِّلَ ولا أُسقِطَ.

**العطبُ قِيسَ ولم يُستَدَلَّ عليهِ.** مُقابَلةُ بايتاتِ العقودِ الستّةِ
المنقولةِ في `docs/contracts/core/` بـ`uxxxug/wasla-core`@`0edb7af` أظهرَت خمسةً
مُطابِقةً وواحداً منحرفاً: `core.fulfillment.cancelled.v1.schema.json` عندَنا
`ed540b6b…` وعندَ مالِكِه `cd9d8369…`. ودورةُ نطاقِ المستأجرِ عندَ CORE
(`acd93c8` · `CORE:B-23`) أضافَت `organization_id` **مطلوباً** و`captured_minor`
و`financial_decision_required` و`partially_captured`. و`validateObject` يردُّ
الحقلَ الزائدَ، و`consume` يُدقِّقُ **قبلَ** الإيداعِ في الواردِ ⇒ **MOVE كانَ
يرفضُ كلَّ إلغاءٍ يُنشِرُه CORE اليومَ** فتبقى المهمّةُ التشغيليّةُ جاريةً. وهذا
أوّلُ حالةٍ أُضيفَت في هذه الزيادةِ، وقد أخفقَت قبلَ الإصلاحِ ونجحَت بعدَه.

**ما صارَ الآنَ.** الملفّاتُ الثلاثةُ أُعيدَ نقلُها بايتاً بايتاً من
`CORE@0edb7af` (المخطَّطُ · `transport/core-v1.yaml` · `transport/outbound-delivery.md`)
وبصماتُها في `PROVENANCE.md` بقسمٍ بعنوانِه، والبصماتُ المُلغاةُ **محفوظةٌ** في
جدولٍ لا يقرأُه حاجزُ السلامةِ فلا يصيرُ للملفِّ بصمتانِ نافذتانِ. وإعلانُ
`PAYLOAD_SPECS["core.fulfillment.cancelled.v1"]` صارَ عقدَ CORE: خمسةٌ مطلوبةٌ،
و`captured_minor` صحيحٌ بحدٍّ أدنى صفراً، و`financial_decision_required` منطقيٌّ
(و`FieldSpec.type` كَسِبَ `"boolean"`)، و`settlement_state` بستِّ قيمٍ.

**والمحرَّماتُ الماليّةُ الثلاثةُ عندَ CORE (`CORE:B-20`) مُستوفاةٌ بالبناءِ لا
بالاتّفاقِ**: لا سطحَ ماليَّ في MOVE ألبتّةَ — لا جدولَ ولا عمودَ ولا حدثَ صادرَ
يُقرأُ رَدَّ مالٍ أو إيراداً مُكتسَباً أو تسويةً — وقد صارَ ذلكَ مَقيساً: إلغاءٌ
بـ`partially_captured` و`captured_minor: 2500` و`financial_decision_required: true`
يُغلِقُ المهمّةَ، و`outcome` يبقى `null`، ولا يُودَعُ حدثٌ جديدٌ. وقيمةٌ مجهولةٌ
في `settlement_state` تُرَدُّ عقديّاً ولا تُقرأُ «أُعيدَ الحجزُ».

**وعطبٌ ثانٍ قِيسَ في أثناءِ التنفيذِ**: اقتباسُ معرّفاتِ حواجزِ CORE في
`ROADMAP.md` أخفقَ `scripts/check-blocker-registry.ts` لأنَّ `mentionedBlockerIds`
كانَ يمسحُ `B-\d+` بلا عِلمٍ **بمن يملِكُ** المعرّفَ. فأُصلِحَ في أصلِه ببادِئةِ
نطاقٍ (`CORE:` · `MARKET:` — مستودَعانِ مُسمَّيانِ لا بادِئةٌ مفتوحةٌ): ما لا
نملِكُه لا حالةَ له عندَنا فلا نُدعي إغلاقَه ولا فتحَه. **ولم تُخفَّفْ قاعدةُ
الحاجزِ**: كلُّ معرّفٍ من حواجزِنا ما زالَ يلزَمُه صفٌّ، وحالتانِ جديدتانِ تقيسانِ
الطرفَينِ. ولم يُعدَّلْ `scripts/check-blocker-registry.ts` نفسُه (لا صفَّ كاذباً
في جدولِنا لحاجزِ CORE، ولا إعفاءَ يدويّاً لكلِّ اقتباسٍ).

**وعطبٌ ثالثٌ**: `node_modules` كانَ متعقَّباً في `main` وصلةً رمزيّةً
(`120000 bc2686f3`) إلى مسارٍ مطلقٍ في جهازٍ واحدٍ، خلافَ `.gitignore` نفسِه.
أُخرِجَ من الفهرسِ وحدَه؛ لا محتوى مُسَّ.

**والمنفيُّ صريحاً**: `DEP-CORE-005` **يبقى مفتوحاً** — أُعيدَ النقلُ بقراءةٍ
يدويّةٍ من التزامٍ مُسمَّىً ولم يُبنَ فحصُ الطزاجةِ الآليُّ، فانحرافٌ لاحقٌ عندَ
CORE سيسكتُ سكوتَ هذا. و`O-1`/`O-2`/`O-3`/`O-4` و`DEP-CORE-006`/`007` على حالِها
ولم تُمَسّ، فحكمُ `verify` يبقى أحمرَ بالقاعدةِ 0.4. **ولا `[x]` ولا `VERIFIED`**
لبندِ `W-5` (`ح-4` · `ح-5`). ولم يُعدَّلْ شيءٌ في CORE أو MARKET، ولم يُدمَجْ ولا
يُحذَفْ فرعٌ ولا طلبُ دمجٍ، ولا سرَّ أُضيفَ.

## 2026-09-12 — حكمُ CI على فرعِ الزيادةِ الثالثةِ، وتصحيحُ دعوىً كُتِبَت قبلَ قراءتِه

إضافةً لا محواً (`ح-8`). المرجعُ الكاملُ شوطاً شوطاً وخطوةً خطوةً: قسمُ «CI
verdicts on branch `feat/w5-recontract-cancellation-tenant-scope`» في `ROADMAP.md`.

**الحكمُ المقروءُ** عندَ `cc0e11d`: `Roadmap freshness` ناجحٌ · `تكامل على
PostgreSQL حقيقي` ناجحٌ (17 خطوةً) · `فوضى متعدد المثيلات (F5-06)` ناجحٌ (13
خطوةً) · `verify` **مُخفِقٌ** عندَ الخطوةِ 19 (`city_id`) والخطواتُ 1–18 ناجحةٌ
و20–55 متخطّاةٌ · `تكامل على Redis حقيقي` **مُخفِقٌ** عندَ الخطوةِ 8. وشوطُ طلبِ
الدمجِ `#11` مثلُه حرفاً. **والإخفاقانِ هما `O-1` و`O-2` عينُهما، بلا تغيُّرٍ عن
حكمِ `main`@`1d361ea`، ولا خطوةَ تمسُّها هذه الزيادةُ أخفقَت.**

**وتصحيحٌ لدعوىً كُتِبَت في هذه الزيادةِ نفسِها قبلَ قراءةِ الحكمِ**: كُتِبَ أنَّ
`bun test` **لم يُحكَمْ عليهِ قطُّ** في `verify`. **وهذا خطأٌ، ويُصحَّحُ
بالإضافةِ لا بمحوِ السطرِ حيثُ كُتِبَ.** الخطوةُ 8 من `verify` اسمُها `Test`
وتُشغِّلُ `bun run test` (وهوَ `bun test`)، وقد **نجحَت** هنا وفي `main`. والذي
تمنعُه الخطوةُ 19 من أن يُحكَمَ عليهِ ألبتّةَ هوَ **سلسلةُ الحوارسِ من 20 إلى
55** — ومنها مُطابَقةُ عقودِ CORE (52) وسلامةُ العقودِ المنقولةِ (53) وبوّابةُ
التغطيةِ (54)، أي الحارسانِ اللذانِ تقومُ عليهِما هذه الزيادةُ. فالنتيجةُ
العمليّةُ تبقى قائمةً وتُقالُ الآنَ بدقّةٍ: **حوارسُ العقودِ خضراءُ محلّيّاً وبلا
حكمِ CI خاصٍّ بها، فلا يُستوفى `ح-4` لها ما دامَ `O-1` مفتوحاً.**

**وتصحيحٌ ثانٍ من الجنسِ عينِه**: الخطوةُ 8 تُشغِّلُ الجريةَ كاملةً بلا
`TEST_DATABASE_URL`، فتتخطّى اختباراتُ التكاملِ نفسَها هناكَ — ومن هنا عددُ
التخطّي الكبيرُ في ذلكَ الشوطِ. والذي يحكمُ عليها شوطُ PostgreSQL، وقد **نجحَ**؛
وهذا يحسِمُ أيضاً الإخفاقاتِ الأربعةَ المحلّيّةَ التي صُنِّفَت أثرَ بيئةٍ في
التزامِ `docs(W-5)`: CI على `postgis/postgis:17-3.5` بقاعدةٍ لكلِّ شوطٍ أخضرُ على
الشيفرةِ عينِها.

## الطلباتُ الثلاثةُ دُمِجَت بأمرِ المالكِ، وحاجزٌ عمرُه ساعةٌ ردَّ أوّلَ دمجٍ · 2026-09-12 (إضافةً لا محواً)

`#10` ثمَّ `#12` ثمَّ `#11` — بهذا الترتيبِ عن قصدٍ: `#12` يحملُ آلةَ السندِ،
فتقديمُه يعني أنَّ إعادةَ النقلِ في `#11` **يُحكَمُ عليها** لا تُمرَّرُ سبقاً.

والتعارضاتُ الثلاثةُ النصّيّةُ حُلَّت **إضافةً لا اختياراً**: خطوةُ `W-9` وخطواتُ
`DEP-CORE-005` الثلاثُ كلُّها باقيةٌ وكلُّها قبلَ الحمراءِ في `ci.yml`؛ وسلسلةُ
`ci` واحدةٌ تحملُ الحارسَينِ؛ وكتلتا حجزٍ وقسمانِ كاملانِ بُقِّيَ عليهما جميعاً.

**والرابعُ لم يكن تعارضاً نصّيّاً وكانَ أخطرَها**: `PROVENANCE.md` اندمجَ نظيفاً —
البايتاتُ من `#11` والسنداتُ من `#12` — فكانَ الحاصلُ ثلاثةَ سندٍ تُسمّي التزاماتٍ
ليسَت بايتاتُها بايتاتَ المنقولِ: سجلُّ أصلٍ يُقرأُ حُجّةً وهوَ كاذبٌ. فرفضَ
`check-vendored-pin-follows-bytes.ts` المدى. ولم يُختلَقْ تصحيحٌ: أُعيدَ تبصيمُ
الملفّاتِ الثلاثةِ على مستودَعِ CORE التزاماً التزاماً، فثبُتَ تطابقُها الحرفيُّ
عندَ `0edb7af` وهوَ الالتزامُ الذي سجَّلَ `#11` قراءتَه، فصارَت السنداتُ إليه.

أي أنَّ الثقبَ المُعلَنَ في `ADR 0091` وقعَ في **أوّلِ** دمجٍ بعدَ نزولِ الحاجزِ،
بالهيئةِ المُتوقَّعةِ عينِها، وكُشِفَ آليّاً لا بذاكرةِ أحدٍ. وهذا أقربُ ما بلغَه
هذا المستودَعُ من دليلٍ على أنَّ حاجزاً يستحقُّ وجودَه.

**وحكمُ CI على `main` عندَ `dccbaa7`** مقروءٌ خطوةً خطوةً: `Roadmap freshness`
نجحَت وحكمَت على المدى الحقيقيِّ؛ و`verify` خطواتُها ١–٢٢ نجحَت ومنها حُرّاسُ
`W-9` و`DEP-CORE-005` الأربعةُ، والإخفاقُ عندَ ٢٣ `city_id` (`O-1`)؛ و`Redis`
عندَ خطوتِها الثامنةِ (`O-2`)؛ و`PostgreSQL` والفوضى نجحَتا.

**وما لا يُدَّعى**: لا `[x]` ولا `VERIFIED`، ولا طزاجةَ عقدٍ أُثبِتَت —
`DEP-CORE-005` مفتوحةٌ بـ`O-6`. و`transport/outbound-delivery.md` **بائتٌ
مجدَّداً** فقد عدَّلَه CORE خمسَ مرّاتٍ بعدَ `0edb7af`، وإعادةُ نقلِه كلَّ ساعةٍ
طاحونةٌ لا ضمانٌ — وذاكَ ما يشتريه `O-6`: آلةٌ تقرأُ الحكمَ بدلاً من وكيلٍ يلحظُ.

---

## ملحقٌ: أوّلُ شاشةِ منتَجٍ وسجلُّ الموافقاتِ — `F2-01` (2026-09-12، إضافةٌ لا محوٌ · `ح-8`)

**ما تغيَّرَ في وصفِ هذا المستودَعِ.** كانَ مكتوباً أعلاه أنَّه «بلا شيفرةِ تطبيقٍ
ولا هجرةٍ ولا سطحِ منتَجٍ»: صارَ فيه اليومَ سطحُ منتَجٍ واحدٌ من اثنتَي عشرةَ،
وجدولٌ حقيقيٌّ، وبابانِ شبكيّانِ. والأسطرُ السابقةُ باقيةٌ كما كُتِبَت.

**البنيةُ.** الموافقةُ **صفٌّ بإصدارٍ وختمٍ زمنيٍّ لا رايةٌ منطقيّةٌ** (ADR-0092):
`user_consents(user_id, kind, version, accepted_at, city_id)` بمفتاحٍ فريدٍ على
الثلاثةِ الأُوَلِ، فالغيابُ ليسَ موافقةً، وإعادةُ الإرسالِ تُعيدُ الختمَ الأوّلَ
عينَه ولا تُنشئُ صفّاً ثانياً، وإصدارٌ أحدثُ **يُضافُ** ولا يمحو الأقدمَ —
والدليلُ القانونيُّ لا يُحذَفُ ولو انتقلَ الجدولُ إلى CORE لاحقاً (وهذا مكتوبٌ
في مسارِ عودةِ مُدخلِ المصفوفةِ). والختمُ من **ساعةِ الخادمِ** لا من الجهازِ.
و`city_id` **مُشتَقٌّ من صفِّ المستخدمِ** داخلَ الدالّةِ لا مُمرَّرٌ من الطلبِ
(القاعدةُ 0.4 بلا ثقةٍ في العميلِ). والعملياتُ الحرجةُ في دالَّتَينِ ذرّيَّتَينِ
(القاعدةُ 0.5)، ولا تُحسَبُ «الإصدارةُ الجاريةُ» في القاعدةِ فلا يصيرُ مصدرُ حقيقةٍ
ثانٍ (القاعدةُ 0.6).

**تعارضٌ في الوثيقةِ الحاكمةِ سُمّيَ وحُسِمَ ولم يُمحَ** (ADR-0093): القسم 9.5
يُدرِجُ «طلبَ إذنِ الموقعِ» في شاشةِ الترحيبِ، والقسم 9.12 يقولُ «كلُّ إذنٍ يُطلَبُ
في لحظةِ الحاجةِ لا مقدَّماً». ولا يمكنُ الجمعُ: لا شيءَ في شاشةِ الترحيبِ يستعملُ
موقعاً. فرجَّحَ القرارُ 9.12 لأنَّه بندٌ تنظيميٌّ يُحيلُ إلى القسم 16، بينما 9.5
جردُ شاشاتٍ؛ والشاشةُ **تُخبِرُ** أنَّ الإذنَ سيُطلَبُ لاحقاً ولا تستدعي
`navigator.geolocation` ألبتَّةَ. ونصُّ 9.5 باقٍ حرفاً كما هوَ (`ح-1`).

**دَينٌ مقيسٌ لا موصوفٌ.** القسم 9.11 يطلبُ حاجزَ CI يمنعُ النصَّ المكتوبَ في
مُكوِّنٍ: **الحاجزُ غيرُ موجودٍ**، وفي `apps/miniapp/src` **48 نصّاً حرفيّاً
عربيّاً في شيفرةٍ قابلةٍ للتنفيذِ في 7 ملفّاتٍ** خارجَ ملفّاتِ `F2-01` (وفيها صفرٌ).
ولم يُوضَع الحاجزُ في هذه الدورةِ لأنَّه كانَ سيُسقِطُ CI على شيفرةٍ سابقةٍ فيُستثنى
منها ستّةُ ملفّاتٍ — وحاجزٌ مُستثنىً منه حاجزٌ لا يحكمُ. والعددُ مسجَّلٌ ههنا
ليُقاسَ نقصانُه لا ليُنسى.

**وحاجزٌ صارَ أدقَّ لا أضعفَ.** `check-business-constants` أسقطَ خريطةَ رموزِ
الحالةِ في بابِ الموافقاتِ قارئاً `400` سعرَ اشتراكٍ. فلم يُستثنَ ملفٌّ ولم يُوسَّع
نوعٌ إلى `number` ولم يُسكَت الحاجزُ، بل أُضيفَ استثناءٌ **مُنطاقٌ بكتلةٍ**: لا
يُقبَلُ `KEY: 400,` إلّا داخلَ كتلةٍ فتحَها تصريحٌ يحملُ في نوعِه اتّحادَ رموزِ
الحالةِ نفسِه، وأيُّ سطرٍ غيرِ مطابقٍ داخلَها يُحاكَمُ كما لو كانَ خارجَها.
**وحدُّ الاستثناءِ مُعلَنٌ**: `Record<Plan, 400 | 250>` **يمرُّ** منه، وذاكَ مقيسٌ
في اختبارٍ باسمِه لا مسكوتٌ عنه.

**وما لا يُدَّعى**: لا `[x]` لـ`F2-01` ولا اقترابَ من بوّابةِ `F2` (رحلةٌ كاملةٌ
على جهازٍ حقيقيٍّ مُسجَّلةٌ بفيديو). وRLS مقيسٌ **مُفعَّلاً** لا **مُنفَذاً** —
اتّصالُ الاختبارِ مالكُ القاعدةِ، والاختبارُ يقولُ ذلكَ بنفسِه. والنصُّ القانونيُّ
للوثيقتَينِ ليسَ مُعتمَداً من مالكٍ؛ المُنفَّذُ آليّةُ الإصدارِ والختمِ. و`ح-4`
تطلبُ ثلاثَ دوراتِ CI خضراءَ متتاليةً، وهيَ غيرُ بالغةٍ ما بقيَ `O-1` يُحمِّرُ
`verify`. والدليلُ كلُّه في
`docs/evidence/architecture/F2-01-CONSENT-20260912.md`.

---

## ملحقٌ 2026-09-13 — الهدفُ النافذُ: منتجٌ تجاريٌّ مستقلٌّ (`S-1` · تعليمةُ المالكِ `O-7`)

**إضافةٌ لا تعديلٌ.** لا سطرَ فوقَ هذا الموضعِ غُيِّرَ ولا حُذِفَ (`ح-8`). وما
سبقَ من وصفٍ يجعلُ هذا المستودَعَ «نظامَ التنفيذِ الميدانيَّ لوَصْلة» طرفاً في
ثلاثيّةِ `CORE`/`MOVE`/`MARKET` يُقرأُ من اليومَ **لقطةً تاريخيّةً**.

### النافذُ

الهدفُ **استقلالُ هذا المستودَعِ ونشرُه منتجاً تجاريّاً منفصلاً**، لا تكاملُه مع
`wasla-core` ولا مع `wasla-market`. الحاكمُ: `ADR 0094` وتعليمةُ المالكِ `O-7`
المسجَّلةُ بنصِّها في `ROADMAP.md`، والملحقُ الحاكمُ في `docs/ROADMAP-MASTER.md` §27.

### ما تغيَّرَ في **الحالةِ** بهذه الزيادةِ

**لا شيءَ في الشيفرةِ ولا في المخطَّطِ ولا في CI.** هذه زيادةٌ وثائقيّةٌ محضةٌ:
لم تُمَسَّ هجرةٌ ولا اختبارٌ ولا حاجزٌ ولا سيرُ عملٍ. وحالةُ CI على `main` عندَ
`a65a184` كما هيَ: `verify` أحمرُ في خطوةِ `city_id` وحدَها، ووظيفةُ Redis حمراءُ
في خطوتِها الثامنةِ، والوظيفتانِ الأخريانِ وطَرافةُ الخارطةِ خضراءُ.

### ما تغيَّرَ في **تصنيفِ الحجبِ**

| قبلَ `O-7` | بعدَها |
|---|---|
| ثمانيةُ `DEP-CORE-*` مفتوحةٌ حاجبةٌ | **مؤجَّلةٌ بقرارِ المالكِ**؛ و`DEP-CORE-006` و`DEP-CORE-008` **انتفى سببُهما** |
| `O-1` بانتظارِ CORE أو ملحقٍ حاكمٍ للقاعدةِ 0.4 | **يُغلَقُ ههنا في جذرِه** (`S-2`)، بلا استثناءٍ في حاجزٍ ولا اسمٍ في قائمةِ الإعفاءِ |
| `O-2` بانتظارِ سِرَّي Upstash من المالكِ | **يُغلَقُ ههنا في جذرِه** (`S-3`): Redis حقيقيٌّ مُستضافٌ في الشغلةِ، والحاجزُ بنصِّه |
| `O-3` · `O-4` · `O-6` | مؤجَّلةٌ — لا بيانَ اعتمادٍ ولا اشتراكَ أحداثٍ ولا قراءةَ مستودَعٍ خارجيٍّ يُحتاجُ |
| `W-3` · `W-5` · `W-6` · `W-7` · `W-9` | تُقرَأُ **مؤجَّلةً**؛ ونصُّ كلِّ بندٍ لم يُمَسَّ |
| `B-1` · `B-2` · `B-4` · `B-5` | **بنصِّها، حواجزُ قائمةٌ** — داخليّةٌ أو نظاميّةٌ لا تبعيّةً على مستودَعٍ آخرَ |

### ما لا يُدَّعى

لا بندَ قُلِبَ `[x]`، ولا شيءَ ههنا «مَقيسٌ» ولا «مُثبَتٌ» (`ح-5`): وثيقةٌ لا
تُخضِّرُ بوّابةً. والإغلاقُ الفعليُّ لـ`O-1` و`O-2` يُقاسُ بحكمِ CI في زيادتَيه
(`S-2` · `S-3`)، ويُكتَبُ ههنا حينَ يُقرَأُ لا قبلَه.

## جهوزيّةُ قاعدةِ البياناتِ — زيادةٌ مُسجَّلةٌ 2026-09-13 (`S-5`)

قاعدةٌ حقيقيّةٌ سلَّمَها المالكُ (PostgreSQL 17.6 + PostGIS) صارَت **مُطابِقةً
لآخرِ هجرةٍ في `main`**: استُنتِجَ آخرُ مُطبَّقٍ من المخطَّطِ نفسِه (القاعدةُ بلا
سجلِّ هجراتٍ بقرارِ `ADR 0068`)، ثمَّ طُبِّقَت 12 هجرةً بـ`scripts/migrate.ts
--from` وحدَه، فصارَت جداولُ `public` **68** بعدَ **46**، وقِيسَ الاسترجاعُ
بإعادةِ الأمرِ نفسِه فنجحَ كلُّه.

وما كانَ **تخطّياً** لغيابِ `TEST_DATABASE_URL` صارَ مقيساً جزئيّاً: **397
حالةَ تكاملٍ نجحَت** على محرِّكٍ حقيقيٍّ، و**212 من 220 ساقطةً سقطَت على مهلةِ
الحالةِ لا على حكمٍ** — والسببُ رقمٌ: `select 1` من محلِّ التنفيذِ **197ms**
وسطاً. فهذا حدُّ **بيئةٍ** لا حكمُ منتجٍ، **ولم تُرفَعْ مهلةٌ ولم يُصنَّفْ ساقطٌ
تخطّياً**؛ وسلطةُ الحكمِ تبقى وظيفةَ «تكامل على PostgreSQL حقيقي» في CI.
والحاكمُ: `ADR 0098`، والدليلُ:
`docs/evidence/architecture/S-5-DB-READINESS-20260913.md`.

### زمنُ القاعدةِ — رقمٌ واحدٌ يُحاكَمُ إليهِ (زيادةٌ 2026-09-13 · `ADR 0099`)

الكلفةُ المقيسةُ إلى قاعدةِ التجربةِ هيَ **ذهابُ شبكةٍ واحدٌ** لا بطءُ محرِّكٍ:
`select 1` = 198.0ms، وعشرةُ صفوفٍ في ذهابٍ واحدٍ = 198.1ms، وعشرةُ استعلاماتٍ
متسلسلةً = 2108.8ms، والتنفيذُ داخلَ الخادمِ = 0.005ms. فالقاعدةُ في
`ap-southeast-2` والعميلُ خارجَها ⇒ **قاعدةُ إنتاجٍ تخدمُ السعوديّةَ لا تكونُ
هناكَ** بل `eu-central-1` أو `me-south-1` (`render.yaml:25`). ولم تُرفَعْ مهلةُ
اختبارٍ واحدةٍ: مهلةُ CI لم تُخرَقْ أصلاً لأنَّ خادمَها في المُشغِّلِ نفسِه.
و**لا خدمةَ حيّةً على Render اليومَ**: `waslah-gateway` و`waslah-worker`
و`ceezr` تُرَدُّ كلُّها بـ`x-render-routing: no-server` — حاجزٌ مُعلَنٌ يمنعُ
القياسَ من داخلِ الإنتاجِ.


## أوّلُ بندِ منتَجٍ يُقلَبُ `[x]` — `F2-01` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

**ما تغيّرَ في حالةِ النظامِ:** لا شيءَ في شِفرةِ الإنتاجِ. البندُ `F2-01` كانَ
مدموجاً منذُ 2026-09-12 وشِفرتُه لم تُمَسَّ بحرفٍ ههنا؛ الذي تغيّرَ أنَّ **سببَ
سقفِ الادّعاءِ** زالَ فصارَ الرمزُ في §9.5 مطابقاً للواقعِ لا متأخّراً عنه.

**السقفُ ولماذا سقطَ.** كُتِبَ في `ROADMAP.md` يومَ 2026-09-12 حرفاً: `O-1`
يُحمِرُ `verify` عندَ `city_id` و`O-2` يُحمِرُ وظيفةَ Redis، **فثلاثُ دوراتٍ
خضراءَ متتاليةٍ غيرُ ممكنةٍ** ⇒ يبقى `F2-01` بلا علامةٍ. وقد أُغلِقَ الحاجزانِ
معاً في هذه الجلسةِ (`O-1` بـ`S-2` و`O-2` بـ`S-3`)، ثمَّ حكمَتْ `main` خضراءَ
بوظائفِها الأربعِ ثلاثَ دوراتٍ: `34732986162` · `34734205137` · `34734666105`.
وكانَ للسقفِ سببٌ ثانٍ: ملفُّ تكاملِ الموافقاتِ كانَ مُعلَّقاً لغيابِ قاعدةٍ،
وبعدَ `S-5` أُجرِيَ على القاعدةِ المُدارةِ الحقيقيّةِ **تسعاً من تسعٍ · 9.97s**.

**ما صارَ مُتحقَّقاً منه.** أنَّ الموافقةَ تُسجَّلُ بختمِ **الخادمِ** لا العميلِ،
وأنَّ المدينةَ تُستنبَطُ من صفِّ المستخدمِ ولا تُمرَّرُ من الطلبِ، وأنَّ الإعادةَ
تردُّ `already_recorded` بلا صفٍّ ثانٍ وبلا تحريكِ الختمِ، وأنَّ `anon`
و`authenticated` لا ينفِّذانِ دالّتَيِ الموافقةِ. هذا حدُّ `مُتحقَّق منه`
بالسلّمِ §1.4، وليسَ `مَقيساً` ولا `مُثبَتاً`.

**ما لم يتغيّرْ ويجبُ أن يُقرأَ معَ ما فوقَه.** لا نشرَ حيَّ ألبتَّةَ: حسابُ
Render بلا خدمةٍ واحدةٍ ولا قاعدةٍ (مقيسٌ في `ADR 0099`)، **فلم يفتحِ الشاشةَ
مستخدمٌ حقيقيٌّ بعدُ**. وبوّابةُ `F2` بعيدةٌ: نصُّها رحلةٌ كاملةٌ على جهازٍ
حقيقيٍّ مسجّلةٌ بالفيديو، وبندٌ من اثنَي عشرَ لا يقترِبُ منها. والثمانيةُ
والأربعونَ نصّاً صريحاً في `apps/miniapp` وحاجزُ §9.11 الغائبُ عقدةٌ مفتوحةٌ.
وسبعةَ عشرَ حاجزاً من ثمانيةَ عشرَ ما زالَتْ مفتوحةً.

**عطلٌ أُصلِحَ في جذرِه لا تُخطّي.** شغلةُ `34734754215` على `9b7b54f` — شجرةُ
ورَقٍ لا شِفرةٍ — أخفقَتْ في `verify` الخطوةِ 8: منفذُ اختبارِ الإغلاقِ الرشيقِ
كانَ يُحسَبُ من `process.pid` فاصطدمَ بشاغلٍ آخرَ على العاملِ (`EADDRINUSE`)،
والمهلةُ كانَتْ عَرَضاً لا سبباً. صارَ المنفذُ يُسألُ منَ النظامِ (`port: 0`)
معَ إعادةٍ عندَ `EADDRINUSE` وحدَه (`ADR 0100`). **لا مهلةَ رُفِعَت ولا توكيدَ
خُفِّف ولا حالةَ صُنِّفَت تخطّياً ولا شِفرةَ إنتاجٍ مُسَّت.**

**حكمُ CI:** خضراءُ بالوظائفِ الأربعِ في `34735979680` و`34735978258` على
`ee5bef9`، مقروءاً لكلِّ وظيفةٍ وخطوةٍ من الواجهةِ البرمجيّةِ.

---

## أوّلُ سطحٍ يُبنى لا يُقرَأُ — `F2-02` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

`F2-01` كانَ إغلاقَ **قراءةٍ**: شِفرتُه مدموجةٌ من قبلُ ولم يُبنَ فيه حرفٌ. وهذا
البندُ **أوّلُ بناءٍ**: لم يكن على `main` جدولُ أماكنَ محفوظةٍ ولا بابٌ ولا شاشةُ
راكبٍ رئيسةٌ. فصارَ في الحالةِ ما لم يكن:

- **جدولُ `saved_places`** بمدينةٍ إلزاميّةٍ مرتبطةٍ بـ`cities`، وحذفٍ متسلسلٍ معَ
  صاحبِه، وقيدِ نوعٍ، وقيدِ طولِ لافتةٍ، ونقطةٍ `geography(Point,4326)`، ومُشغِّلِ
  `updated_at`، و`RLS` مفعَّلٍ بسياسةِ `service_role` وحدَه، وأربعةِ فهارسَ
  **الفريدُ منها جزئيٌّ** على `home` و`work` فيُمنَعُ منزلانِ لمستخدمٍ واحدٍ في
  المحرِّكِ لا في الشِّفرةِ. وجداولُ `public` **69** بعدَ 68.
- **ثلاثُ دوالَّ** في العقدِ الذي يقرؤه فحصُ الجهوزيةِ (٩٨ دالّةً · ٢٥ جدولاً في
  `schema-contract.ts`)، و`execute` مسحوبٌ عن `public` و`anon` و`authenticated`.
- **ثلاثةُ عقودٍ حدّيّةٍ** من §9.8: `GET/POST /v1/me/places` و
  `GET /v1/me/recent-destinations`، بسقفِ حِملٍ 1024 بايتاً.
- **شاشةُ الراكبِ الرئيسةُ** بحقلِ «إلى أين؟» وبطاقتَيِ المنزلِ والعملِ وآخرِ ثلاثِ
  وجهاتٍ وشريحتَيِ الخدمةِ وشريطِ حالةِ المدينةِ، **بلا حرفٍ عربيٍّ في المكوّنِ**
  وبتسعةٍ وأربعينَ مفتاحاً في ثلاثِ لغاتٍ.
- **حاجزٌ سادسٌ وثلاثونَ** `check-place-kinds` يُقابِلُ قائمةَ الأنواعِ في ثلاثةِ
  مواضعَ مجموعةً بمجموعةٍ، وحاجزُ `OPS-009` صارَ يعرفُ **88 ملفَّ تخطٍّ (792 حالةً)**
  بعدَ 87 (779)، وسجلُّ الحدودِ والمصفوفةُ وخطّةُ التحوّلِ **49 جدولاً** بعدَ 48.
- **القرارُ 0101** يُثبِّتُ أنَّ آخرَ الوجهاتِ **قراءةٌ من `orders` لا جدولٌ** —
  فلا مصدرَ ثانياً للحقيقةِ ينحرفُ عندَ الإلغاءِ.

### وما لم يتبدَّلْ في الحالةِ، منصوصاً

**لا نشرَ حيَّ لهذا المستودعِ**: حسابُ Render بلا خدمةٍ واحدةٍ (مقيسٌ في `ADR 0099`)،
**فهذه الشاشةُ لم يفتحْها مستخدمٌ حقيقيٌّ بعدُ**، وهذا الإعلانُ يُكرَّرُ في كلِّ
تقريرِ إغلاقٍ لا مرّةً واحدةً بأمرِ المالكِ. ولا `مَقيس` ولا `مُثبَت` (`ح-5`).
وبوّابةُ `F2` لا تُدَّعى: نصُّها رحلةٌ كاملةٌ على جهازٍ حقيقيٍّ مسجَّلةٌ بفيديو،
وبندانِ من اثنَي عشرَ لا يقترِبانِ منها. والخريطةُ المصغَّرةُ **تُعلِنُ تعذّرَها**
ولا تُرسَمُ. والثمانيةُ والأربعونَ نصّاً الصريحةُ في `apps/miniapp` وحاجزُ §9.11
الغائبُ عقدةٌ منفصلةٌ لم تُمَسَّ. والحواجزُ البيئيّةُ الخمسةُ غيرُ الخضراءِ
(`check-coverage-gate` · `check-performance-budget` · `check-single-origin-assets` ·
`check-core-contract-freshness` بـ`O-6` · `verify-container-build.sh`) باقيةٌ على
حالِها **مُعلَنةً لا مُسكَتةً**.

## أوّلُ حكمٍ جغرافيٍّ يُسنَدُ إلى المحرِّكِ — `F2-03` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

**ما تغيَّرَ في حالةِ النظامِ.** صارَ للنظامِ، أوّلَ مرّةٍ، **جوابٌ عن سؤالِ
«أينَ نخدمُ؟»** مكتوبٌ في القاعدةِ لا في رأسِ أحدٍ: جدولُ `city_service_areas`
بهندسةٍ `geography(MultiPolygon,4326)` وإصدارٍ نصّيٍّ، وصفٌّ مفعَّلٌ واحدٌ لمدينةٍ
**يفرضُه فهرسٌ فريدٌ جزئيٌّ**. وصارَ له **دليلُ معالمَ يملكُه** —
`destination_landmarks`، عشرةُ صفوفٍ لجدةَ ولكلِّ صفٍّ مصدرُه مكتوبٌ في نصِّ
الهجرةِ — بديلاً عن مُرمِّزٍ جغرافيٍّ خارجيٍّ. وصارَ للراكبِ بابانِ:
`GET /v1/destinations/search` و`POST /v1/destinations/resolve`، مُسجَّلانِ
بالإضافةِ في §10.2 من الخارطةِ.

**وأثقلُ ما في هذا البندِ ليسَ الجدولَينِ، بل أنَّ التطبيعَ العربيَّ صارَ دالّةً
في المحرِّكِ.** «جدة» و«جِدَّة» و«جده» مدخلٌ واحدٌ في ثلاثِ صورٍ، فلو طُبِّعَ في
تايبسكربت وحدَه لَبَقِيَ الفهرسُ على النصِّ الخامِّ ولَما طابقَ البحثُ شيئاً.
فالآنَ: دالّةٌ `immutable`، ومنها عمودٌ **مُولَّدٌ `always` مُخزَّنٌ**، وعليه
فهرسٌ. ونسخةُ النطاقِ تُطابِقُها **بحاجزَينِ لا بحاجزٍ**: ساكنٍ يُقابِلُ جدولَ
الطيِّ (38 محرفاً) بوسيطَي `translate` **محرفاً بمحرفٍ**، وحيٍّ يُشغِّلُ
المحرّكَينِ على معجمٍ ويُقابِلُ **المخرَجَ بالمخرَجِ**. ولا تُقرأُ إحداهما
إثباتاً للأخرى: الأولى تُقابِلُ نصَّينِ، والثانيةُ سلوكَينِ.

**والحكمُ على الاحتواءِ في القاعدةِ** (القاعدة 0.5): `resolve_destination` تقرأُ
مدينةَ صاحبِ الحسابِ **من صفِّه** لا من الطلبِ، وتحكمُ بـ`st_covers` في عبارةٍ
واحدةٍ. **والرفضُ رمزٌ في حمولةٍ** لا استثناءٌ، لأنَّ «خارجَ منطقةِ الخدمةِ»
جوابٌ صحيحٌ لا عطبٌ؛ و`USER_NOT_FOUND` مفصولٌ عنه؛ و`(0,0)` يُرفَضُ **بالحدِّ لا
بالشكلِ**.

**وحدُّ الصدقِ الجديدُ، مُعلَنٌ في الهجرةِ نفسِها:** غلافُ جدةَ
`jed-envelope-v1` **مستطيلٌ تقريبيٌّ** (2408.56 كم²) **لا حدٌّ بلديٌّ رسميٌّ** —
مكتوبٌ في تعليقِ الصفِّ وفي `area_version`، لأنَّ من يقرأُ «منطقةُ خدمةِ جدةَ»
بعدَ سنةٍ يجبُ أن يعلمَ أنَّها مستطيلٌ. واستبدالُه حينَ يأتي الحدُّ الرسميُّ
**إضافةُ إصدارٍ** لا تعديلُ صفٍّ يمحو تفسيرَ قبولٍ سابقٍ. **ولا مطابقةَ تقاربٍ**
(`pg_trgm` و`unaccent` غيرُ منصَّبَينِ على القاعدةِ المقيسةِ) — دَينٌ مُعلَنٌ في
`ADR 0102` §6 لا ثغرةٌ صامتةٌ.

**وسجلّاتُ الحقيقةِ الأربعةُ لحقَت بالجدولَينِ** لأنَّ حاجزَينِ أخفقا بحقٍّ:
الاستبقاءُ (`F7-06`) وجردُ الحدودِ (`W-1`) ومصفوفةُ الهجرةِ (`W-2`) وعقدُ
المخطَّطِ — فصارَ الجردُ **51 جدولاً** والعقدُ **100 دالّةً**. ولولاها لَأعلنَ
فحصُ الجهوزيةِ جهوزيّةً عن سطحٍ لا يعلمُه.

**والسقفُ نفسُه يبقى مُعلَنًا، ويُكرَّرُ لا يُختصَرُ: لا نشرَ حيَّ** (`ADR 0099`)
**فهذه الشاشةُ لم يفتحْها مستخدمٌ حقيقيٌّ ولا وجهةَ اختارَها راكبٌ**؛ وبوّابةُ
`F2` **غيرُ مُدَّعاةٍ**؛ ولا `مَقيس` ولا `مُثبَت` (`ح-5`). **ونصفُ البندِ
المرئيُّ غيرُ مُنجَزٍ**: لا أساسَ خرائطَ في العميلِ (`ADR 0007`)، والدبّوسُ
هندسةٌ بلا صورةٍ، و`.rd__map--off` **حدٌّ متقطِّعٌ** لا مستطيلٌ رماديٌّ يُوهِمُ
خريطةً. **وسُجِّلَ `UX-021`**: أصنافُ `.rh__*` في شاشةِ `F2-02` بلا CSS مقابلٍ —
**مفتوحٌ غيرُ مُصلَحٍ**، لأنَّ إصلاحَه تعديلُ سطحٍ خارجَ النطاقِ المحجوزِ. والحواجزُ
البيئيّةُ الخمسةُ غيرُ الخضراءِ ووظيفةُ Redis الحقيقيِّ (`O-2`) باقيةٌ على حالِها
**مُعلَنةً لا مُسكَتةً**.

## أوّلُ حكمِ اقتباسٍ — والأجرةُ مُجمَّدةٌ آلةً لا نيّةً — `F2-04` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

صارَ في المستودَعِ **حكمُ اقتباسٍ** يُجيبُ الراكبَ قبلَ أن يطلبَ: مسافةٌ **موسومةٌ**
`{kind,meters}` من `st_distance` على `geography` **في القاعدةِ** (القاعدة 0.5)، ومدّةٌ
إمّا من محرِّكِ توجيهٍ حقيقيٍّ وإمّا **امتناعٌ مُصنَّفٌ**، وبطاقاتُ الخدماتِ
المخدومةِ في مدينتِه بحكمٍ واحدٍ يجمعُ التوثيقَ والاشتراكَ النافذَ والقدرةَ
المُفعَّلةَ. والنقطةُ `POST /v1/quote/ride` — **لا** `/v1/rides/quote` — لأنَّها
**قراءةٌ محضةٌ لا تُنشئُ صفّاً**، وذلكَ مقيسٌ بعدِّ الصفوفِ قبلَها وبعدَها.

**وأهمُّ ما تغيَّرَ في حالةِ النظامِ ليسَ الشاشةَ بل الإنفاذَ:** `ADR 0039` §٤ كانَ
منذُ شهرٍ **نصّاً يُقرأُ** يحجبُ آليةَ الأجرةِ على `DEC-11`، وصارَ الآنَ **حاجزاً
يُسقِطُ CI**: `scripts/check-quote-contract.ts` بثمانِ قواعدَ يمسحُ شريحةَ الاقتباسِ
كلَّها، فلفظُ أجرةٍ أو وسيلةِ دفعٍ في الشِّفرةِ **يُوقِفُ الدفعَ** — بتخصيصٍ مُعلَنٍ
لاشتراكِ السائقِ (`ADR 0027`). و`م13-7` يُجمِّدُ **حتّى الهياكلَ التمهيديّةَ**، فلا
حقلَ ولا عمودَ ولا رايةً ولا `fare: null`. **ولذا البندُ `[~]` لا `[x]`: نصفُ نصِّ
`SR-04` محجوبٌ تنظيميّاً ويبقى مكتوباً باسمِه.**

**والقياسُ على محرِّكٍ حقيقيٍّ لا في الذاكرةِ**: `tests/integration/quote.test.ts`
**21 من 21 · 0 إخفاقٍ · 48 توكيداً**، وفيها أنَّ المسافةَ **جيوديسيّةٌ** قُوبِلَت
بـ`st_distance` نفسِها (فرقٌ أقلُّ من مترٍ) وقُوبِلَت بالمستويّةِ **بالدرجاتِ**
فيُكشَفُ السهوُ بثلاثِ مراتبَ، وأنَّ رفضَ الانطلاقِ ورفضَ الوجهةِ **رمزانِ
مستقلّانِ**، وأنَّ كلَّ شرطٍ من شروطِ الخدمةِ **نُزِعَ وحدَه فقِيسَ أثرُه**. وعلى
الخادمِ نفسِه `tests/unit/quote-route.test.ts` **19 من 19 · 73 توكيداً**، وفيها أنَّ
الهويّةَ تُقرأُ من **الرمزِ الموقَّعِ** ومعرّفٌ كاذبٌ في الجسمِ وترويسةٌ كاذبةٌ
**لا يُقرآنِ**، وأنَّ مفاتيحَ الجوابِ تُقارَنُ **مجموعةً بمجموعةٍ** فحقلٌ زائدٌ
يُسقِطُ التوكيدَ.

**وحُمرةٌ تُسجَّلُ لأنَّها درسٌ في صدقِ القياسِ**: كانَت نسخةٌ قديمةٌ من `quote_ride`
مُطبَّقةً على قاعدةِ الاختبارِ بمفاتيحِ حمولةٍ أخرى، فأخفقَت **16 من 21** عندَ أوّلِ
تشغيلٍ. أي أنَّ قياساً سابقاً جرى على **دالّةٍ ليسَت هيَ الملفَّ** — وذاكَ أخطرُ من
الحمرةِ لأنَّه أخضرُ كاذبٌ. والعلاجُ: **إعادةُ تطبيقِ الهجرةِ قبلَ القياسِ**
(جولتَينِ، فكانَ التماثُلُ مقيساً) لا تخفيفُ توكيدٍ.

**وعدّادُ التخطّي رُفِعَ يداً 89←90 ملفّاً و814←835 حالةً**، والمدخلُ سُجِّلَ **قبلَ
أوّلِ دفعةٍ** لا بعدَ أن يكشفَه حكمُ CI. وسجلّاتُ الحقيقةِ: **لا جدولَ جديدَ** فلم
يتغيَّر جردُ الحدودِ ولا مصفوفةُ الهجرةِ ولا سياسةُ الاستبقاءِ (51 جدولاً)، وعقدُ
المخطَّطِ صارَ **101 دالّةً** — و`city_served_services` **ليسَت فيه عن صوابٍ**: لا
يناديها كودُ تشغيلٍ، تناديها `quote_ride` وحدَها داخلَ المحرِّكِ.

**والسقفُ نفسُه يبقى مُعلَنًا ويُكرَّرُ لا يُختصَرُ: لا نشرَ حيَّ** (`ADR 0099`)
**فلا راكبٌ رأى اقتباساً ولا سائقٌ ظهرَ في بطاقةٍ لأحدٍ**؛ وبوّابةُ `F2` **غيرُ
مُدَّعاةٍ**؛ ولا `مَقيس` ولا `مُثبَت` (`ح-5`). **والمدّةُ تعودُ `NOT_CONFIGURED` في
كلِّ بيئةٍ اليومَ** — لا محرِّكَ توجيهٍ مُستضافٌ، واستضافتُه **عملُ مشغِّلٍ**
(`ADR 0024`) — وهذا الصدقُ لا العيبُ. **وملاحظةُ السائقِ غيرُ مبنيّةٍ** (أُخرِجَت
إلى `F2-05`: حقلُ إدخالٍ لا يُحفَظُ كذبٌ). **و`UX-021` لا يزالُ مفتوحاً** ولم
يُصلَحْ ههنا: علاجُه الجذريُّ حاجزٌ يُقابِلُ أصنافَ JSX بمُحدِّداتِ CSS، وهوَ زيادةٌ
مستقلّةٌ لها حجزُها.


## أوّلُ رحلةٍ تُطلَبُ — ومفتاحُ التكرارِ قيدُ مخطَّطٍ لا اتّفاقُ تطبيقٍ — `F2-05` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

صارَ في المستودَعِ **طلبُ رحلةٍ حقيقيٌّ**: نداءٌ واحدٌ ذرّيٌّ (`request_ride`)
يجمعُ التحقُّقَ والكتابةَ في معاملةٍ واحدةٍ بقفلِ صفِّ الراكبِ، ويُنشئُ الطلبَ
بحالةِ `searching` و`broadcast_round = 0`. **وأهمُّ ما تغيَّرَ في حالةِ النظامِ
أنَّ `ARCH-006` صارَ قيداً في المخطَّطِ لا اتّفاقاً**: فهرسٌ فريدٌ **جزئيٌّ
متوازٍ** على `(rider_id, idempotency_key)` — فضغطتانِ على زرٍّ واحدٍ **لا
تُنتِجانِ رحلتَينِ** ولو وصلَتا في اللحظةِ نفسِها. **وذلكَ قِيسَ سباقاً لا نصّاً**:
نداءانِ متزامنانِ بمفتاحٍ واحدٍ ⇒ صفٌّ واحدٌ و`reused:true` في واحدٍ منهما
بالضبطِ، وبمفتاحَينِ مختلفَينِ ⇒ صفٌّ واحدٌ ورفضٌ ثانٍ `ACTIVE_RIDE_EXISTS` يحملُ
**معرَّفَ الرحلةِ القائمةِ وحالتَها** فيُبنى عليهما فعلٌ لا شاشةٌ مسدودةٌ.
والمفتاحُ يُقرأُ من **ترويسةِ نقلٍ** لا من جسمِ الطلبِ، **ولا يختلقُه الخادمُ عندَ
غيابِه** — فمفتاحٌ مُولَّدٌ لكلِّ نداءٍ يُبطِلُ `ARCH-006` وهوَ يتظاهرُ بتطبيقِه
(`ADR 0104`).

**وثانيةُ ما تغيَّرَ أنَّ حاجزاً قائماً أسقطَ العملَ فقُدِّمَ عليه، ولم يُستَثنَ**:
أوّلُ نسخةٍ من شاشةِ البحثِ كانَت تقرأُ كلَّ خمسِ ثوانٍ وتدقُّ عقربَ ثوانٍ كلَّ
ثانيةٍ، فأسقطَها `scripts/check-system-screens-policy.ts` بموضعَيْ `setInterval`
(`ADR 0035` §٤ · القسم 9.7). **فتغيَّرَ التصميمُ لا الحاجزُ**: القراءةُ تُطلَبُ
بزرٍّ ظاهرٍ ما دامَ البحثُ جارياً، والمدّةُ تُقرأُ **لحظةَ الرسمِ** فلا حالةَ
تدقُّ، **وأُعلِنَ للراكبِ نصّاً أنَّ الرقمَ لقطةٌ** لا بثٌّ. ولم يُحوَّلْ
`setInterval` إلى `setTimeout` مُتسلسِلٍ — وذاكَ **نقلُ المشكلةِ** لا حلُّها.
**والحاجزُ زِيدَ**: قاعدةٌ حاديةَ عشرةَ في `check-ride-request-contract.ts` تُسقِطُ
البناءَ إن خلَت الشاشةُ من زرِّ قراءةٍ أو من إعلانِ اللقطةِ — لأنَّ المنعَ وحدَه
كانَ يسمحُ بعددٍ جامدٍ إلى الأبدِ بلا بابِ سؤالٍ. **والمفتاحُ
`rider.search.stopped` يبقى في القواميسِ الثلاثةِ** ولا يُعرَضُ، والسببُ مكتوبٌ
تعليقاً في الحاجزِ (`ح-1`) — كما بقيَ `rider.quote.next.notBuilt` في `F2-04`.

**والصدقُ في الحالةِ المعروضةِ**: عددُ السائقينَ المُخطَرينَ **مشتقٌّ من صفوفِ
العروضِ** لا من عدّادٍ يُزادُ في التطبيقِ، **والصفرُ يُنشَرُ صفراً** ويُقالُ نصّاً
«لم يُبلَّغْ سائقٌ بعدُ» (`ADR 0023`: الصمتُ ليسَ رفضاً). وطورُ البحثِ مشتقٌّ لا
محفوظٌ (القاعدة 0.6). وزرُّ الإلغاءِ **لا يُعرَضُ إلّا برايةٍ من القاعدةِ**
(`cancellableWithoutPenalty`)، **وهيَ حكمُ زمنٍ لا حكمُ مالٍ**.

**والقياسُ على محرِّكٍ حقيقيٍّ**: `tests/integration/ride-request.test.ts`
**22 من 22 · 0 إخفاقٍ · 87 توكيداً**، والفهرسُ يُقرأُ من `pg_indexes` لا من نصِّ
الهجرةِ، و`execute` مسحوبٌ بـ`has_function_privilege` و`aclexplode`. وعلى الخادمِ
نفسِه `tests/unit/ride-request-route.test.ts` **28 من 28 · 85 توكيداً**، ومعَها
**مِلفُّ الحاجزِ 35 حالةً** و`ride-request-store.test.ts` **19** و
`ride-request-domain.test.ts` **16** و`ride-search-view.test.ts` **19**.
**وثلاثُ حُمَرٍ من قاعدةٍ حقيقيّةٍ قُوبِلَت بتصحيحِ البذرةِ لا بتعطيلِ القيدِ**
(`orders_matched_requires_driver` · `order_offers.expires_at` · مفتاحُ
`audit_log.actor_user_id`).

**وسجلّاتُ الحقيقةِ**: **لا جدولَ جديدَ** فلم يتغيَّر جردُ الحدودِ ولا مصفوفةُ
الهجرةِ ولا سياسةُ الاستبقاءِ (51 جدولاً)، وعقدُ المخطَّطِ صارَ **104 دالّةً**
(101←104). وعدّادُ التخطّي رُفِعَ يداً **90←91 ملفّاً و835←857 حالةً** والمدخلُ
سُجِّلَ **قبلَ أوّلِ دفعةٍ**.

**والسقفُ نفسُه يبقى مُعلَنًا ويُكرَّرُ لا يُختصَرُ: لا نشرَ حيَّ** (`ADR 0099`)
**فلم يطلبْ راكبٌ حقيقيٌّ رحلةً ولا رأى سائقٌ إخطاراً**؛ وبوّابةُ `F2` **غيرُ
مُدَّعاةٍ**؛ ولا `مَقيس` ولا `مُثبَت` (`ح-5`). **ولا أجرةَ ولا وسيلةَ دفعٍ ولا
عقوبةَ إلغاءٍ ولا خانةً لها** ولا `fare: null` (`ADR 0039` §٤ · `م13-7`). **ولا
بثَّ ولا إسنادَ**: `broadcast_round` يُنشَأُ صفراً وملؤُه آلةُ حالاتِ `F3`. **ولا
نقلَ فوريَّ**: الرقمُ لقطةٌ عندَ آخرِ سؤالٍ **وذلكَ مُعلَنٌ في الشاشةِ**. **وحوارُ
البوتِ لا يزالُ يُدخِلُ `orders` بلا مفتاحٍ** — دَينٌ **مُعلَنٌ** ولذلكَ كانَ
الفهرسُ جزئيّاً، ومسارُ الشبكةِ **مُحرَّمٌ عليه** استيرادُ ذاكَ الكاتبِ بحاجزٍ.
**و`UX-021` لا يزالُ مفتوحاً** ولم يُصلَحْ ههنا.


## صنفٌ بلا قاعدةٍ صارَ حُمرةً في CI لا عَرْضاً عارياً — `UX-021` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

**والنصوصُ أعلاهُ التي تقولُ «`UX-021` لا يزالُ مفتوحاً» صحيحةٌ لوقتِها وتبقى
مكتوبةً**؛ وهذا القسمُ يُقرأُ بعدَها: البندُ أُغلِقَ في جذرِه.

**ما كانَ:** ورقةُ النمطِ الوحيدةُ `apps/miniapp/src/styles/global.css` لا تحملُ
قاعدةً لـ18 صنفاً تُصدِرُها شاشةُ `F2-02`، ولا لـ`.rd__map` في سطحِ `F2-03`. فكانت
الرئيسةُ تُرسَمُ بأنماطِ المتصفِّحِ الافتراضيّةِ، وحاوي الخريطةِ **بارتفاعِ صفرٍ
فلا يُرى أصلاً**. **ولم يكنْ في المستودَعِ فحصٌ يرى ذلكَ**: لا `tsc` ولا مُدقِّقٌ
ولا بناءٌ ولا مُختبَرٌ يقرأُ البنيةَ — العطبُ يظهرُ في عينِ مستخدمٍ وحدَها، ولذلكَ
تكرَّرَ في سطحَينِ متعاقبَينِ.

**ما صارَ:** حاجزٌ في `verify` (خطوتانِ مُسمّاتانِ) وفي سلسلةِ `ci` يقابلُ أصنافَ
العرضِ بمُحدِّداتِ النمطِ **في الاتجاهَينِ**، ويرفضُ تعبيرَ صنفٍ لا يُحَلُّ ساكناً
كي لا يُخدَعَ القياسُ نفسُه، ويوجِبُ لكلِّ قاعدةٍ زالَ مُصدِرُها **سبباً ومالكاً
وما أحلَّها** بدلاً من محوِها (`ح-1`) أو إسكاتِها. وكُتِبَت الأنماطُ الغائبةُ
**إضافةً** بلغةِ التصميمِ القائمةِ (`var(--tg-*)`)، **وبنيةُ الشاشةِ لم تُمَسَّ**.
القرارُ: `ADR 0105`. الدليلُ: `docs/evidence/architecture/UX-021-CLOSURE-20260913.md`.

**والسقفُ:** المقيسُ **وجودُ** النمطِ لا ذوقُه. **لا جمالٌ يُدَّعى، ولا وصولٌ
مقيسٌ** (لا أداةَ `a11y` في CI ولا قياسَ تضادٍّ)، **ولا نشرَ حيَّ** (`ADR 0099`)
فلم يفتحْ راكبٌ هذه الشاشةَ، وبوّابةُ `F2` **غيرُ مُدَّعاةٍ**. **و48 نصّاً صريحاً
في `apps/miniapp` وحاجزُ §9.11 الغائبُ دَينٌ لم يُمَسَّ ههنا.**


## الرحلةُ النشطةُ تُقرأُ لقطةً، والموقعُ لا يُنشَرُ بلا عُمرِه — `F2-06` (2026-09-13 · إضافةٌ لا محوٌ · `ح-8`)

**والنصوصُ أعلاهُ التي تقولُ «ولا `F2-06`» صحيحةٌ لوقتِها وتبقى مكتوبةً**؛ وهذا
القسمُ يُقرأُ بعدَها: **نصفُ البندِ المشروعُ** بُنيَ، والنصفُ الآخرُ **مُسمّى
محجوباً**.

**ما كانَ:** لا مسارَ يقرأُ رحلةً واحدةً بحالتِها. `F2-05` أنتجَ
`GET /v1/rides/:id/search` — وهوَ **حالةُ بحثٍ** قبلَ الإسنادِ لا رحلةً نشطةً.
وبعدَ `claim_ride` (هجرةُ 2026-08-06 تضعُ `status='matched'` و
`assigned_driver_id`) كانَ الراكبُ **بلا شاشةٍ يرى فيها من أُسنِدَ إليه**.

**ما صارَ:** دالّةُ لقطةٍ واحدةٌ في القاعدةِ `active_ride_snapshot(bigint, uuid)`
— `stable security invoker` · `set search_path = public` — **تُجيبُ الأسئلةَ
الثلاثةَ في استفسارٍ واحدٍ**: هل هذه رحلةُ هذا الراكبِ (المِلكيّةُ **قيدُ
الاستفسارِ** `users → riders → orders`، وغيرُ المالكِ يُرَدُّ `ORDER_NOT_FOUND`
**لا `FORBIDDEN`** كي لا يصيرَ المسارُ عدَّادَ معرّفاتٍ) · هل يُعرَضُ سائقٌ
أصلاً (**في حالتَي `matched` و`in_progress` وحدَهما**، فرحلةٌ أُلغِيَت بعدَ
إسنادٍ يبقى سائقُها مكتوباً في صفِّها **ولا يُقرأُ**) · وكم عُمرُ نقطتِه
(**تقيسُه القاعدةُ بساعتِها** لا ساعةُ الجهازِ). ومسارٌ واحدٌ
`GET /v1/rides/:id`، ونطاقٌ نقيٌّ يملكُ حدَّ ٩٠ ثانيةً وتصنيفَ الحجبِ وسياسةَ
الإلغاءِ، وشاشةٌ في `apps/miniapp/src/surfaces/rider/active/`.

**والقاعدةُ الحاكمةُ: نقطةٌ بلا عُمرِها شاهدٌ كاذبٌ.** `BUG-001` مفتوحٌ (كتاباتُ
`drivers.last_location` غيرُ مرتَّبةٍ)، فالعُمرُ يُنشَرُ بالثواني مع كلِّ نقطةٍ،
**والحجبُ في السلكِ لا في نيّةِ العميلِ**: فوقَ الحدِّ أو بلا ختمٍ **لا تُغادِرُ
الإحداثيّةُ الخادمَ** ويُنشَرُ سببُ الحجبِ مُصنَّفاً. والتقييمُ الذي لم يُوجَدْ
بعدُ يُنشَرُ عَدَماً وعدُّه صفراً — **لا «٥٫٠»** (`ADR 0023`). ومدّةُ الوصولِ من
`estimateArrival` القائمةِ، وامتناعُها **مُصنَّفٌ** لا شَرطةٌ.

**والإنفاذُ آليٌّ:** `scripts/check-active-ride-contract.ts` بخمسِ قواعدَ
(`UX-022`) ولكلِّ واحدةٍ **حالةٌ سالبةٌ مزروعةٌ** (١٨ حالةً · `ح-7`)، خطوتانِ
مُسمّاتانِ في `verify` وحلقةٌ في سلسلةِ `ci`. وعقدُ المخطَّطِ زادَ دالّةً واحدةً
(`active_ride_snapshot` في `CONTRACT_FUNCTIONS`) **ولا جدولَ جديدَ**، فلم تتغيَّرْ
سياسةُ الاستبقاءِ ولا جردُ الحدودِ ولا مصفوفةُ الهجرةِ.

**وعطبٌ كشفَه القياسُ على قاعدةٍ حقيقيّةٍ ويُسجَّلُ لأنَّه درسٌ لا لأنَّه مريحٌ:**
سجلٌّ (`record`) لم يُسنَدْ إليه شيءٌ **لا يجوزُ ذِكرُ حقلٍ منه في استعلامِ
الإرجاعِ ولو في فرعٍ لا يُسلَكُ** (`55000`)، فكانَت **كلُّ رحلةٍ بلا سائقٍ مُسنَدٍ
تُسقِطُ الدالّةَ** — و٨٢ اختباراً بمحرِّكٍ مُصنَّعٍ لم تَرَ ذلكَ. أُصلِحَ
**السببُ** وأُعيدَ القياسُ: **20/20** على PostgreSQL حقيقيٍّ.

**والسقفُ، بأسمائِه:** **لا طوارئَ ولا مشاركةَ رحلةٍ ولا اتصالَ ولا صورةَ سائقٍ
ولا باركودَ** — موعِدُها `F2-09` و`F2-10`/`F8-05` وما لا عمودَ له في المخطَّطِ لا
يُخترَعُ له حقلٌ؛ **ولا تُرسَمُ أزرارُها رمادِيّةً** فوعدُ سلامةٍ كاذبٌ أخطرُ من
غيابٍ مُصرَّحٍ. **ولا طَورَ «وصلَ السائقُ»** فلا عمودَ `arrived_at`. **ولا
تتبُّعَ حيَّ**: لقطةٌ بطلبٍ وزرُّ تحديثٍ يملكُه الراكبُ (`ADR 0035` §٤ · القسم
9.7). **ولا أجرةَ ولا وسيلةَ دفعٍ ولا عقوبةَ إلغاءٍ ولا خانةً لها** (`ADR 0039`
§٤ · `م13-7`). **ولا وصولَ (`a11y`) مقيسٌ ولا تضادَّ لونيٍّ.** **ولا نشرَ حيَّ**
(`ADR 0099`) فلم يفتحْ راكبٌ حقيقيٌّ هذه الشاشةَ ولا تابعَ سائقاً على جهازٍ،
**وبوّابةُ `F2` غيرُ مُدَّعاةٍ**. والقرارُ: `ADR 0106`. والدليلُ:
`docs/evidence/architecture/F2-06-CLOSURE-20260913.md`.

### دالّةٌ تُنشَأُ ولا يُنزَعُ تنفيذُها منالٌ للمفتاحِ العامِّ — قاعدةٌ ساكنةٌ الآنَ

منحُ `execute` لدورِ `public` **ضمنيٌّ** في `postgres`: كلُّ دالّةٍ جديدةٍ
منفَّذةٌ حتّى يُنزَعَ ذلكَ صراحةً. وقد أسقطَ CI الدفعةَ الأولى من `F2-06` لأنَّ
`active_ride_snapshot` بقيَت منالاً لـ `anon` و`authenticated`
(`tests/integration/database-privilege-surface.test.ts` — الطبقةُ ٢).

والإصلاحُ لم يقتصرْ على السطرِ: صارَ لحاجزِ `UX-022` **قاعدةٌ سادسةٌ ساكنةٌ**
تقرأُ نصَّ الهجرةِ فتُسقِطُ كلَّ دالّةٍ تُنشَأُ بلا `revoke execute … from
public, anon, authenticated` — بالأدوارِ الثلاثةِ مُسمّاةً، وبلا مرورٍ على
قائمةٍ فارغةٍ، ولها خمسُ حالاتٍ سالبةٍ مزروعةٍ. **فالنسيانُ هوَ الحالةُ
الافتراضيّةُ** ههنا، وحاجزٌ لا يمسكُ الافتراضيَّ لا يحجُزُ شيئاً.

### شاشةُ نهايةِ الرحلةِ: القاعدةُ تحسبُ وتحكمُ، والسطحُ يقرأُ (`F2-07` · 2026-09-13)

انتهاءُ الرحلةِ صارَ سطحاً وطبقاتٍ: `completed_ride_summary(bigint, uuid) →
jsonb` **`stable security invoker`** تُخرِجُ الملخَّصَ في استفسارٍ واحدٍ
والمِلكيّةُ **قيدٌ في الاستعلامِ** لا فحصٌ في التطبيقِ (غيرُ المالكِ يُرَدُّ
`ORDER_NOT_FOUND`)، و`submit_rating_with_tags(uuid, bigint, smallint, text,
text[])` **`security definer`** تكتبُ النجومَ والوسومَ، و`GET
/v1/rides/:id/summary` و`POST /v1/rides/:id/rating`، وشاشةٌ في
`apps/miniapp/src/surfaces/rider/summary/`.

**والقاعدةُ الحاكمةُ: نافذةُ التقييمِ ساعةُ القاعدةِ، ومُعجَمُ الوسومِ قيدُها.**
المدّةُ فرقُ ختمَينِ **بساعةِ القاعدةِ** لا بساعةِ الجهازِ، والأهليّةُ `now()`
مقابلَ `rating_prompt_window_hours` — **والشاشةُ تقرأُ الحكمَ ولا تحكمُ**، فلا
بابٌ يُفتَحُ ثمَّ يُرفَضُ ولا نافذةٌ للزينةِ. والوسومُ `text[]` بقيدِ
`ratings_tags_vocabulary` يقرأُ دالّةً **ثابتةً**، **فالكاتبُ المباشرُ محكومٌ
كالدالّةِ**، ومصفوفةٌ فارغةٌ تُخزَّنُ `null`. **ورفضُ الوسمِ المجهولِ يسبقُ أيَّ
قراءةِ صفٍّ** لأنَّ ترتيبَ الفحوصِ سطحٌ للمعلوماتِ. **ومنطقُ النافذةِ واحدٌ**:
`submit_rating` القائمةُ صارَت **سطرَ تفويضٍ** إلى نسخةِ الوسومِ (القاعدة 0.6)،
ومقيسٌ أنَّ جسمَها لا يحملُ لفظَ الإعدادِ. **والعَدَمُ يُنشَرُ عَدَماً** (`ADR
0023`): ختمٌ ناقصٌ ⇒ مدّةٌ `null`، ووجهةٌ غائبةٌ ⇒ وترٌ `null` — لا «٠ د» ولا «٠
متراً».

**والإنفاذُ آليٌّ:** `scripts/check-ride-summary-contract.ts` بستِّ قواعدَ (لا
لفظَ مالٍ · لا مسافةَ بلا وسمِ استقامةٍ · لا صفرَ بدلَ عَدَمٍ · الأهليّةُ من
الخادمِ · تطابقُ مفاتيحِ القواميسِ الثلاثةِ · لا لفظَ لمسارٍ لم يُبنَ) **ولكلِّ
واحدةٍ حالةٌ سالبةٌ مزروعةٌ** (29 حالةً · `ح-7`)، وخطوتانِ مُسمّاتانِ في
`verify` وحلقةٌ في سلسلةِ `ci`. وعقدُ المخطَّطِ زادَ **دالّتَينِ** ولا جدولَ
جديدَ، فلم تتغيَّرْ سياسةُ الاستبقاءِ ولا جردُ الحدودِ. **ودرسُ `F2-06`
نُفِّذَ**: `execute` نُزِعَ عن `public` و`anon` و`authenticated` **للدوالِّ
الأربعِ** ومقيسٌ بـ`has_function_privilege`.

**وعطبٌ كشفَته الحالاتُ السالبةُ في الحاجزِ نفسِه:** مقابلةُ الألفاظِ المحجوبةِ
**بحدودِ الكلمةِ** (موروثةٌ من `UX-022`) كانَت **تُجيزُ `openSupportTicket`**
وتمسكُ `support ticket` بفراغٍ — **تمنعُ ما لا يُكتَبُ وتُجيزُ ما يُكتَبُ**؛
فأُصلِحَ الجذرُ بفصلِ حدودِ الحالةِ، **مع حالةٍ سالبةٍ ثانيةٍ** تقيسُ أنَّ
`sharedState` لا يُقرأُ `share` كي لا يُعالَجَ عطبٌ بإفراطٍ.

**وعطبٌ في المقياسِ لا في المَقيسِ يُسجَّلُ لأنَّه درسٌ:** جريةُ `bun test`
لمُجلَّدِ التكاملِ **تبقى حيّةً** بعدَ سقوطِ أمرِ الصدَفةِ بمهلتِه، وفيها
ملفّاتٌ تُنفِّذُ `truncate … cascade` في `beforeEach`؛ فسمَّمَت كلَّ قياسٍ تالٍ
على القاعدةِ نفسِها بمفاتيحَ أجنبيّةٍ مفقودةٍ — **حتّى «قياسَ ضبطٍ» بُنيَ عليه
استنتاجُ «تعارضِ ملفّاتٍ» لا أصلَ له**. **فقبلَ أن يُنسَبَ إخفاقٌ إلى شِفرةٍ،
تُفحَصُ العمليّاتُ الحيّةُ على القاعدةِ.**

**والسقفُ، بأسمائِه:** **لا مبلغَ ولا وسيلةَ دفعٍ ولا إيصالَ مالٍ ولا خانةً
محجوزةً لها** (`ADR 0039` §٤ · `م13-7` · `DEC-11`) — والإيصالُ ههنا **ملخَّصُ
رحلةٍ لا فاتورةٌ**. **ولا مسافةَ مقطوعةً**: لا عمودَ ولا أثرَ مسارٍ، فالمنشورُ
**وترٌ مستقيمٌ باسمِه** ونسبةُ الانعراجِ المقيسةُ **1.124–2.834** (`ADR 0024`).
**ولا وصلَ لتذكرةِ دعمٍ**: `open_support_ticket` قائمةٌ في القاعدةِ وسطحُها
نطاقُ `F6`، **والحاجزُ يمنعُ لفظَها** كي لا يُوصَلَ نصفَ وصلٍ. **ولا تقييمَ
سائقٍ للراكبِ** (`SD-09`). **ولا مشاركةَ ولا طوارئَ** (`F2-09` · `F2-10`). **ولا
وصولَ (`a11y`) مقيسٌ ولا تضادَّ لونيٍّ.** **ولا نشرَ حيَّ** (`ADR 0099`) فلم يرَ
راكبٌ حقيقيٌّ هذه الشاشةَ ولم يُقيِّمْ سائقاً، **وبوّابةُ `F2` غيرُ مُدَّعاةٍ**.
والقرارُ: `ADR 0107`. والدليلُ:
`docs/evidence/architecture/F2-07-CLOSURE-20260913.md`.

### سجلُّ الرحلاتِ وتفاصيلُها: مفتاحٌ لا إزاحةٌ، ومنطقةٌ مُسمّاةٌ لا إزاحةُ ساعةٍ (`F2-08` · 2026-09-14)

سجلُّ الراكبِ صارَ سطحاً وطبقاتٍ: `rider_ride_history(bigint, text,
timestamptz, uuid, integer) → jsonb` **`stable security invoker`** تُخرِجُ
الصفحةَ في استفسارٍ واحدٍ، و`rider_ride_detail(bigint, uuid) → jsonb` تُخرِجُ
التفاصيلَ وسِلسِلةَ الأحداثِ، و`GET /v1/rides` و`GET /v1/rides/:id/detail`،
وشاشتانِ في `apps/miniapp/src/surfaces/rider/history/`. والمِلكيّةُ — ههنا
أيضاً — **قيدٌ في الاستعلامِ** لا فحصٌ في التطبيقِ.

**والقاعدةُ الحاكمةُ الأولى: الترقيمُ بمفتاحٍ لا بإزاحةٍ.** الإزاحةُ **لا
ترفعُ خطأً** حينَ يُكتَبُ صفٌّ بينَ قراءتَي صفحتَينِ؛ تُكرِّرُ صفّاً وتُسقِطُ
آخرَ **بصمتٍ**. فالمؤشِّرُ `(created_at, id)` يُطابِقُ
`orders_rider_created_id_idx` القائمَ حرفاً — **ولا فهرسَ جديداً**، و«مزيدٌ»
يُحسَبُ بـ`p_limit + 1` في تعبيرٍ جدوليٍّ **بلا جدولٍ مؤقَّتٍ** (سلامةُ مُجمِّعِ
المعاملاتِ). والمؤشِّرُ يمرُّ في الطبقاتِ **نصّاً كما نطقَت بهِ القاعدةُ**
ولا يُعادُ تحليلُه.

**والقاعدةُ الحاكمةُ الثانيةُ: الشهرُ منطقةٌ مُسمّاةٌ لا إزاحةٌ.** طيُّ الصفحةِ
بالشهرِ حُكمٌ **لا يصحُّ بساعةِ الجهازِ** ولا بإزاحةٍ ثابتةٍ؛ فالمنطقةُ تُقرأُ
من `platform_settings.ride_history_month_timezone` **بمدينةِ الطلبِ** (مزروعةً
`"Asia/Riyadh"` في السبعِ، غيرَ مؤقَّتةٍ)، **ولا عمودَ منطقةٍ أُضيفَ إلى
`cities`** (القاعدة 0.3 · 0.6). وحينَ يغيبُ المفتاحُ أو تُجهَلُ المنطقةُ
تُنشَرُ ثقةُ المصدرِ **باسمِها** (`CITY_SETTING` · `FALLBACK_UTC_SETTING_ABSENT`
· `FALLBACK_UTC_SETTING_UNKNOWN`) — **لا سقوطٌ صامتٌ إلى التوقيتِ العالميِّ**.

**والقاعدةُ الحاكمةُ الثالثةُ: لحظةُ الإلغاءِ من `audit_log`.** لا عمودَ
`cancelled_at` في المخطَّطِ، **ولم يُضَفْ**: حدثُ `order.cancelled` باقٍ في
سجلِّ التدقيقِ بصنفِ احتفاظٍ **لا يُقلَّمُ**، فيُنشَرُ الحدثُ **بمصدرِه
مُسمّىً** (`orders.created_at` … `audit_log` · `UNRECORDED`) كي يُسألَ «من أينَ
هذا الوقتُ؟» فيُجابَ باسمِ الحقلِ. **وختمٌ ناقصٌ يُنشَرُ حدثاً `UNRECORDED` ولا
يُطوى.**

**والإنفاذُ آليٌّ:** `scripts/check-ride-history-contract.ts` **بثمانِ قواعدَ**
ومُعجَماتُها **مستوردةٌ** من حاجزِ `F2-07` لا منسوخةً (القاعدة 0.6)، ولكلِّ
قاعدةٍ حالةٌ سالبةٌ مزروعةٌ (25 حالةً · `ح-7`)، وخطوتانِ مُسمّاتانِ في `verify`
وحلقةٌ في سلسلةِ `ci`. وعقدُ المخطَّطِ زادَ **دالّتَينِ** (109) ولا جدولَ
جديدَ. و`execute` نُزِعَ عن `public` و`anon` و`authenticated` للدالّتَينِ
ومقيسٌ بـ`has_function_privilege` لا بنصِّ الهجرةِ.

**وعطبانِ كشفَهُما كتابةُ الاختبارِ لا المُصرِّفُ:** نموذجُ العرضِ خرَّطَ
`ride`/`errand` و`service_type` في القاعدةِ `('transport','delivery')` **حرفاً**
— فكانَ **كلُّ صفٍّ يُعرَضُ «خدمةً أخرى» وهوَ رحلةٌ**؛ وخرَّطَ
`ORDER_STAMP`/`AUDIT_LOG` والهجرةُ تنطقُ **باسمِ العمودِ** — فكانَ كلُّ حدثٍ
«مصدراً آخرَ». **وكلاهُما نصٌّ صحيحُ التركيبِ خاطئُ المعنى، يُعرَضُ سليماً
ويكذبُ، ولا يمسكُه بناءٌ ولا حاجزٌ ساكنٌ.** والدرسُ: **العقدُ بينَ الهجرةِ
والسطحِ يُقرأُ من الهجرةِ ولا يُكتَبُ من الذاكرةِ.** ومفتاحُ `errand` **حُذِفَ**
من القواميسِ الثلاثةِ (86 مفتاحاً) لأنَّ نصّاً مُترجَماً لا يُعرَضُ أبداً
كَذِبٌ مؤجَّلٌ.

**والسقفُ، بأسمائِه:** **لا إيصالَ ولا مبلغَ ولا خانةً محجوزةً** (`DEC-11`).
**ولا خريطةَ ولا مسارَ مرسومَ** (`ADR 0007` والمخطَّطُ معاً) — **غيابٌ مُصرَّحٌ
بنصٍّ** في الألسنةِ الثلاثةِ. **ولا تذكرةَ دعمٍ** (`F2-12` · `SR-11`) —
**والزرُّ غائبٌ لا مُعطَّلٌ**. **ولا بحثَ عربيّاً مُطبَّعاً ولا ضبابيّاً**: لا
`pg_trgm` ولا `unaccent` في القاعدةِ والبحثُ `ilike` حرفاً. **ودَينٌ باسمِه
`F2-08-D1`**: بطاقةُ السائقِ تُبنى مرّتَينِ (ههنا وفي `completed_ride_summary`)
وتُدمَجُ عندَ **ثالثِ** قارئٍ لا قبلَه. **ولا وصولَ (`a11y`) مقيسٌ.** **ولا
نشرَ حيَّ** (`ADR 0099`) فلم يرَ راكبٌ حقيقيٌّ هاتَينِ الشاشتَينِ، **وبوّابةُ
`F2` غيرُ مُدَّعاةٍ**. **واختبارُ التكاملِ (31 حالةً) لم يُشغَّلْ محلّيّاً
أصلاً** — لا `postgis` في صندوقِ التنفيذِ — **وحكمُه حكمُ CI وحدَه**، وهوَ
مُسجَّلٌ في `scripts/lib/skip-registry.ts` بمالكٍ وشرطِ تفعيلٍ **قبلَ أوّلِ
دفعةٍ**. والقرارُ: `ADR 0108`. والدليلُ:
`docs/evidence/architecture/F2-08-CLOSURE-20260914.md`.

---

## حَكَمٌ واحدٌ للمالكةِ والغريبِ — `F2-09` (2026-09-14 · إضافةٌ لا محوٌ · `ح-8`)

**الحالةُ أوّلاً لا آخِراً: البندُ `F2-09` مُنفَّذٌ كاملاً و`[ ]` بعدُ.** لا
لأنَّ عملاً بقيَ، بل لأنَّ **حكمَ CI غيرُ مقروءٍ** بالحاجزِ `B-CI-001`: الشغلاتُ
تسقطُ في ثانيتَينِ بـ`steps: []` لفوترةِ حسابٍ، فاختباراتُ التكاملِ الإحدى
والعشرونَ **مكتوبةٌ ولم تُشغَّلْ**، و`ح-4` لا تبدأُ، و`ح-6` تمنعُ أن يقومَ
الأخضرُ المحلّيُّ مقامَ الحكمِ.

**والقاعدةُ الحاكمةُ الأولى: حَكَمٌ واحدٌ لا سياستانِ متشابهتانِ.** كانَ
`get_tracking_position(text)` (هجرةُ `20260814150000`) ينشرُ `lat`/`lng` لحاملِ
الرابطِ **بلا بوّابةِ عُمرٍ**، وشاشةُ **المالكةِ** (`F2-06` · `ADR 0106`) تحجبُ
النقطةَ فوقَ تسعينَ ثانيةً وتُصنِّفُ سببَ الحجبِ — **فكانَ الغريبُ المجهولُ
مُصدَّقاً أكثرَ من صاحبةِ الرحلةِ**، ورابطٌ يُشارَكُ للطمأنينةِ يُنتِجُ
طمأنينةً كاذبةً. فاستُخرِجَ حَكَمٌ واحدٌ `tracking_link_view(uuid)` تقرؤه
الصفحةُ العامّةُ ومعاينةُ المالكةِ معاً، وأُعيدَت كتابةُ الدالّةِ القديمةِ
**غلافَ إذنٍ رقيقاً** فوقَه بلا مسِّ توقيعٍ ولا حذفِ دالّةٍ.

**والقاعدةُ الثانيةُ: الحجبُ في القاعدةِ لا في الرسمِ، وبغيابِ المفتاحِ لا
بـ`null`.** متى لم يكن الحكمُ `LOCATED` **تُحذَفُ مفاتيحُ `lat`/`lng` من
الحمولةِ** — فلا يُسرَّبُ رقمٌ إلى السلكِ ثمّ يُخفى برسمٍ، ولا يُترَكُ `null`
يدعو مستهلكاً إلى `?? 0`. والعُمرُ يُنشَرُ **دائماً** متى وُجِدَ ختمٌ، و«لم
يُبلِّغْ قطُّ» و«بلا ختمٍ» و«تقادمَ» ثلاثةُ أحكامٍ مفصولةٍ لا حالٌ صمّاءُ
واحدةٌ.

**والقاعدةُ الثالثةُ: المعاينةُ بايتاتُ المستلمِ لا رسمٌ يُشبهُها.**
`rider_ride_share_state(bigint,uuid)` تُنادي الحَكَمَ **نفسَه** وتنشرُ حمولتَه
**كما هيَ** تحتَ `preview`. ولو حُسِبَت حساباً ثانياً لصارَت شاشةً تُطمئنُ عن
شاشةٍ لا تعرفُها — وذاكَ ما يطلبُه `SR-13` حرفاً: «معاينةُ ما سيراهُ المستلمُ».

**والقاعدةُ الرابعةُ: الحدُّ قيمةُ عملٍ (0.3).** `driver_position_max_age_seconds`
يُبذَرُ `90` لكلِّ مدينةٍ **مطابقاً** للثابتِ في النطاقِ فلا يتغيَّرُ سلوكُ
`F2-06` بحرفٍ، ورفعُه لمدينةٍ **يكشفُ نقطةً محجوبةً في اللحظةِ نفسِها للاثنَينِ
بلا نشرِ كودٍ**، وغيابُه يُنشَرُ `FALLBACK_DEFAULT` **باسمِه** لا صمتاً.

**والقاعدةُ الخامسةُ: السرُّ مرّةً واحدةً.** الرمزُ يُنشَرُ عندَ الإصدارِ وحدَه
ولا يعودُ في أيِّ قراءةٍ، والإيقافُ يُنادي `revoke_order_tracking_tokens`
القائمةَ **بمعرِّفِ الرحلةِ** فيُبطِلُ **كلَّ** روابطِها ولا يُكتَبُ سرٌّ في
مسارِ طلبٍ، ورمزٌ مُلغىً ورمزٌ معدومٌ جوابُهما واحدٌ فلا تصيرُ الصفحةُ أداةَ
استكشافٍ. والبقيّةُ تُطرَحُ **بساعةِ القاعدةِ** وتُنشَرُ عدداً ولا مؤقِّتَ في
الجهازِ.

**ولا مصدرَ حقيقةٍ ثانٍ (0.6):** لا جدولَ مشاركةٍ ثانياً ولا مُولِّدَ رمزٍ
ثانياً ولا صفحةَ عرضٍ ثانيةً — `trip_tracking_tokens` و`issue_tracking_token`
و`revoke_order_tracking_tokens` و`/track/:token` قائمةٌ واستُعمِلَت كما هيَ،
والجديدُ **سطحُ المالكةِ** وحدَه. وعقدُ المخطَّطِ زادَ **دالّتَينِ** ولا جدولَ
جديدَ، و`execute` نُزِعَ عن `public` و`anon` و`authenticated` للدوالِّ الثلاثِ
ومقيسٌ بـ`has_function_privilege` لا بنصِّ الهجرةِ.

**والإنفاذُ آليٌّ:** `scripts/check-ride-share-contract.ts` **بثمانِ قواعدَ**،
ولكلِّ قاعدةٍ حالةٌ سالبةٌ مزروعةٌ (29 حالةً · `ح-7`)، وخطوتانِ مُسمّاتانِ في
`ci.yml` وحلقةٌ في سلسلةِ `ci`. **وحاجزُ `F2-06` لم يُسكَتْ**: مفردةُ
«المشاركةِ» **نُقِلَت** من المحظورِ إلى المسموحِ **معَ مسارِها** كما يأمرُ رأسُ
الحاجزِ نفسُه، وشُدَّ مكانَها **شرطٌ موجبٌ**: كلُّ زرِّ مشاركةٍ يجبُ أن يكونَ
موصولاً بمسارٍ من العقودِ الثلاثةِ. والطوارئُ (`F2-10`) والاتّصالُ يبقيانِ
محظورَينِ بحرفِهما.

**وعطبٌ في حاجزٍ كشفَته السالبةُ المزروعةُ لا أخضرُه:** قاعدةُ «الحَكَمُ واحدٌ»
كانت تقرأُ جسمَ الغلافِ فيدخلُ فيه ذيلُ الهجرةِ حيثُ
`revoke execute on function tracking_link_view(uuid)` — **فيُحسَبُ النصُّ في
أمرِ الصلاحيّةِ نداءً**، ويمرُّ الحاجزُ ولو أُفرِغَ الغلافُ من نداءِ الحَكَمِ.
وكانَ **أخضرَ على المستودَعِ الحقيقيِّ** وهوَ معطوبٌ. صُحِّحَ بقطعِ الجسمِ عندَ
`$$ language` موثَّقاً في الملفِّ. **والدرسُ: أخضرُ الحاجزِ على المستودَعِ ليسَ
دليلاً على أنَّ الحاجزَ يقيسُ — الدليلُ سقوطُه على خرقٍ مزروعٍ.**

**والسقفُ، بأسمائِه:** **لا `SOS`** (`F2-10`) ولا زرَّ مُعطَّلاً له. **ولا
اتّصالَ مُقنَّعاً** (لا مزوّدَ). **ولا إرسالَ رابطٍ من الخادمِ** برسالةٍ أو
واتساب (لا مزوّدَ رسائلَ؛ المشاركةُ من نظامِ الجهازِ). **ولا أجرةَ ولا إيصالَ**
(`DEC-11`). **ولا خريطةَ** (`ADR 0007`). **والحمولةُ العامّةُ ثلاثةُ مفاتيحَ لا
رابعَ**: لا اسمَ ولا هاتفَ ولا لوحةَ ولا وجهةَ ولا معرِّفَ طلبٍ — وهذا مكتوبٌ
**في المعاينةِ نصّاً** كي تعرفَ المشاركةُ ما تُعطي قبلَ أن تُعطيَه. **ولا
وصولَ (`a11y`) مقيسٌ.** **ولا نشرَ حيَّ** (`ADR 0099`) فلم تُشارِكْ راكبةٌ
حقيقيّةٌ رابطاً، **وبوّابةُ `F2` غيرُ مُدَّعاةٍ**. **واختبارُ التكاملِ (21
حالةً) لم يُشغَّلْ محلّيّاً أصلاً** — لا `postgis` في صندوقِ التنفيذِ —
**وحكمُه حكمُ CI وحدَه**، وهوَ مُسجَّلٌ في `scripts/lib/skip-registry.ts` بمالكٍ
وشرطِ تفعيلٍ **قبلَ أوّلِ دفعةٍ** (فصارَ المسجَّلُ **95 ملفّاً و960 حالةً**
مقيسَينِ بالحاجزِ نفسِه، والأرقامُ السابقةُ باقيةٌ بلا محوٍ · `ح-8`).
والقرارُ: `ADR 0109`. والدليلُ:
`docs/evidence/architecture/F2-09-CLOSURE-20260914.md`.

## مُعامِلٌ لا يُربَطُ بـ`::jsonb` — حاجزٌ بعدَ رابعِ مرّةٍ (2026-09-14 · `ح-8`)

**الحالُ الجديدُ**: كتابةُ `jsonb` من العميلِ صارت **مُنفَذةً آلةً** لا موصىً
بها. `scripts/check-jsonb-binding.ts` بوّابةٌ في `verify` و`bun run ci` تمسحُ
`packages` و`apps` و`tests` و`scripts` (١٢٣٨ ملفّاً اليومَ) وتُسقِطُ البناءَ على
أوّلِ `${x}::jsonb`، والبديلُ ثلاثةٌ: `sql.json(x)::jsonb` أو `to_jsonb(x::نوع)`
أو `${x}::text::jsonb`. والمنطقُ في مكتبةٍ خالصةٍ تُقاسُ بتسعِ سالباتٍ مزروعةٍ
**منها السطرُ الذي أسقطَ CI بالفعلِ**.

**السببُ**: في `$1::jsonb` يستنبِطُ PostgreSQL نوعَ المُعامِلِ من هدفِ التحويلِ،
فيُرمِّزُ السائقُ القيمةَ ثانيةً، فيصلُ `90` إلى القاعدةِ `"90"` — نصّاً — بلا
خطأٍ ولا سجلٍّ. دُفِعَ ثمنُه **أربعَ مرّاتٍ**: مُحوِّلُ الدفعِ، ثمَّ مرشِّحاتُ
البثِّ (**فذهبَ البثُّ إلى الجمهورِ كلِّه**)، ثمَّ طزاجةُ موقعِ السائقِ، ثمَّ
`F2-09` (خمسةَ عشرَ اختباراً ساقطاً في ملفّاتٍ لا علاقةَ لها بالبندِ). وكانَ
مكتوباً في ثلاثةِ تعليقاتٍ ولم يكن مُنفَذاً.

**ما لم يتغيَّرْ**: القيدُ `platform_settings_value_type_coherent` باقٍ — هوَ ما
كشفَ الرابعةَ، والحاجزُ يمنعُ الوصولَ إليه لا يُغني عنه. والتعليقاتُ الثلاثةُ
باقيةٌ بنصِّها، والحاجزُ يتجاوزُ التعليقاتَ عن قصدٍ كي لا يُحذَفَ الدرسُ ليخضرَّ
(`ح-1`). راجِعْ `ADR 0110`.

## سطحُ الاستغاثةِ في المِنِصَّةِ الصغيرةِ — بطاقةٌ محكومةٌ لا زرٌّ (2026-09-14 · `F2-10`)

**الحالُ الجديدُ**: للراكبِ في شاشةِ الرحلةِ النشطةِ **بطاقةُ استغاثةٍ بخطوتَينِ**
(تسليحٌ ثمَّ تأكيدٌ)، حكمُ ظهورِها من القاعدةِ وحدَها بدالّةٍ واحدةٍ
`sos_surface_state(bigint, text)`، وبابُها `GET`/`POST /v1/safety/sos` **بلا
مُعرِّفِ طلبٍ في المسارِ**. والبطاقةُ تُخفي نفسَها حينَ لا رحلةَ ولا بلاغَ،
وتبقى مرئيّةً ما دامَ بلاغٌ ولو زالَ الجوازُ. ونافذةُ ما بعدَ الرحلةِ إعدادٌ
لكلِّ مدينةٍ (`sos_post_ride_window_minutes`) ومصدرُ قيمتِها مُعلَنٌ باسمِه.

**الوعدُ المكتوبُ صراحةً**: `SOS_NO_PHONE_CALL` رمزُ إفصاحٍ **إجباريٌّ في كلِّ
حالٍ** — «المنصّةُ لا تتّصلُ بشرطةٍ ولا إسعافٍ نيابةً عنكَ». وحاجزٌ ساكنٌ يمنعُ
`tel:` و`wa.me` و`whatsapp` و`callDriver` في السطحِ والبابِ والقاموسِ، ويمنعُ
رمزَ إفصاحٍ بلا نصٍّ في القواميسِ الثلاثةِ.

**ما لم يتغيَّرْ**: مسارُ الاستقبالِ نفسُه (`ADR 0077`) — الضغطةُ تُنادي
`triggerSos` بانتظارٍ **واحدٍ** و`orderId: null`، والقاعدةُ تحلُّ الطلبَ تحتَ
قفلٍ؛ ولا يُنادى حَكَمُ العرضِ في مسارِ البلاغِ أبداً. وحوارا البوتَينِ لم
يُمَسّا، ولا مزوّدَ اتّصالٍ ولا خريطةَ (`ADR 0007`)، والأجرةُ والدفعُ مُجمَّدانِ
(`ADR 0039` §٤).

**وما لا يُدَّعى (`ح-5`)**: الهجرةُ لم تُطبَّقْ على قاعدةٍ حقيقيّةٍ في جلسةِ
البناءِ، ولم تُفتَحْ البطاقةُ في جهازٍ داخلَ Telegram. راجِعْ `ADR 0111`.

## تصفيةُ الفروعِ الراكدةِ — أربعةَ عشرَ مصدرَ حقيقةٍ ثانياً أُغلِقَت (2026-09-14 · `ح-1`)

**الحالُ الجديدُ**: `main` وحدَها تحملُ العملَ غيرَ المدموجِ. حُذِفَت أربعةَ
عشرَ فرعاً بعيداً راكداً (من 2026-08-27 إلى 2026-09-13، متأخِّرةً عن `main`
بينَ ٢٨ و٤٤٩ التزاماً)، **ورؤوسُها الأربعونَ خانةً محفوظةٌ كاملةً** في
`docs/evidence/repo/stale-branches-reconciliation-20260914.md` فكلُّ فرعٍ
مستعادٌ بحرفِه.

**السببُ**: قيسَ لكلِّ فرعٍ فرقُه عن نقطةِ تفرُّعِه، وقُورِنَ **كلُّ ملفٍّ**
بنظيرِه في `main` بالتجزئةِ — **فكانَ الغائبُ عن `main` صفراً في الأربعةَ عشرَ
جميعاً**. وبنودُها مُغلَقةٌ في §9.5: `CAP-001` · `CAP-003` · `F1-01` ·
`F2-03` · `F5-07` · `F6-01` · `F6-03` · `F7-01` كلُّها `[x]`؛ و`F2-04` `[~]`
بنصفِه المشروعِ لا بنقصِ فرعٍ. وفرعٌ راكدٌ ليسَ حياداً: هوَ مصدرُ حقيقةٍ ثانٍ
يُوهِمُ بعملٍ لم يهبطْ ويُغري بدمجٍ يُحيي شِفرةً ماتت.

**ما لم يتغيَّرْ**: لا سطرَ من `main` مُسَّ، ولا فرعَ مدموجاً حُذِفَ، ولا
`[x]` قُلِبَ. **وما لا يُدَّعى (`ح-5`)**: لم يُبنَ أيٌّ من الفروعِ المحذوفةِ
على `main` الحاليّةِ — المُدَّعى هبوطُ مضمونِها لا أنَّها تُبنى اليومَ.

## حقَّا صاحبِ البيانةِ: تنزيلاً وحذفاً فعليَّينِ (`F2-11` · `SR-12` · 2026-09-14 · `ح-1`)

**الحالُ الجديدُ**: لصاحبِ الحسابِ **شاشةُ حسابٍ** يُنزِّلُ منها بيانتَه ملفَّ
`JSON` باثنَي عشرَ قسماً، ويحذفُ حسابَه **حذفاً يقعُ فعلاً في القاعدةِ** ثمَّ
يُقرأُ إيصالَه. والمساران `GET /v1/me/data-export` و`POST /v1/me/erasure`،
والحكمُ كلُّه في دالّتَينِ في القاعدةِ (`export_my_data` · `erase_my_account`)
لا في التطبيقِ. **والبندُ `[~]` لا `[x]`**: تعديلُ الأماكنِ المحفوظةِ وحذفُها
فرادى وعرضُ الموافقاتِ من نصِّ `SR-12` **ولم يُبنَيا**، وهُما مكتوبانِ دَيناً
يُقرأُ في الشاشةِ نفسِها.

**القاعدةُ الحاكمةُ**: **الحذفُ حكمٌ لكلِّ جدولٍ لا `delete` واحدةٌ.** لكلٍّ من
جداولِ المخطَّطِ الواحدِ والخمسينَ حكمٌ مكتوبٌ في
`packages/shared/config/erasure-policy.ts` من خمسةٍ (`erase-row` ·
`anonymize-in-place` · `retain-declared-legal-basis` · `no-personal-data` ·
`deferred-area-not-live`)، **ومن السجلِّ نفسِه تُشتَقُّ أقسامُ التنزيلِ**
(القاعدة 0.6). و`users` **يُجهَّلُ ولا يُمحى**: قيدُ
`users_erased_rows_carry_no_identity` يمنعُ صفّاً مختوماً بالحذفِ أن يحملَ
هُويّةً، و`telegram_id` يُبدَّلُ بمعرِضٍ سالبٍ. والإيصالُ يُحفَظُ في
`audit_log` حدثاً `account.erased` لا في جدولٍ جديدٍ. راجِعْ `ADR 0112`.

**الإنفاذُ الجديدُ**: سلسلةُ `bun run ci` صارَ فيها الحاجزُ
`scripts/check-erasure-policy.ts`، **وهوَ يُخفِقُ على جدولٍ في المخطَّطِ بلا
حكمٍ** — فلا يمرُّ جدولٌ جديدٌ إلى `main` حتّى يُجيبَ صاحبُه: «وماذا يحدثُ
لهذا عندَ الحذفِ؟».

**تصحيحٌ بالإضافةِ لدعوى سابقةٍ (`ح-8`)**: قيلَ في دورةٍ سابقةٍ إنَّ
`ALREADY_ERASED` و`ACCOUNT_ERASED` فرعانِ مبلوغانِ. **وذاكَ غيرُ صحيحٍ من بابِ
تيليجرامَ**: الحذفُ يُبدِّلُ المعرِّفَ نفسَه، فمَن حذفَ حسابَه ثمَّ عادَ هوَ
**مستخدمٌ جديدٌ**، وما بعدَ الحذفِ يُرَدُّ `USER_NOT_FOUND`. والمِسبارُ الذي
بلغَ الفرعَينِ كانَ ينادي بالمعرِضِ السالبِ مباشرةً — وليسَ ذاكَ مسارَ
مستخدمٍ. والسطرُ السابقُ **لم يُمحَ**.

**ما لا يُدَّعى (`ح-5`)**: لا تحريرَ للأماكنِ ولا حذفَ لواحدٍ منها · ولا عرضَ
للموافقاتِ · ولا سطحَ للسائقِ ولا للإدارةِ (حكمُ جداولِهما مكتوبٌ في السجلِّ
فقط) · **ولا استيفاءَ مُدَّعى لنظامِ حمايةِ البياناتِ الشخصيّةِ** — حقّانِ منه
على سطحٍ واحدٍ · ولا مُدَدَ استبقاءٍ (`DEC-15`) ولا حسمَ للأصنافِ المُعلَّقةِ
(`F12-10`) · **ولا راكبٌ حقيقيٌّ رأى هذه الشاشةَ** (`ADR 0099`) · وبوّابةُ
`F2` غيرُ مفتوحةٍ.

## حاجزُ التنصُّلِ: الحظرُ والمقامُ يَعبُرانِ الحذفَ (`ADR 0113` · 2026-09-14 · `ح-1`)

**ثغرةٌ فتحَها `F2-11` نفسُه وسُدَّت.** التجهيلُ يُبدِّلُ `users.telegram_id`
بمعرِضٍ سالبٍ، ومسارُ التسجيلِ يُصادِمُ على `telegram_id` — فالعائدُ بمعرِّفِه
الحقيقيِّ **لا يُصادِمُ شيئاً** فيُنشَأُ له صفٌّ `is_blocked = false` بمقامٍ
صفرٍ. **فكانَ «احذفْ حسابي» أرخصَ زرٍّ لرفعِ الحظرِ في المنصّةِ**، والضررُ على
الراكبِ الذي حسِبَ أنَّ الحظرَ يحميه وعلى السائقِ الذي كسبَ تقييمَه.

**ما صارَ اليومَ**: جدولانِ (`identity_hash_pepper` سرُّ منصّةٍ يُكتَبُ مرّةً
ولا يُقرَأُ ولا يُدوَّرُ · `identity_marks` أثرٌ بتجزئةٍ أحاديّةٍ مُفلفَلةٍ)،
ودالّةُ `identity_hash(text)` `security definer`، وثلاثةُ مُشغِّلاتٍ:
كتابةُ الأثرِ `before update` من `old` — **النافذةُ الوحيدةُ التي يكونُ فيها
المعرِّفُ حاضراً** — وإعادةُ تطبيقِه `before insert` **فتَشمُلُ كلَّ مسارِ
تسجيلٍ يُضافُ غداً**، وبذرُ المقامِ `after insert` على `riders`. والحظرُ
يُجمَعُ ولا يُرفَعُ، والتقييماتُ تُجمَعُ لا تُستبدَلُ، والمطابقةُ **لا تُصفّى
بالمدينةِ** فالحظرُ يَعبُرُ المدنَ. ولا اسمَ ولا رقمَ في الأثرِ: قيدٌ يردُّ
كلَّ ما ليسَ ستّينَ وأربعَ خانةٍ ستّةَ عشريّةً.

**والإفصاحُ قبلَ الضغطِ**: سطرٌ في شاشةِ الحسابِ بثلاثِ لغاتٍ، وقسمٌ ثالثَ
عشرَ في التنزيلِ (`identityBar`)، وأساسٌ سادسٌ في الإيصالِ
(`BLOCK_AND_STANDING_SURVIVE_ERASURE`) — والتجزئةُ نفسُها لا تُفصَحُ.

**وصنفُ إعفاءٍ ثانٍ من القاعدةِ 0.4** (`platform secret`) أُذِنَ به في **ملحقٍ
حاكمٍ** في `docs/MASTER_DIRECTIVE.md` لا في ADR، وعِلَّتُه **سياديّةٌ لا
زمنيّةٌ**: فِلفِلٌ للمدينةِ يجعلُ تجزئةَ الإنسانِ الواحدِ مختلفةً في كلِّ
مدينةٍ، **فيكونُ الانتقالُ عفواً آليّاً**.

**وعطبانِ في حارسَينِ قائمَينِ أُصلِحا في مصدرِهما**: `check-erasure-policy`
كانَ يقرأُ أوّلَ هجرةٍ تُعرِّفُ `export_my_data` لا آخرَها فيُقابِلُ السجلَّ
**بتعريفٍ ميّتٍ**؛ و`String.raw` بلا هربٍ في `css-class-coverage`.

**وتصحيحٌ بالإضافةِ لا محوٌ (`ح-8`)**: «العائدُ مستخدمٌ جديدٌ بصفٍّ جديدٍ» في
`ADR 0112 §٣-ب` وفي دليلِ `F2-11` **باقٍ مكتوباً** ومُصحَّحٌ بإضافةٍ في
موضعَيه — السلوكُ مُبدَّلٌ لا مُكذَّبٌ.

**ما لا يُدَّعى (`ح-5`)**: لا سطحَ عفوٍ لمشرفٍ · ولا حظرَ سائقٍ مبذورَ مقامٍ
(`drivers` لم يُبنَ — دَينٌ مُعلَنٌ) · ولا استعمالَ للمقامِ في معادلةِ
المطابقةِ · ولا نشرَ حيَّ ولا محتالٌ حقيقيٌّ جُرِّبَ عليه (`ADR 0099`).

## بابُ الدعمِ من داخلِ التطبيقِ — مرجعٌ يُنطَقُ في مكالمةٍ (`F2-12` · `SR-11` · 2026-09-14 · `ح-8`)

**الحاجةُ**: كانَ للدعمِ بابٌ من جهةِ البوتِ يفتحُ تذكرةً في مجموعةِ المدينةِ،
وليسَ للراكبِ في المِنِّي‑آبِّ بابٌ ولا سجلٌّ. وكانَ للتذكرةِ معرِّفٌ واحدٌ
`uuid` — و**المعرِّفُ العالميُّ صالحٌ لكلِّ شيءٍ إلّا للإنسانِ**: لا يُملى في
مكالمةٍ، فيتخلّى عنه المشرفُ ويبحثُ بالاسمِ أو بالرقمِ.

**ما صارَ اليومَ**: `support_tickets.reference` عمودٌ افتراضُه
`'WSL-' || lpad(nextval('support_ticket_reference_seq'), 6, '0')` — **التوليدُ في
القاعدةِ لا في الشِفرةِ**، فصفٌّ يُكتَبُ بأيِّ مسارٍ (دالّةٌ · `insert` من
هجرةٍ · إصلاحٌ في `psql`) يخرجُ بمرجعٍ. وأربعُ هجراتٍ بأطوارٍ مفصولةٍ (توسيعٌ ·
ملءٌ رجعيٌّ · فهرسٌ `concurrently` وحدَه · تصديقٌ)، وفهرسٌ فريدٌ
`support_tickets_reference_key` و قيدُ حضورٍ `support_tickets_reference_present`
**يُقرآنِ من الكاتالوجِ** (`indisvalid` · `convalidated`) لا من نصِّ الهجرةِ —
لأنَّ فهرساً بُنِيَ `concurrently` وسقطَ يبقى موجوداً **غيرَ صحيحٍ**.

وأربعُ قيمٍ جديدةٍ في `support_ticket_type` (مفقودٌ · سلوكُ سائقٍ · عطبُ تطبيقٍ ·
أخرى)، ودالّةُ قراءةٍ `rider_support_tickets(bigint,integer,timestamptz,uuid)`
بترقيمِ **مفتاحٍ لا إزاحةٍ**، و`POST /v1/support/tickets` و`GET /v1/support/tickets`،
وشاشةُ دعمٍ في سطحِ الراكبِ موصولةٌ من الجذرِ ومن شاشةِ الحسابِ ومن تفاصيلِ
الرحلةِ («الإبلاغُ عن مشكلةٍ» صارَ زرّاً يعملُ بعدَ أن كانَ **غائباً** في
`F2-08`)، و59 مفتاحاً في القواميسِ الثلاثةِ.

**والرفضُ حكمُ القاعدةِ لا حكمُ التطبيقِ**: أحدَ عشرَ رمزاً تُترجِمُها طبقةُ
التطبيقِ ولا تُقرِّرُها — فلا فحصَ مِلكيّةٍ ثانياً ولا حسابَ تهدئةٍ ثانياً.
و`COOLDOWN_ACTIVE` يُردُّ **بثانيةٍ تُقرأُ** و**بلا كتابةِ صفٍّ** (يُقاسُ بعدِّ
الصفوفِ قبلَ الرفضِ وبعدَه)، و`subscription` يُردُّ `NOT_A_DRIVER` لراكبٍ
**بحكمِ القاعدةِ** لا بحجبٍ في الواجهةِ.

**وحاجزٌ ساكنٌ جديدٌ** `check-support-intake-contract.ts` بسبعِ قواعدَ ولكلٍّ
منها حالةُ افتراقٍ مزروعةٌ (`ح-7`): لا رمزَ عطبٍ بلا نصٍّ في اللغاتِ الثلاثِ ·
ولا مفتاحَ بلا مقابلٍ · وسقوطٌ للمجهولِ (`error.UNKNOWN` · `category.unknown` ·
`status.unknown`) · ومرجعٌ من متسلسلةٍ · ويُعرَضُ في الشاشةِ (مرجعٌ لا يُرى لا
يُنطَقُ) · **ولا بابَ إرفاقٍ صوريَّ** — فالإرفاقُ مؤجَّلٌ مُعلَناً، وبابٌ
يُفتَحُ ولا يرفعُ ملفّاً كذبٌ في أسوأِ لحظةٍ: لحظةِ الشكوى · ولا دالّةَ بلا
نزعِ تنفيذٍ. مُدرَجٌ في سلسلةِ `ci` وفي وظيفةِ `verify` بخطوتَينِ.

**القياسُ**: 16 حالةَ تكاملٍ على PostgreSQL حقيقيّةٍ (منها الترقيمُ بمفتاحٍ مع
صفٍّ يُكتَبُ **بينَ** الصفحتَينِ، وعزلُ راكبَينِ، والإعدادُ الغائبُ يُقاسُ
**بحذفٍ داخلَ معاملةٍ تُرجَعُ** كي لا تُلمَسَ قاعدةُ الاختبارِ)، و67 حالةَ
وحدةٍ (19 افتراقاً مزروعاً للحاجزِ · 28 لطبقةِ التطبيقِ · 20 للعرضِ)، وعقدُ
المخطَّطِ صارَ 114 دالّةً.

**الحالةُ `[~]` لا `[x]`** — `ح-4` يُوجِبُ ثلاثَ جولاتٍ خضراءَ متتاليةٍ من CI.

**ما لا يُدَّعى (`ح-5`)**: لا إرفاقَ · ولا خيطَ ردٍّ داخلَ التطبيقِ (الردُّ في
مجموعةِ تيليجرامَ) · ولا أسئلةً شائعةً · ولا صفحةَ مفقوداتٍ · ولا بابَ دعمٍ
للسائقِ (`SD-10`) · وزمنُ الاستجابةِ المنشورُ **إعدادٌ مُعلَنٌ مؤقّتاً**
(`is_provisional`) لا وعدٌ مقيسٌ من بياناتِ تشغيلٍ.

## إغلاقُ `F2-10` و`F2-11` — قلبُ الحالةِ بحكمِ CI لا بحُسنِ الظنِّ (2026-09-14 · `ح-4` · `ح-8`)

**لا دعوى جديدةً ههنا، ولا سطرَ كودٍ**: هذا قلبُ حالةٍ **بعدَ** أن وُفِيَ شرطُ
`ح-4` — ثلاثُ جولاتٍ خضراءَ متتاليةٍ من CI على `main` لكلِّ بندٍ — والأرقامُ
مذكورةٌ كي تُراجَعَ:

- **`F2-10`** (سطحُ الاستغاثةِ): `34839711572` على `cb87948` ·
  `34852249908` على `6254fd6` · `34861538030` على `6af6c35`.
- **`F2-11`** (حقَّا البيانةِ): `34861538030` على `6af6c35` ·
  `34880822092` على `2b0f4e1` · `34887616045` على `75b72b8`.

والوظائفُ الأربعُ (`verify` · تكاملُ PostgreSQL · تكاملُ Redis · فوضى `F5-06`)
ناجحةٌ في الجولاتِ كلِّها بلا خطوةٍ ساقطةٍ.

**وما كُتِبَ سابقاً باقٍ بحرفِه (`ح-8`)**: «مبنيٌّ ومقيسٌ محلّيّاً ولا `[x]`
قبلَ ثلاثِ جولاتٍ» بقيَ منصوصاً في صفِّ `F2-10` مقروناً بقلبِه — فالشرطُ
**وُفِيَ ولم يُخفَّفْ**.

**وما لا يُدَّعى (`ح-5`)**: لا تجربةَ على جهازٍ حقيقيٍّ داخلَ Telegram · ولا
وصولَ (`a11y`) مقيسٌ · **وبوّابةُ `F2` غيرُ مُدَّعاةٍ** فهيَ رحلةٌ كاملةٌ
مسجَّلةٌ بالفيديو، و`F2-12` نفسُه ما زالَ `[~]` حتّى تكتملَ جولاتُه.

## إغلاقُ `F2-12` — بابُ الدعمِ صارَ `[x]` بحكمِ CI (2026-09-14 · `ح-4` · `ح-8`)

ثلاثُ جولاتٍ خضراءَ متتاليةٍ: `34887051555` على `3f28049` · `34887616045` على
`main`@`75b72b8` · `34889897158` على `main`@`368eb11`. والوظائفُ الأربعُ
ناجحةٌ بلا خطوةٍ ساقطةٍ، وحالاتُ التكاملِ السِتَّ عشرةَ جرَت **فعلاً** على
PostgreSQL حقيقيٍّ لا على محرِّكٍ مُصنَّعٍ.

**وأوّلُ جولةٍ سقطَت — والحاجزُ أصابَ**: `verify` رُدَّ باختبارِ الأرقامِ
المُثبَّتةِ في تدقيقِ التجاوزِ (97 ملفّاً · 978 حالةً) لأنَّ مدخلَ
`skip-registry` الجديدَ رفعَ العدَّ ولم يُرفَعْ معَه الرقمُ. **والإصلاحُ
بالزيادةِ** إلى 98 و994 معَ إبقاءِ الأرقامِ السابقةِ مكتوبةً (`ح-8`)، **لا
بتعطيلِ الاختبارِ ولا بتخفيفِه** — فوظيفةُ ذلكَ الرقمِ أن يُوقِفَ كلَّ زيادةٍ
في التجاوزِ لا تُقصَدُ.

وبهذا صارَ **بنودُ `F2` كلُّها مغلقةً** (`F2-01` … `F2-12`) — **وبوّابةُ `F2`
غيرُ مُدَّعاةٍ** فهيَ رحلةٌ كاملةٌ على جهازٍ حقيقيٍّ داخلَ Telegram مسجَّلةٌ
بالفيديو ومرفوعةٌ دليلاً، ولم تُجرَ.


## `F3-01` + `F12-14` — وثائقُ السائقِ ورفعٌ برابطٍ موقَّعٍ (2026-09-15)

**ما صارَ في المستودعِ.** جدولٌ واحدٌ `driver_documents` وستُّ دوالَّ
(`driver_required_document_types` · `driver_document_block_reasons` ·
`driver_document_upload_slot` · `record_driver_document` ·
`submit_driver_documents_for_review` · `driver_document_dashboard`) وستُّ هجراتٍ
مفصولةِ الأطوارِ (`20260915000000` … `000500`)، وطبقاتُ نطاقٍ وتطبيقٍ وبنيةٍ
وبوّابةٍ (أربعُ نقاطٍ تحتَ `/v1/driver/documents`)، وسطحُ سائقٍ في المِنِّي‑آبِّ
بثلاثِ خُطواتٍ (توقيعٌ ← رفعٌ ← تسجيلٌ)، وموقِّعٌ `HttpUploadSigner` يقرأُ سرَّه
من البيئةِ، وسكربتُ تهيئةِ دلوٍ خاصٍّ، وحاجزٌ ساكنٌ بسبعِ قواعدَ في سلسلةِ `ci`
وفي وظيفةِ `verify`.

**الحكمُ المُثبَّتُ.** `ADR 0115`: الوثيقةُ تنتهي، فالحجبُ **ساعةٌ لا رايةٌ**
تُحسَبُ عندَ كلِّ سؤالٍ؛ والإنفاذُ في **الدالّةِ التي تُنشِئُ العرضَ** لا في
البوّابةِ؛ والمِلفُّ في مخزنِ الكائناتِ والقاعدةُ تحفظُ مسارَه وحُكمَه.

**القياسُ.** 25 حالةَ تكاملٍ على PostgreSQL حقيقيٍّ محلّيّاً (لا محرِّكٌ
مُصنَّعٌ)، و73 حالةَ وحدةٍ (29 للحاجزِ · 29 للعرضِ · 15 للموقِّعِ). والقيودُ
الثلاثةُ والفهارسُ تُقرأُ **من الكاتالوجِ** (`convalidated` · `indisvalid`) لا
من نصِّ هجرةٍ. وسجلُّ التجاوزِ رُفِعَ بالزيادةِ إلى 99 ملفّاً و1019 حالةً
(`ح-8`)، وعقدُ المخطَّطِ أُعيدَ توليدُه إلى 118 دالّةً.

**والحاجزُ الجديدُ أصابَ عندَ أوّلِ تشغيلٍ** — وهذا يُسجَّلُ لا يُخفى: كشفَ
افتراقاً حقيقيّاً بينَ رموزِ الأخطاءِ المنشورةِ من طبقةِ التطبيقِ وبينَ قواميسِ
الواجهةِ الثلاثةِ (ثلاثةُ رموزٍ باسمٍ مختلفٍ، وخمسةُ مفاتيحَ لرموزٍ غيرِ
منشورةٍ، ورمزٌ منشورٌ بلا نصٍّ)، فأُصلِحَ الافتراقُ في القواميسِ وفي قائمةِ
الواجهةِ. وكشفَ اختبارُ العرضِ عطباً حقيقيّاً ثانياً: `rejectExpiryLocally`
كانت تقبلُ `2027-02-31` لأنَّ تحليلَ التواريخِ متسامحٌ — فأُصلِحَ بمقارنةِ
ذهابٍ وعودةٍ. وأُصلِحَ في الحاجزِ نفسِه عطبانِ (توقُّعُ `revoke execute` وحدَه،
وقطعُ تحليلِ النوعِ المعدودِ عندَ قوسٍ داخلَ تعليقٍ عربيٍّ) وحُبِسا باختبارٍ.

**ما لا يُدَّعى (`ح-5`).** لا لوحَ مراجعةٍ للمشرفِ — والقبولُ في الاختبارِ
**حالٌ مزروعةٌ بـ`update` مكشوفٍ** مُعلَنةٌ في رأسِ الملفِّ · ولا مطابقةَ وجودِ
الكائنِ في المخزنِ · ولا توقيعَ يُقاسُ في CI (لا مخزنَ في الوظيفةِ)، والتوقيعُ
الحقيقيُّ مقيسٌ **مرّةً** على مشروعِ تجربةٍ بأثرٍ موثَّقٍ في
`docs/evidence/storage/F3-01-SIGNED-UPLOAD-20260915.md` ومعَه ضابطٌ سالبٌ
(مفتاحٌ منشورٌ يُرَدُّ `403`) · ولا مفتاحَ سرِّيَّ إنتاجٍ جُرِّبَ · ولا فحصَ
محتوى مِلفٍّ · ولا تنظيفَ للكائنِ القديمِ عندَ الإحلالِ (دَينٌ مُعلَنٌ) ·
**ولا حكمَ CI بعدُ**: البندانِ `[~]`، ولا `[x]` قبلَ ثلاثِ جولاتٍ خضراءَ
متتاليةٍ (`ح-4`).

### زيادةٌ على البندِ نفسِه — ستُّ سجلّاتٍ مغلقةٍ أصابَت جدولاً واحداً (2026-09-15)

**السببُ.** تشغيلُ حزمةِ الوحدةِ كاملةً بعدَ اكتمالِ الشيفرةِ أخفقَ في **ثمانِ
حالاتٍ**، وكلُّها من صنفٍ واحدٍ: `driver_documents` جدولٌ جديدٌ، والمستودعُ يحملُ
**قوائمَ مغلقةً** تُلزِمُ كلَّ جدولٍ بحكمٍ مكتوبٍ. فما أخفقَ ليسَ الشيفرةَ بل
**غيابُ الحكمِ**، وهذا هوَ عملُ الحواجزِ بعينِه — فأُضيفَ الحكمُ ولم يُمَسَّ حاجزٌ:

| السجلُّ | الحكمُ المُضافُ |
| --- | --- |
| `retention-policy.ts` | `lifecycleBound` — لا مؤقِّتَ يحذفُ، لأنَّ حذفَ صفٍّ منتهيَ الصلاحيّةِ **يفتحُ الحجبَ** بدلَ أن يُبقِيَه |
| `erasure-policy.ts` | `erase` لصاحبِ الحسابِ، قسمُ تنزيلٍ `driverDocuments`، دَينُه `SD-12` — ومعَه مكتوبٌ أنَّ **حذفَ الصفِّ لا يحذفُ الجسمَ من المخزنِ** |
| `wasla-boundary-registry.ts` | `KEEP` لـMOVE: الوثيقةُ **حكمُ أهليّةِ قيادةٍ** يُقرأُ في مسارِ العرضِ الحارِّ، ونقلُه إلى نظامٍ آخرَ يجعلُ الحجبَ عُرضةً لعطبِ شبكةٍ — وحجبٌ يسقطُ عندَ العطبِ يُقلَبُ إذناً |
| `wasla-migration-matrix.ts` | `NONE` في الموجةِ ٠، والعددُ المُثبَّتُ رُفِعَ بالزيادةِ 53 → 54 (`ح-8`) |
| `rollback-registry.ts` | مسارُ عودةِ `open_offer_round(4)`: `code-only` بلا اقترانِ نشرٍ، وأثرُ العودةِ مكتوبٌ (سائقٌ منتهيةٌ وثيقتُه يعودُ إلى الصفِّ حتّى تُعادَ الهجرةُ) |
| `egress-registry.ts` | مقصدٌ جديدٌ `supabase-object-storage` **مُبوَّبٌ** (`gated`)، ومفاتيحُه الثلاثةُ في `.env.example`، وموضعُ الرفعِ من المتصفّحِ مُعلَنٌ في `BROWSER_FETCH_SITES` بسببِه |

**وحاجزُ حدِّ الصادرِ أصابَ إصابةً حقيقيّةً** تستحقُّ التسجيلَ: المُوقِّعُ كانَ
يسقطُ إلى `fetch` **عارياً** حينَ لا يُحقَنُ جالبٌ، ومضيفُ المخزنِ يأتي من
البيئةِ — فكانَ الطريقُ مفتوحاً إلى أيِّ مضيفٍ يُوضَعُ في المتغيّرِ بلا أن
يُخفِقَ سطرٌ واحدٌ. فصارَ الافتراضُ `createGuardedFetch("supabase-object-storage")`.
وأُعيدَ تسميةُ حقلِ الناقلِ من `#fetch` إلى `#transport` لأنَّ الحاجزَ يقرأُ
`#fetch(` نداءً عارياً بحقٍّ — **والاسمُ يُغيَّرُ ولا يُسكَتُ الحاجزُ**.

**والمقاسُ بعدَ الإصلاحِ.** 3914 حالةَ وحدةٍ (0 إخفاق) · 25 حالةَ تكاملٍ على
PostgreSQL حقيقيٍّ (0 إخفاق) · تسعةُ حواجزَ خضراءُ منها الجردُ 54 جدولاً
والمصفوفةُ 54 والحذفُ 54 والصادرُ 16 مقصداً · `tsc` بلا خطأٍ · وميزانُ التوثيقِ
0.463 مقابلَ سقفِ 0.75.

**ما لا يُدَّعى (`ح-5`).** حذفُ أجسامِ المخزنِ عندَ محوِ الحسابِ **غيرُ منفَّذٍ**
وهوَ دَينٌ مكتوبٌ في `SD-12` لا وعدٌ شفهيٌّ · ولا حكمَ CI بعدُ على هذه الزيادةِ.

## حكمُ CI هوَ الحاكمُ — ستّةٌ وأربعونَ إخفاقاً كشفَت عيبَ حاجزٍ لا عيبَ اختبارٍ (2026-09-15)

**الواقعةُ.** حكمُ CI على `PR #40` (جولتانِ: `34905813829` و`34905864520`) كانَ:
`verify` **مُخفِقٌ** · `تكامل على PostgreSQL حقيقي` **مُخفِقٌ** · `roadmap`
ناجحٌ · `تكامل على Redis حقيقي` ناجحٌ · `فوضى متعدد المثيلات (F5-06)` ناجحٌ.
والأخضرُ المحليُّ **لم يكن بديلاً عن الحكمِ** — وذاكَ نصُّ التوجيهِ.

**السببُ الجذريُّ الأوّلُ — `verify`.** `scripts/check-migrations.ts` قالَ:
`driver_documents: RLS غير مفعّلة`. ولم يكن ذلكَ سهوَ حاجزٍ بل **سهوَ هجرةٍ**:
الجدولُ أُنشئَ بلا `enable row level security` وبلا سياسةٍ. فأُضيفَ إلى
`20260915000000` قسمٌ `٤·١`: تفعيلُ RLS وسياسةُ `service_role` وحدَها، ومكتوبٌ
معَها أنَّ **غيابَ سياسةٍ للمستخدمِ قرارٌ لا سهوٌ** — الجدولُ لا يُقرأُ إلّا من
دالّاتٍ بـ`security definer`، ومفتاحٌ منشورٌ في تطبيقٍ مصغَّرٍ لا يُؤتمَنُ على
صفٍّ فيه رقمُ رخصةٍ وصورتُها. وقُيسَ الأثرُ في الكتالوجِ لا في نصِّ الهجرةِ:
`select relrowsecurity from pg_class where relname='driver_documents'` → `t`.

**السببُ الجذريُّ الثاني — التكاملُ، و46 إخفاقاً.** الإخفاقاتُ كلُّها في
مساراتٍ **لا صلةَ لها بالوثائقِ**: `full-ride` · `notification_outbox` ·
`five-cities-launch` · `mutual-ratings` · `dispatch-redispatch` ·
`dispatch-reject-offer-scoped` · `blocking-enforcement` · `driver-visibility` ·
`free-trial` · `order-cancellation`. والسببُ واحدٌ: سائقو تلكَ الاختباراتِ
مزروعونَ `verified` بلا صفِّ وثيقةٍ، وحاجزُ `20260915000500` أسقطَ كلَّ من له
**أيُّ** سببِ حجبٍ — بما فيه `MISSING:`.

**والحكمُ الذي اتُّخِذَ: تضييقُ الحاجزِ لا تخفيفُ الاختبارِ.** لأنَّ المُقاسَ
ههنا ليسَ عيبَ زرعٍ: كلُّ سائقٍ اعتمدَته الإدارةُ **قبلَ** `F3-01` هوَ في
القاعدةِ الحقيقيّةِ سائقٌ `verified` بلا صفِّ وثيقةٍ، فنشرُ الحاجزِ الأوسعِ
كانَ **سيُخرِجُ سائقي المنصّةِ كلَّهم من الإسنادِ في لحظةِ الهجرةِ** بلا ترحيلٍ
ولا إشعارٍ. فأُضيفَت هجرةُ `20260915010000` (تصحيحٌ بالإضافةِ، `ح-8`، ولا سطرَ
مُحيَ من `20260915000500`):

| ما تغيَّرَ | كيفَ |
| --- | --- |
| شرطُ الأهليّةِ في `open_offer_round` | من `driver_document_block_reasons` إلى `driver_document_dispatch_block_reasons` — **حرفٌ واحدٌ في `where`**، وباقي جسمِ الدالّةِ منسوخٌ حرفاً بحرفٍ |
| دالّةٌ جديدةٌ **مُشتقّةٌ لا مكرَّرةٌ** | `driver_document_dispatch_block_reasons(uuid, date)` تُصفّي مُخرَجَ الدالّةِ الأمِّ فتُبقي `EXPIRED:` و`REJECTED:` — فمصدرُ الاشتقاقِ واحدٌ |
| الصلاحيّاتُ | `revoke all from public, anon, authenticated` ثمَّ `grant execute to service_role` — كسائرِ الدالّاتِ |
| سجلُّ الارتدادِ | مدخلٌ لـ`20260915010000::revoke_function:open_offer_round(4)`: `code-only`، وأثرُ العودةِ مكتوبٌ (**تشديدُ حكمٍ** لا فقدُ بياناتٍ) |
| الاختبارُ | لم يُضعَّفْ بل **زادَ**: ٢٣ و٢٤ صارَ محجوبُهما محجوباً **بوثيقةٍ منتهيةٍ** لا بغيابِ صفٍّ، وأُضيفَ ٢٦ (الرفضُ يحجبُ) و٢٧ (**غيابُ الوثائقِ كلِّها لا يُسقِطُ سائقاً اعتمدَته الإدارةُ** — حدُّ البندِ مقيسٌ لا مضمرٌ) |

**والمقاسُ محليّاً بعدَ الإصلاحَينِ.** 27 حالةَ وثائقٍ (0 إخفاق) · 3914 حالةَ
وحدةٍ (0 إخفاق بعدَ إضافةِ مدخلِ سجلِّ الارتدادِ؛ وقبلَه إخفاقانِ صادقانِ من
حاجزِ الارتدادِ نفسِه) · التكاملُ الكاملُ 871/877 · `tsc` بلا خطأٍ · `biome`
بلا خطأٍ (25 تحذيراً و1 معلومةٌ سابقتانِ) · عشرةُ حواجزَ خضراءُ منها
`check-migrations` (51 جدولاً بـRLS) و`check-migration-safety` (128 هجرةً) و
`check-schema-contract` (118 دالّةً · 25 جدولاً) و`check-rollback-safety`.

**ما لا يُدَّعى (`ح-5`).**
ــ **ستُّ حالاتٍ محليّةٍ ما زالَت مُخفِقةً وهيَ — بحسبِ قراءتي — أثرُ بيئةٍ لا
   أثرُ هذا العملِ**: مهلتا `beforeEach` في `safe-migration-runner` و
   `dispatch-nearby-candidates-postgis` (5s على صندوقٍ بنواتَينِ)، وخطّةُ
   `audit_log_action_entity_idx` في `hot-query-index-plans` (مُخطِّطُ PG 18
   المحليُّ يختارُ `audit_log_entity_idx` بدلَ `Seq Scan`)، و`quote.test.ts`
   («لا مدينةَ مفعَّلةً» — تزاحمُ زرعٍ متوازٍ). **ولم تظهرْ واحدةٌ منها في قائمةِ
   إخفاقاتِ CI**، وحكمُ CI بعدَ الدفعِ هوَ الفيصلُ لا هذا التفسيرُ.
ــ **لا `[x]`**: `F3-01` و`F12-14` يبقيانِ `[~]` حتّى ثلاثِ جولاتٍ خضراءَ
   متتاليةٍ (`ح-4`).
ــ **لا إنفاذَ لإلزامِ الوثائقِ ابتداءً**: منعُ اعتمادِ سائقٍ ناقصِ الوثائقِ
   بابُه لوحُ المراجعةِ (`SD-02`) — دَينٌ مُعلَنٌ.

## حُمرةٌ واحدةٌ كشفَت **شرطاً مُستعارَاً** لا عطبَ بندٍ — و`OPS-019` مفتوحٌ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الجولةُ [`34909694080`](https://github.com/uxxxug/ceezr/actions/runs/34909694080)
على فرعِ `feat/f3-01-driver-documents`: أربعُ وظائفَ خضراءُ (`verify` ·
`roadmap` · «تكامل على Redis حقيقي» · «فوضى متعدد المثيلات») **ووظيفةُ «تكامل
على PostgreSQL حقيقي» حمراءُ** بـ`871 pass · 1 fail`. والمُخفِقُ خطّافُ
`beforeAll` في `tests/integration/driver-documents.test.ts:181`.

**والسببُ ليسَ `F3-01` ولا `F12-14`**: الملفُّ كانَ **يستعيرُ شرطَه المسبقَ** —
يسألُ عن أوّلِ مدينةٍ `is_active` لها منطقةُ خدمةٍ مفعَّلةٌ، والبذرةُ تُنشئُ
المدنَ الخمسَ **معطَّلةً** بقرارِ `F2-05`. فعلى قاعدةٍ نظيفةٍ لا صفَّ،
والاختبارُ **يُخفِقُ منفرداً حتماً** — قِيسَ ذلكَ في الصندوقِ على PostgreSQL
حقيقيٍّ بقاعدةٍ مهجورةٍ حديثاً، لا استُنتِجَ.

**والحالةُ بعدَ الإصلاحِ**: الملفُّ صارَ يصنعُ شرطَه — يختارُ `JED` بالرمزِ،
ويُفعِّلُها **مستوفياً** قيدَ `cities_active_requires_groups` (القروباتُ
الثلاثةُ) لا مُخفِّفاً له، ثمَّ يردُّها إلى خطِّ الأساسِ في `afterAll`. والقياسُ
بعدَه: `27 pass · 0 fail · 90 توكيداً`، وخطُّ الأساسِ يعودُ بالفحصِ (`JED`
معطَّلةٌ بلا قروبات · صفُّ `city_service_areas` واحدٌ مفعَّلٌ). **ولا توكيدَ
حُذِفَ ولا خُفِّفَ، ولا `skip` أُضيفَ، ولا قيدَ قاعدةٍ أُرخيَ.**

**والحالةُ الجديدةُ المُعلَنةُ — `OPS-019` مفتوحٌ**: النمطُ عينُه قائمٌ في
**سبعةِ** ملفّاتٍ أُخرى (`quote` · `active-ride` · `ride-request` ·
`ride-summary` · `ride-history` · `ride-share` · `rider-support-intake`)،
وخضرتُها في CI **رهنُ ترتيبِ التشغيلِ** لا صحّةِ ما تقيسُ. وأخفقَ `quote` فعلاً
في الجولةِ المحلّيّةِ الكاملةِ بعدَ `five-cities-launch`. فحتّى يُغلَقَ
`OPS-019` **لا تُقرأُ خضرةُ السبعةِ إثباتاً** لِما تدّعي قياسَه، والحجزُ
مكتوبٌ في `ROADMAP.md` بنطاقِه وحاجزِه الآليِّ.

**وحالتانِ محلّيّتانِ لا تُنسَبانِ إلى الشيفرةِ ولا تُسكَتُ عنهما**:
`safe-migration-runner.test.ts` (مهلةُ خطّافٍ 5000ms) و
`hot-query-index-plans.test.ts:251` (مُخطِّطُ PG **18** المحليُّ اختارَ
`Index Scan` بكلفةِ 8.42 حيثُ يتوقّعُ الاختبارُ `Seq Scan`). والملفّانِ
**خارجَ تغييرِ هذا الفرعِ** بالقياسِ (`git diff --name-only main...HEAD`)،
وCI يُشغِّلُ `postgis/postgis:17-3.5`. **فحكمُ CI هوَ الفيصلُ** ولا تُبدَّلُ
توكيداتُهما لإرضاءِ بيئةٍ محلّيّةٍ؛ وإن ظهرَا في CI فُتِحَ لهما حجزٌ باسمِهما.

ــ **ولا `[x]`**: `F3-01` و`F12-14` يبقيانِ `[~]` حتّى ثلاثِ جولاتٍ خضراءَ
متعاقبةٍ (`ح-4`).

## `OPS-019` مُغلَقٌ: معينٌ واحدٌ للشرطِ المسبقِ، وحاجزٌ بخمسِ قواعدَ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

القسمُ الذي قبلَهُ يبقى كما كُتِبَ ولا يُمحى؛ وهذا **حكمُهُ** بعدَ التنفيذِ.

**ما كانَ.** ثمانيةُ ملفّاتِ تكاملٍ تختارُ مدينتَها بـ`where c.is_active`
والبذرةُ تُنشئُ مدنَ الإطلاقِ معطَّلةً (`F2-05`)، وثمانيةٌ وثلاثونَ ملفّاً
تُفعِّلُ مدينةً **ولا تردُّها**. فخضرةُ الثمانيةِ مُستعارةٌ من ترتيبِ الاكتشافِ،
ودمجُ `F3-01` غيَّرَ الترتيبَ فأحمرَ CI (التشغيلُ `34909694080`).

**ما صارَ.** معينٌ واحدٌ `tests/support/active-city.ts`: `ensureActiveCity`
تلتقطُ لقطةً كاملةً لصفِّ `JED` (`is_active` والأعمدةَ الثلاثةَ للقروباتِ) ثمَّ
تُفعِّلُ، و`restoreCityBaseline` تكتبُ اللقطةَ **حرفاً**. و`options.prior`
تُبقي **أوّلَ** لقطةٍ وحدَها حاكمةً — وهوَ لازمٌ لأنَّ ستّاً وثلاثينَ ملفّاً
تُنادي المعينَ في `beforeEach`. وستٌّ وأربعونَ ملفّاً أُعيدَ توصيلُها إليهِ.

**الإنفاذُ.** `scripts/lib/city-precondition-audit.ts` حَكَمٌ نقيٌّ بخمسِ قواعدَ
(لا شرطَ مُستعارٌ · من فعَّلَ ردَّ · منادي المعينِ يردُّ · المعينُ نفسُهُ
مُستوفٍ · لا إعفاءَ ميّتٌ)، وسجلُّ إعفاءاتِهِ **فارغٌ بقصدٍ**. وخطوتانِ
مُسمّاتانِ في `verify` وسلسلةِ `ci`: الحاجزُ، وعشرونَ حالةَ وحدةٍ سالبةً (`ح-7`).

**المقيسُ.** السبعةُ منفردةً: من `0 pass · 1 fail` لكلِّ واحدٍ إلى نجاحٍ كاملٍ
(٢١ · ٢٠ · ٢٢ · ٣١ · ٣١ · ٢١ · ١٦)، و`driver-documents` ٢٧. وخروقُ الحاجزِ من
**٣٩** إلى **صفرٍ** في ٩٦ ملفّاً. وخطُّ الأساسِ مقروءٌ من القاعدةِ بعدَ كلِّ
تشغيلٍ: `is_active = false` وقروباتٌ `null`. والحزمةُ كاملةً محليّاً:
`main` نفسُهُ `868 pass · 11 fail` مقابلَ الفرعِ `892 pass · 5 fail` — والخمسةُ
الباقيةُ **تُخفِقُ على `main` كذلكَ** فلا تُنسَبُ إلى البندِ، ولا يُضعَّفُ منها
توكيدٌ واحدٌ. القرارُ: `ADR 0116`، وحدودُ المُدَّعى في §٤ منهُ.

**واكتُشِفَ أثناءَهُ** أنَّ `check-test-city-activation.ts` و
`check-integration-city-precondition.ts` يحكُمانِ موضوعاً واحداً بسجلَّي
استثناءاتٍ منفصلَينِ — تكرارٌ في مصدرِ الحقيقةِ. حُجِزَ توحيدُهُ في `OPS-020`
ولم يُمَسَّ ههنا. والأخضرُ المحليُّ ليسَ حكماً؛ الحكمُ حُمرةُ CI أو خضرتُهُ.

## `F3-02` — عروضُ السائقِ: مؤقّتٌ بحقيقةِ خادمٍ وقبولٌ بكاتبٍ واحدٍ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الأقسامُ التي قبلَهُ تبقى كما كُتِبَت ولا تُمحى؛ وهذا **حكمُ البندِ** بعدَ
التنفيذِ.

**ما كانَ.** الدَّرْزُ يكتبُ صفوفَ `order_offers` بمهلةٍ لكلِّ عرضٍ، و`claim_ride`
تُقبِّلُ عرضاً واحداً ذرّيّاً — **ولا بابَ للسائقِ يقرأُ عرضَهُ ولا يقبلُه**: لا
دالّةَ قراءةٍ، ولا مسارَ بوّابةٍ، ولا شاشةَ. فكانَ الجانبُ المرئيُّ من دورةِ
العروضِ معدوماً معَ أنَّ محرِّكَها قائمٌ.

**ما صارَ.** ثلاثُ دوالَّ في هجرةِ توسيعٍ واحدةٍ
(`20260915020000_f3_02_driver_offer_functions.sql`)، كلُّها `security definer`
بمسارِ بحثٍ مثبَّتٍ ومنزوعةُ التنفيذِ عن `public`/`anon`/`authenticated` وممنوحةٌ
لـ`service_role` وحدَهُ: لوحٌ، وتفصيلٌ، وقبولٌ **يُفوِّضُ** إلى `claim_ride`.
وطبقةُ تطبيقٍ بخمسةَ عشرَ رمزَ خطأٍ عامّاً، وخمسةُ مساراتِ بوّابةٍ، وشاشتانِ
موصولتانِ من جذرِ السائقِ، و64 مفتاحاً بثلاثِ لغاتٍ.

**الحكمُ المُنفَذُ.** `expires_at` **لا يُنشَرُ ألبتّةَ**؛ يُنشَرُ `seconds_left`
و`server_time` فالعدُّ فارقٌ محليٌّ تُلغى فيهِ إزاحةُ ساعةِ الجهازِ، والصلاحيّةُ
`is_claimable` حكمُ خادمٍ. والقبولُ **بلا كاتبٍ ثانٍ**: لا `for update` ولا
`skip locked` ولا `update orders` ولا `update order_offers` في الهجرةِ. والجوابُ
**ثلاثةُ مفاتيحَ** (`ok` · `order_id` · `matched_at`) فهويّةُ الراكبِ تُسقَطُ من
حِملِ `claim_ride`. وكلُّ مسافةٍ **موسومةٌ** `{kind:'STRAIGHT_LINE',meters}` أو
`null`.

**«التقديرُ» طبقةٌ غائبةٌ بإعلانٍ.** `SD-03` و`SD-04` يطلبانِ تقديراً،
و**المقيسُ أن لا عمودَ أجرةٍ في المخطَّطِ كلِّهِ** ولا مفتاحَ تعريفةٍ في
`platform_settings`. فلا رقمٌ يُخترَعُ (يُقرأُ وعداً) ولا حقلٌ يُنشَرُ فارغاً
(يُقرأُ عطباً): أُعلِنَ الغيابُ في `ADR 0117` §٣ و`ROADMAP.md`، ولا مفردةَ مالٍ
في شيفرةٍ ولا SQL ولا قاموسٍ — يحرسُها الحاجزُ.

**وحاجزٌ قائمٌ أسقطَ الشاشتَينِ فعُولِجَ في الجذرِ.** أوّلُ نسختَيهما حرَّكَتا
قراءةَ الساعةِ بـ`setInterval` كلَّ ثانيةٍ **بلا نداءِ شبكةٍ**، فأسقطَهما
`scripts/check-system-screens-policy.ts` بموضعَينِ: `F1-07` و`ADR 0035` §٤
يمنعانِ **كلَّ** مؤقّتٍ دوريٍّ في التطبيقِ المصغَّرِ لا الاستقصاءَ الشبكيَّ
وحدَه. ولم يُستَثنَ الحاجزُ ولم يُلتَفَّ عليهِ بـ`requestAnimationFrame`:
**الباقي محسوبٌ لحظةَ الرسمِ** من آخرِ قراءةٍ ويُحدَّثُ بفعلِ السائقِ، والنصُّ
المُعلَنُ في الشاشةِ صُحِّحَ في اللغاتِ الثلاثِ ليقولَ ذلكَ لا ليَدَّعيَ عقرباً
يدقُّ.

**الإنفاذُ.** `scripts/check-driver-offers-contract.ts` بثمانِ قواعدَ، ولكلِّ
واحدةٍ **حالةُ افتراقٍ مزروعةٌ** (`ح-7`) — فالحاجزُ مقيسٌ ساقطاً لا مفروضاً
صادقاً — وخطوتانِ مُسمّاتانِ في `verify` ومدخلٌ في سلسلةِ `ci`.

**المقيسُ محلّيّاً.** 24 حالةَ تكاملٍ على PostgreSQL حقيقيٍّ:
`24 pass · 0 fail · 73 توكيداً` — ومنها **قبولانِ متزامنانِ على اتّصالَينِ
مستقلَّينِ يُنجِحانِ واحداً فقط** وصفُّ `accepted` واحدٌ بالعدِّ، وتناقصُ الباقي
بينَ قراءتَينِ حقيقيّتَينِ، وغيابُ `expires_at` من JSON بالفحصِ. وحاجزُ الوحدةِ
`41 pass · 0 fail · 49 توكيداً`. وأوّلُ تشغيلٍ للاختبارِ التكامليِّ **أصابَ في
الاختبارِ نفسِهِ لا في الميزةِ**: صفُّ توفُّرٍ بلا `city_id`، وزرعُ
`distance_km = null` يُقلَبُ افتراضاً بـ`??` — فأُصلِحا بلا إضعافِ توكيدٍ.

**وما لا يُدَّعى.** ذرّيّةُ `claim_ride` **سابقةٌ** لا مكسبَ هذا البندِ (المقيسُ
أنَّ الغلافَ لم يُضِفْ كاتباً) · و`distance_km` وترٌ محسوبٌ وقتَ الدَّرْزِ لا
يُعادُ قياسُه · وانحرافُ ساعةِ الأجهزةِ الحقيقيِّ غيرُ مقيسٍ · و**العدُّ لا يدقُّ** فالرقمُ صادقٌ
لحظةَ رسمِهِ ويَقدُمُ بينَ رسمَينِ (حدُّ `F1-07`) · ولا إشعارَ فوريَّ
ولا خريطةَ مرسومةً (دَينٌ مُعلَنٌ في الشاشةِ) · والأخضرُ المحليُّ ليسَ حكماً؛
الحكمُ حُمرةُ CI أو خضرتُهُ.

## `F3-03` — مَهمّةُ السائقِ النشطةُ: الطَورُ ختمُ إنسانٍ لا استنتاجُ مسافةٍ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الأقسامُ التي قبلَهُ تبقى كما كُتِبَت ولا تُمحى؛ وهذا **حكمُ البندِ** بعدَ
التنفيذِ.

**ما كانَ.** القبولُ في `F3-02` يُنتِجُ طلباً `matched` مُسنَداً إلى السائقِ،
ثمَّ **ينقطعُ الطريقُ**: لا بابَ يقرأُ به السائقُ مَهمّتَهُ، ولا زرَّ يُقدِّمُ
بهِ طَوراً. و`start_ride`/`complete_ride` قائمتانِ ذرّيّتَينِ منذُ آبَ **بلا
واجهةٍ تُنادِيهما من سطحِ السائقِ**. وطَورُ «وصلتُ» **لا عمودَ لهُ ألبتّةَ**:
`orders` فيها `matched_at` و`started_at` و`completed_at` ولا `arrived_at` — وهوَ
عائقٌ كُتِبَ صريحاً في `F2-06`.

**ما صارَ.** هجرةُ توسيعٍ واحدةٌ (`20260915030000_f3_03_driver_active_job.sql`)
تُضيفُ العمودَ `orders.arrived_at` وقيدَهُ `orders_arrived_requires_driver`
وأربعَ دوالَّ `security definer` بمسارِ بحثٍ مثبَّتٍ، منزوعةِ التنفيذِ عن
`public`/`anon`/`authenticated` وممنوحةٍ لـ`service_role` وحدَهُ:
`driver_active_job` · `driver_mark_arrived` · `driver_start_ride` ·
`driver_complete_ride`. وطبقةُ تطبيقٍ بأحدَ عشرَ رمزَ خطأٍ عامّاً، وأربعةُ
مساراتِ بوّابةٍ، وشاشةٌ موصولةٌ من جذرِ السائقِ بأربعةِ ملفّاتٍ، و46 مفتاحاً
بثلاثِ لغاتٍ، ومدخلُ خروجٍ واحدٌ في سجلِّ الحدودِ (`W-6`).

**الحكمُ المُنفَذُ.** **الطَورُ ختمٌ لا استنتاجٌ**: لا قراءةَ موقعٍ ولا
`st_distance` ولا مقارنةَ لحظةٍ في الشريحةِ كلِّها لتقريرِ طَورٍ. **والزرُّ اسمٌ
من الخادمِ**: `next_action` ∈ {`MARK_ARRIVED`, `START_RIDE`, `COMPLETE_RIDE`,
`null`}، والشاشةُ **مُمنوعةٌ حرفيّاً** من `"matched"` و`"in_progress"` فمصدرُ
حقيقةِ الأطوارِ واحدٌ. **ولِـ`arrived_at` كاتبٌ واحدٌ** في المستودعِ كلِّه،
يقفلُ `for update` ويُرَدُّ `ALREADY_ARRIVED` **معَ الختمِ الأوّلِ نفسِه** فلا
يُزحزَحُ؛ ويحرسُهُ قيدُ مخطَّطٍ فتجاوزُ الكاتبِ يسقطُ منَ المحرِّكِ. **والبدءُ
والإنهاءُ مُفوَّضانِ**: لا `update orders set status` ولا `update order_offers`
ولا `update driver_availability` في الهجرةِ، ورموزُ الكاتبِ الخامَّةُ تُترجَمُ
إلى `PHASE_MISMATCH`/`NOT_A_DRIVER`/`TRANSITION_REFUSED` لا تُمرَّرُ. **والمِلكيّةُ
قيدُ استعلامٍ**: مَهمّةُ غيرِه ومعرِّفٌ معدومٌ يُرَدّانِ `JOB_NOT_FOUND` سواءً.
**وحمولةُ الراكبِ اسمٌ أوّلٌ ولغةٌ** — ثمانيةُ حقولٍ محجوبةٌ حرفيّاً، والمباحُ
مباحٌ **بإعلانٍ** في الحاجزِ.

**وقرارٌ يُعلَنُ لا يُسكَتُ عنه.** بدءُ الرحلةِ **بلا ختمِ وصولٍ يمرُّ**: لأنَّ
`start_ride` — الكاتبُ القائمُ — لا تشترطُهُ، ولو اشترطَهُ الغلافُ لَصارَ
حَكَماً موازياً لحالةِ الطلبِ. وأثرُ النسيانِ **مُعلَنٌ عَدَماً** لا صفراً
(`ADR 0023`) فمَن قاسَ الانتظارَ غداً وجدَ غياباً صريحاً لا رقماً مُخترَعاً.

**الإنفاذُ.** `scripts/check-driver-job-contract.ts` بثمانِ قواعدَ (تطابقُ
اللغاتِ · تطابقُ نصوصِ الأخطاءِ في الاتّجاهَينِ · منعُ لفظِ المالِ · الكاتبُ
الواحدُ · نزعُ الصلاحيّاتِ · صدقُ الطَورِ · استيفاءُ `STATUS_BY_ERROR` ·
خصوصيّةُ الراكبِ)، ولكلِّ واحدةٍ **حالةُ افتراقٍ مزروعةٌ** (`ح-7`) — 44 حالةَ
وحدةٍ — وخطوتانِ مُسمّاتانِ في `verify` ومدخلٌ في سلسلةِ `ci`. **والحاجزُ سقطَ
على نموذجٍ فأُصلِحَ**: `start_ride(` كانَ يُطابِقُ اسمَ الغلافِ نفسِه
(`driver_start_ride`) فصارَ بلَحاقٍ خلفيٍّ `(?<!driver_)` — قاعدةٌ كانَت تُقرأُ
مُنفَذةً وهيَ لا تحرسُ.

**المقيسُ محلّيّاً.** 19 حالةَ تكاملٍ على PostgreSQL حقيقيٍّ:
`19 pass · 0 fail · 57 توكيداً` — ومنها **ختمانِ متزامنانِ يُنجِحانِ واحداً
ولحظةُ الوصولِ لا تُزحزَحُ**، و**إنهاءٌ يُعيدُ السائقَ متاحاً** برهاناً على
التفويضِ لا على النسخِ، و**قيدُ المخطَّطِ يُسقِطُ كتابةً تتجاوزُ الكاتبَ**،
وعَدُّ مفاتيحِ `rider` وغيابُ الهاتفِ والمعرِّفِ من الحمولةِ بالفحصِ. والملفُّ
**يُنشئُ مدينتَه المفعَّلةَ ويُعيدُها** (`OPS-019`). وأرضيّاتُ `skip-audit`
رُفِعَت بالزيادةِ 100→101 ملفّاً و1043→1062 حالةً.

**وما لا يُدَّعى.** صدقُ الختمِ ميدانيّاً **غيرُ مقيسٍ** — المقيسُ أنَّ النظامَ
لا يخترعُه ولا يُزحزِحُه وأنَّ صاحبَهُ مُسمّىً في `audit_log` · ومدّةُ
الانتظارِ **غيرُ محسوبةٍ** · **وعائقُ `F2-06` يبقى مفتوحاً**: سببُهُ زالَ
(العمودُ وكاتبُهُ وُجِدا) وسطحُ الراكبِ **لم يُبنَ**، وعائقُ «لا تتبُّعَ حيَّ»
(`ADR 0035` §٤) قائمٌ · **واستغاثةُ السائقِ زيادةٌ ثانيةٌ مُعلَنةٌ دَيناً**
(`createSafetyRoutes` بدورٍ واحدٍ، خارجَ النطاقِ المحجوزِ) · ولا خريطةَ مدمَجةٌ
(`ADR 0007`) · ولا أثرَ صلاحيّاتٍ مقيسٌ · وذرّيّةُ
`start_ride`/`complete_ride` **سابقةٌ** لا مكسبَ هذا البندِ · والأخضرُ المحليُّ
ليسَ حكماً؛ الحكمُ حُمرةُ CI أو خضرتُهُ.

### وتصحيحٌ بالإضافةِ — رابطُ الملاحةِ صارَ حقيقةَ خادمٍ (`F3-03` · `ح-8`)

حكمُ CI الأوّلُ على PR ‏#44 أسقطَ `verify` عندَ حاجزِ النطاقِ الواحدِ
(`F1-10` · `TG-005`): شيفرةُ المصغَّرِ كانت تُركِّبُ `https://maps.google.com`.
فصارَت البوّابةُ تنشرُ `navigation_url` في كلِّ نقطةٍ من حمولةِ المَهمّةِ،
والشاشةُ تفتحُ ما أُعطِيَت. وموضعُ السجلِّ في سجلِّ المخارجِ (`W-6`) صارَ
`apps/gateway/src/routes/driver-job.ts`، **وقاعدةٌ تاسعةٌ** في حاجزِ العقدِ
تمنعُ الرجعةَ بخمسِ حالاتٍ مزروعةٍ (44 ⇒ 49 حالةَ وحدةٍ). ولم تُوسَّعْ
القائمةُ المغلقةُ: توسيعُها يُوسِّعُ `script-src` لِمُضيفٍ لا نُشغِّلُ منه
شيفرةً — إضعافُ حاجزٍ لِسببٍ لا يستحقُّه.

## `F3-04` — بثُّ الموقعِ التكيّفيُّ: النبضةُ لها سببٌ منشورٌ ومُدّةٌ من الخادمِ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الأقسامُ التي قبلَهُ تبقى كما كُتِبَت ولا تُمحى؛ وهذا **حكمُ البندِ** بعدَ
التنفيذِ.

**ما كانَ.** المستقبِلُ قائمٌ منذُ `F4-01`: `POST /v1/driver/location` بحدِّ
معدَّلٍ وحدِّ حجمٍ وتصفيةِ إصلاحاتٍ. **ولا أحدَ يبثُّ إليه**: سطحُ السائقِ يقرأُ
مَهمّتَهُ ولا يُرسِلُ موضعاً، و`tg/location.ts` غلافٌ كُتِبَ في رأسِه أنَّ سياسةَ
البثِّ **ليست فيه**.

**ما صارَ.** كتلةٌ واحدةٌ في **جذرِ** حمولةِ `driver_active_job` في مَخرجَيها
كِلَيهِما: `location_broadcast {reason, interval_seconds}`. والسببُ من الخادمِ
(`ON_TRIP` · `TO_PICKUP` · `AVAILABLE`)، والمُدّةُ من `platform_settings`
لمدينةِ السائقِ بثلاثةِ مفاتيحَ، **والغيابُ يُنشَرُ `null`** — إعدادٌ غائبٌ أو
غيرُ موجبٍ يُسكِتُ البثَّ سكوناً مُعلَناً ولا يُخترَعُ لهُ رقمٌ في عميلٍ.
**وختمُ الوصولِ يُسكِتُ النبضةَ** حتّى بدءِ الرحلةِ. والقرارُ دالّةٌ **نقيّةٌ**
في النطاقِ بلا ساعةٍ ولا مؤقّتٍ، والمؤقّتُ في الغلافِ وحدَه، **والمُرسِلُ واحدٌ**
إلى مسارِ `F4-01` بلا تعديلِ سطرٍ فيه. وحاجزٌ ساكنٌ بثمانِ قواعدَ لكلٍّ افتراقٌ
مزروعٌ، و16 حالةَ تكاملٍ على PostgreSQL حقيقيٍّ، و14 حالةَ وحدةٍ للقرارِ
(ADR 0119).

**ودرسٌ من المحرِّكِ**: `order_status` لا يُحوَّلُ ضمناً إلى `text`، فسقطَت
القراءةُ كلُّها عندَ أوّلِ مَهمّةٍ حقيقيّةٍ — ولم يرَهُ فحصُ أنواعٍ ولا حاجزٌ
ساكنٌ ولا نجاحُ هجرةٍ، بل اختبارُ تكاملٍ على قاعدةٍ حقيقيّةٍ.

**وما لمّا يُبنَ**: لا سطحَ راكبٍ يُبَثُّ إليه (عائقُ `F2-06` **مفتوحٌ** ·
ADR 0035 §٤) · ولا خريطةَ · ولا نبضةَ في الخلفيّةِ · ولا قياسَ بطاريّةٍ.

## `F3-05` — الشفافيّةُ **مقامٌ يُنشَرُ** لا شِعارٌ (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الأقسامُ التي قبلَهُ تبقى كما كُتِبَت ولا تُمحى؛ وهذا **حكمُ البندِ** بعدَ
التنفيذِ.

**ما كانَ.** السائقُ يعملُ ولا يرى من عملِه شيئاً: لا عددَ رحلاتٍ، ولا ساعةَ
تواجدٍ، ولا نسبةَ قبولٍ، ولا يعرفُ **بأيِّ شيءٍ يُسنَدُ إليه** — والمعادلةُ
قائمةٌ في `scoreCandidate` تقرأُ قُرباً وتقييماً ومنطقةً مُفضَّلةً، **مقروءةٌ في
الشِفرةِ ومحجوبةٌ عن صاحبِ الشأنِ**. و`SD-06` و`SD-09` موجودانِ في المُتطلَّباتِ
بلا مسارٍ يخدمُهما.

**ما صارَ.** أربعُ دوالِّ قراءةٍ (`stable`) في هجرةِ توسيعٍ واحدةٍ — **بلا كاتبٍ
ولا عمودٍ مُجمَّعٍ مخزونٍ** — ومسارانِ في البوّابةِ وشاشةٌ واحدةٌ:

- **كلُّ نسبةٍ كسرٌ بمقامِه** `{numerator, denominator, rate}` و`rate: null`
  عندَ مقامٍ صفرٍ. **ومقامُ القبولِ العروضُ الواصلةُ إليه** لا طلباتُ المدينةِ:
  لا يُحاسَبُ على ما لم يُعرَضْ عليه. **ومقامُ الإلغاءِ ما قبِلَه** لا ما عُرِضَ
  عليه، وإلّا خفَّت النسبةُ بكثرةِ العروضِ فصارَ العدَّادُ يُجمِّلُ نفسَه.
- **والمالُ غيابٌ مُعلَنٌ بسببِه**: `money {amount: null, basis:
  'NOT_INTERMEDIATED'}`. لا عمودَ أجرةٍ في المخطَّطِ ولا وساطةَ نقدٍ
  (`ADR 0039` §٤ · `DEC-11`)، فأيُّ رقمٍ تحتَ «أرباحُك» **تقديرٌ**، و`0` أسوأُ
  من الحذفِ: يُقرأُ «لم تكسِبْ» وحقيقتُه «لا نعلمُ». والحقلُ يبقى **بسببِه** كي
  لا تخترعَ شاشةٌ صفراً، والمجالُ **مغلقٌ بقيمةٍ واحدةٍ** فيومَ يصيرُ للمنصّةِ
  وسيطُ دفعٍ (`SD-08`) **يُسقِطُ المُترجِمُ** كلَّ قارئٍ لم يُحرَّرْ.
- **والنافذةُ حقيقةُ خادمٍ بمنطقةِ زمنِ المدينةِ** من `platform_settings`
  (`[from, to)` والمنطقةُ **تُنشَرُ لتُعرَضَ** فيفهمَ السائقُ لِمَ رحلةُ
  الثانيةِ صباحاً في يومِ أمسِ)، وغيابُ الإعدادِ `WINDOW_UNRESOLVED` ⇒ `409`
  **رفضٌ مُسمّىً** لا UTC مُرتَدّاً: رقمٌ محسوبٌ على نافذةٍ خاطئةٍ يُقرأُ صحيحاً
  ولا يُكتشَفُ أبداً.
- **والتواجدُ يُشتَقُّ لا يُخزَنُ**: ثوانيه من **أزواجِ تبديلِ
  `attendance_log`** مقصوصةً على حدِّ النافذةِ وعلى الآنِ، ومعَها `open` تقولُ
  إنَّ الرقمَ ما زالَ يزيدُ. وعدَّادٌ مخزونٌ يُنسى تحديثُه مرّةً **يكذبُ إلى
  الأبدِ**.
- **والتقييمُ تراكميٌّ لا نافذيٌّ** — هوَ الرقمُ الذي تقرأُه معادلةُ الإسنادِ
  نفسُها — ويستثني المَوسومَ (`is_flagged`) كما تستثنيهِ، وحدُّ الثقةِ غيرُ
  المضبوطِ `below_trust: null` **فلا يُخترَعُ حكمٌ**.
- **وعواملُ الترتيبِ وأوزانُها من نفسِ صفوفِ إعداداتِ المطابَقةِ**، ومعَها
  `behaviour_affects_ranking: false` — **مفتاحٌ من الخادمِ** لا نصٌّ في شاشةٍ
  يُخيفُ سائقاً بحُكمٍ لا تقرأُه المعادلةُ.

وحاجزٌ ساكنٌ **بتسعِ قواعدَ** لكلٍّ افتراقٌ مزروعٌ (`ح-7`)، و52 مفتاحاً بثلاثِ
لغاتٍ، و35 حالةَ وحدةٍ، و23 حالةَ تكاملٍ على PostgreSQL حقيقيٍّ (ADR 0120).

**ودرسٌ من الحاجزِ نفسِه**: أوّلُ تشغيلٍ للحاجزِ أعطى **ثلاثَ مخالفاتٍ كاذبةٍ**،
وسببُها أنَّ `telegram_id` **مفتاحُ المِلكيّةِ المشروعُ في الخادمِ** وحرامٌ في
العميلِ — فقاعدةٌ تمنعُه في كلِّ الطبقاتِ تمنعُ الصوابَ. فشُقَّت رموزُ الهويّةِ
طبقتَينِ ولم يُضعَّفْ حاجزٌ ولم يُسكَتْ.

**وزيادةٌ في القائمةِ المغلقةِ للمساراتِ الحرجةِ** (`ح-8`): زِيدَ
«شفافيّةُ الأداءِ والحصيلة» لأنَّ رقماً كاذباً عن رحلةٍ تمَّت سليمةً
**لا يُسقِطُ شيئاً** — والضررُ ثقةٌ تُفقَدُ ولا تُستردُّ — فتصنيفُه تحتَ دورةِ
الرحلةِ يُخفيهِ. ورُفِعَ حِرزُ `skip-audit` إلى **103 ملفّاً و1101 حالةً** رفعاً
بالزيادةِ لا استبدالاً.

**وما لمّا يُبنَ (`ح-5`)**: لا مبلغَ يُعرَضُ — **وذاكَ عائقٌ مفتوحٌ** يُغلَقُ في
`SD-08` لا يُداوى بنصٍّ · ولا تصنيفَ سائقٍ ولا درجةَ ولا وسمَ تحفيزٍ · ولا
مقارنةَ بسائقٍ آخرَ ولا متوسّطَ مدينةٍ · ولا مسافةَ طريقٍ (خطٌّ مستقيمٌ
**موسومٌ بأساسِه** · ADR 0007) · ولا ذاكرةَ للتقريرِ · ولا قياسَ أداءٍ على
بياناتٍ إنتاجيّةٍ. **ولا `[x]` قبلَ ثلاثِ جولاتِ CI خضراءَ متتاليةٍ (`ح-4`).** **(2026-09-16): الشرطُ مُستوفى** — عشرُ جولاتٍ خضراءَ متتاليةٍ على `main` (`34953942937` … `35040522114`)، **فقُلِبَ إلى `[x]`**.

## `F3-06` — لوحُ اشتراكِ السائقِ: **السعرُ من `platform_settings` لا ثابتٍ** (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الـ`Mini App` تَعرضُ اشتراكَ السائقِ **بكلِّ مَعطياتِه من القاعدةِ لا من
كودٍ**: `GET /v1/driver/subscription` يَقرأُ `driver_subscription_dashboard` التي
تُجمِعُ الحالَ والخطةَ والسعرَ من `platform_settings` وأيّامَ الصلاحيةِ والتحذير
قبلَ الانتهاءِ وحالَ الإلغاءِ، و`GET /v1/driver/subscription/history` يَقرأُ
`driver_subscription_history` لآخرِ الدفعاتِ بسقفٍ خادميٍّ (١…٥٠). **والتجديدُ
«ابدأِ الدفعَ» لا «أكملِه»**: `POST /v1/driver/subscription/renew` يُعيدُ استخدامَ
`subscribePlan` القائمِ الذي يقرأُ السعرَ من `priceReader` ويُنشئُ معاملةَ دفعٍ
بمفتاحِ إيديمبوتنسيّةٍ، وعندَ غيابِ المزوّدِ ⇒ `PAYMENT_PROVIDER_NOT_AVAILABLE`
`503` — **فشلٌ مغلقٌ** لا نجاحٌ كاذبٌ. **والملكيّةُ في القاعدةِ**: `USER_NOT_FOUND`
و`NOT_A_DRIVER` من الدالّتَينِ لا من الطبقةِ. و٩ رموزِ عطبٍ منشورةٍ، و٥٧ مفتاحاً
بثلاثِ لغاتٍ (٧٩٢ ← ٩٠٥)، و٤٤ قاعدةَ نمطٍ ببادئةِ `dsub` مُسجَّلةٍ في `DECLARED_BLOCKS`.
وحاجزٌ ساكنٌ **بخمسِ قواعدَ** لكلٍّ افتراقٌ مزروعٌ (`ح-٧`)، و١٤ حالةَ وحدةٍ و١٣ حالةَ
تكاملٍ (تُتخطّى بلا `TEST_DATABASE_URL`). الهجرةُ لم تُطبَّقْ على قاعدةٍ حقيقيّةٍ
(`ح-٥`)، والتجديدُ لم يُختَبرْ بمزوّدِ دفعٍ حقيقيٍّ — المزوّدُ اليدويُّ يُرجِعُ `pending`
بلا `checkoutUrl`، ولا `[x]` قبلَ ثلاثِ جولاتِ CI خضراءَ (`ح-٤`). **(2026-09-16): الشرطُ مُستوفى** — ثماني جولاتٍ خضراءَ متتاليةٍ على `main` (`34987624903` … `35040522114`)، **فقُلِبَ إلى `[x]`**.

## `F3-07` — مركبتي + الشعارُ والباركودُ: **العرضُ والتحديثُ في ثلاثةِ مساراتَ** (2026-09-15 · إضافةٌ لا محوٌ · `ح-8`)

الـ`Mini App` تَعرضُ مركبةَ السائقِ **بنوعِها ولوحتِها وسنةِ صنعِها ووثائقِها الثلاثِ**
(رخصةُ السيرِ، التأمينُ، الفحصُ الفنّيُّ) بحالاتِها وتواريخِ انتهائِها، وملفَّي الشعارِ
والباركود. **والتحديثُ ثلاثُ مساراتَ**: `GET /v1/driver/vehicle` يَقرأُ `driver_vehicle`
التي تُجمِعُ كلَّ ذلك في نداءٍ واحدٍ، و`PATCH /v1/driver/vehicle` يُحدِّثُ النوعَ واللوحةَ
والسنةَ، و`POST /v1/driver/vehicle/assets` يُحدِّثُ مسارَي الشعارِ والباركود. **والملكيّةُ
في القاعدةِ**: `USER_NOT_FOUND` و`NOT_A_DRIVER` من الدوالِّ الثلاثِ لا من الطبقةِ. **والفشلُ
مغلقٌ**: غيابُ المخزنِ ⇒ `503`. **وسنةُ الصنعِ تُتحقَّقُ منها** في الطبقةِ
(`validateVehicleYear` بحدودٍ `VEHICLE_YEAR_MIN`/`MAX`). **والبنيةُ القائمةُ تُعادُ**:
`vehicle_type` و`plate_number` موجودانِ مسبقًا، `vehicle_photo_file_id` من `F3-01`،
`driver_documents` يخدمُ الوثائقَ الثلاثَ، `MiniAppSessionReader` يُعادُ استخدامُه. و٧ رموزِ
عطبٍ منشورةٍ، و٤٩ مفتاحاً بثلاثِ لغاتٍ (٩٠٥ ← ٩٥٤)، وبادئةُ `dveh` مُسجَّلةٌ في `DECLARED_BLOCKS`.
وحاجزٌ ساكنٌ **بخمسِ قواعدَ** لكلٍّ افتراقٌ مزروعٌ (`ح-٧`)، و١٠ اختباراتِ وحدةٍ و١٣ حالةَ
تكاملٍ (تُتخطّى بلا `TEST_DATABASE_URL`). الهجرةُ لم تُطبَّقْ على قاعدةٍ حقيقيّةٍ (`ح-٥`)،
ولا `[x]` قبلَ ثلاثِ جولاتِ CI خضراءَ (`ح-٤`).

### زيادةٌ على `F3-07` (`ح-8`) — العقدُ صُحِّحَ بعدَ حكمِ CI، والدرسُ صارَ حاجزاً (2026-09-15 · `ADR 0121`)

حكمُ CI الأوّلُ ([35005161181](https://github.com/uxxxug/ceezr/actions/runs/35005161181))
أسقطَ وظيفةَ `تكامل على PostgreSQL حقيقي` وحدَها، وكشفَ **عطبَ منتَجٍ**: تواريخُ
انتهاءِ الوثائقِ الثلاثِ كانت تُخرَجُ من `driver_vehicle` بنوعِ `date`، فيُحوِّلُها
`postgres.js` إلى `Date` عندَ منتصفِ ليلِ UTC، فتقرأُها `readDate` (عقدُها
`string | null`) **عَدَماً** — فتختفي تواريخُ الانتهاءِ من شاشةِ «مركبتي» بلا خطأٍ
ولا سجلٍّ، وهيَ الحقيقةُ التي تحجُبُ السائقَ عن العملِ (`ADR 0115`).

**فصارَ في الحالةِ**: عقدُ الخرجِ `text` بـ`to_char(x,'YYYY-MM-DD')`؛ وحاجزٌ ساكنٌ
جديدٌ `scripts/check-date-only-boundary.ts` يمسحُ **١٣٤ هجرةً** فيمنعُ عمودَ `date`
في عقدِ خرجِ أيِّ دالّةٍ (مُعامِلُ الدخلِ و`timestamptz` مُباحانِ)؛ وعشرُ حالاتِ
وحدةٍ تزرعُ السالبةَ التي أسقطَت CI (`ح-7`)؛ وخطوتانِ مُسمّاتانِ في `verify`؛
و`ADR 0121` يحملُ القرارَ والبديلَ المرفوضَ.

**وقدرةُ قياسٍ جديدةٌ في بيئةِ العملِ**: PostgreSQL 18 + PostGIS 3.6 مُنصَّبانِ في
الصندوقِ، والهجراتُ الـ١٣٤ تُطبَّقُ بـ`scripts/migrate.ts` (طريقُ CI نفسُه ·
`ADR 0068`)، فاختباراتُ التكاملِ **تُقاسُ محليّاً قبلَ الدفعِ** بعدَ أن كانت تُتخطّى:
`driver-vehicle` ٩/٩ خضراءُ. **وحكمُ CI يبقى الحاكمَ** (`ح-6`) — خادمُه إصدارُ ١٧.

### زيادةٌ ثانيةٌ على `F3-07` — **التعليقُ الصامتُ** صارَ ممنوعاً حاجزاً وسقفاً (`ADR 0122`)

حكمُ CI الثانِ ([35013549945](https://github.com/uxxxug/ceezr/actions/runs/35013549945))
لم يكن أحمرَ ولا أخضرَ: وظيفةُ `تكامل على PostgreSQL حقيقي` بقيَت `in_progress`
أكثرَ من ساعةٍ وأطولُ تشغيلٍ ناجحٍ لها ≈ ٣ دقائقُ. والسببُ: حالتانِ في
`tests/integration/driver-vehicle.test.ts` مرَّرَتا وسمَ `sql` **خامّاً** إلى
`expect(…).rejects`، ووسمُ استعلامِ `postgres.js` **مُرجَأٌ لا وعدٌ منطلقٌ** — لا
يُرسَلُ حتّى يُنادى `then`، ومُطابِقُ الرفضِ لا يُناديه — فالوعدُ لا يُحسَمُ أبداً.

**فصارَ في الحالةِ**: الحالتانِ تُمرِّرانِ وعداً منطلقاً من دالّةٍ غيرِ متزامنةٍ
(١٣/١٣ خضراءُ والعمليّةُ تخرُجُ)؛ وحاجزٌ ساكنٌ جديدٌ
`scripts/check-lazy-query-assertion.ts` يمسحُ **٣٦٢ ملفَّ اختبارٍ** فيمنعُ أيَّ وسمِ
قالبٍ داخلَ `expect(…)` (و`expect(await sql`…`)` مباحٌ)؛ وعشرُ حالاتِ وحدةٍ تزرعُ
الحالةَ التي علَّقَت CI (`ح-7`)؛ وخطوتانِ مُسمّاتانِ في `verify`؛ و**سقفُ زمنٍ لكلِّ
وظيفةٍ** (`verify` ٢٠د · التكاملُ ٢٥د · Redis ١٥د) فأيُّ تعليقٍ قادمٍ يصيرُ حكماً
أحمرَ مُسمّىً لا صمتاً؛ و`ADR 0122` يحملُ القرارَ وبديلَيهِ المرفوضَينِ.

**ودرسُ الحالةِ**: عطبٌ يُخفي عطباً — الخطأُ الأوّلُ كانَ يُسقِطُ الحالةَ الأولى من
الملفِّ فلم تُبلَغِ العاشرةُ أصلاً. فالأحمرُ الواحدُ لا يُثبِتُ أنَّ ما بعدَه سليمٌ.

### زيادةٌ ثالثةٌ على `F3-07` — **نافذةٌ محليّةٌ لا تُترَكُ لساعةِ الحائطِ** (`ADR 0123`)

حكمُ CI الثالثُ ([35024571423](https://github.com/uxxxug/ceezr/actions/runs/35024571423))
كانَ **أحمرَ يقولُ أينَ** بعدَ أن كانَ صمتاً: `verify` أخضرُ في ١م٥٦ث، و`تكامل على
PostgreSQL حقيقي` سقطَ في ٢م٤٢ث بـ**١٠٠٤ ناجحةً و٢ ساقطةً من ١٠٠٦ في ١٠٣ ملفّاتٍ** —
فحاجزُ `ADR 0122` وسقفُه أدَّيا ما وُضِعا له.

**والساقطتانِ فحصانِ صحيحانِ منتجاً وكاذبانِ قياساً**: الدورةُ عندَ ٢١:١٨Z أي ٠٠:١٨
بتوقيتِ الرياضِ، و`tests/integration/driver-activity.test.ts` كانَ يُثبِّتُ
`city_timezone` على `Asia/Riyadh` **حرفاً** ويزرعُ صفوفاً نسبيّةً (`now() - 30 min` و
`now() - 2h`) ويقرأُ نافذةَ `day`. ونافذةُ اليومِ تبدأُ عندَ منتصفِ ليلِ المدينةِ،
فعمرُها ١٨ دقيقةً: قُصَّ التواجدُ إلى ١١٠٠ ثانيةٍ بدلَ ١٨٠٠، وسقطَت الرحلةُ الأقدمُ
خارجَ النافذةِ. وعطبٌ ثانٍ وراءَه: `withSetting` كانَ يُرجِعُ **لقطةَ مطلعِ الملفِّ**
فيمحو أيَّ تثبيتٍ في `beforeAll`.

**فصارَ في الحالةِ**: منطقةُ الزمنِ **تُحسَبُ** من ساعةِ القاعدةِ (`Etc/GMT±N`) ليقعَ
«الآنَ» وسَطَ اليومِ المحليِّ، و`beforeAll` **يُثبِتُ** بُعدَ الحدَّينِ (≥ ٤ ساعاتٍ)،
و`withSetting` يُرجِعُ قيمةَ اللحظةِ، **والأرقامُ المتوقَّعةُ لم تُخفَّفْ**؛ وحاجزٌ
ساكنٌ جديدٌ `scripts/check-wall-clock-window-tests.ts` يمسحُ **٣٦٣ ملفَّ اختبارٍ**
فيمنعُ منطقةَ زمنٍ مكتوبةً حرفاً في كتابةِ إعدادٍ داخلَ ملفٍّ يقرأُ نافذةً محليّةً
(والمنطقةُ **معامِلاً** لدالّةٍ خالصةٍ بزمنٍ مُعطىً مباحةٌ، والرخصةُ تعليقٌ بسببٍ
مكتوبٍ)؛ و١٢ حالةَ وحدةٍ تزرعُ **السطرَ ٣٣٤ الذي أسقطَ CI** (`ح-7`)؛ وخطوتانِ
مُسمّاتانِ في `verify`؛ و`ADR 0123` يحملُ القرارَ وبدائلَه المرفوضةَ.

**ودليلُ العلاجِ لا دعواهُ**: الملفُّ **٢٣ من ٢٣ خضراءُ عندَ ٠٠:٤٠ بتوقيتِ الرياضِ** —
الساعةِ نفسِها التي أسقطَتهُ في CI؛ وقد أُعيدَ إنتاجُ العطبِ محليّاً **قبلَ** أن
يُمَسَّ حرفٌ. **ولم يتغيّر حرفٌ من الهجرةِ**: العطبُ كانَ في القياسِ لا في المقيسِ.

### حكمُ `F3-07` — أخضرُ في جولتَينِ، ومُدمَجٌ في `main`

الفرعُ على `30a70d7` مرَّ بخمسِ وظائفَ خضراءَ
([35027998264](https://github.com/uxxxug/ceezr/actions/runs/35027998264)) —
و`تكامل على PostgreSQL حقيقي` منها في **٣م٧ث** بعدَ أن علَّقَ ساعةً ثمَّ سقطَ
بفحصَينِ — **فدُمِجَ الطلبُ `#50`** ضغطاً إلى `main` (`0b5d1bf`)، ومرَّ `main` أخضرَ
([35028339423](https://github.com/uxxxug/ceezr/actions/runs/35028339423)). فحالُ
`F3-07` **`[~]`**: جولتانِ من ثلاثٍ (`ح-4`)، والأحمرانِ السابقانِ باقيانِ في السجلِّ
لأنَّهما أنتَجا `ADR 0122` و`ADR 0123` وحاجزَيهما.

## `F3-08` — `SD-10` دعمُ السائقِ: **بوّابةُ دورٍ في المحرِّكِ** (2026-09-16 · إضافةٌ لا محوٌ · `ح-8`)

**ما صارَ قائماً في المستودعِ بعدَ هذه الدفعةِ** (والحكمُ النهائيُّ حكمُ CI — `ح-6`):

- **القاعدةُ**: `support_ticket_type` تحملُ اليومَ **تسعَ** قيمٍ (زِيدَ `deduction`
  و`rider_conduct` و`vehicle` في `20260916010000` بلا استعمالٍ في الهجرةِ نفسِها).
  و`open_support_ticket` أُعيدَ إنشاؤها في `20260916020000` **ببوّابةِ دورٍ**:
  صنفُ سائقٍ من حسابٍ بلا صفِّ سياقةٍ ⇒ `NOT_A_DRIVER`. و`driver_support_tickets`
  دالّةُ قراءةٍ جديدةٌ نظيرَ `rider_support_tickets` — **١٣٣ دالّةً** في عقدِ
  المخطَّطِ الآنَ.
- **الخادمُ**: `POST|GET /v1/driver/support/tickets` — والدورُ **لا يُصدَّقُ من
  العنوانِ**؛ العنوانُ يختارُ الدالّةَ، والقاعدةُ تحكمُ في الاستحقاقِ.
- **الطبقاتُ**: لبٌّ مشتركٌ في `packages/domain/support/ticket-types.ts` و
  `packages/application/support/intake.ts` و`packages/infrastructure/support/ticket-store.ts`؛
  ومِلفّاتُ الراكبِ **مُحوِّلاتٌ** تحفظُ كلَّ تصديرٍ وتوقيعٍ.
- **السطحُ**: `apps/miniapp/src/surfaces/support/*` (اللبُّ المشتركُ · ٣٤٤ سطرَ
  شاشةٍ واحدةٍ للدورَينِ) · `…/rider/support/*` مُحوِّلاتٌ · `…/driver/support/*`
  وصفٌ ومُحوِّلٌ. ومدخلٌ في لوحِ العروضِ (`dof__support`). والقواميسُ **١٠١٣
  مفتاحاً** في كلٍّ من العربيّةِ والإنجليزيّةِ والأردو.
- **الإنفاذُ**: `check-support-intake-contract` يقيسُ قواعدَه السبعَ على
  **دورَينِ** (`SUPPORT_ROLES`) وعلى **كلِّ هجرةِ دعمٍ** — و`create or replace`
  يُعيدُ منحَ التنفيذِ ضمنيّاً، فهجرةٌ ثانيةٌ لا تنزعُ تفتحُ بابَ الأولى.
  والقاعدةُ ٥ انتقلَت إلى الشاشةِ المشتركةِ: **الحاجزُ يقيسُ حيثُ العرضُ يُكتَبُ**.
- **الحاكمُ الجديدُ**: `ADR 0124` — مجالُ القراءةِ أوسعُ من مجالِ الكتابةِ.

**وما لم يصرْ قائماً**: `SD-12` (حسابي) لم يُبنَ. ومعَه سؤالٌ مفتوحٌ مقروءٌ من
`F2-11`: رفضُ `POST /v1/me/erasure` يُردُّ `NOT_A_RIDER` — فحذفُ حسابِ سائقٍ بلا
صفِّ راكبٍ **يُقاسُ على قاعدةٍ حقيقيّةٍ قبلَ أن تُبنى شاشةٌ تَعِدُ به**. والدَّينُ
المُعلَنُ في الشاشةِ: لا مُرفَقَ، ولا خيطَ ردٍّ، ولا كشفَ خصمٍ تفصيليّاً.

## حكمُ CI على `F3-08` — مقروءٌ لا مفترَضٌ (2026-09-16)

`PR #51` دُمِجَ في `main` بالضغطِ (`8b0bee2`) بعدَ **جولتَينِ خضراءَ مقروءتَينِ**:
الفرعُ [35034757161](https://github.com/uxxxug/ceezr/actions/runs/35034757161) ثمَّ
`main` بعدَ الدمجِ [35035556445](https://github.com/uxxxug/ceezr/actions/runs/35035556445)
— `verify` · `تكامل على PostgreSQL حقيقي` · `تكامل على Redis حقيقي` ·
`فوضى متعدد المثيلات (F5-06)` · `Roadmap freshness`، بلا خطوةٍ ساقطةٍ.

**والأحمرانِ قبلَهما لا يمحوهما الأخضرُ** (`ح-8`)، وكلاهما عطبُ **قياسٍ في
دفتري لا في المنتَجِ**، وهذا نصُّهما:

1. [35033555657](https://github.com/uxxxug/ceezr/actions/runs/35033555657) —
   `Lint` سقطَ بخطأٍ واحدٍ (لا تحذيرٍ): `scripts/lib/skip-registry.ts` كُتِبَ فيه
   سطرُ `suites` **بعدَ** آخرِ نداءِ مُنسِّقٍ، فاختلفَ عن صياغةِ `biome`.
   والدرسُ المُنفَذُ: كلُّ تعديلٍ بعدَ التنسيقِ يُعادُ تنسيقُه قبلَ الدفعِ.
2. [35033713939](https://github.com/uxxxug/ceezr/actions/runs/35033713939) —
   `Test` سقطَ في `tests/unit/skip-audit.test.ts`: أرقامُ سجلِّ التجاوزِ
   **مُثبَّتةٌ** (105 ملفّاً · 1127 حالةً) ومدخلُ `driver-support-intake` رفعَها
   إلى 106 و1140. والجولةُ المحلّيّةُ سبقَت إضافةَ المدخلِ فلم ترَه —
   **والأخضرُ المحلّيُّ ليسَ بديلاً عن حكمِ CI** (`ح-6`). والرقمانِ السابقانِ
   باقيانِ مذكورَينِ في الاختبارِ لا ممحوَّينِ، والرفعُ بالزيادةِ (`ح-8`).

**و`F3-07` قُلِبَ إلى `[x]`**: جولتُه الثالثةُ الخضراءُ هيَ `main` نفسُها
(`35035556445`)، فاكتملَ شرطُ `ح-4` — ثلاثُ جولاتٍ متتاليةٍ مقروءةٍ. و`F3-08`
يبقى `[~]` حتّى جولتِه الثالثةِ.

**والقادمُ**: `SD-12` (حسابي) — وسؤالُه المفتوحُ صارَ **مقيساً في القاعدةِ**:
`erase_my_account` تردُّ `NOT_A_RIDER` لكلِّ دورٍ غيرِ `rider` صريحاً، وسجلُّ
الحذفِ يُعلِنُ **ثمانيةَ عشرَ جدولاً** مؤجَّلاً إلى `SD-12` (خمسةٌ تُمحى صفّاً ·
خمسةٌ تُجهَّلُ في مكانِها · ثمانيةٌ تُستبقى بأساسٍ نظاميٍّ) — فالحذفُ للسائقِ
**بندٌ في القاعدةِ لا زرٌّ في شاشةٍ**.

## `SD-12` — حقَّا بيانةِ السائقِ: **المالُ يمنعُ لا الدورُ** (2026-09-16 · إضافةٌ لا محوٌ · `ح-8`)

**ما كانَ مكتوباً أعلاه ويبقى مكتوباً**: أنَّ `erase_my_account` تردُّ
`NOT_A_RIDER` لكلِّ دورٍ غيرِ `rider`، وأنَّ ستةَ عشرَ جدولاً موسومٌ
`deferredTo: "SD-12"`. **وذاكَ صحيحٌ في وقتِه ومنسوخٌ اليومَ** — يُصحَّحُ
بالزيادةِ لا بالمحوِ.

**وما صارَ قائماً**:

- **الدورُ لا يمنعُ المحوَ**: `role not in ('rider','driver')` ⇒
  `ROLE_NOT_SELF_ERASABLE` (حسابُ مؤسّسةٍ لا حسابُ إنسانٍ). و`NOT_A_RIDER` يبقى
  في الاتّحادِ موسوماً بـ`ح-8` لأنَّه مذكورٌ في أدلّةٍ سابقةٍ.
- **المالُ يمنعُ**: محفظةُ اشتراكٍ برصيدٍ ≠ صفرٍ ⇒ `WALLET_HAS_BALANCE` ومعَها
  `wallet_balance_minor` **مقروءٌ من `subscription_wallet_balance`** لا محسوبٌ
  ثانيةً · والرصيدُ قد يكونُ سالباً فقُرِئَ بـ`readSignedAmount`.
- **رحلةٌ جاريةٌ مُسنَدةٌ تمنعُ** (`ACTIVE_ORDER`) — وهذا الحاجزُ **كانَ معطوباً
  حينَ كُتِبَ** (`o.driver_id` والعمودُ `assigned_driver_id`)، و`plpgsql` لا
  يُصرِّفُ الجسمَ عندَ الإنشاءِ، فلم يرَه مُصرِّفٌ ولا حاجزٌ ولا تطبيقُ الهجرةِ.
  **رآه اختبارُ التكاملِ وحدَه** وأُصلِحَ في الهجرةِ لا في الاختبارِ.
- **صفُّ `drivers` يُجهَّلُ ولا يُمحى** (مفاتيحُ `restrict` في أبنائِه · ورقمُ
  الهويّةِ يُبدَّلُ بمعرِضٍ لا يُطابِقُ إنساناً فلا يُخرَقُ فريدُ المدينةِ).
- **صفرُ جدولٍ مؤجَّلٍ**: الستةَ عشرَ صارَت محكومةً — والمالُ والحضورُ وقرارُ
  الإسنادِ **تبقى بأسسٍ مكتوبةٍ ثلاثةٍ** لا بسكوتٍ:
  `MONEY_RECORD_IS_ACCOUNTING_EVIDENCE` ·
  `ATTENDANCE_PROVES_DRIVER_ENTITLEMENT` ·
  `DISPATCH_DECISION_IS_EVIDENCE_FOR_THE_OTHER_PARTY`.
- **حزمةُ التنزيلِ واحدةٌ لا اثنتانِ**: `export_my_data` نفسُها من **١٣ قسماً إلى
  ٣١**، و`subject` مقروءٌ من `users.role` بعدَ أن كانَ مسطوراً `'rider'`.
  وحزمةُ راكبٍ تحملُ أقسامَ السائقِ **فارغةً لا غائبةً** — شكلٌ ثابتٌ لا يكسِرُ
  قارئاً آليّاً، وفراغٌ قولٌ صادقٌ.
- **ما لا يُصدَّرُ خاماً يُصدَّرُ رايةً**: `national_id_present` ·
  `file_disclosed: false` · `payload_disclosed: false` · وسِجلُّ الموقعِ مسقوفٌ
  بـ`cap: 5000` **مُعلَناً في الحزمةِ** — حزمةٌ تقصُّ بلا إعلانٍ تكذبُ على صاحبِها.
- **الحاجزُ صارَ يُلزِمُ العبارةَ لا الاسمَ** (القاعدةُ ٩ في
  `check-erasure-policy`): `erase-row` ⇒ `delete from <t>` · `anonymize-in-place`
  ⇒ `update <t>` · على **مجموعةِ رعايا مُعلَنةٍ** `[rider, driver]`. والعَدُّ
  اليومَ: `54 جدولاً محكوماً · 31 قسمَ تنزيلٍ · (rider: 13 · driver: 27 · admin: 5)`.

**الدَّينُ المُسجَّلُ (`ح-5`)**: **بايتاتُ المخزَنِ لا تُمحى بمعاملةِ القاعدةِ** —
`driver_documents.object_path` و`companies.logo_object_path`
و`barcode_object_path`: الصفوفُ تُمحى أو تُجهَّلُ، والجسمُ في المخزَنِ يبقى حتّى
يمرَّ عاملٌ خارجَ المعاملةِ. **دَينٌ مقيسٌ لا صفةٌ مُدَّعاةٌ.**
**وشاشةُ حسابِ السائقِ في التطبيقِ غيرُ مبنيّةٍ** — بندٌ تالٍ، ولا يُدَّعى زرٌّ
لم يُبنَ.

**الدليلُ**: `docs/adr/0125-a-drivers-erasure-stops-at-money-not-at-role.md` ·
`docs/evidence/architecture/SD-12-driver-account-2026-09-16.md` ·
`supabase/migrations/20260916030000_sd_12_driver_account_erasure.sql` ·
`tests/integration/driver-account-erasure.test.ts`.

## حكمُ CI على `SD-12` — مقروءٌ لا مفترَضٌ · و`F3-08` يُقلَبُ إلى `[x]` (2026-09-16)

`PR #52` دُمِجَ في `main` بالضغطِ (`a484372`) **بجولةٍ خضراءَ أولى بلا حمرةٍ**:
الفرعُ [35039776259](https://github.com/uxxxug/ceezr/actions/runs/35039776259) ثمَّ
`main` بعدَ الدمجِ [35040085897](https://github.com/uxxxug/ceezr/actions/runs/35040085897)
— `verify` · `تكامل على PostgreSQL حقيقي` · `تكامل على Redis حقيقي` ·
`فوضى متعدد المثيلات (F5-06)` · `roadmap`، بلا خطوةٍ ساقطةٍ.

**و`تكامل على PostgreSQL حقيقي` في CI أثبتَ الحالاتِ الخمسَ بأسمائِها**، فلم
يبقَ ادّعاءُ `SD-12` معلَّقاً على قاعدةِ الصندوقِ وحدَها (`ح-6`).

**والانحرافُ المحلّيُّ يبقى مكتوباً لا ممحوّاً** (`ح-8`): سقطَ محلّيّاً في هذه
الدفعةِ `tests/integration/hot-query-index-plans.test.ts`
(`audit_log_action_entity_idx`: كلفةُ إسقاطِ الفهرسِ محلّيّاً `12.59` ⇒
`Index Scan`)، **وفي CI مرَّ خضراءَ** (`346.01` ⇒ `Seq Scan`). فالفرقُ في
**إحصاءاتِ قاعدةٍ مُقتطَعةٍ مئاتِ المرّاتٍ** لا في الشيفرةِ، والجدولُ لم يُلمَسْ
في هذا العملِ. ومعَه تجاوزاتُ مُهَلٍ محلّيّةٌ سببُها اتّصالاتٌ عالقةٌ من جولاتٍ
مقتولةٍ — تزولُ بإنهاءِ الخوادمِ الخلفيّةِ. **والدرسُ المُسجَّلُ**: الأحمرُ
المحلّيُّ يُفحَصُ ولا يُسكَتُ عنه، **ولا يُقرأُ حكماً** — الحاكمُ CI.

**و`F3-08` قُلِبَ إلى `[x]`**: خمسُ جولاتٍ خضراءُ متتاليةٌ مقروءةٌ
(`35034757161` · `35035556445` · `35035865721` · `35039776259` ·
`35040085897`)، فاكتملَ شرطُ `ح-4` وزادَ عليه.

**وما لا يشملُه هذا `[x]`** (`ح-5`): **شاشةُ حسابِ السائقِ في التطبيقِ لم تُبنَ**
— لا شاشةَ لغةٍ ولا إشعاراتٍ ولا منطقةٍ ولا زرَّ حذفٍ في سطحِ السائقِ؛ والعقدُ
والقاعدةُ والحاجزُ قائمةٌ تحتَها. **ودَينُ بايتاتِ المخزَنِ** قائمٌ مسجَّلاً.
وهذانِ **بندُ الدفعةِ التاليةِ** لا صفتانِ مُدَّعاتانِ.

---

## تصحيحٌ مُضافٌ — 2026-09-16 · سبعةُ بنودٍ + `CAP-009` ⇒ `[x]` بعدَ رفعِ حجبِ فوترةِ Actions

**السببُ:** يومَ 2026-09-10 نُفِّذَت سبعةُ بنودٍ ودُمِجَت في `main`، ولكنّ حجبَ مُنفِّذي Actions على مستوى الحسابِ (فوترةٌ) منعَ CI من التشغيلِ أصلاً — لا خطوةٌ واحدةٌ شُغِّلَت. ثُمَّ رُفِعَ الحجبُ، وجالتِ CI خضراءَ متتاليةً تجاوزَت الثلاثةَ.

**البنودُ المُقلَبةُ:**

| البند | الدمج | الجولاتُ الخضراءُ المُقتبَسَةُ |
|---|---|---|
| `F4-03` (مجرى أحداثٍ مشترك) | PR #64 · `78ebc82` | `34953942937` · `34976110848` · `34987624903` |
| `F4-08` (سباقاتُ مسارِ الموقع) | PR #59 · `7b674f0` | نفسُها |
| `F7-03` (فصلُ ساخنٍ/تاريخيٍّ بتقسيمٍ) | PR #60 · `ecaaebc` | نفسُها |
| `F7-06` (سياسةُ استبقاءٍ وأرشفة) | PR #63 · `d287401` | نفسُها |
| `F8-03` (سجلّاتٌ مُهيكلةٌ) | PR #67 · `e5de9f6` | نفسُها |
| `F8-04` (صمودُ الاعتماديّات) | PR #68 · `15484b8` | نفسُها |
| `F8-05` (عزلُ مسارِ الاستغاثة) | PR #66 · `8c30d62` | نفسُها |

**و`CAP-009` قُلِبَ معَها:** عمودُ حلِّها ثلاثيٌّ — حالةٌ ساخنةٌ في Redis (`F4-02` `[x]`) + استمرارٌ غيرُ متزامنٍ مجمَّعٌ (`F4-02` `[x]`) + جدولُ تاريخٍ مقسَّمٌ (`F7-03` `[x]`) — فاكتملَ.

**وما لا يُقلَبُ:**

- **`CAP-010` تبقى `[ ]`** — قلبُ قدرةٍ يحتاجُ قياساً على حجمٍ إنتاجيٍّ لا وجودَ لهُ (نصٌّ صريحٌ في سطرِ التنفيذِ).
- **`F4-05` تبقى `[ ]`** — دليلُ التنفيذِ لإصلاحِ طابعِ `last_location_at` لا للهجرةِ الكاملةِ إلى المجرى المشترك.
- **`F4-06` و`F4-07`** تبقى `[ ]` — لم تُنفَّذْ بعدُ.

**ولا يُدَّعى `مَقيس` ولا `مُثبَت`** (`ح-5`): الأخضرُ حكمٌ على سلوكٍ تحتَ بوّابةٍ لا حِملٌ إنتاجيٌّ قِيسَ.

---

## تصحيحٌ مُضافٌ — 2026-09-16 · `F4-04`: قناةُ Socket.IO الآنيةُ للرحلةِ النشطة

**المُنفَّذُ:** قناةُ Socket.IO للرحلةِ النشطةِ (ADR 0042). `Bun.serve` استُبدِلَ بـ`http.createServer`
(Node) ليرتبطَ به Socket.IO على المنفذِ نفسه. وجسرٌ نحيفٌ (`http-bridge.ts`) يحوِّلُ بين
`IncomingMessage` و`Request` من Web API ويُمرِّرُه إلى `app.fetch`. والقناةُ (`ride-channel.ts`):
العميلُ يتّصلُ بـ`auth.sessionToken`، الخادمُ يُصادِقُ الجلسةَ ويحلُّ `rider_id` من `telegram_id`،
ثم يشتقُّ الرحلةَ النشطةَ من القاعدةِ (لا يثقُ بـ`tripId` من العميلِ) ويُنضمُّ إلى غرفةِ
`ride:<tripId>`. اشتراكٌ واحدٌ على ناقلِ الأحداثِ لكلِّ نسخة يُمرِّرُ `location_updated` وغيرَه.
`session_ended` **لا يُغلِقُ الغرفةَ**. بثُّ Live Location التلغراميُّ **لم يُمسَّ**.

**والحالةُ `[~]` لا `[x]`**: `ح-4` تشترطُ ثلاثَ جولاتٍ خضراءَ متتاليةً على `main`.

**وما لا يُدَّعى** (`ح-5`): لا حِملَ إنتاجيّاً قِيسَ، ولا اتصالَ على جهازٍ حقيقيٍّ في شبكةِ تلغرام،
ولا واجهةَ Mini App متّصلةً بهذه القناةِ بعدُ.

---

## تصحيحٌ مُضافٌ — 2026-09-16 · `F4-05`: التتبّعُ العامُّ من القناةِ المشتركةِ

**المُنفَّذُ:** أُضيفَ منفذُ `DriverLocationHotStateReader` بـ`HGETALL` على Redis إلى
`RedisDriverLocationHotState`. `createTrackingTokenRpc` يقبلُ `TrackingTokenRpcOptions`
بقارئٍ ساخنٍ اختياريّ، ويقرأُ الحالةَ الساخنةَ أوّلاً ويُعَدُّ للقاعدةِ عند غيابِها.
والمعرّفاتُ (driver_id و city_id) تُحلُّ بنداءٍ مستقلٍّ لا بتعديلِ `get_tracking_position` —
فحمولتُها العامّةُ بلا هويّةٍ عقدٌ مكتوبٌ لا يُكسَر. والقاعدةُ تظلُّ الأرضيّةَ: لا Redis يعني نسقَ `F4-01`.

**والحالةُ `[~]` لا `[x]`**: `ح-4` تشترطُ ثلاثَ جولاتٍ خضراءَ متتاليةً على `main`.


---

## تصحيحٌ مُضافٌ — 2026-09-16 · `F4-06`: خريطةُ المشرفِ من المجرى المشترك

**المُنفَّذُ:** ربطت صفحة `/admin/live-map` بمجرى SSE القائم عند
`/admin/api/live/drivers` بدلَ إعادة التحميلِ الدوريّة كلَّ ٢٠ ثانية. اختزالُ
حالةٍ قابلٌ للاختبارِ في `packages/maps/live-map-reducer.ts`:
- `applySnapshot`: اللقطةُ تُستبدَل كاملةً (مصالحة تامّة).
- `applyTrackingDelta`: الدلتا بفاصلِ الترتيب (BUG-009 / ADR 0053) — تبدُّلُ
  `sessionId` فجوةٌ تُوجب لقطةً، وتراجعُ التسلسلِ يُسقَط.

ونصُّ عميل SSE في `renderLiveMapPage` يُحقَن حين تُقدَّم البصمةُ والرابط (`cspNonce`،
`sseUrl`). وأُزيلَ `LIVE_REFRESH_SECONDS` من مسارِ الخريطة في `admin-ui.ts`.
والسطحُ الخادميُّ يبقى احتياطيًّا: الجدولُ والخريطةُ مُصيَّران عند الطلب الأوّل.

**وما لا يُدَّعى:** CAP-011 لم يُغلق — استعلاماتُ الإدارة لا تزال تضرب القاعدة
الرئيسية في اللقطة الدورية. نماذجُ القراءة المادية والنسخةُ التحليليةُ تبقى F7-08.

**والحالةُ `[~]` لا `[x]`**: `ح-4` تشترطُ ثلاثَ جولاتٍ خضراءَ متتاليةً على `main`.


---

## تصحيحٌ مضافٌ — `F2-06` الخطوةُ الأولى (2026-09-16 · `ح-8`)

**`ActiveRidePhase`** صارَ خمسةَ أطوارٍ لا أربعةً: أُضيفَ `driver_arrived` — يُشتَقُّ
من `arrived_at` حينَ `matched` + `hasDriver` + `arrived_at !== null`، ويتقدَّمُ على
`started_at`. و`active_ride_snapshot` صارَتْ تُخرِجُ `arrived_at` في الـ JSONB (هجرةٌ
`create or replace function`، لا دالّةٌ جديدةٌ).

**وفي Mini App**: عميلُ Socket.IO في `ride-channel-client.ts` (حقنُ ناقلٍ وجلسةٍ)،
ومحوّلُ تتبُّعٍ حيٍّ في `live-tracking-reducer.ts` (حارسُ تسلسلٍ BUG-009)، ودمجٌ في
`ActiveRideScreen.tsx` (اشتراكٌ عندَ الدخولِ، فصلٌ نظيفٌ عندَ الخروجِ، موقعٌ حيٌّ
يُغطّي اللقطةَ). و`socket.io-client` أُضيفَ إلى `apps/miniapp`. ٢١ اختبارَ وحدةٍ ناجحةً.

**وما لا يُدَّعى**: `F4-07` لم يُنفَّذْ بعدُ — Live Location يبقى المسارَ الرئيسيَّ،
وخريطةُ `tripId→messageId` لم تُحذفْ. سيُنفَّذُ كفرعٍ منفصلٍ بعدَ دمجِ هذا.


---

## تصحيحٌ مضافٌ — `F4-07` (2026-09-16 · `ح-8`)

**`CustomerLiveRelay` صارَ احتياطًا لا مسارًا رئيسيًّا**: لا يُنشَأُ ولا يُشترِكُ
إلّا إن كان `liveLocationFallbackEnabled` مُفعَّلًا (افتراضيًّا `false`)، أو
`overrides.liveLocationChannel` مُمرَّرًا. وخريطةُ `tripId→messageId`
(مخزنُ البثِّ) لا تُنشَأُ إلّا معه. والكودُ لم يُحذَفْ.

**وفي Mini App**: `RiderRoot.tsx` يُمرِّرُ ناقلَ Socket.IO حقيقيًّا ورمزَ جلسةٍ
من `getSession` إلى `ActiveRideScreen` في الإنتاجِ. فقناةُ Socket.IO صارَت
المسارَ الرئيسيَّ للتتبُّعِ.

**وما لا يُدَّعى**: لا قياسَ إنتاجيَّ بعدُ (`ADR 0099`).


---

## `SD-12` — سطحُ الحسابِ: **لبٌّ واحدٌ ودورانِ يُلبَسانِه** (2026-09-16 · إضافةٌ لا محوٌ · `ح-8`)

كانَ مكتوباً في هذا المِلفِّ وفي شاهدِ `SD-12` الأوّلِ: «**وشاشةُ حسابِ السائقِ
في التطبيقِ ليسَت في هذا العملِ… بندٌ تالٍ صريحٌ**». **وذاكَ صحيحٌ في وقتِه
ومنسوخٌ اليومَ** — يُصحَّحُ بالإضافةِ لا بالمحوِ.

**والحاكمُ**: `docs/adr/0126-one-account-core-two-roles.md`.

### ثلاثةُ أعطابٍ حقيقيّةٍ كانت قائمةً — عُولِجَت في أصلِها لا في عَرَضِها

1. **`KNOWN_BASES` في `rider/account/account-view.ts` كانَ خمسةَ أسسٍ والنطاقُ
   يُعلِنُ تسعةً.** ومن الأربعةِ الناقصةِ `BLOCK_AND_STANDING_SURVIVE_ERASURE`
   — **وهوَ يُرسَلُ في إيصالِ كلِّ حذفٍ** سطراً باسمِ `identityBar`. فكانَ مَن
   حذفَ حسابَه وسألَ «لماذا بقيَ حظري؟» يُجابُ **«سببُ إبقاءٍ لا نعرفُ نصَّه
   بعدُ»** — ونصُّه مكتوبٌ عندَنا في القواميسِ الثلاثةِ منذُ `ADR 0113`. أي أنَّنا
   كنّا نُخفي عن إنسانٍ جواباً **نملكُه** في لحظةِ ممارستِه حقّاً.
   **والإصلاحُ استيرادُ المجالِ من `packages/domain/privacy/data-rights.ts`** لا
   زيادةُ عضوٍ في نسخةٍ: العلّةُ كانت **وجودَ نسخةٍ ثانيةٍ للمجالِ** (القاعدة ٠.٦).
2. **`rider.account.section.identityBar` غائبٌ من القواميسِ الثلاثةِ**،
   والمُترجِمُ يسقطُ إلى العربيّةِ ثمَّ **يردُّ المفتاحَ نفسَه** — فكانَ يُقرأُ
   على إيصالِ حذفٍ سطرٌ نصُّه حرفيّاً `rider.account.section.identityBar`.
3. **`notificationsSent` قسمٌ يُصدَّرُ للسائقِ فعلاً** من `notification_outbox`
   **ولا نصَّ له في أيِّ لغةٍ** — أوّلُ ما أسقطَه الحاجزُ الجديدُ. فأقسامُ
   الإيصالِ **اثنتانِ وثلاثونَ** لا إحدى وثلاثينَ كما هوَ مكتوبٌ في شاهدِ
   `SD-12` الأوّلِ (وذاكَ يبقى بنصِّه وفيه سطرُ تصحيحٍ · `ح-8`).

**ولم يرَ شيئاً من ذلكَ مُصرِّفٌ ولا اختبارٌ ولا حاجزٌ، وكلُّ الأدواتِ خضراءُ**:
نقصُ عضوٍ في `Set<string>` لا يُنكِرُه نوعٌ، ومفتاحٌ ناقصٌ لا تُنكِرُه تغطيةٌ.

### ما صارَ قائماً

**اللبُّ** `apps/miniapp/src/surfaces/account/`: `AccountRights.tsx` (الشاشةُ) ·
`account-view.ts` (`accountViewModel(spec)` بوصفٍ من عنصرَينِ: `keyPrefix`
و`declaredDebtKeys`) · `account-api.ts` · `account-contract.ts` (وفيهِ
`walletBalanceMinor?` — **مُنشَرٌ من الحدِّ فعلاً** لا حقلٌ متوقَّعٌ).

**المُهايئانِ**: `rider/account/*` صارَ مُهايئاً رقيقاً **بلا تغييرِ اسمِ تصديرٍ
واحدٍ** — ولذا بقيَ `tests/unit/data-rights.test.ts` أخضرَ **بلا سطرٍ مُعدَّلٍ**،
وقياسٌ بقيَ أخضرَ بلا تعديلٍ أصدقُ من قياسٍ عُدِّلَ ليخضرَّ. و`driver/account/*`
جديدٌ: بادئةٌ وأربعةُ دُيونٍ مُعلَنةٍ.

**الوصلُ**: `DriverRoot.tsx` فيهِ `{kind:"account"}`، ولوحُ العروضِ فيهِ زرُّ
`driver.offers.openAccount`. **ولا مُوجِّهَ URL ولا مسارَ** — على نمطِ `DriverRoot` القائمِ.

**النقدُ**: `packages/domain/financial/minor-units.ts` — موضعٌ واحدٌ للقاسمِ.
وليسَ عددُ خاناتِ العملةِ رقمَ أعمالٍ (القاعدة ٠.٣): هوَ بِنيةُ العملةِ.

**الحاجزُ**: `scripts/check-account-surface-contract.ts` +
`scripts/lib/account-surface-contract.ts` — **ثمانِ قواعدَ**، وأقسامُ الإيصالِ
تُقرأُ **من جسمِ الدالّةِ في الهجرةِ** لا من سجلٍّ موازٍ (سابقةُ
`check-erasure-policy`). ولكلِّ قاعدةٍ **حالةٌ سلبيّةٌ مبذورةٌ** في
`tests/unit/account-surface.test.ts` (`ح-7`). وخطوتانِ مُسمّاتانِ في
`.github/workflows/ci.yml` و`check:account-surface` في `package.json` وفي سلسلةِ `ci`.

**و`i18n`**: ١٣٤ مفتاحاً مُضافاً إلى القواميسِ الثلاثةِ (`1000` ⇒ `1134`)،
والتطابقُ مقيسٌ في `check-i18n.ts` وفي القاعدةِ الثالثةِ من الحاجزِ الجديدِ.

### وما لا يُدَّعى (`ح-5`)

- **لا مَنفذَ ثانٍ ولا هجرةَ ولا تعديلَ في طبقةِ التطبيقِ**: الحدُّ لا يعرفُ
  دوراً، والدورُ يُقرأُ في القاعدةِ من `users.role` (`ADR 0125`).
- **`SubscriptionScreen.tsx` لم يُحوَّلْ إلى القاسمِ الجديدِ**: يعرضُ `45`
  والجديدُ `45.00`، فالتحويلُ **تغييرُ معروضٍ** خارجَ الحجزِ المُعلَنِ —
  **دَينٌ مُسجَّلٌ** في `ADR 0126` §٤ لا صفةٌ مُدَّعاةٌ.
- **تعديلُ هويّةٍ وتفضيلُ إشعارٍ وتبديلُ لغةٍ للسائقِ غيرُ مبنيّةٍ**: لا جدولَ
  ولا حدَّ API لها — تُقالُ نصّاً على الشاشةِ **ولا زرَّ صوريَّ لها**.
- **بايتاتُ المخزَنِ** تبقى دَيناً كما هوَ مكتوبٌ في `ADR 0125` §٤ — لم يُلمَسْ ههنا.
- **ولا `[x]` ولا ترقيةَ `[~]`**: `F4-04`/`F4-05`/`F4-06`/`F4-07`/`F2-06` تنتظرُ
  ثلاثَ جولاتٍ خضراءَ مقروءةً لكلِّ مَهمّةٍ (`ح-4`).

## `F3-09` · `SD-08` — الفاتورةُ الضريبيّةُ المبسَّطةُ: **سجلٌّ واحدٌ لا اثنانِ** (2026-09-16 · إضافةٌ لا محوٌ · `ح-8`)

**الحالةُ**: الدفعةُ الأولى (الخادمُ والقاعدةُ) مبنيّةٌ ومقيسةٌ محلّيّاً · البندُ
`[ ]` حتّى ثلاثِ جولاتِ CI خضراءَ (`ح-4`) · الدليلُ
`docs/evidence/architecture/F3-09-simplified-tax-invoice-2026-09-16.md` · القرارُ
`ADR 0127`.

### ما صارَ قائماً

- **سجلُّ الفواتيرِ واحدٌ**: `subscription_invoices` (القائمُ منذُ
  `20260813010000`) امتدَّ بسبعةِ أعمدةٍ ضريبيّةٍ **كلُّها تقبلُ العدمَ** وقيدِ
  «الكلُّ أو لا شيءَ» (`subscription_invoices_tax_all_or_none`) وزنادَي ثباتٍ يرفعانِ
  `TAX_INVOICE_IS_IMMUTABLE`. **ولا عمودَ للإجماليِّ شاملَ الضريبةِ**: هوَ
  `amount_minor`، والقيدُ `total_excl + vat = amount_minor`.
- **كاتبٌ وحيدٌ**: `issue_subscription_invoice(uuid)` — بترقيمِه القائمِ لكلِّ مدينةٍ
  وسنةٍ (`CODE-YYYY-NNNNNN`) وقُفلِه الاستشاريِّ وإصدارِه المُتماثِلِ — صارَ يُفوتِرُ
  ضريبيّاً و**يُغلِقُ** بـ`TAX_IDENTITY_NOT_CONFIGURED` أو `VAT_RATE_NOT_CONFIGURED`.
  و`issue_subscription_tax_invoice(bigint, uuid)` مُغلِّفُ مِلكيّةٍ **يُفوِّضُ إليه**.
- **الضريبةُ مُستخرَجةٌ** من مبلغٍ شاملٍ بدقّةِ `numeric` في المحرِّكِ، ونسبتُها
  `vat_rate_bps` في `platform_settings` لا في شِفرةٍ (القاعدةُ ٠٫٣).
- **رمزُ استجابةٍ** بخمسةِ حقولِ `TLV` مُركَّبٍ في `plpgsql`، ويقرؤه **مُفكِّكٌ بلا
  مُركِّبٍ** في `tests/support/zatca-tlv.ts`.
- **رفوضُ المخزنِ تسعةٌ ورموزُ العامِّ اثنا عشرَ**، ومنها
  `INVOICE_WITHOUT_TAX_FIELDS` (`409`) لصفٍّ سبقَ الهجرةَ — **ولا تُلفَّقُ له حقولٌ
  ولا يُبَقَّرُ رجعيّاً**، و`PAYMENT_PLAN_MISSING` (`503`، عطبُ بياناتٍ داخليٌّ).
- **حاجزٌ جديدٌ**: `scripts/check-tax-invoice-contract.ts` بثمانِ قواعدَ ولكلِّ
  واحدةٍ سالبةٌ مبذورةٌ (`ح-7`)، ومنها منعُ بذرِ رقمٍ ضريبيٍّ في هجرةٍ.

### وما حُذِفَ قبلَ الدفعِ — وهوَ أهمُّ ما في الدفعةِ

جدولٌ ثانٍ `tax_invoices` بمُتوالٍ عامٍّ كانَ قد كُتِبَ في الهجرةِ، فأسقطَ
`tests/unit/retention-policy.test.ts` البناءَ. **ولم يُصنَّف الجدولُ لِيَخضَرَّ
الحاجزُ**، بل قُرِئَ إسقاطُه إشارةَ **تكرارِ مصدرِ حقيقةٍ** — فحُذِفَ الجدولُ
ومُتوالُه وزنادَاهُ ودالّةُ حمولتِه، ورُدَّ جردُ الحدودِ ومصفوفةُ الهجرةِ إلى ٥٤
جدولاً. وفاتورتانِ برقمَينِ لتوريدٍ واحدٍ ليسَتا تكرارَ صفٍّ بل **تكرارَ إقرارٍ**.

### شرطٌ تشغيليٌّ مُعلَنٌ (كسرٌ مقصودٌ لصورةِ الشِفرةِ السابقةِ)

الهجرةُ تبذُرُ `vat_rate_bps = 1500` لكلِّ مدينةٍ **ولا تبذُرُ هُويّةَ البائعِ**.
فمسارُ تثبيتِ دفعِ الاشتراكِ يتلقّى استثناءً حيثُ لم تُضبَط الهُويّةُ — وذاكَ
مُسجَّلٌ بـ`breaksPreviousRelease: true` في `scripts/lib/rollback-registry.ts`
وإجراؤه في `docs/rollback.md`. والعلاجُ **إعدادٌ لا هجرةٌ ولا شيفرةٌ**.

### وما لا يُدَّعى (`ح-5`)

لا مرحلةً ثانيةً من الفوترةِ الإلكترونيّةِ (لا رَبطَ ولا إبلاغَ ولا `CSID` ولا
`UBL 2.1`) · لا إشعارَ دائنٍ ولا إلغاءَ فاتورةٍ · لا سطحَ فاتورةٍ في التطبيقِ
المُصغَّرِ (الدفعةُ الثانيةُ) · لا فهرسَ زائداً · **ولا حكمَ محرِّكٍ**: الهجرةُ لم
تُطبَّقْ على PostgreSQL حقيقيّةٍ بعدُ، فحكمُ CI هوَ الحاكمُ (`ح-6`).

### تصحيحٌ بعدَ حكمِ المحرِّكِ — `F3-09` (الجولةُ `35076192872`)

أوّلُ تطبيقٍ للهجرةِ على PostgreSQL حقيقيّةٍ ردَّ أربعةَ عيوبٍ في شِفرَتِنا:
لفُّ `base64` كلَّ ٧٦ محرفاً في حِمْلِ رمزِ الاستجابةِ (صُحِّحَ الكاتبُ لا
العرّافُ)، وأربعُ دوالَّ جديدةٍ مكشوفةٍ لـ`anon`/`authenticated` بمنحِ `public`
التلقائيِّ (أُقفِلَت بـ`revoke`)، وبذرُ هُويّةِ بائعٍ في
`tests/integration/financial-wallet.test.ts` بلا نزعٍ أسقطَ تكافؤَ إعداداتِ المدنِ
(يُنزَعُ الآنَ في `afterAll`)، ورفعُ العددِ المُثبَّتِ للتجاوزاتِ إلى 35/1180.
**وعشرونَ حالةً من حالاتِ الإصدارِ والحسابِ والثباتِ والتفرُّدِ نجحَت على المحرِّكِ**
في الجولةِ نفسِها. والدليلُ في
`docs/evidence/architecture/F3-09-simplified-tax-invoice-2026-09-16.md` §٧٫٢.

### الدفعةُ الثانيةُ — سطحُ الفاتورةِ ووحدةُ محدِّدِ السدادِ (2026-09-16)

الدفعةُ الأولى مدموجةٌ في `main` بـ`6ba8177`. وما صارَ قائماً بعدَها:

- **محدِّدٌ واحدٌ للسدادِ**: `subscription_payment_is_settled(text)` — `immutable`،
  مقفلةٌ عن `public`/`anon`/`authenticated`، مُتاحةٌ لـ`service_role`. تُرجِعُ
  صِدقاً لـ`active` و`refunded` وحدَهما. **يُنادِيها الكاتبُ وقارئُ الحالِ معاً**،
  فلا يُمكِنُ أن تُظهِرَ رايةٌ زرّاً يرفضُه الكاتبُ.
- **`invoice_issuable`** في جوابِ `driver_subscription_payment_status(bigint, uuid)`
  وفي جسمِ المسلكِ: «أيُمكِنُ الإصدارُ الآنَ» — مُسدَّدةٌ **ولم** تُصدَر بعدُ.
  **وغيابُها عطبُ عقدٍ** (`MALFORMED_RESULT` ⇒ `503`) لا «لا» صامتةٌ: نشرُ شيفرةٍ
  قبلَ الهجرةِ يُقرأُ صريحاً لا لوحاً بلا زرٍّ.
- **سطحٌ في التطبيقِ المُصغَّرِ**: عقدُ جوابٍ ومُنادي مسلكٍ ونموذجُ عرضٍ ولوحُ
  وثيقةٍ (اتّحادٌ موسومٌ بسبعةِ أحوالٍ) داخلَ شاشةِ الاشتراكِ — بعدَ تجديدٍ ناجحٍ
  وتحتَ كلِّ دفعةٍ في السجلِّ، بلوحٍ **واحدٍ مفتوحٍ** لا أكثرَ. وحِمْلُ رمزِ
  الاستجابةِ يُعرَضُ نصّاً يُنتقى كلُّه بلمسةٍ.
- **الحاجزُ صارَ اثنتَي عشرةَ قاعدةً** لا ثمانياً: زِيدَ منعُ قياسِ حالِ سدادٍ في
  مِلفِّ سطحٍ (ومنه `paid` **وهوَ لا وجودَ له في القيدِ** لأنَّه الحرفُ الذي وقعَ
  فعلاً)، ومنعُ ترميزِ حِمْلٍ في عميلٍ، وتكافؤُ نصوصِ الأعطابِ في `ar`/`en`/`ur`
  معَ `UNKNOWN`، ومنعُ رقمِ ضريبةٍ منسوخٍ — ولكلٍّ سالبةٌ مبذورةٌ (`ح-7`).
- **الهجرةُ إضافةٌ لا تعديلٌ**: `20260916120000_…settled_predicate_and_issuable_flag.sql`
  بجسمٍ منسوخٍ حرفاً بحرفٍ لا يتغيَّرُ فيه غيرُ سطرِ الشرطِ وحقلِ الجوابِ، ومُرجَعٌ
  **مجموعةٌ فائقةٌ** فلا ينكسرُ نشرٌ قديمٌ، والاعتمادُ في الاتّجاهِ الآخرِ مُعلَنٌ
  في سجلِّ العودةِ (`code-only` · `coupledDeploy`).
- **الأعدادُ المُثبَّتةُ رُفِعَت بالزيادةِ**: التجاوزاتُ 41 في مِلفِّ الفاتورةِ
  و1186 مجموعاً (كانت 35/1180 — والرقمُ السابقُ يبقى مذكوراً · `ح-8`).

**وما لا يُدَّعى** (`ح-5`): لا رسمَ مقيساً للوحِ (نسختانِ من `react` في المستودعِ،
ورسمٌ يخلطُهما يقيسُ المُوزِّعَ لا الشاشةَ) · لا صورةَ لرمزِ الاستجابةِ ولا حفظَ
وثيقةٍ ملفّاً · **ولا حكمَ محرِّكٍ للهجرةِ الثانيةِ**: حكمُ CI هوَ الحاكمُ (`ح-6`).

---

## ملحقٌ 2026-09-16 — `F7-08`: تجميعاتُ الإدارةِ المادِّيّةُ، ولا رقمَ يُنشَرُ بلا عُمرِه (إضافةٌ لا محوٌ · `ح-8`)

**الفرعُ** `feat/f7-08-admin-read-models-materialized` مقطوعٌ من `main`@`966cbdb`.
**الحالةُ** `[~]` — ساقانِ من ثلاثٍ. وحكمُ CI يُقرأُ بعدَ الدفعِ لكلِّ وظيفةٍ ويُكتَبُ
إضافةً في `docs/evidence/architecture/F7-08-20260916.md`. **ولا `[x]`** قبلَ ثلاثِ
جولاتٍ خضراءَ متتاليةٍ مقروءةٍ بالوظيفةِ (`ح-4`).

### ما صارَ قائماً في المخطَّطِ

- **جدولٌ مُصنَّفٌ لا `materialized view`**: `admin_metric_snapshots`
  (`20260916140000`، `-- migration-phase: expand`) بـ`city_id uuid not null`
  مرجِعاً `cities(id) on delete cascade` (القاعدة 0.4)، وRLS مفعَّلةً، وصلاحيّاتٍ
  منزوعةً عن `public`/`anon`/`authenticated`، وفريدٍ على `(city_id, window_hours)`،
  وقيودِ لاسلبيّةٍ على كلِّ عدَّادٍ. **والسببُ**: العرضُ المادِّيُّ يُفلِتُ من RLS
  ومن قيدِ المدينةِ ومن تصنيفِ الاستبقاءِ، ويُسرَّبُ عبرَ PostgREST (`ADR 0014`).
  والجدولُ **مُسجَّلٌ في القوائمِ المغلقةِ الأربعِ** (٥٥ جدولاً):
  `TABLE_RETENTION` (`lifecycleBound`) · `TABLE_ERASURE` (`reference()`) ·
  `WASLA_BOUNDARY_INVENTORY` (مالكٌ `MOVE`، مصيرٌ `KEEP`) · و`docs/wasla/boundary-audit.md`
  مُعادُ توليدُه بـ`--write` لا بيدٍ.
- **المتوسّطاتُ بسطاً ومقاماً**: `match_seconds_sum`/`match_seconds_count` و
  `rating_stars_sum`/`rating_count` — **ولا عمودَ متوسّطٍ مخزَّنٍ**. فمجموعُ المدنِ
  **مطابِقٌ** للحيِّ لا مُقارِبٌ له (`ADR 0120`)، و`average = null` عندَ مقامٍ صفرٍ.
- **شوطٌ ذرِّيٌّ**: `refresh_admin_metric_snapshots(integer) returns jsonb`
  (`20260916140100`، `security definer`، `set search_path = public, pg_temp`،
  تنفيذُها لـ`service_role` وحدَه). **عبارةٌ واحدةٌ** بستِّ `CTE` وقائعَ ثمَّ
  `computed` ثمَّ `written` — فلقطةُ `MVCC` واحدةٌ ⇒ **لا يقولُ عدَّادٌ ما لا يقولُهُ
  جارُهُ**. وتَرفعُ `INVALID_WINDOW_HOURS:%` على نافذةٍ غيرِ موجَبةٍ: **الرفضُ في
  القاعدةِ** لا في الطبقةِ.
- **مفتاحانِ مبذورانِ لكلِّ مدينةٍ لا ثلاثةٌ**: `admin_metrics_stale_after_seconds`
  (١٨٠ · `is_provisional`) و`admin_overview_live_fallback_enabled` (`false`).
  **وغيابُ الثالثِ مقصودٌ وموثَّقٌ** في §٣ من الهجرةِ: انظر «الكادةُ» أدناه.

### ما صارَ قائماً في الشِفرةِ

- **نطاقٌ نقيٌّ بلا ساعةٍ**: `packages/domain/admin/metric-snapshot.ts` —
  `ADMIN_METRIC_WINDOW_HOURS = 24` (**مصدرُ النافذةِ الواحدُ** للكاتبِ والقارئِ)،
  و`metricAverage` و`sumMetricRows` و`oldestComputedAt` و`ageSecondsOf` (مقصوصٌ
  عندَ صفرٍ) و`metricTruthStamp`. **والزمنُ مُعطىً لا مقروءٌ**: لا `new Date(` ولا
  `Date.now(` في النطاقِ.
- **تطبيقٌ وبنيةٌ**: `refreshMetricSnapshots` يرفضُ النافذةَ غيرَ الصحيحةِ ثمَّ
  يُفوِّضُ المَنفَذَ؛ و`createMetricSnapshotRefreshPort` يُصنِّفُ `INVALID_WINDOW_HOURS`
  رفضاً وما عداهُ `STORE_ERROR`، **ويُلزِمُ `ok === true` و`computed_at` نصّاً و
  `cities_written` رقماً** وإلّا `MALFORMED_RESULT` — **ولا يُرقَّعُ زمنٌ غائبٌ
  بساعةٍ محلّيّةٍ ألبتّةَ**.
- **مهمّةٌ عامّةٌ** `refreshAdminMetrics` كلَّ ٦٠ ثانيةً في `JOB_INTERVALS`،
  تُرمى عندَ `!report.ok`، وتُسجِّلُ `log.error("admin_metrics.no_cities_written")`
  عندَ صفرِ مدنٍ — **فالشوطُ الصامتُ الفاشلُ لا وجودَ له**. وليست في
  `CRITICAL_GLOBAL_JOBS` ولا `runOnStart`.
- **القراءةُ استعلامٌ واحدٌ**: `adminOverviewReading(sql, windowHours, observedAt)`
  في `apps/gateway/src/admin/queries.ts` — `CTE policy` (العتبةُ `min` فالأشدُّ
  يحكمُ، والإذنُ `bool_and` فلا تأذنُ مدينةٌ بحملٍ على غيرِها، والغيابُ يُقرأُ
  تقادُماً: **فشلٌ مُغلَقٌ**) ثمَّ `cities cross join policy left join snapshots`.
  **بدلاً من أربعةَ عشرَ استعلاماً فرعياً + ثلاثةِ عدَّاداتٍ × عددِ المدنِ في كلِّ
  فتحةِ صفحةٍ.** والجسمانِ القديمانِ **لم يُمحَيا** بل صارا `overviewCountersLive`
  و`cityPulseLive` ولا يُنادَيانِ إلّا عندَ **غيابِ كلِّ لقطةٍ** ومعَ إذنٍ مبذورٍ
  مُطفأً — ويُوسَمُ مخرَجُهما `source = "live"` فيرى المُشغِّلُ أنَّهُ يدفعُ ثمنَ المسحِ.
- **وَسْمُ صدقٍ إلزاميٌّ في النوعِ**: `AdminOverviewReading.stamp` و
  `OverviewData.stamp` **غيرُ اختياريَّينِ**، و`admin-api` يُصدِّرُ
  `metrics: reading.stamp` و`now`. **وإلزامُه أسقطَ `tsc` على نداءٍ قائمٍ بلا وَسْمٍ
  — فأنفذَ المُصرِّفُ القاعدةَ لا القارئُ.** والوَسْمُ: `source` · `computedAt` ·
  `ageSeconds` · `isStale` · `staleAfterSeconds` · `citiesMeasured`/`citiesExpected`.
  **وعمرُ التجميعِ عمرُ أقدمِ أجزائِه** لا أحدثِها، وإلّا أخفَت مدينةٌ نشيطةٌ تقادُمَ
  الباقي.
- **الغيابُ يُقالُ غياباً**: العدَّاداتُ صارت `number | null`، و`measured()` تكتبُ
  **«لم تُقَسْ»** لا صفراً — **والمدينةُ الساكنةُ تُكتَبُ أصفاراً مقيسةً**، فالفرقُ
  بينَ «صفرٌ قِيسَ» و«لم يُقَسْ» منشورٌ. وشريطُ الصدقِ يُعادُ استخدامُ أصنافِ
  `note` و`badge badge--*` القائمةِ: **لا صنفَ CSS جديداً**.
- **زمنُ ملاحظةٍ واحدٌ لكلِّ طلبٍ** (`const observedAt = new Date()` مرّةً في كلِّ
  مسلكٍ) فلا يختلفُ لوحانِ في العمرِ.

### الكادةُ في مكانٍ واحدٍ — ونقضُ قرارِ حجزٍ (`ح-8`)

**قرارُ الحجزِ كانَ**: تُقرأُ دورةُ التحديثِ من `platform_settings`
(`admin_metrics_refresh_seconds`). **ونُقِضَ بعدَ قراءةِ المُشغِّلِ**: هوَ يقرأُ
`everySeconds` **مرّةً عندَ بناءِ الحاويةِ** ثمَّ لا يرجِعُ إليهِ — فمفتاحٌ في
القاعدةِ **وعدٌ مكتوبٌ لا يُوفّى**، وأسوأُ من ثابتٍ صريحٍ لأنَّه يُوهِمُ المُشغِّلَ
بقدرةٍ لا يملكُها. والكادةُ **تقنيّةٌ لا تجاريّةٌ** فلا تقعُ تحتَ القاعدةِ 0.3.
**أمّا العتبةُ والإذنُ فيبقيانِ في الإعداداتِ** لأنَّهما يُقرأانِ **في كلِّ طلبٍ**
فيستجيبانِ فوراً. والنقضُ مُسجَّلٌ إضافةً في `ROADMAP.md` تحتَ كتلةِ الحجزِ، ومُقنَّنٌ
في `ADR 0128` §٤، **ومحروسٌ بالقاعدةِ ٥ من الحاجزِ** فلا يُردُّ بمراجعةٍ بشريّةٍ.

### الإنفاذُ الآليُّ

- **حاجزٌ جديدٌ** `scripts/check-admin-metric-snapshot-contract.ts` + لبٌّ نقيٌّ
  `scripts/lib/admin-metric-snapshot-contract.ts` — **سبعُ قواعدَ ولكلِّ واحدةٍ
  سالبةٌ مزروعةٌ** (`ح-7`) في `tests/unit/check-admin-metric-snapshot-contract.test.ts`
  (٢٢ حالةً) ومعَها **موجبةٌ تقرأُ المستودَعَ الحقيقيَّ** كي لا يكونَ الحاجزُ أخضرَ
  ميّتاً. والقواعدُ: وَسْمُ صدقٍ لكلِّ رقمٍ منشورٍ · نطاقٌ بلا ساعةٍ · نافذةٌ بمصدرٍ
  واحدٍ · جدولٌ في القوائمِ الأربعِ · كادةٌ في مكانٍ واحدٍ · لا اصطناعَ زمنٍ في
  المُحوِّلِ · سقوطٌ حيٌّ مُعلَنٌ ومحروسٌ. والقارئُ **يُجرِّدُ التعليقاتِ** قبلَ
  الحكمِ (`blankComments`/`blankSqlComments`) — إذ أوّلُ تشغيلٍ أوقعَ الحاجزَ على
  تعليقاتِ نفسِه.
- **خطوتانِ مُسمّاتانِ** في `.github/workflows/ci.yml`: تشغيلُ الحاجزِ، وتشغيلُ
  اختبارِ سالباتِه. وخطوةٌ في سلسلةِ `bun run ci` وسكربتٌ `check:admin-metric-snapshot`.
- **الاختباراتُ**: ٤٧ حالةَ وحدةٍ جديدةً (نطاقٌ ١٦ · تطبيقٌ ١٠ · قراءةٌ ١٠ ومنها
  فحصٌ يُثبِتُ **استعلاماً واحداً** بعَدِّ النداءاتِ · حاجزٌ ٢٢ · ولوحُ الإدارةِ
  ٢٩ إجمالاً بخمسٍ جديدةٍ)، و**١١ حالةَ تكاملٍ على PostgreSQL حقيقيٍّ** في
  `tests/integration/admin-metric-snapshots.test.ts`: صفٌّ لكلِّ مدينةٍ · أصفارٌ
  مقيسةٌ للساكنةِ · `computed_at` من ساعةِ القاعدةِ · عدَّادٌ يُطابِقُ عَدّاً مستقلّاً ·
  إعادةُ تنفيذٍ بلا تكرارٍ · رفضُ النافذةِ في القاعدةِ وعبرَ المَنفَذِ · تكافؤُ الوَسْمِ
  والمجموعِ · «غيرُ متوفّرٍ» بعدَ محوِ اللقطاتِ ثمَّ استعادةٌ · بذرُ الإعداداتِ.
  وتُبنى المدينةُ النشطةُ بـ`ensureActiveCity`/`restoreCityBaseline` (`OPS-019`) لا
  بانتقاءِ مدينةٍ قائمةٍ.

### وما لا يُدَّعى (`ح-5`)

- **النسخةُ التحليليّةُ لم تُبنَ.** الساقُ الثالثةُ من نصِّ البندِ تقتضي **مورداً لا
  شيفرةً** (نسخةً قارئةً بكلفةٍ وحدِّ إقامةٍ يشتبِكُ بـ`DEC-01`) ففُتِحَ لها
  **`DEC-16`** في §٢٦. فالبندُ `[~]` **باسمِ نصفِه غيرِ المبنيِّ**، و`CAP-011`
  **لا يُغلَقُ**، و`F4-06` لا يُقلَبُ بهذا وحدَه، **ولا تُكتَبُ طبقةُ تجريدٍ لنسخةٍ
  لا وجودَ لها** (`ح-6`).
- **لا قياسَ حملٍ إنتاجيٍّ**: قِيسَ **عددُ الاستعلاماتِ** وصحّةُ الأرقامِ على محرِّكٍ
  حقيقيٍّ؛ ولم يُقَس زمنُ الاستجابةِ تحتَ تزامنٍ على حجمٍ إنتاجيٍّ، ولا حجمُ الجدولِ
  بعدَ شهورٍ. **فلا يُقالُ «الحملُ نُزِعَ»** بل «المسحُ الحيُّ في كلِّ فتحةِ صفحةٍ
  زالَ من المسارِ الافتراضيِّ».
- **الكتابةُ نفسُها تقرأُ الجداولَ الحارّةَ كلَّ ٦٠ ثانيةً** — حملٌ ثابتٌ معروفٌ بدلَ
  حملٍ متقلِّبٍ بعددِ فواتِحِ اللوحةِ، **لا انعدامُ حملٍ**. وهذا نصُّ `DEC-16`.
- **الهجرتانِ لم تُطبَّقا على قاعدةٍ إنتاجيّةٍ**: حكمُ وظيفةِ التكاملِ في CI هوَ
  الحاكمُ، والأخضرُ المحلّيُّ لا يُغني عنه.

## ملحقٌ 2026-09-16 — `F8-01`: **معرِّفٌ واحدٌ من الحافةِ إلى الصفِّ** (إضافةٌ لا محوٌ · `ح-8`)

كانَ في المستودعِ ثلاثةُ أشياءَ صحيحةٍ لا يعرفُ بعضُها بعضاً: معرِّفُ طلبٍ يُولَّدُ
على الحافةِ ويُعادُ في ترويسةِ الردِّ (`ADR 0043`)، وسجلٌّ مُهيكلٌ بمُصدِرٍ واحدٍ
(`ADR 0078`)، و`audit_log` يُكتَبُ في تسعينَ موضعاً داخلَ `plpgsql`. فترويسةُ الردِّ
عندَ المستخدمِ **لا تدلُّ على سطرِ سجلٍّ**، وسطرُ السجلِّ **لا يدلُّ على صفٍّ في
القاعدةِ**، وصفُّ الطابورِ **لا يُعرَفُ لأيِّ طلبٍ هوَ**. والعطبُ لا يُخفِقُ اختباراً؛
يُرى مرّةً واحدةً: ليلةَ يُقالُ «طلبي فشلَ» فيُقرأُ ألفُ سطرٍ متجاورٍ لا يُعرَفُ
أيُّها من أيٍّ.

**فصارَ**: معرِّفٌ يُولَّدُ في موضعٍ واحدٍ
(`packages/infrastructure/observability/correlation.ts`)، ويُتوارَثُ بـ`AsyncLocalStorage`
لا بتوقيعِ دالّةٍ، ويُلحَقُ بكلِّ سطرِ سجلٍّ في المُصدِرِ الوحيدِ، ويُنقَلُ إلى
المحرِّكِ بمتغيّرٍ جلسيٍّ **محدودِ المعاملةِ** (`set_config(..., true)` في
`withRequestContext` — شرطُ الصحّةِ تحتَ تجمُّعِ المعاملاتِ)، ويُحفَظُ دليلاً في
عمودِ `request_id` على `audit_log` و`notification_outbox` و`ledger_entries` يملؤه
**مُشغِّلُ `before insert`** لا افتراضيٌّ غيرُ ثابتٍ (الذي كانَ سيُعيدُ كتابةَ
الجدولِ كلِّه تحتَ قفلٍ حصريٍّ · `CAP-007`/`F7-07`). وعبورُ الطابورِ يتمُّ بالصفِّ
نفسِه: العاملُ يقرأُ `OutboxDelivery.requestId` ويُعالِجُ الصفَّ **داخلَ سياقٍ
مُستعادٍ منه**، وكلُّ شوطٍ دوريٍّ يجري بسياقٍ ببادئةِ `job-` فلا يتنكّرُ شوطٌ
بمعرِّفِ طلبٍ. والتفصيلُ والطرقُ المرفوضةُ في `ADR 0129`، والدليلُ في
`docs/evidence/architecture/F8-01-20260916.md`.

### الإنفاذُ الآليُّ

- **حاجزٌ جديدٌ** `scripts/check-request-correlation.ts` + عقدٌ نقيٌّ
  `scripts/lib/request-correlation-contract.ts` — **عشرُ قواعدَ ولكلِّ واحدةٍ سالبةٌ
  مبذورةٌ** (`ح-7`) في `tests/unit/request-correlation-guard.test.ts` (١٨ حالةً)
  ومعَها موجبةٌ تقرأُ المستودَعَ الحقيقيَّ فلا يكونُ الحاجزُ أخضرَ ميّتاً. والقواعدُ:
  عمودٌ وقيدٌ ومُشغِّلٌ لكلِّ جدولٍ موصولٍ · تحقُّقُ القيدِ في طورِ `validate` · لا
  هجرةَ تُدرِجُ العمودَ صريحاً · اسمُ المتغيّرِ الجلسيِّ محصورٌ في قائمةٍ مُعلَنةٍ ·
  ترويسةُ الطلبِ الوارِدةُ لا تُقرأُ في سطحِ الخادمِ (والواجهةُ تقرأُ ترويسةَ
  **ردِّنا** بحقٍّ · `F1-08`) · الحافةُ تلفُّ · المُصدِرُ يُلحِقُ · الشوطُ يلفُّ ·
  كلُّ موضعٍ موصولٍ مُعلَنٍ ملفوفٌ فعلاً · لا تعريفَ ثانياً لـ`withRequestContext`
  ولا توليدَ معرِّفٍ في طبقةِ القاعدةِ.
- **خطوةٌ مُسمّاةٌ** في `.github/workflows/ci.yml` (وظيفةُ `verify`) وخطوةٌ في
  سلسلةِ `bun run ci`.
- **الاختباراتُ**: ١٣ حالةَ وحدةٍ للسياقِ نفسِه (منها عزلُ شوطينِ متوازيينِ، وأنَّ
  `set_config` **أوّلُ أمرٍ** في المعاملةِ، وأنَّ غيابَ السياقِ لا يُرسِلُ ضبطاً)،
  و١٨ سالبةً للحاجزِ، و**٨ حالاتِ تكاملٍ على PostgreSQL حقيقيٍّ** في
  `tests/integration/request-correlation.test.ts`: وصولُ المعرِّفِ إلى الصفِّ ·
  الغيابُ `NULL` · **محدوديّةُ الضبطِ بالمعاملةِ** على المُجمَّعِ عينِه · رفضُ قيدِ
  الشكلِ لقيمةٍ فاسدةٍ من مُدرِجٍ صريحٍ · المُشغِّلُ لا يطمِسُ قيمةً صريحةً صحيحةً ·
  `convalidated` و`tgenabled` **مقروءانِ من فهرسِ النظامِ** لا من نصِّ الهجرةِ.

### وما لا يُدَّعى (`ح-5`)

- **لا ناقلَ OpenTelemetry ولا SDK ولا جامِعَ آثارٍ في المستودعِ**: لا `traceparent`
  ولا `span` ولا تصديرَ. المبنيُّ **سلسلةُ ارتباطٍ** لا **آثارٌ موزَّعةٌ**. فالبندُ
  `F8-01` يبقى `[~]` **باسمِ نصفِه غيرِ المبنيِّ**، و`OPS-002` **لا يُغلَقُ**،
  وفُتِحَ لاختيارِ الناقلِ والجامعِ **`DEC-17`** في §٢٦ لأنَّه **بنيةٌ تحتيّةٌ لا
  شيفرةٌ** (`OPS-001` · `F9-01`). **ولا تُكتَبُ طبقةُ تجريدٍ لجامعٍ لا وجودَ له**
  (`ح-6`).
- **المواضعُ الموصولةُ خمسٌ من ١٣٧ دالّةَ RPC**، مُعلَنةٌ بالاسمِ في العقدِ.
  والحاجزُ يمنعُ **نقصانَها** ولا يزعُمُ شمولَها. والجداولُ الموصولةُ **ثلاثةٌ من
  خمسينَ**.
- **لا قياسَ أثرٍ على الأداءِ**: `withRequestContext` تفتحُ معاملةً حيثُ كانَ
  استعلامٌ مفردٌ، وأمرُ `set_config` زائدٌ واحدٌ لكلِّ وحدةِ عملٍ موصولةٍ. ولم
  يُقَس ذلكَ تحتَ تزامنٍ إنتاجيٍّ، **فلا يُقالُ «بلا كلفةٍ»**.
- **تصحيحٌ إضافيٌّ (`ح-8`)**: نصُّ الحجزِ في `ROADMAP.md` ذكرَ `move_event_outbox`
  جدولاً ثالثاً، **ولا وجودَ لجدولٍ بهذا الاسمِ** في المستودعِ (تُحقِّقَ من ذلكَ في
  جردِ الجداولِ كلِّها)، فحلَّ محلَّه `ledger_entries` (أثرُ المالِ). ونصُّ الحجزِ
  يبقى مكتوباً لا ممحوّاً.
- **الهجرتانِ لم تُطبَّقا على قاعدةٍ إنتاجيّةٍ**: حكمُ وظيفةِ «تكامل على PostgreSQL
  حقيقي» في CI هوَ الحاكمُ، والأخضرُ المحلّيُّ لا يُغني عنه — ولا تجري اختباراتُ
  التكاملِ محلّيّاً أصلاً (لا حاويةَ في بيئةِ التنفيذِ).

### تصحيحٌ إضافيٌّ على `F8-01` بعدَ حكمِ CI (2026-09-16 · `ح-8`)

**لا يُمحى ممّا سبقَ حرفٌ.** أخفقَت وظيفةُ «تكامل على PostgreSQL حقيقي» في الجولةِ
الأولى (`35123225494`) بحالتَينِ **قائمتَينِ** تحرسانِ سطحَ الصلاحياتِ، إذ نزعَت
هجرةُ التوسيعِ صلاحيّةَ دالّتَيِ الارتباطِ من `anon` و`authenticated` وحدَهما
**ومنحةُ `PUBLIC` التلقائيّةُ على الدوالِّ تبقى ويورِّثُها `anon`**. وحالاتُ
السلسلةِ الثمانِ مرَّت في الجولةِ عينِها. فأُضيفَ `public` إلى النزعِ، **وأُضيفَت
قاعدةٌ ساكنةٌ حادِيةَ عَشْرةَ** (`grant.public`) بسالبةٍ مبذورةٍ وموجبةٍ — فصارَ
الحاجزُ إحدى عشرةَ قاعدةً و٢٠ حالةً بدلاً من عشرٍ و١٨، والرقمانِ السابقانِ باقيانِ
مذكورَينِ. **ولم يُخفَّفْ اختبارٌ ولم يُعطَّلْ حاجزٌ ولم يُصنَّفْ تجاوزٌ ولم
يُنقَلْ عطبٌ**، والدرسُ عُمِّمَ صنفاً لا موضعاً.

### `ح-4` — جولاتُ `main` الخضراءُ تُقرأُ بالوظيفةِ (2026-09-16 · لا قلبَ حالةٍ)

`35115060956`@`09e05ef` · `35116319583`@`1b79426` · `35127423244`@`e2ebd66`:
الوظائفُ الأربعُ `success` في الثلاثِ ومعَها `Roadmap freshness`. **فاستوفى
`F7-08` شرطَ الجولاتِ الثلاثِ ويبقى `[~]`** لأنَّ الساقَ الثالثةَ من نصِّه لم
تُبنَ (`DEC-16`) — الاستيفاءُ يُسجَّلُ ولا يُصرَفُ. و`F8-01` مُدمَجٌ في
`e2ebd66` ولهُ جولةٌ واحدةٌ من ثلاثٍ ويبقى `[~]` (`DEC-17`). **ولم تُمحَ
إخفاقاتُ الطريقِ**: `35123225494` (سطحُ الصلاحياتِ) و`35124024068` (توكيدُ
الصمودِ · `DEC-18`) و`35124024098` (`Roadmap freshness`) مكتوبةٌ بأسبابِها.

## ملحقٌ 2026-09-16 — `DEC-18`: **التدهورُ يُقاسُ بعملٍ لا بزمنِ ساعةٍ** (إضافةٌ لا محوٌ · `ح-8`)

**لا يُمحى ممّا سبقَ حرفٌ.** هذا الملحقُ يصفُ **ما صارَ نافذاً** في قياسِ تدهورِ
الصمودِ، ويُقرأُ معَ [ADR 0130](adr/0130-degradation-is-measured-in-work-not-in-clock-time.md)
ودليلِه `docs/evidence/capacity/DEC-18-20260916.md`.

### ما كانَ

`tests/e2e/ride-soak.test.ts` يوكِّدُ أنَّ **متوسِّطَ زمنِ** آخرِ خمسِ رحلاتٍ أقلُّ من
ثلاثةِ أضعافِ متوسِّطِ أوّلِ خمسٍ. والغرضُ مشروعٌ (كشفُ فهرسٍ مفقودٍ · تسريبِ حالةٍ ·
استعلامٍ يمسحُ جدولاً ينمو)، لكنَّ **المقيسَ كانَ زمنَ ساعةٍ على مُنفِّذٍ مُشترَكٍ**:
قرأَ ٣٫٢ في الجولةِ `35124024068` وأسقطَ الوظيفةَ، ثمَّ مرَّ **بإعادةِ التشغيلِ
بالبصمةِ عينِها** وعلى طلبِ الدمجِ — ولا حرفَ تغيَّرَ بينَ القراءتَينِ.

### ما صارَ نافذاً

- **وحدةُ القياسِ**: `(tup_returned + tup_fetched)` و`(blks_read + blks_hit)` من
  `pg_stat_database` محدودةً بـ`current_database()` — في `tests/support/engine-work.ts`.
  **و`pg_stat_statements` غيرُ متاحٍ** في CI (صورةُ الخدمةِ `postgis/postgis:17-3.5`
  بلا `shared_preload_libraries`) فلم يُعتمَدْ.
- **السقفُ ×٣ كما كانَ** — `WORK_GROWTH_CEILING` **مصدرُ حقيقةٍ واحدٌ**، ورفعُه
  **يُسقِطُه حاجزٌ ساكنٌ** لا مراجعةٌ بشريّةٌ.
- **القراءةُ عندَ ثلاثةِ حدودٍ** (بعدَ الرحلةِ ١٠ و٢٠ و٣٠)، والمقارنةُ **دافئةٌ
  بدافئةٍ**، والكتلةُ الأولى **إحماءٌ مُعلَنٌ مُستبعَدٌ** — وهذا **تضييقٌ للحاجزِ**:
  لو صارَت مقاماً لاتّسعَ المقامُ فخضَّرَ نموّاً حقيقيّاً.
- **السكونُ قبلَ كلِّ قراءةٍ شرطُ صحّةٍ لا تأدُّبٌ**: `pg_stat_clear_snapshot()` لأنَّ
  `stats_fetch_consistency=cache` يُجمِّدُ اللقطةَ داخلَ المعاملةِ، ومُهلةٌ تتجاوزُ
  **حدَّ الإفراغِ الأدنى (ثانيةً)**، وثلاثُ قراءاتٍ مستقرّةٍ، وسقفُ انتظارٍ يُرمى
  عندَه **خطأٌ مُعلَنٌ لا رقمٌ مشكوكٌ فيه**.
- **كتلةٌ بلا عملٍ تُرمى خطأً في الطرفَينِ**: صفرٌ في المقامِ يُفجِّرُ النسبةَ، وصفرٌ
  في البسطِ **يُخضِّرُ الحاجزَ وهوَ أعمى**.
- **حاجزٌ ساكنٌ**: `scripts/lib/soak-work-measure.ts` (قواعدُ خالصةٌ) +
  `scripts/check-soak-work-measure.ts` (قرصٌ وحكمُ خروجٍ، **وغيابُ ملفٍّ محكومٍ خرقٌ لا
  صمتٌ**) + `tests/unit/check-soak-work-measure.test.ts` (**سالبةٌ لكلِّ قاعدةٍ** ·
  `ح-7`)، مُشغَّلاً في سلسلةِ `ci` وفي وظيفةِ `verify`.

### ما لا يُدَّعى (`ح-5`)

- **لا بندَ مرحلةٍ قُلِبَ**: `F8-01` و`F7-08` يبقيانِ `[~]`، و`OPS-002` و`CAP-011`
  **لا يُغلَقانِ** بهذا العملِ.
- **ولا يُدَّعى صمودٌ عندَ الحِملِ**: المقيسُ ثلاثونَ رحلةً متتابعةً على قاعدةٍ
  حقيقيّةٍ — لا ألفٌ ولا تزامُنٌ ولا نشرٌ حيٌّ.
- **وما ينمو خارجَ المحرِّكِ لا يراهُ هذا العدَّادُ**: شبكةٌ · تسلسُلُ JSON · ذاكرةُ
  العمليّةِ. فهوَ حاجزُ عملِ قاعدةٍ لا حاجزُ أداءٍ شاملٌ.
- **ولم يُقَسْ تقلُّبُ العدَّادِ نفسِه على مُنفِّذِ CI** قبلَ قراءةِ حكمِه؛ الأرقامُ
  المذكورةُ محلّيّةٌ، **وحكمُ CI وحدَه حكمٌ**.

## ملحقٌ 2026-09-16 — `F8-02`: **المقياسُ المنشورُ عقدٌ لا تعليقٌ** (إضافةٌ لا محوٌ · `ح-8`)

**لا يُمحى ممّا سبقَ حرفٌ.** الحاكمُ: `docs/adr/0131-a-published-metric-is-a-contract-not-a-comment.md`،
والدليلُ: `docs/evidence/architecture/F8-02-20260916.md`.

- **ما كانَ**: أربعٌ وعشرونَ عائلةَ مقاييسَ منشورةً على `/metrics`، **وليسَ فيها**
  معدَّلُ الطلباتِ ولا زمنُها ولا أخطاؤها ولا ذاكرةُ العمليّةِ ولا اتّصالاتُ
  القاعدةِ ولا زمنُ إسنادِ الطلبِ — فأوّلُ سؤالَينِ عندَ عُطلٍ («أينَ البُطءُ؟»
  و«هل نفدَتِ الاتّصالاتُ؟») يُجابانِ بالتخمينِ.
- **ما صارَ**: ثلاثَ عشرةَ عائلةً جديدةً تُغطّي الفئاتِ التسعَ المذكورةَ في نصِّ
  البندِ. والوسمُ **قالبُ المسارِ** الذي يعرِفُه Hono لا المسارُ الخامُّ: معرِّفٌ
  في وسمٍ **يُسقِطُ المُجمِّعَ لا الخدمةَ**، فلا يُرى في اختبارٍ ولا في مراجعةٍ
  بل في فاتورةِ الإنتاجِ.
- **ولا نسبةَ تُنشَرُ رقماً**: معدَّلُ قبولِ العروضِ بسطُه ومقامُه منشورانِ
  عدّادَينِ، ونشرُ النسبةِ يُنشئُ **مصدرَ حقيقةٍ ثالثاً** يناقضُهما (النسبُ لا
  تُجمَعُ عبرَ النسخِ، والعدّاداتُ تُجمَعُ) — `القاعدة 0.6`.
- **وزمنُ الإسنادِ يُقاسُ في القاعدةِ من الختمَينِ المكتوبَينِ**
  (`orders.created_at`/`matched_at`) بـ`percentile_cont`، لا بمؤقِّتٍ في الذاكرةِ
  يُفقَدُ بإعادةِ التشغيلِ ويرى نسختَه وحدَها. والشرطُ على `matched_at` لا على
  `created_at` كي لا يسقُطَ من النافذةِ **أبطأُ الطلباتِ** وهيَ المقصودةُ
  بالقياسِ. ولا عمودَ `assigned_at` في المخطَّطِ **ولم يُخترَعْ عمودٌ لمقياسٍ**.
- **والنافذةُ منشورةٌ عائلةً** (`waslah_order_assignment_window_seconds`) ومعَها
  عددُ ما وقعَ فيها: `F7-08` يحسُبُ الفرقَ نفسَه بنافذةٍ **يضبطُها المُشغِّلُ**،
  فرقمانِ مختلفانِ بحقٍّ، ومَن قرأهُما بلا نافذةٍ ظنَّ أحدَهما خطأً (`ADR 0128`).
- **والاتّصالاتُ من رؤيةِ المحرِّكِ مع سقفِها**: `pg_stat_activity` مُرشَّحاً
  بقاعدةِ البياناتِ الجاريةِ، والسقفُ من `current_setting('max_connections')` —
  عدٌّ في العمليّةِ يرى مجمعَ نفسِها وحدَها والسقفُ مشترَكٌ بينَ النسخِ والعاملينَ
  والهجراتِ، ورقمٌ بلا حدٍّ لا يُقرأُ.
- **والذاكرةُ تُقرأُ عندَ المسحِ** بعدَ فحصِ السرِّ لا بمؤقِّتٍ دائرٍ،
  و`/metrics` يبقى بسرٍّ في ترويسةٍ ومقارنةٍ ثابتةِ الزمنِ ويفشلُ مغلقاً.
- **والإنفاذُ آليٌّ**: `scripts/check-core-metrics.ts` (منطقُه الخالصُ في
  `scripts/lib/core-metrics-contract.ts`) في سلسلةِ `bun run ci` وخطوتَينِ
  مُسمّاتَينِ في وظيفةِ `verify`، بإحدى عشرةَ قاعدةً **لكلِّ واحدةٍ سالبةٌ مبذورةٌ**
  (`ح-7` · ٢٥ حالةً)، ومعَها ١٥ حالةً سلوكيّةً على المُسجِّلِ الحقيقيِّ.
- **وقِيسَ ثمَّ صُحِّحَ من جذرِه**: التطبيعُ كانَ يقبلُ مساراً خامّاً يبدو سليمَ
  الشكلِ (`/v1/rides/<uuid>`) فيصيرُ لكلِّ رحلةٍ سلسلةٌ. صُحِّحَ التطبيعُ (لا رقمَ
  في مقطعٍ حرفيٍّ إلّا وسمَ إصدارٍ)، **وأُضيفَت قاعدةٌ ثانيةٌ**
  (`http.every-route-labelled`) تقرأُ **كلَّ** قالبٍ مُعلَنٍ في البوّابةِ (٧٨
  قالباً) وتُسقِطُ البناءَ إن طُمِرَ أيٌّ منها في `other` — كي لا يكونَ علاجُ
  الانفجارِ خرَساً. **ولم يُخفَّفْ اختبارٌ ولم تُسكَّتْ قاعدةٌ.**
- **والحاجزُ يُسقِطُ التعليقاتِ قبلَ الحكمِ**: المحكومُ عليه ما يُنَفَّذُ لا ما
  يُشرَحُ — حاجزٌ يعاقِبُ على **ذِكرِ** العطبِ يُعلِّمُ المؤلِّفينَ كتمانَ السببِ.
- **وفهرسٌ بطَورِ فهرسٍ** (`create index concurrently`) على `orders (matched_at)`
  كي لا يصيرَ المقياسُ نفسُه مسحاً كاملاً كلَّ مسحةٍ (`F7-07`).
- **وما لا يُدَّعى (`ح-5`)**: **لا لوحةَ ولا إنذارَ ولا جامِعَ Prometheus** في
  المستودَعِ ولا في أيِّ بيئةٍ — لا خادمَ يمسحُ `/metrics`، ومقياسٌ لا يقرأُه أحدٌ
  ليسَ مراقبةً. اللوحاتُ `F8-07` والإنذاراتُ `OPS-003`، **ولا تُبنى طبقةٌ حولَ
  جامعٍ لا وجودَ له** (`ح-6`). ولا أثرَ موزَّعاً (`F8-01` `[~]` · `DEC-17`). ولا
  قياسَ لكلفةِ الوسيطِ تحتَ تزامنٍ إنتاجيٍّ **فلا يُقالُ «بلا كلفةٍ»**. فالبندُ
  `[~]` لا `[x]`.

---

## زيادةٌ 2026-09-16 — المِلكِيَّةُ صارَت قيدَ بناءٍ لا عُرفَ مراجعةٍ (`F8-08` · الضابطُ الأوّلُ · `ح-8`)

**قبلَ هذا التاريخِ** كانَت المِلكِيَّةُ مفروضةً فعلاً في `SQL` — دوالُّ الراكبِ
والسائقِ تقيِّدُ بـ`and o.rider_id = v_rider_id` وأخواتِها — لكنَّ **لا شيءَ في
المستودَعِ يمنعُ مساراً جديداً من إغفالِها**. وذاكَ الصنفُ من العَطَبِ لا يظهرُ في
مقياسٍ: المسارُ يعملُ ويُعيدُ `200`، وجسمُه صفٌّ صحيحٌ لغيرِ سائلِه.

**والآنَ** حاجزٌ في وظيفةِ `verify` يقرأُ شجرَ `apps/gateway/src/routes` كاملاً:
٢٥ مساراً بمعرِّفِ كائنٍ — **١٦ مُقيَّداً بالناظرِ · ٩ مُستثنىً** بثلاثةِ أصنافٍ
مغلقةٍ (`admin-guard` · `unguessable-token` · `signed-webhook`) لكلٍّ سببٌ مكتوبٌ
ودليلٌ نصِّيٌّ مُشتَرَطٌ في الملفِّ. **واستثناءٌ ماتَ مسارُه يُسقِطُ البناءَ**،
**وتعذُّرُ قراءةِ الشجرِ كاملاً يُسقِطُه** — فالعجزُ عن القياسِ عطبٌ لا عذرٌ.

ومعَه قياسٌ على PostgreSQL حقيقيٍّ: اثنتا عشرةَ دالّةً تُنادى بهويّةِ غريبٍ،
فتَرُدُّ، **وردُّها يطابقُ ردَّ المعدومِ حرفاً** (فلا يُعَدُّ المسارُ عدّادَ
وجودٍ)، **والكاتباتُ السّتُّ لا تُغيِّرُ صفّاً** (الرفضُ قبلَ الكتابةِ لا بعدَها).

**وما لا يُدَّعى**: `F8-08` ستّةَ عشرَ ضابطاً والمبنيُّ **واحدٌ** — فسِمَةُ البندِ
في §12 تبقى `[ ]`. ولا يُدَّعى أمانٌ من `IDOR`؛ يُدَّعى أنَّ مساراً جديداً يُغفِلُ
هويّةَ ناظرِه يُسقِطُ البناءَ. **وفجوةٌ مُعلَنةٌ دَيناً**: مُعالِجٌ يحملُ الهويّةَ
ثمَّ يُهمِلُها في استدعاءٍ داخليٍّ آخرَ لا يلتقطُه أيٌّ من البرهانَينِ. و`RLS`
غيرُ مقيسٍ ههنا (الاتّصالُ بمالكِ القاعدةِ). والحاكمُ `ADR 0132`.

## زيادةٌ — سِجلُّ ضوابطِ `F8-08` الستّةَ عشرَ (2026-09-16 · `ADR 0133`)

نصُّ `F8-08` يشترطُ «١٦ ضابط أمن» **ولا قائمةَ لها في المستودَعِ**. وبندٌ يُقاسُ
إتمامُه بعددٍ بلا قائمةٍ لا يُغلَقُ بصدقٍ، **ولا تُفنَّدُ دعوى إغلاقِه**. فصارَ
لها سِجلٌّ مغلقٌ مُنفَذٌ في `scripts/lib/security-controls-registry.ts`، يحرسُه
`scripts/check-security-controls.ts` بستِّ قواعدَ لكلٍّ سالبةٌ مزروعةٌ.

**الحالُ المقيسُ**: ٩ مبنيّاً · ٦ جزئيّاً · ١ غيرَ مبنيٍّ — واثنانِ محجوزانِ
بعائقِ مالِكٍ (`SEC-15` ⇒ `O-6` · `SEC-16` ⇒ `DEC-17`).

**وأحكمُ قواعدِه**: رمزُ `F8-08` **مُشتَقٌّ من السِجلِّ لا مكتوبٌ بيدٍ** — ما دامَ
ضابطٌ واحدٌ غيرَ مبنيٍّ فالرمزُ `[ ]`، ويُسقِطُ الحاجزُ البناءَ على مَن رفعَه.

**وما لا يُدَّعى**: تسميةُ الضابطِ ليسَت بناءَه. هذهِ رِجلُ مِعيارٍ لا مُنجَزٍ: لم
تُؤمِّن سطراً واحداً، ولم يُبنَ فيها ضابطٌ جديدٌ، والمبنيُّ من `F8-08` يبقى
الضابطَ الأوّلَ وحدَه. و«تسعةٌ مبنيّاً» جردٌ لعملٍ سابقٍ لا تقدُّمٌ أُحرِزَ اليومَ.
والحاكمُ `ADR 0133`.

## زيادةٌ — `SEC-09` من مُركَّبٍ نصّاً إلى مقيسٍ سلوكاً (2026-09-16 · `ADR 0134`)

سجَّلَ `ADR 0133` فجوةَ `SEC-09` حرفاً: الحارسُ مقيسٌ **بوجودِه نصّاً** لا
بسلوكِه. وقُفلٌ مُعلَّقٌ على البابِ ليسَ قُفلاً مُغلَقاً.

فبُنِيَ `tests/integration/admin-guard-authority.test.ts` — **١١ حالةً · ٢٢
توكيداً على PostgreSQL حقيقيٍّ**: جلسةٌ مُبطَلةٌ ومنقضيةٌ (الوقتُ يُزوَّرُ في
القاعدةِ لا يُنتظَرُ) ومَن نُزِعَت عنهُ الصفةُ (**وتُبطَلُ جلستُه في الصفِّ**)
ومَن حُظِرَ، وراكبٌ لا تُفتَحُ لهُ جلسةٌ أصلاً (`NOT_ADMIN` من المحرِّكِ)، وثلاثُ
حالاتِ كتابةٍ مرفوضةٍ **مقيسةٍ بالأثرِ** لا بالردِّ، ومعَها **الطرفُ الموجَبُ**
في موضعَينِ.

**والسِجلُّ صارَ: ١٠ مبنيّاً · ٥ جزئيّاً · ١ غيرَ مبنيٍّ.** والحيثيّةُ القديمةُ
محفوظةٌ في تعليقٍ فوقَ الحقلِ لا ممحوّةٌ (`ح-8`).

**وما لا يُدَّعى**: رمزُ `F8-08` يبقى `[ ]`؛ والمقيسُ مسارٌ كتابيٌّ واحدٌ نموذجاً؛
ولا سطحَ `HTTP` حقيقيّاً في القياسِ (`app.fetch` لا مقبسٌ) — وفجوةُ `SEC-06`
باقيةٌ؛ ولا يُقاسُ زمنُ المقارنةِ. والحاكمُ `ADR 0134`.

## زيادةٌ — `SEC-06`: حاجزٌ معكوسُ القياسِ وبرهانٌ بالردِّ (2026-09-16 · `ADR 0135`)

**حاجزٌ يفحصُ الموجودَ لا يكشفُ الناقصَ.** فقياسُ `SEC-06` معكوسٌ: تُكتشَفُ أسطحُ
الصفحاتِ **من القرصِ** (كلُّ وحدةِ توجيهٍ فيها `c.html(`) ثمَّ تُطابَقُ بسِجلٍّ
مغلقٍ — فسطحٌ جديدٌ غيرُ مُسجَّلٍ يُسقِطُ البناءَ، **والتسجيلُ التزامٌ لا إعفاءٌ**.

**وعطبٌ وُجِدَ لا افتُرِضَ**: `securityHeaders?` مُركَّباً بشرطِ `!== undefined` —
وعدٌ لا ضمانٌ. **وأُصلِحَ في الجِذرِ**: الحقلُ مطلوبٌ والتركيبُ بلا شرطٍ، فالمُترجِمُ
يرفضُ مَن يُهمِلُه.

**والنصُّ لا يُغني عن الردِّ**: `page-headers-measured.test.ts` يقرأُ الترويساتَ من
ردٍّ فعليٍّ للموجِّهِ الحقيقيِّ في حالِ «غيرِ موجودٍ» — أشيعُ ردٍّ وأشيعُ ما يُغفَلُ.

**والسِجلُّ صارَ: ١١ مبنيّاً · ٤ جزئيّاً · ١ غيرَ مبنيٍّ.**

**وما لا يُدَّعى**: رمزُ `F8-08` يبقى `[ ]`؛ ولا `Strict-Transport-Security` (بنيةٌ
تحتيّةٌ)؛ والاكتشافُ بوسمِ `c.html(` لا اكتشافاً تامّاً؛ وسطحُ اللوحةِ مبرهَنٌ
ساكناً لا مقيساً. والحاكمُ `ADR 0135`.

## زيادةٌ — `SEC-12`: سِجلٌّ مغلقٌ وأثرٌ يُقاسُ بالأثرِ (2026-09-16 · `ADR 0136`)

**سِجلٌّ يعدُّ المحاولاتِ أفعالاً أسوأُ من لا سِجلٍّ، لأنَّهُ يُتَّهَمُ بهِ بريءٌ.**
كانَ `SEC-12` `partial` بفجوةٍ مُسمّاةٍ: لا سِجلَّ مغلقٌ يقولُ **أيُّ** الأعمالِ يجبُ
أن تُدقَّقَ. **والعِلّةُ أنَّ الأثرَ موجودٌ** في نحوِ خمسينَ دالَّةً فيُقرأُ ذلكَ
تغطيةً — **والعددُ ليسَ برهاناً**.

فبُنِيَ سِجلٌّ **مغلقٌ** لثمانيةَ عشرَ فعلَ فاعلٍ مُتسلِّطٍ بعشرِ قواعدَ،
**والاكتشافُ معكوسٌ**: تُقرأُ تعاريفُ الدوالِّ كلُّها من الهجراتِ (١٩٥ تعريفاً من
١٤٥ هجرةً) فدالَّةٌ مُتسلِّطةٌ كاتبةٌ غيرُ مُسجَّلةٍ تُسقِطُ البناءَ.

**والحُكمُ يُكتشَفُ مُنابَاً** — نوّابُ الحُكمِ (دالَّةٌ تحكُمُ ولا تكتبُ) مُكتشَفونَ
لا مكتوبونَ، وبلا ذلكَ لأفلَتَ `claim_support_ticket` و`resolve_support_ticket`
اللذانِ يُوكِلانِ الحُكمَ إلى `is_support_actor(...)`. **ومعجمُ الأفعالِ محروسٌ في
الحدَّينِ** — المكتوبُ غيرُ المُعلَنِ خرقٌ، والمُعلَنُ غيرُ المكتوبِ خرقٌ — والاسمُ
المُركَّبُ لا يُقبَلُ إلّا محصورَ اللاحقةِ في جسمِه.

**وعطبٌ أُصلِحَ في جِذرِه**: `claim_safety_incident` و`resolve_safety_incident` كانا
الفعلَينِ المُتسلِّطَينِ الوحيدَينِ بلا أثرٍ، والثاني **يُغلِقُ بلاغَ استغاثةٍ وقد
يحظِرُ المُبلِّغَ** — فكانَ يُقرأُ **حظرٌ بلا سببٍ مكتوبٍ**. فزِيدَ إدراجٌ **بعدَ**
التحديثِ الناجحِ وفي المعاملةِ نفسِها، **والأثرُ ليسَ أثراً إن جازَ أن يوجدَ بلا
فعلِه أو أن يوجدَ الفعلُ بلا أثرِه**.

**ومقيسٌ بالأثرِ لا بالنصِّ**: ٩ حالاتٍ · ٣١ توكيداً على PostgreSQL 18.6 — الصفُّ
يُنسَبُ إلى مدينةِ **الحادثةِ** لا الفاعلِ (وهُما مبذورانِ في مدينتَينِ قصداً)،
وخمسُ حالاتِ رفضٍ **لا تُخلِّفُ شيئاً** بلقطةِ عدَدٍ قبلُ وبعدُ.

**والسِجلُّ صارَ: ١٢ مبنيّاً · ٣ جزئيّاً · ١ غيرَ مبنيٍّ.** والحيثيّةُ القديمةُ
محفوظةٌ في تعليقٍ فوقَ الحقلِ لا ممحوّةٌ (`ح-8`).

**وما لا يُدَّعى**: رمزُ `F8-08` يبقى `[ ]`؛ والمحورُ **التسلُّطُ لا الحساسيّةُ**
فأفعالُ المستخدمِ على بياناتِ نفسِه خارجَه وفجوتُها مُسمّاةٌ؛ ولا تدقيقُ كلِّ فرعٍ؛
ولا يُدَّعى أنَّ السِجلَّ **يُقرأُ**؛ ولا منعُ المحوِ (فجوةُ `SEC-10`). والحاكمُ
`ADR 0136`.

## زيادةٌ 2026-09-17 — فرادةُ طابعِ الهجرةِ مفروضةٌ آليّاً (تصحيحٌ بالإضافةِ · `ح-8`)

كشفَ دمجُ `PR #75` أنَّ هجرتَينِ حملتا **الطابعَ الزمنيَّ عينَه**
(`20260916210000` لِـ`F8-02` ولِـ`SEC-12`) في فرعَينِ متوازيَينِ، ولم يتعارضا في
`git` ولا أسقطا حاجزاً — لأنَّ قاعدةَ CI تُبنى من الصفرِ فتُطبَّقُ الهجرتانِ كلتاهما.

**والعطبُ في الاستئنافِ**: `scripts/migrate.ts` **بلا سجلِّ هجراتٍ مُطبَّقةٍ**،
و`--from` **حصريٌّ** نصّيّاً، فطابعٌ مكرَّرٌ يعني **تخطّياً صامتاً** للهجرةِ الثانيةِ
على قاعدةٍ قائمةٍ — مخطَّطٌ ناقصٌ يُظنُّ تامّاً، ولا يظهرُ إلّا في الإنتاجِ.

**الحالُ الآنَ**: طابعُ هجرةِ `SEC-12` صارَ `20260916210100` (بلا تغييرِ محتوىً)،
و`scripts/check-migrations.ts` يحملُ قاعدةً تُسقِطُ البناءَ على أيِّ طابعٍ مكرَّرٍ
مُسمِّيةً المتنازعَينِ، مُبرهَنةَ السقوطِ بسالبةٍ مزروعةٍ بالاسمَينِ الحقيقيَّينِ
(`ح-7`) وبقياسٍ فعليٍّ لخروجِ الحاجزِ بالرمزِ `1` ثمَّ `0`.

**وما لا يُدَّعى** (`ح-5`): **لا سجلَّ هجراتٍ مُطبَّقةٍ في القاعدةِ بعدُ** — الاستئنافُ
لم يصِرْ صادقاً بذاتِه، وإنّما مُنِعَ مصدرُ الالتباسِ الواحدُ. ولا يُدَّعى سلامةُ أيِّ
قاعدةٍ قائمةٍ طُبِّقَ عليها الطابعُ المكرَّرُ. (`ADR 0137`)

## زيادةٌ 2026-09-17 — دورةُ حياةِ الدفعِ مقيسةٌ بأطوارِها، وعطبانِ ماليّانِ أُغلِقا (`F8-06`)

**الحالُ الآنَ**: أطوارُ دورةِ حياةِ الدفعِ مقيسةٌ باثنتَي عشرةَ حالةً على الأصنافِ
الستّةِ التي في نصِّ البندِ (نجاحٌ · فشلٌ · معلّقٌ · مكرّرٌ · استرجاعٌ · تناقضٌ)،
تُشغِّلُها وظيفةُ التكاملِ على PostgreSQL حقيقيٍّ بخطوةٍ **باسمِها** في CI، ويحرسُ
شمولَها حاجزٌ ساكنٌ **يكتشفُ الكُتّابَ من الهجراتِ** (خمسةٌ اليومَ، كلُّهم مُمارَسونَ)
**والحالاتَ من قيدِ المخطَّطِ** (سبعٌ، كلُّها مقيسةٌ، ولا دَينَ). وحاجزُ الشمولِ
نفسُه مقيسٌ بسالبٍ مبذورٍ لكلِّ قاعدةٍ من قواعدِه الأربعَ عشرةَ (`ح-7`).

**وعطبانِ حقيقيّانِ أظهرَهُما القياسُ وأُغلِقا بهجرتَينِ مُضافتَينِ** (`ح-8`):

  ١. **الاستردادُ الجزئيُّ كانَ يُغلِقُ الباقيَ إلى الأبدِ**: مبلغٌ ناقصٌ يُقبَلُ
     ويُوسَمُ الصفُّ `refunded` كاملاً، فطلبُ البقيّةِ يردُّ `already_refunded: true`.
     صارَ مرفوضاً صريحاً بـ`REFUND_PARTIAL_UNSUPPORTED`.
  ٢. **حصانةُ مرجعِ المزوّدِ كانت مثقوبةً** في الحملِ الثلاثيِّ من `confirm_payment`:
     كانَ يستبدِلُ مرجعاً محفوظاً مختلفاً بلا خطأٍ ولا سجلٍّ. صارَ يرفضُ بـ
     `PROVIDER_TRANSACTION_MISMATCH` قبلَ أيِّ كتابةٍ.

**والدليلُ مقيسٌ لا مُدَّعى**: على قاعدةٍ بلا الهجرتَينِ تسقطُ `PLC-06` و`PLC-07`
**وحدَهما** (١٠ ناجحةً · ٢ ساقطتَينِ)، وبهما ١٢ ناجحةً · ٧٩ تحقُّقاً.
(`docs/evidence/financial/F8-06-20260917.md` · `ADR 0138`)

**ومسارُ العودةِ مُعلَنٌ**: كِلا التغييرَينِ **يكسرُ نسخةً سابقةً** ومسارُه
**`forward-only` بنشرٍ مقرونٍ** — إعادةُ نشرِ صورةٍ سابقةٍ لا تُرجِعُ السلوكَ لأنَّه
في القاعدةِ لا في الصورةِ — والإجراءانِ مكتوبانِ في `docs/rollback.md`.

**وما لا يُدَّعى** (`ح-5`): **البندُ `F8-06` لا يُوسَمُ `[x]`** حتّى ثلاثِ جولاتٍ
خضراءَ متتاليةٍ على `main` تُقرأُ لكلِّ وظيفةٍ (`ح-4`). **ومحاسبةُ الاستردادِ
الجزئيِّ دَينٌ مُعلَنٌ لم يُبْنَ** (عمودُ مُستَرَدٍّ تراكميٍّ · حالٌ وسطى · مفتاحٌ
مُركَّبٌ · أثرٌ في الفاتورةِ الضريبيّةِ). **ولا يُدَّعى سلامةُ صفوفٍ ماضيةٍ**
استُرِدَّت جزئيّاً أو استُبدِلَ مرجعُها قبلَ اليومِ؛ تصحيحُها قرارُ مالٍ لا شغلُ هجرةٍ.

---

## زيادةٌ — `SEC-07`: مسارٌ مكشوفٌ غيرُ مُصنَّفٍ يُسقِطُ البناءَ (2026-09-17 · `ADR 0139`)

**الحالُ قبلَ**: حاصرُ معدَّلٍ قائمٌ ومُركَّبٌ على ويبهوكِ تيليجرامَ وموقعِ السائقِ،
**ولا مِعيارَ شمولٍ**: مسارٌ مكشوفٌ جديدٌ يُولَدُ بلا حدٍّ والبناءُ أخضرُ.

**الحالُ الآنَ**: القياسُ معكوسٌ — يُقرأُ **93 مساراً** من القرصِ بمسارِها **كما
تُخدَمُ** (بتتبُّعِ `app.route`) ثمَّ تُطابَقُ بسِجلِّ سياساتٍ مغلقٍ في التطبيقِ
(`apps/gateway/src/rate-limit/policy.ts`) لا في `scripts/` — مصدرُ حقٍّ واحدٌ يقرؤُه
المُشغِّلُ والحاجزُ معاً. **10 مساراتٍ في أصنافٍ يُوجَبُ فيها حدٌّ**: 8 محدودةٌ · 3
مُعفاةٌ بسببٍ مكتوبٍ ومالكٍ. وموضعُ توصيلِ كلِّ حدٍّ **مقروءٌ من ملفِّه** لا مُدَّعىً.

وثلاثةُ براهينَ تختلفُ في الجنسِ: حاجزٌ ساكنٌ بعشرِ قواعدَ (سالبةٌ مزروعةٌ لكلٍّ ·
`ح-7`) · إنفاذٌ **مقيسٌ على الردِّ** من موجِّهاتٍ حقيقيّةٍ (`429` + `Retry-After`) ·
ونافذةٌ **واحدةٌ بينَ نسختَينِ** على Redis حقيقيٍّ بضبطٍ موجبٍ معكوسٍ.

**وعطبانِ وُجِدا لا افتُرِضا**: (١) مساراتُ مركبةِ السائقِ كانَت تُخدَمُ على الجِذرِ
`"/"` لا على `/v1/driver/vehicle` الذي تصفُهُ الوثيقةُ والشاشةُ وحاجزُ العقدِ —
وثلاثتُها خُضْرٌ لأنَّ حاجزاً يقرأُ النصَّ لا يرى ما يُركِّبُه `app.route`، ولم يُنادِ
اختبارٌ ذاكَ الموجِّهَ قطُّ؛ (٢) `429` موقعِ السائقِ كانَ بلا رأسِ `Retry-After`.

**وما لا يُدَّعى**: رمزُ `F8-08` يبقى `[ ]` (ضابطٌ من ستّةَ عشرَ) · لا حمايةَ من
حجبٍ موزَّعٍ (عملُ حافةٍ لا عدَّادٍ) · حاصرُ Redis يفشلُ مفتوحاً (`ADR 0055`) ·
والحاصرُ الموزَّعُ شرطُهُ `SESSION_STORE=redis` (`ADR 0011`) وفي غيرِها يبقى الحدُّ
الفعليُّ ضِعفَ المُعلَنِ بعددِ النسخِ.

---

## زيادةٌ — `SEC-10`: أثرُ أمنِ الصفِّ مقيسٌ لا موصوفٌ (2026-09-17)

المقيسُ من قاعدةٍ حقيقيّةٍ بالهجراتِ مطبَّقةً: **69** جدولاً في `public`،
**68** عليها `row level security` (والباقي `spatial_ref_sys` جدولُ امتدادِ
PostGIS)، **25** سياسةً على **21** جدولاً **كلُّها** `to service_role using
(true)`، **47** جدولاً مُفعَّلاً بلا سياسةٍ (منعٌ شاملٌ لغيرِ المالكِ)،
و**`force row level security` صفرٌ على كلِّ جدولٍ**.

**فالقاعدةُ ليسَت خطَّ الدفاعِ** — الخدمةُ تتّصلُ بمالكِ الجداولِ والمالكُ يمرُّ
(`ADR 0006`، قائمٌ لا مَنسوخٌ). والذي زادَ في هذا التاريخِ أمرانِ:

1. **الأثرُ صارَ مقيساً** بدورٍ `nobypassrls` يُنشِئُه
   `tests/integration/row-security-effect.test.ts` — والصلاحيّةُ تُمنَحُ قبلَ
   القياسِ حتّى يكونَ الرفضُ رفضَ سياسةٍ لا رفضَ صلاحيّةٍ.
2. **الشرطُ الحاكمُ صارَ يُسقِطُ البناءَ** بِـ
   `scripts/check-row-security-condition.ts` بدلاً من أن يُقرأَ في وثيقةٍ بالنيّةِ.

وسِجلُّ الضوابطِ صارَ يُفرِّقُ حقلَ **دليلِ البناءِ** من حقلِ **قياسِ الفجوةِ**
(`gapMeasurement`) بقاعدتَينِ جديدتَينِ، بلا تخفيفِ القاعدةِ التي تمنعُ دليلاً على
ما لم يُبنَ. والحالُ: `SEC-10` `partial`، و`F8-08` `[ ]` باشتقاقٍ لا بيدٍ.
والحاكمُ `ADR 0140`، والدليلُ `docs/evidence/security/SEC-10-20260917.md`.


---

## زيادةٌ — `F8-06` ⇒ `[x]` بثلاثِ جولاتٍ خضراءَ مقروءةٍ بالوظيفةِ (2026-09-17 · `ح-4`)

**الحالُ قبلَ**: البندُ `F8-06` `[ ]`، والمصفوفةُ مبنيةٌ مقيسةٌ في `PR #76`
(الفرعُ `feat/f8-06-payment-lifecycle-matrix`، المدموجُ في `main`@`99c28f7`)،
وقد سُجِّلَ في `SYSTEM_STATE.md` أعلاهَ أنَّ «البندُ `F8-06` لا يُوسَمُ `[x]` حتّى
ثلاثِ جولاتٍ خضراءَ متتاليةٍ على `main` تُقرأُ لكلِّ وظيفةٍ (`ح-4`)».

**الحالُ الآنَ**: الشرطُ مُستوفىً بحرفِه. ثلاثُ جولاتٍ خضراءَ متتاليةٍ على `main`
تُقرأُ لكلِّ وظيفةٍ من وظائفِها الأربعِ، شِفرةُ `F8-06` مدموجةٌ قبلَ أُولاهنَّ:

| الجولةُ | البصمةُ | `verify` | «تكامل على PostgreSQL حقيقي» | «تكامل على Redis حقيقي» | «فوضى متعدد المثيلات (F5-06)» |
|---|---|---|---|---|---|
| `35169477174` | `99c28f7` (دمجُ `F8-06`) | ✅ | ✅ | ✅ | ✅ |
| `35172844771` | `a310be4` (دمجُ `SEC-07`) | ✅ | ✅ | ✅ | ✅ |
| `35176421723` | `4c7fbc5` (دمجُ `SEC-10`) | ✅ | ✅ | ✅ | ✅ |

كلُّ وظيفةٍ ناجحةٌ في الجولاتِ الثلاثِ بلا خطوةٍ ساقطةٍ، **ولم يُخفَّفْ الشرطُ**.
فقُلِبَ رمزُ `F8-06` في `docs/ROADMAP-MASTER.md` §12 من `[ ]` إلى `[x]`.

**وما لا يُدَّعى** (`ح-5`): لا نشرَ حيَّ (`ADR 0099`)، والاستردادُ الجزئيُّ دَينٌ
مُعلَنٌ لم يُبْنَ، ولا تُدَّعى سلامةُ صفوفٍ ماضيةٍ استُرِدَّت جزئيّاً أو استُبدِلَ
مرجعُها قبلَ اليومِ. والدعوى السابقةُ باقيةٌ بحرفِها ولم تُمحَ (`ح-8`). والبندُ
`F8-08` يبقى `[ ]` باشتقاقٍ لا بيدٍ، و`SEC-10` يبقى `partial`.

---

## تصحيحٌ مُلحَق — 2026-09-17 · سجلُّ ديونٍ من تدقيقٍ خارجيٍّ

قُوبِلَ تقريرا تدقيقٍ خارجيَّانِ (مرفقانِ من المالكِ) بقراءةٍ مباشرةٍ على `main`@`832e4a3`.
النتيجةُ في `docs/technical-debt/20260917-audit-debt-registry.md` — 22 بندًا، منها:

- **4 ديون P0** (D-01…D-04): توحيدُ مسارِ إنشاءِ الطلب، قراراتُ المالك، `numInstances: 1`، لا إثباتَ أداء.
- **6 ديون P1** (D-05…D-09, D-22): مراقبة، RLS، حداثةُ عقود، أسرار، ثغراتُ اعتماديات، لا رحلةَ على جهاز.
- **5 ديون P2** (D-10…D-14): Node.js 20، تباينُ Bun، فشلٌ غيرُ متكرِّر، migration ledger (مقصود)، JOB_INTERVALS (مقصود).
- **7 ديون P3** (D-15…D-21): scaffold، script مسمّى، مُغلَقانِ (BUG-008/BUG-002)، SYSTEM_STATE قديم، F8-09 غيرُ محدَّد، deferred.

**رُفِضَ أو صُحِّحَ من التقرير**: رسالةُ `driver-dialog` (BUG-008 `[x]`) · تكرارُ `update_id` (BUG-002 `[x]`) · فشلُ `vendored-pin-follows-bytes` (غيرُ متكرِّر) · migration ledger (تنازلٌ مقصود · ADR 0068).

**لا تغييرَ في حالاتِ البنود** · **لا ادّعاءَ إغلاقِ بند** · السجلُّ مُدخلٌ للمرجعيّةِ المستقبليّة.

---

## قاعدةُ حوكمةٍ جديدة — 2026-09-17

أُضيفت القاعدةُ 0-9 إلى `docs/MASTER_DIRECTIVE.md` §0: لا يُغلقُ دينٌ يتعلَّقُ بالمالكِ إلّا بعدَ تصريحٍ صريحٍ منهُ بالإغلاقِ أو التجاوزِ. والديونُ غيرُ المرتبطةِ بالمالكِ تُغلَقُ بالأدلّةِ الآليّةِ وحكمِ CI.

لا تغييرَ في حالاتِ البنود.

---

## D-01: توحيدُ مسارِ إنشاءِ الطلبِ — 2026-09-17

**الفرعُ:** `fix/d-01-unify-order-authority` · **من** `main`@`7a600b3`

نُفِّذَ الدينُ D-01 (غيرُ مرتبطٍ بالمالكِ): وحَّدَ مسارَ إنشاءِ الطلبِ بحيثُ يمرُّ كلُّ إنشاءٍ (بوتاً كانَ أم توصيلاً) عبرَ `RideRequestCommand.create()` الذرّيِّ الآمنِ لا الكاتبِ الأقدمِ المباشرِ.

**ما نُفِّذَ:**
- هجرةُ `request_ride()` لقبولِ وجهةٍ معدومةٍ (transport `/skip`)
- `updateId` في `IncomingUpdate` لكلِّ المتغيِّراتِ — مفتاحُ التكرارِ `telegram-update:${telegramUserId}:${updateId}`
- `createOrderAndMatch` و`requestDelivery` يُنشِئانِ عبرَ `RideRequestCommand.create()`
- `OrderCancellationPort` منفصلٌ عن `OrderWriter` — البوتُ يحملُ الإلغاءَ وحدَه لا الإنشاءَ (تغليفٌ وقتَ التشغيلِ لا TypeScript فقط)
- `ACTIVE_RIDE_EXISTS` يُعالَجُ صراحةً في مسارِ التوصيلِ (`ActiveDeliveryExistsError`) لا يُبتلَعُ كـ`STORE_ERROR`
- حاجزُ `check-ride-request-contract.ts` القاعدةُ ٤ توسَّعَتْ من «مسارُ الشبكةِ» إلى «كلُّ مسارِ إنتاجٍ»
- اختباراتٌ سلبيّةٌ للحاجزِ الموسَّعِ (ح-7)

**الفحوصُ المحليّةُ:** typecheck نجحَ · 6435 اختباراً ناجحاً (0 فشل) · lint 0 أخطاء · الحاجزُ نجحَ. الفحصُ النهائي فشلَ بسببِ اختلافِ إصدارِ Bun المحلّي `1.4.2` عن المتوقَّعِ `1.3.14`.

**ما لا يُدَّعى:** لا يُدَّعى الإغلاقُ النهائيُّ إلا بعدَ قراءةِ حكمِ GitHub CI لكلِّ وظيفةٍ بعدَ الدفعِ. ولا يُدَّعى أنَّ `ActiveRideScreen.tsx` و`check-driver-vehicle-contract.test.ts` ضمنَ نطاقِ D-01 — هما إصلاحُ lint سابقٌ.

## تحديثُ لقطةِ الحالةِ (D-19) — 2026-09-17

**الفرعُ:** `fix/d-19-d21-system-state-and-deferred-review` · **من** `main`@`a223f15`

تحديثُ لقطةِ الحالةِ بعدَ إغلاقِ 8 ديونٍ غيرِ مملوكةٍ (D-01 · D-09 · D-10 · D-11 · D-15 · D-16 · D-20 · D-21).

### العداداتُ المقيسةُ

| المقياسُ | القيمةُ |
| --- | --- |
| اختباراتٌ | 6438 (5104 ناجحةً · 1334 متجاوَزةً · 0 فاشلةً) |
| ملفّاتُ الاختباراتِ | 431 |
| تأكيداتٌ | 16558 |
| ملفّاتُ المصدرِ | 933 |
| ملفّاتُ القوالبِ الفارغةِ | 222 |
| ديونٌ مُغلَقةٌ | 8 (D-01/09/10/11/15/16/20/21) |
| ديونٌ محجوزةٌ | 1 (D-06 — ADR 0006) |
| تنازلاتٌ مُوثَّقةٌ | 1 (D-14) |

### ما لا يُدَّعى

لا يُدَّعى أنَّ هذه العداداتِ نهائيّةٌ — هي لقطةٌ في الزمنِ. والديونُ المملوكةُ (D-02 · D-03 · D-05 · D-07 · D-08 · D-22) لا تُغلقُ إلّا بتصريحٍ صريحٍ من المالكِ (القاعدةُ ح 0-9).

## تصحيحٌ ملحقٌ — D-09 · فحصُ الاعتمادياتِ كانَ يعملُ ولا يُثمِرُ دمجاً — 2026-09-17

**الفرعُ:** `chore/d-09-dependency-bumps-lockfile` · **من** `main`@`eb6685f` · **القرارُ:** [ADR 0142](./adr/0142-dependabot-does-not-maintain-a-bun-lockfile.md) · **الدليلُ:** [`docs/evidence/toolchain/D-09-20260917.md`](./evidence/toolchain/D-09-20260917.md)

**التصحيحُ بالإضافةِ لا بالمحوِ (ح-8):** اللقطةُ السابقةُ تُدرِجُ `D-09` في «ديونٍ مُغلَقةٍ» بوجودِ `.github/dependabot.yml`. والوجودُ ثابتٌ ولا يُنكَرُ، **لكنَّه لم يكن إثباتاً إنتاجيًّا**: Dependabot في نظامِه البيئيِّ `npm` لا يُحدِّثُ `bun.lock`، ووظيفةُ `verify` تبدأُ بـ`bun install --frozen-lockfile`، فكانَ كلُّ طلبٍ يفتحُه **يُولَدُ أحمرَ بالبنيةِ** ويسقطُ قبلَ أن يبلغَ حاجزاً واحداً. الحالةُ الصحيحةُ: **مُغلَقٌ جزئيًّا** — الفحصُ قائمٌ، وقناةُ الدمجِ صارَت مُعالَجةً آليًّا في هذه الوحدةِ، وفحصُ الصورِ العميقُ (Trivy/Snyk) ما زالَ مفتوحاً.

### ما تغيَّرَ في الحالةِ

| العنصرُ | قبلَ | بعدَ |
| --- | --- | --- |
| `typescript` (الجذرُ + miniapp) | `^5.6.0` | `^7.0.2` |
| `baseUrl` في `tsconfig` (الجذرُ + miniapp) | مُعلَنٌ | محذوفٌ (مُزالٌ في TS 7) · المساراتُ نسبيّةٌ |
| `types` في `apps/miniapp/tsconfig.json` | ضمنيٌّ من `node_modules` الأجدادِ | `["bun"]` صريحاً |
| قفلُ فروعِ `dependabot/**` | يبقى قديماً فيسقطُ التجميدُ | يُعادُ توليدُه آليًّا ويُتحقَّقُ منه ثمَّ يُدفَعُ ويُستدعى CI |
| زنادُ `ci.yml` | `push` + `pull_request` | `+ workflow_dispatch` (لأنَّ دفعةَ `GITHUB_TOKEN` لا تُشعِلُ زناداً) |
| إصداراتُ `vite`/`@vitejs/plugin-react` الرئيسةُ | مفتوحةٌ لـDependabot | `ignore` بقرارٍ مكتوبٍ + بندُ خارطةٍ `D-23` |
| سجلُّ الديونِ | D-01…D-22 | + صفُّ إضافةٍ لـD-09 · + `D-23` مُكتشَفٌ |

### العداداتُ المقيسةُ (محلّيًّا · Bun 1.4.2)

| المقياسُ | القيمةُ |
| --- | --- |
| `bun run typecheck` | `0` |
| `bun run lint` | `0` (28 تحذيراً قائماً سلفاً · لا خطأَ) |
| `bun run build:miniapp` | `0` |
| ميزانيةُ الأداءِ (القسم 9.9) | `0` — الحملُ الأوّلُ 76.3 KB / 180 · 6 طلباتٍ / 6 · 3 حزمٍ مؤجَّلةٍ |
| `bun test` | 6438 اختباراً (5104 ناجحةً · 1334 متجاوَزةً · **0 فاشلةً**) · 16562 تحقُّقاً · 431 ملفّاً · 36.64s · خروجٌ `0` |
| فرقُ `bun.lock` | 43 إضافةً · 3 حذوفاً (ثنائيّاتُ `@typescript/typescript-*` لكلِّ منصّةٍ) |

### ما لا يُدَّعى

- **لا يُدَّعى أنَّ الأخضرَ المحلّيَّ حكمٌ.** الحكمُ حكمُ CI لكلِّ وظيفةٍ بعدَ الدفعِ (`verify` · PostgreSQL · Redis · F5-06 · roadmap).
- **لا يُدَّعى أنَّ سيرَ مزامنةِ القفلِ مُتحقَّقٌ منه من طرفٍ إلى طرفٍ.** لا يُشعَلُ إلّا بدفعةٍ من `dependabot[bot]`، وأوّلُ مناسبةٍ الدورةُ الأسبوعيّةُ القادمةُ؛ وحكمُها يُلحَقُ بملفِّ الدليلِ بالإضافةِ.
- **لا يُدَّعى أنَّ `D-09` مُغلَقٌ** ولا أنَّ `D-23` مُنفَّذٌ — الثاني مُقاسٌ ومؤجَّلٌ بقرارٍ مكتوبٍ.
- **لا يُدَّعى أنَّ ترقيةَ TS 7 لا تُخفي انحداراً** غيرَ ما تكشفُه البوّاباتُ القائمةُ.

## تصحيحٌ مُضافٌ — تكافؤُ إنفاذِ الحواجزِ (2026-09-17 · `ADR 0143`)

**الحالةُ قبلَ هذا التاريخِ:** سبعةُ حواجزَ كانَت في `package.json → ci`
**ولا خطوةَ لها في أيِّ سيرِ عملٍ** — `check-deferred-area` ·
`check-driver-subscription-contract` · `check-driver-vehicle-contract` ·
`check-erasure-policy` · `check-instance-invariant` · `check-retention-policy` ·
`check-rollback-safety`. فلم تكن تدخلُ حكمَ CI الذي تشترطُه القاعدةُ `0-7`، وكلُّ
دعوى «حاجزٌ قائمٌ» عنها في وثيقةٍ **كانَت أوسعَ من الحقيقةِ**. وثلاثةُ حواجزَ في
المقابلِ كانَت في سيرِ العملِ ولا تُشغَّلُ محلّيًّا، فالأخضرُ المحلّيُّ وعدٌ ناقصٌ.

**الحالةُ بعدَه:** السبعةُ خطواتٌ مُسمّاةٌ في وظيفةِ `verify`، والثلاثةُ في
المشغِّلِ المحلّيِّ، والتكافؤُ نفسُه مفروضٌ بحاجزٍ آليٍّ
(`scripts/check-guard-enforcement.ts`) يُنادى في الجانبَينِ ولا يُعفي نفسَه،
ومصدرُ حقيقتِه المستودعُ لا قائمةٌ تُصانُ يدويًّا. والإعفاءاتُ تسعةٌ، كلُّها
**تُنفَّذُ بعيداً** والإعفاءُ عن الجانبِ المحلّيِّ وحدَه لاستحالةِ البيئةِ.

**ما بقيَ:** الأخضرُ ههنا محلّيٌّ حتّى يُقرأَ حكمُ CI بعدَ الدفعِ، ولا بندَ يُقلَبُ
إلى `[x]` قبلَ `ح-4`. الدليلُ: `docs/evidence/repo/GUARD-ENFORCEMENT-20260917.md`.

## تصحيحٌ مُضافٌ — 2026-09-17 · العضويّةُ ليست صلاحيّةً (`D-24`)

سلسلةُ `package.json → ci` كانَ فيها أمرٌ **يُخفِقُ في كلِّ نداءٍ محلّيٍّ**
(`check-no-skipped-tests.ts` بلا وسيطِ سجلٍّ) وهوَ أخضرُ في CI، لأنَّ CI لا
تُشغِّلُ السلسلةَ ولأنَّ حاجزَ `ADR 0143` يقيسُ العضويّةَ لا الصلاحيّةَ.

- **الحالةُ الآنَ:** الأمرُ أُخرِجَ من السلسلةِ إلى إعفاءٍ منطوقِ السببِ، وقامَ
  حاجزٌ ساكنٌ (`scripts/check-runner-command-validity.ts` · `ADR 0144`) يمنعُ
  عودةَ الصنفِ نفسِه: 98 أمراً مقروءاً · 0 مخالفةٍ · 13 حالةَ اختبارٍ سالبةً.
- **وما يبقى دَيناً مُعلَناً (`D-24` `[~]`):** الفحصُ ساكنٌ يُثبِتُ **النداءَ** لا
  **النجاحَ**، وسلسلةُ `bun run ci` لا تُشغَّلُ في CI بقرارٍ مكتوبٍ
  (`ADR 0144 §٤`: تكرارُ مصدرِ الحقيقةِ)، وسكربتٌ يشترطُ وسيطاً بلا سطرِ
  استعمالٍ مطبوعٍ لا يراهُ الحاجزُ.

## تصحيحٌ مُضافٌ — 2026-09-18 · الطارئُ لا يشترطُ رحلةً (`F12-03`)

**الحالةُ قبلَ هذا التاريخِ:** بطاقةُ الاستغاثةِ في المِنِصَّةِ الصغيرةِ كانت
**تُخفي نفسَها** لكلِّ حسابٍ لا طلبَ قائماً له ولا طلبَ انتهى داخلَ نافذةِ ما بعدَ
الرحلةِ (`sos_surface_state` يُرجِعُ `origin = null`)، و`trigger_sos` تردُّ
`NO_ACTIVE_ORDER`. أي أنَّ الطارئَ كانَ **مشروطاً برحلةٍ**. وكانَ
`safety_incidents.order_id` `not null` بحكمِ المخطَّطِ، فالحالُ لم تكن قابلةً
للتقييدِ أصلاً.

**الحالةُ بعدَه:**

- `safety_incidents.order_id` يقبلُ `null` (**توسيعٌ**: كلُّ صفٍّ قائمٍ يبقى
  صالحاً)، وفهرسٌ جزئيٌّ يخدمُ مسارَ «بلا طلبٍ».
- أصلٌ ثالثٌ `NO_ORDER` في `trigger_sos` و`sos_surface_state` **معاً حرفاً**،
  مدينتُه `users.city_id` (عمودٌ قائمٌ `not null` — القاعدةُ `0-4`)، وله مفتاحُ
  قفلٍ استشاريٍّ ونطاقُ منعِ تكرارٍ **مستقلّانِ** عن بلاغِ الرحلةِ.
- إفصاحٌ يُنشَرُ **قبلَ** الضغطِ: لا مرجعَ رحلةٍ (`SOS_NO_ORDER_REFERENCE`) · لا
  موقعَ للراكبِ (`SOS_NO_LOCATION_AVAILABLE`) · فريقُ **مدينةِ الحسابِ** صراحةً
  (`SOS_NOTIFIES_ACCOUNT_CITY_TEAM`) · ولا مكالمةَ (`SOS_NO_PHONE_CALL`).
- **عطبٌ كامنٌ أُصلِحَ**: `claim_safety_incident_delivery` كانت تَسِمُ صفَّ
  الصندوقِ `sending` ثمَّ تقرأُه بـ`join orders`، فصفٌّ بلا طلبٍ يعلَقُ `sending`
  **أبداً** — بلاغُ استغاثةٍ يُدفَنُ بصمتٍ. صارَ `left join orders`، وقاعدةٌ
  سابعةٌ في حاجزِ عقدِ السطحِ تمنعُ عودتَه، والحاجزُ صارَ يحكمُ كلَّ ملفِّ هجرةٍ
  في نفسِه.

**ما بقيَ ولا يُدَّعى:** **لا رابطَ تقنيّاً بمركزِ البلاغاتِ الموحَّدِ** بوزارةِ
الداخليّةِ — وذاكَ شقُّ `F12-01`/`F12-02` بيدِ المالكِ (`[!]` · القاعدةُ `0-9`)،
**ولا مكالمةَ تُجرى نيابةً عن أحدٍ**، ولا إثباتَ إنتاجيّاً؛ فالبندُ `F12-03` يبقى
`[~]` لا `[x]`، و`ح-4` تشترطُ ثلاثةَ أشواطٍ خضراءَ متتاليةً على `main`. الدليلُ:
`docs/evidence/architecture/F12-03-20260918.md` · القرارُ: `ADR 0145`.


## تصحيحٌ مُضافٌ — 2026-09-18 · حياةُ رابطِ المشاركةِ حكمٌ يُحسَبُ (`F12-04`)

**الحالةُ قبلَ هذا التاريخِ:** حياةُ رابطِ تتبّعِ الرحلةِ كانت **رايةً مخزَّنةً**:
`issue_tracking_token` تكتبُ `expires_at = now() + tracking_link_max_lifetime_minutes`
(سقفٌ أعمى · 720 دقيقةً بذرةً)، و**الوظيفةُ الدوريّةُ** `expire_tracking_tokens`
هيَ التي تسحبُه إلى «نهايةِ الرحلةِ + `tracking_link_grace_minutes`»، والقارئانِ
(`get_tracking_position` · `rider_ride_share_state`) يحكمانِ بـ`expires_at > now()`
وحدَه. فحياةُ الرابطِ كانت **رهنَ وظيفةٍ تدورُ**: تأخُّرُها يعني رابطاً ينشرُ موقعَ
السائقِ إلى اثنتَي عشرةَ ساعةً بعدَ نهايةِ الرحلةِ، والعدُّ المنشورُ للمالكةِ
يُطرَحُ من السقفِ لا من الموعدِ.

**الحالةُ بعدَه:**

- `tracking_link_lifetime(uuid)` هوَ **الحَكَمُ الواحدُ**، ويُحسَبُ لحظةَ القراءةِ:
  `LIVE_RIDE_ACTIVE` (حيٌّ · **لا عدَّ تنازليّاً ألبتّةَ**) · `LIVE_GRACE` (عدٌّ نحوَ
  «نهايةِ الرحلةِ + المهلةِ») · `EXPIRED_RIDE_ENDED` (ميّتٌ **بلا حاجةٍ إلى وظيفةٍ
  تدورُ**). ونهايةُ الرحلةِ `coalesce(completed_at, updated_at)` حرفاً واحداً
  (`0-6`)، والمهلةُ من `platform_settings` بمصدرٍ مُعلَنٍ (`0-3`).
- `tracking_link_token_order(text)` تُحوِّلُ رمزاً إلى طلبٍ بحكمِ الحَكَمِ، وقارئا
  الرابطِ أُعيدَ تعريفُهما فوقَهما **بلا مسِّ توقيعٍ**، وحمولةُ الصفحةِ العامّةِ
  ثلاثةُ مفاتيحَ لا رابعَ كما كانت.
- **`expires_at` صارَ سقفَ سلامةٍ لا موعداً**، ويُنشَرُ للمالكةِ باسمِه
  `ceiling_seconds_remaining` (والحقلُ القديمُ `seconds_remaining` رُفِعَ من حمولةِ
  الرابطِ). والوظيفةُ الدوريّةُ باقيةٌ **تقارُباً وتنظيفاً لا مصدرَ حقيقةٍ**: إن لم
  تدُرْ قطُّ فالحكمُ لا يتغيَّرُ.
- **الإنفاذُ آليٌّ**: قواعدُ عقدِ المشاركةِ صارت عشراً — كلُّ دالّةٍ تقرأُ
  `expires_at` لتحكمَ بحياةٍ يلزمُها المرورُ بالحَكَمِ (ويُستثنى **باسمِه** كاتبُ
  السقفِ وحدَه)، ومهلةُ الشِّفرةِ ومهلةُ البذرةِ رقمٌ واحدٌ. والقارئُ في الشِّفرةِ
  يرفضُ حمولةً غيرَ متماسكةٍ بـ`STORE_ERROR`.

**ما بقيَ ولا يُدَّعى:** **السقفُ ما زالَ يقتلُ رحلةً جاريةً جاوزَتْه** (720 دقيقةً
بذرةً) وذاكَ خِلافُ حرفِ البندِ **ولا يُدَّعى إغلاقُه**: إسقاطُه بلا كاشفِ حالاتٍ
عالقةٍ يعني رابطاً بلا نهايةٍ لرحلةٍ عَلِقَت على `in_progress` — فالسقفُ **حدُّ
سلامةٍ مُعلَنٌ** يُقرأُ في الحمولةِ، وإغلاقُ الوجهِ بندٌ آخرُ. **ولا إرسالَ من
الخادمِ** برسالةٍ نصّيّةٍ أو واتساب، ولا إنسانٌ فتحَ رابطاً، ولا نشرَ إنتاجيّاً،
ولا قياسَ لسهولةِ الوصولِ؛ فالبندُ `F12-04` يبقى `[~]` لا `[x]`، و`ح-4` تشترطُ
ثلاثةَ أشواطٍ خضراءَ متتاليةً على `main`. الدليلُ:
`docs/evidence/architecture/F12-04-20260918.md` · القرارُ: `ADR 0146`.

## تصحيحٌ مُضافٌ — 2026-09-18 · آليّةُ بلاغِ المفقودِ (`F12-07`)

**الحالةُ قبلَ هذا التاريخِ:** تذكرةُ `lost_item` كانت تُكتَبُ في `support_tickets`
وينتهي الأثرُ هناك — فالسائقُ الذي في سيّارتِهِ المفقودُ لا يعرفُ أن يفتّشَ،
والمعلومةُ الوحيدةُ التي لها قيمةٌ زمنيّةٌ («فتِّشْ سيّارتَك الآنَ») تبقى في قائمةٍ
تُقرأُ متى قُرئَت. ولا إشعارَ موجَّهٌ إلى السائقِ في صندوقِ الصادرِ.

**الحالةُ بعدَه:**

- `open_support_ticket` يُعيدُ تعريفَ نفسِه بفرعٍ واحدٍ للنوعِ `lost_item`: بعدَ كتابةِ
  التذكرةِ وسجلِّ التدقيقِ — وفي المعاملةِ نفسِها — يُودِعُ صفَّ `lost_item_report`
  في `notification_outbox` موجَّهاً إلى السائقِ المُسنَدِ **فقط** حينَ
  `p_type = 'lost_item'` و`assigned_driver_id is not null` و`status = 'completed'`.
- **عقدُ `F3-08` محفوظٌ** (`ح-8`): تذكرةُ المفقودِ تُفتَحُ بلا طلبٍ كما كانت،
  وإنَّما الإيداعُ مشروطٌ لا مردودٌ — لا يُودَعُ البلاغُ إلّا حينَ تتحقَّقُ الشروطُ
  الأربعةُ (طلبٌ مملوكٌ · سائقٌ مُسنَدٌ · رحلةٌ منتهيةٌ).
- نوعُ `lost_item_report` (رتبةُ `medium` · قناةُ `critical`) في القيدِ والسياسةِ
  والقائمةِ والرتبةِ، والحاجزُ `check-notification-classification` **مُوسِّعٌ**
  ليقرأَ بذرَ السياسةِ من **كلِّ** هجرةٍ لا من `F6-05` وحدَها.
- **الإغناءُ حيٌّ**: محادثةُ السائقِ ولغتُه تُقرآنِ من القاعدةِ لحظةَ الالتقاطِ لا من
  الحمولةِ، والصفُّ بلا سائقٍ يُهجَرُ ولا يُعطِّلُ غيرَه.
- هجرتانِ: `20260918030000` (طورُ `contract`: drop+add `not valid` مجموعةٌ فائقةٌ +
  بذرُ السياسةِ + استبدالُ أربعِ دوالَّ) و`20260918030100` (طورُ `validate`:
  `validate constraint`).
- **حاجزٌ جديدٌ**: `scripts/check-lost-item-mechanism.ts` في سلسلةِ `ci` وفي خطوةٍ
  مُسمّاةٍ في `verify`.

**ما بقيَ ولا يُدَّعى:** لا آلةَ حالةٍ للتسليمِ ولا جدولَ مفقوداتٍ ولا شاشةَ تطبيقٍ ولا
يُدَّعى ردُّ مفقودٍ — فالبندُ `F12-07` يبقى `[~]` لا `[x]`، و`ح-4` تشترطُ ثلاثةَ
أشواطٍ خضراءَ متتاليةً على `main`. الدليلُ: `docs/evidence/architecture/F12-07-20260918.md`
· لا ADR لهذا البندِ.

## تصحيحٌ مُضافٌ — 2026-09-18 · التقييمُ المتبادلُ ومنظورُ السائقِ (`F12-05`)

`completed_ride_summary` تمتدُّ لتُحسِمَ المنظورَ من علاقةِ الطلبِ لا «راكبٌ ثمّ سائق»: السائقُ
يرى بطاقةَ راكبٍ ومعرّفَ الاتّجاه `driver_to_rider`، والراكبُ يرى `rider: null, direction:
"rider_to_driver"` فيوازي عقدَ `F2-07` بالكامل. ودالة `submit_rating_with_tags` تُحصي
الاتّجاهينِ عبر `ratings_one_per_order_direction` الموجودِ منذُ `20260807170000`.

وللسائقِ شاشةُ ملخَّصٍ (`apps/miniapp/src/surfaces/driver/summary/`) ونجومٌ وملاحظةٌ بلا وسوم،
موصِّلةٌ إلى `DriverRoot` بعدَ `COMPLETE_RIDE`.

والشطرُ الأوّلُ («إظهارُ بياناتِ الرحلةِ والسائقِ والسيارةِ طوالَ الرحلة») يُثبَتُ بعقدِ الرحلةِ
النشطةِ (`F2-06`) وحاجِزِ `check-active-ride-contract.ts` الذي يتحقَّقُ من `assigned_driver_id
is not null` وبطاقةِ السائقِ (الاسم، `vehicle_type`، `plate_number`، `rating_average`،
`rating_count`) في لقطةِ `20260913235000`.

**ما قِيسَ:** 8 اختباراتِ تكاملٍ على قاعدةِ `Supabase` + سلسلةُ `bun run ci` كاملةً خضراءُ.

**ما بقيَ ولا يُدَّعى:** البندُ `F12-05` يبقى `[~]` لا `[x]`، و`ح-4` تشترطُ ثلاثةَ أشواطٍ
خضراءَ متتاليةً على `main`. الدليلُ: `docs/evidence/architecture/F12-05-20260918.md` · لا
ADR لهذا البندِ.

## تصحيحٌ مُضافٌ — 2026-09-18 · إصلاحُ عطبِ `completed_ride_summary` (55000) (`F12-05`)

### العطبُ

هجرةُ `20260918040000_f12_05_mutual_rating_driver_summary.sql` أعادتْ تعريفَ
`completed_ride_summary` لتُحسِمَ المنظورَ من علاقةِ الطلبِ لا «راكبٌ ثمّ سائق»،
ولكنَّها **تركت مساراً تُلقِي فيه القاعدةُ خطأً `55000`** (`record "v_order" is not
assigned yet`): المتغيِّرُ `v_order` مُعرَّفٌ كـ`record` ولا يُسنَدُ إلّا داخلَ
فروعٍ مشروطةٍ، فحينَ يكونُ المستخدمُ له صفٌّ في `users` (سُجِّل بدورٍ ما) ولا صفَّ
له في `riders` ولا في `drivers`، لا يُنفَّذُ أيُّ `select ... into v_order`،
ويبقى `v_order` **غيرَ مُسنَدٍ**، ثمَّ يصلُ التنفيذُ إلى `if v_order.id is null`
فيُطلِقُ `55000`.

### ما كشفَهُ الاختبارُ

`tests/integration/ride-summary.test.ts` الحالةُ ٤ — «مستخدمٌ غيرُ موجودٍ وراكبٌ
غيرُ مسجَّلٍ: رمزانِ متمايزانِ» — التي تُعِدُّ مستخدِماً في `users` (بدورِ `rider`)
بلا صفٍّ في `riders` ولا `drivers`، فتتوقَّعُ `RIDER_NOT_REGISTERED`. القاعدةُ كانت
تُطلِقُ `55000` بدلَه.

### الجذرُ لا العَرَضُ

الإصلاحُ **ليسَ** تهيئةَ `v_order.id := null` — فالسجلَّ `record` لا يأخذُ شكلَهُ قبلَ
الإسنادِ الأوّلِ، والتهيئةُ اليدويّةُ هشَّةٌ. بل **إعادةُ هيكلةٍ**:

1. **هويّتا المجالِ تُقرآنِ معاً أوّلاً** (`v_rider_id` و`v_driver_id`) قبلَ أيِّ
   مساسٍ بـ`v_order`.
2. **حارسٌ مبكِّرٌ**: `if v_rider_id is null and v_driver_id is null then return
   'RIDER_NOT_REGISTERED'` — قبلَ أيِّ `select ... into v_order`.
3. **عَلَمُ وُجودٍ** (`v_order_found boolean`) يُستَنَدُ إلى `found` فورَ كلِّ
   استعلامٍ، بدلَ `v_order.id is null` على سجلٍّ لم يُسنَد قط.
4. لا يُلمَسُ أيُّ حقلٍ من `v_order` قبلَ `if not v_order_found then return
   'ORDER_NOT_FOUND'`.

### القيدُ الحاسمُ

`users.role` ليسَ مصدرَ الحقيقةِ — التسجيلُ الفعليُّ هو وجودُ صفٍّ في `riders` أو
`drivers`. فحالةُ «مستخدمٌ بلا صفِّ راكبٍ ولا سائقٍ» تُغلَقُ مبكِّراً بـ
`RIDER_NOT_REGISTERED`، لا تُترَكُ لتصِلَ إلى `v_order`.

### ما قِيسَ

- `tests/integration/ride-summary.test.ts` · 31 اختباراً — **كلُّها خضراءُ** (محلِّيّاً
  على PostgreSQL 18 + PostGIS 3.6) بما فيها الحالةُ ٤ المُصلَحة.
- `tests/integration/mutual-rating.test.ts` · 8 اختباراتٍ — **كلُّها خضراءُ** في
  العزلِ.
- `tests/integration/object-level-authorization.test.ts` · 5 اختباراتٍ — **كلُّها
  خضراءُ** في العزلِ.
- `bun run lint` و`bun run typecheck` — **أخضرانِ**.

### ما **لم** يُقَسْ ولا يُدَّعى (`ح-٥`)

- **لا `[x]`**: `ح-٤` تشترطُ ثلاثةَ أشواطٍ خضراءَ متتاليةً على `main`. هذا التصحيحُ
  أوّلُها.
- فشلُ وظيفةِ `F5-06` (الفوضى متعددةُ المثيلاتِ) في الشوطِ السابقِ كانَ **بنيةًّ** —
  `server closed the connection unexpectedly` على المنفذِ `5433` — لا علاقةَ له
  بهذا الإصلاحِ ولا يُعالَجُ ههنا.

## ملحقٌ 2026-09-20 — `DEC-16`: قرارُ المالكِ — الاكتفاءُ بالتجميعاتِ المادِّيّةِ (إضافةٌ لا محوٌ · `ح-8`)

**حُسِمَ `DEC-16` بقرارِ مالكٍ صريحٍ: الخيارُ (ب) — الاكتفاءُ بالتجميعاتِ المادِّيّةِ.** لا نسخةَ قارئةً ولا وجهةً تحليليّةً منفصلةً. التجميعُ المادِّيُّ الموجودُ (`admin_metric_snapshots` + `refresh_admin_metric_snapshots`) هو الحلُّ النهائيُّ لا المؤقَّتُ. الساقُ الثالثةُ من `F7-08` مُسقَطةٌ بقرارِ المالكِ.

**وقُلِبَت ثلاثةُ بنودٍ:**
- `F7-08`: `[~]` → `[x]` — الساقانِ الأوليانِ مبنيّتانِ ومحروستانِ (`ADR 0128`)، والساقُ الثالثةُ مُسقَطةٌ، وثلاثُ جولاتٍ خضراءَ متتاليةٍ على `main` مُكتملةٌ منذ 2026-09-16
- `F7-05`: `[ ]` → `[x]` — البندُ كلُّه عن النسخِ القارئةِ والمالكُ قرَّرَ عدمَ بنائِها
- `CAP-011`: `[ ]` → `[x]` — الساقانِ المبنيّتانِ + الساقُ المسقطةُ = القدرةُ مُغلَقةٌ

**وما لا يُدَّعى** (`ح-5`): لا `مَقيس` ولا `مُثبَت` — لا قياسَ حملٍ إنتاجيٍّ، والكتابةُ تظلُّ تقرأُ الجداولَ الحارّةَ كلَّ ٦٠ ثانيةً (حملٌ ثابتٌ مقبولٌ بقرارِ المالكِ). ولا يُدَّعى أنَّ التجميعَ المادِّيَّ يُغني عن النسخةِ في كلِّ حجمٍ — القرارُ للمُشهدِ الحاليِّ، وإذا نما الحملُ فوقَ ما يحتملُهُ التجميعُ يُعادُ فتحُ القرارِ.

**المراجع:** `ADR-0155` · `docs/decisions/DEC-16-analytical-replica-aggregates-only-20260920.md` · `docs/evidence/architecture/DEC-16-20260920.md`

**وما كُتِبَ سابقاً عن `DEC-16` مفتوحاً يبقى محفوظاً بزيادةٍ لا ممحوّاً** (`ح-8`) — كانَ وصفَ حالةٍ صادقةٍ وقتَها، والقرارُ يُضافُ إليهِ لا يُلغي ما قبله.

## ملحقٌ 2026-09-20 — `DEC-11`: تصحيحٌ توثيقيٌّ (إضافةٌ لا محوٌ · `ح-8`)

**`DEC-11` كانَ مُقرَّراً في 2026-09-17** بملفِّ قرارٍ كاملٍ (`docs/decisions/DEC-11-20260917-owner-fare-policy.md`)، ولكنَّ الخارطةَ لم تُحدَّث. فأُغلِقَ `[!]` → `[x]`.

القرارُ: الأجرةُ تقديرٌ لا وساطةَ نقدٍ، الأسعارُ 150/150/300 ريالاً، بوابةُ الدفعِ مؤجَّلةٌ حتى ١٠ أيامٍ قبلَ نهايةِ الشهرِ الأولِ. **ولا قلبَ بنودٍ تابعةٍ**: `F2-04` و`F2-07` و`F2-08` و`F3-05` تبقى بحالِها — `DEC-11` قرَّرَ التجميدَ لا الفتحَ.

كذلك أُضيفت ملاحظةٌ توضيحيّةٌ إلى `docs/decisions/DEC-16-20260917-secrets-deferral.md` لتمييزِه عن `DEC-16` في جدولِ الخارطةِ (النسخةُ التحليليّةُ) — تشابهُ الترقيمِ صدفةٌ.
<!-- PD-051: جولةُ CI الثالثة الخضراءُ — 2026-09-20 -->
