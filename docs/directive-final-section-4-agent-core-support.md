# تقرير القسم 4 (التوجيه الختامي) — agent-core في الدعم فقط

> **تنبيه ترقيم:** `docs/section-4-report.md` يعود إلى توجيه سابق (مراجعة تجربة
> البوت) ولم يُمسّ. جدول المطابقة في `docs/directive-final-report.md`.

**التاريخ:** 2026-08-10
**الملفات:** `tests/unit/agent-core-scope-confinement.test.ts` (**6/6**) ·
`tests/integration/admin-disputes-advice.test.ts` (**5/5**) · اختبار وحدة إضافي في
`tests/unit/admin-dashboard.test.ts`

---

## 1) المطلوب مقابل المُنجَز

| المطلوب (§4.1) | الحالة |
|---|---|
| التأكد أن مفتاح التفعيل يعمل | ✅ مُثبَت |
| التأكد أن تذكرة دعم حقيقية تُنتج اقتراحاً | ✅ مُثبَت |
| **التأكد أن الاقتراح يظهر في لوحة الإدارة في المكان الصحيح** | ⚠️ **كان لا يظهر — ثغرة حقيقية سُدَّت** |
| التأكد أن التعطيل الكامل لا يكسر النظام | ✅ مُثبَت |

| الممنوع (§4.1) | الحالة |
|---|---|
| توسيع الاستخدام إلى أي حدث آخر | ✅ لم يحدث — حدث واحد فقط |
| منح agent-core صلاحية تنفيذ | ✅ لم يحدث — `SUGGEST` سقفاً |
| كسر العزل المعماري | ✅ لم يحدث — البوّابة تمرّ |
| كسر `check-agent-core-isolation` | ✅ تمرّ |

---

## 2) حصر النطاق المُثبَت — 6 اختبارات

فُحص الحصر بالكود لا بالثقة:

| الحقيقة | الدليل |
|---|---|
| **مُستدعٍ واحد فقط** | `apps/gateway/src/agent-advisor.ts` → `core.process` — لا ثانيَ له في المستودع |
| **نوع حدث واحد فقط** | `support_ticket_opened` |
| **وكيل واحد** | السجلّ (registry) يُثبّت وكيلاً واحداً بلا توسّع ديناميكي |
| **سقف الأدوات** | `MAX_ALLOWED_TOOL_LEVEL = "SUGGEST"` — لا تنفيذ |
| **معطَّل افتراضياً** | `DEFAULT_CONFIG.enabled = false` |
| **مفتاح التفعيل** | `AGENT_CORE_ENABLED` — النصّ `"true"` حرفياً وحده يُفعّل |
| **التعطيل لا يُسقط النظام** | `loadConfig()` لا يرمي استثناءً أبداً |

أنواع أحداث محجوزة **وغير موصولة** بأي مسار: `operational_anomaly_detected`،
`fraud_signal_raised`، `growth_opportunity_spotted`. وجودها في النوع لا يعني
وجودها في التنفيذ — وهذا مُثبَت بالاختبار لا مُفترَض.

**مبدأ «اقترح لا تنفّذ» قائم:** المخرَج صفٌّ في `agent_decisions` ونصٌّ يُعرض.
لا مسار تنفيذ واحد.

---

## 3) الثغرة الحقيقية: الاقتراح لم يكن يظهر في اللوحة

### السبب

كان الاقتراح يُنشَر إلى قروب الدعم على تلغرام **فقط**. صفّ `agent_decisions`
يُكتب في القاعدة، لكن لوحة الإدارة لم تكن تقرأه ولا تعرضه في أي مكان. بينما
§4.1 تشترط صراحةً: «تأكد أن الاقتراح **يظهر في لوحة الإدارة في المكان الصحيح**».

### الأثر

المسؤول الذي يعمل من اللوحة لا يرى رأي الطبقة إطلاقاً. القيمة كلها كانت محبوسة
في قروب تلغرام.

### الإصلاح — للقراءة فقط

| الملف | التغيير |
|---|---|
| `apps/gateway/src/admin/queries.ts` | في `listDisputes()`: `left join lateral` على `agent_decisions` (اسم مستعار `dec`)، مُرشَّح بـ `published`، مرتَّب `created_at desc limit 1` |
| `apps/admin-dashboard/src/pages/disputes.ts` | حقول `agentSuggestion` / `agentClassification` / `agentConfidence` في `DisputeRow`، ودالة `renderAdvice()`، وعمود «اقتراح الطبقة» |

**ما يجعل هذا الإصلاح متوافقاً مع §4.1 حرفاً:**
- لا `import` من `agent-core` في لوحة الإدارة — العزل المعماري لم يُكسر،
  و`check-agent-core-isolation` تمرّ.
- قراءة فقط: `left join` على جدول قائم. لا مسار تنفيذ، ولا كتابة، ولا رفع
  للصلاحية. مبدأ «اقترح لا تنفّذ» لم يُمسّ.
- `left join` لا `inner`: تذكرة بلا اقتراح تبقى ظاهرة كما كانت. لا انحدار.
- الاقتراح `published` وحده يُعرض — لا مسوّدات.
- المخرَج يمرّ عبر `escapeHtml` كسائر نصوص اللوحة.

### إعادة التحقّق

`tests/integration/admin-disputes-advice.test.ts` — **5/5** على قاعدة حقيقية:
اقتراح منشور يظهر · اقتراح غير منشور لا يظهر · تذكرة بلا اقتراح تبقى ظاهرة ·
الأحدث وحده يُعرض عند تعدّد الاقتراحات · المخرَج مُهرَّب.
وأُضيف اختبار وحدة في `admin-dashboard.test.ts` لعرض الحقول الجديدة.

---

## 4) أخطاء ارتُكبت أثناء الإصلاح (لا عيوب في المشروع)

| الخطأ | السبب | الإصلاح |
|---|---|---|
| القالب النصّي انكسر | علامات خلفية داخل تعليق SQL أنهت القالب | أُزيلت من التعليق العربي |
| تصادم أسماء مستعارة | `d` مربوط أصلاً بوصلة `drivers` | الاسم المستعار الجديد `dec` |
| نوع خاطئ | `occurredAt` نصّ لا `Date` في `EventPayload` | `.toISOString()` |
| ترتيب الاستيراد | قاعدة Biome | أُعيد الترتيب |
| اختبار قائم انكسر | كائن `DisputeRow` ينقصه 3 حقول جديدة | أُضيفت |

اكتُشف أثناء العمل أن `support_ticket_type` يقبل `subscription` و`ride_dispute`
**فقط** (لا `complaint`)، وأن `support_ticket_status` يقبل `open` و`claimed`
و`resolved` و`rejected`.

---

## 5) الحالة النهائية

`check-agent-core-isolation` **PASS** · agent-core معطَّل افتراضياً ومحصور في
حدث واحد ومُستدعٍ واحد وسقف `SUGGEST` · والاقتراح يظهر الآن في اللوحة في مكانه
الصحيح، مُثبَتاً من طرف إلى طرف.
